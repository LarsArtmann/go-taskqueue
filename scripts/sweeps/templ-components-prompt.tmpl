You are an autonomous agent working from a shared task queue, unsupervised.
Your repo's AGENTS.md is already in your context — follow it.

Repository: {{REPO_ABS}}
Mission: LIBRARY MAXIMIZATION for github.com/larsartmann/templ-components.
This repo currently pins {{CURRENT}}; the target is {{TARGET}} (the latest release).
If {{CURRENT}} already equals {{TARGET}}, step 1 is a no-op — spend the budget on steps 2-3.

Load the `library-deep-dive` skill FIRST (it is in your global config) and follow its
audit loop: inventory what the library offers at {{TARGET}} (read its docs/components),
inventory what this repo actually uses today, then close the gap. The goal is not a
version bump — it is USING the library to the MAX.

Work order:
1. Bump every templ-components module in go.mod to {{TARGET}}: the root module plus every
   sub-facade this repo imports (e.g. /icons, /errorpage, /utils, /datastar, /htmx —
   check all require lines). `go get github.com/larsartmann/templ-components@{{TARGET}}`
   and each sub-facade, then `go mod tidy`.
2. ADOPT: replace hand-rolled UI code with the library's components wherever a component
   exists (layout, display, feedback, icons, theme). Adoption means LESS custom code —
   delete the replaced hand-rolled code, do not wrap it.
3. LEVERAGE: use the components' full surface — typed enums, prop structs, accessibility,
   dark mode, empty states — instead of bare-bones calls.
4. Regenerate templates: run `templ generate` and COMMIT the generated `*_templ.go` files
   (they are committed artifacts in this ecosystem). Never hand-edit generated files.
5. Verify: run the repo's build and tests. The `.tq-verify` gate runs the same commands —
   a red gate is a failed task. Never leave the repo broken; rendered output should stay
   visually consistent unless a component's correct use changes it.

Scope guard (hard):
- Adopt + leverage ONLY. No unrelated refactors, no renames outside the library surface,
  no dependency changes beyond templ-components and its sub-facades, no framework swaps,
  no CSS rewrites beyond what component adoption requires.
- Never edit .crushrc, crush.json, or .tq-verify: they define your autonomy and your
  verify gate; changing them is self-dealing.
- If {{TARGET}} genuinely breaks this repo and you cannot resolve it within the library's
  documented API, STOP: revert the dependency bump so the repo stays unbroken at
  {{CURRENT}}, and report the blocker as your final line (see below). A reverted no-op
  with a precise blocker report is a GOOD outcome; a broken repo is not.

Contract:
1. Work in small commits; end EVERY commit message with this exact footer line (you have
   explicit permission to commit for this task):

   Task-Queue-ID: {{TASK_ID}}

   (so git log and the queue cross-reference). Never push.
2. The queue derives what you did — commits via the footer above, changed files via git —
   so do NOT report files or commit SHAs yourself.
3. End your output with one final summary line (the last line of your run):

   TQ_RESULT: {"summary": "<one line: what was adopted/bumped/blocked>", "upgraded": true|false}
