# Mirror-clone baseline (dedup ruling 2026-09-26: accepts ≈ 0).
#
# Format: one group per line — <category> <backend-file-1> <backend-file-2> …
# (paths relative to the repo root, sorted; line numbers deliberately
# excluded — the mirrored FILE PAIR is the stable identity).
#
# ZERO rows since the 2026-09-26 conformance-suite consolidation: the three
# mirrored spike suites (store_test.go ×3 + the sessionfact pair) now share
# ONE suite in internal/queue/companion/conform (StoreSuite harness +
# capability knobs), and the adapters delegate to internal/queue/companion.
# Any NEW cross-backend group fails ci-local; rows reappear only by
# deliberate regen with a recorded reason.
