# Features

> Honest inventory of what this project does, by status. Studied from the
> code, not the marketing claims. Updated as features ship, change, or break.

## Status legend

| Status                    | Meaning                                                      |
| ------------------------- | ------------------------------------------------------------ |
| 🟢 `FULLY_FUNCTIONAL`     | Works as intended, exercised by tests or daily use.          |
| 🟡 `PARTIALLY_FUNCTIONAL` | Ships but has known gaps, edge-case bugs, or missing pieces. |
| 🔴 `BROKEN`               | Present in code but not working / disabled / failing.        |
| ⚪ `PLANNED`              | Designed or documented but **not yet implemented** in code.  |

## Queue core

| Feature                                                                   | Status                    | Notes                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Durable enqueue (type, project, payload, priority, delay, attempt budget) | 🟢 `FULLY_FUNCTIONAL`     | `internal/queue/sqlite/sqlite.go` (`Enqueue`); semantics pinned by `internal/queue/sqlite/store_test.go`                                                                                                                                                                                                                                                 |
| Store backends as separate modules (ADR-0012)                             | 🟢 `FULLY_FUNCTIONAL`     | `internal/queue` carries the contract (`Store`, `Filter`, `Queue`); `internal/queue/sqlite` (modernc, pure Go) and `internal/queue/postgres` (pgx/v5, `FOR UPDATE SKIP LOCKED`) are independently tagged driver modules — embedders pick one import line; both satisfy the contract at compile time (`var _ queue.Store`)                                |
| Idempotent enqueue via dedup keys                                         | 🟢 `FULLY_FUNCTIONAL`     | Partial unique index + in-tx re-check; legacy-DB migration tested                                                                                                                                                                                                                                                                                        |
| Lease-based exclusive claims                                              | 🟢 `FULLY_FUNCTIONAL`     | Claim atomicity under concurrency: `TestExactlyOnceUnderConcurrency`                                                                                                                                                                                                                                                                                     |
| Crash reclaim (lease expiry)                                              | 🟢 `FULLY_FUNCTIONAL`     | `TestLeaseExpiryAllowsReclaim`; reclaim records `task.released`                                                                                                                                                                                                                                                                                          |
| Heartbeat lease renewal                                                   | 🟢 `FULLY_FUNCTIONAL`     | `TestHeartbeatExtendsLease`; lease-lost mid-run discards the attempt (at-least-once)                                                                                                                                                                                                                                                                     |
| Retries with exponential backoff                                          | 🟢 `FULLY_FUNCTIONAL`     | `worker.ExpBackoff` (2^n s, 5m cap); backoff gates via `not_before`                                                                                                                                                                                                                                                                                      |
| Dead-letter queue + rescue                                                | 🟢 `FULLY_FUNCTIONAL`     | `tq dlq --rescue` re-queues with a fresh attempt budget                                                                                                                                                                                                                                                                                                  |
| DAG dependencies (deps gate at claim)                                     | 🟢 `FULLY_FUNCTIONAL`     | `TestDepsBlockUntilCompleted`; no orchestrator, one `NOT EXISTS` clause                                                                                                                                                                                                                                                                                  |
| Delayed tasks (`NotBefore`)                                               | 🟢 `FULLY_FUNCTIONAL`     | `TestNotBeforeDelays`                                                                                                                                                                                                                                                                                                                                    |
| Priority ordering                                                         | 🟢 `FULLY_FUNCTIONAL`     | Higher priority claims first, then FIFO                                                                                                                                                                                                                                                                                                                  |
| Cancellation                                                              | 🟢 `FULLY_FUNCTIONAL`     | Pending: immediate. Running: `tq cancel --force` cooperative cancel — request fact observed at the worker's heartbeat, process tree killed, finalized without burning the attempt; expired leases finalize the cancel at reclaim (ADR-0005)                                                                                                              |
| Append-only fact journal + replay                                         | 🟢 `FULLY_FUNCTIONAL`     | `tq facts`, `tq tail -f`; facts written in the same tx as state                                                                                                                                                                                                                                                                                          |
| Per-project stats and filters                                             | 🟢 `FULLY_FUNCTIONAL`     | `tq stats [--project] [--json]`, `List` filters                                                                                                                                                                                                                                                                                                          |
| Permanent vs transient error classes                                      | 🟢 `FULLY_FUNCTIONAL`     | `executor.PermanentError` → `FailPermanent` dead-letters after ONE attempt; `tq facts` shows `[class=permanent]`                                                                                                                                                                                                                                         |
| Preflight requeue (no attempt burn)                                       | 🟢 `FULLY_FUNCTIONAL`     | `executor.PreflightError` → `Store.Requeue`: pending again, attempts unchanged, `task.requeued` fact                                                                                                                                                                                                                                                     |
| Orphaned-lease detection + marking                                        | 🟢 `FULLY_FUNCTIONAL`     | `tq doctor` reports expired-lease unreclaimed tasks; `--mark-orphans` appends an idempotent `task.orphaned` fact (observation only, state untouched)                                                                                                                                                                                                     |
| SQLite throughput baseline (10k)                                          | 🟢 `FULLY_FUNCTIONAL`     | `TestEnqueueClaimBaseline10k` (on demand: `TQ_BASELINE=1`): enqueue ~28.5k/s; claim+complete ~234/s at 10k pending (claim cost grows with depth — the number to beat for Postgres); multi-process exactly-once proven by a 2-worker+reader e2e                                                                                                           |
| Per-project claim exclusivity (opt-in)                                    | 🟢 `FULLY_FUNCTIONAL`     | `WithProjectExclusivity` / `--project-exclusive`; store-level, cross-pool; live-smoked with 2 pools × 3 repos                                                                                                                                                                                                                                            |
| Journal-consumer watermarks (`watermarks` table)                          | 🟢 `FULLY_FUNCTIONAL`     | Durable per-consumer cursor over the fact journal (`queue.Store` `Watermark`/`SaveWatermark`, monotonic upsert — a save never lowers the stored seq; SQLite + Postgres). Feeds the papdashboard bridge and the review/status sweepers; admin `ListWatermarks`/`SetWatermark` (forced rewind) kept off the interface, exposed as `tq watermarks show/set` |
| Journal subscription dispatcher (`internal/consumer`)                     | 🟢 `FULLY_FUNCTIONAL`     | Per-subscriber cursor over `Store` bounded reads: at-least-once, in-order delivery, flaky-handler isolation, burst paging, `Lag`/`Cursor` observability (ADR-0009: exact vs signal consumer classes, slow consumers block). Consumer-lag table in `tq stats`; the papdashboard bridge's migration onto it is deliberately deferred                       |
| Journal compaction prototype (ADR-0006)                                   | 🟢 `FULLY_FUNCTIONAL`     | `sqlite.Store.ArchiveFactsBefore` moves whole fact trails of TERMINAL tasks into `facts_archive` (all-or-nothing per task) in one tx + high-water mark in `journal_meta`; projections untouched (pinned by test). Operator-facing CLI awaits ADR-0010's demand triggers                                                                                  |
| Failure evidence on `task.failed` facts                                   | 🟢 `FULLY_FUNCTIONAL`     | Executors publish `FailureEvidence{stage, exit_code, tail}` (agent run, agent verify, sh command); both stores append it to the failed-attempt fact — a failed task is debuggable from the journal alone (`TestFailureEvidenceRidesFailedFact`)                                                                                                          |
| Postgres store (`SELECT … FOR UPDATE SKIP LOCKED`, ADR-0007)              | 🟡 `PARTIALLY_FUNCTIONAL` | Full Store semantics over pgx (pure Go, CGO off) + conformance battery in CI (`TestPostgresConformance`: DAG gating, scheduling, retry ladder + evidence, cancels with reasons, heartbeat, read surface) + parallel-claim hammer against a real cluster. Gaps: CLI `--store` wiring not exposed; `ArchiveFactsBefore` twin missing                       |

## Executors

| Feature                           | Status                | Notes                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sh` command executor             | 🟢 `FULLY_FUNCTIONAL` | Payload shapes: raw line, JSON string, `{"cmd":...}`; output tail in errors                                                                                                                                                                                                                                                           |
| HTTP webhook executor             | 🟢 `FULLY_FUNCTIONAL` | POSTs the task envelope; 2xx completes; 408/429/5xx retry, other failures dead-letter after one attempt                                                                                                                                                                                                                               |
| Headless agent executor (`agent`) | 🟢 `FULLY_FUNCTIONAL` | Live-proven end-to-end (2026-09-06); verify gate enforced (`.tq-verify` file wins, then payload, then auto-detect incl. Makefile/flake/cargo), clean-tree guard, argv contract test. Input mistakes dead-letter after one attempt; dirty tree / missing autonomy requeue without attempt burn. Gap: Windows has no process-group kill |
| Custom Go executors (Registry)    | 🟢 `FULLY_FUNCTIONAL` | 1-method interface + `RegisterFunc`; module-internal until packages go public                                                                                                                                                                                                                                                         |

## Agent pool

| Feature                                                                              | Status                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------ | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Harvest TODO_LIST.md → agent tasks                                                   | 🟢 `FULLY_FUNCTIONAL`     | Checkbox parsing (code-fence aware), stable dedup keys, per-repo pacing, tick cap, dry-run. Proven live (harvest → agent → `[x]` → commit → complete)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `tq bootstrap` — one command from zero                                               | 🟢 `FULLY_FUNCTIONAL`     | Per repo: validates checkout, pins `.tq-verify`, writes a managed `.crushrc` block (autonomy + model/reasoning), commits exactly those files (never pushes), previews the harvest, then delegates to `agent-pool` (or `--install` renders the systemd unit + `pool.conf`, or `--no-run`/`--dry-run`). Idempotent: managed block replaced in place, user config untouched                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `tq agent-pool` self-managing loop                                                   | 🟢 `FULLY_FUNCTIONAL`     | Periodic harvest + worker pool in one process; graceful drain lets agents finish. `--once` runs one tick and exits (systemd unit in `deploy/systemd/`); store-level per-project exclusivity opt-in (`--project-exclusive`); `--config`/`TQ_POOL_CONFIG` flag-file persistence (flag > env > file) survives restarts with drifted flags                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Output sidecars (`--log-dir`/`TQ_LOG_DIR`)                                           | 🟢 `FULLY_FUNCTIONAL`     | Full agent + verify output per task to `DIR/<task-id>.log` (0600); flag, config-file key, or env (flag > env > file); path lands in the result detail. Retention: `--log-dir-max-age`/`TQ_LOG_DIR_MAX_AGE` sweeps `*.log` older than the age once per tick AND `--log-dir-max-bytes`/`TQ_LOG_DIR_MAX_BYTES` caps the directory total (oldest deleted first; defaults off = never delete)                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Autonomy guard (`.crushrc` required for yolo)                                        | 🟢 `FULLY_FUNCTIONAL`     | Fails fast with remediation guidance; user-global crush config satisfies the probe; refusals requeue without burning an attempt                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Cost ceilings (`--daily-budget`, `--budget-cmd`, `--repo-interval`, `--dlq-backoff`) | 🟢 `FULLY_FUNCTIONAL`     | Daily cap projected from journal facts; budget command is the final authority; per-repo pacing + poisoned-repo cooldown. Unit-tested + live-smoked                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| CQA findings → fix tasks                                                             | 🟡 `PARTIALLY_FUNCTIONAL` | `internal/bridge/cqa` groups fixable issues per file, dedup keys include scan ID. httptest-tested only — never verified against a live CQA API                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Agent reviews (`--review`, `--review-autofix`)                                       | 🟢 `FULLY_FUNCTIONAL`     | `internal/executor/review.go` + `internal/review`: every completed agent task gets ONE review by a second agent; strict `TQ_RESULT` verdict contract (approve / request_changes + findings) lands in the completion fact. Autofix mints deduped fix tasks per finding. Reviews are never reviewed (structural loop guard) and every enqueue counts against the budget. Sweeper checkpoints the `review-sweeper` watermark — restarts and downtime gaps resume instead of starting at head; first-ever run bootstraps at head, rewind via `tq watermarks set review-sweeper SEQ`                                                                                                                                                                                                                                                                                     |
| Automated done-prompt loop (`--status-every N`)                                      | 🟢 `FULLY_FUNCTIONAL`     | `internal/executor/status.go` + `internal/status`: every N completed agent tasks per project mint ONE status task whose agent writes `docs/status/<ts>_<name>.md` (done/partial/not-started/broken/improve/next-50/3-questions) and appends next items to TODO_LIST.md (questions as `— BLOCKED:` items) — closing the loop back into harvest. Contract: `TQ_RESULT {"report","next_items"}` naming an existing repo-relative file PLUS the repo verify gate (`.tq-verify`/auto-detect). Window entries carry per-completion commit/files and the pinned raw TODO item; the sweeper propagates `--allow-dirty` + `--task-timeout` into payloads; prompt carries a hard scope rule. Only `agent` completions count (loop guard), one report in flight per project, window derived from queue state (restart-safe), budget-gated; e2e: `scripts/smoke/status-loop.sh` |

## Bridges

| Feature                         | Status                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PapDashboard dead-letter alerts | 🟢 `FULLY_FUNCTIONAL` | `alert.triggered` on dead-letter, `alert.resolved` on later completion (resolve correlation derived from the task's fact trail, not in-memory state — restart-proof). Verified by the httptest suite, the restart battery (`restart_test.go`: zero-loss mid-stream restart with identical idempotency keys, checkpoint-failure gating + heal, FromSeq precedence) and the stub-mode E2E (`scripts/smoke/papdashboard-e2e.sh`); the script's real-dashboard (`PAP_URL=…`) mode is broken-by-construction (19:49 report) — live-instance verification still owed. Checkpoints persist per watermark, so restarts resume from the last accepted fact; the very first run bootstraps at head (pre-bridge history is never replayed) |
| Dead-pool detection alert       | 🟢 `FULLY_FUNCTIONAL` | `tq agent-pool --dead-pool-ticks N` (default 3, 0 = off): when EVERY watched repo scan-fails N consecutive harvest ticks (the pool reads no TODO_LIST, so nothing can enqueue — the 2026-09-10 pool-deploy incident class), one `alert.triggered` fires via the bridge's direct `NotifyDeadPool` path (skips are process-local observations, not journal facts) plus a WARN log even without `--alert-url`; the first tick with a readable repo posts `alert.resolved` and resets the streak. Pinned by `TestDeadPoolDetectorStreakLifecycle` + `TestDeadPoolAlertTriggerAndResolve`                                                                                                                                            |

## CLI (`tq`)

| Feature                                                                             | Status                | Notes                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `tq doctor` (health checks)                                                         | 🟢 `FULLY_FUNCTIONAL` | DB integrity/WAL, queue mix + expired leases, worker liveness via heartbeats, budget vs `--daily-budget`, agent binary, per-repo autonomy files; `--json`, exit 1 on failures                                                                                                                                            |
| enqueue / worker / stats / top / audit / show / dlq / cancel / facts / tail / tasks | 🟢 `FULLY_FUNCTIONAL` | `tq worker` runs until signalled; `--once` drains the claimable queue then exits. `tq facts` renders the dead-letter error class; `tq stats` carries a consumer-lag table, `budget today N/M` spend and an aggregate `--json` payload; `tq show`/`tq cancel` resolve unique ID prefixes (ambiguous names its candidates) |
| `tq show` — task + full fact trail                                                  | 🟢 `FULLY_FUNCTIONAL` | Completed agent tasks record session id + verify tail in `task.completed`; `tq show` renders the whole trail                                                                                                                                                                                                             |
| `tq top` — live per-project view                                                    | 🟢 `FULLY_FUNCTIONAL` | Counts + last-run/active durations from the journal; `--once`, `--json`, terminal repaint                                                                                                                                                                                                                                |
| `tq audit` — docs-drift auditor                                                     | 🟢 `FULLY_FUNCTIONAL` | stale-open repaired by enqueue-once catch-up tasks; stale-done reported; `--dry-run`, `--json`, harvest-parity `--todo-file`/`--type`/`--max-attempts`                                                                                                                                                                   |
| `tq dlq --rescue-all --older-than`                                                  | 🟢 `FULLY_FUNCTIONAL` | Bulk rescue after a human decision; `--older-than` gates on dead duration                                                                                                                                                                                                                                                |
| `tq harvest --json` / `--repo-subset`                                               | 🟢 `FULLY_FUNCTIONAL` | JSON result output; glob filter over discovered repos (daemon-backed too when `--discovery-addr` is set)                                                                                                                                                                                                                 |
| `tq tasks` — filtered list view                                                     | 🟢 `FULLY_FUNCTIONAL` | `--project/--status/--type/--since DUR/--limit/--json`, newest first, full IDs + attempts + last-error excerpt — the completion-window view that previously needed raw sqlite                                                                                                                                            |
| `tq version` — build identity                                                       | 🟢 `FULLY_FUNCTIONAL` | ldflags-injected release version in nix builds; VCS revision from build info in go builds                                                                                                                                                                                                                                |
| `tq harvest --prune-stale`                                                          | 🟢 `FULLY_FUNCTIONAL` | Cancels pending tasks whose TODO_LIST item is `[x]` (dedup match; reason on the cancel fact); running/dead reported only; `--dry-run`, `--json`                                                                                                                                                                          |
| `tq watermarks show/set`                                                            | 🟢 `FULLY_FUNCTIONAL` | Per-consumer journal cursor + lag vs head (`show`); forced rewind (`set CONSUMER SEQ`) for re-delivery after a fix — the ops escape hatch for watermark-backed consumers (papdashboard bridge, review/status sweepers)                                                                                                   |
| harvest / agent-pool                                                                | 🟢 `FULLY_FUNCTIONAL` | See Agent pool; `--dry-run` for preview; `--model`, `--once`, cost ceilings; `--discovery-addr`/`TQ_DISCOVERY_ADDR` opts repo discovery into the project-discovery-daemon (`POST /v1/discover`) — unreachable daemon = one warning + local-scan fallback, never fails the tick                                           |

## Web UI (`tq serve`)

| Feature                                            | Status                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tq serve` — live dashboard (read-only)            | 🟢 `FULLY_FUNCTIONAL` | Status cards, live task table, DLQ, per-project chips, fact feed; SSE-pushed server-rendered fragments (`internal/webui`, ADR-0003)                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Board view (`?view=board`)                         | 🟢 `FULLY_FUNCTIONAL` | Kanban-style alternative to the task table: one column per lifecycle status (pending → running → completed, dead/cancelled exits), status-colored top rules, newest 25 cards per column with live ages, readiness countdowns, lease owners, dead-card error tails; true counts with an honest "+N older →" link into the status-filtered table. Rides the same `#frag-table` SSE container (view rides the URL; `app.js` forwards it); respects project/query filters; moving cards is queue mutation and stays behind the writes gate (`board_test.go`, webui smoke) |
| SSE stream with snapshot-on-(re)connect            | 🟢 `FULLY_FUNCTIONAL` | Every event is a full projection re-render under the client's URL filter — reconnects never lose state; `Last-Event-ID` honored                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Filters & search (`?project=&status=&q=`)          | 🟢 `FULLY_FUNCTIONAL` | URL is the source of truth; search covers type/payload/id/project/owner/errors; shareable links                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Task detail pages (`/task/{id}`)                   | 🟢 `FULLY_FUNCTIONAL` | Full record + per-task fact timeline, live over a task-scoped SSE stream (`/task/{id}/events`); plain links, works without JS                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Live smoke (no browser)                            | 🟢 `FULLY_FUNCTIONAL` | `scripts/smoke/webui.sh`: worker + serve + HTTP/SSE assertions, CI-safe                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Request logging (`tq serve --verbose`)             | 🟢 `FULLY_FUNCTIONAL` | Per-request slog lines (method, path, status, duration) on stderr; SSE-safe wrapper; off by default                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Token auth (`tq serve --auth-token`)               | 🟢 `FULLY_FUNCTIONAL` | Non-loopback binds are default-deny (refuse without a token); constant-time check on every route; `Authorization: Bearer` or `?token=` — a valid token ALSO issues an `HttpOnly SameSite=Lax` session cookie (`tq_token`) so subresources (CSS/JS/favicon/SSE) authenticate; `--verbose` logs redact the token (ADR-0003 amendments)                                                                                                                                                                                                                                  |
| CSP nonce regime                                   | 🟢 `FULLY_FUNCTIONAL` | Per-request 128-bit nonce, `script-src 'self' 'nonce-…'` (never `unsafe-inline`), stamped onto the theme bootstrap + toggle inline scripts (`TestCSPNonceCoversInlineScripts`); the `app.js` script tag's presence is pinned so the ghosted-client class cannot recur                                                                                                                                                                                                                                                                                                 |
| Admin writes (`--allow-writes`/`$TQ_SERVE_WRITES`) | 🟢 `FULLY_FUNCTIONAL` | Opt-in control layer registering exactly two routes — `POST /task/{id}/cancel` (pending direct, running cooperative) and `POST /task/{id}/rescue` — each CSRF-guarded (`tq_csrf` cookie + constant-time field check, forged → 403) with native `<details>` reason forms (zero JS under CSP); writes-enabled banner; live-verified over real HTTP (2026-09-09)                                                                                                                                                                                                         |
| UI write actions — enqueue / bulk rescue           | ⚪ `PLANNED`          | Cancel/stop/rescue shipped behind `--allow-writes` + CSRF (row above); enqueue-from-UI and bulk actions remain open — scope ceiling is an owner decision (ADR-0003 Phase D)                                                                                                                                                                                                                                                                                                                                                                                           |
| Pagination (`?page=`) + severity ordering          | 🟢 `FULLY_FUNCTIONAL` | Task table paginates in SQL (200/page, dead→running→pending→cancelled→completed order stable across pages) with prev/next pager; `CountTasks` pushdown drives "page N of M"                                                                                                                                                                                                                                                                                                                                                                                           |
| Bounded reads at scale                             | 🟢 `FULLY_FUNCTIONAL` | Measured at 100k tasks + 100k facts: page query 18ms, count 1.5ms, LIKE-search count 36ms, last-50-facts 0.2ms, per-task trail 0.05ms (`TestLoadSnapshotScaleAt100k`); no dashboard path scans the whole journal or table                                                                                                                                                                                                                                                                                                                                             |
| Security headers (CSP et al.)                      | 🟢 `FULLY_FUNCTIONAL` | Strict CSP (`default-src 'none'`, self-only scripts/styles, no inline, no framing) + nosniff/no-referrer/DENY + Permissions-Policy device-feature deny (camera/microphone/geolocation) on every response incl. 401s; route table + guardrail test enforce read-only                                                                                                                                                                                                                                                                                                   |
| Budget + readiness visibility                      | 🟢 `FULLY_FUNCTIONAL` | Budget stat card (spent/cap today, green/amber/red at 75%/100%) when `--daily-budget` is set; "ready" column shows when pending tasks become claimable ("in 12m"/"ready", exact `notBefore` on tooltip)                                                                                                                                                                                                                                                                                                                                                               |
| Design system (templ-components)                   | 🟢 `FULLY_FUNCTIONAL` | Themed with `github.com/larsartmann/templ-components` v1.16.x: `layout.Base` shell with light/dark toggle, library tables/badges/empty states + feedback.Alert; always-dark nowband (RUNNING/PENDING/DEAD/COMPLETED/CANCELLED ledger segments) + two-tier ACTIVE NOW/settled table redesign; steel-navy + cyan token remap and JetBrains Mono (SIL OFL 1.1 subsets) via `internal/webui/theme.css`; the minified `internal/webui/static/app.css` is committed and recompiled with `nix run .#webui-css` after template changes                                        |
| Review verdicts in the UI                          | 🟢 `FULLY_FUNCTIONAL` | Approve/request_changes badge in the task-table status cell + findings card (title + severity) on the detail page, read best-effort from the review task's completion fact; rendered only when a completion exists (`TestReviewVerdictBadgeAndFindings`, `TestReviewVerdictAbsentWithoutCompletion`)                                                                                                                                                                                                                                                                  |

## Tooling

| Feature                                 | Status                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --------------------------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NixOS module (`services.tq-agent-pool`) | 🟢 `FULLY_FUNCTIONAL` | `deploy/nixos/tq-agent-pool.nix` via `nixosModules.default`: declarative poolSettings → pool.conf, `dbPath` (StateDirectory vs mount-gated pool path), serve dashboard unit (token file for non-loopback), drain invariants pinned (SIGINT / 45min / process-only); `checks.module-eval` evals both branches in `nix flake check`. Deployed on Evo-x2 by SystemNix (house overlay `modules/nixos/services/tq-agent-pool.nix`) |
| Nix flake build + vendor-hash check     | 🟢 `FULLY_FUNCTIONAL` | `nix build` produces the `tq` binary (`CGO_ENABLED=0`); `nix flake check` passes including the vendor-hash drift gate — re-verified locally 2026-09-10 (incl. rebuilding the exact CI-failed derivation); the CI nix job is red on runners for a separate reason (see the row below)                                                                                                                                          |
| CI (vet, build, test -race, gofmt)      | 🟢 `FULLY_FUNCTIONAL` | `.github/workflows/ci.yml`; runs on every push; includes the TODO_LIST harvest-parse guard, the live web-UI smoke, the doc ghost-reference check, and an advisory (non-blocking) golangci-lint step that loops the root module AND every `internal/*` sub-module (f32 parity with ci-local.sh) whose findings stay visible as annotations                                                                                     |
| CI Windows test job                     | 🟢 `FULLY_FUNCTIONAL` | `test-windows` runs the honestly-tagged suites on `windows-latest` (POSIX-only suites carry `//go:build unix` and drop out); race coverage stays on Linux                                                                                                                                                                                                                                                                     |
| Release runner                          | 🟢 `FULLY_FUNCTIONAL` | `scripts/release.sh vX.Y.Z [--tag                                                                                                                                                                                                                                                                                                                                                                                             |
| CI nix build + flake check              | 🔴 `BROKEN`           | Keyless runner-safe (HTTPS flake inputs); red on runners since the 2026-09-10 fullcore push: go-modules FOD hash mismatch (committed vendorHash verified correct locally, same got-hash across two trees, exact failed drv builds green locally) — runner-only fetch divergence under investigation (docs/status/2026-09-10_06-25 report d1)                                                                                  |
| Multi-repo two-pool live smoke          | 🟢 `FULLY_FUNCTIONAL` | `scripts/smoke/multi-repo.sh`: 3 repos, 2 pools, 1 DB — no double-enqueue, one claim per task, both pools work                                                                                                                                                                                                                                                                                                                |
| E2E subprocess suite                    | 🟢 `FULLY_FUNCTIONAL` | `internal/e2e`: builds the real `tq` binary, drives `agent-pool --once` with a stub agent from outside; runs in CI. SIGTERM shutdown-ordering e2e (`shutdown_test.go`): in-flight claimed task completes, bridge watermark ≥ dead-letter seq, SSE closed before exit 0                                                                                                                                                        |
| Property + fuzz tests (harvest)         | 🟢 `FULLY_FUNCTIONAL` | Dedup-key stability property; `FuzzParseRepo` (CRLF/BOM/nesting), 1.8M+ execs clean; nightly CI campaign (`scripts/fuzz/nightly.sh`, 60s) commits new seed-corpus inputs into `internal/harvest/testdata/fuzz` (165 seeds so far)                                                                                                                                                                                             |
| Chaos test (kill mid-run)               | 🟢 `FULLY_FUNCTIONAL` | `internal/e2e/chaos_test.go`: SIGKILL victim, lease-expiry reclaim, exactly one completion in the journal                                                                                                                                                                                                                                                                                                                     |

## Examples (`examples/` — experimental by design)

| Feature                                    | Status                | Notes                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------ | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Full-core embed demo (`examples/fullcore`) | 🟢 `FULLY_FUNCTIONAL` | One file proving the embed story: enqueue, custom + shell executors (retry proof included), a worker pool draining the queue, and the sqlite-vs-postgres backend picked at run time (`--backend`) from two store imports that both satisfy `queue.Store`; sqlite path executed end-to-end 2026-09-10 (12 tasks, retry proof); postgres path compiles but is not yet executed |

## Production API (`tq api`) and remaining plans

| Feature                                                   | Status                    | Notes                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HTTP API server for non-Go producers (`tq api`, ADR-0008) | 🟡 `PARTIALLY_FUNCTIONAL` | Production slice shipped: mandatory bearer token (no loopback exemption), enqueue contract with actionable `{error, fix}` validation JSON, 1 MiB body cap, dedup passthrough, stats + healthz; PoC remains in `examples/api`. Cancel/claim-over-HTTP and fencing tokens designed but not built |
| Decision → question fan-out (PapDashboard)                | ⚪ `PLANNED`              | Design note in `docs/planning/2026-09-06_decision-question-fanout.md`                                                                                                                                                                                                                          |
| Per-repo daily budgets                                    | ⚪ `PLANNED`              | Global daily cap + per-repo intervals ship; per-REPO daily caps don't yet (m23 sketch)                                                                                                                                                                                                         |
| Remaining C27 seeds (D83, D94, D97-D100)                  | ⚪ `PLANNED`              | Sketches in `docs/planning/2026-09-06_deferred-bundle-seeds.md` (D80/D90/D91 shipped; D83 cron recurring tasks still a seed — see ROADMAP)                                                                                                                                                     |
