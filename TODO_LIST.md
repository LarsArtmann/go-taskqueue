# TODO List

Short- and mid-term actionable work. Long-term direction lives in ROADMAP.md.

**This file is machine-consumed**: `tq harvest` turns every unchecked item
below into an agent task. Keep the `- [ ]` checkbox format, one item per
line — do not convert to tables. Mark done items `[x]` or delete them
(the safer default is `[x]`: deletion removes the item from
`tq harvest --prune-stale`'s match surface — see the caveat in AGENTS.md);
appending `— BLOCKED: <reason>` keeps an item out of the pool.

Items carry their evidence: a code path and/or the status report that filed
them (`docs/status/<date>_<slug>.md`). Completed items live in CHANGELOG.md,
not here.

## High Impact

- [ ] Self-fixing dead-letter queue with AI agents: when a task dead-letters, a sweeper mints ONE agent task (dedup `dlqfix:<id>:<hash>`, mirroring the review-sweeper pattern) that diagnoses the failure from its FailureEvidence tail and either applies a fix + rescues the original or cancels with a reasoned verdict — write the design in docs/planning/ first (code paths: `tq dlq` rescue/cancel, `internal/worker` DLQ path, `internal/review` sweeper)
- [x] `--prune-stale` zombie-matching gap: absent item text is now stale too — pending harvested tasks (payload dedup key, `catchup:` prefixes stripped) whose item is gone from the file are cancelled with a key-carrying reason; external tasks untouched. Policy decided + implemented + tested 2026-09-09 (`internal/harvest/prune.go`, round-10 session)
- [x] Pin the cooperative-cancel finalize contract in a test: wrapped `context.Canceled` through `runAgent` finalizes as Cancelled, never Failed, no `task.failed` fact, attempts unburned (`TestCooperativeCancelWrappedErrorFinalizesAsCancelled`, 01:48 report d7/e5)
- [x] `tq bootstrap` parity check against the hand-rolled sibling-repo rails — fleet switched to the managed-block form (one commit per repo), bootstrap dry-run now reports truthful change state (22:42 §d1/f2, 01:35 §c1/f1)
- [x] SECURITY.md: the `--allow-writes` blast radius, token/CSRF/loopback matrix, defense layers, residual risks (01:35 §c5/f6)
- [x] `tq harvest --prune-stale` sweep inside `agent-pool` startup — synchronous, before any actor (the worker's first claim would race an in-actor sweep), `--prune-stale=false` opts out; e2e pins zero-zombie relaunches (01:48 f11)
- [x] Rate-limit the two write endpoints: 3 failed CSRF tokens → 60s lockout (429 before CSRF, reads unlimited, success resets), smoke-asserted (01:35 f29)

## Medium Impact

- [x] `Filter.Since` SQL pushdown in both stores (`pgWhere` extracted, CountTasks shares it); `tq tasks --since` rides it (01:48 f3)
- [x] `journal_head` in `tq stats --json`; budget line labeled `(all projects)` when the table is scoped (01:48 f4/f5)
- [x] Pool `MkdirAll` on `--log-dir` at startup (23:10 e5/f4)
- [x] Dirty-tree requeue backoff ladder (base×2ⁿ capped 15m, ±20% jitter) + per-task refusal log rate-limit (23:10 e7/f7)
- [x] `tq facts --json` + `--detail` non-truncating rendering (23:10 e8/f6)
- [x] Retention keys (`log-dir-max-age`/`-max-bytes`) in `renderPoolConfig` + bootstrap flags + NixOS docs; `--repo-interval`/`--dlq-backoff` passthrough too (01:48 f7/f8, 23:10 e9)
- [x] `status-sweeper` consumer never starts when `--status-every 0` (verified); `tq watermarks show` labels current cursors and explains that lagging may mean off (23:10 e11/f27)
- [x] Structured evidence on `task.requeued` facts: `RequeueEvidence{reason, retry_in_ms}` in both stores; ONE `FailureEvidence.Tail` constant (`EvidenceTailBytes`) (01:48 b3/f2/f23)

## Web UI

- [x] Status-color vocabulary: ONE `statusColorTable` produces badge + accent (00:21 e5/f5, `internal/webui/components.go`)
- [x] Shared empty-state templ helper for TaskTable + Board (00:21 e4/f4)
- [x] Serve banner text shared const (`webui.BannerPrefix`…) used by the CLI and the e2e parser (00:21 e3/f21)
- [x] `checks.module-eval` repaired: argv-correct extraArgs example, unknown-poolSettings-key survival branch (via the new `renderedConfigFile` option), `authTokenFile`→`EnvironmentFile` assertion (23:48 e5/f16-f19)

## Fleet / deploy

- [ ] SystemNix host-side deploy + round-9 cutover (sudo-gated, user-run): `nix run .#deploy` on evo-x2 starts tq-agent-pool/tq-serve/tq-storage-dir/tq-bootstrap (all wiring shipped 2026-09-08: rev-pinned git+file input 7890cc9, house module `modules/nixos/services/tq-agent-pool.nix`, ports.tq 8100, tq.home.lan vHost + DNS, Gatus checks, btrbk-pool subvolume, dedicated sops bridge template, post-deploy smoke); then the one-time manual-pool cutover in SystemNix `docs/services/tq.md` (stop the `/tmp/tq` processes — DONE 2026-09-10, optionally copy the dogfood tasks.db onto the pool journal). The origin push is done — flip the SystemNix input to `github:LarsArtmann/go-taskqueue?ref=master` — 2026-09-10: the deployed pool was found DEAD (every tick skipped all repos: bare repo names resolved via service cwd + no git/go/crush on the service PATH + silent scan-fail logging); all three fixed on master, so the input flip + redeploy is what brings the Flash pool to life — BLOCKED: owner-run (sudo on evo-x2 — an agent cannot execute this; pool must not pick it up)

## Owner-blocked decisions (BLOCKED items are skipped by the pool)

- [ ] Push authorization (updated 2026-09-10): master + root v0.1.0/v0.2.0 + the five round-1 module tags are now on the remote (owner pushed); still latent: the two backend tags `internal/queue/{sqlite,postgres}/v0.2.0` — after their push run the per-module proxy check (`go list -m -versions`) + the real clean-room `go install` (docs/status/2026-09-09_23-47_round2-queue-backend-split.md f1/f13); post-split executor/queue content waits for the next release's module tags (published v0.2.0 tags are immutable; restored after a mistaken local re-cut) — 2026-09-10 06:25: both backend tags verified on the remote (`git ls-remote`); the remaining proxy check + clean-room install moved to the Fullcore-window section below — BLOCKED: owner push authorization (never push without it)
- [ ] `internal/consumer`: wire into `tq serve` journal tailing or delete the ghost package (ADR-0009 dispatcher, zero production importers; write the ADR outcome either way) (23:47 f2/g1) — BLOCKED: owner intent call
- [ ] Decide the three near-identical interfaces (`consumer.Source`, `budget.FactSource`, `papdashboard.FactSource`) — one interface or documented duplication (23:47 f4) — BLOCKED: owner architecture decision
- [ ] Postgres CLI store wiring (`--store postgres://…` on worker/serve/agent-pool): gives queue/postgres its consumer; root re-adds pgx; sequence BEFORE any public-API promotion (23:47 f5/g3) — BLOCKED: owner release-timing call (v0.3?)
- [ ] Dependabot/renovate policy for the 8-module tree (23:47 f33) — BLOCKED: owner policy decision
- [x] After the next real release: write the "first multi-module release" retrospective into docs/release/ (23:47 f50) — SCAFFOLD LANDED 2026-09-12 round-13 T16: docs/release/FIRST-MULTI-MODULE-RELEASE-RETRO.md (sectioned template with pre-seeded known-lesson slots); the retro itself stays blocked on the next real release, as originally scoped

## Post-split follow-ups (harvested from docs/status/2026-09-09_23-47, verified 2026-09-10)

- [x] Dead-export audit script as a periodic check: exported-with-zero-importers detector (substring matching, NOT `rg -w` — it misses suffixed references like NewSink/NewCommandExecutor and undercounts); park it next to scripts/check-go-mods.sh (23:47 f19; manual re-derivation lives in the 2026-09-10 session) — DONE 2026-09-10: scripts/check-dead-exports.sh (advisory report, `--strict` to gate), wired into ci-local.sh
- [x] govulncheck step in CI (non-blocking job first; needs network on the runner) (23:47 f20) — DONE 2026-09-10: separate continue-on-error `govulncheck` job in ci.yml (govulncheck@v1.8.0, root + disk-derived sub-module loop); local root+sqlite scans clean
- [x] gosec advisory scan pass over the modules (23:47 f21) — DONE 2026-09-10: gosec v2.29.0 over root + every sub-module (48 findings); triaged all FP/by-design with per-rule rationale in the AGENTS.md "gosec advisory baseline" note; the two real findings (G114 timeout-less serves in examples/) fixed; advisory `gosec` job added to ci.yml (continue-on-error, mirrors govulncheck)
- [x] templ-components deep-dive audit: adoption table says v1.16 — verify we use the current component APIs and none of the retired ones (23:47 f22; internal/webui/fragments.templ) — DONE 2026-09-10: go.mod pins v1.16.0 = latest published; the only Deprecated marker in the whole library (forms.Form layout field) is in a package we don't import; v1.14→v1.16 changelog is purely additive (KanbanBoard, icons.Render, PageProps.SEO), zero Removed/Deprecated in span; adoption table ↔ template usage already pinned both directions by TestAdoptionTableCoversTemplates/PinsCustomRows; stale "v1.14.x" claims corrected to v1.16.x in AGENTS.md conventions bullet + FEATURES.md design-system row (ADR-0003 left as historical record)
- [x] webui dedup deep pass: render path 728 + handlers 527 LOC have grown similar branches (23:47 f23; internal/webui/render.go, handlers.go) — DONE 2026-09-10: one generic `completionDetail` core behind `statusResultFor`/`reviewResultFor`, one `pageResults` collector replacing the twin loop pairs (loadSnapshot + handleTaskDetail), one `runEventStream` SSE session behind `handleEvents`/`handleTaskEvents`, one `sendSnapshotPayload`+`queryErr` tail behind both snapshot senders, one `taskFromPath` behind the four Get→404 heads; dead `filterSuffix` removed; net −67 LOC, loadSnapshot gocognit 36→clear, `--new-from-rev` lint clean, race suite + webui smoke green
- [x] httpapi/webui API-surface split-brain check: overlapping handler + route definitions between `tq api` and `tq serve` (23:47 f24) — DONE 2026-09-10: the real split-brain was the stats surface — two handlers computing the same payload independently (dashboard via full snapshot projection keyed by HTML badge labels; API via raw GROUP BY rows that dropped zero-count statuses) — both now derive keys from the task.Status list, always emit all five statuses + total, and TestStatsSurfacesAgree (internal/webui, imports httpapi) pins the two payloads equal over one seeded store plus the disjoint route namespaces (/api/v1/* vs /api/*); dead label constants removed; webui /api/stats no longer pays for a full snapshot load
- [x] Full-core example: worker + executor + queue proving the embed story, including the sqlite-vs-postgres backend-choice import (23:47 f25; examples/) — DONE 2026-09-10: examples/fullcore (single file: sqlite/postgres chosen by --backend, both imports satisfy queue.Store; enqueues sh + custom in-process executors incl. a retry, drains via a worker.Pool, prints status counts); root module gains internal/queue/postgres require + flake vendorHash update
- [x] Document the round-2 multi-module release flow as docs/release/RELEASE.md (sub-tag cutting, sibling-replace allowlist, two-phase --tag/--push; scripts/release.sh owns the code) (23:47 f26) — DONE 2026-09-10: docs/release/RELEASE.md (gate sequence, two-phase --tag/--push with resume, disk-derived sub-tag cutting, allowlist rules + the round-2 nested-module lesson; v0.1.0 checklist kept as archived history)
- [x] Version-surface inventory doc: flake.nix version attr + ldflags + root tag + per-module tags + CHANGELOG — who must move when (23:47 f27; the flake checks.version-sync check covers attr↔binary, not the rest) — DONE 2026-09-10: docs/release/VERSION-SURFACES.md (all 7 surfaces with current values + what verifies each, manual-bump order CHANGELOG → flake attr+ldflags → root tag → sub-tags, non-version-bearing surfaces, deliberate coverage gaps; every claim checked against scripts/release.sh, scripts/lib/release-gates.sh, scripts/check-go-mods.sh, flake.nix)
- [x] Lint-baseline slice-triage: wrapcheck findings (~50) first, then varnamelen (~50) — shrink the ~400 advisory baseline, never mass-fix (23:47 f28/f29; .golangci.yml) — DONE 2026-09-10: both classes triaged to zero (config policy + renames)
- [x] `ExitCause` → `ExitError` rename consideration (errname finding; exported rename needs a v0.3 window) (23:47 f30; internal/executor/agent.go) — DONE 2026-09-10: renamed (actually internal/runactor, not executor); no v0.3 window needed — `internal/` import path means zero external importers, so the rename is binary-internal
- [x] Per-module golangci runs in .github/workflows/ci.yml (ci-local.sh already loops modules; ci.yml runs root only) (23:47 f32) — DONE 2026-09-10: advisory lint step now loops every internal/* sub-module after the root run, mirroring ci-local.sh
- [x] Multi-repo smoke against the nix-built 0.2.0 binary (`TQ_BIN=result/bin/tq ./scripts/smoke/multi-repo.sh`) (23:47 f35) — DONE 2026-09-10: multi-repo.sh now honors TQ_BIN (same pattern as webui.sh/status-loop.sh — it was the only smoke that always rebuilt from source); green against result/bin/tq 0.2.0 (6 enqueued / 6 completed / 6 claims / 0 dead, both pools claimed) and on the default build path
- [x] Measure CI time impact of the disk-derived `-count=1` module loops; tune if it dominates the job (23:47 f49; .github/workflows/ci.yml) — DONE 2026-09-10: measured from green run 34406684629 step timings (gh api): test job 'Module isolation gates (GOWORK=off per sub-module)' 26s/271s (~10%), test-windows 'Module isolation gates (per sub-module)' 39s/173s (~23%); neither dominates (Test step itself 82-89s, lint 84s), and setup-go's build cache already amortizes the per-module compiles — no tuning warranted
- [x] Dogfood hygiene: stale queued tasks in the live pool DB may verify with the OLD single-module default command — drain or cancel them (`tq tasks`, `tq cancel`) (23:47 f46) — DONE 2026-09-10: audited the live journal — only two non-terminal tasks exist (one pending review task whose payload is self-contained and carries no verify command, plus the running hygiene task itself); zero pending/queued agent tasks remain, so no stale verify string can fire; nothing to drain or cancel

## Dogfood round (harvested from docs/status/2026-09-10_02-00 self-review §f)

- [x] cwd-dependence sweep: audit every repo-name consuming command (`tq audit`, prune, harvest) for bare-name resolution against the projects dir, add tests, fix any cwd-dependent path found (02:00 f8) — DONE 2026-09-11: audit found `tq harvest --repos` (resolveHarvestRepos) and `tq audit --repos` passing bare names raw, so the sweeps Abs()ed them against the cwd (agent-pool + bootstrap already expanded); both now route through a shared expandRepoSpecs (absolute + existing cwd-relative pass through, bare names join --projects-dir), which also covers `tq harvest --prune-stale`; pinned by cmd/tq/repospec_test.go (expandRepoSpecs table, harvest wiring, audit e2e from a foreign cwd)
- [x] `tq doctor --repos`: expand bare repo names via expandRepoSpecs like harvest/audit — last raw splitRepos consumer; bare names stat against the cwd in doctorRepoAutonomy (02:00 f8 residue, 04:05 verify session; cmd/tq/doctor.go:355 + repospec_test.go row) — DONE 2026-09-11: doctorEnvironment routes --repos through expandRepoSpecs; doctor gained a --projects-dir flag (default $TQ_PROJECTS_DIR or ~/projects, same as harvest/audit); pinned by TestCmdDoctorResolvesBareRepoNamesAgainstProjectsDir in cmd/tq/repospec_test.go (cmdDoctor from a foreign cwd, ok for a bare name resolved against --projects-dir)
- [x] module-eval hardening: extend the flake module-eval check to assert the tq-agent-pool unit's Environment carries a non-empty PATH (02:00 f11; flake.nix, deploy/nixos/tq-agent-pool.nix) — DONE 2026-09-11: `poolHasPath` helper asserts some Environment entry matches `PATH=.+` on the deployed-shape pool unit in checks.module-eval; nix build of the check green
- [x] `checkProjectsDir` should not require the projects dir when every `--repos` entry is absolute (02:00 f16; cmd/tq/agentpool.go + test)
- [x] harvest skip-log change detection: log a skip class only when its example changes (or on first tick) instead of every 5m tick flooding journald (02:00 f17; cmd/tq/main.go)
- [x] `tq doctor`: warn when crush/git/go are missing from PATH — pool-context visibility of the exact failure class that killed the deployed pool for 20h (02:00 f18; cmd/tq/doctor.go)
- [x] dead-pool detection: alert via the PapDashboard bridge when a pool's scan-failed skip count covers every repo for N consecutive ticks (02:00 f6; cmd/tq agent-pool tick loop + internal/bridge) — DONE 2026-09-11: `--dead-pool-ticks N` (default 3, 0 = off) counts CONSECUTIVE all-repos scan-failed ticks (one `deadPoolDetector.observe` per successful harvest, coverage = scan-failed skip count == res.Repos); at N it fires once per streak via the bridge's new direct `NotifyDeadPool` (trigger on the stable `agent-pool-dead-pool` aggregate, resolve + streak reset on the first readable tick) plus a WARN log even without `--alert-url`; pinned by TestDeadPoolDetectorStreakLifecycle (cmd/tq) + TestDeadPoolAlertTriggerAndResolve (papdashboard httptest)
- [x] dogfood smoke: scripts/smoke/dogfood-once.sh replicating the 2026-09-10 --once proof (env-gated TQ_DOGFOOD=1 because it spends API money; stub-agent variant runs ungated) (02:00 f10) — DONE 2026-09-11: stub variant ungated + CI-safe (scratch repo, bare-name + foreign-cwd expansion, harvest → work turn with `{{TASK_ID}}` footer commit → review approve → drained, doctor at head), wired into ci-local.sh; TQ_DOGFOOD=1 re-runs the real proof against this repo with a pinned scratch DB
- [x] archive dogfood evidence durably: copy the 2026-09-10 proof journal (/tmp/tq-dogfood.db) + review log into docs/status/ assets or ~/.local/state/tq/ before /tmp reboots (02:00 f9) — DONE 2026-09-11: all 16 agent-task logs (work turn `000001a0888b…` + review approve verdict `000001a088933b…`) archived verbatim in docs/status/assets/2026-09-10-dogfood-proof/ with a provenance README; the proof journal itself (/tmp/tq-dogfood.db) was already gone from /tmp when this ran — its fact trail lives on only in the 01-55 report
- [x] Anti-ghost-archive gate: CI check asserting every `docs/status/assets/*/README.md`'s promised evidence files are actually git-tracked — kills the global-`*.log`-ignore + auto-commit-daemon silent-loss triangle that nearly shipped the f9 archive as README-only (06-41 report §d1; scripts/) — DONE 2026-09-11: `scripts/check-ghost-archives.sh` extracts backticked bare evidence filenames from every archive README and demands each is git-tracked (GHOST = on disk but gitignored, MISSING = promised-but-absent, UNTRACKED = whole archive uncommitted; placeholders/globs/paths/dotfiles exempt), gated in ci-local.sh + ci.yml, failure modes scratch-tested and the real f9 archive green
- [x] SHA256SUMS manifest for `docs/status/assets/2026-09-10-dogfood-proof/` — completeness + tamper-evidence in one file; pairs with the anti-ghost-archive gate (06-41 report §c2) — DONE 2026-09-12: SHA256SUMS (17 entries: 16 logs + README) generated in the archive dir, README documents the `sha256sum -c` verify; check-ghost-archives.sh now requires every `docs/status/assets/*/` archive to ship a git-tracked manifest and re-verifies hashes + full file-set coverage (NO MANIFEST / UNTRACKED / STALE / INCOMPLETE failure modes, negative-tested with a scratch fixture; fixture removed after the test)
- [x] AGENTS.md gotcha paragraph: evidence copies into the repo must run `git check-ignore` on targets FIRST and diff the daemon commit's `--stat` against the intended file set — a clean `git status` is not a complete archive while the auto-commit daemon is live (06-41 report §d1/§e1-2) — DONE 2026-09-12: gotcha bullet added to AGENTS.md Conventions directly after the Evidence-archives bullet (check-ignore first, copy second, diff the daemon commit's --stat; names the ignore+daemon+clean-status silent-loss triangle and cites the f9 near-miss)
- [x] Harvest same-session-priority flag: enqueued items whose text references /tmp paths get surfaced as hot at harvest time — f9's "before /tmp reboots" framing gave false comfort; the journal died to ordinary /tmp cleanup within a day (06-41 report §e3; internal/harvest) — DONE 2026-09-12 (docs-health verify): `--same-session-priority` flag shipped, harvest options carry SameSessionPriority (cmd/tq/main.go:455-490); TestRunSameSessionPriority pins the plumbing (internal/harvest/harvest_test.go:213)
- [ ] `tq pool-health`: one-shot summarizing per-repo skip streaks + last harvest activity from the journal (liveness ≠ process up) (02:00 f27)
- [x] worktree-per-agent design doc: claim → dedicated git worktree → verify → merge; the intra-repo parallelism path — write to docs/planning/ with tradeoffs and an explicit open-questions section (02:00 f15) — DONE 2026-09-12: docs/planning/2026-09-12_worktree-per-agent-design.md (three-layer serial lock named: WithProjectExclusivity claim filter + assertCleanTree preflight + slot cap; pipeline claim → worktree-create → execute → verify → merge → reap; contract interactions (close-out session-in-same-worktree, review-before/after-merge, per-repo rate-limit gates unchanged, daemon invisibility, gitignored non-propagation, footer forensics on branches); tradeoffs table; 9 explicit open questions with the owner merge-policy call marked gating)
- [x] session-close bridge design + prototype: every INTERACTIVE crush session gets the same close-out as pool agents — `tq session begin/close` minting a journal fact + footer-based commit attribution (Crush-Session: <id>), a direct-enqueued review task over the session's commit range, and a done-prompt status task; trigger research (2026-09-10, deep pass): crush PR charmbracelet/crush#3146 (SessionStart/SessionEnd/TurnEnd hooks) is maintainer-approved + CI-green + users-on-branch, and issue #2707 ("Lifecycle hooks for external observers", closed pointing at it) confirms the maintainers consider it THE orchestrator/turn-done answer — SessionEnd has a 2s timeout so a hook must ONLY enqueue (pool does the rest); lineage: #1337/#1487/#2612 closed-folded into #2598 (PreToolUse only, merged); interim trigger ranking: (1) PreToolUse session registry — CRUSH_SESSION_ID+CRUSH_CWD already arrive on every tool call, a hook appends {id,cwd,last_seen} and a sweeper mints the close-out when no crush process owns the ID (tested: undocumented SessionEnd/Stop names do NOT fire today), (2) `tq crush` wrapper on process exit, (3) polling ~/.local/share/crush/crush.db (schema-private, migration-fragile — #2707's own warning), (4) crush.log tailing (fragile); attribution gap (daemon-folded commits) remains ours either way; the trust asymmetry (interactive = human-reviewed) predates interactive-sessions-also-being-Flash (2026-09-10 operator insight) — DONE 2026-09-12: `internal/session` package + `tq session begin/close` (cmd/tq/session.go): begin mints `session.opened` (double-begin refused, prints the footer convention); close scans `Crush-Session: <id>` trailers via ONE git-log call (`%(trailers)`, git ≥ 2.15, exact value match — quoting others' footers never mis-attributes; unborn HEAD = empty session) and direct-enqueues ONE review (dedup `review:session:<id>`) + ONE status task (dedup `status:<project>:session:<id>`, keys share the sweeper namespaces) then appends `session.closed` carrying commits + minted IDs; zero attributed commits → fact only, nothing minted; payloads pin Yolo, omit Model (.crushrc owns model+effort), `--allow-dirty` mirrors into RequireClean; close is enqueue-only (SessionEnd-hook-ready). New fact types `session.opened/closed` on the synthetic `session:<id>` TaskID (collision-proof, invisible to sweepers/budget/bridges); `(*sqlite.Store).AppendFact` = the one sanctioned non-task fact write. Tests: internal/session (hermetic stub-scanner suite + pure parser suite + unix real-git integration), sqlite AppendFact, cmd/tq end-to-end begin→work→close→replay. Design + open questions (trigger automation, daemon attribution, budget bypass, postgres parity): docs/planning/2026-09-12_session-close-bridge-design.md; verified 2026-09-12: root build+vet+`go test ./... -race` 13 pkgs ok (exit 0) + all 7 sub-module build/vet/test gates green on the shipped tree (code landed via daemon commits 1e85603/277bd4f)

- [x] Cut v0.2.0: full ci-local gate + nix-binary smoke green, annotated tag v0.2.0 pushed, module proxy verified, clean-room go get verified, GitHub Release published (also fixed a release.sh bug: the awk section cut matched the bare heading, not the dated one) — 2026-09-09 `v0.2.0`, module proxy + GitHub release + smokes (19:49 f2; v0.1.0 shipped 2026-09-06)
- [x] Kill the stray `/tmp/papdbg` worker (PID 1039418) — killed 2026-09-09 (second-signal force-exit; audit had verified it self-contained and inert)
- [x] `--status-every` enablement: N=20 chosen + documented in the `deploy/systemd` sample's recommended pool.conf keys (cost/verbosity balance; the live smoke shipped 2026-09-08) (20:56 g1/f17)
- [x] Status-report review ceiling: DECIDED (default policy 2026-09-09): the mechanical file-exists + verify contract stays the ceiling — reports are exempt from review minting like reviews themselves; loop safety stays structural (sweeper exempts the type) + budgetary (every enqueue caps) (20:56 g2)
- [x] Hard mechanical cap on status-agent TODO_LIST appends: DECIDED (default policy 2026-09-09): prompt-level caps + the budget guard remain the ceiling — a diff-parsing hard cap would reject legitimate multi-item reports and add a fragile parser; blast radius is bounded by `--daily-budget`/`--max-per-tick` on every minted item (20:56 g3)
- [x] `tq cancel` dedup-key release: DECIDED (default policy 2026-09-09): keys stay suppressive after cancel — releasing them would let a stale harvest re-arm withdrawn work; the documented escape hatch (edit the item text → new key) is deliberate and now also re-arms correctly since absent items are pruned
- [ ] Verify the CQA bridge against a live CQA API instance and fix contract drift (`internal/bridge/cqa` response shapes are httptest-informed guesses today); upgrade its FEATURES.md status after (plan C25) — BLOCKED: needs a live CQA instance URL + owner ID + token from the owner

## Window f20–f24 follow-ups (harvested from docs/status/2026-09-10_04-09, verified 2026-09-10)

- [x] Fix test-windows red: `TestHarvestConfigFromOptionsExpandsBareRepoNames` (subtests mixed_entries_with_spacing, absolute_repos_stay_untouched) fails on windows-latest — audit bare-repo-name expansion for `filepath.Separator`/abs-path assumptions (landed a0b720e 01:38; red on master since) — DONE 2026-09-10 (08:25 docs-health pass, re-verified 09:13 pass): inputs/expectations follow the running OS's path rules per the red-master-trio CHANGELOG entry; separator-aware logic confirmed in internal/harvest
- [x] Fix release-gates smoke on runners: `scripts/smoke/release-gates.sh:42` `git tag -a` lacks the `-c user.email/-c user.name` the init commit (line 40) carries — annotated tags need committer identity, runners have none (exit 128 since 00:22) — DONE 2026-09-10 (re-verified 09:13 pass at HEAD): fixture commit (line 40) and tag (line 44) both carry `-c user.email=t@t -c user.name=t`
- [x] Bump `golang.org/x/text` to ≥v0.39.0 in `internal/queue/postgres` (GO-2026-5970 infinite loop, reachable via `postgres.Open` → pgxpool per the first govulncheck CI run) and sweep every module for the same x/text floor — DONE 2026-09-10 (re-verified 09:13 pass): `internal/queue/postgres/go.mod` at v0.41.0; per-module gate loop green over all 7 sub-modules
- [x] Add a gosec config encoding the 2026-09-10 FP triage (exclude-rule list for G204/G702/G703/G304/G306/G301/G302/G124/G710/G118/G104/G404/G202) so the advisory job goes green and future new classes stand out — DONE 2026-09-12 round-13 T6: ci.yml gosec job runs `-exclude=<14 triaged classes incl. G115>` on root + sub-module loop and `.golangci.yml` gosec.excludes matches for scanner parity; local post-config scan v2.29.0 = root 0 findings (pre-config 37, all triage classes); a new finding in an excluded class needs a fresh triage note, not a silent pass
- [x] Flip govulncheck's `continue-on-error` to a hard gate once the x/text bump makes the job green on the runner — DONE 2026-09-12 round-13 T7: evidence-gated flip (locally clean at HEAD root + all sub-modules on v1.8.0 AND runner step-level green in run 34661978016); the next push's run proves the hard gate stays green
- [x] ci-local.sh: run the release-gates smoke under `GIT_CONFIG_GLOBAL=/dev/null` (or sanitized HOME) so identity-dependent git ops fail locally the way they do on runners — DONE 2026-09-12: ci-local release-gates step runs under `GIT_CONFIG_GLOBAL=/dev/null` + `user.useConfigOnly` via env config — /dev/null alone was NOT enough on this host (git auto-detects identity from the passwd GECOS and commits anyway); strict sanitizer proven both ways: a flag-stripped smoke copy dies at the fixture commit ("Author identity unknown", the runner mode), the real smoke stays green under the exact ci-local env
- [x] Add a master-CI state gate: `scripts/check-ci.sh` failing when the latest master run is a failure (via `gh run list`), wired into ci-local.sh — five DONE verdicts landed on a 3h-red master without anyone looking — DONE 2026-09-11 (verified: scripts/check-ci.sh exists, wired at ci-local.sh:20-21; bypass `CI_CHECK=off`; CHANGELOG Unreleased documents it)
- [x] Add a changed-lines line-length gate (lll/golines, 120 cols) to ci-local.sh so signature-wrap regressions (the c5c654c class) are caught before commit — DONE round-11: ci-local step "line-length gate (changed lines only, 120 cols)" runs scripts/lint-lll-changed.sh; verified live at HEAD 2026-09-12
- [x] Backfill CHANGELOG: the gosec advisory job + the two examples G114 ReadHeaderTimeout fixes (17a5940) never got an entry (govulncheck's did) — DONE 2026-09-10 06:25 docs-health pass: gosec job + G114 fixes backfilled under Added; the fullcore example, stats wire-contract fix, and the root go.mod postgres replace got their entries in the same pass
- [x] Export the status enum list from `internal/task` (`task.AllStatuses`) and retire the twin lists (webui `allStatuses`, httpapi `apiStatuses`) — DONE 2026-09-12: `task.AllStatuses()` (function, not exported var — no new gochecknoglobals row and callers can't mutate the enum list) with a hardcoded-oracle pin test (`TestAllStatuses`, internal/task); webui `allStatuses` + httpapi `apiStatuses` vars deleted, all usage sites (render.go, handlers.go, fragments.templ + regenerated fragments_templ.go, stats_contract_test.go) range the export; root build/vet/test -race + all 7 sub-module gates + gofmt + check-webui-css green; root go.mod still pins internal/task v0.2.0 (local replace carries the export until the next sub-module re-tag, as planned)
- [x] Publish gosec/govulncheck findings as CI job-summary artifacts so advisory-red runs are readable without log-diving — DONE 2026-09-12 round-13 T7: both jobs write `$GITHUB_STEP_SUMMARY` tables (govulncheck: per-module clean verdicts; gosec: zero-findings + new-class triage rule)
- [ ] Write the required-checks proposal (test-windows + release-gates smoke first, then the scan jobs) into docs/planning/ for the next release window
- [x] Triage the webui zero-importer exports the dedup pass left behind (`BudgetView`, `BoardColumn`, `DashboardData`; part of the 48-symbol check-dead-exports advisory report) — DONE 15-10 (15-10 report M82): all three LIVE (used in-package by the templ fragments — the zero-importer detector counts cross-package importers only), zero dead exports; `factLines` flagged separately by gopls (15-39 f18)
- [ ] Evaluate templ-components `KanbanBoard` (added v1.15+) against the custom board columns/cards in internal/webui/fragments.templ
- [ ] Evaluate templ-components `PageProps.SEO` + `icons.Render` adoption for layout.Base and the filter/empty-state icons (04-09 report §a/f22)
- [ ] Harden the two example servers beyond `ReadHeaderTimeout` (IdleTimeout/ReadTimeout), keeping the SSE `WriteTimeout=0` exemption documented (examples/api, examples/sse)
- [ ] docs/planning/: verification-claims guidance — DONE notes must state gate SCOPE (the govulncheck "local scans clean" claim was root+sqlite only while postgres carried the real finding)
- [x] Fix the err113 finding at `cmd/tq/doctor.go:472` (dynamic `errors.New("failing checks found")`) as a static sentinel — it hard-failed lint-annotations once, then silently aged into the `--new-from-rev` baseline — DONE 15-10: static `var errDoctorFailed` at cmd/tq/doctor.go:41 (verified at HEAD 2026-09-11)
- [x] SECURITY.md: add the two advisory scan jobs (govulncheck f20, gosec f21) to the defense-layers matrix — DONE (verified at HEAD 2026-09-11: SECURITY.md defense-layers carries govulncheck per-module + gosec with the baseline note)
- [x] Confirm a CI run exists for the current HEAD (c5c654c had none at 04:09 despite being pushed 04:02) and record the outcome — RECORDED 2026-09-10 06:25: runs exist for HEAD (7c5f5e0, run 34436225021) and are RED — nix (new, runner-only FOD hash mismatch), test (release-gates smoke 128), test-windows, govulncheck (GO-2026-5970), gosec; details in docs/status/2026-09-10_06-25_fullcore-window-five-review-fixes-nix-gate-red.md d1/d2
- [ ] gosec gating: once a suppression config encodes the triage, should gosec become a blocking required check or stay advisory permanently? — BLOCKED: owner gate-vs-advisory call on the security-scan jobs
- [ ] `internal/task` public surface: export `task.AllStatuses` in the next minor release to kill the httpapi/webui twin status lists, or keep the duplication + cross-module pin test (TestStatsSurfacesAgree) as the permanent shape? — BLOCKED: needs a public-API/versioning call on the task sub-module
- [ ] Pool policy when master CI is red: refuse agent DONE verdicts (fix-forward or park required) vs report-only disclosure of the red state — BLOCKED: needs a pool completion-contract decision

## Fullcore-window follow-ups (harvested from docs/status/2026-09-10_06-25, verified 2026-09-10)

- [x] Restore the nix CI gate: runner-only go-modules FOD hash mismatch — specified `sha256-ZyjpU20K…` (= committed vendorHash) vs the SAME got-hash `sha256-/rKFWqGR…` on two different trees; the exact failed drv (`61cshlxl…`, evaluated from the failed commit via `builtins.getFlake ?rev=`) builds green locally incl. `checks.vendor-hash` + full `nix build`; first failing run coincides with the push carrying the fullcore postgres require + go.mod replace (`98794b5` 03:45Z; runs 34434518454, 34436225021) — differential-dump the runner's FOD module tree vs local to find the divergent fetch (06:25 report d1) — RESOLVED 2026-09-11: the FOD never recurred; the concurrent CI red was root-caused and FIXED by round-11 — first diagnosed as missing `GOEXPERIMENT=jsonv2` (necessary), then CORRECTED (16-00 report §a): `setup-go: stable` floated to go 1.27.1 where json/v2 is stdversion-gated to modules declaring `go 1.27`; all 7 setup-go steps pinned to 1.26.7 (ci.yml ×6, fuzz.yml ×1) and the nix job is green on recent runs (15-39 a11's failing list has no nix); the remaining red (test-windows, windows cross-compile, test-postgres, gosec) is the same toolchain class pending the pin's next push
- [x] Backend-tag verification now unblocked (both `internal/queue/{sqlite,postgres}/v0.2.0` tags verified on the remote via ls-remote): per-module proxy check (`go list -m -versions`) + real clean-room `go install` (updates the BLOCKED push item's remaining ask; 06:25 report b3; DONE 15-20 — `go list -m -versions` shows v0.2.0 for BOTH backends via the proxy, and a clean-room `go mod tidy`/`go get` fetches both with full dependency resolution; the compile step correctly refuses `import` of internal packages from outside the repo (Go internal rule — by design, the root module is the app layer), so the multi-module supply-side promise is PROVEN: tags resolve, checksums verify, deps resolve)
- [ ] Run examples/fullcore once against a throwaway postgres DB (`--backend postgres`; the example applies its schema — keep it pointed at a local/test database) (06:25 report c2)
- [ ] Hermetic fullcore smoke: `scripts/smoke/fullcore.sh` (sqlite path, explicit `TQ_DB=<scratch>` export per the production-DB trap; 05-58 report f25, harvested by the 06:25 pass)
- [ ] Status-task window payload: include the window's closeout/status report paths so the done prompt reads them directly instead of re-deriving from task ids (05-30 report f25; internal/executor/status.go mint payload)
- [ ] Docs-health continuation: annotate + archive the six 2026-09-07 reports (18-41, 19-33, 20-32, 21-19, 21-44, 23-08) — every-item-resolved verification per the archive convention before each `git mv` (06:25 report f6)
- [ ] Closeout-report placement: every task writes `docs/status/<ts>_task-<id>.md` — root floods at ~10 tasks/day; root vs `docs/status/tasks/` subdir vs write-but-sample-index — BLOCKED: owner volume/placement call (05-30 report g1)
- [ ] Status/closeout TODO-append caps: reports may append up to ~50 items each and the backlog outgrows the 60/day enqueue drain — cap appends (~10), raise the budget, or let it grow? — BLOCKED: owner backlog-policy call (05-30 report g2)
- [ ] Ticket 000001a08915cf6f1c872a00bb18a4f9e326 close-out: its commit 0275120 carries a phantom footer (`…0ca7ae2`, zero commits in git) and the content landed via upstream twin 20dafb9 — mark complete queue-side and decide the dual-footer/disposition convention for review-fix and rebase-dropped commits — BLOCKED: owner queue-convention call (06:25 report g1)
- [ ] Master push authorization: master moved 05:45 + 06:03 today while AGENTS.md says agents never push — were those owner-run, and is autonomous push now sanctioned for agent sessions? — BLOCKED: owner push-policy answer (06:25 report g2)
- [ ] Module-fetch trust for the nix gate: if the runner FOD divergence is proxy/fetch-content drift, vendor modules (pinned fetch, trade repo size for gate determinism) vs keep proxy fetches and investigate per incident — BLOCKED: owner hermeticity call (06:25 report g3)

## Window follow-ups (harvested from docs/status/2026-09-10_08-25, verified 2026-09-10)

- [ ] Rename the two varnamelen raw sites (`o` cmd/tq/agentpool.go:278, `f` cmd/tq/poolconfig.go:26) or extend the ignore list, so scoped `--enable-only` diagnostics agree with full-config runs (2 vs 0 discrepancy, 08:25 report b2)
- [ ] Pin the fullcore drain-deadline determinism mechanically: extend the planned hermetic fullcore smoke to exercise the deadline path N times (`16a15d8` was hand-verified only; 08:25 report b3)
- [ ] Lint-baseline slice-triage round 2: goconst (≥50), mnd (≥50), paralleltest (≥50), testpackage (30) — same triage-or-policy discipline as wrapcheck/varnamelen, never mass-fix (08:25 report f3)
- [x] Record a per-linter findings baseline (checked-in file or CI job summary) so shrink claims are measurable; backfill the current root count (~380 across 29 classes, wrapcheck/varnamelen 0) as the starting point (08:25 report f4) — DONE 2026-09-12 round-13 T5: `.golangci-baseline.txt` checked in (per-module × linter counts, CI-pinned golangci-lint v2.13.2) AND wired as a ci-local gate (`scripts/lint-baseline.sh --check`: growth/new-class fails, shrink advisory); regen + green check verified at HEAD
- [x] Execute `scripts/release.sh` against a throwaway fixture repo, including gates, `--tag`, and sub-tag cutting on fixture modules — the flow has only ever been line-read (08:25 report f5; 07-49 report f23) — DONE 2026-09-12 (docs-health verify; round-13's 03-21 "genuinely open" verdict was WRONG, refuted by the 03-46 §f10 call): round-12 T14 executed release.sh on a real fixture (01-09 report) — gates green (nix stubbed, honest limitation), `--tag` + disk-derived sub-tag cutting incl. a nested module, negative gates, AND a real prod bug found+fixed (scripts/release.sh:93 env-prefix substitution never aborts under set -e)
- [x] Audit `scripts/release.sh` + `scripts/lib/release-gates.sh` for tag-ancestry assumptions broken by the forked lineage (git describe, merge-base, rev-list patterns); add a doctor/release-gate warning for release tags not reachable from master (08:25 report f6; 07-49 f2/f3/f8) — DONE round-11 T15: `tq doctor` tag-ancestry check (cmd/tq/doctor.go `doctorTagAncestry`, WARNs on unreachable v* tags) + RELEASE.md "Tag ancestry (forked lineages)" section; verified at HEAD 2026-09-12
- [x] RELEASE.md: add a tag-ancestry/rewrite section (what the flow assumes; the 2026-09-10 reword fork as the live example) and document the `--push` failure path when tags are already cut but the push failed (08:25 report f7; 07-49 f4/f27) — DONE: RELEASE.md carries both "Tag ancestry (forked lineages)" and "The --push failure path" (per-step failure modes incl. the NEVER re-tag proxy rule); verified at HEAD 2026-09-12
- [x] Smoke sub-tag cutting end-to-end: cut a throwaway `internal/<mod>/vX.Y.Z` tag on a fixture and resolve it via local module resolution — the release doc's most operationally risky claim (08:25 report f8; 07-49 f26) — DONE 2026-09-12 (docs-health verify, same round-12 T14 evidence): fixture `internal/queue/sqlite@v0.2.1` sub-tag cut AND resolved by a clean-room consumer module via GOPROXY=direct + insteadOf (go get + go list -m all green, 01-09 report M49)
- [x] `release.sh --push`: automate or one-command the "confirm CI green on refs/tags/vX.Y.Z" manual step (gh run list/watch on the tag ref) (08:25 report f9) — DONE round-11 T15: release.sh polls `gh run list --commit <tag-sha>` until completion (30-min cap, scripts/release.sh:188-205) and RELEASE.md documents it as automated; verified at HEAD 2026-09-12
- [x] Commit-msg hook (via scripts/install-pre-commit.sh): require exactly one `Task-Queue-ID` trailer matching the assigned-ID shape on non-daemon commits (08:25 report f10; 07-49 f12/f13; DONE 14-xx — shape + exactly-one enforcement, positive/negative smoke-tested; ID-reuse rejection deliberately NOT built: multiple commits per task are the norm (work + close-out report), so an already-used trailer is not evidence of a wrong ID)
- [x] `tq facts`/`tq show`: a commits-per-Task-Queue-ID view so wrong or duplicate footers surface mechanically instead of by reviewer eyeball (08:25 report f11; 07-49 f17; DONE — `tq show --commits` scans the payload repo's git log and verdicts MISSING FOOTER / AMBIGUOUS / ok)
- [x] check-status-index.sh family: cross-check each report's filename ID against some commit trailer (or mark report-task artifacts) — the f26 three-ID cluster would have been flagged mechanically (08:25 report f12; 07-49 f14; DONE 15-15 — trailer cross-check IS live and flagged exactly the f26 case (07-49 filename ...c3919ce4 vs trailer ...b141f022); WARNING-level until your canonical-ID ruling, then flip to failing)
- [x] Extend check-status-index.sh: flag filename-date vs report "Written" line drift (08:25 report f13; 07-49 f31; DONE 16-00 — T12/M43: BODY-DATE DRIFT check live in scripts/check-status-index.sh, verified at HEAD 2026-09-11)
- [x] CHANGELOG audit: confirm the v0.2.0 section cites no commit SHAs from the pre-reword lineage (they would dangle) (08:25 report f14; 07-49 f5; DONE 14-55 — CHANGELOG cites no SHAs at all)
- [x] Sweep non-status docs for references to `41b817b` (today only docs/status hits) — keep it that way; the remote-only bad-footer twin must not leak into living docs (08:25 report f15; 07-49 f6; DONE 14-55 — only the sweep item itself and the Round-11 plan snapshot mention it)
- [x] CI check: confirm no workflow assumes fast-forward from `origin/master` while the local/remote divergence (14/1) stands (08:25 report f16; 07-49 f10; DONE 14-55 — ci.yml assumes nothing about push topology)
- [x] Mark the stale-done TODO items [x] with DONE verdicts after re-verification: windows-test fix, release-gates identity, x/text ≥0.39 — all three fixed per the red-master-trio CHANGELOG entry, verified at HEAD 2026-09-10 (08:25 report f17/d4; DONE 15-22 — confirmed all three are already [x] with DONE verdicts at lines 103-105)
- [x] Attribution convention for daemon-swept work: when the auto-commit daemon folds a task's working-tree changes into a footer-less `chore:` commit (the lint triage: content in bebc35a/fc495e8, footer-only eaf73a9), how is ticket↔content attribution recorded? Draft a proposal in docs/planning/ (08:25 report f18) — PROPOSED 2026-09-12 round-13 T12: O7 appended to docs/planning/2026-09-12_02-48_OWNER-RULINGS-PACKAGE-O1-O6.md (recommendation: report-side attribution — chore: commits are attribution deserts by design, TQ_RESULT commits + `tq show --commits` are the canonical map); ruling still owner-gated
- [ ] Docs-health cadence: continue the annotation pass over the 2026-09-06 and 2026-09-08/09 report batches (18-41 gained 3 strikes 2026-09-10; none of the six 09-07 reports qualified for archiving — all carry open items); re-evaluate archive eligibility per report instead of assuming (08:25 report f19)
- [ ] Status-index maintenance: the README index grows ~10 rows/day — add a monthly digest row or an archived/-sweep cadence check so the index stays scannable (08:25 report f20; 07-49 f39)
- [ ] Draft the history-rewrite policy line for AGENTS.md: when (if ever) a review-demanded reword of an already-pushed commit is allowed, and what must be recorded when it forks the lineage (07:49 incident as the case study) (08:25 report f21; 07-49 f22)
- [x] Origin reconciliation: origin/master tips at bad-footer `41b817b` while local master holds the corrected 14-commit lineage — one-time `--force-with-lease` push to replace it (making the queue cross-reference resolve remotely), or is origin append-only and permanent divergence accepted? — BLOCKED: owner-run either way, agents never push (08:25 report g1; RESOLVED de facto 14-55 — the corrected lineage was pushed 13:55 and origin/master now contains BOTH sides appended; no force needed, remote cross-reference resolves)
- [x] Tag-lineage fork acceptance: v0.2.0 + all seven `internal/*/v0.2.0` tags descend from the remote pre-reword side and are no longer ancestors of master — acceptable for release tooling and the module proxy (tag→commit only), or harden release.sh/release-gates/doctor against forked lineages before the next release? — BLOCKED: owner release-tooling call (08:25 report g2; RESOLVED de facto 14-55 — merge-append healed the fork: v0.2.0 and all sub-tags ARE ancestors of master AND origin/master again; no release-tooling hardening needed for THIS fork)
- [ ] Canonical ID for the f26 cluster: the release-doc work exists under three IDs (`a3864d95` original commit + 06-55 report, `b141f022` reviewer-assigned on cd09c1c + 07-49 footer, `c3919ce4` 07-49 report filename) — which is the queue's canonical entry, and should the others be merged or marked duplicates queue-side? — BLOCKED: owner queue-convention call (08:25 report g3)

## Review-window follow-ups (harvested from the 07:49/08:32/08:39/08:42/08:47 close-outs, verified 2026-09-10 09:13)

- [ ] Rename the `exitCause` local in `internal/runactor` `Run()` (runactor.go:162) to the `exitErr`/`exitError` vocabulary — the ExitError rename commit left the old identifier behind (08:32 report b3)
- [ ] Run golangci-lint scoped to `internal/runactor` to confirm errname is quiet after the ExitError rename — the close-out verified via compiler + `rg`, not the linter that filed the original finding (08:32 report b2)
- [x] Extend `TestParseFilterQuery`-style round-trip pins to the other FilterState fields (Project/Status/Sort/View) so emitter↔parse drift fails the suite in either direction (08:39 report c1/f1) — DONE 2026-09-12 round-13 T15: TestFilterRoundTripAllFields (internal/webui/filter_test.go) pins all fields + allowlist fallbacks + board-drops-status + href round-trips incl. escaped/unicode values
- [x] Handler-level end-to-end webui test: GET `/?q=sh` renders filtered rows and the search chip carries the query (08:39 report f6/f31) — DONE 2026-09-12 round-13 T15: TestDashboardFilterE2E (?q=, ?project=, /project/{name} through the real handler; chips + narrowing asserted)
- [x] Verify `filterHref` URL-encodes Query values (spaces/`&` in search text); pin with a test if gaps found (08:39 report f8/f23) — DONE 2026-09-12 round-13 T15: GAP CONFIRMED + FIXED (QueryString wrote raw values; now url.QueryEscape on project/status/q) — pinned by TestQueryStringEscapesMetacharacters + round-trip rows
- [x] Single source of truth for webui query param names (q/project/status/sort/view/page): one table-driven test enumerating them, or constants shared wherever templ allows (08:39 report f4/f5) — DONE 2026-09-12 round-13 T15: the round-trip table enumerates every param name on BOTH sides (parseFilter reads what QueryString emits — any drift fails TestFilterRoundTripAllFields)
- [x] Check unbounded `?q=` search against SQLite LIKE payload scans — cap length or escape metacharacters if a huge query can degrade the dashboard (08:39 report g2/f43) — DONE 2026-09-12 round-13 T15: maxQueryLen=200 clamp at parseFilter (generous for search, trivial for the store; length-cap chosen over escaping so intentional % patterns keep working) — pinned by TestParseFilterQueryCap
- [ ] Sweep the eaf73a9 rename diff repo-wide for further literal leaks (`git log -S` probes for receiver-equals-key `Get("query")` patterns and parenthesized-identifier corruptions like `task(store)`) (08:39 f3/f22; 08:42 f4)
- [ ] Help-text smoke: run `tq` subcommand help (minimum `tq dlq -h`) asserting no parenthesized-identifier artifacts in flag strings — help text is exercised by no test and the class has bitten once (08:42 report e3/f2)
- [x] Verify the gopls "unused func waitFor" hint in internal/webui tests is genuine and delete it on next touch of that file (08:39 report f48) — DONE 15-10: deleted after verifying the hint genuine (15-10 report M82; repo-wide grep clean at HEAD 2026-09-11)
- [ ] Extend `scripts/lint-annotations.sh` to the sub-module loop, or decide root-only annotations deliberately (needs a per-module `--new-from-rev` baseline strategy) (08:47 report c1/f1/g2)
- [x] Verify the root `.golangci.yml` actually applies when golangci-lint runs inside a sub-module directory; document the config-resolution rule (or per-module config policy) in AGENTS.md (08:47 report e4/f2) — DONE 2026-09-12 round-13 T17: verified (no per-module configs exist; golangci-lint ascends from the module cwd to the root config — sub-module baseline counts like gosec/godox prove the root policy applies) and the rule documented in the AGENTS.md advisory-lint note
- [ ] Extract the disk-derived module loop into `scripts/for-each-module.sh` and consume it from ci.yml (4+ copies) and ci-local.sh (08:47 report e1/f4)
- [x] Add `actionlint` to the devShell and ci-local.sh so workflow edits validate locally instead of costing a CI round trip per fix (08:47 report e2/f5) — DONE round-11: flake devShell carries pkgs.actionlint (flake.nix:83) and ci-local has an actionlint step with an `nix shell nixpkgs#actionlint` fallback when the binary is missing; verified at HEAD 2026-09-12
- [x] End-of-step summary line in the advisory lint job ("modules linted: N, failing: [...]") so green runs stay scannable (08:47 report e3/f6) — DONE round-11/12: both ci.yml ("lint summary: N findings", ci.yml:88) and ci-local.sh:96 print the finding-count summary line; verified at HEAD 2026-09-12
- [ ] Unify the golangci-lint version pin (duplicated in ci.yml and ci-local.sh) into one source so the two can never drift (08:47 report f22/f23)
- [x] Quantify the advisory-lint baseline per sub-module — the ~400 number is root-only; sub-module counts are unknown — before deciding whether per-module runs need triage slices (08:47 report f25/f26) — DONE 2026-09-12: `.golangci-baseline.txt` IS the per-module×linter quantification (114 rows, 887 findings, CI-pinned golangci-lint v2.13.2; round-13 T5) — and the growth gate it enables is RED at HEAD pending triage (04-45 report §d1)
- [x] Verify `find internal -name go.mod` deliberately excludes fixture/testdata go.mods — the CI loops assume production modules only (08:47 report f39; DONE 15-25 — repo-wide sweep: exactly 8 go.mods (root + 7 sub-modules), zero testdata/fixture go.mods anywhere; the release-gates smoke builds its fixtures in mktemp dirs OUTSIDE the repo, so the glob can never see them)
- [ ] CI topology one-pager (docs/planning/): every job and step, which ones loop modules and why — prevents the next agent re-deriving it (08:47 report f20)
- [ ] Report the session-snapshot ghost upstream: work-commit hashes pre-appearing in the session-start git snapshot (bafc720 incident, 08:32 report d2/g2) — harness state-capture artifact or concurrent-agent duplicate?
- [ ] CHANGELOG policy for non-released-boundary changes: do `internal/`-only renames and same-day introduce+fix regressions (never crossing a release; the pool deploys master as a rolling release) get entries, or is [Unreleased] net-since-last-release only? This pass added one combined entry for the two rename-leak fixes and none for the ExitError rename — confirm or overrule — BLOCKED: owner changelog-policy call (08:32 g1; 08:42 g1)
- [ ] CI-time budget for the disk-derived module loops: what threshold should the f49 "tune if it dominates" item optimize against (e.g. lint step ≤ N minutes)? — BLOCKED: owner budget number (08:47 g3)
- [ ] TODO-item accuracy bar: should the harvester verify each item against code at harvest time (f30's wrong package + wrong version-window claim cost a day of deferral across seven reports), or is pickup-time verification by the executing agent the accepted contract? — BLOCKED: owner process call (08:32 g3)
- [ ] Enable `--allow-writes` on the deployed `tq-serve` (SystemNix module flag) so DLQ rescue/cancel work from the WebUI — the live 127.0.0.1:8100 instance runs read-only (no writes-ENABLED banner, no dead-task buttons), so the 21 dead tasks incl. `000001a08edf` need CLI `tq dlq --rescue` today; rescue AFTER the rate-limit-fix input flip so a rescued task doesn't re-burn attempts on 429s (13:29 report §b2/f1/f2) — BLOCKED: SystemNix service config + deploy are owner-run

## Rate-limit window follow-ups (harvested from docs/status/2026-09-11_13-29, verified 2026-09-11)

- [x] Incident-paste regression fixture: pin `DetectRateLimit` against the EXACT Z.ai dashboard tail of dead task `000001a08edf` (committed as testdata) so provider phrasing drift fails the suite instead of silently degrading to the 15min fallback (13:29 report f5; DONE 14-50 rc-gate report)
- [x] Pin the parked-requeue vs lease-expiry-reclaim contract: a `Requeue`-parked task (rate-limited, not_before in the future) must not be resurrectable by a stale twin's lease write — store-level test on sqlite + postgres parity (13:29 report f16; DONE 14-50 — also fixed sqlite Store.Fail's missing lease re-check)
- [x] `http` executor: classify 429 responses as `*executor.RateLimitError` (same no-attempt-burn requeue as agent tasks; today an HTTP 429 burns the retry budget exactly like the incident) (13:29 report f4c; DONE — Retry-After header/body reset honored)
- [x] Provider-tagged rate-limit gates: extract `provider=<x>` from crush output so side-by-side Z.ai/synthetic pools don't cross-park each other's tasks (13:29 report f4b/g3; DONE — repo-keyed gates, .crushrc fixes the provider per repo)
- [x] Closeout-429 double-work gap: a 429 during the close-out turn requeues the WHOLE task and re-runs the WORK turn on re-claim (doubling agent cost) — persist the work turn's session id into the payload so re-claim resumes at closeout, or document the double cost as accepted (13:29 report f15; DONE — in-process closeoutPending registry, re-claim resumes at closeout)
- [x] `tq doctor`: surface the armed rate-limit gate (provider, parked counts, until-time) so an idle pool diagnoses as WAITING, not broken (13:29 report f10; DONE — parked check + `tq tasks --parked` + stats parked count)
- [x] `tq tasks --parked` filter + rate-limit park counts in `tq stats` — "11 tasks parked until 19:40" should be one glance, not a journal dive (13:29 report f11/f14/f49; DONE)
- [x] Hermetic rate-limit e2e smoke: stub 429 agent against a scratch DB (`TQ_DB` exported per the production-trap rule) asserting the requeued fact carries `retry_in_ms>0` and attempts stay 0 through the real CLI binary (13:29 report f32; DONE 14-50 — scripts/smoke/ratelimit-e2e.sh)
- [x] Fuzz `DetectRateLimit` (nightly-campaign pattern, seed corpus from real provider failure logs) — regexes over untrusted output deserve the same treatment as FuzzParseRepo (13:29 report f8/f42; DONE 14-50 — FuzzDetectRateLimit, found+fixed a Duration-overflow)
- [x] Audit PapDashboard bridge HTTP calls for 429 backoff: `alert.triggered`/`NotifyDeadPool` posts must not hammer a rate-limited PapDashboard (13:29 report f9; DONE — 429 reclassified transient; the old permanent branch silently DROPPED alerts)
- [ ] Post-deploy retro: one week after the input flip, count 429 requeues vs pre-deploy dead-letters and record the number in a status report (13:29 report f50)

## Docs-health harvest (2026-09-11 evening; sources: 16-00/15-39/15-10/13-29 reports + task-closeout residue; deduped against existing rows)

- [x] Land the setup-go 1.26.7 pin (ci.yml ×6, fuzz.yml ×1) and verify master CI green — the corrected T5 closure; until green, ci-local's check-ci step stays bypassed (16-00 report f1/f2, §a CI-root-cause) — DONE 2026-09-12: pin landed round-12 and master runner-proven GREEN (runs 34661441493 + 34661978016, gh run list); check-ci re-armed (round-13 T1)
- [ ] Orphaned-guard audit: every `scripts/check-*.sh` + smoke must be referenced by ci-local/ci.yml/flake or be deleted — generalize the check-webui-css lesson into a ci-local meta-check (15-39 report c7/f5, e1)
- [ ] Wire `check-webui-css.sh` into ci.yml (byte-canonical pin, not just the a11y minify-property test) (15-39 report b1/f6, e4)
- [x] gosec FP config encoding the 2026-09-10 triage (exclude rules for the triaged G-classes) so the advisory job reads green and NEW classes stand out — also fixes the gosec job showing red in `gh run list` (TODO L106 + 16-00 f25) — DONE 2026-09-12: same closure as TODO L106 (ci.yml -exclude list + .golangci.yml parity; root scan 37→0 locally)
- [x] TQ_BIN rot-guard: hard gate that every smoke building tq honors `TQ_BIN` (parity for papdashboard-e2e.sh + release-gates.sh; print version+path at start) — kills the green-lie class that queued a release-artifact-blind command (09-01 report f1/f2/f3, 07-49 f35 residue) — DONE 2026-09-12 round-13 T12: papdashboard-e2e.sh honors TQ_BIN (webui.sh pattern) and prints `tq version` first in BOTH branches (proven green: source-build + TQ_BIN=/tmp scratch binary); release-gates.sh verified to consume NO tq binary (pure go.mod fixture smoke) — guard not applicable there
- [x] Daemon commits bypass pre-commit hooks: unindexed reports were silently lost twice (05-38, 05-2x retro-indexed; 09-01 f11) — daemon path must run check-status-index, or document the amend maneuver as the rule (09-01 report f11/f12; 07-49 f28) — DONE 2026-09-12 round-13 T12: amend maneuver documented as the rule in AGENTS.md (status-reports convention: amend the row into the daemon commit pre-ship, else immediate follow-up; check-status-index in ci-local/CI is the catcher, not the preventer); the script's UNINDEXED message now points at the rule
- [ ] Review-pipeline hardening: review findings must anchor commit sha + quoted anchor text; queue-side re-anchoring pre-flight for cited line ranges; disposition rule for rejected/dangling commits; dual-footer convention (05-58 report f16-f19, g1-g3 — one bad anchor cost a rework day)
- [x] Version-agreement gate: CHANGELOG↔flake attr↔ldflags checked as a set + collapse the duplicated flake version literal — closes VERSION-SURFACES.md's own declared coverage gap (07-15 report f4/f5/f6) — DONE 2026-09-12 round-13 T8: scripts/check-version-agreement.sh (flake attr = ldflags exactly; CHANGELOG latest release never older, newer = mid-cycle warn) wired into ci-local next to the release-gates smoke; the duplicated ldflags literal now derives from the flake attr (rec + ${version}); red-probed (doctored attr → exit 1) and green-proven
- [x] Doc↔script drift smoke for RELEASE.md: pin its cited modes/gates/allowlist to release.sh behavior (06-55 report f3, e1) — DONE 2026-09-12 round-13 T16: scripts/check-release-docs.sh (modes, gate_gomod, proxy wait, disk-derived sub-tags, --prerelease, CI poll, clean-room install, allowlist module path, companion scripts, `tq show --commits`, doctor tag-ancestry — both directions doc→script and script→doc) wired into ci-local.sh; its first run caught a real drift (the sub-tag command string was line-wrapped out of greppability in RELEASE.md)
- [ ] `golangci-lint config verify` in ci-local before the run — schema-invalid keys silently disable linter settings (07-39 report f3)
- [ ] Rename-hygiene scanner script: sweep quoted string literals in a diff for identifier-shaped corruptions (the `q.Get("query")`/`task(store)` class caught twice by hand) (07-39 report f4; AGENTS.md rule already shipped)
- [ ] Review-loop e2e: `scripts/smoke/reviews.sh` (stub reviewer; review approve/request_changes paths through the real binary) (07-48 report f1/f2)
- [ ] `tq enqueue` guardrail: warn when `$TQ_DB` points somewhere other than `./tasks.db` (the production-DB trap cost a live enqueue 2026-09-10) (05-29 report c4/d2/e2)
- [ ] `tq enqueue --wait [--timeout]`: blocking run streaming the task's facts to the terminal — cron one-shots today need tail+grep dance (05-29 report c1/f5/g3)
- [ ] `tq doctor --hygiene`: stale `.tq-verify` payload pins vs current repo gate + claim-time re-resolution option — retires the stale-payload audit class (09-39 report f2/f3)
- [ ] Rate-limiter global prune: bound the write-lockout strikes map against rotating source IPs (per-contact prune only today, `internal/webui/auth.go:315`) (06-01 report c7)
- [ ] Secrets-in-logs pass + `--redact`: agent output tails can carry provider tokens into facts/sidecars (17-21 report #18; 20-58 f33)
- [ ] httpapi parity hardening: `X-Content-Type-Options: nosniff` + failed-bearer-auth lockout decision on `tq api` (03-05 report f2/f3/g2)
- [x] Webui smoke: assert the payload section + retry strip render (`/task/<id>` fetch, grep `payload · agent` + `retry trail`) (15-39 report c6/f4) — DONE 2026-09-12 round-13 T15: webui.sh detail-page fetch now asserts `tq-payload-cmd` (payload pane) + retry strip markers; smoke green
- [x] Golden/snapshot test for `payloadSection` rendered HTML (15-39 report c1/f12; 14-22 f7) — DONE 2026-09-12 round-13 T15: TestPayloadSectionGoldenRender pins the owner-approved shapes (item-leads + prompt folded below; item-less leads with prompt; unparseable → raw pane) against the real templ render
- [ ] CI retry-on-transient-foreign-break wrapper for ci-local (poll 45s ×3, then fail with "concurrent edit in flight" context) (15-39 report f9, e2)
- [ ] Small rate-limit/observability batch (16-00 f29-f38/f43-f48): `--commits` on `tq facts`; parked×MarkOrphaned pin; closeout-resume flag in requeue fact; HTTP-date Retry-After fixture; stats parked JSON contract test; doctor earliest `not_before`; armed-provider log; NotifyDeadPool 429-transient; ratelimit-e2e second-claim assertion; postgres stale-fail fact-count parity; parked count in webui nowband; WithoutCloseout gate-leak test; ratelimit-e2e into ci-local smokes
- [ ] `ci.yml` concurrency group to cancel superseded runs (the daemon pushes in bursts); `check-ci.sh` "red predates your tree" wording; go-mod verify GOMODCACHE pin (16-00 f39/f41/f49)
- [x] RELEASE.md additions: multi-commits-per-task norm + clean-room backend verification as the documented module-tag step; `tq show --commits` git-missing note (16-00 f42/f43; 15-10 f44/f45) — DONE 2026-09-12 round-13 T16: new "Multi-commits per task" section (multi-commits are the norm; healthy shape = several footer hits; degradation notes for commitsForTask verified in cmd/tq/main.go: no-repo/inaccessible-repo/git-failure all surface as `note`, never crash) + clean-room module-tag verification recipe appended to "Sub-tag cutting" (go get + `go list -m all` as the gate, GOPROXY=direct + insteadOf, fixture-proven round-12 T14)
- [ ] Status-append cap/dedup + re-dispatch root cause: the loop's top fix (36/75 close-outs, up to 5× re-fires; a TODO fix item was proven LOST between report and TODO append) (14-01 report; 16-00 f50)
- [ ] Delete dead `factLines` (`internal/webui/components.go:133`, gopls unusedfunc) + add `retry trail`/`payload section`/`work item`/`verify gate` terms to docs/DOMAIN_LANGUAGE.md (15-39 f18/f19; 14-22 f49)
- [x] Triage the 3 gosec findings ci-local surfaced on the ratelimit code; check pending dependabot PRs (15-39 f20/f31) — DONE 2026-09-12 round-13 T6: all 3 are G204 exec-with-variable on the agent run/closeout/verify exec sites (internal/executor/agent.go:445/518/611) — FP-by-design per the triage (executors RUN task-payload commands as the core feature; argv never shell-interpolated), now suppressed by config; dependabot checked 2026-09-12: one open PR (#2, actions group bump) — deferred deliberately, it changes the runner inputs the round-13 CI proof just validated

## Round-13 planning additions (harvested from docs/status/2026-09-12_01-47 + the round-12 T17 tail; verified 2026-09-12)

- [x] Wire `scripts/lint-baseline.sh` + `.golangci-baseline.txt` into ci-local's advisory lint step (fail on growth), red-probe the gate before declaring done, record per-module baseline counts (2026-09-12 01-09 report T17 tail) — DONE 2026-09-12: wired at scripts/ci-local.sh:117 after the advisory lint step; red+green probed and the probe's first real run caught genuine drift 835→887 (round-13 T5)
- [x] Minted verify commands become env-self-contained: every producer (agent-pool mint) embeds the GOEXPERIMENT=jsonv2 export itself so the pool-env lie cannot eat attempts (2026-09-12 01-06/00-54 reports) — DONE 2026-09-12, AMENDED by review ruling same day: executor defaultVerify + bootstrap pin path mint the `export GOEXPERIMENT=jsonv2;` prelude; repo `.tq-verify` was REVERTED to owner-owned prelude-free form (an agent must never edit its own gate — the unit-env fix stays the BLOCKED item below); TestMintedGoVerifyIsEnvSelfContained pins prelude/idempotence/non-Go (round-13 T2)
- [x] `tq doctor` environment check: detect the encoding/json/v2 build-constraint failure (missing GOEXPERIMENT), verdict ENV-LIE + exact fix line, before any verify is judged (2026-09-12 00-35 §f) — DONE 2026-09-12: ambient-first probe against a synthetic json/v2 module, verdicts ok / FAIL ENV-LIE with the exact fix line / warn toolchain-gate (cmd/tq/doctor.go:432-520); live-proofed both ways, TestClassifyGoEnvProbe hermetic (round-13 T3)
- [ ] SystemNix tq-agent-pool unit gains `Environment=GOEXPERIMENT=jsonv2` (same treatment as agentPath) — ends the 5-window re-dispatch burn; rides the L45 input flip + deploy — BLOCKED: owner sudo on evo-x2 (2026-09-12 01-06 §g1)
- [ ] Ruling: TQ_RESULT contract schema — fields allowlist (no invented fields), record-vs-work sha semantics, verification-only attempt semantics — BLOCKED: owner contract call (2026-09-12 00-54/00-35 §g)
- [ ] Ruling: close-out report placement — file-per-window vs append-to-one-report (01-06 CONTRADICTS 00-54 §e3); implement the ruling in the closeout executor — BLOCKED: owner placement call (2026-09-12 01-06 §g2)
- [ ] Webui rulings: unknown `/project/{name}` behavior (404 vs empty-state) + per-project-vs-global budget display in filtered views — BLOCKED: owner UX calls (2026-09-12 01-47 §g1/§g2)
- [x] Webui UX batch (after the 01-47 rulings): project-chip totals + "all" reset link, drop redundant project column when filtered, chips overflow cap (2026-09-12 01-47 §f) — DONE 2026-09-12 round-13 T14 (mechanical items; the M62/M63 product rulings stay in the O4 package): ProjectSummary.Total + chip `name N · R/P/D` text, "all projects" reset chip when any filter is active, task table + board card drop the project cell/column under a single-project filter (taskHeaders + taskRows + boardCard agree), chips cap at 12 + "+N more projects"; templ generate + webui-css rebuilt; pinned by TestPerProjectUXBatch + TestVisibleProjectsCap
- [ ] AGENTS.md claims-with-citations convention: every filter-scope/behavior claim in reports and handoffs carries file:line or a test name (2026-09-12 01-47 §d3)

## Done-prompt window harvest (2026-09-12; sources: docs/status/2026-09-12_04-45_done-prompt-five-task-window.md + the 03-28/03-46 close-outs + round-13 residue; verified against code this pass; 8 stale rows closed above)

- [ ] Triage the RED lint-baseline gate at HEAD: 7 growth rows (executor mnd 20→24 + noctx 1→5; queue/sqlite varnamelen 32→33; worker mnd 7→8; root lll 37→38 + noctx 22→29 + varnamelen 9→10) — fix the noctx class for real, deliberate regen for mnd/varnamelen/lll with a policy note; this hard ci-local gate blocks the next full gate/push until then (verified live 2026-09-12 04:40; 04-45 report §d1) — DONE 2026-09-12 round-13: function-level attribution = `Store.ListWatermarks` varnamelen `e` (sqlite.go:1342, landed 05:02 via 5afd2b0 concurrent lineage); my session's only own-growth was payload_test.go lll (4 long lines, wrapped content-identical); the rest covered by the sanctioned deliberate regen (895de1e8, commit message carries per-module attribution); gate green at HEAD (820 findings, within baseline)
- [ ] `scripts/smoke/session-close.sh`: scratch-TQ_DB smoke — begin → footer commit → close → assert one review + one status + replay-safe second close; wire into ci-local (03-28 report §f7)
- [ ] Session trigger #1 — PreToolUse session registry: hook appends {id, cwd, last_seen} per tool call; a sweeper mints the close-out when no crush process owns the ID (03-28 §f1; design doc OQ3)
- [ ] Session trigger #2 — `tq crush` wrapper that calls `session close` on process exit, the no-hook fallback (03-28 §f2)
- [ ] Session trigger #3 — adopt crush #3146 SessionEnd when released: point the hook at `tq session close` (enqueue-only fits the ~2s budget) (03-28 §f3)
- [ ] Postgres session bridge: implement AppendFact on internal/queue/postgres + conformance coverage, or fail fast in `tq session` on postgres journals with a clear "bridge needs the sqlite journal" error (03-28 §f4/§g3; design doc OQ4)
- [ ] `tq session list` (opened-without-closed, crash recovery) + `tq session close --dry-run` previewing the attributed commits before enqueueing anything (03-28 §f8/f10)
- [ ] Stale-open session visibility in doctor/stats: sessions that began and never closed; never auto-close without a ruling (03-28 §f9)
- [ ] Session forensics: CloseDetail records the AllowDirty/RequireClean decision + review range; `tq facts --type session.opened,closed`; `tq show session:<id>` graceful degradation for the synthetic ID (03-28 §f14/f18/f19)
- [ ] Session volume in the dashboard: session-fact counts + an open-sessions lamp in `tq stats`/webui (03-28 §f20)
- [ ] Session-bridge safety pins: prune-stale must never cancel session-minted tasks (external-provenance guard test); sweeper-namespace cross-dedup (`review:session:<id>` never collides with sweeper `review:<task-id>`); trailer-parser edge tests (empty/repeated/whitespace footers) (03-28 §f15/f16/f17)
- [ ] Multi-repo session semantics: document one-close-per-(session,repo) or design multi-repo close (replay-dedup assumes one close today) (03-28 §f11)
- [ ] Session ergonomics: git ≥ 2.15 guard with an actionable message; zero-commit close prints the footer-convention hint (begin already does) (03-28 §f12/f13)
- [ ] gosec post-config scan over internal/session + the cmd/tq session call sites stays zero; extend the AGENTS.md gosec triage note with the new exec(git) sites (03-28 §f23)
- [ ] Dead-export audit re-run (scripts/check-dead-exports.sh, substring matching) over internal/session's exports (Trailer, SyntheticTaskID, CloseInput, CloseResult, GitScanner, …) (03-28 §f31)
- [ ] Session E2E dogfood proof: one REAL interactive session through begin→commits→close on this repo with the pool reviewing the minted task (03-28 §f28) — BLOCKED: spends API money on the real agent run (owner-gated spend)
- [ ] Mirror the git-identity sanitizer into ci.yml's release-gates step (ci.yml:106) or document the ci-local-only asymmetry as deliberate (03-46 §f2)
- [ ] Self-testing negative fixture in release-gates.sh: a temp copy with the `-c user.*` flags stripped must fail "Author identity unknown", so the identity-blind property can't rot silently (03-46 §f3)
- [ ] AGENTS.md one-liner near the smoke list: the release-gates smoke runs identity-blind via ci-local (`GIT_CONFIG_GLOBAL=/dev/null` + `user.useConfigOnly`) — so nobody "simplifies" it away (03-46 §f5)
- [ ] `tq show --commits`: surface footer-less daemon commits ADJACENT to a footer-bearing work commit as "folded here" instead of silent (the 147bd17 shape) (03-46 §f9)
- [x] lint-baseline.sh attribution aid: print which files carry the new findings per growth row (04-31 §f15) — DONE 2026-09-12 round-13: the --check failure path now lints each affected module once and lists the files per growing linter; pipefail-safe (`|| true` on the grep pipeline — first probe died silently on set -euo pipefail + no-match grep) and colon-strips the linter name; red-probed for real (forced growth row printed `internal/queue/sqlite/sqlite.go`), restored green after each probe
- [ ] Bank the gochecknoglobals shrink (root 9→7 after the twin-list retirement) in the same regen that resolves the baseline-triage item (04-31 §f14)
- [ ] Check whether scripts/lint-lll-changed.sh excludes `*_templ.go`: generated long lines make every templ regen a latent gate trip (04-31 §f16)
- [ ] Share the two hardcoded status oracles in internal/task status_test.go as one package-level test const feeding TestStatusTableExhaustive + TestAllStatuses (04-31 §f13)
- [ ] Commit-msg hook: require the Task-Queue-ID footer in the FINAL trailer block — mechanical prevention of the footer-before-attribution mistake (04-13 §d3; 04-31 §f8)
- [ ] Re-tag `internal/task` at the next release + bump the root require off v0.2.0 so proxy consumers receive `task.AllStatuses()` (04-31 §f3; rides the L124 public-API ruling)
- [ ] Run `./scripts/ci-local.sh` end-to-end on the current lineage before the next push — the nix + master-CI delta no recent window has covered; also surfaces the baseline triage (03-46 §f6)
- [x] CI proof gap: the last runner-green run (34661978016, 00:32Z) predates internal/session, the review-prompt fix, and the AllStatuses retirement — 26 local commits unproven; the first push after the baseline triage is the proof; watch test-windows for session.go git portability (verified 2026-09-12; 03-28 §f21) — CLOSED 2026-09-12: pushed and runner-proven — 2f70deb (treefmt un-red fix) run 34670079022 SUCCESS, then the full batch through e0dadb5 run 34670475702 SUCCESS (test + test-windows + govulncheck + gosec + test-postgres all green on a tree carrying internal/session, the review-prompt fix, AllStatuses retirement, and the round-13 webui work); test-windows exercised the session.go git portability concern
- [x] RELEASE.md: record the fixture-execution caveat — the round-12 T14 gates ran with a stubbed nix (sandbox limitation); the release-gates nix leg is runner-proven only (04-45 report §a5 verification residue) — DONE 2026-09-12 round-13 T16: caveat recorded as the second bullet of the "Tag ancestry" fixture-proof notes
- [ ] `tq tasks`: expose payload/verify content (`--json` payload field or `--verify-contains SUBSTR` filter) so stale-verify hygiene audits stop needing a `tq show` per task (09-39 report §e1)
- [ ] `tq doctor --hygiene` (or audit extension): encode the stale-queued-task check — no pending agent task carries a verify string matching known-stale patterns — so the f46-style audit is a gate, not an investigation (09-39 report §e3)
- [ ] Ruling: session-minted review/status tasks vs the daily budget — operator-class bypass or budget-counted (park when exhausted)? They mint even when the pool is paused today (design doc OQ2; 03-28 §g1; 04-45 §g1) — BLOCKED: owner budget-class ruling
- [ ] Ruling: daemon-fold attribution — teach the auto-commit daemon to carry `Crush-Session: <id>` footers from an env-carried session id (is the daemon yours to change; want tq's help?) vs a timestamp/author-range fallback in `tq session close` (fourth consecutive ask: 03-28/03-46/04-13/04-31 §g; 04-45 §g2) — BLOCKED: owner daemon/attribution ruling
- [ ] Ruling: duplicate-claim re-delivery (the 04-13/04-31 pair) — queue-side guard against re-dispatching a task whose terminal fact exists, or ratify the verification-only second window as the sanctioned shape and define what its TQ_RESULT may claim (04-31 §g1; 04-45 §g3/§d4) — BLOCKED: owner duplicate-claim policy
