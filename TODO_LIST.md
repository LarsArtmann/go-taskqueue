# TODO List

Short- and mid-term actionable work. Long-term direction lives in ROADMAP.md.

**This file is machine-consumed**: `tq harvest` turns every unchecked item
below into an agent task. Keep the `- [ ]` checkbox format, one item per
line — do not convert to tables. Mark done items `[x]` or delete them;
appending `— BLOCKED: <reason>` keeps an item out of the pool.

Items carry their evidence: a code path and/or the status report that filed
them (`docs/status/<date>_<slug>.md`, cited as `(19:33 report …)` etc.).

## High Impact

- [x] Write `scripts/ci-local.sh` replicating the full CI test-job sequence locally (vet → build → GOOS=windows → race tests → gofmt → advisory lint → harvest-parse guard → web UI smoke → doc-reference check, then `git add -A` + `nix build` + `nix flake check` on a fully tracked tree) and wire it into AGENTS.md + CONTRIBUTING.md as the pre-push gate (19:33 report f1 — a stale-tree nix check and unverified pushes caused the 2026-09-07 red-master incident)
- [x] Fix the papdashboard watermark regression: `Bridge.Run` returns the misleading "cannot read journal head" error when the context is already cancelled at entry because `startWatermark(ctx)` reads under `Run`'s ctx (`internal/bridge/papdashboard/papdashboard.go:112`); guard `ctx.Err()` after init and add a test that cancels ctx before `Run` (19:33 report d1/f2)
- [x] Add a flake check that executes the nix-built binary (`result/bin/tq --help`) so a swallowed build can never produce an empty-but-"successful" store path again (18:41 report f4, 19:33 report f4; `flake.nix` currently has only `checks.vendor-hash`) — done as `checks.binary-runs`, which references the package directly (not the `result` symlink) so `nix flake check` runs it in CI
- [x] Verify that advisory lint findings actually surface as CI annotations on green runs (`continue-on-error` + annotation caps may swallow them); if not, adopt reviewdog or a scoped-lint step (19:33 report b/f6) — done: adopted the scoped-lint option: `scripts/lint-annotations.sh` re-runs the same binary/config with `--new-from-rev` and emits findings on changed lines as `::warning` annotations, wired into ci.yml + ci-local.sh (reviewdog rejected: needs `checks:write`/`pull-requests:write` permission elevation and still caps unscoped); re-verified 2026-09-07 on green run 34157520457: the 5 scoped warnings surfaced as file-bound `[warning]` annotations, but 10 stale baseline `[failure]` annotations also leaked — not from golangci-lint (v2.13.2 emits zero annotation commands, confirmed locally with `GITHUB_ACTIONS=true` on a package with findings) but from the `go` problem matcher actions/setup-go registers, which scraped the advisory step's `file.go:line:col:` text output (410 lines matched, 10-per-step cap kept 10); fixed with `::remove-matcher owner=go::` in the advisory CI step so only scoped new findings annotate
- [ ] Cut v0.2.0: finalize the CHANGELOG `[Unreleased]`, release checklist, annotated tag, GitHub release, then run the nix-built binary as a smoke — additive changes only (web UI, pool hardening), fine for 0.x (19:49 report f2) — BLOCKED: owner go/no-go (note: v0.1.0 already shipped 2026-09-06; the 09-07 reports' "cut v0.1.0" lines were stale)
- [ ] Verify the CQA bridge against a live CQA API instance and fix contract drift (`internal/bridge/cqa` response shapes are httptest-informed guesses today); upgrade its FEATURES.md status after (plan C25) — BLOCKED: needs a live CQA instance URL + owner ID + token from the owner
- [x] Inventory the journal/tailer subscription surface (`internal/journal`, `internal/webui/tailer.go`) and document gaps vs the proposed `Subscribe(ctx, since Seq)` fact-stream contract before any implementation; confirm `2d5e729`'s bounded journal reads cannot break replay-from-seq (05:24 report d1/f1/f3 — the recommendation was designed without reading the Journal interface) — DONE 2026-09-08: inventory + gap analysis in `docs/planning/2026-09-08_journal-subscription-surface-inventory.md`; verdicts: `2d5e729` is safe for replay-from-seq (strictly-greater cursors, AUTOINCREMENT seq, no delete path, every bounded caller pages or is a feed render — the webui tailer's batch jump is change-signal-only and must never leak into fact delivery), the proposal's pull half already exists on `queue.Store` (`Facts(after, limit)` + `HeadSeq`), and `journal.Journal` is production-dead (split brain to resolve when Subscribe lands); real gaps are all push-side: no dispatcher, no slow-consumer policy, volatile watermarks, no Last-Event-ID↔Seq mapping
- [x] Enumerate what a persisted bridge watermark needs (storage shape: side table vs metadata fact; ack semantics; idempotent resend window) against the real `startWatermark` code in `internal/bridge/papdashboard` (05:24 report c/f9-f10 — the missed-incident gap is cross-process and framework-independent) — DONE 2026-09-08: design enumeration in `docs/planning/2026-09-08_persisted-bridge-watermark-design.md`; verdicts: side table wins (metadata fact rejected — pollutes the task-scoped journal, would punch the no-public-append invariant), checkpoint per drained batch after acceptance (never before; 4xx counts as acked; monotonic upsert guards regression), resend window bounded by batch cadence and absorbed by the seq-derived Idempotency-Key (caveat: assumes PapDashboard key retention outlives restarts); also enumerated: the volatile `alerted` map is the second half of the gap (derive the resolve correlation from `FactsForTask`, don't store it), `startWatermark` bootstrap/precedence (FromSeq > persisted > head; first run = head so existing deployments never see a history replay), and a 05:24 f11 correction (the cqa bridge is not a journal consumer — dedup keys already make it durable; watermark work is papdashboard-shaped); §7 is the implementation checklist for the f10 migration

## Medium Impact

- [x] Fix `scripts/smoke/papdashboard-e2e.sh` real-dashboard mode: the assertion loops grep `INGEST_LOG`, which only the stub writes, so `PAP_URL=…` mode always times out and fails — make the real mode verify against the dashboard (or split the script per mode) (19:49 report d1/f5)
- [x] Make `harvest.Audit` report per-repo scan failures instead of a silent `continue` (`internal/harvest/drift.go:100`), and fix the comment above it that claims parity with `Harvester.Run` which does not exist (19:49 report d2/f6)
- [x] Extract the remaining cmd/tq complexity hotspots the same way `main`/`cmdAudit` were fixed: `cmdHarvest` (17), `aggregateTop` (16), `cmdStats` (14), `cmdDLQ` (13) (19:33 report b/f9; advisory cyclop baseline) — DONE 2026-09-07: all four extracted (resolveHarvestRepos/printHarvestLines, tallyStats/printStats, rescueAllDead/listDead/printDLQ, indexTaskProjects/applyRunDurations/applyActiveDurations); `golangci-lint run cmd/tq/...` reports zero cyclop findings
- [x] Add CLI-level tests: golden-output test for the `tq audit` drift report, dispatch test for unknown-command/help exit codes, table tests for `splitRepos` (empty, spaces, trailing comma) (19:33 report d4/f10 — the CLI refactor shipped with zero CLI-level tests)
- [x] `tq worker --once`: run until the claimable queue is drained, then exit — parity with `tq agent-pool --once` for scripts and tests (18:34 report f22; the worker still has no one-shot mode) — DONE 2026-09-07: `worker --once` reuses the agent-pool drain loop (`hasClaimableWork`: exit when nothing in flight and no pending/due-soon tasks; other pools' claims and future NotBefore are left alone); smoke-verified drain-then-exit on a fresh DB
- [x] Nightly/weekly fuzz job (60s `FuzzParseRepo`) so corpus growth does not depend on session memory; commit interesting seeds into `testdata/fuzz` (18:00 report f21, 18:34 report f21) — DONE 2026-09-07: nightly `.github/workflows/fuzz.yml` runs `scripts/fuzz/nightly.sh` (60s campaign, hermetic GOCACHE — the corpus never lands on shared-cache mounts) and commits new seeds back to master; first batches committed (165 seeds, ~2.2M execs, zero findings), `MAX_SEEDS` caps the committed corpus at 1000
- [x] Run `nix flake check --all-systems` (darwin/aarch64 coverage) and smoke the nix-built binary through `scripts/smoke/webui.sh` — the smoke only ever exercises the `go build` binary today (19:33 report b/f15/f16) — DONE 2026-09-07: `--all-systems` failed at eval (nixpkgs 26.11 dropped x86_64-darwin while go-standard's default system list still carries it); pinned `go-standard.systems` to x86_64-linux/aarch64-linux/aarch64-darwin → eval now covers darwin/aarch64 and all checks pass (cross-system realisation still needs remote builders, eval coverage is what `--all-systems` provides); webui.sh gained a `TQ_BIN` override and the nix-built `result/bin/tq` passes the full smoke (stats/fragments/SSE)
- [x] Windows honesty: run the e2e/chaos suite on a real `windows-latest` runner or mark those tests `//go:build unix` so the GOOS=windows compile gate stops implying runtime coverage it does not have (19:49 report f15) — DONE 2026-09-07: chose the marker option; all three suites depend on `#!/bin/sh` stub binaries (and chaos on SIGKILL), so `//go:build unix` added to `internal/e2e/{e2e,chaos}_test.go` and `internal/harvest/e2e_test.go`; verified `GOOS=windows go list` excludes them (other harvest tests still compile) and `GOOS=windows go build/vet ./...` stay green
- [x] Write ADR: lifecycle & streaming library stance — distill the do/ro/cordis verdict (patterns now, cordis only at the plugin horizon, with trigger conditions) from the 05:24 report so the decision survives chat — DONE 2026-09-08: `docs/adr/0004-lifecycle-streaming-library-stance.md`; verdict persisted as five decisions (stdlib actor patterns + journal-as-bus now; do+ro over cordis if small libs are ever needed; cordis gated behind five triggers: plugin-era go/no-go, cordis stable v1, locally verified test suite, drain-invariant prototype behind a build tag, framework-independent plugin API); all cited repo facts re-verified at HEAD (main.go 1,269 lines, 5 signal sites, 14 defer-Close sites, no errgroup dep, cordis at v4.0.0-rc.9+48) and the report's unverified vendor claims labeled as such in the evidence ledger
- [x] Design spike: SSE `Last-Event-ID` ↔ journal Seq client-resume mapping — the webui twin of bridge watermarks (05:24 report f40) — DONE 2026-09-08: design in `docs/planning/2026-09-08_sse-last-event-id-resume-mapping.md`; verdicts: the id↔seq mapping already exists on the wire (each snapshot's closing event carries the journal seq) and snapshot-at-head IS the exact resume for a projection dashboard (per-fact replay rejected — every event is a full re-render; ADR-0003 amended to stop implying replay); Last-Event-ID's real value is reconnect-lag observability (`head − N`) plus the forward-compatible seam to a future per-fact stream (Subscribe NOT needed for the dashboard); no server-side persistence — the browser is the client watermark's store and garbage/beyond-head ids map to fresh-connect; found + scratch-test-verified the actual gap (the stream ignores the page's filter params) — queued as the next item
- [ ] Web UI: forward the page's filter query (`project`/`status`/`q`/`page`) to the EventSource URL (merged with the `token` param) so live ticks and reconnects re-render the FILTERED projection — `app.js` opens `/api/events` bare, so a filtered page's table is clobbered by the unfiltered one on the next tick; server needs zero changes (`parseFilter` already reads the params) (f40 spike §4, `docs/planning/2026-09-08_sse-last-event-id-resume-mapping.md`)
- [ ] Run the cordis Go test suite locally (`GOCACHE=/tmp/gocache`) and record verified coverage/race results as the real input to the plugin-era decision (05:24 report b/d2 — currently a vendor README claim)
- [ ] Web UI: surface agent-review verdicts — render an approve/request_changes badge plus findings (title + severity) from the `review` task's completion-fact detail in the task table and per-task trail (`internal/webui` components already render fact detail; the review detail JSON is `executor.ReviewResult`) (agent-reviews ship, 2026-09-08)

## Lower Impact

- [ ] Add `TestCheckProjectsDir` unit tests (the `--projects-dir /` and `$HOME` refusal guard has manual smokes but no test in CI) (19:49 report f7)
- [ ] Review-watermark catch-up: the `--review` sweeper starts at journal head, so agent tasks completed while no `--review` pool was running never get reviewed (documented papdashboard-bridge semantics); evaluate a persisted watermark or a time-bounded lookback (`CompletedSince` needs a facts-time index — same storage-shape question as the bridge watermark item in High Impact) (agent-reviews ship, 2026-09-08)
- [ ] D91-lite: capture `crush --version` in the pool startup line and warn when the binary is missing, so flag-contract drift surfaces early (19:49 report f19, seeds D91)
- [ ] Wire dprint into the treefmt gate (markdown/json) or formally decide that docs formatting stays manual — today dprint is in the devShell but un-gated, so docs drift freely (19:33 report b/f12)
- [ ] Sidecar retention: `--log-dir-max-age` / size cap for `TQ_LOG_DIR` output logs, and document that sidecars are plaintext and may contain repo paths (19:49 report f20)
- [x] Fuzz `ExtractResultPayload` (regex over untrusted agent output) with a seed corpus (19:49 report f18) — DONE 2026-09-08: `FuzzExtractResultPayload` (`internal/executor/result_fuzz_test.go`; never-panic, deterministic, ok⇒marker-line properties) plus a 183-input committed corpus under `internal/executor/testdata/fuzz/FuzzExtractResultPayload`; 60s campaign clean (~6.3M execs, zero findings), every seed runs as a test case on each `go test`
- [x] `tq audit --json` plus `--todo-file`/`--type`/`--max-attempts` flags for parity with `tq harvest` (19:49 report f17) — DONE 2026-09-08: `cmdAudit` takes `--json` (indented `DriftResult`, golden-pinned by `TestPrintDriftJSONGolden`), `--todo-file`, `--type` (filters the audited queue tasks AND the catch-up task type), and `--max-attempts` (catch-up attempt budget, default 2); CLI-smoke-verified end-to-end (catch-up landed with the given maxAttempts)
- [x] Free-port selection in `scripts/smoke/webui.sh` (fixed port 8095 collides on busy machines) (18:41 report f11) — DONE 2026-09-08: default mode derives a kernel-chosen ephemeral port (`free_port()` python3 helper) just before `serve` binds (small pick-to-bind race window); `WEBUI_SMOKE_PORT` still pins an explicit port and the smoke fails loudly when a pinned port is taken; verified with 8095 held busy (smoke passed on a derived port) and with a pinned free port
- [x] Request-logging option for `tq serve` (`--verbose` or an slog handler) — the serve process currently logs only tailer/hub/shutdown failures (18:41 report f12) — DONE 2026-09-08: `tq serve --verbose` wires `webui.Config.RequestLog`; a middleware logs method/path/status/duration per request via slog (Info, stderr) and forwards `Flush` so SSE streaming is unchanged (SSE connections log once, on stream close); off by default; covered by `TestRequestLoggingEnabled`/`OffByDefault`/`KeepsSSEStreaming` and CLI-smoke-verified
- [x] `tq serve` auth for non-localhost binds: token auth (or basic) + docs, per plan W16 — the dashboard is now bound to the LAN (read-only by construction, ADR-0003) with zero auth, so anyone on the LAN sees all task payloads and error tails — DONE 2026-09-08: `--auth-token`/`TQ_SERVE_TOKEN` with constant-time middleware on every route (`Authorization: Bearer` or `?token=`, needed for EventSource; `--verbose` logs redact the token); non-loopback binds (incl. `:port`, non-`localhost` hostnames) are default-deny (`webui.Config.Validate`, enforced by `Server.Run` + CLI); ADR-0003 amended, README/FEATURES/CHANGELOG/AGENTS updated, matrix tests in `internal/webui/auth_test.go`, smoke-asserted in `scripts/smoke/webui.sh` (plan F11–F16; owner action: restart any LAN serve with a token)
