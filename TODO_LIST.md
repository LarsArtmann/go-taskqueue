# TODO List

Short- and mid-term actionable work. Long-term direction lives in ROADMAP.md.

**This file is machine-consumed**: `tq harvest` turns every unchecked item
below into an agent task. Keep the `- [ ]` checkbox format, one item per
line — do not convert to tables. DONE ITEMS ARE DELETED on closure
(owner ruling 2026-09-14) — a completed item lives in CHANGELOG.md, never
here as a checked row; `--prune-stale` also cancels pending tasks whose
item text is gone from the file, so deletion is the safe form. Appending
`— BLOCKED: <reason>` keeps an item out of the pool.

Items carry their evidence: a code path and/or the status report that filed
them (`docs/status/<date>_<slug>.md`). Completed items live in CHANGELOG.md,
not here.

## Fleet / deploy

- [ ] SystemNix host-side deploy + round-9 cutover (sudo-gated, user-run): `nix run .#deploy` on evo-x2 starts tq-agent-pool/tq-serve/tq-storage-dir/tq-bootstrap (all wiring shipped 2026-09-08: rev-pinned git+file input 28a8ae4, house module `modules/nixos/services/tq-agent-pool.nix`, ports.tq 8100, tq.home.lan vHost + DNS, Gatus checks, btrbk-pool subvolume, dedicated sops bridge template, post-deploy smoke); then the one-time manual-pool cutover in SystemNix `docs/services/tq.md` (stop the `/tmp/tq` processes — DONE 2026-09-10, optionally copy the dogfood tasks.db onto the pool journal). The origin push is done — flip the SystemNix input to `github:LarsArtmann/go-taskqueue?ref=master` — 2026-09-10: the deployed pool was found DEAD (every tick skipped all repos: bare repo names resolved via service cwd + no git/go/crush on the service PATH + silent scan-fail logging); all three fixed on master, so the input flip + redeploy is what brings the Flash pool to life — BLOCKED: owner-run (sudo on evo-x2 — an agent cannot execute this; pool must not pick it up)

## Owner-blocked decisions (BLOCKED items are skipped by the pool)

- [ ] Push authorization (updated 2026-09-10): master + root v0.1.0/v0.2.0 + the five round-1 module tags are now on the remote (owner pushed); still latent: the two backend tags `internal/queue/{sqlite,postgres}/v0.2.0` — after their push run the per-module proxy check (`go list -m -versions`) + the real clean-room `go install` (docs/status/archived/2026-09-09_23-47_round2-queue-backend-split.md f1/f13); post-split executor/queue content waits for the next release's module tags (published v0.2.0 tags are immutable; restored after a mistaken local re-cut) — 2026-09-10 06:25: both backend tags verified on the remote (`git ls-remote`); the remaining proxy check + clean-room install moved to the Fullcore-window section below — BLOCKED: owner push authorization (never push without it)
- [ ] `internal/consumer`: wire into `tq serve` journal tailing or delete the ghost package (ADR-0009 dispatcher, zero production importers; write the ADR outcome either way) (23:47 f2/g1) — BLOCKED: owner intent call
- [ ] Decide the three near-identical interfaces (`consumer.Source`, `budget.FactSource`, `papdashboard.FactSource`) — one interface or documented duplication (23:47 f4) — BLOCKED: owner architecture decision
- [ ] Postgres CLI store wiring (`--store postgres://…` on worker/serve/agent-pool): gives queue/postgres its consumer; root re-adds pgx; sequence BEFORE any public-API promotion (23:47 f5/g3) — BLOCKED: owner release-timing call (v0.3?)
- [ ] Dependabot/renovate policy for the 8-module tree (23:47 f33) — BLOCKED: owner policy decision

## go-cqrs-lite platform adoption (ADR-0019, owner ruling 2026-09-22 — serialize S1→S4)

- [x] ADR-0019 S1 spike (sqlite): new module implementing tq's `queue.Store` over `queue/sqlite/v4.0.0` (tagged + pushed; `scripts/new-module.sh` scaffold) with the tq extras (RecordAnswer/questions, PriorityScores, CountFacts/FactsSince/LastFacts, ProjectCounts) as same-DB companion tables; run tq's existing sqlite suite against it and report every divergence (token- vs executor-string finalizes, fact vocabulary, heartbeat facts) in a status report citing docs/adr/0019-go-cqrs-lite-platform-adoption.md §S1
- [ ] sqlitev4 adapter: reset `last_error` on Complete (tq parity — tq's sqlite clears it, the upstream engine leaves the stale error on a completed task; divergence D1 of the 16-28 report) — either adapter-side or as the upstream fix
- [ ] Lint-baseline regen covering the spike modules: `sqlitev4` + `cqrsqlite` have ZERO rows in `.golangci-baseline.txt`, so `scripts/lint-baseline.sh --check` is structurally blind to them (close-out 16-36 §c2, grep-confirmed 16-46); run the gate, then regen with triage notes (clean golangci cache first, AGENTS.md ruling)
- [ ] ADR-0019 S1 spike (postgres): same contract over `queue/postgres/v4.0.0`, judged by the TQ_TEST_POSTGRES suite; both spikes feed one decision memo
- [ ] ADR-0019 S1 decision memo: per tq-extra surface choose upstream-grown vs companion-table (prefer upstream; upstream ratification-memo pipeline is the channel), pick the tq-fact append path (upstream escape vs companion journal), and sketch the replay tool; verdict appended to docs/adr/0019 or a docs/planning/ doc
- [ ] ADR-0019 S1 replay tool: fact-journal → fresh engine store replay verified by projection equality (StatusCounts, per-task fact tails, DLQ contents, watermarks) — part of S1's definition of done (docs/adr/0019 §Data migration)
- [ ] ADR-0019 S1 flip: make the queue/v4-backed store the default sqlite+postgres store, worker finalizes move to claim tokens, facades/vendorHash follow (ADR-0016 require+replace rules; `scripts/check-go-mods.sh` + the nix vendorHash fast gate)
- [ ] ADR-0019 S2: unify the journal on upstream `facts.Fact` (open FactType) with tq-specific fact constants; re-point `internal/journal/cqrs`, webui tailer, sweepers, bridges; facts-in-same-tx becomes engine-enforced
- [ ] ADR-0019 S3: `tq serve`/`tq stats`/httpapi read side onto metaengine Store collections (planned tables) + `Watcher`/`ServeSSE` live fragments replacing the hand tailer fan-out; `TestRoutesAreReadOnly` + health-CSP pins stay green
- [ ] ADR-0019 S4: compose runtime (store, executors, sweepers, bridges) via `system/` DomainConfig; DELETE the hand-rolled sqlite/postgres engines + mirrored suites once proven in dogfood (the 12 art-dupl mirror clone groups as of 2026-09-23 die here)
- [ ] ADR-0019 dogfood cutover: replay `/mnt/pool/services/tq/tq.db` into the new engine store, verify projection equality, swap `TQ_DB` in the systemd units — BLOCKED: owner-run (production journal; never without explicit owner go)

## Dogfood round (harvested from docs/status/2026-09-10_02-00 self-review §f)

- [ ] `tq pool-health`: one-shot summarizing per-repo skip streaks + last harvest activity from the journal (liveness ≠ process up) (02:00 f27)

- [ ] Verify the CQA bridge against a live CQA API instance and fix contract drift (`internal/bridge/cqa` response shapes are httptest-informed guesses today); upgrade its FEATURES.md status after (plan C25) — BLOCKED: needs a live CQA instance URL + owner ID + token from the owner

## Window f20–f24 follow-ups (harvested from docs/status/2026-09-10_04-09, verified 2026-09-10)

- [ ] gosec gating: once a suppression config encodes the triage, should gosec become a blocking required check or stay advisory permanently? — BLOCKED: owner gate-vs-advisory call on the security-scan jobs
- [ ] Pool policy when master CI is red: refuse agent DONE verdicts (fix-forward or park required) vs report-only disclosure of the red state — BLOCKED: needs a pool completion-contract decision

## Fullcore-window follow-ups (harvested from docs/status/2026-09-10_06-25, verified 2026-09-10)

- [ ] Closeout-report placement: every task writes `docs/status/<ts>_task-<id>.md` — root floods at ~10 tasks/day; root vs `docs/status/tasks/` subdir vs write-but-sample-index — BLOCKED: owner volume/placement call (05-30 report g1)
- [ ] Status/closeout TODO-append caps: reports may append up to ~50 items each and the backlog outgrows the 60/day enqueue drain — cap appends (~10), raise the budget, or let it grow? — BLOCKED: owner backlog-policy call (05-30 report g2)
- [ ] Ticket 000001a08915cf6f1c872a00bb18a4f9e326 close-out: its commit 20dafb9 carries a phantom footer (`…0ca7ae2`, zero commits in git) and the content landed via upstream twin 20dafb9 — mark complete queue-side and decide the dual-footer/disposition convention for review-fix and rebase-dropped commits — BLOCKED: owner queue-convention call (06:25 report g1)
- [ ] Master push authorization: master moved 05:45 + 06:03 today while AGENTS.md says agents never push — were those owner-run, and is autonomous push now sanctioned for agent sessions? — BLOCKED: owner push-policy answer (06:25 report g2)
- [ ] Module-fetch trust for the nix gate: if the runner FOD divergence is proxy/fetch-content drift, vendor modules (pinned fetch, trade repo size for gate determinism) vs keep proxy fetches and investigate per incident — BLOCKED: owner hermeticity call (06:25 report g3)

## Window follow-ups (harvested from docs/status/2026-09-10_08-25, verified 2026-09-10)

- [ ] Canonical ID for the f26 cluster: the release-doc work exists under three IDs (`a3864d95` original commit + 06-55 report, `b141f022` reviewer-assigned on 41b817b + 07-49 footer, `c3919ce4` 07-49 report filename) — which is the queue's canonical entry, and should the others be merged or marked duplicates queue-side? — BLOCKED: owner queue-convention call (08:25 report g3)

## Review-window follow-ups (harvested from the 07:49/08:32/08:39/08:42/08:47 close-outs, verified 2026-09-10 09:13)

- [ ] CHANGELOG policy for non-released-boundary changes: do `internal/`-only renames and same-day introduce+fix regressions (never crossing a release; the pool deploys master as a rolling release) get entries, or is [Unreleased] net-since-last-release only? This pass added one combined entry for the two rename-leak fixes and none for the ExitError rename — confirm or overrule — BLOCKED: owner changelog-policy call (08:32 g1; 08:42 g1)
- [ ] CI-time budget for the disk-derived module loops: what threshold should the f49 "tune if it dominates" item optimize against (e.g. lint step ≤ N minutes)? — BLOCKED: owner budget number (08:47 g3)
- [ ] TODO-item accuracy bar: should the harvester verify each item against code at harvest time (f30's wrong package + wrong version-window claim cost a day of deferral across seven reports), or is pickup-time verification by the executing agent the accepted contract? — BLOCKED: owner process call (08:32 g3)
- [ ] Enable `--allow-writes` on the deployed `tq-serve` (SystemNix module flag) so DLQ rescue/cancel work from the WebUI — the live 127.0.0.1:8100 instance runs read-only (no writes-ENABLED banner, no dead-task buttons), so the 21 dead tasks incl. `000001a08edf` need CLI `tq dlq --rescue` today; rescue AFTER the rate-limit-fix input flip so a rescued task doesn't re-burn attempts on 429s (13:29 report §b2/f1/f2) — BLOCKED: SystemNix service config + deploy are owner-run

## Docs-health harvest (2026-09-11 evening; sources: 16-00/15-39/15-10/13-29 reports + task-closeout residue; deduped against existing rows)

- [ ] Extend ci-local transient-retry wrapper to the smokes + add a durable self-test pin for the retry loop (01-46 report f1/f2; smokes re-run whole up to 3× needs a runtime-budget ruling first) — self-test pin SHIPPED 2026-09-16: `scripts/check-transient-retry.sh` sed-extracts the SHIPPED helper (marker-guarded so a rotted range fails loudly), pins defaults 45s×3 + heal/exhaust/argument-forwarding/`TRANSIENT_MAX_POLLS=0` semantics, wired as a ci-local step before vet (negative-tested: catches marker rot + poll-counter drift) — BLOCKED: smoke half awaits the owner runtime-budget ruling (01-46 §g3: whole re-run ×3 vs two-phase tree-quiet probe)

## Round-13 planning additions (harvested from docs/status/2026-09-12_01-47 + the round-12 T17 tail; verified 2026-09-12)

- [ ] SystemNix tq-agent-pool unit gains `Environment=GOEXPERIMENT=jsonv2` (same treatment as agentPath) — ends the 5-window re-dispatch burn; rides the L45 input flip + deploy — BLOCKED: owner sudo on evo-x2 (2026-09-12 01-06 §g1)
- [ ] Ruling: close-out report placement — file-per-window vs append-to-one-report (01-06 CONTRADICTS 00-54 §e3); implement the ruling in the closeout executor — BLOCKED: owner placement call (2026-09-12 01-06 §g2)
- [ ] Webui rulings: unknown `/project/{name}` behavior (404 vs empty-state) + per-project-vs-global budget display in filtered views — BLOCKED: owner UX calls (2026-09-12 01-47 §g1/§g2)

## Done-prompt window harvest (2026-09-12; sources: docs/status/archived/2026-09-12_04-45_done-prompt-five-task-window.md + the 03-28/03-46 close-outs + round-13 residue; verified against code this pass; 8 stale rows closed above)

- [ ] Session trigger #3 — adopt crush #3146 SessionEnd when released: point the hook at `tq session close` (enqueue-only fits the ~2s budget) (03-28 §f3) — BLOCKED: crush #3146 still OPEN upstream (merge conflicts as of 2026-09-17), absent from v0.95.0, and installed v0.94.2 has no SessionEnd hook to adopt; re-checked 2026-09-17 (second + third verify windows): PR still `{"merged":false,"state":"open"}`, v0.95.0 is still the latest release (no hook/SessionEnd/#3146 in it), installed crush v0.94.2 unchanged; re-check with `gh api repos/charmbracelet/crush/pulls/3146 --jq .merged`
- [ ] check-gosec.sh: cover cmd/tq (for-each-module.sh omits it, so the gate and the CI gosec loop both skip the CLI module; devmod shim or an owner ruling that CLI gosec stays CI-only) (02-52 gosec-gate report §b) — BLOCKED: owner ruling on CLI gosec coverage (03-07 §g3)
- [ ] Converge ci.yml's hand-rolled per-module gosec loop onto scripts/check-gosec.sh (settles whether the cd-without-GOWORK loop hits the Files:0 silent skip on runners; counter-risk: a broken script would then silence both layers) — BLOCKED: owner ruling on CI independence (02-52 report §g q3)
- [ ] Drop the msg-filter backup refs (refs/original + any filter-branch backup) once the healed lineage is pushed — AGENTS.md history policy keeps them "until the push lands" (heal d035911/ca514a8 local-only, origin/master 34 commits behind as of 2026-09-19 04:2x); the DROP itself is a destructive git op — BLOCKED: owner push of the healed lineage + owner ruling on ref deletion

## Priority-system follow-ups (harvested from the 2026-09-12 T27-T38 windows; reports 15-43 + 16-28; verified against code at mint time)

- [ ] Usage-key pin parity: extend the executor-level exact-key-set pin to ReviewResult + StatusResult (same real-type marshal table as TestPrioritizeResultUsageKeysMatchAgentResult, prioritize_test.go:101) so all four result types carry the exact-marshal pin, not only budget_test's parse-sum check — CLAIMED FILED by the 04-40 report §f1 but absent from this file at HEAD 0ce62ad (git-verified by the 04-46 window; likely the same mangled insertion that prefixed the Score-cache row with `---`), restored 2026-09-21 04:55 with this provenance — BLOCKED: owner granularity ruling (04-40 §g1) decides close-vs-keep; the pin extension itself is mechanical once unblocked (04-40 report f1; internal/executor review_test.go/status_test.go)
- [ ] Publish the v0.3.0 GitHub Release (`gh release create v0.3.0` with the CHANGELOG section, or the `release.sh --push` resume path) + spot-check pkg.go.dev renders the 7 facade modules — HALF DONE 2026-09-13: the release was created by the tag push (full CHANGELOG body) and is now published/Latest (was Pre-release; `gh release edit v0.3.0 --prerelease=false --latest`); all 7 facades resolve v0.3.0 from proxy.golang.org + clean-room enqueue pass (ALL-FACADES-OK); REMAINING: pkg.go.dev render check (still 404 as of 09:0x — crawl lag after the tag push, proxy hit usually triggers it; re-check and treat persistent 404 as a blocker to investigate)
- [ ] Ruling: public `journal/cqrs` facade or internal-only? ADR-0016's seven facades leave the PROPRIETARY go-cqrs-lite seam on the `internal/` path, so the external ecosystem consumers the adapter names cannot import it — BLOCKED: owner facade-scope call (08-00 §g1, 08-21 §g1)
- [ ] Ruling: may an agent patch `cqrs-lint` rules in the owner's local go-cqrs-lite repo? Evidence scoped for three factual fixes (A014 stale deprecation claim, D013 default-blindness, V006 lockstep-policy assumption) — BLOCKED: owner tool-policy call (08-21 §g3)
- [ ] Enable `--prioritize` on the dogfood pool — gated on the one-batch live scorer cost measurement (T28: ONE batch against a scratch journal = real API spend) recording tokens + wall time; the flag, sweeper, and budget gating are shipped and default-OFF (16-28 report g3; internal/prioritize, cmd/tq agentpool) — BLOCKED: owner spend go/no-go (real money)
- [ ] Post-pilot calibration of the keyword table + marker ladder from real claim order (T34 feedback loop over review verdicts × score source) and re-check G1 sizing (2000 items / 50 repos) against the observed drain rate — needs the pilot's first window of data (16-28 report f6/f7) — BLOCKED: owner pilot start (SystemNix proposal at docs/planning/2026-09-12_priority-system-dogfood-pilot.md)
- [x] Dead-export heuristic noise: extend scripts/check-dead-exports.sh with an advisory annotation marking symbols that appear only as return/parameter types of live exports — the false-positive class where 7/7 internal/session flags landed in the 2026-09-21 re-run (RegistryEntry, SweepOutcome, CloseResult, … all structurally alive behind Sweep/Close); keeps the audit raw but cuts manual triage per re-run (found by task 000001a0c3658a96, docs/status/2026-09-21_12-01_task-000001a0c3658a961763c57a159acbb423b1.md)
- [ ] Session E2E dogfood proof: one REAL interactive session through begin→commits→close on this repo with the pool reviewing the minted task (03-28 §f28) — BLOCKED: spends API money on the real agent run (owner-gated spend)
- [x] Mirror the git-identity sanitizer into ci.yml's release-gates step (ci.yml:106) or document the ci-local-only asymmetry as deliberate (03-46 §f2) — DONE: env-mirrored (GIT_CONFIG_GLOBAL=/dev/null + user.useConfigOnly) on the ci.yml step; smoke green under the sanitizer, YAML parsed
- [x] Investigate why task 000001a0c3658a96 re-dispatched 3x after its DONE row + 3 cited reports (TODO row 179 closed 11-58, windows 12-01/12-05/12-11): trace the queue journal for the re-enqueue/re-mint source — harvest rewording, retry ladder, or manual re-enqueue (the 14-01 reworded-duplicate loop class)? ANSWERED (22-30 report): single enqueue, dedup held — the 3 dispatches are the retry ladder over the known vendor-gofmt verify defect, each attempt completing its work before the gate kill (found by task 000001a0c3658a96, docs/status/archived/2026-09-21_12-11_task-000001a0c3658a961763c57a159acbb423b1.md)
- [x] Dead-export audit webui flags recur every run (ErrTokenRequiredOnLAN, HealthDashboardPath, HealthSSEPath): substring-verify each against real call sites; annotate same as the internal/session class or file a prune if genuinely dead (found by task 000001a0c3658a96, docs/status/archived/2026-09-21_12-11_task-000001a0c3658a961763c57a159acbb423b1.md) — DONE 2026-09-22: all three verified live via in-package call sites (ErrTokenRequiredOnLAN returned at internal/webui/auth.go:39 + errors.Is in auth_test.go:41/60; HealthDashboardPath routed at webui.go:169 + health.go:298; HealthSSEPath routed at webui.go:170 + health.go:299) — not dead, just invisible to the outside-package search; annotated-keep class added to scripts/check-dead-exports.sh (summary now reports "3 annotated keep", audit re-run confirms zero dead for these)
- [x] Fold the repeat-dispatch check into scripts/session-start.sh: when a task ID argument is given, grep docs/status/ for that ID and print prior windows + DONE-row state, so turn-1 duplicate detection is mechanical instead of memory-dependent (found by task 000001a0c3658a96, docs/status/archived/2026-09-21_12-11_task-000001a0c3658a961763c57a159acbb423b1.md) — DONE 2026-09-22: session-start.sh now lists prior windows and DONE-row hits for each ID argument; verified against live reports (000001a0c3658a96 surfaces 10 prior windows + 2 DONE mentions) and shellcheck clean via check-script-syntax.sh
- [x] Self-testing negative fixture in release-gates.sh: a temp copy with the `-c user.*` flags stripped must fail "Author identity unknown", so the identity-blind property can't rot silently (03-46 §f3) — DONE: the smoke self-tests a stripped commit under the ci-local sanitizer (GIT_CONFIG_GLOBAL=/dev/null + user.useConfigOnly) and fails unless git answers "Author identity unknown"; a bare stripped commit locally SUCCEEDS via autodetection, proving the sanitizer is what enforces blindness (release-gates smoke green)
- [x] AGENTS.md one-liner near the smoke list: the release-gates smoke runs identity-blind via ci-local (`GIT_CONFIG_GLOBAL=/dev/null` + `user.useConfigOnly`) — so nobody "simplifies" it away (03-46 §f5) — DONE: smoke-list entry extended with the identity-blind env, the bare-/dev/null-is-inert caveat, and the self-test fixture pointer (release-gates smoke green under the sanitizer, battery build+vet rc=0)
- [ ] `tq show --commits`: surface footer-less daemon commits ADJACENT to a footer-bearing work commit as "folded here" instead of silent (the 147bd17 shape) (03-46 §f9)
- [ ] Bank the gochecknoglobals shrink (root 9→7 after the twin-list retirement) in the same regen that resolves the baseline-triage item (04-31 §f14)
- [ ] Check whether scripts/lint-lll-changed.sh excludes `*_templ.go`: generated long lines make every templ regen a latent gate trip (04-31 §f16)
- [ ] Share the two hardcoded status oracles in internal/task status_test.go as one package-level test const feeding TestStatusTableExhaustive + TestAllStatuses (04-31 §f13)
- [ ] Commit-msg hook: require the Task-Queue-ID footer in the FINAL trailer block — mechanical prevention of the footer-before-attribution mistake (04-13 §d3; 04-31 §f8)
- [ ] Re-tag `internal/task` at the next release + bump the root require off v0.2.0 so proxy consumers receive `task.AllStatuses()` (04-31 §f3; rides the L124 public-API ruling)
- [ ] Run `./scripts/ci-local.sh` end-to-end on the current lineage before the next push — the nix + master-CI delta no recent window has covered; also surfaces the baseline triage (03-46 §f6)
- [ ] `tq tasks`: expose payload/verify content (`--json` payload field or `--verify-contains SUBSTR` filter) so stale-verify hygiene audits stop needing a `tq show` per task (09-39 report §e1)
- [ ] `tq doctor --hygiene` (or audit extension): encode the stale-queued-task check — no pending agent task carries a verify string matching known-stale patterns — so the f46-style audit is a gate, not an investigation (09-39 report §e3)
- [ ] Ruling: session-minted review/status tasks vs the daily budget — operator-class bypass or budget-counted (park when exhausted)? They mint even when the pool is paused today (design doc OQ2; 03-28 §g1; 04-45 §g1) — BLOCKED: owner budget-class ruling
- [ ] Ruling: daemon-fold attribution — teach the auto-commit daemon to carry `Crush-Session: <id>` footers from an env-carried session id (is the daemon yours to change; want tq's help?) vs a timestamp/author-range fallback in `tq session close` (fourth consecutive ask: 03-28/03-46/04-13/04-31 §g; 04-45 §g2) — BLOCKED: owner daemon/attribution ruling
- [ ] Ruling: duplicate-claim re-delivery (the 04-13/04-31 pair) — queue-side guard against re-dispatching a task whose terminal fact exists, or ratify the verification-only second window as the sanctioned shape and define what its verdict may claim (04-31 §g1; 04-45 §g3/§d4; the TQ_RESULT half died 2026-09-14 with derivation — a no-op re-dispatch now derives zero footer commits, no ruling needed; only the re-delivery guard itself is still open) — BLOCKED: owner duplicate-claim policy
- [ ] Webui detail: render the review card's `full agent output` log-path line like the other three result cards carry — ReviewResult.LogPath is recorded and the agent/status/prioritize cards render theirs; the review card omits it (2026-09-21 09-52 report §e5; internal/webui fragments.templ)
- [ ] Webui table/detail parity for agent outcomes: completed agent tasks get a detail "agent run" card since 74fb357 but NO table badge, and loadSnapshot populates no AgentResults — decide the badge (e.g. "N commits") and wire loadSnapshot + taskRows in the same change if yes (2026-09-21 09-52 report §b1/§g2; internal/webui)
- [ ] Budget projection scope: DLQ autopsy runs are paid agent turns but dlqfix results carry no Session* fields, so their spend never joins the budget token projection — verify the field gap, extend the result type + deriveOutcome + the shared parse if ruled in (2026-09-21 09-52 report §c/§g3; internal/executor/dlqfix.go, internal/budget)
- [ ] Verify the `tq api` surface mirrors the webui's new agent/review usage rendering (agent result + review usage in the JSON, or document the API as queue-metadata-only) (2026-09-21 09-52 report §c; internal/httpapi)
- [ ] CHANGELOG/FEATURES entries for the agent-run card + review-card usage line (74fb357): CHANGELOG [Unreleased]/Added sentence + FEATURES usage-rendering row extension, docs-only (2026-09-21 09-52 report §b2; rides the row-336 placement ruling) — BLOCKED: owner duplicate-claim policy

## Docs-health harvest (2026-09-14 archive sweep; sources: triaged KEEP-OPEN reports, deduped against existing rows, claims spot-verified at HEAD)

- [ ] Dead-pool alert lost on transient notify failure: `deadPoolDetector.observe` sets `alerted = true` BEFORE the notify result (cmd/tq/agentpool.go:468) — a transient post failure burns the streak's only alert; set the flag from the notify outcome (06-06 report §b1, re-confirmed by the 06-11/06-14 re-dispatches; docs/status/2026-09-11_06-06_task-000001a08e967ebc090a3cd706280ed4d659.md)
- [ ] Confidentiality gate for outbound artifacts: reports/PR drafts are swept for the private evaluator's identity class before leaving the repo (10-33 retro §f4 — the incident class, gate still unlanded; AGENTS.md grep = zero hits); the proxy-zip removal + history rewrite half is owner-gated (§f1/f2) — the gate half is agent-executable, land it and leave the owner half annotated — BLOCKED: owner call on the proxy/history half (docs/status/2026-09-13_10-33_confidentiality-incident-retro.md)
- [ ] Crush-integration batch: `tq doctor` crush check (binary present, version floor, xhigh managed-block pin) + `tq bootstrap` writes xhigh by default; upstream misleading "does not support reasoning effort" error is an issue candidate (12-23 report §f; docs/status/2026-09-14_12-23_crush-releases-integration-assessment.md)
- [ ] templ-components sweep P0 swap-guard batch (activeElement/details active-swap guard in app.js) + RelativeTime/Pagination/filter-submit UX items from the webui UX audit — harvest was explicitly pending owner (§b3) — minting the mechanical swap-guard here so it is executable — BLOCKED: owner sign-off on the UX batch scope (docs/status/2026-09-14_11-41_templ-components-deep-dive-webui-ux-audit.md)
- [ ] Facade release tail batch: clean-room install check moved INTO release gates (pre-tag), T5 out-of-tree consumer CI job, facadeparity in-repo self-test, VERSION-SURFACES.md refreshed to 0.3.0 values, FEATURES/CHANGELOG rows for `tq audit --journal` + check-features-ci gate, drift-audit postgres parity, `scripts/check-pkg-proxy.sh` (09-06/09-14/10-27 reports §f; docs/status/2026-09-13_09-14_brutal-self-review-v030-release.md)
- [ ] DONE-on-arrival dispatch dedup: queue-side done-guard (terminal fact exists → refuse re-dispatch) + dispatcher dedup — the 5th same-ID burn (11-31 report §f; relates to the BLOCKED duplicate-claim ruling row above) (docs/status/archived/2026-09-14_11-31_sse-flake-redispatch-and-checkout-race.md)
- [ ] internal/e2e race-runtime vs the task verify gate's headroom: attempt 1 of the §f4 task died in verify with e2e killed at 240.029s ("ran too long (4m0s)", raw SIGQUIT dump, no panic/running-tests lines in the executor tail) under a gate pinned `-timeout 180s`, while every other package passed and the SAME tree passed `-timeout 300s` in the 02-32 battery — profile/trim e2e (TestMain's build-tq.sh + subprocess scenarios are the long pole) and reconcile the 4m0s-vs-180s mechanics; a `.tq-verify` timeout-cap change is outside agent reach and stays out of scope here (02-53 report §a/§f; log: ~/.local/state/tq/logs/000001a0c152893609690d1585860fe22d0f.verify-failure.log)
- [ ] Verify-gate gofmt stage is STRUCTURALLY red on this host: `test -z "$(gofmt -l .)"` (the repo `.tq-verify` tail and every bootstrap-minted verify) walks the GITIGNORED `vendor/` tree (root builds need it since the 2026-09-17 vendor arc, .gitignore:63) and vendored third-party code is never gofmt-clean — the gate returns rc=1 with EVERY package ok, deterministic since vendor/ materialized: attempt 3 of the §f4 task reproduced it (~03:00, all ok incl. e2e 181.6s; `gofmt -l .` lists ONLY vendor files while `git ls-files '*.go'` is clean), and dead tasks 000001a0bd9a… (09-20 08:57) + 000001a0c124… (09-21 01:26) died 3/3 with the same all-ok signature, 5 dead-on-gate tasks visible — fix is one owner edit: `test -z "$(gofmt -l $(git ls-files '*.go'))"` in `.tq-verify` AND the bootstrap mint template (CI is unaffected: no vendor/ on runners) — BLOCKED: agents must never edit their own verify gate (AGENTS.md ruling)
- [ ] Forensic sweep of the gofmt-vendor dead-letter class: list every go-taskqueue task dead on the verify gate since 2026-09-17 (tq tasks --status dead), classify each death from its verify-failure log (gofmt-stage all-ok tail vs e2e-timeout FAIL), check whether the underlying work shipped anyway (footer commits + checked TODO rows), and produce the rescue/dismiss recommendation list as a report (read-only: tq show + ~/.local/state/tq/logs/*.verify-failure.log; pairs with the gate-fix ruling row above)
- [ ] SystemNix pool validation: poolSettings diff on evo-x2 never validated after the deploy fixes; machine-wide agent slot cap; Gatus journal-head liveness probe (01-55/02-00 reports) — the sudo/deploy parts are owner rows already — BLOCKED: SystemNix-side validation needs the deployed host (owner-run) (docs/status/2026-09-10_02-00_self-review-dead-pool-fix-flash-dogfood-session.md)
- [ ] Security hygiene batch: SECURITY.md per-surface security-header matrix, `redactedRequestURI` pin test, bearer-token dedup consideration (`internal/httpauth`) (03-05 report §f; docs/status/2026-09-10_03-05_httputil-assessment-and-permissions-policy.md)
- [ ] `scripts/archive-evidence.sh`: one-command evidence copy (check-ignore FIRST, copy second, SHA256SUMS regen, daemon --stat diff) — asked for in four consecutive windows, still absent (00-43 report §f; docs/status/2026-09-12_00-43_task-000001a092a10a7edf3a3836f61deba98eed.md)
- [ ] Ghost-archive gate restructure: report ALL failure classes in one run (elif short-circuits) + INCOMPLETE-survives-the-gate negative test (00-10 report §f; docs/status/2026-09-12_00-10_task-000001a0927c6b76816c188ac334cb868ddf.md)
- [ ] Repo-name expansion policy unification: agent-pool inline-join vs expandRepoSpecs divergence ruling + `tq worker`/`tq audit` bypass-parity check + `--repos` help text hiding bare-name semantics (03-34/04-43/05-12 reports; docs/status/2026-09-11_03-34_task-000001a08a4851ce7feb14c30837a9b399dd.md)
- [ ] `tq doctor` service-context mode: diagnose the systemd unit's PATH (the actual failure surface of the 20h dead pool), not the caller's (05-38 report §f; docs/status/2026-09-11_05-38_task-000001a08e88c2fb41274f6ed6bfbe896b25.md)
- [ ] AGENTS.md size-guard test (≤15KB) + `tq facts --json` golden test (docs/status/2026-09-09_06-01_round10-self-review.md; the httpapi architecture-table row itself landed 2026-09-22)
- [ ] `tq tasks` notBefore/backoff column + `tq show` lease-staleness surface (05-30 report c3/f6; docs/status/2026-09-10_05-30_loop-engine-built-task-closeout-and-docs-health.md)
- [ ] README-as-contract rot guard (smoke asserting install snippets actually work) + `agent-pool --model` retirement decision (it resets reasoning effort per the .crushrc ruling) (05-29 report c3/f6, c5/g2; docs/status/archived/2026-09-10_05-29_readme-overhaul-tqdb-incident.md)
- [ ] fullcore fatal-path close-before-exit: closeout turn skipped when the process exits on a fatal path after verify (06-39 report b3/e1; docs/status/2026-09-10_06-39_task-000001a0893f0253e25fe15e759f78a899ed.md)
- [ ] Smoke forensics batch: keep-logs-on-fail flag for smokes, zero-requeue assertion, multi-repo.sh added to the AGENTS.md smoke list (09-01 report f5/f6/f8; docs/status/2026-09-10_09-01_task-000001a08a1a8b0e7666677dc99c4cd84b16.md)
- [ ] Worktree design open questions minted: Q1 merge-policy is gating — lift the 9 open questions from docs/planning/2026-09-12_worktree-per-agent-design.md into TODO/ROADMAP rows (01-43 report §f; docs/status/2026-09-12_01-43_task-000001a092d3650a2b85c03583a22d9c2c5e.md)
- [ ] Status-index re-sweep: live rows re-bloated past 100 after this pass's intake — the 12-39 e5 cadence item (row above) needs its next scheduled sweep (docs/status/2026-09-14_12-39_claiming-arc-brutal-status.md §e5)

## Webui overhaul leftovers (2026-09-14 stunning-overhaul execution; docs/research/2026-09-14_templ-components-deep-dive.html §05)

- [ ] Adopt `display` MaxTicks in the two dashboard metric AreaCharts (560x200) once templ-components releases past v1.17.0 — the prop is implemented upstream (templ-components df0d70f4, untagged); the Height-200 stopgap stands until then
- [ ] Sort state as removable filter chip + sort-header link visual verification against theme duality (G2 polish, deferred)
- [ ] CSP `form-action 'none'` kills the no-JS filter fallback — allow form-action 'self' or accept JS-only filters — BLOCKED: owner ruling on the security posture (surfaced during G1)

## Done-prompt window harvest (2026-09-16; sources: the five-task hardening window close-outs 00-11/00-55/01-21/01-46 + 06-43, deduped against rows 89-93/97; all claims verified at HEAD this pass; docs/status/archived/2026-09-16_02-05_done-prompt-five-task-hardening-window.md)

- [ ] `scripts/smoke/api.sh`: live-socket lockout smoke — spawn `tq api` on a scratch DB (export TQ_DB!), 3×401 → 429 + Retry-After → expiry → 200; wire into ci-local smokes (01-21 report §f4; docs/status/2026-09-16_01-21_task-000001a0a7515734d342737ff2e133e427b6.md)
- [ ] `scripts/smoke/redaction.sh`: stub agent emitting a fake `sk-ant-…`/`AKIA…` token; assert stored evidence tail is `[REDACTED]`, sidecars redacted, `tq audit --journal` reports zero post-pass SECRET EVIDENCE; fold a zero-SECRET-EVIDENCE assertion into `journal-drift.sh` (00-55 report §f1/§f5; docs/status/2026-09-16_00-55_task-000001a0a7237f14896a336463023ce6a28c.md)
- [ ] Extract `internal/httpauth`: bearer-token extraction ×2 (webui presentedToken vs httpapi bearerToken) + the twin rate limiters behind ONE tested implementation with per-surface policy knobs (strike trigger, guarded scope) — a limiter bug-fix today can silently miss the twin (01-21 report §c2/§f5)
- [ ] De-sleep the lockout tests via `nowFunc` fake-clock injection (httpapi TestAuthLockout* + webui TestWriteRateLimit* — ~100ms real sleeps each, theoretical loaded-runner flake) (01-21 report §b2/§f6)
- [ ] `slog.Warn` on first bound-eviction in webui `boundLocked` (an eviction under legit traffic means a surprise key population) + unify the limiter's two clock sources (`nowFunc` injected vs hardcoded `time.Until` for Retry-After) (00-11 report §e2/§e3; docs/status/2026-09-16_00-11_task-000001a0a715d4eabb3535222203793fbd7d.md)
- [ ] Pin the `?token=`-query auth-failure strike path (bearerToken fallback only pinned on success) + "lockout covers POST /api/v1/tasks" (all-routes contract; only GET healthz tested) (01-21 report §f8/§f9)
- [ ] Decide 401/429 body shape on `tq api`: handlers answer `{error, fix}` JSON but auth failures + lockout use plain-text `http.Error` — align or document why plain text is right for the auth plane (01-21 report §b3/§f7)
- [ ] Secrets-pass polish bundle: document the token-shape growth policy in AGENTS.md (pattern + test + baseline note per new provider); pin test that `secretPatterns` and `SecretHits` compile the identical table (audit vs redaction must never fork); verify `redact_test.go` actually runs on windows-latest CI (no `//go:build unix` tag — confirm, don't assume) (00-55 report §f7/§f8/§f9)
- [ ] `tq doctor --hygiene` scratch-DB smoke (seed a stale-pin task, assert the WARN text via `--json`) + a claim-time proof test that `--reresolve-verify` actually uses the NEW gate after a `.tq-verify` flip (06-43 report §f4/§f5; docs/status/2026-09-15_06-43_task-000001a0a2f63b92bdc1a0d85ef9935cb821.md)
- [ ] Daemon-commit attribution sweep: doctor-style audit mapping footer-less `chore:` commits to task IDs via their `docs/status/*_task-*.md` reports, flagging unattributed shipping commits (00-55 report §f2)
- [ ] CHANGELOG convention: behavior changes visible to external API consumers (e.g. the new `tq api` 429s) get an explicit behavior-change flag instead of a plain Added bullet (01-21 report §f28)
- [ ] Next archive sweep (docs-health ANNOTATE): annotate + git-mv the five 2026-09-15 morning re-dispatch triplet reports (03-25→04-40); monthly digest row added 2026-09-16 keeps the index scannable meanwhile; live count 142 vs threshold 100 (02-05 report §c7)
- [ ] ci-local transient-retry wrapper polish bundle: "healed on poll N" success log line; AGENTS.md note for `TRANSIENT_MAX_POLLS=0` escape-hatch semantics; one-line rationale for the deliberately unwrapped `check-ci` step (01-46 report §f3/§f14/§f6)
- [ ] `check-script-syntax.sh`: separate/quiet the nix-shell fetch path that raced its own output once (exit-1 false red during the 01-46 lap) (01-46 report §f15)
- [ ] `closeoutPending` lifetime audit: entries registered on 429-during-closeout whose task dies CANCELLED while parked — leak until process restart? (00-11 report §e5)
- [ ] Per-key map bound sweep: one grep pass over `map[string]` in internal/webui + internal/executor for members needing the strikes-map bound treatment (00-11 report §f26)
- [ ] `tq api` `--help` mentions the lockout (429/Retry-After) + `tq doctor`/serve startup surfaces recent auth-lockout WARNs (misconfigured producers discoverable without journal digging) (01-21 report §f13/§f14)
- [ ] `tq pool-health` one-shot liveness summarizer: per-repo skip streaks + last harvest activity from the journal (liveness ≠ process up) (carried; unchanged rank from the 09-12 harvest)
- [ ] Deploy-readiness bundle for the next SystemNix input flip: `--hygiene`/`--reresolve-verify`/`--redact`/httpapi hardening all activate together (deployed tq v0.3.0 has NONE of them — verified `tq doctor --hygiene` errors on the live binary); smoke each on the pool unit after the flip (02-05 report §b1) — BLOCKED: rides the owner-run input flip (rows 21/97)
- [ ] Daemon-race footer policy: when the auto-commit daemon folds a task's diff before the explicit commit, what is the sanctioned terminal state — footer on an empty marker/report commit (precedent: 37e83d4, 3e0f941), amend-rescue of the just-created daemon commit (05962e2's move), or scripted reword of unpushed daemon commits? — BLOCKED: owner queue-convention call (00-55 g3, 01-21 g3, 01-46 g1 all re-ask it)
- [ ] Lint-baseline growth ownership: when concurrent feature sessions grow the baseline (gate RED at HEAD right now), must each feature session regenerate with triage notes, or is a dedicated periodic sweep the ritual? — BLOCKED: owner process call (01-21 report g2)
- [ ] Deploy cadence: flip the SystemNix input + `nix run .#deploy` so the window's shipped features reach the live pool — until then every hardening window ships code production cannot exercise (deployed binary lacks hygiene/redact/reresolve, verified) — BLOCKED: owner-run (sudo on evo-x2; rows 21/97)

## Done-prompt harvest (2026-09-17; sources: the six-task CI-resilience/observability window close-outs 01-46/02-26/02-37/03-04/04-03/04-21, deduped against open rows; claims verified at HEAD this pass; docs/status/archived/2026-09-17_00-25_done-prompt-six-task-hardening-window.md)

- [ ] Mint-time done-check for repeat dispatches: at enqueue/harvest time, refuse or downgrade a mint whose task ID already has a terminal fact or an indexed close-out (the loop's standing top fix; a verify-only re-dispatch of the status-append task closed 2026-09-17 00:24 — an entire paid lap to re-verify a DONE row) (01-46 §f7; docs/status/2026-09-16_01-46_task-000001a0a7682e60d5308538ec1aff235005.md)
- [ ] Calibrate the transient-retry 45s×3 budget from git-history/report evidence of past mid-edit breaks (still zero datapoints; the wrapper has never healed a real break) (01-46 §f4; docs/status/2026-09-16_01-46_task-000001a0a7682e60d5308538ec1aff235005.md)
- [ ] First full ci-local soak with the wrapper + pin wired: record whether any poll healed and whether the pin step behaves in situ (closes 01-46 §b6, 03-04 §b4, 04-21 §b2 residues) (01-46 §c1)
- [ ] Call-site census pin for `with_transient_retry`: a pinned "N wrapped call sites" makes silently unwrapping a gate a loud event (03-04 §b2/§f6; docs/status/2026-09-16_03-04_task-000001a0a7b1684f2f9c0f1f1ae2dd687895.md)
- [ ] Run `check-transient-retry.sh` in ci.yml too (hermetic, sub-second; CI runners never execute the ci-local-only pin today) (03-04 §f8)
- [ ] Split row 89 at the next docs-health pass: the smoke half becomes its own BLOCKED row so the shipped pin half could close as DONE (03-04 §e3; rows can't partially close today)
- [ ] Durable pin for check-go-mods' verify retry (house check-transient-retry style: sed-extract the shipped loop, assert WARN + retry + fail semantics) (04-21 §b2; docs/status/2026-09-16_04-21_task-000001a0a7faa45cea4656ad816e85a83395.md)
- [ ] Audit every silent retry/absorption site in scripts/ for a WARN line (04-21 §e2 generalization)
- [ ] check-ci.sh: single `gh run list --jq` call replacing three python3 subprocesses; print the local HEAD sha in the predates branch; ancestry-precise wording via `git merge-base --is-ancestor` (04-21 §e4/§e5)
- [ ] fuzz.yml concurrency ruling recorded: whether the scheduled nightly opts out of cancel-in-progress (a manual push would kill a campaign mid-write) — question filed, ruling not yet recorded (04-21 §c1) — BLOCKED: owner ruling on scheduled-workflow concurrency semantics
- [ ] Table-driven pin over ALL secretPatterns pairs: assert bearer×auth-header is the ONLY overlapping pair so a future pattern fails loudly (02-37 §b4/§f1; docs/status/2026-09-16_02-37_task-000001a0a76cd0808802847ba630d2957bb5.md)
- [ ] Property test: N injected non-overlapping fake tokens → SecretHits == N regardless of surrounding benign text (02-37 §f2)
- [ ] Pin that RedactSecrets masks an auth-header line to exactly ONE marker (output-side mirror of the count fix) (02-37 §f11)
- [ ] Structural redaction guard: a check that every executor tail helper delegates to redactOutput, so a future third tail helper cannot silently bypass the pass (02-26 §b2/§f4; docs/status/2026-09-16_02-26_task-000001a0a76ccb60c50236a4d09dd0b693b5.md)
- [ ] `SetFailureEvidence` for depbump (structured stage/exit_code/tail evidence, agent-parity; today error text is the only persisted depbump evidence) (02-26 §c1)
- [ ] Re-run `journal-drift.sh` + the seeded audit fixture once at HEAD before the next release; add the operator note that SECRET EVIDENCE counts in pre-4ba81d8 reports are not comparable with fresh `tq audit --journal` output (02-37 §f8/§f7)
- [ ] Rename the `tq audit --journal` "hit-count" label to "locations" for operator-facing honesty (needs the CHANGELOG/AGENTS wording swept with it) (02-37 §f18)
- [ ] Annotate pre-fix status reports citing SECRET EVIDENCE hit-counts with the span-merge caveat (docs-health ANNOTATE, low priority) (02-37 §b3/§f10)
- [ ] Frozen-date fixture sweep: rg scripts/smoke + testdata for hardcoded calendar-date literals, convert to run-time computation; a small check-* gate if the convention is ratified (04-03 §f3/§g3; docs/status/2026-09-16_04-03_task-000001a0a7c3b5f42d9e2ab428f1024d6622.md)
- [ ] ratelimit-e2e GNU-date dependency: header note or loud portability guard (requires `date -d`; silent breakage on BSD/macOS) (04-03 §f7)
- [ ] ratelimit-e2e into ci.yml: the smoke gates ci-local only; ubuntu CI runs webui/fullcore/release-gates — parity call (04-03 §f11)
- [ ] ratelimit-e2e stub-log assertion: grep the worker log for the armed-gate/provider line, pinning the f35 observability half end-to-end (currently only Go-tested) (04-03 §f12)
- [ ] Provider tag in the parked surface: `tq tasks --parked` shows only the reason string; surface `RateLimitError.Provider` so "which provider is exhausted" is one glance (04-03 §f8)
- [ ] doctor earliest-release branch pin: verify a test covers doctorParked's not-today day-aware layout case; pin if not (04-03 §f9)
- [ ] Pin `tq stats --project X` parked-count × project-filter interaction (04-03 §f10)
- [ ] Smoke-retry budget for ci-local smokes (row 89's blocked half, third ask): two-phase tree-quiet probe (recommended — milliseconds vs multiplying minutes-long smokes ×3), whole re-run ×3, or wrap-only-cheap-smokes (01-46 §g3, 03-04 §g1) — BLOCKED: owner runtime-budget ruling
- [ ] Daemon-race attribution protocol (fifth recorded instance: 3e0f941, 025cd6b, 9648a19, d4125d1 + today's report commit): ratify daemon-SHA citation as permanent, or bless content-first explicit commits (work commit BEFORE docs/TODO edits) so the footer commit carries the pinned bytes (01-46 §g1, 02-37 §g1, 04-03 §g1) — BLOCKED: owner queue-convention call

## Done-prompt harvest (2026-09-17; sources: the lint-baseline row-104 re-dispatch close-outs 01-17/01-22/01-23, first harvested this pass; row 104 itself is DONE + re-verified 3x)

- [ ] Spot-check round-13's noctx "fix for real" claim against code: the row-104 close-outs confirmed absence-of-growth but never verified the noctx findings (executor 1→5, root 22→29) got REAL context-carrier fixes, not a baseline regen (01-17 §f14, 01-23 §b1)
- [ ] Queue-side dedup-key→COMPLETED short-circuit: when a dispatch's task ID already has a COMPLETED task, mint a zero-cost verify stub or skip — now argued from 3+ sibling re-dispatch windows in as many days (01-17 §f1, 01-22 §e2, 01-23 §e2)
- [ ] Owner ruling: adopt or policy-disable `modernize` (per-gate whack-a-mole burns a window per drift class) (01-17 §g1)
- [ ] Pin golangci-lint version in the flake to stop analyzer-drift gate-killers (01-17 §g2)
- [ ] Pre-commit hook (or gate) validating report filename timestamps against `date` — the 01-22 report shipped a fabricated `04-52` timestamp (01-23 §d1/§e1)
- [ ] Owner ruling: agent ask-policy for `tq ask` — teach it in work-turn prompts with a cap, leave interactive-only, or teach + budget-exempt (21-04 §g1)
- [ ] Owner ruling: confirm the 72h question-expiry default (re-enter on expiry) or switch expired questions to cancel the task (21-04 §g2)
- [ ] Executor: export `TQ_TASK_ID` env to agent runs so prompts/scripts stop needing the sed-the-prompt hack (`tq ask --task` could then default to it) (21-04 §f28)
- [ ] Expiry sweep: append a fact when a question's NotBefore lapse re-enters a task — today the re-entry is silent (forensics gap) (21-04 §f34)
- [ ] Pin with a test that closeout-free executors (review/status/dlqfix/prioritize) never receive the `$TQ_QUESTION_FILE` channel (21-04 §f42)
- [ ] SECURITY.md: note that questions carry agent-authored text toward PapDashboard (redaction pass covers it; document the trust boundary) (21-04 §f44)
- [ ] Queue↔git attribution fix: 59% of footer commits are invisible to GitLogScanner — git %(trailers) parses ONLY the final paragraph, and the harness commit template's trailing blank+Assisted-by line (e.g. fc6e03d) demotes the footer out of it (74 of 180 footer commits visible over the last 400) — BLOCKED: owner direction in the 03-00 report §g1 (scanner %B fallback trades the quoted-footer guarantee at gitscan.go:141 vs a footer-last shape rule touching the harness contract)
- [ ] Pin GitLogScanner's trailer visibility END-TO-END: commit a harness-template-shaped message (footer, blank line, Assisted-by last) into a temp git repo and assert TODAY's zero-attribution with a comment citing the 03-00 report §e2; flip the assertion when the attribution fix lands — gitscan_test.go's rec() fixtures simulate git OUTPUT and are structurally blind to git-side trailer detection
- [ ] tq show review-task detail: render session tokens/cost for review turns the way agent and prioritize tasks already show theirs (mirror of the 09-52 f5 prioritize treatment; internal/webui)

## Process follow-ups (harvested from the 2026-09-21 verify-window close-outs; sources: 00-39/02-01/04-07/04-17/04-24/04-46; verified 2026-09-21 04:55)

- [ ] check-todo-list.sh + internal/harvest TestRepoTodoListParses: reject/flag checkbox lines whose prefix is not exactly `- [ ]` — the 04-40 close-out commit (0ce62ad) shipped the Score-cache row as `--- [ ] …` and BOTH consumers stayed silent (gate rc=0 on the damaged file; the row was invisible to harvest) until the 04-46 window caught it by eye (04-46 §d4/§f1; scripts/check-todo-list.sh, internal/harvest)
- [ ] session-start.sh: after the task-ID grep, print the tail of the docs/status/README.md index (last ~10 rows) so the sibling close-outs and their §d remedy patterns (stop-artifact shape, battery bar, daemon-fold handling) are visible at turn 1 — the zero-artifact-stop class (00-39, 02-01, 04-07, 04-46) recurs because only the SAME-ID report gets read (04-46 §e1)
- [ ] Ruling: verify-only repeat-dispatch windows mint a stop artifact BY DEFAULT (row re-verify annotation + footer commit, the 02-10/04-07 pattern) vs chat-only stop until the owner asks for paper — five recurrences of the chat-only class (00-39, 02-01, 04-07, 04-46 §d1) — BLOCKED: owner stop-artifact policy (04-46 §g1)
- [ ] History-rewrite heals must re-run scripts/check-dead-sha-refs.sh in the heal window's OWN battery: the 00-27 msg-filter (5-pair fork record) orphaned 5 post-baseline cite-sites (4544fda→1d4e1a1, 4d2c720→c89e93e ×3, 2e9f04c→f8f84a9 across the 07-30/08-02/09-30 reports + the README index) and the HARD ci-local gate rode red at HEAD for ~9 hours / 9 windows until the 04-55 window's report prep ran it and healed the cites with fork records (targets 1d4e1a1/c89e93e/f8f84a9 verified reachable before citing) (04-55 §d6; scripts/check-dead-sha-refs.sh)
- [ ] AGENTS.md: codify the verify-only re-dispatch checklist as a Conventions block next to the minimum-battery clause — substance-read the pinned artifact (not name-greps), re-cite prior claims at file:line, fresh named battery + explicit no-delta statement, append the re-verified annotation to the done row, note the re-dispatch cause — the de-facto bar rows 160-163/167 already follow, currently discoverable only by reading their annotations (07-12 report §e1/§f2; docs-only)
- [ ] Verify-failure logs don't capture the failing stage's output for the gofmt stage: task 000001a0c245's attempt-1 log ends with every package ok + `exit status 1` and contains ZERO gofmt/vendor lines, so gofmt-stage deaths are inferable only from the all-ok shape — capture the failing stage's stdout in the verify-failure log (internal/executor verify runner) so row 205's forensic sweep can classify from evidence, not shape-matching (07-12 report §f3)
- [ ] Harvest anti-race guard: a dispatch minted for a TODO row races the row's check-off commit and burns a full verify-only window (this task ID alone consumed 4 windows for 1 work window; rows 161-164 show the same 3-4x churn; row 161 names the cause) — re-read the item's checkbox at claim/dispatch time and skip already-checked rows (08-35 report §e3; internal/harvest)
- [ ] Daemon-fold footer-first rule as a check script: source work keeps landing in unfootered daemon folds with the footer commit carrying docs-only (EIGHTH occurrence on the 08-12 SessionMessageCount window — sources rode d556a3f, footer commit 021f8a4 = TODO_LIST only; the 04-07 §e1 remedy is known but enforced by nothing), so the queue's git cross-reference systematically under-attributes work — flag any footer-carrying commit whose diff is docs-only while the same window touched source files (08-35 report §d2; scripts)
- [ ] Row-note accretion convention: attempt notes on a DONE TODO row collapse to ONE note + a pointer to the latest report instead of append-only accretion — row 171 now carries 3 same-day attempt notes on a single ~1.6k-char line, each re-grepping the same artifacts (08-56/09-10/09-20 windows); the 06-34 §g append-vs-replace ask now has a hard datapoint (09-20 report §f1; TODO_LIST.md conventions)
- [ ] Doc-citation convention: citations into concurrently-edited DOC files (CHANGELOG/FEATURES/README/TODO rows) use section anchor + landing SHA instead of bare file:line — CHANGELOG.md:28-36 (09-10 row note) sat at :25-34 one hour later via concurrent edits above the block, so stale line-cites accumulate silently in DONE-row annotations (09-20 report §f2; AGENTS.md claims-carry-citations clause)

## Docs-window follow-ups (2026-09-21 usage-rendering docs window; sources: this window's close-out report §b2/§d4; docs-only)

- [ ] FEATURES.md placement check for the 2026-09-21 usage-rendering cards: the statusReportCard/prioritizeResultCard rendering facts live only on the sweeper rows (done-prompt loop + AI batch scorer); decide whether the webui detail-page section should carry them as first-class surface rows and move/extend if yes (2026-09-21 08-56 report §b2; docs-only)

## Process follow-ups (2026-09-21 show-JSON message-count pin window; source: docs/status/2026-09-21_10-19_task-000001a0c3056902a0ee78d22edce0da668b.md §e1/§d1)

## Docs-health harvest (2026-09-21 archive sweep; sources: full-corpus per-item triage of every live report 09-08..09-21 + planning docs, deduped against open rows; claims tree-verified at mint time)

- [ ] ci.yml parity sweep: wire the ci-local-only gates into ci.yml (or record each asymmetry deliberately) — check-dead-sha-refs.sh, check-guard-wiring.sh, check-module-loop-capture.sh, `golangci-lint config verify`, smoke/reviews.sh, smoke/journal-drift.sh, smoke/ratelimit-e2e.sh are all ci-local-only today (2026-09-20 05-16 §f4; 2026-09-15 02-41 §f1, 03-25 §f4, 03-55 §f2; 2026-09-15 05-40 §f17; .github/workflows/ci.yml)
- [ ] Gosec binary provenance + stamped install: owner installs a stamped `golangci/gosec@v2.29.0` binary (every ci-local gosec step carries the UNSTAMPED WARN until then; go install is sandbox-blocked for agents) and rules on unstamped-binary provenance — BLOCKED: owner install + provenance ruling (2026-09-20 00-19 §f2; 2026-09-19 01-58 §f1/§f2, 02-38 §f3)
- [ ] Owner ruling: budget cap unit (task-count vs token/cost) now that the token projection ships — decides cap semantics, `UsageToday` fail-open vs error-return (budget.go:138), and the cap-unit ADR (2026-09-21 00-27 §f12/§f13/§g1; 01-49 §g3) — BLOCKED: owner token-vs-count ruling (AGENTS.md budget section)
- [ ] Owner ruling: DefaultScoreTTL 30d + aging-unification (score-cache aging constants were shipped on judgment, not ruling; refresh-burst pacing when many verdicts expire the same day) — BLOCKED: owner calibration ruling pending the --prioritize pilot (2026-09-21 05-39 §g1; 06-20 §g2)
- [ ] tq session sweep automation: wire `session sweep` into agent-pool ticks or a systemd timer (quiet-close of unowned sessions is manual today) + PreToolUse hook (`scripts/hook-session-registry.sh`) is shipped but unwired into the crush config + registry JSONL growth bound + pgrep ownership check hardening (substring vs pid) — BLOCKED: owner runs the hook wiring + cadence call (2026-09-18 00-14 §f4/§f15/§f16/§f17)
- [ ] depbump/depsweep e2e + first live run: `scripts/smoke/dep-sweep.sh` (go get→commit→tag happy path over a scratch repo) + the first live `--dep-sweep` pool run calibration (both untracked since the 06-44 hardening) (2026-09-15 06-44 §f4; 05-20 §f12; internal/depsweep)
- [ ] Questions-loop surface bundle (post-ask-policy webui/CLI half): parked-on-question badge in the task table, questions section on the detail page, `tq tasks` open-questions filter, AnswerPoller max-answers-per-poll bound, asked/answered/expired counters in `tq stats` (2026-09-17 21-04 §f31-§f33/§f37/§f38/§f40; internal/webui, internal/bridge/papdashboard)
- [ ] Review-anchor residue: webui finding card renders the quoted anchor, `tq show` retry-trail surfaces parse-time rejections, anchor discipline extended to dlqfix + cqa bridge findings (2026-09-15 03-10 §e1/§f1/§f4/§f7/§f8; internal/webui fragments.templ, internal/executor)
- [ ] Status-window/closeout report surfacing: StatusCompletion.Report links in the webui status window strip + `tq show` per-entry report paths + negative closeoutReportPath test (repo dir absent) (2026-09-14 01-12 §b1/§f1/§f7/§f10/§f11; internal/status, internal/webui)
- [ ] Round-10/11 micro-residue batch: release.sh auto re-add `[Unreleased]` header after cutting; e2e budget-bypass test (session-minted tasks skip the cap, design OQ2); TODO-linter keyword widening ([USER], awaiting-decision); SystemNix/Caddy/Gatus stale `:8090` reference sweep; first live ETXTBSY retry-firing check in the pool journal (docs/status/2026-09-09_06-01 §f6/§f10/§f12; 2026-09-10_02-00 §c6/§f20; 2026-09-10_05-30 §f24)
- [ ] Smoke-assert gap bundle: webui smoke lacks Permissions-Policy header assert + X-Robots-Tag/CSP curl-level asserts + a `/project/{unknown}` assertion (2026-09-10_03-05 §f14; 2026-09-16 18-12 §f15; 2026-09-12_01-47 §f11; scripts/smoke/webui.sh)
- [ ] scripts/check-archive-eligibility.sh: mechanical archive bar (every numbered/- [ ] item in a candidate struck or marked, else refuse `git mv`) — turns the every-item-resolved rule from agent judgment into a gate; wire into ci-local next to check-status-index (2026-09-14 14-17 §e1; 17-15 §e2/§f7)
- [ ] TQ_BIN rot-guard + smoke binary provenance: a check that smokes' TQ_BIN branches actually run a current binary (`tq version` line), plus a pre-release smoke step convention (2026-09-10_09-01 §f1/§f2; 2026-09-19_00-10 §f19; scripts/smoke/*)
- [ ] Dependency-reuse review residue: A3 hand-rolled-vs-library survey, A4 evidence-struct dead-field pass, A5 vestigial go-sse/cenkalti cleanup (docs/planning/2026-09-10_02-02_SUPERB-DEPENDENCY-REUSE-REVIEW.md A3-A5; templ-components row there also stale at 1.16.0 vs root v1.17.0)
- [ ] Webui-health bundle: SECURITY.md /health surface row + auth-smoke assertions, chromedp browser-level health/page test, dark-mode /health verification, adoption ADR or knobs ruling (`--health-dashboard=false`) (2026-09-16 08-28 §f8/§f20/§f4; 12-11 §f12/§f13/§f25/§f26; internal/webui/health.go)
- [ ] Small executor/budget pins: `factTone` cases for the 6 newer fact types (question-asked, reprioritized…), `detailItems` unused `now` param, `agentResultEmpty` helper extraction, formatInt for large message counts (2026-09-21 09-52 §f17/§f18/§f20; 08-12 §f3/§f4; internal/webui/components.go)
- [ ] Feature-window polish: `tq tasks` PRI/BAND column + derived band field in --json, `tq stats`/`tq top` band breakdowns (scope ruling), band deep-links from the board "+N older" link, shared `queue.ParseBand` helper (2026-09-20 08-43 §f1/§f2/§f9; 09-30 §f5/§f8; cmd/tq, internal/webui)
- [ ] Legacy `TQ_RESULT` stdout channel deletion once the live pool shows derived outcomes (in-flight tasks retired) — flip the executor to file-channel-only and delete the stdout fallback (docs/planning/archived/2026-09-14_derived-outcomes-verdict-channel.md; 2026-09-14 12-52 §b2/§f1)
- [ ] Webui overhaul P2 batch (the 11-41/18-14 untracked tail, highest-value first): measure SSE tick frequency on the live pool, a11y audit of custom surfaces, cancel/rescue POST feedback, sticky-header + keyboard t/b, dark-mode QA profile (2026-09-14_11-41 §f14/§f21/§f22; 18-14 §f33+ tail; internal/webui)

## Repeat-dispatch window follow-ups (2026-09-22 02-06; sources: this window's close-out §e/§f, deduped against rows 88/92)

- [ ] Dead-export audit: warn when the annotated-keep allowlist grows past 10 entries, forcing a revisit of the general in-package-use heuristic before silent allowlist rot (scripts/check-dead-exports.sh)
- [ ] Dead-export audit: emit the full dead list to a committed reference file (e.g. docs/audits/dead-exports.txt) so triage windows stop re-deriving the 53-row set from scratch (scripts/check-dead-exports.sh)
- [ ] Track the advisory audit's dead-count over time: append one summary line per ci-local run to a small log so prune candidates surface by trend instead of per-window triage
- [ ] tq doctor check: flag queue tasks whose dedup-key TODO row is already ticked in the source repo's TODO_LIST.md (queue-side view of the harvester already-done skip)

## Identity-blind one-liner window follow-ups (2026-09-22 03-05; source: docs/status/2026-09-22_03-05_task-000001a0c698f12f76176d0ecf682956927f.md §e/§f; deduped against rows 95-97)

- [ ] Daemon-fold footer marker script: `scripts/fold-marker.sh <task-id> <folded-sha> <subject>` creates the empty footer commit with standard boilerplate (fold disclosure + foreign-hunk disclaimer) so windows stop hand-inventing the maneuver when the daemon folds footer-deserving work into chore commits
- [ ] session-start.sh: surface a one-line prior-windows summary (window count + any DONE-row hits) inside the FINAL completion line, so tail-truncated invocations (`./scripts/session-start.sh <id> | tail -20`) still surface duplicates without a manual re-grep
- [ ] AGENTS.md session-shell hazards: extend the PIPESTATUS bullet with the multi-file `tail -N` quirk ("option used in invalid context" in the session shell) so agents keep redirecting gate output to files instead of piping tails

## Verify-gate + pool-scheduling follow-ups (2026-09-22 11-50; source: docs/status/2026-09-22_11-50_task-000001a0c698f12f76176d0ecf682956927f.md §e/§f; deduped against rows 91/123/124/226)

- [ ] AGENTS.md vendor-gofmt known-issue: correct the "all-ok tail = THIS bug" attribution rule to require grepping the FULL verify-failure log (FAIL/panic/ENOSPC lines) before attributing any gate death — task 000001a0c698's attempt-1 death shows the same all-ok tail but died on a mid-log `no space left on device` build-cache write, a proven false positive of the shape match
- [ ] Pool-side dirty-tree starvation: task 000001a0c698 sat through ~35 consecutive claim→preflight-refuse→requeue cycles over 8.5h (03:12-11:27, fact seq 5485-5684) because require_clean refuses on a multi-agent repo whose tree is almost never clean — design a starvation counter/escalation (papdashboard alert after N consecutive dirty refusals on one task, or a quiet-window claim mode; read-only investigation over tq show fact trails)
- [ ] Verify-failure FailureEvidence tail is too small to classify deaths: the 8-line tail of task 000001a0c698's attempt-1 log hid BOTH the real `FAIL internal/e2e` line AND the `no space left on device` cause — enlarge the tail or attach the failing stage's output (extends row 226's stage-capture fix to the tail size itself)

## Row-95 attempt-3 window follow-ups (2026-09-22 11-58; source: docs/status/2026-09-22_11-58_task-000001a0c698f12f76176d0ecf682956927f.md §e/§f; deduped against the 11-50 section)

- [ ] Pin the re-dispatch work-turn protocol in AGENTS.md next to the session-start ritual: for any re-dispatched task (a) read the NEWEST prior report for the task id first, (b) run `tq show <task-id>` on the live record (attempt count, fact seqs — never cite lineage second-hand), (c) fresh battery at current HEAD with every rc captured to file, (d) persist the battery as a citable artifact in the SAME window — attempt 3 of task 000001a0c698 skipped (b) and (d), leaving its claim time and fact seqs unrecorded
- [ ] While row 123 is live, extend the AGENTS.md verify-window minimum battery with two probe lines: a `gofmt -l .` check scoped to non-vendor output (separates "verified" from "verified-except-the-known-gate") and a cache-mount `df` headroom line (ENOSPC has killed two gates now: the 03-37 toolchain dir and task 000001a0c698 attempt 1)

## art-dupl dedup pass follow-ups (2026-09-23; sources: docs/status/2026-09-23_01-34_art-dupl-dedup-pass.md §f + the same-day continuation window; deduped against open rows — rationale comments, the 12-group count fix, and the AGENTS.md shared-seams bullet are DONE, not re-listed)

- [ ] Rune-safe `executor.Excerpt` truncation: `line[:200]` is a byte cut and can split a UTF-8 rune on multi-byte prompts; all historical copies had the same behavior, now ONE choke point exists at internal/executor/excerpt.go so the fix is cheap + add a multi-byte boundary test (01-34 report §f2/§f7)
- [ ] Dedicated `TestRecordRunOutcome`: sink detail contains log_path + result fields, sidecar path honored (01-34 report §f5)
- [ ] Move `LogPath` into `sessionUsage` (wire-identical, embedding flattens JSON) and drop the two-pointer `recordRunOutcome` signature (01-34 report §f8)
- [ ] art-dupl accept-list gate in ci-local: growth over an accepted-set file fails CI (lint-baseline pattern) — the mirror mass grew 7→12 groups unnoticed — BLOCKED: new hard gate needs an owner policy ruling (01-34 report §f6, §g2)
- [ ] Wire dlqfix autopsies into `deriveUsage`/budget token projection — paid agent turns invisible to daily-cap accounting — BLOCKED: needs owner ruling on whether autopsy spend counts (01-34 report §f9/§g3, §f37)
- [ ] Re-run art-dupl at `-t 3` once the accept-list gate exists, to see what a stricter threshold surfaces (01-34 report §f34)
- [ ] Root-cause the `TestSweepPinsCloseoutReportPaths` one-off flake (02-35 continuation window): one full-suite run got `window[0].Report = ""` while isolation, package 5x, and a second full suite all pass — suspect completion-window ordering or the report-dir scan under parallel load; repro protocol: package runs at `-count=10` + full-suite repeat, then fix the ordering (docs/status/2026-09-23_02-35_dedup-continuation-window.md §c1)
