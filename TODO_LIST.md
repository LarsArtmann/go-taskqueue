# TODO List

Short- and mid-term actionable work. Long-term direction lives in ROADMAP.md.

**This file is machine-consumed**: `tq harvest` turns every unchecked item
below into an agent task. Keep the `- [ ]` checkbox format, one item per
line — do not convert to tables. DONE ITEMS ARE DELETED on closure
(owner ruling 2026-09-14) — a completed item lives in CHANGELOG.md, never
here as a checked row; `--prune-stale` also cancels pending tasks whose
item text is gone from the file, so deletion is the safe form. Appending
`— BLOCKED: <reason>` keeps an item out of the pool.

Items carry their evidence: a code path and/or the status report that filed
them (`docs/status/<date>_<slug>.md`). Completed items live in CHANGELOG.md,
not here.

## High Impact

## Fleet / deploy

- [ ] SystemNix host-side deploy + round-9 cutover (sudo-gated, user-run): `nix run .#deploy` on evo-x2 starts tq-agent-pool/tq-serve/tq-storage-dir/tq-bootstrap (all wiring shipped 2026-09-08: rev-pinned git+file input 28a8ae4, house module `modules/nixos/services/tq-agent-pool.nix`, ports.tq 8100, tq.home.lan vHost + DNS, Gatus checks, btrbk-pool subvolume, dedicated sops bridge template, post-deploy smoke); then the one-time manual-pool cutover in SystemNix `docs/services/tq.md` (stop the `/tmp/tq` processes — DONE 2026-09-10, optionally copy the dogfood tasks.db onto the pool journal). The origin push is done — flip the SystemNix input to `github:LarsArtmann/go-taskqueue?ref=master` — 2026-09-10: the deployed pool was found DEAD (every tick skipped all repos: bare repo names resolved via service cwd + no git/go/crush on the service PATH + silent scan-fail logging); all three fixed on master, so the input flip + redeploy is what brings the Flash pool to life — BLOCKED: owner-run (sudo on evo-x2 — an agent cannot execute this; pool must not pick it up)

## Owner-blocked decisions (BLOCKED items are skipped by the pool)

- [ ] Push authorization (updated 2026-09-10): master + root v0.1.0/v0.2.0 + the five round-1 module tags are now on the remote (owner pushed); still latent: the two backend tags `internal/queue/{sqlite,postgres}/v0.2.0` — after their push run the per-module proxy check (`go list -m -versions`) + the real clean-room `go install` (docs/status/archived/2026-09-09_23-47_round2-queue-backend-split.md f1/f13); post-split executor/queue content waits for the next release's module tags (published v0.2.0 tags are immutable; restored after a mistaken local re-cut) — 2026-09-10 06:25: both backend tags verified on the remote (`git ls-remote`); the remaining proxy check + clean-room install moved to the Fullcore-window section below — BLOCKED: owner push authorization (never push without it)
- [ ] `internal/consumer`: wire into `tq serve` journal tailing or delete the ghost package (ADR-0009 dispatcher, zero production importers; write the ADR outcome either way) (23:47 f2/g1) — BLOCKED: owner intent call
- [ ] Decide the three near-identical interfaces (`consumer.Source`, `budget.FactSource`, `papdashboard.FactSource`) — one interface or documented duplication (23:47 f4) — BLOCKED: owner architecture decision
- [ ] Postgres CLI store wiring (`--store postgres://…` on worker/serve/agent-pool): gives queue/postgres its consumer; root re-adds pgx; sequence BEFORE any public-API promotion (23:47 f5/g3) — BLOCKED: owner release-timing call (v0.3?)
- [ ] Dependabot/renovate policy for the 8-module tree (23:47 f33) — BLOCKED: owner policy decision
- [x] Lint-baseline repair pass for the growth rows still failing `scripts/lint-baseline.sh --check` after the 2026-09-18 03-13 surgical repair (cmd/tq cyclop/tagliatelle, executor err113/mnd/nonamedreturns, root nonamedreturns/prealloc/varnamelen — pre-existing concurrent drift, ~10 findings across foreign files; `golangci-lint cache clean` first, warm cache flip-flops)

## Dogfood round (harvested from docs/status/2026-09-10_02-00 self-review §f)

- [ ] `tq pool-health`: one-shot summarizing per-repo skip streaks + last harvest activity from the journal (liveness ≠ process up) (02:00 f27)

- [ ] Verify the CQA bridge against a live CQA API instance and fix contract drift (`internal/bridge/cqa` response shapes are httptest-informed guesses today); upgrade its FEATURES.md status after (plan C25) — BLOCKED: needs a live CQA instance URL + owner ID + token from the owner

## Window f20–f24 follow-ups (harvested from docs/status/2026-09-10_04-09, verified 2026-09-10)

- [ ] gosec gating: once a suppression config encodes the triage, should gosec become a blocking required check or stay advisory permanently? — BLOCKED: owner gate-vs-advisory call on the security-scan jobs
- [x] `internal/task` public surface: export `task.AllStatuses` in the next minor release to kill the httpapi/webui twin status lists, or keep the duplication + cross-module pin test (TestStatsSurfacesAgree) as the permanent shape? — BLOCKED: needs a public-API/versioning call on the task sub-module — DONE 2026-09-12: the export shipped (`aeebe888`, function not global, oracle pin test; webui/httpapi twins deleted) and the re-tag condition closed 2026-09-13: `task/v0.3.0` + `internal/task/v0.3.0` both contain the commit, facade alias at task/task.go:38, root go.mod pins v0.3.0 (verified 2026-09-16 docs pass; docs/status/2026-09-16_02-05_done-prompt-five-task-hardening-window.md §a1)
- [ ] Pool policy when master CI is red: refuse agent DONE verdicts (fix-forward or park required) vs report-only disclosure of the red state — BLOCKED: needs a pool completion-contract decision

## Fullcore-window follow-ups (harvested from docs/status/2026-09-10_06-25, verified 2026-09-10)

- [ ] Closeout-report placement: every task writes `docs/status/<ts>_task-<id>.md` — root floods at ~10 tasks/day; root vs `docs/status/tasks/` subdir vs write-but-sample-index — BLOCKED: owner volume/placement call (05-30 report g1)
- [ ] Status/closeout TODO-append caps: reports may append up to ~50 items each and the backlog outgrows the 60/day enqueue drain — cap appends (~10), raise the budget, or let it grow? — BLOCKED: owner backlog-policy call (05-30 report g2)
- [ ] Ticket 000001a08915cf6f1c872a00bb18a4f9e326 close-out: its commit 20dafb9 carries a phantom footer (`…0ca7ae2`, zero commits in git) and the content landed via upstream twin 20dafb9 — mark complete queue-side and decide the dual-footer/disposition convention for review-fix and rebase-dropped commits — BLOCKED: owner queue-convention call (06:25 report g1)
- [ ] Master push authorization: master moved 05:45 + 06:03 today while AGENTS.md says agents never push — were those owner-run, and is autonomous push now sanctioned for agent sessions? — BLOCKED: owner push-policy answer (06:25 report g2)
- [ ] Module-fetch trust for the nix gate: if the runner FOD divergence is proxy/fetch-content drift, vendor modules (pinned fetch, trade repo size for gate determinism) vs keep proxy fetches and investigate per incident — BLOCKED: owner hermeticity call (06:25 report g3)

## Window follow-ups (harvested from docs/status/2026-09-10_08-25, verified 2026-09-10)

- [ ] Canonical ID for the f26 cluster: the release-doc work exists under three IDs (`a3864d95` original commit + 06-55 report, `b141f022` reviewer-assigned on 41b817b + 07-49 footer, `c3919ce4` 07-49 report filename) — which is the queue's canonical entry, and should the others be merged or marked duplicates queue-side? — BLOCKED: owner queue-convention call (08:25 report g3)

## Review-window follow-ups (harvested from the 07:49/08:32/08:39/08:42/08:47 close-outs, verified 2026-09-10 09:13)

- [x] Run golangci-lint scoped to `internal/runactor` to confirm errname is quiet after the ExitError rename — the close-out verified via compiler + `rg`, not the linter that filed the original finding (08:32 report b2)
- [x] Sweep the 6384f1d rename diff repo-wide for further literal leaks (`git log -S` probes for receiver-equals-key `Get("query")` patterns and parenthesized-identifier corruptions like `task(store)`) (08:39 f3/f22; 08:42 f4) — swept 2026-09-15: zero leaks at HEAD (rg probes for `%store`/`%queue`/`task(store)`/`Get("query")` all clean; the earlier fixes c95adb8 + d5ee08d caught the real ones)
- [x] Help-text smoke: run `tq` subcommand help (minimum `tq dlq -h`) asserting no parenthesized-identifier artifacts in flag strings — help text is exercised by no test and the class has bitten once (08:42 report e3/f2) — done 2026-09-15: `scripts/smoke/help-text.sh` (all 23 subcommand helps, wired into ci-local; positive + negative verified)
- [x] Extend `scripts/lint-annotations.sh` to the sub-module loop, or decide root-only annotations deliberately (needs a per-module `--new-from-rev` baseline strategy) (08:47 report c1/f1/g2) — extended 2026-09-15: same disk-derived module loop as lint-baseline.sh (root + internal/*, per-module `--new-from-rev` with the shared base, GOWORK=off, sub-module filenames prefixed with the module path); verified 0 findings across all 16 module runs at HEAD
- [x] Extract the disk-derived module loop into `scripts/for-each-module.sh` and consume it from ci.yml (4+ copies) and ci-local.sh (08:47 report e1/f4) — done 2026-09-15: script already landed in ba1657e; all 6 ci.yml module loops + ci-local windows-cross/isolation/lint steps consume it
- [x] Unify the golangci-lint version pin (duplicated in ci.yml and ci-local.sh) into one source so the two can never drift (08:47 report f22/f23) — done (already shipped in 550fec1 "lint pin dedup", re-verified 2026-09-15): ci.yml:82 owns the single `@v2.13.2` literal; ci-local.sh derives it via grep over ci.yml (M55 comment); repo-wide sweep shows zero other pins
- [x] CI topology one-pager (docs/planning/): every job and step, which ones loop modules and why — prevents the next agent re-deriving it (08:47 report f20) — done 2026-09-15: docs/planning/2026-09-15_ci-topology-one-pager.md
- [x] Report the session-snapshot ghost upstream: work-commit hashes pre-appearing in the session-start git snapshot (2574569 incident, 08:32 report d2/g2) — harness state-capture artifact or concurrent-agent duplicate? — answered + filed 2026-09-15: charmbracelet/crush#3842; forensics resolved it as a harness state-capture artifact (2574569 real, authored 08:32:18; its reachable twin 2574569 is a 09:15:43 replay orphaning the original during the reword-incident rebase), root cause source-verified: Crush re-captures the system-prompt git snapshot on every app init incl. session resume while `coder.md.tpl:378` labels it "conversation start"
- [ ] CHANGELOG policy for non-released-boundary changes: do `internal/`-only renames and same-day introduce+fix regressions (never crossing a release; the pool deploys master as a rolling release) get entries, or is [Unreleased] net-since-last-release only? This pass added one combined entry for the two rename-leak fixes and none for the ExitError rename — confirm or overrule — BLOCKED: owner changelog-policy call (08:32 g1; 08:42 g1)
- [ ] CI-time budget for the disk-derived module loops: what threshold should the f49 "tune if it dominates" item optimize against (e.g. lint step ≤ N minutes)? — BLOCKED: owner budget number (08:47 g3)
- [ ] TODO-item accuracy bar: should the harvester verify each item against code at harvest time (f30's wrong package + wrong version-window claim cost a day of deferral across seven reports), or is pickup-time verification by the executing agent the accepted contract? — BLOCKED: owner process call (08:32 g3)
- [ ] Enable `--allow-writes` on the deployed `tq-serve` (SystemNix module flag) so DLQ rescue/cancel work from the WebUI — the live 127.0.0.1:8100 instance runs read-only (no writes-ENABLED banner, no dead-task buttons), so the 21 dead tasks incl. `000001a08edf` need CLI `tq dlq --rescue` today; rescue AFTER the rate-limit-fix input flip so a rescued task doesn't re-burn attempts on 429s (13:29 report §b2/f1/f2) — BLOCKED: SystemNix service config + deploy are owner-run

## Rate-limit window follow-ups (harvested from docs/status/2026-09-11_13-29, verified 2026-09-11)

- [x] Post-deploy retro: one week after the input flip, count 429 requeues vs pre-deploy dead-letters and record the number in a status report (13:29 report f50) — DONE 2026-09-15, interim at day +4: docs/status/2026-09-15_02-30_post-deploy-rate-limit-retro.md (pre-flip 8 429-dead vs post-flip 0; zero 429 requeues — zero 429s occurred; 7-day re-check 2026-09-18)

## Docs-health harvest (2026-09-11 evening; sources: 16-00/15-39/15-10/13-29 reports + task-closeout residue; deduped against existing rows)

- [x] Orphaned-guard audit: every `scripts/check-*.sh` + smoke must be referenced by ci-local/ci.yml/flake or be deleted — generalize the check-webui-css lesson into a ci-local meta-check (15-39 report c7/f5, e1) — DONE 2026-09-15: `scripts/check-guard-wiring.sh` wired into ci-local; dead duplicate `check-lint-baseline.sh` deleted (superseded by `lint-baseline.sh --check`); orphaned smokes multi-repo/papdashboard-e2e/ratelimit-e2e wired into ci-local (all hermetic, verified passing)
- [x] Wire `check-webui-css.sh` into ci.yml (byte-canonical pin, not just the a11y minify-property test) (15-39 report b1/f6, e4) — DONE 2026-09-15: step added to the ci.yml `nix` job (the only job with nix, post flake-check); also caught + fixed REAL drift at HEAD — committed app.css was unminified (6361 lines, daemon-regen artifact), regenerated byte-canonical via `nix run .#webui-css`, gate verified green twice
- [x] Review-pipeline hardening: review findings must anchor commit sha + quoted anchor text; queue-side re-anchoring pre-flight for cited line ranges; disposition rule for rejected/dangling commits; dual-footer convention (05-58 report f16-f19, g1-g3 — one bad anchor cost a rework day) — DONE 2026-09-15: `ReviewFinding` gained `anchor` (verbatim quote; bare positions rejected at parse time, pinned by table rows) + `commit_sha` (backfilled from the review payload); fix prompts carry the anchor + dangling-commit disposition (`git cat-file -e` first) + single-footer ruling (exactly one footer per commit — the fix ticket's; original-task lineage lives queue-side). Tests: `internal/executor/review_test.go`, `internal/review/sweep_test.go`; gates: build/vet/tests green both modules, facade parity OK
- [x] `golangci-lint config verify` in ci-local before the run — schema-invalid keys silently disable linter settings (07-39 report f3) — DONE 2026-09-15: `scripts/ci-local.sh` gained a "lint config verify" gate step (hard failure) immediately before the advisory lint run; verified `golangci-lint config verify` passes on the root `.golangci.yml`
- [x] Rename-hygiene scanner script: sweep quoted string literals in a diff for identifier-shaped corruptions (the `q.Get("query")`/`task(store)` class caught twice by hand) (07-39 report f4; AGENTS.md rule already shipped) — DONE 2026-09-15: `scripts/check-rename-hygiene.sh` (flags double-quoted literals on added lines whose content exactly matches an identifier removed by the diff; `--strict` hard-fails, default advisory) wired into ci-local after the dead-export audit
- [x] Review-loop e2e: `scripts/smoke/reviews.sh` (stub reviewer; review approve/request_changes paths through the real binary) (07-48 report f1/f2)
- [x] `tq enqueue` guardrail: warn when `$TQ_DB` points somewhere other than `./tasks.db` (the production-DB trap cost a live enqueue 2026-09-10) (05-29 report c4/d2/e2)
- [x] `tq enqueue --wait [--timeout]`: blocking run streaming the task's facts to the terminal — cron one-shots today need tail+grep dance (05-29 report c1/f5/g3)
- [x] `tq doctor --hygiene`: stale `.tq-verify` payload pins vs current repo gate + claim-time re-resolution option — retires the stale-payload audit class (09-39 report f2/f3) (shipped 2026-09-15, commit d0e2a62)
- [x] Rate-limiter global prune: bound the write-lockout strikes map against rotating source IPs (per-contact prune only today, `internal/webui/auth.go:315`) (06-01 report c7) (shipped 2026-09-16: maxKeys cap with global idle sweep + least-recently-active eviction, live lockouts survive)
- [x] Secrets-in-logs pass + `--redact`: agent output tails can carry provider tokens into facts/sidecars (17-21 report #18; 20-58 f33) (shipped 2026-09-16: `tailBytes` choke-point redaction (default ON, `--redact=false`/`$TQ_REDACT=false` opt-out on worker + agent-pool) masks provider-token shapes in evidence tails, embedded error text and both sidecar writers; `tq audit --journal` adds the SECRET EVIDENCE scan over stored facts, never printing the secret; reviewer fix ac54a38: depbump tailOutput delegates to the same pass; reviewer fix 4ba81d8: audit hit-counts count distinct secret LOCATIONS (overlapping pattern spans merge — one `Authorization: Bearer <token>` line was double-counted as 2)
- [x] httpapi parity hardening: `X-Content-Type-Options: nosniff` + failed-bearer-auth lockout decision on `tq api` (03-05 report f2/f3/g2) (shipped 2026-09-16: nosniff on every response incl. 401; lockout DECIDED + IMPLEMENTED — 3 failed bearer auths per client IP lock ALL routes 60s (429 + Retry-After, `internal/httpapi` authRateLimiter mirroring webui's writeRateLimiter), success resets strikes, bounded map; SECURITY.md gained the `tq api` section)
- [x] CI retry-on-transient-foreign-break wrapper for ci-local (poll 45s ×3, then fail with "concurrent edit in flight" context) (15-39 report f9, e2) — DONE 2026-09-16: `scripts/ci-local.sh` gained `with_transient_retry` (on failure: sleep 45s, retry, max 3 polls, then hard-fail with "concurrent edit in flight" context; `TRANSIENT_POLL_SECS`/`TRANSIENT_MAX_POLLS` overridable for testing) wrapping the tree-reading Go gates (vet, build, windows cross-compile root+modules, cmd/tq gates, -race tests, module isolation gates, embed example, harvest-parse guard); nix steps deliberately unwrapped (they measure the staged tree — a foreign break there is about to be pushed and must fail), smokes left as a deliberate follow-up
- [ ] Extend ci-local transient-retry wrapper to the smokes + add a durable self-test pin for the retry loop (01-46 report f1/f2; smokes re-run whole up to 3× needs a runtime-budget ruling first) — self-test pin SHIPPED 2026-09-16: `scripts/check-transient-retry.sh` sed-extracts the SHIPPED helper (marker-guarded so a rotted range fails loudly), pins defaults 45s×3 + heal/exhaust/argument-forwarding/`TRANSIENT_MAX_POLLS=0` semantics, wired as a ci-local step before vet (negative-tested: catches marker rot + poll-counter drift) — BLOCKED: smoke half awaits the owner runtime-budget ruling (01-46 §g3: whole re-run ×3 vs two-phase tree-quiet probe)
- [x] Small rate-limit/observability batch (16-00 f29-f38/f43-f48): `--commits` on `tq facts`; parked×MarkOrphaned pin; closeout-resume flag in requeue fact; HTTP-date Retry-After fixture; stats parked JSON contract test; doctor earliest `not_before`; armed-provider log; NotifyDeadPool 429-transient; ratelimit-e2e second-claim assertion; postgres stale-fail fact-count parity; parked count in webui nowband; WithoutCloseout gate-leak test; ratelimit-e2e into ci-local smokes (shipped across concurrent windows, all 13 verified on HEAD 2026-09-16: `--commits` on `tq facts` (f29, facts cmd); parked×MarkOrphaned pin in BOTH stores (f30, sqlite store_test + postgres conformance); `resume_closeout` on RequeueEvidence + requeue-fact pins (f31); HTTP-date Retry-After fixture-server test (f32, http_test.go); `parked` key JSON contract test (f33, stats_test.go); doctor parked check names the earliest release (f34); RateLimitError.Provider surfaced by the armed-gate log, pinned (f35); NotifyDeadPool 429-transient pin (f36); ratelimit-e2e second-claim assertion (f37: stub reset timestamp now computed +5s ahead so the window is assertable — a claim inside the window reaches no agent, the re-claim fires only after not_before, pinned via stub-invocation counts); postgres stale-fail fact-count parity (f38, conformance_test.go); parked count in the nowband (f44, fragments.templ + webui_test.go); WithoutCloseout repo-gate-leak pin (f46); ratelimit-e2e wired into ci-local smokes (f48))
- [x] `ci.yml` concurrency group to cancel superseded runs (the daemon pushes in bursts); `check-ci.sh` "red predates your tree" wording; go-mod verify GOMODCACHE pin (16-00 f39/f41/f49) — DONE 2026-09-16: `ci.yml` gained `concurrency: ci-${{ github.ref }}` + `cancel-in-progress: true` (f39, actionlint-clean); `check-ci.sh` compares the red run's headSha against local HEAD and, when the run predates the tree, says so ("The red run predates your tree — pushing a fix? Bypass consciously with CI_CHECK=off") instead of the generic fix-master text (f49; exercised live: master red on f7fabfe — the daemon commit carrying go 1.26 downgrades — flagged as predating the repair); `check-go-mods.sh` retries `go mod verify` once per module before failing (f41, chosen over the GOMODCACHE pin — retry is surgical and doesn't relocate the CI module cache); BONUS root-cause repair: the six zero-dep leaf modules (internal/{task,journal,queue} + facades) had drifted to `go 1.26` vs root `go 1.26.7` (the documented `go mod tidy`-on-a-leaf failure class, riding daemon commit f7fabfe, red on master) — restored to 1.26.7, `check-go-mods.sh` green
- [x] Status-append cap/dedup + re-dispatch root cause: the loop's top fix (36/75 close-outs, up to 5× re-fires; a TODO fix item was proven LOST between report and TODO append) (14-01 report; 16-00 f50)
- [x] Delete dead `factLines` (`internal/webui/components.go:133`, gopls unusedfunc) + add `retry trail`/`payload section`/`work item`/`verify gate` terms to docs/DOMAIN_LANGUAGE.md (15-39 f18/f19; 14-22 f49)

## Round-13 planning additions (harvested from docs/status/2026-09-12_01-47 + the round-12 T17 tail; verified 2026-09-12)

- [ ] SystemNix tq-agent-pool unit gains `Environment=GOEXPERIMENT=jsonv2` (same treatment as agentPath) — ends the 5-window re-dispatch burn; rides the L45 input flip + deploy — BLOCKED: owner sudo on evo-x2 (2026-09-12 01-06 §g1)
- [ ] Ruling: close-out report placement — file-per-window vs append-to-one-report (01-06 CONTRADICTS 00-54 §e3); implement the ruling in the closeout executor — BLOCKED: owner placement call (2026-09-12 01-06 §g2)
- [ ] Webui rulings: unknown `/project/{name}` behavior (404 vs empty-state) + per-project-vs-global budget display in filtered views — BLOCKED: owner UX calls (2026-09-12 01-47 §g1/§g2)
- [x] AGENTS.md claims-with-citations convention: every filter-scope/behavior claim in reports and handoffs carries file:line or a test name (2026-09-12 01-47 §d3)

## Done-prompt window harvest (2026-09-12; sources: docs/status/archived/2026-09-12_04-45_done-prompt-five-task-window.md + the 03-28/03-46 close-outs + round-13 residue; verified against code this pass; 8 stale rows closed above)

- [x] Triage the RED lint-baseline gate at HEAD: 7 growth rows (executor mnd 20→24 + noctx 1→5; queue/sqlite varnamelen 32→33; worker mnd 7→8; root lll 37→38 + noctx 22→29 + varnamelen 9→10) — fix the noctx class for real, deliberate regen for mnd/varnamelen/lll with a policy note; this hard ci-local gate blocks the next full gate/push until then (verified live 2026-09-12 04:40; 04-45 report §d1) — DONE 2026-09-12 round-13: function-level attribution = `Store.ListWatermarks` varnamelen `e` (sqlite.go:1342, landed 05:02 via 5afd2b0 concurrent lineage); my session's only own-growth was payload_test.go lll (4 long lines, wrapped content-identical); the rest covered by the sanctioned deliberate regen (895de1e8, commit message carries per-module attribution); gate green at HEAD (820 findings, within baseline)
- [x] `scripts/smoke/session-close.sh`: scratch-TQ_DB smoke — begin → footer commit → close → assert one review + one status + replay-safe second close; wire into ci-local (03-28 report §f7) — DONE 2026-09-17: smoke green live (dedup on replay, 2x session.closed facts pinned), wired after reviews.sh in ci-local; required bumping cmd/tq/go.mod's go directive to 1.27.1 (root/journal/queue were bumped 2026-09-16, leaving cmd/tq misaligned and build-tq.sh's devmod failing with "requires go >= 1.27.1")
- [x] Session trigger #1 — PreToolUse session registry: hook appends {id, cwd, last_seen} per tool call; a sweeper mints the close-out when no crush process owns the ID (03-28 §f1; design doc OQ3) — DONE 2026-09-17: `tq session ping` appends the registry line (internal/session/registry.go; $TQ_SESSION_REGISTRY or cache dir; latest-wins load, torn lines skipped), `tq session sweep` closes quiet+unowned sessions through the replay-safe close and prunes the registry (ownership via pgrep -f; a FAILED ownership check KEEPS the entry — never close blind); scripts/hook-session-registry.sh is the best-effort PreToolUse hook (never blocks a tool call; owner wires it into the crush config); pinned by internal/session/registry_test.go; live smoke: ping→sweep minted exactly one review + one status, registry emptied, session-close smoke green
- [x] Session trigger #2 — `tq crush` wrapper that calls `session close` on process exit, the no-hook fallback (03-28 §f2) — DONE 2026-09-17: `tq crush [--bin --repo --project --summary --allow-dirty --id --db] [crush args...]` (cmd/tq/crush.go) runs crush with inherited stdio (operator sees everything live), tees the output, and on exit — clean OR crashed — runs the same replay-safe `tq session close` (shared runSessionClose helper with `tq session close`); session id resolution: --id > $CRUSH_SESSION_ID > scan of the child output (executor.ExtractSessionID: line-anchored `session_id:` line or the verbose INFO marker); the child's exit code is preserved (os.Exit passthrough), SIGINT/SIGTERM forwarded to the child; no id found → honest note, nothing closed, no DB touched; pinned by cmd/tq/crush_test.go (tee+close mints 1/1 over footer commits, crash path preserves exit code and records only session.closed, no-id path opens no DB); windows cross-compile gate green
- [ ] Session trigger #3 — adopt crush #3146 SessionEnd when released: point the hook at `tq session close` (enqueue-only fits the ~2s budget) (03-28 §f3) — BLOCKED: crush #3146 still OPEN upstream (merge conflicts as of 2026-09-17), absent from v0.95.0, and installed v0.94.2 has no SessionEnd hook to adopt; re-checked 2026-09-17 (second + third verify windows): PR still `{"merged":false,"state":"open"}`, v0.95.0 is still the latest release (no hook/SessionEnd/#3146 in it), installed crush v0.94.2 unchanged; re-check with `gh api repos/charmbracelet/crush/pulls/3146 --jq .merged`
- [x] Postgres session bridge: implement AppendFact on internal/queue/postgres + conformance coverage, or fail fast in `tq session` on postgres journals with a clear "bridge needs the sqlite journal" error (03-28 §f4/§g3; design doc OQ4) — DONE 2026-09-17: took the implement branch — `(*postgres.Store).AppendFact` mirrors sqlite's (same withTx + appendFact helper the task paths already use; BIGSERIAL assigns Seq, Time set when zero), pinned by a `session facts (AppendFact)` subtest in TestPostgresConformance (fact persists under the synthetic `session:<id>` key, Seq+Time assigned, detail JSON roundtrips, Get returns task.ErrNotFound — no task rows); verified live against a throwaway postgres container: full TestPostgres suite green
- [x] `tq session list` (opened-without-closed, crash recovery) + `tq session close --dry-run` previewing the attributed commits before enqueueing anything (03-28 §f8/f10)
- [x] Stale-open session visibility in doctor/stats: sessions that began and never closed; never auto-close without a ruling (03-28 §f9) — DONE 2026-09-18: doctor `open-sessions` check (cmd/tq/doctor.go:535, WARN with open ids + explicit no-auto-close wording) + `tq stats` open-sessions row and `open_sessions` JSON field; never auto-closes, verified E2E on a scratch DB (begin → stats WARN + doctor WARN → close)
- [x] Session forensics: CloseDetail records the AllowDirty/RequireClean decision + review range; `tq facts --type session.opened,closed`; `tq show session:<id>` graceful degradation for the synthetic ID (03-28 §f14/f18/f19)
- [x] Session volume in the dashboard: session-fact counts + an open-sessions lamp in `tq stats`/webui (03-28 §f20) — DONE 2026-09-18: `tq stats` counts session.opened/closed (`sessions_opened`/`sessions_closed` JSON, omitempty; human "sessions N opened / M closed" line) alongside the existing open-sessions row; webui nowband meta gains the sessions volume segment (card-sessions, silent on a bare store) + the open-sessions lamp (card-sessions-lamp) fed by a sessionStats projection over CountFacts + session.List; pinned by TestStatsSessionVolume and TestSessionSegmentsRenderFromSnapshot
- [x] Session-bridge safety pins: prune-stale must never cancel session-minted tasks (external-provenance guard test); sweeper-namespace cross-dedup (`review:session:<id>` never collides with sweeper `review:<task-id>`); trailer-parser edge tests (empty/repeated/whitespace footers) (03-28 §f15/f16/f17) — DONE 2026-09-18: f15 pinned by TestPruneStaleNeverTouchesSessionMintedTasks (prune lists only the harvester's own task type, so pending `review:session:`/`status:…:session:` tasks survive a sweep), f16 was already pinned by TestSessionAndSweeperReviewKeysStayDisjoint (verified at HEAD, internal/session/session_test.go), f17 pinned by TestParseTrailerCommitsIgnoresBlankAndWhitespaceFooters + TestParseTrailerCommitsRepeatedFooterAttributesOnce
- [x] Multi-repo session semantics: document one-close-per-(session,repo) or design multi-repo close (replay-dedup assumes one close today) (03-28 §f11)
- [x] Session ergonomics: git ≥ 2.15 guard with an actionable message; zero-commit close prints the footer-convention hint (begin already does) (03-28 §f12/f13)
- [x] gosec post-config scan over internal/session + the cmd/tq session call sites stays zero; extend the AGENTS.md gosec triage note with the new exec(git) sites (03-28 §f23)
- [x] scripts/check-gosec.sh: one-command post-config gosec gate (pinned v2.29.0, encoded excludes, asserts Files > 0) — must fail on the in-module Files:0 silent skip (`cd internal/session && gosec ./...` returns 0/0 silently; scan from repo root via `./internal/session/...` instead) (02-38 §e1) — SHIPPED: scripts/check-gosec.sh (root `./...` + per-module `GOWORK=off` scans; any `Files: 0` is a hard FAIL — the in-module no-GOWORK invocation is the proven silent-skip shape, root-path scans cover root-module packages); wired into ci-local.sh; 17/17 scans green (Files 1-41, Issues 0)
- [x] Add `crush` to the AGENTS.md cmd/tq subcommand list + a one-line wrapper note in the session-close bridge bullet (the 02-51 close-out's top starred item, still unfixed at the 02-54 re-dispatch)
- [x] Re-cite `scripts/smoke/session-close.sh` green over the `runSessionClose` extraction AS USED BY the `tq crush` wrapper (one smoke run + citation; unit tests cover the wrapper path, the smoke hasn't been re-cited since the refactor) (02-51/02-54 §f) — DONE 2026-09-19: smoke green over the extraction (1 footer commit attributed → exactly 1 review + 1 status minted → replay close dedup, 1 session.opened + 2 session.closed facts); wrapper path proven LIVE E2E — `tq crush --bin <stub child>` over a scratch repo: child session id scanned from output, close minted 1 review + 1 status through the shared `runSessionClose` (cmd/tq/crush.go:114), child exit code preserved; cmd/tq unit gate green (`scripts/test-cmd-tq.sh`, incl. the crush_test.go wrapper pins)
- [x] Document `tq crush` in README (short subsection under session triggers) + FEATURES.md row (02-54 §f) — DONE 2026-09-19: README gains "Session triggers: `tq crush`" under the command map (wrapper semantics: inherited stdio + tee, id resolution --id > $CRUSH_SESSION_ID > output scan, exit-code passthrough, signal forwarding, no-id = nothing closed; the two hook-based triggers named) + a 🟢 FULLY_FUNCTIONAL FEATURES row after `tq session begin/close` citing the crush_test.go pins
- [x] Live-no-id smoke assertion: wrapper exit preserves child code, nothing closed, no DB touched (no-id path is unit-pinned; live path isn't) (02-54 §f) — DONE 2026-09-19: scripts/smoke/session-close.sh gains the committed live leg — real `$TQ crush --bin <stub child>` where the child prints no session marker and exits 7: wrapper process exit code == 7 (passthrough), child stdout forwarded live, stderr carries the "no session id" note, and the explicit `--db` target file never exists (nothing closed, nothing minted, no DB touched); smoke green end-to-end (exit 0 captured bare) + negative control (mutated copy expecting 8 exits 1 with the FAIL line — the harness bites), shellcheck clean (`scripts/check-script-syntax.sh`)
- [ ] check-gosec.sh: cover cmd/tq (for-each-module.sh omits it, so the gate and the CI gosec loop both skip the CLI module; devmod shim or an owner ruling that CLI gosec stays CI-only) (02-52 gosec-gate report §b) — BLOCKED: owner ruling on CLI gosec coverage (03-07 §g3)
- [x] check-gosec.sh: verify a pre-existing GOSEC_BIN is actually v2.29.0 (today the pin only governs the install path; stale binary silently changes what green means) (02-52 gosec-gate report §b) — DONE 2026-09-19: check-gosec.sh now verifies the DISCOVERED binary (GOSEC override + PATH/GOPATH auto-discovery) against the pin before any scan: stamped ≠ v2.29.0 hard-FAILs fast with the `go install …@v2.29.0` fix line (stub-tested, exit 1, nothing scanned); UNSTAMPED (`Version: dev`, this host's 02-52 scratch build) WARNS loudly and continues, because go install is sandbox-blocked for agent sessions and binary provenance is the still-open 02-52 §g q2 owner ruling; only the script's own install path is exempt (v2.29.0 by construction); verified all three branches (FAIL stub, OK stub, real dev-stamped binary full run: 16/16 scans green, exit 0) + check-script-syntax.sh 0 findings; until the owner installs a stamped v2.29.0 binary, every ci-local gosec step carries the WARN line
- [x] check-gosec.sh `--self-test`: pin the three version branches (stamped pin ok / stamped mismatch FAIL / unstamped WARN) plus the Files:0 parse with canned stub binaries, so the proof stops living in report prose (the 2026-09-19 verify run's /tmp stubs are the design; 02-52 report §f3) — DONE 2026-09-19: `check-gosec.sh --self-test` creates canned gosec stubs (mktemp dir outside gated trees, trap-cleaned) and runs the shipped script RECURSIVELY with GOSEC pointed at each stub, so every branch is exercised end to end: stamped-ok (full gate green, no WARN/FAIL), stale v2.28.1 (exit 1, exact detected-version + fix lines, zero scans — `== (root)` asserted absent), dev-stamped (WARN + gate proceeds), Files:0 summary (silent-skip FAIL), Issues>0 summary (new-class FAIL); stub outputs auto-adapt to a future GOSEC_VERSION bump (only stale/dev stamps are hardcoded, both pin-independent); wired as a ci-local step before the real gate; 18/18 assertions green, real gate re-run green (16 scans), check-script-syntax 0 findings, check-guard-wiring ok, root build+vet green
- [ ] Converge ci.yml's hand-rolled per-module gosec loop onto scripts/check-gosec.sh (settles whether the cd-without-GOWORK loop hits the Files:0 silent skip on runners; counter-risk: a broken script would then silence both layers) — BLOCKED: owner ruling on CI independence (02-52 report §g q3)
- [x] AGENTS.md gosec baseline note: point at scripts/check-gosec.sh as the canonical one-command gate entry (the note predates the script; 02-52 report §f5) — DONE 2026-09-19: the note's header now names `./scripts/check-gosec.sh` as the canonical one-command gate entry (same v2.29.0 pin + exclude set, root `./...` + per-module `GOWORK=off` scans, Files:0 hard-fail); check-doc-refs.sh green
- [x] check-gosec.sh: hard-fail when the module loop enumerates 0 targets — today a broken/empty `scripts/for-each-module.sh` silently narrows the gate to the root scan only and exits 0 (process-substitution exit status ignored); same silent-skip family as Files:0, one level up (02-18 report §e6) — DONE 2026-09-19: the gate enumerates FIRST via `modules="$(enum)"` and hard-fails BOTH failure shapes before any scan: a non-zero enumerator exit (`||` handler replaces the ignored process-substitution status) and a zero-target list (`-z` guard); the module loop now feeds from the captured list (`<<<"$modules"`); new branches pinned in `--self-test` via a `TQ_GOSEC_ENUM` stub-enumerator hook (mirrors the GOSEC stub hook) — empty list and failing enumerator both exit 1 with `== (root)` asserted absent (fail-fast before any scan), 24/24 self-test checks green (7 stub-gate runs + 17 assertions); real gate re-run green (exit 0, root + every sub-module), check-script-syntax.sh 0 findings
- [x] check-gosec.sh: print an end-of-run "N scans ok, M failed" summary line so close-out citations stop hand-counting ok-lines (16 scans today = 1 root + 15 module targets; the 02-09 report's "1 root + 16 module scans" miscount included the version-ok line — 02-18 report §e3/§f3) — DONE 2026-09-19: the gate prints `gosec summary: N scans ok, M failed` after the scan loop; counters live in the PARENT shell and are incremented per scan via if/else (the module loop runs scan() inside a subshell, so counters incremented in scan() would never propagate), so the version-ok line can never inflate the count — it is not a scan; both counts pinned in `--self-test` (green run asserts "scans ok, 0 failed", Files:0 stub run asserts "0 scans ok," with every scan failed); real gate re-run green: `gosec summary: 16 scans ok, 0 failed` = 1 root + 15 module targets, rc=0; --self-test 26/26 green, check-script-syntax.sh 0 findings
- [x] ci.yml gosec job: derive GOSEC_VERSION + excludes from scripts/check-gosec.sh (sed the GOSEC_VERSION= line) instead of the second hand-pinned copy (ci.yml:277) — a future pin bump must not need two edits; does NOT force row-127's full convergence (02-18 report §e5/§f4, question §g3) — DONE 2026-09-19: the job seds both values from scripts/check-gosec.sh (`s/^GOSEC_VERSION=//p` for the pin, quote-stripping capture for GOSEC_EXCLUDES), fails closed on an empty derivation before any scan, installs/summarizes via the derived `$GOSEC_VERSION`; derived values verified byte-identical to the old hand-pinned pair, actionlint green, loop itself untouched (row 127 stays owner-blocked)
- [x] check-gosec.sh `--self-test`: resolve the self path to an ABSOLUTE path BEFORE the top-level `cd` — the recursive child re-invokes `"$0"` and from inside `scripts/` under stock bash that is `./check-gosec.sh` from the repo root (rc=127, "No such file or directory", reproduced 2026-09-19; the session's mvdan shell masks it, ci-local's root-relative call unaffected) + add an assertion that the mode works from a foreign CWD (02-38 report §b/§d4) — DONE 2026-09-19: `self_abs` is captured via `cd $(dirname) && pwd` before the top-level cd and both gate_run child invocations use it, so recursion is invocation-position-independent; new self-test branch re-runs the mode from `/` guarded by `TQ_GOSEC_SELFTEST_REENTRY` (one re-entry level, else infinite recursion); evidence: stock-bash repro healed (`bash ./check-gosec.sh --self-test` from scripts/ rc=127 → rc=0, 27/27 ok incl. the foreign-CWD line), root-relative and `cd /tmp` invocations rc=0, negative control (in-tree copy with `$0` restored, trashed after) trips rc=127 again so the assertion is live, real gate green (16 scans ok, 0 failed), check-script-syntax.sh 0 findings
- [x] for-each-module.sh consumers: zero-target enumeration silently narrows every per-module loop — ci.yml's six `for m in $(...)` loops catch NEITHER a non-zero enumerator exit NOR an empty list (ci.yml:45,58,96,158,251,280), ci-local.sh:73 catches non-zero via `set -euo pipefail` but not empty (harvested 2026-09-19, gosec 0-target window d035911); same silent-skip family as the 02-18 §e6 fix, one consumer layer up — DONE 2026-09-19: the enumerator itself now hard-fails on 0 module targets (FAIL to stderr + exit 1; clean-empty repro in a scratch tree: find succeeds, zero go.mods → rc=1, find-failure paths rc=1 too), so every consumer capturing the list under errexit inherits both guards (ci-local.sh:73 needed no edit; check-gosec.sh gate mode likewise); ci.yml's six loops switched from word-position substitution (which ignores the enumerator's exit status entirely) to `mods="$(./scripts/for-each-module.sh)"` + `for m in $mods` — the assignment under `bash -e` fails the step on enumerator death, and the enumerator refuses to exit 0 empty, closing both shapes; evidence: capture-vs-word-position demo under bash -ec (rc=1 vs silent loop skip), enumerator happy path rc=0 (15 modules), ci.yml YAML-parses with all six captures at ci.yml:45,59,98,161,255,294, check-script-syntax.sh 0 findings, check-gosec.sh --self-test green incl. its empty/broken-enumeration branches, root build+vet+`go test ./... -race` green; re-verified 2026-09-19 (second re-dispatch, verify-only, zero source delta since the fix commit): both failure branches RE-EXECUTED fresh in a /tmp scratch tree (clean-empty rc=1 with the exact FAIL line; missing-roots rc=1 via pipefail), happy path rc=0 (15 targets), all six ci.yml capture-form loops confirmed at :45/:59/:98/:161/:255/:294 + ci-local.sh:73 under `set -euo pipefail`, check-script-syntax.sh rc=0, ci.yml YAML-parses, root build+vet+`go test ./... -count=1 -race` rc=0 (15 ok / 0 FAIL) at HEAD; re-verified 2026-09-19 (third re-dispatch, verify-only, still zero source delta since the fix commit): both failure branches RE-EXECUTED with branch-contract fixtures (clean-empty = all six roots present + zero go.mods → rc=1 via the -z guard, exact FAIL line, zero stdout; missing-roots → rc=1 via pipefail, find errors, no FAIL line — this window's first fixture attempt repeated the 07:16 §d2 mis-built-fixture mistake (top-level executor/worker absent) and was caught by branch attribution before any claim), happy path rc=0 (15 targets), all six capture loops re-confirmed at :45/:59/:98/:161/:255/:294 + ci-local.sh:73, check-script-syntax.sh rc=0, ci.yml YAML-parses, root build+vet+`go test ./... -count=1 -race` rc=0 (15 ok / 0 FAIL) at HEAD
- [x] check-gosec.sh: pin a SHORT module list as a failure — a non-zero-but-short enumeration (a renamed sub-module dropping out of `find`) scans the survivors and exits 0 today; the 0-target guard (d035911) and the per-scan Files>0 guard (scripts/check-gosec.sh scan()) are both blind to missing members; needs an expected-count or canary-module assertion — DONE 2026-09-20: canary-module form chosen over expected-count (a count needs a conscious bump on every ADDED module, breaking the enumerator's "newly added modules need no consumer edits" property; canaries pin identity, not cardinality): the gate pins the five core sub-modules (internal/task, internal/journal, internal/queue, internal/executor, internal/worker) and hard-fails before any scan when the enumeration is missing ANY of them, naming every missing canary + the conscious-update fix hint (bash array, `grep -qxF` exact-line membership, so suffix-similar paths can't false-match); new branch pinned in `--self-test` via an `enum-short` stub enumerator (returns internal/task + internal/journal only — the exact survivors shape both older guards accept): rc=1 asserted with `== (root)` absent; evidence: self-test 31/31 ok (8 stub-gate runs incl. the new short-enum run), real gate green end-to-end through the real enumerator (16 scans ok, 0 failed, rc=0; dev-stamped gosec WARN is the documented host branch), check-script-syntax.sh rc=0 (shellcheck 0 findings), check-doc-refs.sh rc=0
- [x] End-of-run "N ok, M failed" summary lines for the sibling countable check-* gates (check-script-syntax.sh, check-go-mods.sh, check-dead-exports.sh, check-guard-wiring.sh): check-gosec.sh got its scan summary 2026-09-19 (81f2db8) because close-outs hand-counted ok-lines; the same hand-count pain applies to every sibling gate's per-script/per-module output lines (close-out harvest 2026-09-19 04-46; fleet-convention-or-gosec-only ruling: that report §g2) — DONE 2026-09-20: the gosec mechanism (parent-shell counters incremented per check via if/else, one summary echo before exit) applied to all four gates — syntax: `syntax summary: N checks ok, M failed` (52 per-script bash -n results + the single shellcheck pass as one check), go-mods: `go-mods summary: N checks ok, M failed` (2 repo-level greps + per-module toolchain alignment + per-module `go mod verify`, retry counted once per module), dead-exports: `dead-exports summary: N symbols ok, M dead` (printed before the strict exit so it survives both paths), guard-wiring: `guard-wiring summary: N scripts wired, M orphaned`; evidence: green runs rc=0 with summaries 53/0, 35/0 (= 2+16+17, matches the 16-module enumeration), 304/66, 32/0; negative shape proven on guard-wiring via create+assert+trash temp orphan (rc=1, `32 scripts wired, 1 orphaned`, post-trash rc=0); check-script-syntax.sh rc=0 (own edits shellcheck-clean)
- [x] ci.yml gosec derivation drift guard: check-gosec.sh `--self-test` (or a ci-local step) must run ci.yml's exact sed lines against the shipped script and assert both extractions are non-empty and byte-match the pinned constants — today the derivation (ci.yml:281-282) runs only on runners inside a continue-on-error job, so a format drift in the check-gosec.sh:22-23 constants fails advisory-only in CI and no local gate catches it pre-push (harvested 2026-09-19, derive window) — DONE 2026-09-19 (fix commit 39d340e), re-verified 2026-09-20 (verify-only re-dispatch, zero source delta since the fix): the self-test's drift branch greps ci.yml for BOTH `GOSEC_VERSION="$(sed` and `GOSEC_EXCLUDES="$(sed` lines (fail-fast with an exact FAIL line if either vanishes — the "workflow dropped the derivation" shape), evals the EXACT lifted lines so any quoting change in ci.yml is what gets tested, asserts both extractions non-empty (the runner's empty-derivation fail-closed shape), and byte-compares both results against the pinned `GOSEC_VERSION`/`GOSEC_EXCLUDES` constants; negative control re-executed fresh in a /tmp worktree (mangled pin v2.30.0 → rc=1 with the exact mismatch FAIL naming both values, worktree removed after); green path rc=0 with `ok: ci.yml derivation matches the pinned constants byte-for-byte`; check-script-syntax.sh rc=0 (53/0), check-guard-wiring.sh rc=0 (32/0) at HEAD
- [x] .golangci.yml gosec.excludes is the last hand-synced copy of the triage (ci.yml now derives from check-gosec.sh) — derive it or add a parity assertion that the linter's exclude set equals check-gosec.sh's GOSEC_EXCLUDES, so a triage change is one edit with zero silent skew (AGENTS.md gosec note promises scanner parity; harvested 2026-09-19, derive window)
- [x] check-gosec.sh --self-test: make the assertion-liveness negative control daemon-proof — the rc=127 control currently modifies a gated script IN-TREE (copy with "$0" restored, trashed after; 06-02 window), daemon-food shape per AGENTS.md; run it from a /tmp extract instead (copy scripts/ to a scratch dir, sed-revert the self_abs line back to "$0", invoke CWD-relative from the extract's scripts/) and assert the same rc=127 (2026-09-19 06-22 report §b/§f10) — DONE 2026-09-20: the control now builds a /tmp extract (`$tmp/negctl/scripts/check-gosec.sh`) of THIS script with the `self_abs` line sed-reverted to `self_abs="$0"`, invokes it CWD-relative (`cd $ctl_dir && bash ./check-gosec.sh --self-test`) under `TQ_GOSEC_SELFTEST_REENTRY=1`, and asserts rc≠0 plus an `rc=127` line in the child output; the gated tree is never touched (in-tree copy+trash shape gone) and the extract dies with the EXIT trap; verified: `bash ./check-gosec.sh --self-test` from scripts/ rc=0 with `ok: negative control trips rc=127 on the reverted self_abs extract (assertion is live)` (34/34 ok), check-script-syntax.sh rc=0 (53/0), tree clean after the run
- [x] for-each-module.sh: pin the same five canary sub-modules check-gosec.sh now pins (internal/task, internal/journal, internal/queue, internal/executor, internal/worker) so ci-local.sh:73 and ci.yml's six per-module loops also hard-fail on a non-zero-but-SHORT enumeration (renamed sub-module dropping out of `find`) — the gosec gate carries its own canary assertion (2026-09-20) but the CI consumers still scan survivors silently; prefer single-sourcing the list in the enumerator so every consumer inherits — DONE (verify window 2026-09-20, zero source delta; assertion landed in d115f37): short-enumeration branch RE-EXECUTED in a /tmp scratch tree (internal/task moved away → rc=1, exact FAIL line naming the missing canary + conscious-update hint; restored → happy rc=0, 15 targets); all six ci.yml loops on the enumerator capture form (ci.yml:45,59,98,161,255,294) + ci-local.sh:73 inherit the hard-fail; check-script-syntax.sh rc=0 (53 checks), check-gosec.sh --self-test green, root build+vet rc=0 at HEAD
- [x] Dead-SHA citation gate: docs citing a commit SHA must not cite one unreachable from master — history rewrites (msg-filter heals) leave stale live citations behind (dd543ec survived in TODO_LIST 133/134 for one window, healed in the 0-target window by d035911/ca514a8, reachable side); extend scripts/check-doc-refs.sh (or a sibling script) to extract 7-40-hex tokens from docs/status/*.md + TODO_LIST.md + AGENTS.md and fail on any not reachable from HEAD, with a fork-record escape (`old→new` arrow form or an allowlist file) so the sanctioned AGENTS.md:408 fork note and healing reports keep citing the dead side; shape open: hard ci-local gate vs advisory — DONE 2026-09-20: new sibling gate scripts/check-dead-sha-refs.sh, HARD ci-local step (after doc-reference check): only tokens resolving to a REAL commit object count (task IDs/CI run numbers ignored — the 000001a0… ULID class), reachable-from-HEAD passes, `old→new`/`→` arrow lines escape as fork records, everything else fails; the 242 legacy dead SHAs from the 2026-09-10/09-19 rewrites are absorbed in scripts/dead-sha-baseline.txt (973 baselined cite-sites, silent unless TQ_DEAD_SHA_VERBOSE=1; shrink advisory-only, NEW dead citations fail); verified: clean run rc=0, baseline-removed run rc=1 with exact file:line hits, arrow-line escape passes, final gate rc=0 at HEAD
- [x] Shrink the dead-sha baseline (scripts/dead-sha-baseline.txt, 242 SHAs / 973 cite-sites from the 2026-09-10/09-19 msg-filter rewrites): re-resolve each dead SHA to its reachable twin via the reflog/filter-branch playbook (docs/status/archived/2026-09-10_07-49 report §e), repoint the living-doc citations, drop from baseline — MUST complete before any `git gc` or the refs/original drop (existing row above), or the old→new mappings become underivable; gate: `./scripts/check-dead-sha-refs.sh` stays rc=0 after each drop, TQ_DEAD_SHA_VERBOSE=1 counts shrink toward 0 — DONE 2026-09-20: 214/242 SHAs re-resolved to reachable twins (staged signature matching over `--all --reflog`: subject+author-date unique → tree+author-date → tree → subject-only, candidate must be a HEAD ancestor; 971 cite-sites repointed across AGENTS.md + TODO_LIST.md + docs/status/*.md top level), 4 same-subject collision mis-maps rejected back to baseline (1537fd2/c888be6/4a28269/5d93115 daemon folds with no derivable twin), 22 squashed-away intermediates from abandoned lineages left baselined (no twin exists — their citations are inherently unresolvable), dd543ec/e9ef358 kept baselined for the heal-forensics prose and the sanctioned :408 fork note that DESCRIBE the dead side (20 dead-context sentences audited against the repoint diff and restored — the "replaced `d035911` with `d035911`" falsification class); baseline now 28 SHAs / 82 silent cite-sites (was 242/973); verified: check-dead-sha-refs.sh rc=0, verbose STALE 1000→82, gate green after every baseline edit (residue shrink needs positional rebase mapping — follow-up row below)
- [x] check-dead-sha-refs.sh `--self-test` mode: pin the gate's semantics durably (clean run rc=0, non-baselined dead SHA rc=1 with exact file:line, arrow `old→new` line passes, baseline-hit silent unless TQ_DEAD_SHA_VERBOSE=1) using /tmp fixture files, wired into ci-local next to the gate — mirror of check-gosec.sh --self-test; the semantics are currently execution-verified once (2026-09-20 05-16 window), not pinned — DONE 2026-09-20: gate logic extracted into run_gate (baseline + files as caller state), --self-test builds a /tmp fixture git repo (dangling commit via commit-tree child of HEAD — no git reset) and pins all four branches plus the arrow-line escape; wired as ci-local step right after the gate; verified: --self-test rc=0, real gate rc=0 at HEAD, check-script-syntax 0 findings
- [x] Re-point AGENTS.md:758/793 dead-SHA cites (c7ca518; d5ee08d/c95adb8) to their reachable twins via reflog resolution — keep the sanctioned :408 fork-note pair (dd543ec/e9ef358) as-is; drop the three from scripts/dead-sha-baseline.txt only after the citations are healed and the gate stays rc=0 — DONE 2026-09-20 (absorbed into the baseline-shrink pass, row above): 748e989→c7ca518, 9b7c46b→d5ee08d, da8f331→c95adb8 repointed in AGENTS.md:758/793 and everywhere else cited; the three dropped from the baseline; :408 fork-note pair untouched (still baselined, still cited); gate rc=0
- [ ] Drop the msg-filter backup refs (refs/original + any filter-branch backup) once the healed lineage is pushed — AGENTS.md history policy keeps them "until the push lands" (heal d035911/ca514a8 local-only, origin/master 34 commits behind as of 2026-09-19 04:2x); the DROP itself is a destructive git op — BLOCKED: owner push of the healed lineage + owner ruling on ref deletion
- [x] AGENTS.md Conventions one-liner: DONE-note citations may only be WRITTEN after the cited gate's output is in hand (claims-carry-citations governs WHAT to cite; the ORDER needs its own sentence — the 02-38 window wrote "real gate re-run green (16 scans)" while the background gate was still running and its first exit-code capture had come back empty via a PIPESTATUS bashism) (02-38 report §d1/§e1) — DONE 2026-09-20: "The citation may only be WRITTEN after the cited gate's output is in hand — never cite a gate that is still running" + 02-38 precedent added to the claims-carry-citations bullet in AGENTS.md Conventions
- [x] Committed wrapper-path smoke leg: drive `tq crush --bin <stub child>` over a scratch repo (footer commit + `session_id:` output), assert id scan + 1 review + 1 status minted through runSessionClose + exit passthrough; wire into ci-local after session-close.sh — the wrapper's only E2E today is unit pins + the ad-hoc stub run cited in docs/status/2026-09-19_00-10_task-000001a0b68a0a70be1e9eb8ecdbb32cff59.md (gate-vs-cited-one-off ruling pending, report §g1)

- [x] Dead-SHA residue positional mapping: the 28 baselined SHAs after the shrink pass (scripts/dead-sha-baseline.txt) split into 22 squashed-away daemon/rebase intermediates + 4 wrong-twin-rejected folds + dd543ec/e9ef358 fork-note prose; the 22+4 have no subject/tree twin — resolve positionally (anchor each in the pre-rewrite lineage between two mapped twins via reflog tips, then identify the surviving commit that absorbed the content) or rule them citation-lost and drop the baseline rows when the refs/original drop lands (existing row above) — only while refs/original + reflog still hold the old lineage — DONE 2026-09-20 (same task): every remaining SHA re-checked by EXACT patch-id match over HEAD (content-identity, immune to the same-subject collisions that rejected these in the shrink pass) + positional anchoring (reachable parent / refs/original ancestry): the 4 wrong-twin folds RESOLVED content-exactly (1537fd2→9aaa7fc, c888be6→9aaa7fc, 4a28269→e2e3f2e, 5d93115→d035911 via dd543ec's twin), 7 more of the residue carry exact patch-id twins (3a394f0→8759e6d, 4d7605c→d7a0f2e, 8ef1330→4ba81d8, a1a3fe6→0369e09, d26252d→d6f5ad0, dd543ec→d035911, e9ef358→ca514a8); their citations deliberately DESCRIBE the dead side (amend/fork forensics), so they stay per the shrink pass's falsification ruling; the remaining 15 squashed-away intermediates ruled CITATION-LOST (no patch-id twin, no tree twin; absorber spread across multiple surviving commits — reachable parents recorded per SHA in the baseline header); verdicts recorded in scripts/dead-sha-baseline.txt comments — drop ALL rows only when the refs/original drop lands; one new same-class residue absorbed (78e2768→4172f1e, exact patch-id); verified: check-dead-sha-refs.sh rc=0 (107 baselined, 0 new), --self-test ok, refs/original + reflog untouched
- [x] Session-start ritual script: `scripts/session-start.sh` runs the turn-1 ritual (whole-repo `git log`/`git status`/`git stash list` + task-ID grep over `docs/status/` + CONTRIBUTING.md head) so the documented skip/late class (00-52 d4, 02-17 d3, 00-22 d1, 00-32 d2) becomes structurally impossible (00-22 §e1; recurring ask across ≥7 reports, first minted as a row 2026-09-19 00-32) — DONE 2026-09-20: script shipped (read-only; optional task-ID args drive the `rg -l <id> docs/status/` grep; CONTRIBUTING.md + CLAUDE.md heads included), AGENTS.md ritual bullet now points at it, check-script-syntax.sh green (54 checks, 0 findings), smoke-run with this task's ID rc=0
- [x] Verify-window minimum battery (AGENTS.md Conventions clause): a verify-only re-dispatch window must re-run the CHEAP gates itself at HEAD (check-doc-refs.sh + root build+vet under GOEXPERIMENT=jsonv2 — seconds each) and produce at least ONE fresh delta or state explicitly why none exists; expensive gates (race suite, nix) may be inherited only from a same-HEAD prior report (03-27 §e2 set the bar; the 03-39 window regressed by citing inherited gate runs in its stop verdict; extension of the citation-order row above)
- [x] for-each-module residual consumers: lint-baseline.sh:35 + lint-annotations.sh:69 derive the module list INLINE in word position (`for m in $(find …)`) so a failing find AND an empty list both silently narrow the loop (missing module rows read as baseline shrink, so --check vacuously passes), and check-go-mods.sh:31 captures under errexit (find-failure covered) yet a zero-target list still narrows its loops silently — switch all three to a captured, guarded enumeration, preserving each script's exact module set (check-go-mods extends with cmd/tq, which for-each-module.sh omits per row 124) (harvested 2026-09-19 07:00 verify window on the row-133 re-dispatch; same silent-skip family)
- [x] Regression pin for the row-133 capture shape: NOTHING fails today if a future ci.yml/ci-local.sh edit reverts a module loop to word-position substitution (`for m in $(…)`) and the enumeration guards silently rot back — add a cheap mechanical assertion (ci-local step or check-guard-wiring-style audit: every `for m in $mods` in ci.yml + scripts/ci-local.sh must be fed by a `mods="$(./scripts/for-each-module.sh)"` capture) (harvested 2026-09-19 07:00 verify window) — DONE 2026-09-20: scripts/check-module-loop-capture.sh (audit-style, wired as a ci-local step after guard-wiring; wired-ness itself caught by check-guard-wiring.sh) pins three rules over ci-local.sh + ci.yml: every `for m in <list>` must iterate `$mods` (no inline word-position substitution), every `mods=` assignment must be the `mods="$(./scripts/for-each-module.sh)"` capture, and any file with loops must carry at least one such capture; evidence: happy path rc=0 (ci-local.sh loops=3/captures=1, ci.yml loops=6/captures=6), negative test re-executed fresh (inline `$(find internal -name go.mod)` loop → BAD LOOP rc=1, then restored byte-identical from backup), check-script-syntax.sh rc=0 (shellcheck 0 findings), check-guard-wiring.sh rc=0 (35 wired/0 orphaned), root build+vet+`go test ./... -count=1 -race` rc=0 (15 ok / 0 FAIL) at HEAD under GOEXPERIMENT=jsonv2

## Priority-system follow-ups (harvested from the 2026-09-12 T27-T38 windows; reports 15-43 + 16-28; verified against code at mint time)

- [x] `tq tasks --band hot|machine|backlog` CLI parity with the webui band filter — the store-level `Filter.PriorityMin/PriorityMax` pushdown exists in BOTH backends (`listWhere` sqlite, `pgWhere` postgres, conformance-tested); only the CLI flag is missing (16-28 report f9; cmd/tq tasks + internal/queue Filter)
- [ ] Webui task detail: priority provenance section (current + band, item key + markerLevel for harvest tasks, cached AI verdict, repri history distilled from the fact trail) — `tq show`'s `buildPriorityProvenance` (cmd/tq/main.go) has all of it; the page shows only the raw record (16-28 report f11; internal/webui render.go/handlers.go)
- [ ] Webui board: band-grouped columns (hot / machine / backlog) instead of status columns, or band as a secondary grouping — plan item 44's tail (16-28 report f10; internal/webui/fragments.templ)
- [ ] Executor usage parsing: populate the `tokens` field on PrioritizeResult/agent results (always 0 today) so one-batch scorer cost is measurable from the journal — feeds the post-pilot calibration (16-28 report f12; internal/executor result.go/agent.go, budget projections)
- [ ] Score-cache TTL/eviction pass: stale item texts keep their cached verdicts forever in `priority_scores` (bounded by repo count today); add a prune/refresh pass keyed on item-text change or age when the cache grows (16-28 report f8; internal/prioritize + internal/queue stores)
- [ ] Publish the v0.3.0 GitHub Release (`gh release create v0.3.0` with the CHANGELOG section, or the `release.sh --push` resume path) + spot-check pkg.go.dev renders the 7 facade modules — HALF DONE 2026-09-13: the release was created by the tag push (full CHANGELOG body) and is now published/Latest (was Pre-release; `gh release edit v0.3.0 --prerelease=false --latest`); all 7 facades resolve v0.3.0 from proxy.golang.org + clean-room enqueue pass (ALL-FACADES-OK); REMAINING: pkg.go.dev render check (still 404 as of 09:0x — crawl lag after the tag push, proxy hit usually triggers it; re-check and treat persistent 404 as a blocker to investigate)
- [ ] Ruling: public `journal/cqrs` facade or internal-only? ADR-0016's seven facades leave the PROPRIETARY go-cqrs-lite seam on the `internal/` path, so the external ecosystem consumers the adapter names cannot import it — BLOCKED: owner facade-scope call (08-00 §g1, 08-21 §g1)
- [ ] Ruling: may an agent patch `cqrs-lint` rules in the owner's local go-cqrs-lite repo? Evidence scoped for three factual fixes (A014 stale deprecation claim, D013 default-blindness, V006 lockstep-policy assumption) — BLOCKED: owner tool-policy call (08-21 §g3)
- [ ] Enable `--prioritize` on the dogfood pool — gated on the one-batch live scorer cost measurement (T28: ONE batch against a scratch journal = real API spend) recording tokens + wall time; the flag, sweeper, and budget gating are shipped and default-OFF (16-28 report g3; internal/prioritize, cmd/tq agentpool) — BLOCKED: owner spend go/no-go (real money)
- [ ] Post-pilot calibration of the keyword table + marker ladder from real claim order (T34 feedback loop over review verdicts × score source) and re-check G1 sizing (2000 items / 50 repos) against the observed drain rate — needs the pilot's first window of data (16-28 report f6/f7) — BLOCKED: owner pilot start (SystemNix proposal at docs/planning/2026-09-12_priority-system-dogfood-pilot.md)
- [ ] Dead-export audit re-run (scripts/check-dead-exports.sh, substring matching) over internal/session's exports (Trailer, SyntheticTaskID, CloseInput, CloseResult, GitScanner, …) (03-28 §f31)
- [ ] Session E2E dogfood proof: one REAL interactive session through begin→commits→close on this repo with the pool reviewing the minted task (03-28 §f28) — BLOCKED: spends API money on the real agent run (owner-gated spend)
- [ ] Mirror the git-identity sanitizer into ci.yml's release-gates step (ci.yml:106) or document the ci-local-only asymmetry as deliberate (03-46 §f2)
- [ ] Self-testing negative fixture in release-gates.sh: a temp copy with the `-c user.*` flags stripped must fail "Author identity unknown", so the identity-blind property can't rot silently (03-46 §f3)
- [ ] AGENTS.md one-liner near the smoke list: the release-gates smoke runs identity-blind via ci-local (`GIT_CONFIG_GLOBAL=/dev/null` + `user.useConfigOnly`) — so nobody "simplifies" it away (03-46 §f5)
- [ ] `tq show --commits`: surface footer-less daemon commits ADJACENT to a footer-bearing work commit as "folded here" instead of silent (the 147bd17 shape) (03-46 §f9)
- [ ] Bank the gochecknoglobals shrink (root 9→7 after the twin-list retirement) in the same regen that resolves the baseline-triage item (04-31 §f14)
- [ ] Check whether scripts/lint-lll-changed.sh excludes `*_templ.go`: generated long lines make every templ regen a latent gate trip (04-31 §f16)
- [ ] Share the two hardcoded status oracles in internal/task status_test.go as one package-level test const feeding TestStatusTableExhaustive + TestAllStatuses (04-31 §f13)
- [ ] Commit-msg hook: require the Task-Queue-ID footer in the FINAL trailer block — mechanical prevention of the footer-before-attribution mistake (04-13 §d3; 04-31 §f8)
- [ ] Re-tag `internal/task` at the next release + bump the root require off v0.2.0 so proxy consumers receive `task.AllStatuses()` (04-31 §f3; rides the L124 public-API ruling)
- [ ] Run `./scripts/ci-local.sh` end-to-end on the current lineage before the next push — the nix + master-CI delta no recent window has covered; also surfaces the baseline triage (03-46 §f6)
- [ ] `tq tasks`: expose payload/verify content (`--json` payload field or `--verify-contains SUBSTR` filter) so stale-verify hygiene audits stop needing a `tq show` per task (09-39 report §e1)
- [ ] `tq doctor --hygiene` (or audit extension): encode the stale-queued-task check — no pending agent task carries a verify string matching known-stale patterns — so the f46-style audit is a gate, not an investigation (09-39 report §e3)
- [ ] Ruling: session-minted review/status tasks vs the daily budget — operator-class bypass or budget-counted (park when exhausted)? They mint even when the pool is paused today (design doc OQ2; 03-28 §g1; 04-45 §g1) — BLOCKED: owner budget-class ruling
- [ ] Ruling: daemon-fold attribution — teach the auto-commit daemon to carry `Crush-Session: <id>` footers from an env-carried session id (is the daemon yours to change; want tq's help?) vs a timestamp/author-range fallback in `tq session close` (fourth consecutive ask: 03-28/03-46/04-13/04-31 §g; 04-45 §g2) — BLOCKED: owner daemon/attribution ruling
- [ ] Ruling: duplicate-claim re-delivery (the 04-13/04-31 pair) — queue-side guard against re-dispatching a task whose terminal fact exists, or ratify the verification-only second window as the sanctioned shape and define what its verdict may claim (04-31 §g1; 04-45 §g3/§d4; the TQ_RESULT half died 2026-09-14 with derivation — a no-op re-dispatch now derives zero footer commits, no ruling needed; only the re-delivery guard itself is still open) — BLOCKED: owner duplicate-claim policy

## Docs-health harvest (2026-09-14 archive sweep; sources: triaged KEEP-OPEN reports, deduped against existing rows, claims spot-verified at HEAD)

- [ ] Dead-pool alert lost on transient notify failure: `deadPoolDetector.observe` sets `alerted = true` BEFORE the notify result (cmd/tq/agentpool.go:468) — a transient post failure burns the streak's only alert; set the flag from the notify outcome (06-06 report §b1, re-confirmed by the 06-11/06-14 re-dispatches; docs/status/2026-09-11_06-06_task-000001a08e967ebc090a3cd706280ed4d659.md)
- [ ] Confidentiality gate for outbound artifacts: reports/PR drafts are swept for the private evaluator's identity class before leaving the repo (10-33 retro §f4 — the incident class, gate still unlanded; AGENTS.md grep = zero hits); the proxy-zip removal + history rewrite half is owner-gated (§f1/f2) — the gate half is agent-executable, land it and leave the owner half annotated — BLOCKED: owner call on the proxy/history half (docs/status/2026-09-13_10-33_confidentiality-incident-retro.md)
- [ ] Crush-integration batch: `tq doctor` crush check (binary present, version floor, xhigh managed-block pin) + `tq bootstrap` writes xhigh by default; upstream misleading "does not support reasoning effort" error is an issue candidate (12-23 report §f; docs/status/2026-09-14_12-23_crush-releases-integration-assessment.md)
- [ ] templ-components sweep P0 swap-guard batch (activeElement/details active-swap guard in app.js) + RelativeTime/Pagination/filter-submit UX items from the webui UX audit — harvest was explicitly pending owner (§b3) — minting the mechanical swap-guard here so it is executable — BLOCKED: owner sign-off on the UX batch scope (docs/status/2026-09-14_11-41_templ-components-deep-dive-webui-ux-audit.md)
- [ ] Facade release tail batch: clean-room install check moved INTO release gates (pre-tag), T5 out-of-tree consumer CI job, facadeparity in-repo self-test, VERSION-SURFACES.md refreshed to 0.3.0 values, FEATURES/CHANGELOG rows for `tq audit --journal` + check-features-ci gate, drift-audit postgres parity, `scripts/check-pkg-proxy.sh` (09-06/09-14/10-27 reports §f; docs/status/2026-09-13_09-14_brutal-self-review-v030-release.md)
- [ ] DONE-on-arrival dispatch dedup: queue-side done-guard (terminal fact exists → refuse re-dispatch) + dispatcher dedup — the 5th same-ID burn (11-31 report §f; relates to the BLOCKED duplicate-claim ruling row above) (docs/status/2026-09-14_11-31_sse-flake-redispatch-and-checkout-race.md)
- [ ] SystemNix pool validation: poolSettings diff on evo-x2 never validated after the deploy fixes; machine-wide agent slot cap; Gatus journal-head liveness probe (01-55/02-00 reports) — the sudo/deploy parts are owner rows already — BLOCKED: SystemNix-side validation needs the deployed host (owner-run) (docs/status/2026-09-10_02-00_self-review-dead-pool-fix-flash-dogfood-session.md)
- [ ] Security hygiene batch: SECURITY.md per-surface security-header matrix, `redactedRequestURI` pin test, bearer-token dedup consideration (`internal/httpauth`) (03-05 report §f; docs/status/2026-09-10_03-05_httputil-assessment-and-permissions-policy.md)
- [ ] `scripts/archive-evidence.sh`: one-command evidence copy (check-ignore FIRST, copy second, SHA256SUMS regen, daemon --stat diff) — asked for in four consecutive windows, still absent (00-43 report §f; docs/status/2026-09-12_00-43_task-000001a092a10a7edf3a3836f61deba98eed.md)
- [ ] Ghost-archive gate restructure: report ALL failure classes in one run (elif short-circuits) + INCOMPLETE-survives-the-gate negative test (00-10 report §f; docs/status/2026-09-12_00-10_task-000001a0927c6b76816c188ac334cb868ddf.md)
- [ ] Repo-name expansion policy unification: agent-pool inline-join vs expandRepoSpecs divergence ruling + `tq worker`/`tq audit` bypass-parity check + `--repos` help text hiding bare-name semantics (03-34/04-43/05-12 reports; docs/status/2026-09-11_03-34_task-000001a08a4851ce7feb14c30837a9b399dd.md)
- [ ] `tq doctor` service-context mode: diagnose the systemd unit's PATH (the actual failure surface of the 20h dead pool), not the caller's (05-38 report §f; docs/status/2026-09-11_05-38_task-000001a08e88c2fb41274f6ed6bfbe896b25.md)
- [ ] AGENTS.md `internal/httpapi` package row missing from the architecture table (verified: zero hits at HEAD; 06-01 report §f) + AGENTS.md size-guard test (≤15KB) + `tq facts --json` golden test (docs/status/2026-09-09_06-01_round10-self-review.md)
- [ ] Orphan-SHA cleanup: repoint 15ff1f9 → pushed twin 15ff1f9 in docs/status/README.md:166 and annotate the 09-33 report row (03-46 report item 20; docs/status/2026-09-11_03-46_task-000001a08a4851e129179c8cf04e126208a7.md)
- [ ] AGENTS.md vendorHash note prescribes the stale `lib.fakeHash` dance — correct to the current prescribed procedure (09-44 report §e1; docs/status/2026-09-10_09-44_red-master-repaired-closeout-proven-self-review.md)
- [ ] `tq tasks` notBefore/backoff column + `tq show` lease-staleness surface (05-30 report c3/f6; docs/status/2026-09-10_05-30_loop-engine-built-task-closeout-and-docs-health.md)
- [ ] README-as-contract rot guard (smoke asserting install snippets actually work) + `agent-pool --model` retirement decision (it resets reasoning effort per the .crushrc ruling) (05-29 report c3/f6, c5/g2; docs/status/2026-09-10_05-29_readme-overhaul-tqdb-incident.md)
- [ ] fullcore fatal-path close-before-exit: closeout turn skipped when the process exits on a fatal path after verify (06-39 report b3/e1; docs/status/2026-09-10_06-39_task-000001a0893f0253e25fe15e759f78a899ed.md)
- [ ] Smoke forensics batch: keep-logs-on-fail flag for smokes, zero-requeue assertion, multi-repo.sh added to the AGENTS.md smoke list (09-01 report f5/f6/f8; docs/status/2026-09-10_09-01_task-000001a08a1a8b0e7666677dc99c4cd84b16.md)
- [ ] Worktree design open questions minted: Q1 merge-policy is gating — lift the 9 open questions from docs/planning/2026-09-12_worktree-per-agent-design.md into TODO/ROADMAP rows (01-43 report §f; docs/status/2026-09-12_01-43_task-000001a092d3650a2b85c03583a22d9c2c5e.md)
- [ ] Status-index re-sweep: live rows re-bloated past 100 after this pass's intake — the 12-39 e5 cadence item (row above) needs its next scheduled sweep (docs/status/2026-09-14_12-39_claiming-arc-brutal-status.md §e5)

## Docs-health per-item triage harvest (2026-09-14 evening; ~1.3k items verdicted across 39 live reports; only substantively untracked themes minted, deduped)

- [x] fullcore smoke hardening batch: ci.yml parity step (ci-local-only today), `DEADLINE_RUNS` knob (hardcoded 3), the example actually CONSUMING TQ_DB (`--db` is the real guard; env is belt-and-braces), a postgres deadline variant, a shellcheck/`bash -n` gate for `scripts/smoke/*`, and a message-format assertion audit over the other smokes (01-59/02-04 report tails; docs/status/2026-09-14_01-59_task-000001a09d3497d276f684a8b156135306cb.md) — done 2026-09-15: knobs+TQ_DB+postgres variant+ci.yml parity (6e40681); `scripts/check-script-syntax.sh` (bash -n + shellcheck, hard, zero findings) wired into ci-local+ci.yml; message-format audit PASS (all 12 smokes assert content, not just exit codes)
- [x] `cmd/tq/tq.exe` committed binary + no `.gitignore` entry + cmd/tq missing from the ci.yml per-module lint loop (00-37/01-46/01-54 report tails; `rg --files cmd/tq` shows the exe at HEAD) — closed 2026-09-15: tq.exe claim STALE (untracked, zero git history, `*.exe` already ignored at .gitignore:59); cmd/tq lint loop added via devmod+devwork shims in ci-local+ci.yml with deliberate baseline regen (+268 findings/31 rows, non-cmd/tq counts byte-identical)
- [x] Status-index gate hardening: derive the bloat threshold instead of the bare 100, self-test fixture for the live-row counter, archived-row backtick convention pin, digest-row format proposal — the mechanical items are executable; the WARNING-vs-FAIL flip stays with the owner — BLOCKED: owner ruling on WARNING-vs-FAIL (02-24 report §e/§g; docs/status/2026-09-14_02-24_task-000001a09d4b7836c71a8e0e25e4b3c1c018.md) — done 2026-09-15: INDEX_BLOAT_THRESHOLD named constant + rationale, count_live_rows + --self-test wired into ci-local, backtick pin + digest proposal in docs/status/README.md cadence paragraph; the WARNING-vs-FAIL flip remains the one open owner item
- [x] Codify the turn-1 ritual in AGENTS.md: grep prior same-ID reports AND read CONTRIBUTING.md at turn 1 — both misses are documented Nth-recurrence in every closeout since 09-12 and neither is written down as a rule (02-17/00-52/00-37 §e tails) — done 2026-09-14 (743ca3c, plus the missing fullcore/reviews smoke entries)

## Webui overhaul leftovers (2026-09-14 stunning-overhaul execution; docs/research/2026-09-14_templ-components-deep-dive.html §05)

- [x] Adopt `navigation.Pagination` for the numbered task pager (replaces the hand-rolled prev/next row in fragments.templ taskPager; library ships numbered pages + ellipsis) — done 2026-09-15 (adoption table + guard tests green; rel prev/next kept by the library, pagerBaseURL replaces pageHref)
- [x] Adopt `display.ListNote` for the "+N more projects" chip in the filter bar project chips row — done 2026-09-15 with a site correction: ListNote adopted at the task pager ("showing N of M" — the real truncation-notice surface); the +N-more-projects chip verified WRONG-SHAPE for ListNote (it renders the fixed sentence "Showing X of Y. Narrow your search to see more.", not a terse inline chip) and stays a deliberate custom span
- [x] `errorpage.WriteError` adoption: styled 500s consistent with the chrome (currently bare http.Error paths in handlers.go) — resolved 2026-09-15 as REJECTED with rationale: library error pages render their OWN page chrome while every tq surface is in-chrome; the task-404 is already styled in-chrome (renderTaskNotFound); remaining http.Error sites are form-post/fragment contexts; AGENTS.md adoption prose carries the verdict (also skips a new module require + vendorHash dance for four error lines)
- [x] File upstream templ-components chart contribution: MaxTicks/MaxYTicks prop (lineChartMaxTicks is a private const; Height 200 was the tq-side stopgap — verify-before-filing against latest master first) — done 2026-09-15: no filing needed (own repo); implemented as maintainer in templ-components df0d70f4 (MaxTicks prop on Line/Area charts, ComputeNiceTicks budget, default path byte-identical, goldens unchanged, BuildFlow green)
- [x] templ-components bump when the unreleased a11y pack ships (a11y_new_test.go present at v1.16.0 but the forced-colors/prefers-contrast sweep landed after; re-run the version-pinned audit on bump) — closed 2026-09-15: the axe-core accessibility sweep SHIPPED in v1.17.0 (CHANGELOG 2026-09-13) and root already pins v1.17.0 — nothing to bump
- [ ] Adopt `display` MaxTicks in the two dashboard metric AreaCharts (560x200) once templ-components releases past v1.17.0 — the prop is implemented upstream (templ-components df0d70f4, untagged); the Height-200 stopgap stands until then
- [ ] Sort state as removable filter chip + sort-header link visual verification against theme duality (G2 polish, deferred)
- [ ] CSP `form-action 'none'` kills the no-JS filter fallback — allow form-action 'self' or accept JS-only filters — BLOCKED: owner ruling on the security posture (surfaced during G1)

## Done-prompt window harvest (2026-09-16; sources: the five-task hardening window close-outs 00-11/00-55/01-21/01-46 + 06-43, deduped against rows 89-93/97; all claims verified at HEAD this pass; docs/status/2026-09-16_02-05_done-prompt-five-task-hardening-window.md)

- [x] Regenerate `.golangci-baseline.txt` WITH per-module triage attribution: 6 growth rows at HEAD (cmd/tq tagliatelle 9→10, varnamelen 6→7; executor gochecknoglobals 4→5, mnd 29→31, varnamelen 16→17; root mnd 44→45; files: journalaudit.go, redact.go, sidecar.go, agent.go, main.go, top.go) — hard ci-local gate is RED until then; run the check WITHOUT pipes (a `| tail` masks the exit code) (02-05 report §d1) (closed 2026-09-16: 23259f2 repaired 5/6 rows; the last row (executor varnamelen 17→18, redact.go) fixed by the sp→candidate rename in 8759e6d; `lint-baseline.sh --check` exit 0, 1084 findings vs 1097 baseline)
- [ ] `scripts/smoke/api.sh`: live-socket lockout smoke — spawn `tq api` on a scratch DB (export TQ_DB!), 3×401 → 429 + Retry-After → expiry → 200; wire into ci-local smokes (01-21 report §f4; docs/status/2026-09-16_01-21_task-000001a0a7515734d342737ff2e133e427b6.md)
- [ ] `scripts/smoke/redaction.sh`: stub agent emitting a fake `sk-ant-…`/`AKIA…` token; assert stored evidence tail is `[REDACTED]`, sidecars redacted, `tq audit --journal` reports zero post-pass SECRET EVIDENCE; fold a zero-SECRET-EVIDENCE assertion into `journal-drift.sh` (00-55 report §f1/§f5; docs/status/2026-09-16_00-55_task-000001a0a7237f14896a336463023ce6a28c.md)
- [ ] Extract `internal/httpauth`: bearer-token extraction ×2 (webui presentedToken vs httpapi bearerToken) + the twin rate limiters behind ONE tested implementation with per-surface policy knobs (strike trigger, guarded scope) — a limiter bug-fix today can silently miss the twin (01-21 report §c2/§f5)
- [ ] De-sleep the lockout tests via `nowFunc` fake-clock injection (httpapi TestAuthLockout* + webui TestWriteRateLimit* — ~100ms real sleeps each, theoretical loaded-runner flake) (01-21 report §b2/§f6)
- [ ] `slog.Warn` on first bound-eviction in webui `boundLocked` (an eviction under legit traffic means a surprise key population) + unify the limiter's two clock sources (`nowFunc` injected vs hardcoded `time.Until` for Retry-After) (00-11 report §e2/§e3; docs/status/2026-09-16_00-11_task-000001a0a715d4eabb3535222203793fbd7d.md)
- [ ] Pin the `?token=`-query auth-failure strike path (bearerToken fallback only pinned on success) + "lockout covers POST /api/v1/tasks" (all-routes contract; only GET healthz tested) (01-21 report §f8/§f9)
- [ ] Decide 401/429 body shape on `tq api`: handlers answer `{error, fix}` JSON but auth failures + lockout use plain-text `http.Error` — align or document why plain text is right for the auth plane (01-21 report §b3/§f7)
- [ ] Secrets-pass polish bundle: document the token-shape growth policy in AGENTS.md (pattern + test + baseline note per new provider); pin test that `secretPatterns` and `SecretHits` compile the identical table (audit vs redaction must never fork); verify `redact_test.go` actually runs on windows-latest CI (no `//go:build unix` tag — confirm, don't assume) (00-55 report §f7/§f8/§f9)
- [ ] `tq doctor --hygiene` scratch-DB smoke (seed a stale-pin task, assert the WARN text via `--json`) + a claim-time proof test that `--reresolve-verify` actually uses the NEW gate after a `.tq-verify` flip (06-43 report §f4/§f5; docs/status/2026-09-15_06-43_task-000001a0a2f63b92bdc1a0d85ef9935cb821.md)
- [ ] Daemon-commit attribution sweep: doctor-style audit mapping footer-less `chore:` commits to task IDs via their `docs/status/*_task-*.md` reports, flagging unattributed shipping commits (00-55 report §f2)
- [ ] CHANGELOG convention: behavior changes visible to external API consumers (e.g. the new `tq api` 429s) get an explicit behavior-change flag instead of a plain Added bullet (01-21 report §f28)
- [ ] Next archive sweep (docs-health ANNOTATE): annotate + git-mv the five 2026-09-15 morning re-dispatch triplet reports (03-25→04-40); monthly digest row added 2026-09-16 keeps the index scannable meanwhile; live count 142 vs threshold 100 (02-05 report §c7)
- [ ] ci-local transient-retry wrapper polish bundle: "healed on poll N" success log line; AGENTS.md note for `TRANSIENT_MAX_POLLS=0` escape-hatch semantics; one-line rationale for the deliberately unwrapped `check-ci` step (01-46 report §f3/§f14/§f6)
- [ ] `check-script-syntax.sh`: separate/quiet the nix-shell fetch path that raced its own output once (exit-1 false red during the 01-46 lap) (01-46 report §f15)
- [ ] `closeoutPending` lifetime audit: entries registered on 429-during-closeout whose task dies CANCELLED while parked — leak until process restart? (00-11 report §e5)
- [ ] Per-key map bound sweep: one grep pass over `map[string]` in internal/webui + internal/executor for members needing the strikes-map bound treatment (00-11 report §f26)
- [ ] `tq api` `--help` mentions the lockout (429/Retry-After) + `tq doctor`/serve startup surfaces recent auth-lockout WARNs (misconfigured producers discoverable without journal digging) (01-21 report §f13/§f14)
- [ ] `tq pool-health` one-shot liveness summarizer: per-repo skip streaks + last harvest activity from the journal (liveness ≠ process up) (carried; unchanged rank from the 09-12 harvest)
- [ ] Deploy-readiness bundle for the next SystemNix input flip: `--hygiene`/`--reresolve-verify`/`--redact`/httpapi hardening all activate together (deployed tq v0.3.0 has NONE of them — verified `tq doctor --hygiene` errors on the live binary); smoke each on the pool unit after the flip (02-05 report §b1) — BLOCKED: rides the owner-run input flip (rows 21/97)
- [ ] Daemon-race footer policy: when the auto-commit daemon folds a task's diff before the explicit commit, what is the sanctioned terminal state — footer on an empty marker/report commit (precedent: 37e83d4, 3e0f941), amend-rescue of the just-created daemon commit (05962e2's move), or scripted reword of unpushed daemon commits? — BLOCKED: owner queue-convention call (00-55 g3, 01-21 g3, 01-46 g1 all re-ask it)
- [ ] Lint-baseline growth ownership: when concurrent feature sessions grow the baseline (gate RED at HEAD right now), must each feature session regenerate with triage notes, or is a dedicated periodic sweep the ritual? — BLOCKED: owner process call (01-21 report g2)
- [ ] Deploy cadence: flip the SystemNix input + `nix run .#deploy` so the window's shipped features reach the live pool — until then every hardening window ships code production cannot exercise (deployed binary lacks hygiene/redact/reresolve, verified) — BLOCKED: owner-run (sudo on evo-x2; rows 21/97)

## Done-prompt harvest (2026-09-17; sources: the six-task CI-resilience/observability window close-outs 01-46/02-26/02-37/03-04/04-03/04-21, deduped against open rows; claims verified at HEAD this pass; docs/status/2026-09-17_00-25_done-prompt-six-task-hardening-window.md)

- [ ] Finish the go 1.27.1 go.mod alignment sweep: only root/journal/queue-top (+ cmd/tq via the session-close smoke task) declare 1.27.1 — task, worker, queue, queue/sqlite, queue/postgres and any other sub-modules still declare 1.26/1.26.7, so `scripts/check-go-mods.sh` fails at HEAD (verified live 2026-09-17); bump every remaining module to the root's exact version and confirm the gate green
- [ ] Mint-time done-check for repeat dispatches: at enqueue/harvest time, refuse or downgrade a mint whose task ID already has a terminal fact or an indexed close-out (the loop's standing top fix; a verify-only re-dispatch of the status-append task closed 2026-09-17 00:24 — an entire paid lap to re-verify a DONE row) (01-46 §f7; docs/status/2026-09-16_01-46_task-000001a0a7682e60d5308538ec1aff235005.md)
- [ ] Calibrate the transient-retry 45s×3 budget from git-history/report evidence of past mid-edit breaks (still zero datapoints; the wrapper has never healed a real break) (01-46 §f4; docs/status/2026-09-16_01-46_task-000001a0a7682e60d5308538ec1aff235005.md)
- [ ] First full ci-local soak with the wrapper + pin wired: record whether any poll healed and whether the pin step behaves in situ (closes 01-46 §b6, 03-04 §b4, 04-21 §b2 residues) (01-46 §c1)
- [ ] Call-site census pin for `with_transient_retry`: a pinned "N wrapped call sites" makes silently unwrapping a gate a loud event (03-04 §b2/§f6; docs/status/2026-09-16_03-04_task-000001a0a7b1684f2f9c0f1f1ae2dd687895.md)
- [ ] Run `check-transient-retry.sh` in ci.yml too (hermetic, sub-second; CI runners never execute the ci-local-only pin today) (03-04 §f8)
- [ ] Split row 89 at the next docs-health pass: the smoke half becomes its own BLOCKED row so the shipped pin half could close as DONE (03-04 §e3; rows can't partially close today)
- [ ] Durable pin for check-go-mods' verify retry (house check-transient-retry style: sed-extract the shipped loop, assert WARN + retry + fail semantics) (04-21 §b2; docs/status/2026-09-16_04-21_task-000001a0a7faa45cea4656ad816e85a83395.md)
- [ ] check-go-mods.sh: include the underlying stderr in the retry-failure FAIL line (currently swallowed by >/dev/null — the FAIL names the module, not the symptom) (04-21 §f14)
- [ ] Audit every silent retry/absorption site in scripts/ for a WARN line (04-21 §e2 generalization)
- [ ] check-ci.sh: single `gh run list --jq` call replacing three python3 subprocesses; print the local HEAD sha in the predates branch; ancestry-precise wording via `git merge-base --is-ancestor` (04-21 §e4/§e5)
- [ ] fuzz.yml concurrency ruling recorded: whether the scheduled nightly opts out of cancel-in-progress (a manual push would kill a campaign mid-write) — question filed, ruling not yet recorded (04-21 §c1) — BLOCKED: owner ruling on scheduled-workflow concurrency semantics
- [ ] CONTRIBUTING.md "individual gates" sweep: add check-transient-retry.sh, check-go-mods.sh, check-ci.sh, check-facade-parity.sh, check-status-index.sh, check-gosec.sh + the lint-baseline gate (both postdate the sweep — the gate list every turn-1 reader sees teaches an incomplete inventory) (03-04 §f14, 04-21 §f4, 03-15 report §f7)
- [ ] Table-driven pin over ALL secretPatterns pairs: assert bearer×auth-header is the ONLY overlapping pair so a future pattern fails loudly (02-37 §b4/§f1; docs/status/2026-09-16_02-37_task-000001a0a76cd0808802847ba630d2957bb5.md)
- [ ] Property test: N injected non-overlapping fake tokens → SecretHits == N regardless of surrounding benign text (02-37 §f2)
- [ ] Pin that RedactSecrets masks an auth-header line to exactly ONE marker (output-side mirror of the count fix) (02-37 §f11)
- [ ] Structural redaction guard: a check that every executor tail helper delegates to redactOutput, so a future third tail helper cannot silently bypass the pass (02-26 §b2/§f4; docs/status/2026-09-16_02-26_task-000001a0a76ccb60c50236a4d09dd0b693b5.md)
- [ ] `SetFailureEvidence` for depbump (structured stage/exit_code/tail evidence, agent-parity; today error text is the only persisted depbump evidence) (02-26 §c1)
- [ ] Re-run `journal-drift.sh` + the seeded audit fixture once at HEAD before the next release; add the operator note that SECRET EVIDENCE counts in pre-4ba81d8 reports are not comparable with fresh `tq audit --journal` output (02-37 §f8/§f7)
- [ ] Rename the `tq audit --journal` "hit-count" label to "locations" for operator-facing honesty (needs the CHANGELOG/AGENTS wording swept with it) (02-37 §f18)
- [ ] Annotate pre-fix status reports citing SECRET EVIDENCE hit-counts with the span-merge caveat (docs-health ANNOTATE, low priority) (02-37 §b3/§f10)
- [ ] Frozen-date fixture sweep: rg scripts/smoke + testdata for hardcoded calendar-date literals, convert to run-time computation; a small check-* gate if the convention is ratified (04-03 §f3/§g3; docs/status/2026-09-16_04-03_task-000001a0a7c3b5f42d9e2ab428f1024d6622.md)
- [ ] ratelimit-e2e GNU-date dependency: header note or loud portability guard (requires `date -d`; silent breakage on BSD/macOS) (04-03 §f7)
- [ ] ratelimit-e2e into ci.yml: the smoke gates ci-local only; ubuntu CI runs webui/fullcore/release-gates — parity call (04-03 §f11)
- [ ] ratelimit-e2e stub-log assertion: grep the worker log for the armed-gate/provider line, pinning the f35 observability half end-to-end (currently only Go-tested) (04-03 §f12)
- [ ] Provider tag in the parked surface: `tq tasks --parked` shows only the reason string; surface `RateLimitError.Provider` so "which provider is exhausted" is one glance (04-03 §f8)
- [ ] doctor earliest-release branch pin: verify a test covers doctorParked's not-today day-aware layout case; pin if not (04-03 §f9)
- [ ] Pin `tq stats --project X` parked-count × project-filter interaction (04-03 §f10)
- [ ] Smoke-retry budget for ci-local smokes (row 89's blocked half, third ask): two-phase tree-quiet probe (recommended — milliseconds vs multiplying minutes-long smokes ×3), whole re-run ×3, or wrap-only-cheap-smokes (01-46 §g3, 03-04 §g1) — BLOCKED: owner runtime-budget ruling
- [ ] Daemon-race attribution protocol (fifth recorded instance: 3e0f941, 025cd6b, 9648a19, d4125d1 + today's report commit): ratify daemon-SHA citation as permanent, or bless content-first explicit commits (work commit BEFORE docs/TODO edits) so the footer commit carries the pinned bytes (01-46 §g1, 02-37 §g1, 04-03 §g1) — BLOCKED: owner queue-convention call

## Done-prompt harvest (2026-09-17; sources: the lint-baseline row-104 re-dispatch close-outs 01-17/01-22/01-23, first harvested this pass; row 104 itself is DONE + re-verified 3x)

- [ ] Spot-check round-13's noctx "fix for real" claim against code: the row-104 close-outs confirmed absence-of-growth but never verified the noctx findings (executor 1→5, root 22→29) got REAL context-carrier fixes, not a baseline regen (01-17 §f14, 01-23 §b1)
- [ ] Queue-side dedup-key→COMPLETED short-circuit: when a dispatch's task ID already has a COMPLETED task, mint a zero-cost verify stub or skip — now argued from 3+ sibling re-dispatch windows in as many days (01-17 §f1, 01-22 §e2, 01-23 §e2)
- [ ] Owner ruling: adopt or policy-disable `modernize` (per-gate whack-a-mole burns a window per drift class) (01-17 §g1)
- [ ] Pin golangci-lint version in the flake to stop analyzer-drift gate-killers (01-17 §g2)
- [ ] `lint-baseline.sh --check`: print a final verdict line (`lint-baseline: OK/FAIL`) so piped/untruncated reads can't misread the verdict (01-17 §e3, 01-23 §e3)
- [ ] Pre-commit hook (or gate) validating report filename timestamps against `date` — the 01-22 report shipped a fabricated `04-52` timestamp (01-23 §d1/§e1)
- [ ] Owner ruling: agent ask-policy for `tq ask` — teach it in work-turn prompts with a cap, leave interactive-only, or teach + budget-exempt (21-04 §g1)
- [ ] Owner ruling: confirm the 72h question-expiry default (re-enter on expiry) or switch expired questions to cancel the task (21-04 §g2)
- [ ] Executor: export `TQ_TASK_ID` env to agent runs so prompts/scripts stop needing the sed-the-prompt hack (`tq ask --task` could then default to it) (21-04 §f28)
- [ ] Expiry sweep: append a fact when a question's NotBefore lapse re-enters a task — today the re-entry is silent (forensics gap) (21-04 §f34)
- [ ] Pin with a test that closeout-free executors (review/status/dlqfix/prioritize) never receive the `$TQ_QUESTION_FILE` channel (21-04 §f42)
- [ ] SECURITY.md: note that questions carry agent-authored text toward PapDashboard (redaction pass covers it; document the trust boundary) (21-04 §f44)
- [ ] Queue↔git attribution fix: 59% of footer commits are invisible to GitLogScanner — git %(trailers) parses ONLY the final paragraph, and the harness commit template's trailing blank+Assisted-by line (e.g. fc6e03d) demotes the footer out of it (74 of 180 footer commits visible over the last 400) — BLOCKED: owner direction in the 03-00 report §g1 (scanner %B fallback trades the quoted-footer guarantee at gitscan.go:141 vs a footer-last shape rule touching the harness contract)
- [ ] Pin GitLogScanner's trailer visibility END-TO-END: commit a harness-template-shaped message (footer, blank line, Assisted-by last) into a temp git repo and assert TODAY's zero-attribution with a comment citing the 03-00 report §e2; flip the assertion when the attribution fix lands — gitscan_test.go's rec() fixtures simulate git OUTPUT and are structurally blind to git-side trailer detection
