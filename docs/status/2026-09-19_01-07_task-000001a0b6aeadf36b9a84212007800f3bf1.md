# Third window on the tq crush doc-gap task (second re-dispatch verification) — task 000001a0b6aeadf36b9a84212007800f3bf1

2026-09-19 01:07. The queue re-dispatched the SAME work item ("Document
`tq crush` in README (short subsection under session triggers) +
FEATURES.md row (02-54 §f)") for the THIRD time tonight. Timeline: window
one did the work (commit 7c0d8b4, close-out 0d168bc, report 00-49), window
two verified (report 01-01), this window verified again. The row has been
`[x]` since 00:49 — every dispatch since has been pure verification.
Docs-only task; ZERO tree edits this window; the only commit is this
report + its index row.

## a) FULLY DONE

1. **Re-dispatch caught at turn 1 — third consecutive window on this ID to
   do so.** Batch one held the whole-repo `git log --oneline -8` (subject
   "docs: document the tq crush session wrapper in README and FEATURES"
   was the tell), `git status --short` (clean) + `git stash list` (empty),
   and the footer grep (`git log --grep -F` over the exact Task-Queue-ID),
   which located all three footer commits (7c0d8b4 work, 0d168bc +
   3def03c close-outs) before any action. No duplicate docs were written;
   no empty commit was created.
2. **Deliverables re-verified fresh, by direct reads (not rg hits alone):**
   - README.md:296-330 viewed in full this window — the
     `### Session triggers: tq crush` subsection: purpose, two
     invocations, id resolution order, exit-code passthrough + signal
     forwarding, no-id no-op guarantee, remaining flags, both hook-based
     trigger alternatives (including the `…` placeholder at README.md:318,
     see §c6).
   - FEATURES.md:102 — the 🟢 `FULLY_FUNCTIONAL` wrapper row with the
     cmd/tq/crush_test.go pins, read verbatim.
   - TODO_LIST.md:122 — `[x]` verbatim with the DONE note naming both doc
     artifacts.
3. **Cross-reference verified mechanically:** `git log --grep
   'Task-Queue-ID: 000001a0b6…' -F` returns exactly the three known
   commits — the queue's derivation has everything it needs from tonight.
4. **Both prior same-ID reports read IN FULL before writing this one**
   (00-49: 219 lines; 01-01: 208 lines). Their §c/§e/§f/§g were mined so
   this report carries state forward instead of re-deriving it; every
   carried claim below was re-greped fresh anyway (§a6).
5. **Cheap gates EXECUTED fresh at HEAD this window, not merely cited**
   (the 01-01 §e3 pencil rule — "if a cited gate runs under a minute, run
   it" — finally applied on this ID): `scripts/check-doc-refs.sh` ok;
   `scripts/check-todo-list.sh` ok ("no unblocked owner-gated items");
   `scripts/check-features-roadmap.sh` ok ("seed cross-check ok"); root
   `go build ./...` exit 0 with `GOEXPERIMENT=jsonv2` exported. All four
   bare, no pipeline masking, exit codes captured directly.
6. **Follow-up state re-verified with fresh greps, so §c is current truth
   at 01:07, not the 01-01 snapshot:** FEATURES.md:101 gap text still
   stale (verbatim re-read); README Contents anchor for the new H3 still
   absent (rg empty); `…` still at README.md:318; AGENTS.md daemon-tell +
   cmd/tq-gopls-phantom lines still absent (rg empty); TODO rows 109,
   123, 126, 157, 128, 40, 124, 125, 230 all confirmed open verbatim.

## b) PARTIALLY DONE

1. **The pencil-rule gates ran LATE — during close-out prep, not the
   verification phase.** I read the rule in the 01-01 report in full
   (§a4) and still sequenced the gate chain after the follow-up greps,
   only because the report template demanded it. Compliance is real but
   mis-ordered; the natural slot was batch 2 alongside the deliverable
   re-reads. See §d2 — reading a lesson is not applying it.
2. **Skill-loading friction:** docs-health matched the task (doc
   verification), but my first `view` call used a
   `crush://skills/docs-health/SKILL.md` identifier that does not exist
   for it ("Builtin file not found") — the skill listing carried the real
   path (`/home/lars/.config/crush/skills/...`), which worked on the
   retry, same round. One wasted call. Cousin of the qmd misselection
   series (01-01 §d2): reaching for the wrong accessor for a resource
   whose real location was already in hand.
3. **The full battery remains cited-not-run** (root race suite, cmd/tq
   gate) — same rationale as both prior windows: no code changed, the
   point-in-time rule says re-run before CHANGING code, and the 00-49
   window captured the race suite exit 0 directly at this HEAD lineage.
   Three windows of precedent now define the accepted verify depth:
   deliverable re-reads + the under-a-minute gates (done this window).

## c) NOT STARTED (fresh-verified open this pass)

1. **FEATURES `tq session begin/close` gap-text rewrite** — FEATURES.md:101
   still says "trigger automation not wired (waits on crush #3146 or a
   PreToolUse registry)" although BOTH named alternatives shipped
   2026-09-17 (registry + wrapper). Pending the §g2 flip ruling (carried
   since 00-49 §c1).
2. **Committed wrapper-path smoke leg** (TODO row 126) — pending the §g1
   gate-vs-cited-one-off ruling. Still the strongest test gap on this
   surface.
3. **go 1.27.1 go.mod alignment sweep** (TODO row 230) — open verbatim;
   its "check-go-mods.sh fails at HEAD" claim was last verified live
   2026-09-17, NOT re-verified tonight (out of scope for a verify-only
   window on a docs ID; cheap for whoever takes the row).
4. **Live no-id smoke assertion** for the wrapper (TODO row 123).
5. **The 00-49 §e1/§e2 AGENTS.md lines** (daemon-tell after own edits;
   cmd/tq gopls phantoms ⇒ trust scripts/test-cmd-tq.sh) — re-verified
   ABSENT tonight; proposed three windows ago, still unowned.
6. **Doc-hygiene bundle**: README Contents anchor for the H3 (verified
   absent), `…` → concrete prompt string (README.md:318, verified in my
   own §a2 read), command-map pointer to the subsection, design-doc link
   from the README subsection, date-stamp on the "unreleased" claim
   (00-49 §b1).
7. **TODO row 128 (session-ritual script)** — open verbatim; the miss
   series it would end grows to N+8 below while the fix sits in the
   backlog.
8. **Rows 109 / 124 / 125 / 157 / 40** — open verbatim; blocked upstream
   (#3146) or owner (gate/advisory, CLI gosec, GOSEC_BIN assert, daemon
   attribution — the 157 ask is at its fourth-plus consecutive §g).

## d) TOTALLY FUCKED UP

Nothing shipped is broken — zero edits this window, tree clean at report
time, all four gates green (§a5), the work commit's footer verified. The
honest failures are process, and both counted series GREW tonight:

1. **CONTRIBUTING.md skipped at turn 1 — N+8 in the counted series**
   (00-22 counted N+6; 01-01 counted N+7; tonight N+8). Zero damage this
   window (verify-only; CONTRIBUTING's CI-gate content could not have
   changed a single decision), but three-for-three ritual misses on THIS
   ID alone, and the remedy (TODO row 128) has sat minted in the backlog
   since 00:32 while the series grows. The ritual is either mechanical or
   it is a prayer; tonight it was a prayer again.
2. **Read-the-rule-violate-the-rule (pencil rule, §b1).** The 01-01 report
   I read in full contains the exact sequencing lesson; I applied it ~40
   minutes late anyway. This is the meta-failure behind the whole
   close-out genre: prose lessons do not gate behavior. The only fix that
   survives a re-dispatch is mechanical (a script that runs the reads and
   the cheap gates in order) — which is again TODO row 128.
3. **Wrong-accessor miss (crush:// view failure, §b2)** — one wasted call,
   same-round recovery. If counted with the qmd series as "reached for
   the wrong tool for a resource whose location I already had," it is
   N+4. Small, but the series definition should cover both accessors.
4. **The systemic item compounded:** third dispatch on a row that has been
   `[x]` since 00:49, third indexed same-ID report in ~18 minutes, each
   costing a full dispatch + a window + an index row. Same pattern as the
   smoke-re-cite triplet (00-10/00-22/00-32) and the lint-baseline
   triplet (04-04/04-05/03-57) — this is now at least the THIRD ID to
   burn three windows post-DONE. §e4/§g re-escalate.

## e) WHAT WE SHOULD IMPROVE

1. **Ship TODO row 128 (session-ritual script).** N+8; third consecutive
   same-ID ask. It converts §d1 and §d2 from memory-dependent to
   structurally impossible: the script runs the reads AND the under-a-
   minute gates in order. Highest-leverage process item in this report;
   its evidence base now spans ≥9 reports across ≥4 IDs.
2. **Make the verify-window depth mechanical, not derived from prose.**
   Batch 2 of a verify-only window = deliverable re-reads + cheap-gate
   chain in ONE command. Tonight's sequencing error (§b1) is exactly what
   happens when the depth standard lives in a prior report's §e instead
   of a script.
3. **Codify the two 00-49 AGENTS.md lines** (daemon-tell; cmd/tq gopls
   phantoms). Proposed 00:49, re-verified absent at 01:01 and again at
   01:07. One line each; nobody has picked them up.
4. **Done-row re-dispatch dedup — escalate with the fuller evidence.**
   Three same-ID windows here, three on the smoke-re-cite ID, three on
   lint-baseline (04-04 window's own count). Each instance burns a full
   agent dispatch on verify→cite→stop. Either build the queue-side
   short-circuit (a DONE row with a same-ID close-out blocks re-dispatch
   for a cooldown) or rule the cost accepted — the current limbo is the
   worst option, and it is the single largest pure-cost item in this
   report.
5. **Report policy for verify windows (carried §g3):** tonight is the
   third same-ID report; the 01-01 §e5 append-vs-fresh math stands
   unanswered. An owner one-liner would decide whether windows like this
   write 200 lines or a 5-line dated append.
6. **Keep refusing the 50-item padding** — §f carries 16 honest items.
   Thirded (00-49 §e6, 01-01 §e6); the count is the deliverable.

## f) NEXT — ranked (16 honest items, not 50; "tracked" = a TODO row owns it, NEW = harvest candidate)

1. **Ship TODO 128 session-ritual script** — ends the N+8 series and
   mechanizes §e2 — High / S / Process (tracked, row 128)
2. **Committed wrapper-path smoke leg** (`tq crush --bin <stub child>`,
   wired into ci-local after session-close.sh) — pending §g1 — High / M /
   Test (tracked, row 126)
3. **FEATURES session-bridge gap-text rewrite** (FEATURES.md:101;
   triggers #1+#2 shipped) — pending §g2 flip ruling — High / S /
   Documentation (NEW, blocked)
4. **go 1.27.1 go.mod alignment sweep** (row 230; re-verify the red-gate
   claim first, last checked 09-17) — High / M / Build (tracked)
5. **Queue-side done-row re-dispatch dedup** (§d4/§e4; now n=3 on this ID
   alone, ≥3 IDs affected) — High / M / Process (owner; NEW)
6. **AGENTS.md daemon-tell line** (clean status right after your own
   edits ⇒ stop, `git log`, amend-or-follow-up; 00-49 §d1) — Medium / S /
   Process (NEW)
7. **AGENTS.md cmd/tq gopls-phantom line** (trust
   scripts/test-cmd-tq.sh) — Low / S / Process (NEW)
8. **Doc-hygiene bundle as ONE window**: Contents anchor for
   `#session-triggers-tq-crush`, `…` → concrete prompt string
   (README.md:318), command-map pointer to the subsection, design-doc
   link, date-stamp on "unreleased" — Low / S / Documentation (NEW)
9. **Live no-id smoke assertion** (row 123) — Medium / S / Test (tracked)
10. **check-gosec.sh GOSEC_BIN version assert** (row 125) — Medium / S /
    Build (tracked)
11. **check-gosec.sh cmd/tq coverage** (row 124) — Medium / S / Build
    (tracked, blocked owner)
12. **Re-verify crush #3146 state** (`gh api` one-liner, row 109 cadence)
    before any doc repeats "unreleased" — Low / S / Documentation
    (tracked)
13. **SessionEnd hook adoption** when #3146 lands (row 109) — Medium / S /
    Feature (tracked, blocked upstream)
14. **Daemon-fold attribution ruling** (row 157; fourth-plus consecutive
    ask) — High / M / Feature (tracked, blocked owner)
15. **CLI gosec gating ruling** (row 40) — Medium / S / Build (tracked,
    blocked owner)
16. **Re-verify the row-230 red claim** (`scripts/check-go-mods.sh` at
    HEAD) as a standalone cheap probe so §c3 stops carrying an unverified
    "still true?" — Low / S / Build (NEW)

Items 1-5 are the real queue-order candidates; 6-8 bundle into one
micro-window; 9-16 are tracked or blocked.

## g) QUESTIONS (3; all three are carried, all three predate this window, and all three gate §f items — none is answerable from the repo)

1. **(carried from 00-49 §g1, gates §f2 / row 126)** Gate-vs-cited-one-off
   for the wrapper's E2E: does `tq crush` need a COMMITTED ci-local smoke
   leg before more wrapper surface accrues, or are the unit pins + the
   cited live stub-child runs a permanent sufficiency standard? I cannot
   set that bar.
2. **(carried from 00-49 §g2, gates §f3)** May the FEATURES
   `tq session begin/close` row flip/rewrite now that 2 of 3 triggers
   shipped (registry + wrapper), or does "trigger automation lands" mean
   specifically the in-crush SessionEnd hook (#3146)? I can draft either
   text; only the owner can say which is honest.
3. **(carried from 01-01 §g3, gates §e5)** For a verify-only re-dispatch
   of a DONE row: is a fresh indexed report wanted each time, or should
   the verification be a dated append to the EXISTING same-ID report?
   Tonight's report is the third same-ID index entry in ~18 minutes; only
   the owner can weigh the audit trail against the index cost.
