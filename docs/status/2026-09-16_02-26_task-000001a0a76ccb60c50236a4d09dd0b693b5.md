# Close-out self-review: reviewer-finding fix — depbump tails bypassed redaction (task 000001a0a76c)

**Date**: 2026-09-16 02:26 CEST · **Task**: reviewer finding (medium) on the
secrets-in-logs change 748e989 — "depbump output tails bypass the redaction
choke point while AGENTS.md/CHANGELOG claim universal coverage" · **Work
commit**: `98417c0` (exactly one well-formed `Task-Queue-ID` footer, verified
via `git log --format=%(trailers)`) · **Report commit**: same footer.

Scope discipline: this report covers THIS fix task's work and what I noticed
in passing. No unrelated research was done; every §f item is grounded in
something this session actually observed.

---

## a) FULLY DONE

1. **Reviewer finding fixed at the choke point** (`98417c0`):
   `tailOutput` (internal/executor/depbump.go:724) now applies
   `redactOutput` over the FULL output BEFORE the 12-line/2 KiB cut, so all
   12 error sites (build/test/go get/tidy/pin-recheck/stage/commit/tag/
   push/go mod edit/rev-parse — depbump.go:299-644) land masked in the
   error text that `store.Fail(execErr.Error(), …)` persists (depbump
   records no SetFailureEvidence — the error text is the only persisted
   evidence). Redacting before the cut preserves the documented
   token-spanning-the-boundary property; `TQ_REDACT=false` restores raw
   tails like every other site.
2. **Pin test**: `TestDepBumpTailOutputRedactsSecrets`
   (internal/executor/depbump_test.go:24) — fake `ghp_` token (40+ chars)
   inside a go-get-shaped output is absent from the tail, `[REDACTED]` and
   the trailing failure line survive the 12-line cap. Green under `-race`.
3. **Reviewer premise independently verified, not just trusted**:
   (i) worker Fail paths embed `execErr.Error()` — internal/worker/worker.go:453-470
   (shutdown + retry paths; reviewer's line numbers 461-466 were ~8 lines
   stale, premise correct); (ii) `rg sidecar|SetFailureEvidence
   internal/executor/depbump.go` = zero hits — confirming error text is the
   only persisted depbump evidence.
4. **Coverage sweep**: every other tail site in internal/executor routes
   through `tailBytes`/`redactOutput` (agent.go ×10, command.go:55,
   ratelimit.go:238) — depbump was the LAST bypass, so the AGENTS.md claim
   "every output tail goes through the redaction pass" is now TRUE.
5. **Docs made truthful**: AGENTS.md + CHANGELOG no longer call `tailBytes`
   the "single choke point" — they now state depbump's line-based
   `tailOutput` delegates to the same redactOutput pass.
6. **Gates, cited**: executor module `go build` + `go vet` + full `-race`
   suite green (10.2 s, ok) + gofmt clean; `check-todo-list.sh` green.
7. **Lint-baseline gate repaired before it red-flagged the next ci-local**
   (noticed in passing, see §d3): `scripts/lint-baseline.sh --check` failed
   on FIVE drifted (module,linter) rows — executor mnd 29→31 /
   gochecknoglobals 4→5 / varnamelen 16→17 (drift from 748e989's redact.go
   landing after the 09-15 fifth regen) plus cmd/tq tagliatelle 9→10 /
   varnamelen 6→7 and root mnd 44→45 (concurrent post-regen windows).
   Attribution proof: `golangci-lint run --new-from-rev HEAD~1` over THIS
   window's diff = zero findings. Surgical row repair + full `--check`
   re-run GREEN (exit 0, "1081 findings vs baseline 1097"); AGENTS.md
   carries the repair note per regen-history convention (07-00 one-row
   repair precedent).

## b) PARTIALLY DONE

1. Redaction pin covers the masking path but NOT the escape hatch for
   `tailOutput` specifically: no `TQ_REDACT=false` test at that helper
   (covered only indirectly — `redactOutput`'s env check is shared with the
   tested `tailBytes` path). The delegation itself is unpinned.
2. "Single choke point" honesty is now wording-deep, still
   convention-enforced: nothing fails if a future executor grows a third
   tail helper that forgets redaction (structural guard = §f4).
3. Verification scope was the executor sub-module only (per AGENTS.md
   per-module gates; zero exported-API change makes root breakage
   implausible) — but a root `go build ./...` costs seconds and was not run
   before the work commit.
4. CHANGELOG: the gap fix is folded into the Unreleased "Added" bullet
   rather than a distinct `### Fixed` bullet — defensible while unreleased,
   but the gap's existence leaves no CHANGELOG trace (§g3).
5. Baseline repair fixes the GATE, not the per-commit ATTRIBUTION of the
   root/mnd + cmd/tq rows (file lists captured by the check output; which
   concurrent commit moved each was not chased — out of scope).

## c) NOT STARTED (deliberately out of scope for the finding fix)

1. `SetFailureEvidence` for depbump (structured stage/exit_code/tail
   evidence, agent-parity) — the finding is fixed at the error-text layer;
   the structured-evidence layer is untouched.
2. End-to-end redaction smoke (the 00-55 sibling report's own §f1, still
   open): stub depbump run whose go get output carries a token → assert the
   task.failed FACT carries `[REDACTED]` through the real store.Fail path.
3. `tq audit --journal` over the production dogfood journal to enumerate
   pre-fix depbump SECRET EVIDENCE rows (depbump shipped 09-15; exposure
   window ≈ 26 h; owner surface — TQ_DB env hazard).
4. Release mechanics: internal/executor + executor/ patch tags carrying the
   fix (VERSION-SURFACES order, pre-cut sub-tags before release gates).
5. SECURITY.md currency: the security model doc predates the pass; depbump
   tail coverage is not mentioned there.

## d) TOTALLY FUCKED UP

Nothing shipped broken this window. Honest failures and near-misses:

1. **Premise verified late**: I trusted the reviewer's finding (including
   its stale worker.go line numbers) and edited first; verification of the
   Fail path and the no-sidecar claim came in this close-out lap. Premise
   held — but verify-external-claims applies to reviewer findings too, and
   the discipline fired one lap late.
2. **Baseline gate skipped at commit time**: I knew `lint-baseline.sh
   --check` existed (it is in my own AGENTS.md notes) and still shipped the
   work commit without it; the close-out lap then caught drift that would
   have red-flagged ci-local for everyone — and the check surfaced FIVE
   drifted rows, not the one linter the finding implied.
3. **My own gate script silently lied once**: the first baseline diff I
   ran joined on the wrong key and printed NOTHING — one more broken
   comparison away from concluding "no growth". Caught only because the
   raw summary tail had already shown 17 vs baseline 16. A comparison that
   silently reports nothing is worse than no comparison; re-verified
   against the raw file rows.
4. **One wasted round trip**: first CHANGELOG edit failed outright ("read
   the file before editing") — the edit-before-view reflex this repo
   warns about, on a two-line doc edit.

## e) WHAT WE SHOULD IMPROVE

1. **Make redaction coverage structural, not conventional**: a check
   script (or lint rule) flagging error-construction sites that embed raw
   process output outside tailBytes/redactOutput/tailOutput — the exact
   bypass class the reviewer caught is mechanically greppable
   (`fmt.Errorf(…, out)` shapes in executor sources).
2. **Baseline check belongs in the window, not the next one**: run
   `lint-baseline.sh --check` before ANY Go-touching commit, not at the
   next ci-local; this window is the second data point (after 07-00) of
   drift being caught late by luck.
3. **Verify a reviewer finding's citations as part of acceptance** —
   file:line references go stale fast in a multi-agent repo; the premise
   matters, the line numbers are a hypothesis.
4. **Machine-band executors deserve agent-parity evidence**: bare error
   text was the reason the depbump leak class existed at all; structured
   FailureEvidence would make tails a first-class, testable channel.
5. **Pin the escape hatch per helper family** with one table-driven test
   over tailBytes/tailOutput/redactOutput instead of inferring from shared
   code paths.
6. **Baseline repairs are accumulating as surgical notes** (917 lesson,
   07-00 repair, this five-row repair); at the next natural boundary do a
   deliberate regen with per-row attribution (the check script already
   prints file lists) instead of stacking repair prose.

## f) UP TO 50 THINGS WE SHOULD GET DONE NEXT

Direct follow-ups on this fix:

1. E2E redaction smoke: stub `go get` wrapper emitting fakeGitHub → assert
   task.failed fact error contains `[REDACTED]` and `SecretHits == 0`
   (closes the last untested hop: tailOutput → execErr.Error() → store.Fail;
   also retires 00-55 §f1).
2. `TQ_REDACT=false` pin for `tailOutput` (table-driven across the three
   helpers).
3. `SetFailureEvidence` for depbump: FailureEvidence{stage, exit_code,
   tail} into the failed fact's detail (agent-parity), incl. a postgres
   conformance pin for the fact shape.
4. Structural guard: check script over executor non-test sources for
   raw-output error embeddings bypassing the redaction helpers; advisory
   wire into ci-local (§e1).
5. `SecretHits` assertion added to the depbump pin test (audit half agrees
   with masking half).
6. Widen the depbump pin's token shapes: bearer-header, AIza, xoxb (only
   ghp_ is pinned today).
7. SECURITY.md: one line noting depbump/go-get/push tails pass the
   redaction pass (security-model currency).
8. Release: pre-cut internal/executor + executor/ patch tags with the fix,
   then root/module sweeps per VERSION-SURFACES (owner-run release.sh).
9. Decide CHANGELOG shape: distinct `### Fixed` bullet vs folded Added
   bullet (§g3).
10. If owner prefers a literal single choke point: fold `tailOutput` INTO
    `tailBytes` (line-cap wrapper), delete the helper, rewrite the 12 sites
    — kills the two-helper convention risk entirely.

Production hygiene (owner surfaces):

11. `tq audit --journal` over /mnt/pool dogfood journal: enumerate pre-fix
    depbump facts (window 09-15 → ~02:00 09-16), review SECRET EVIDENCE rows.
12. If any pre-fix depbump fact carries a REAL token: rotate the
    credential; facts are immutable — record the rotation, do not edit the
    journal.
13. Check whether a depbump task is mid-flight against a credentialed
    remote in the live pool right now (stop/rescue per runbook if so).
14. Defense in depth at the source: `GIT_TERMINAL_PROMPT=0` (+ explicit
    credential-helper policy) in depbump `run()` env so remotes cannot echo
    credentials into output at all.

Docs/queue hygiene:

15. TODO_LIST row 86 now cites the reviewer fix (98417c0) — done this
    window; keep the row's citation current if item 8 cuts tags.
16. Annotate the 00-55 sibling report (docs-health ANNOTATE mode) with the
    finding + fix reference — its "verified fresh" §a pass missed the
    depbump bypass entirely.
17. THIS report's index row in docs/status/README.md (landed in this
    commit; daemon-bypass rule respected by committing it explicitly).
18. 00-55 §f leftovers still open: attribution sweep, mint-time done-check.
19. FEATURES.md: if the redaction row exists, add the depbump coverage
    half-sentence.
20. 07-00's lint-flake question (golangci v2.13.2 built with go1.27.1):
    this window's executor counts were stable across three runs — add the
    data point when revisiting.
21. Confirm redaction stays OUT of the sales-page README (internal safety
    feature; owner call).
22. AGENTS.md bullet says "evidence tail" and "output tail"
    interchangeably — pick one term (DOMAIN_LANGUAGE.md alignment).

Standing patterns noticed in passing:

23. Mint-time DONE-check still unbuilt (tops §f lists in 01-46, 01-21,
    00-55, 07-00) — this window was a findings-fix dispatch, different
    class, but the re-dispatch tax keeps compounding.
24. 15 gopls `writestring` warnings (command/dlqfix/review) — pre-existing
    advisory class, never triaged into any baseline note; one-time pass.
25. Tail-size magic numbers span three helper families (512 git-status /
    2048 verify / 8192 run / depbump 12-lines+2048 / EvidenceTailBytes) —
    one named-constant table would stop drift (mnd is policy-disabled, so
    nothing nags).
26. `tailOutput` now under-describes (it redacts + tails): rename candidate
    if item 10 keeps the helper (naming-review rails).
27. The new cmd/tq tagliatelle finding (journalaudit/main/top) is
    machine-payload snake_case BY CONTRACT — fold the new structs into the
    path-scoped exclusion at the next deliberate regen instead of carrying
    the count.
28. Concurrent windows keep landing between regen and gate (917 lesson
    repeating): adopt "regen immediately after any lint-visible feature
    lands" as convention, shrinking repair notes.
29. Pin the SHUTDOWN Fail path's redaction too (worker.go:453) — today the
    masking pin lives only in depbump's own test.
30. Postgres conformance: pin fact-error column bounds for 2048-cap redacted
    tails (sqlite tested; postgres covers fact shape, not tail length).
31. Facade parity unchanged this window (no exported symbols), but the
    RedactSecrets/SecretHits/RedactMarker aliases from 748e989 deserve one
    examples/embed usage line proving the adopter surface (rot guard only
    builds today).
32. Codify in the session ritual: run `lint-baseline.sh --check` in any
    window touching executor/ or cmd/tq Go sources.

Adjacent hardening (same pass, cheap):

33. Secret-pattern audit: `gh[pousr]_` covers ghs_/ghu_; verify
    `github_pat_` min-length 20 against real fine-grained token anatomy
    before tightening (verify-before-editing the regex).
34. Extend the benign-pass test with near-miss prose ("sk-12345678901234567890"
    style test keys) guarding against false-positive masking of docs.
35. `redactActive()` accepts only the literal "false" — document strictness
    or accept 0/no for parity with other flags.
36. RateLimitError text pin: a 429 body carrying a token is masked
    (ratelimit.go:238 already routes through tailBytes — pin it).
37. HTTP executor 429 body path: same redaction pin (http.go detection).
38. Sidecar writers: one pin each proving FULL-body redaction (748e989
    claims both; insurance is cheap).
39. Empty-output contract differs: tailBytes returns "(no output)",
    tailOutput returns "" — unify + pin.
40. `tq audit --journal --since <seq|time>`: scope SECRET EVIDENCE scans to
    post-incident windows on long production journals.

Process:

41. Commit-per-step held (fix commit; then baseline repair + report
    commit) — keep it.
42. Personal pre-commit checklist for Go-touching windows: add
    `lint-baseline.sh --check` until ci-local runs it pre-push anyway.
43. The review→fix loop worked (reviewer finding → dispatch → fix);
    consider routing reviewer findings through the pool as `reviewfix:`
    tasks so the loop is queue-native.
44. Baseline repair + report ride one commit here — if the daemon folds
    them, the amend maneuver applies (status re-checked before staging).
45. Every §a claim above cites its gate or commit — keep the standard.

Backlog seeds only if owner wants them harvested:

46. Evidence-tail length bounds as store-level policy (reject/truncate at
    the store boundary — journal immutability vs hygiene needs a ruling).
47. `tq doctor`: surface "facts written before redaction shipped" counts
    proactively (the audit half is manual today).
48. TailLines constant per executor class (depbump's 12-line cap is
    arbitrary and local).
49. Document the redaction tradeoff once (redacted debug tail vs live
    credential) in SECURITY.md's residual-risk list — currently only in
    CHANGELOG/AGENTS.md.
50. Unused on purpose — 49 real items beat one padded row (the report's
    own honesty rule; prior reports refused 50-padding the same way).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Evidence shape ruling**: should depbump failures carry structured
   FailureEvidence (stage/exit_code/tail, agent-parity) in the failed fact
   detail, or is redacted bare error text the intended terminal state for
   machine-band executors? (Determines §f3 vs §f10 sizing.)
2. **Production audit sanction**: may an agent run `tq audit --journal`
   against the dogfood journal (/mnt/pool/services/tq/tq.db — TQ_DB env
   hazard documented) to enumerate pre-fix depbump SECRET EVIDENCE rows,
   or is that owner-only? If a row carries a REAL token, is rotate-and-note
   the sanctioned response?
3. **CHANGELOG shape**: distinct `### Fixed` bullet making the depbump
   bypass visible in history, or stay folded into the Unreleased Added
   bullet as now (nothing has shipped yet)?

## Verification appendix (gates run this window)

- executor module: `go build` + `go vet` + `go test ./... -race -count=1`
  green (10.2 s); `gofmt -l` clean.
- Targeted redaction pins green: TestDepBumpTailOutputRedactsSecrets,
  TestTailBytesRedactsSecrets, TestTailBytesHonorsTQRedactFalse,
  TestSetFailureEvidenceRedactsTail, TestRedactSecrets*.
- `scripts/lint-baseline.sh --check` GREEN after five-row repair (exit 0,
  "1081 findings vs baseline 1097"; shrink advisory-only).
- `scripts/check-todo-list.sh` green (row 86 citation append).
- Attribution: `golangci-lint run --new-from-rev HEAD~1` = zero findings on
  this window's diff; `git log -1 --format=%(trailers)` = exactly one
  well-formed Task-Queue-ID footer on 98417c0.
