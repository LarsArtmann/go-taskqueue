# Status Report — task 000001a0d802 (ADR-0019 S2), verify window 2026-09-25 12:16

**Window type:** RE-DISPATCH verification window. The full close-out + brutal
self-review for this task ID shipped at 12:11 today
(`2026-09-25_12-11_task-000001a0d802640e8d1f9e21765644ebd87c.md`, commit
d4cf9afe) — five minutes before this dispatch. Per the 2026-09-24 01-40
verify-window precedent this report is a fresh-evidence verify pass plus the
requested full sections, deduplicating against the 12-11 close-out rather
than restating it. Scope: THIS task's work and what was noticed in passing
only (explicit dispatch instruction — no unrelated research).

## Headline verdict

The item is **correctly unchecked + BLOCKED** and was left untouched by this
window. S2's flip-independent slice shipped in the prior window; everything
remaining is serialized behind the S1 flip (TODO_LIST line 38, unchecked, in
flight concurrently — claim-token fallout is already visible at HEAD, §d3).

## a) FULLY DONE

Work on this task ID (across windows), with this window's evidence:

- **Flip-independent S2 slice** (prior window, commit 8e8837df, footer
  trailer-verified visible via `%(trailers:key=Task-Queue-ID,valueonly)`):
  both spike drivers' `AppendFact` now ride the engine's
  `FactTx.WithFacts` sink through a `journal.Fact`→`facts.Fact` mapper
  (S1 decision memo §2) — the transitional hand INSERT is gone.
- **Close-out + self-review shipped and indexed** (d4cf9afe), including §d
  ownership of the daemon-absorbed footerless code (98a35318) and the
  turn-1-ritual skip.
- **This window's verification battery, rc-captured:**
  - Root `go build ./... && go vet ./...` (GOEXPERIMENT=jsonv2,
    GOTOOLCHAIN=auto): **rc=0** at HEAD d4cf9afe.
  - `internal/queue/postgresv4` build+vet (GOWORK=off): **rc=0**.
  - TODO_LIST line 39 re-read at HEAD: item unchecked, BLOCKED note present
    and accurate (blocker = S1 flip line 38, still `[ ]`).
  - git status clean; no new task-attributed commits beyond d4cf9afe —
    nothing to re-verify beyond the above, no duplicate work performed.

## b) PARTIALLY DONE

- **ADR-0019 S2 as a whole** — the mapper slice exists in the spike
  drivers, but the item's core is NOT deliverable yet:
  - consumer re-pointing (`internal/journal/cqrs`, webui tailer, the four
    watermark sweepers, papdashboard bridge + answer poller) — not started,
    blocked on the S1 flip making the v4 engine store the default;
  - facts-in-same-tx becoming engine-enforced (deleting the hand-tx paths)
    — same blocker.
- **sqlitev4 spike health** — module compiled by prior gates, but red at
  HEAD as of this window (see §d3): the code side is a concurrent-window
  casualty, not a defect of this task's slice.

## c) NOT STARTED (this window)

- Any code change: correct for a blocked verify window — zero edits is the
  intended outcome.
- S1 flip itself, S2 consumer re-pointing, S3 read models, S4 composition
  root + mirrored-backend deletion (all serialized ahead/behind per the
  ADR-0019 S1→S4 ordering).
- No follow-up rows appended to TODO_LIST: every candidate was found already
  owned by row 38 ("facades/vendorHash follow") or the 12-11 report's §f
  list — appending would have created duplicate-claim noise.

## d) TOTALLY FUCKED UP (this window's own misses, bluntly)

1. **Turn-1 prior-report grep missed — again.** The ritual (`rg -l <task-id>
   docs/status/`) would have surfaced the 12-11 close-out immediately; I
   found it only when opening docs/status/README.md to index this report.
   A same-ID report landing five minutes before dispatch is exactly the
   repeat-dispatch case the ritual exists for. Second window in a row on
   this task to fumble the ritual (12-11 §d1 owned its own skip).
2. **The work-phase response was evidence-free.** I stopped at a 3-line
   "blocked, no change, nothing committed" verdict with zero gate runs —
   contract-compliant but uncited. This report is the retrofit; the battery
   should have run before the stop verdict, not after a follow-up prompt
   (the exact §e1 lesson of the 01-33 window, re-instantiated).
3. **sqlitev4 is red at HEAD and nobody gated it** — discovered only during
   this report's battery. Two failures, both from the concurrent claim-token
   sweep landing ahead of its dependents:
   - `sqlitev4/store_test.go:61` — `claim_w2 declared and not used`
     (vet-fail in the test file);
   - the committed facade require pins `queue/sqlite v0.3.0`, whose
     `CancelOwned(ctx, task.ID, string)` no longer satisfies the mutated
     `queue.Store` contract (`CancelOwned(ctx, task.ID, queue.Claim)`) —
     the proxy-fetched facade cannot compile against HEAD's internal/queue.
   This is the in-flight flip's territory (row 38 owns facades), but until
   it lands, any per-module gate over sqlitev4 is red and the 12-11 report's
   "sqlitev4 build+vet rc=0" claim is already stale at HEAD.
4. **Inherited (restated, not re-owned):** 98a35318 daemon-absorbed the code
   footerless; only the report commit carries attribution. The §d2 maneuver
   (footer-follow-up commit) from 12-11 remains pending an owner ruling.

## e) WHAT WE SHOULD IMPROVE

1. **Serialize-then-suppress at the queue level:** a row explicitly blocked
   on an in-flight sibling row should not be dispatched to a fresh agent;
   the first tool call of this window re-derived a fact the TODO_LIST
   already encoded (see §g1).
2. **Blocked windows still owe a battery.** "No code changed" does not mean
   "no evidence owed" — a stopped verdict must cite at least the cheap gates
   at HEAD (this report's §a is the bar; encode it as a rule, not a habit).
3. **Standing cheap gate for the spike modules** (sqlitev4/postgresv4/replay
   build+vet) in ci-local so a concurrent sweep landing ahead of its
   dependents turns red immediately instead of at the next incidental touch.
4. **Facade require bumps must ride the flip commit** — the v0.3.0 pin
   breaking against the new `queue.Store` contract is foreseeable at the
   moment `CancelOwned`'s signature changes, not at gate time.
5. **Turn-1 ritual ordering:** prior-report grep before TODO_LIST reading —
   the ID-indexed close-out answers "is this DONE-on-arrival?" faster than
   the item text does.

## f) Up to 50 things to get done next

Deduplication: items 1–25 = the 12-11 close-out's §f list, named-not-restated
(top: S1 flip landing, g1 footer reword ruling, ratification memo, replay
rehearsal, claim-token migration design). Items 26–50 are this window's
delta, sorted by impact:

26. Land the **S1 flip**: v4-backed store becomes default sqlite+postgres, worker finalizes → claim tokens (TODO row 38)
27. Fix `sqlitev4/store_test.go:61` unused `claim_w2` (concurrent-sweep casualty, blocks the module gate)
28. Bump `queue/sqlite` + `queue/postgres` facade requires past the `CancelOwned(queue.Claim)` contract change (proxy pin is stale at v0.3.0)
29. `go mod vendor` + nix vendorHash fast-gate re-verify after the flip's go.mod moves
30. S2: settle the open `FactType` shape upstream; define tq fact constants against it
31. S2: promote the `journal.Fact`→`facts.Fact` mapper from spike-local to the canonical journal seam
32. S2: re-point `internal/journal/cqrs` onto the unified fact type (ADR-0014 seq-ULID encoding must survive)
33. S2: re-point the webui journal tailer + SSE hub
34. S2: re-point the four watermark sweepers (review/dlqfix/status/prioritize)
35. S2: re-point papdashboard bridge + answer poller cursors
36. S2: flip facts-in-same-tx to engine-enforced; delete the hand-tx append paths in both spike drivers
37. S2: extend `sqlitev4/replay` to ingest a `facts.Fact` journal (today it replays transitions)
38. Re-run the projection-equality gate over a dogfood journal copy post-S2
39. Ship the CountFacts/FactsSince upstream-growth ratification memo
40. Heartbeat-fact vocabulary mapping (tq ↔ engine) before sweepers re-point
41. Decide `internal/journal` MemoryJournal's fate post-S2 (test fixture vs deleted)
42. Budget sweeper / usage projection re-verify after journal unification (fact-detail json keys are load-bearing)
43. Postgres spike re-run under `TQ_TEST_POSTGRES` after the flip lands
44. Baseline regen for any module whose finding counts move with the flip (`golangci-lint cache clean` first)
45. Facade-parity gate re-run after any new exported symbol on the flip's seam
46. `examples/embed` rot-guard rebuild against post-flip facades
47. Smokes battery post-flip: fullcore, multi-repo, webui, journal-drift, reviews, session-close
48. Record the daemon-footerless-code disposition in CHANGELOG/lessons once ruled (98a35318 + 6ecd0391 class)
49. art-dupl `-t 4` sweep over the spike drivers immediately before S4 deletes the mirrors (mirror-clone inventory refresh)
50. ADR-0019 doc: append the S2 mapper lesson + "blocked-window battery" rule to the plan's open-questions section

## g) Questions I cannot figure out myself

1. **Should the queue suppress re-dispatch of a row whose BLOCKED note names
   an in-flight sibling row?** Both this window and 12-11 spent their budget
   re-deriving serialization facts the TODO_LIST already encoded. If
   suppression isn't mechanical, is a `— BLOCKED:` convention change
   (e.g. `— BLOCKED-ON: <row-line>`) wanted so gates can parse it?
2. **Owner ruling needed: does the footerless daemon-absorbed code
   (98a35318) get a repair maneuver, or does 8e8837df's attribution stand?**
   The 12-11 report left this pending; it determines whether future
   task-attribution audits under-count S2 work.
3. **What is the endgame for `internal/journal` after S2 completes** —
   deleted (its types fully absorbed into upstream `facts.Fact` + tq
   constants), or retained as the tq-fact-vocabulary home? The answer
   changes how far the consumer re-pointing goes (aliases vs real imports).
