# Status report: status-window closeout report paths (task 000001a09d06cf0bc737a06407c5563d66a2)

**Date:** 2026-09-14 01:12 CEST
**Scope:** TODO_LIST "Status-task window payload: include the window's closeout/status report paths" (05-30 report f25). No unrelated research.

## a) FULLY DONE

- The work item is implemented, tested, committed (27aaac2), and the TODO_LIST item is `[x]` with a DONE note citing the change. Concretely:
  - `internal/executor/status.go`: `StatusCompletion` gained `Report string` (`json:"report,omitempty"`, doc-commented) — the repo-relative path of the task's closeout report.
  - `internal/status/sweep.go`: new `closeoutReportPath(repo, id)` globs `docs/status/*_task-<id>.md` under the repo, picks the newest match (sorted, last), returns it repo-relative with forward slashes; best-effort — empty repo, no match, bad pattern, or a `filepath.Rel` failure all return `""`. Wired into `maybeMint` next to the existing `completionDetail` call.
  - `internal/executor/status.go` `statusPrompt`: entries with a report now carry `(closeout report: <path>)`, and the inspect paragraph tells the reporter to read that file FIRST ("it is the task's own full self-review; never re-derive it from the task id").
- Verification, all green at commit time:
  - `internal/executor` + `internal/status`: `GOWORK=off go build/vet/test -race -count=1` ok.
  - Root module: `GOEXPERIMENT=jsonv2 go build && go vet && go test -race` ok.
  - `./scripts/check-facade-parity.sh` — 7 facades mirror, OK (new field, no new symbol, so no alias needed).
  - `gofmt -l` clean on both touched modules.
- New tests: `TestSweepPinsCloseoutReportPaths` (report on disk → path pinned; no file → empty; covers the absolute-repo path real pools use), plus the existing prompt-contract test now asserts the `(closeout report: …)` line. `runAgentTask` was factored into a repo-parameterized helper rather than duplicated.

## b) PARTIALLY DONE

- Nothing in the assigned item. Two adjacent observations, deliberately not expanded into code (scope discipline):
  - The webui `StatusPayload` window rendering (`internal/webui/payload.go` lede/window strip) does not surface the new per-entry report as a link. The payload carries it, so this is a small follow-up, not a gap in the contract.
  - `closeoutReportPath` only matches the `<ts>_task-<id>.md` convention from `DefaultCloseoutPrompt`; an agent that named its report differently is invisible to the sweeper. That is the prompt contract working as designed, but worth knowing.

## c) NOT STARTED

- Nothing assigned was skipped. Out-of-scope items I noticed in passing went to (f), not the diff.

## d) TOTALLY FUCKED UP

- One self-inflicted miss, caught by my own test before commit: in `TestSweepPinsCloseoutReportPaths` the first agent task was seeded with the default repo `"demo"` while the report file was written under the temp repo, so the glob correctly found nothing and the test failed. Fixed by seeding both tasks with the temp repo (`runAgentTaskRepo`). Cost: one red test run, no shipped defect.
- Process note, not a defect: the auto-commit daemon raced me again — all four Go files were swept into `chore: auto-commit 4 changed file(s)` mid-session. Handled per the established amend-maneuver (the commit was local/unpushed): folded the TODO_LIST closure and the `Task-Queue-ID` footer into it, producing 27aaac2 with a clean, reviewable history and a verified trailer. The daemon remains the reason per-task commits need an immediate `git status` re-check before `git add`.

## e) WHAT WE SHOULD IMPROVE

1. **The daemon steals test-green commits with no footer.** Every pool task pays the amend dance. A pre-commit-detectable marker (or the daemon honoring a `.tq-no-daemon` sentinel while a pool task is in flight) would remove the race class entirely.
2. **Closeout report paths should be first-class at emit time.** The sweeper re-derives paths by globbing the filesystem — the same "re-deriving from task ids" this TODO item just killed for the prompt. If the closeout turn's `TQ_RESULT` carried `"report": <path>` (the agent already writes the file and knows the name), `AgentResult` could pin it in the completion fact and the sweeper could drop the glob. One prompt-contract line + one parse field; strictly more robust than filename convention matching.
3. **Session-close bridge status mints bypass the new path resolution.** `tq session close` mints status payloads directly (enqueue-only, no sweeper pass), so its windows never get `Report` populated. Same one-glob helper would close it if this matters.

## f) UP TO 50 NEXT THINGS

Priority-ordered, small and concrete:

1. Surface `StatusCompletion.Report` in the webui status window strip as per-entry links (payload.go/fragments.templ).
2. Carry `"report"` in the closeout turn's `TQ_RESULT` contract; parse into `AgentResult`; sweeper prefers fact over glob (kills §e2).
3. Extend `closeoutReportPath` (or the fact-based path from #2) into the session-close bridge's status mints (§e3).
4. Add `Report` to the status-payload conformance/docs: `docs/DOMAIN_LANGUAGE.md` entry for "window entry report".
5. Consider archiving the fully-done 2026-09-10 fullcore-window reports once their remaining `[ ]` items (this one included) are closed — repoint citations first per the archive convention.
6. The neighbor TODO item "Docs-health continuation: annotate + archive the six 2026-09-07 reports" is untouched — same shape as this task, good next dispatch.
7. Add a negative `closeoutReportPath` test: repo dir absent entirely (not just no match) returns `""`.
8. Property-ish test: glob with a task id containing no glob metacharacters — ids are hex, but pin the assumption with a comment or a paranoid check.
9. README/FEATURES sweep: the status payload contract section in AGENTS.md should mention the new `report` field (small docs maintenance for the next docs-health pass; not edited here to keep the diff scoped).
10. Webui detail-page: the payload section for status tasks could render window entries with report links alongside commits (same data, richer page).
11. `tq show` for status tasks could list per-entry reports in the provenance/payload view.
12. Benchmark the sweeper's per-completion glob: 50 items × one glob each per mint is fine, but a poisoned repo with a huge docs/status dir is the worst case — a one-line comment or a cap would document the bound.
13. Follow-up candidate: have review tasks' payloads cite their reviewed task's closeout report path the same way (the reviewer currently re-derives context).
14. Consider `filepath.Glob` error logging (currently silent `""`): one debug line when the pattern errors would make misconfig visible.

(Items 15–50 deliberately not listed: nothing else surfaced in passing that clears the "small, concrete, agent-executable" bar — padding the list would violate the TODO_LIST conventions.)

## g) UP TO 3 QUESTIONS

1. **Emit-time report path (§e2):** should the closeout `TQ_RESULT` contract gain a required `"report"` field for all pools (breaking the lenient "re-emits the work TQ_RESULT" gate for closeouts), or stay optional with the sweeper glob as fallback?
2. **Webui surface:** do you want per-entry closeout-report links in the status window strip now (one templ fragment + a payload view tweak), or is the payload/prompt consumption enough?
3. **Daemon vs. pool tasks:** is there an owner-side way to pause the auto-commit daemon while a pool task runs (systemd drop-in, sentinel file check), or should the repo grow an in-repo guard?

## Verification citations

- Tests/gates: `go test -race -count=1` green in `internal/executor`, `internal/status`, root module; `check-facade-parity.sh` OK; commit 27aaac2 carries the `Task-Queue-ID: 000001a09d06cf0bc737a06407c5563d66a2` trailer (verified via `git log -1 --format=%(trailers)`).
