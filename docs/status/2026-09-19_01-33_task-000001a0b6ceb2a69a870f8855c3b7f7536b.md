# Third window on the live-no-id smoke task — verify-only re-dispatch, negative control re-run — task 000001a0b6ceb2a69a870f8855c3b7f7536b

2026-09-19 (early hours). Work item (done-prompt-window harvest section):
"Live-no-id smoke assertion: wrapper exit preserves child code, nothing
closed, no DB touched (no-id path is unit-pinned; live path isn't) (02-54
§f)". THIRD dispatch of this ID: TODO_LIST.md:123 `[x]` with a gate-cited
DONE note found in batch one, so this window ran verify → cite → stop.
ZERO edits to code, scripts, or TODO_LIST.md; this report + its index row
is the only write.

## a) FULLY DONE

1. **Re-dispatch caught at turn 1:** whole-repo `git log --oneline -5`
   (top three commits are this ID's own history), `git status` clean,
   `git stash list` empty, footer grep returned both prior reports
   (01-22 work window, 01-27 second verify window) before any tool
   touched the tree.
2. **CONTRIBUTING.md read before verification started** (batch two, not
   batch one — see §d1); CLAUDE.md absent (confirmed).
3. **All four pinned assertions re-verified IN SOURCE at current HEAD
   via the View tool** (the 01-27 §d2 accessor miss not repeated):
   `scripts/smoke/session-close.sh:108-134` — stub child prints an
   unmarker'd line and exits 7; wrapper run as a real process with exit
   captured via `|| NOID_CODE=$?`; asserts (i) wrapper exit == 7
   (passthrough of a NON-ZERO child code), (ii) child stdout forwarded,
   (iii) "no session id" on stderr, (iv) `[ ! -e "$NOID_DB" ]` — no DB
   touched, nothing closed, nothing minted. Last commit touching the
   script is 2d86bef.
4. **Both cited gates re-RUN bare at HEAD, exit codes captured unpiped:**
   `scripts/smoke/session-close.sh` exit 0 (log tail: no-id leg line →
   "session-close smoke passed"); `scripts/check-script-syntax.sh` exit 0;
   `scripts/check-todo-list.sh` exit 0.
5. **The negative control was RE-RUN, not cited** — this window's entire
   reason to exist, closing 01-27 §b1's asymmetric-execution-bar finding:
   in-tree scratch copy of the smoke with the expected wrapper code
   mutated 7 → 8 exits 1 with `FAIL: wrapper exit code 7, want the
   child's 7` — the harness bites. Copy created, run, and removed in ONE
   shell chain; `git status --short` clean immediately after (no
   daemon-food, no residue).
6. **TODO_LIST.md:123 confirmed `[x]`**, DONE note matches the verified
   file state clause for clause — no citation drift found.
7. **Paper trail shipped:** this report + index row, committed with the
   task footer so the queue derives the verify window.

## b) PARTIALLY DONE

1. **No Go build/test suite run.** Same posture as 01-27 §b2, recorded
   consciously: zero Go diffs this window, and the smoke itself builds
   the CLI through the devmod shim (twice counting the negative-control
   run), exercising the real build path. The blanket `-race` ritual stays
   skipped for zero-edit windows until an owner ruling says otherwise.

## c) NOT STARTED (carried; all re-checked open where cheap this pass)

1. **TODO 126 — the positive wrapper leg** (committed leg: footer-commit
   stub + `session_id:` output → id scan → 1 review + 1 status through
   `runSessionClose` + exit passthrough): row verbatim open; its "wire
   into ci-local after session-close.sh" clause is stale (the no-id leg
   folded in made wiring automatic) — re-word when picked up.
2. **TODO 124 + 125 (check-gosec.sh scope/GOSEC_BIN)** — open verbatim,
   adjacent section, not this task's.
3. **Close-failure exit contract owner ruling:** a failed `tq session
   close` inside the wrapper masks the child's exit code with 1
   (cmd/tq/crush.go) — undecided contract-vs-bug; the no-id leg pins
   passthrough only on the no-close path.
4. **Signal-death passthrough:** `forwardSignals` still has no live
   coverage anywhere; 143-through-the-wrapper unpinned end-to-end.
5. **Unit-pin extension:** `TestCrushWrapperNoSessionID` still lacks the
   stdout-forwarding assertion — live pin leads unit pin by one property.
6. **AGENTS.md session-close smoke bullet** still describes begin →
   footer commit → close → replay only; wrapper legs absent (one-line
   docs fix, next docs touch).
7. **Sibling "unit-pinned; live path isn't" asymmetry grep** over
   TODO_LIST.md — not run this pass.
8. **No new backlog items appended:** nothing surfaced that the carried
   list doesn't already own; forcing a row would be padding (same ruling
   as windows 1 and 2).

## d) TOTALLY FUCKED UP

1. **CONTRIBUTING.md landed in batch two, not batch one.** Better than
   the 01-27 window (where it was skipped entirely, N+1 on this ID) but
   the ritual is "batch one"; the read happened before verification
   started, so no evidence is tainted. TODO 128 (session-start ritual
   script) remains the structural fix.
2. **First negative-control attempt ran the mutated copy from /tmp and
   died 127** — the script's `cd "$(dirname "$0")/../.."` resolved to
   `/` from /tmp, so `scripts/build-tq.sh` was not found. Recovered
   immediately with the documented in-tree copy + one-chain cleanup, and
   the /tmp attempt's log was inspected (no FAIL line, so no false
   signal consumed). Cost: one wasted round trip.

## e) WHAT WE SHOULD IMPROVE

1. **The verify-window execution bar is now symmetric and codified by
   example:** in-source read + gates re-run + negative control re-run,
   all fresh at HEAD. Next verify window on any smoke-claim row should
   match it without re-deriving it.
2. **In-tree scratch copies need the one-chain discipline every time**
   (copy → run → rm in a single command; `git status` after) — the /tmp
   placement trap is now recorded here so it costs nobody a second
   round trip.
3. **Done-row re-dispatch keeps burning windows:** this is the third
   window on this ID post-DONE, after the 00-22/00-32 and 01-01/01-07
   IDs each burned three. The queue-side suppression question (01-27
   §g1) stands.

## f) NEXT (carries 01-27's list; nothing new minted)

TODO 126 positive leg first; then the close-failure ruling + signal-death
decision + unit-pin stdout extension + AGENTS.md bullet + asymmetry grep
+ TODO 128 + the status-index annotate sweep. Full enumeration: 01-27
report §c/§f — all items re-verified open this pass or carried verbatim.

## g) QUESTIONS (carried, genuinely not self-answerable)

1. **Re-dispatch policy for DONE rows** (01-27 §g1, now three windows
   deep on this ID): deliberate audit pass, or suppress when the DONE
   note cites gates fresher than the dispatch?
2. **Close-failure exit contract** (01-27 §g2): preserve the child's
   code and print the close error, or is exit 1 correct because the
   close failure is the operator's last concern?
3. **Signal-death passthrough** (01-27 §g3): live-pin `forwardSignals`
   in CI, unit-only, or documented manual check?
