# Task 000001a0b178b22f16b9ef751a5f30d9255a — Session forensics (re-dispatch, verification-only window)

- **Date:** 2026-09-18 00:57 CEST
- **Work item:** "Session forensics: CloseDetail records the AllowDirty/RequireClean decision +
  review range; `tq facts --type session.opened,closed`; `tq show session:<id>` graceful
  degradation for the synthetic ID" (TODO_LIST.md:112)
- **Verdict:** ALREADY DONE by a prior window carrying this exact Task-Queue-ID (commit
  `8da4f33`, "feat: session forensics surfaces in tq facts and tq show"). This window verified
  the claims against code and gates, cited evidence, and stopped — the repeat-dispatch rule
  (fastest correct response to a DONE row is verify → cite → stop).

## a) FULLY DONE

The work item itself, by the prior window (verified THIS window against current code, not memory):

1. **CloseDetail forensics** — `internal/session/session.go:85` (`CloseDetail` carries
   `AllowDirty` (json `allow_dirty`) and `ReviewRange` (json `review_range,omitempty`,
   derived via `reviewRange(commits)` at `internal/session/session.go:220`); marshalled into
   the `session.closed` fact at `internal/session/session.go:214-220`).
2. **`tq facts --type`** — comma-separated fact-type filter exists (cmd/tq/main.go:2433 help
   text names `--type session.opened,session.closed` explicitly).
3. **`tq show session:<id>`** — graceful degradation for the synthetic ID: the show command
   branches on the `session:` prefix (cmd/tq/main.go:1859) and renders opened/closed
   forensics without needing a task row (cmd/tq/main.go:1896).
4. **Tests pin it** — `cmd/tq/session_forensics_test.go` (incl.
   `TestFactsTypeFilterSelectsSessionLifecycle`, session_forensics_test.go:51).
5. **TODO_LIST.md:112** already `[x]` — checked by the same commit's TODO row flip
   (`8da4f33` diff shows TODO_LIST.md | 2 +/-).

This window's verification (cited per the claims-carry-citations rule):

- `go build ./...` (root, GOEXPERIMENT=jsonv2): green.
- `./scripts/test-cmd-tq.sh` (the ADR-0017 devmod-shim gate): **ok** —
  `github.com/larsartmann/go-taskqueue/cmd/tq` 7.674s.
- Commit `8da4f33` message carries `Task-Queue-ID: 000001a0b178b22f16b9ef751a5f30d9255a`
  verbatim — the queue↔git cross-reference already resolves this task.

## b) PARTIALLY DONE

Nothing. Every sub-clause of the item is implemented and pinned by tests.

## c) NOT STARTED

Nothing belonging to this item.

## d) TOTALLY FUCKED UP

Nothing shipped broken. Two window-level notes (honesty, not damage):

1. **First verify attempt used the wrong gate:** I ran `go test ./cmd/tq` from the root and
   hit the documented "main module does not contain package …/cmd/tq" failure (AGENTS.md
   warns exactly this: `go test ./internal/foo` from root fails by design under the
   replace-only multi-module layout). Corrected immediately to `./scripts/test-cmd-tq.sh`.
   Cost: one round trip, no false verdict issued.
2. **Re-dispatch again reached a DONE row.** This is at least the third documented triplet of
   re-dispatches on this queue (02-24 e3 digest names the pattern). The dedup improvement
   below (f-1) is the systemic fix; this window can only keep responding correctly.

## e) WHAT WE SHOULD IMPROVE

- The queue-side mint path re-dispatched an item whose dedup evidence (same Task-Queue-ID on
  an existing commit) was already on master — the check is one `rg -l <task-id> docs/status/`
  + `git log --grep` away. Cheaper at mint time than a burned window at run time.
- Prior-window attribution: the feat commit's own report was never written under this task id
  (this is the first report for it) — the close-out arc produced the commit + TODO flip but
  no `docs/status/` record until now. Filing this report closes that gap.

## f) Up to 50 things we should get done next

Grounded in what this window touched or noticed in passing; most are small. I cap at the ones
I can justify — no invented filler:

1. Queue/mint-side repeat-dispatch guard: before enqueueing a harvested item, grep master
   history for the item's task id / dedup key evidence (like the session-start ritual does) —
   the systemic fix for re-dispatch triplets.
2. Add a `tq` subcommand or flag that surfaces "this TODO item was already completed by task
   <id>" for harvest-time dedup (machine-readable version of the ritual grep).
3. `tq show session:<id>` when the session has an `opened` but NO `closed` fact: verify the
   degradation message is distinct from "unknown id" (I did not exercise the opened-only
   path; only its existence).
4. Consider a conformance pin that every new `CloseDetail` field also renders in the
   `tq show session:` forensics view (adoption-table-style two-direction rot guard).
5. `tq facts --type` help text (main.go:2433) is good — consider `tq facts --type` with an
   invalid type erroring with the list of known fact types (usability, mirrors the crush
   `--reasoning-effort` misleading-error lesson in AGENTS.md).
6. Re-dispatch reports: the 02-24 digest proposed targeting re-dispatch triplets in the next
   ANNOTATE sweep — still open.
7. `INDEX BLOAT WARNING` threshold (~100 live rows) is at ~143 per the digest row — an archive
   sweep is due regardless of this task.

(7 items — the remaining 43 would be fabrication; this window touched nothing else.)

## g) Questions I can NOT figure out myself

1. **Ask-policy for re-dispatches:** should the pool mint-side dedup check (f-1/f-2) treat a
   prior DONE commit with the same footer as a hard skip (cancel the new task silently) or a
   soft surface (enqueue anyway, let the agent verify-and-stop as I did)? Both are defensible;
   the second preserves an independent verification pass.
2. **Should the close-out arc be required to file a status report even on verify-only
   re-dispatch windows?** This task had a commit + TODO flip but no report for ~24h. If yes,
   the check-status-index gate could grow a lint for footer-carrying commits without a
   matching report row — owner call on whether that's worth the gate cost.
3. **`tq show session:<id>` for a session whose facts were journaled under a DIFFERENT
   `--db`:** the synthetic ID resolves facts from the current DB only; is a "not in this
   journal — check --db" hint wanted, or is the plain unknown-id degradation honest enough?
