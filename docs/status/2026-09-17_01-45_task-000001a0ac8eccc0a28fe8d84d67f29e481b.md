# Session-Close Smoke Task — Verify-Only Close-Out (task 000001a0ac8e)

- **When:** 2026-09-17 01:45
- **Task:** TODO_LIST row — "`scripts/smoke/session-close.sh`: scratch-TQ_DB smoke — begin → footer commit → close → assert one review + one status + replay-safe second close; wire into ci-local (03-28 report §f7)"
- **Window type:** verify-only re-dispatch. The fastest correct response to a DONE row is verify → cite → stop.

## a) FULLY DONE

- **The work item itself — done by a prior window, re-verified live this window.**
  - The row was already checked off at TODO_LIST.md:105 with full rationale: smoke green live (dedup on replay, 2× `session.closed` facts pinned), wired after reviews.sh in `scripts/ci-local.sh:236` (commit 1876df3, docs: f60eac5).
  - This window's independent verification: ran `./scripts/smoke/session-close.sh` with a scratch `TQ_DB` — full pass: `tq session begin` → footer commit attributed (1 commit) → close minted exactly ONE review + ONE status task (asserted) → second close replay-safe (both mints answered "known (dedup)") → journal facts asserted (one `session.opened`, one `session.closed` per close). Output ended `== session-close smoke passed`.
- **Turn-1 ritual compliance:** session-start git log/status over the whole repo, DONE-row grep by task ID before doing anything — caught the repeat dispatch immediately and avoided duplicating shipped work.
- No repo changes were made this window; nothing to commit for the work itself (this report is the only artifact).

## b) PARTIALLY DONE

- Nothing from THIS task. The item is complete end-to-end (script + ci-local wiring + TODO row closed).

## c) NOT STARTED (noticed in passing, related to this task's residue)

- **go 1.27.1 go.mod alignment sweep** (TODO_LIST.md:219, already tracked): the prior window fixed cmd/tq to 1.27.1 (required by the devmod shim), but `task`, `worker`, `queue`, `queue/sqlite`, `queue/postgres` still declare 1.26/1.26.7. Re-confirmed live this window: `./scripts/check-go-mods.sh` FAILS at HEAD (five module FAILs, all "root declares go 1.27.1"). This is a known red gate, already a tracked TODO row — not started as of 01:45.

## d) TOTALLY FUCKED UP

- Nothing from this window. Honest note in passing: the tree currently fails `check-go-mods.sh` at HEAD (section c). That is inherited from the prior window's partial alignment, is already tracked as TODO_LIST row 219, and is exactly the class the AGENTS.md "never lower a module's go directive" warning covers — it should be fixed before the next release gate run, but it is not new damage.

## e) WHAT WE SHOULD IMPROVE

1. **Verify-only windows should run the cheap adjacent gates they already know about.** I ran the smoke but only later ran `check-go-mods.sh`; a verify window that re-runs the DONE row's gate should also re-run the gates its own DONE-note mentions (the row's note explicitly calls out the go-directive bump it made).
2. **Partial alignments should not land alone.** The prior window bumped ONE module's go directive to unblock its own smoke and left five others misaligned with a failing gate at HEAD. A single-window sweep (or an immediate TODO row — which did happen) is the better shape; the row exists, but the gate sat red in between.
3. **DONE-note citations are working well** — row 105 cites the smoke result and the exact failure mode that motivated the cmd/tq bump. Keep that bar.
4. **Scratch-DB discipline held** (`TQ_DB=/tmp/...` exported, cleaned up) — worth keeping as the default reflex for every smoke re-run.

## f) Next tasks (up to 50 — brainstorm; most are already tracked or ROADMAP fuel; DO NOT re-file what TODO_LIST already owns)

Tracked already (do NOT duplicate):
1. Finish the go 1.27.1 go.mod alignment sweep across every remaining sub-module; make `check-go-mods.sh` green at HEAD (TODO_LIST:219 — highest priority, gate is red today).
2. Session-close bridge open items from its design doc: trigger automation (crush #3146), daemon-commit attribution gap, budget bypass, postgres parity (docs/planning/2026-09-12_session-close-bridge-design.md).
3. Delete the legacy stdout `TQ_RESULT:` fallback once the live pool shows derived outcomes (verdict-file channel is primary).
4. Owner fix still pending: `Environment=GOEXPERIMENT=jsonv2` on the NixOS tq-agent-pool module (env-lie class, 2026-09-11 task 000001a08ebf).
5. GitHub Push Protection fixture hardening: compose token-shaped fixtures so no scanner matches source (redact_test.go Slack fixture class).
6. Pin `crush run --reasoning-effort` failure-mode upstream issue: mistyped level yields misleading "does not support reasoning effort" instead of listing accepted values.
7. Document the `--override-input go-nix-helpers <path>` local-testing recipe (noted in AGENTS.md as "not yet documented").
8. ci-local currently leaves a failing `check-go-mods.sh` as the catcher — consider ordering it earlier so the red gate surfaces before the long module loop (small win, judgment needed).

New, small, from this window (candidates for TODO_LIST if not covered above):
9. When a smoke script bumps a go directive to unblock a devmod shim, assert the FULL module matrix (run `check-go-mods.sh` in the same window) — cheap guard against partial alignments.
10. Session-close smoke could assert the `session.closed` fact CARRIES the attributed commit list (currently asserts fact counts only) — one-line strengthening if deemed valuable.

(11–50: intentionally empty — per the run contract, no unrelated research; everything else lives in TODO_LIST/ROADMAP and should be harvested from there, not re-invented here.)

## g) Questions I cannot figure out myself

1. **Should the go 1.27.1 sweep land before the next release tag?** `check-go-mods.sh` is red at HEAD; if release gates run it, the next release is blocked until row 219 is done — is the next release planned soon enough that this jumps the queue?
2. **Is the root at 1.27.1 deliberate-and-sticky, or should the LEAVES pull the root back down to a common 1.26.7?** The AGENTS.md rule says "never lower" and "match the root's exact version", so the sweep direction is clear mechanically — but if 1.27.1 on root was itself accidental, the cheaper fix inverts.
3. **Does the dogfood pool currently eat unchecked TODO_LIST items from THIS repo?** If yes, the red `check-go-mods.sh` gate will bite the next agent task that runs repo-level gates outside the flake devShell — should row 219 carry `— P[1]` marker protection until it lands?

— End of report. Verified claims cite the live gate runs above (smoke pass 01:44, check-go-mods FAIL 01:45) and TODO_LIST.md:105.
