# SSE-Flake Re-Dispatch Verification Close-Out (brutal a)-g)

Task: `000001a097c2d273f5768bed42c801cba529` — "Root-cause the load-dependent SSE test flake: TestSSEHeartbeatStopsBeforeHandlerExit … fix the budget or load-skip explicitly".

**Window type: verification re-dispatch.** The work item was already DONE on
arrival: commit `b198572` (01:12, ~10 minutes before this window started)
implemented the fix under this exact Task-Queue-ID footer, closed the TODO row,
and `bbbe8fc` indexed its close-out (`2026-09-13_01-15_task-…97c2…md`). This is
the first re-dispatch of THIS task id (the 00-19/00-30 trio was the required-
checks id `…97a2…`), and the third redundant dispatch across both ids in one
night.

This window changed ZERO deliverable files. Its product is independent
verification — per AGENTS.md, status reports are point-in-time and uncited
claims are hypotheses, so the prior window's green matrix was re-proven from
scratch, not trusted.

## a) FULLY DONE

1. **Re-dispatch forensics before any work** (session-start ritual: whole-repo
   `git log`, `git status`, stash list — all clean): `b198572`'s footer carries
   the exact dispatch ID; TODO row 115 already `[x]`; close-out report
   git-tracked and indexed (`bbbe8fc`). No duplicate fix attempted, no empty
   commit manufactured.
2. **Independent isolated stress**: `cd internal/webui && go test -race -run
   TestSSEHeartbeatStopsBeforeHandlerExit -count=10` → **10/10 PASS**
   (~2.8 s each, 29.6 s wall).
3. **Independent under-load stress**: same `-count=10` run CONCURRENT with the
   full root `go test ./... -race` suite (background, started 5 s prior; real
   16-package contention on 4 cores) → **10/10 PASS**. This upgrades the
   01-15 close-out's §b caveat: its matrix used a _synthetic_ double-load,
   while this window's negative control ran against the _literal_ suite
   condition that produced the 00:24–00:29 failures.
4. **Full root suite**: exit 0, 15 packages ok, zero FAIL.
5. **Standard verify gate**: `GOEXPERIMENT=jsonv2; go build ./... && go vet
   ./...` green; `gofmt -l` clean on `webui_test.go`.
6. **Receipt integrity**: the fix commit already carries the exact footer, so
   `TQ_RESULT` cites `b198572` rather than minting a no-op commit.

## b) PARTIALLY DONE

1. **Sub-module gate loop and ci-local NOT run** — defensible for a zero-code-
   change window (last full ci-local green lineage; root gate green at HEAD),
   but it is a declared skip, not a proof.
2. **The concurrent stress is one notch off the literal failure environment**:
   the background suite's own webui package ran `(cached)`, so the exact path
   "webui compiled + scheduled inside the suite's own -race invocation" was
   exercised only by the background suite passing, not by my stress loop.
   The flake condition (loaded connect+snapshot tail > budget) is still what
   the stress reproduced; the packaging difference is noted for honesty.
3. **2-core runner headroom still unproven** (inherited from 01-15 §b): 4-core
   host + full-suite contention is the best local proxy; the GitHub runners
   have 2 cores.
4. TODO row convention verified only by grep (`[x]` prefix), not by re-reading
   the row's full text after the prior window's edit.

## c) NOT STARTED

1. Consumer load-transient item (TODO line 116,
   `TestHandlerErrorPausesOnlyThatSubscriber`) — deliberately left to its own
   dispatch; this window held scope.
2. Examples `IdleTimeout`/`ReadTimeout` hardening (TODO line 121).
3. 01-15 §f carries: CI wall-time watch, repo-wide timing-budget audit,
   go-sse hang-guard option, transient-family policy — none advanced here.
4. 2-core-constrained budget verification (taskset proxy) — not run.

## d) TOTALLY FUCKED UP

1. **Broken exit-code capture on the first gate, shipped anyway**: the
   isolated-stress command echoed `exit=` EMPTY (`${PIPESTATUS[0]}` does not
   survive the mvdan/sh handoff the way I wrote it), and I let the run stand on
   the human-readable `PASS/ok` tail. That is the pipeline-masking sin family —
   the exact class documented in TWO index rows I read minutes earlier
   (00-30: "FULL_EXIT=$? after a grep chain printed 0 on a FAILED run"; 19-04:
   "pipeline-masked exit code AGAIN"). I got lucky: the raw output was genuine
   PASS. Reading output is not a gate; a gate that cannot fail is decoration.
2. **Wrong search pattern wasted a round trip**: `ls docs/status | grep -iE
   "sse|heartbeat"` missed the task-id-named report files (only the httputil
   report matched); the 01-15 close-out surfaced only via the README index.
   Reading the index head FIRST was the one-hop move.
3. **The `(cached)` wrinkle in b2 was discovered after the fact**, not designed
   around: I declared "under live suite load" without checking what the
   suite's own webui run did. Declared here because it would have been
   cheapest to design correctly before running.
4. **Lineage meta-sin, not this window's**: the queue minted this dispatch
   ~10 minutes after the fix commit closed the item — dispatch-time dedup
   (Task-Queue-ID footer check against `git log` + TODO state at mint time) is
   still missing. Third redundant dispatch in one night, flagged in 00-30 and
   again here; nothing prevents number four.

## e) WHAT WE SHOULD IMPROVE

1. **Dispatch-time dedup** in the queue minter: refuse to mint when the
   Task-Queue-ID already appears in recent commit footers or the TODO row is
   already `[x]` (escalation of the 00-30 proposal, now n=3 evidence).
2. **Gates must capture exit codes mechanically**: `set -o pipefail` + explicit
   capture before any tail/filter; a personal-checklist line "no pipeline
   whose failure cannot surface" would have caught d1 pre-ship.
3. **Standardize the load-stress matrix** as a small script (suite in
   background + target package `-count=N`, concurrent exit codes joined) —
   every window since 00-19 has hand-rolled a variant with different caveats.
4. **2-core proxy run**: `taskset -c 0,1` on the SSE budget matrix before the
   next push, converting "wall-time faith" into evidence for the 250 ms
   budget on runner-class hardware.
5. **Repo-wide timing-budget audit** (carried from 01-15 §f): find remaining
   30 ms-class deadlines under `-race` and give each the same root-cause or
   explicit-headroom treatment — the third consecutive window with a
   load-dependent transient says the class, not the instance, is the bug.
6. **Session-start ritual addition**: read `docs/status/README.md` head rows
   alongside `git log` — it is the fastest duplicate-work detector this repo
   has (would have identified the re-dispatch in one hop).

## f) Up to 50 things to get done next (grounded; carried items cited, not re-derived)

_From this window's observations:_

1. Queue dispatch-time dedup guard (footer/TODO-state check at mint; d4/e1).
2. Load-stress helper script (e3) — one command, joined exit codes.
3. `taskset -c 0,1` 2-core stress of the 250 ms budget before next push (e4).
4. Pipefail gate helper or checklist rule to kill the pipeline-masking class (e2).
5. Timing-budget audit across the repo's test deadlines (e5).
6. Read `docs/status/README.md` head rows in the session-start ritual (e6).
7. At the next code-touching window: full sub-module loop + ci-local (b1 skip
   must not become a habit).

_Open TODO_LIST items noticed in passing:_
8. Consumer transient: `TestHandlerErrorPausesOnlyThatSubscriber` timing
assumption fix or cursor-based skip (TODO line 116, minted by 01-15).
9. Examples hardening: `IdleTimeout`/`ReadTimeout` on examples/api and
examples/sse, keeping the SSE `WriteTimeout=0` exemption documented
(TODO line 121).

_Carried from the 01-15 close-out §f (index row, cited):_
10. CI wall-time watch for the raised budget (test went 0.93 s → ~2.8 s).
11. Transient-family policy ruling: per-test budget fixes vs a shared
load-budget helper for the whole repo.
12. go-sse hang-guard option evaluation (CollectWithTimeout fatal paths).

_Carried from index rows read in passing this window (cited, not re-filed):_
13. Push the cqrs tag — flagged CI-blocking in the 17-30 and 19-04 rows.
14. Lint-baseline regen acceptance: revert the one deliberate config edit
(19-04 row §g).
15. Priority-system pilot go/no-go — T28 scorer spend is the only decision
(17-30 row §g).
16. Root-cause the httptest `CloseIdleConnections` flake class (17-30 row).
17. Fix the CONTRIBUTING-vs-AGENTS dprint split-brain (00-19 row, noticed
not fixed).
18. Move the blame-based lint-baseline classifier from /tmp into `scripts/`
(19-04 row §d).

## g) Questions I cannot figure out myself

1. **Was this dispatch an intentional verification pass or a duplicate mint?**
   If duplicate: may I build the dispatch-time dedup guard (e1)? Three
   redundant dispatches in one night is the evidence base; the fix is a small
   minter-side check, but enqueue policy is owner territory.
2. **Is the 250 ms budget accepted for 2-core CI runners on wall-time faith,
   or should a taskset-constrained stress be REQUIRED before the next push?**
   The suite is green and the headroom math (~10x) is sound, but runner-class
   hardware is the one environment nobody has measured.
3. **TQ_RESULT sha semantics for re-dispatches of already-committed work:**
   this window cites the fix commit (`b198572`) since nothing new was
   committed. If the gate or dispatcher expects the close-out commit's sha
   instead, the mismatch may cause yet another re-mint. Same question was
   asked in the 00-19/00-30 rows for the other task id — still unanswered,
   which is why it returns here.
