# Task 000001a0b7a5dbe23dee8a0b8386f4d1b384 — ci.yml gosec pin derivation (window 1, work + brutal pass)

2026-09-19 05:20. First dispatch of this ID. TODO row 131 (02-18 report §f4/§e5):
derive GOSEC_VERSION + GOSEC_EXCLUDES in the ci.yml gosec job from
scripts/check-gosec.sh instead of the second hand-pinned copy, WITHOUT forcing
row 127's full CI-loop convergence (owner-blocked). Smallest correct change
shipped, TODO closed, report + 2 follow-up rows + AGENTS.md drift fix in the
close-out commit.

## a) FULLY DONE

1. **The deliverable** (commit 6c2211b, exactly 2 files: .github/workflows/ci.yml + TODO_LIST.md):
   - ci.yml:281-282 sed both values out of scripts/check-gosec.sh:22-23
     (`s/^GOSEC_VERSION=//p` for the pin; quote-stripping capture
     `s/^GOSEC_EXCLUDES="\(.*\)"$/\1/p` for the excludes).
   - ci.yml:283-286 fail-closed guard: empty derivation → loud FAIL + exit 1
     before any install/scan (the repo's scanner-must-prove-it-ran ethos).
   - ci.yml:287 install + ci.yml:294 step summary now interpolate
     `$GOSEC_VERSION`; the literal `v2.29.0` is gone from the run block.
   - ci.yml:266-269 job comment records the single-pin-source derivation and
     that the loop stays hand-rolled (row 127 remains owner-blocked).
   - Loop, for-each-module consumption, continue-on-error: untouched.
2. **Byte-identical derivation proof**: the exact sed lines executed locally
   returned `v2.29.0` and
   `-exclude=G104,G115,G118,G124,G202,G204,G301,G302,G304,G306,G404,G702,G703,G710`
   — identical to the deleted hand-pinned pair (old ci.yml:277-278). Zero
   behavior change in what CI installs and excludes.
3. **Sweep for other copies**: `rg "v2.29.0|G703" .github/workflows/` → only
   ci.yml prose comments (:260, :264, descriptive, not functional); fuzz.yml
   carries no gosec. The functional pin now lives exactly once
   (scripts/check-gosec.sh:22-23). `.golangci.yml` gosec.excludes is a
   DIFFERENT consumer's copy (linter config, scanner parity per AGENTS.md) —
   deliberately out of this item's scope, queued (§f2).
4. **Gates, cited**: actionlint green; TestRepoTodoListParses ok (TODO_LIST
   machine-parse); root `go build ./... && go vet ./...` OK (GOEXPERIMENT=jsonv2);
   full root race suite rc=0, 15 packages ok, zero FAIL lines (rc captured to
   file on the second run — see §d2 for why there were two).
5. **Daemon-race recovery done right**: the auto-commit daemon swept both work
   files into footer-less `cce4a3c` while gates ran. Verified `NOT_PUSHED`
   (`git merge-base --is-ancestor` against origin/master) BEFORE amending →
   6c2211b with the real message + exactly one footer; `%(trailers)` resolves
   the ID; footer-last shape so gitscan attribution sees it (03-00 lesson).
   Fork record: cce4a3c → 6c2211b (unpushed amend, no twins existed).
6. **TODO row 131** closed `[x]` with a DONE note citing the sed shapes, the
   byte-identity check, actionlint, and the untouched-loop scope.
7. **Drift fix on sight**: AGENTS.md gosec note said the ci.yml job "runs
   -exclude=…" — stale the moment this change landed; now says the job DERIVES
   pin+excludes from the script and .golangci.yml remains the hand-synced copy.
8. This report + docs/status/README.md index row, committed together (the
   check-status-index gate's own standard).

## b) PARTIALLY DONE

1. **Fail-closed guard is inspection-verified, not execution-verified.** I
   reasoned `[ -z … ] || exit 1` is trivially correct and did not run the
   empty-derivation one-liner. Costs ~5 seconds to cite; should have.
2. **No executable pin on the derivation itself.** If someone reformats
   scripts/check-gosec.sh:22-23 (quotes, `export`, leading spaces), ci.yml's
   sed goes empty → the guard fires — but only on a runner, inside a
   continue-on-error job that cannot block merge, and no local gate ever
   exercises ci.yml's shell. The drift alarm is advisory-only (queued §f1).
3. **Verification battery is targeted, not the full ci-local replicant.**
   build/vet/race + actionlint + harvest-parse ran; nix build / flake check /
   webui smoke did not. Justification: the diff touches YAML + markdown only,
   no Go/script/nix surface; a push would have required the full gate. No push
   happened. Stated as scope, not excuse — the pre-push standard remains
   ci-local.

## c) NOT STARTED (queued or owner-blocked; unchanged by this window)

- Row 127: full convergence of the ci.yml loop onto check-gosec.sh (owner
  CI-independence ruling). This change shrank its surface to the loop only.
- Row 124: cmd/tq gosec coverage (owner ruling); Row 40: gate-vs-advisory O5.
- Rows 132 (self-test foreign-CWD), 133 (for-each-module zero-target
  consumers — my job's `for m in $(…)` at ci.yml:288 is one of the six),
  134 (short-enumeration canary), 135 (sibling summary lines): sibling
  windows' harvest, untouched.
- Unstamped GOSEC provenance (02-52 §g q2): still WARN on this host.

## d) TOTALLY FUCKED UP

1. **Edit-before-read, twice, in the first five minutes.** multiedit fired at
   ci.yml and TODO_LIST.md before any View; the tool refused both ("you must
   read the file before editing"). Zero damage, two wasted round trips — and
   it is the same refusal class the 05-04 report recorded on the sibling ID
   (miss N+12 family). The rule exists because the tool will eventually NOT
   refuse; treat the refusal as the last warning, not a speed bump.
2. **Re-hit the pipeline-masking hazard on the first test battery** —
   `go test ./… -race | rg -v "^ok|no test files" | head; echo RC_MARKER_DONE`:
   no true exit code, filter-tail verdict, exactly the shape AGENTS.md codified
   TODAY after the 04-32 window's identical miss. I caught it unprompted and
   re-ran with `rc=$?` captured to a file (rc=0, 15 ok), so nothing shipped on
   the masked reading — but a codified lesson re-burning one window after
   codification is a ritual failure, not bad luck. §f6's one-liner exists to
   kill this class.
3. **Lost the daemon race on the work commit itself.** Gates ran between the
   last edit and the commit; the daemon swept ci.yml+TODO_LIST into footer-less
   cce4a3c; my chained `git add && git commit` then found a clean tree and died
   with exit 1. Recovery was correct and policy-compliant (unpushed-verified
   amend → 6c2211b), but this is the third live occurrence across windows
   (04-46 lost its close-out; here the WORK commit needed manual attribution).
   The structural fix is commit-first-then-gate (§e1) or a daemon-side change
   (§g3); I again paid the manual-amend tax.
4. **The first battery's output was theater**: `RC_MARKER_DONE` is a literal
   string, not a status. Anyone skimming transcript #1 sees "green" with no
   evidentiary basis. Honest rc only existed after run #2. A verification
   command that cannot fail is decoration.

## e) WHAT WE SHOULD IMPROVE

1. **Commit-first, gates-after, amend-on-red.** The daemon's opening is the
   3-4 minute gate window. Committing immediately after the edit (footer on),
   then gating and amending on red, inverts the race in my favor. Tradeoff:
   transient ungated commits on master — owner-taste call, echoed from 05-04 §e.
2. **Derivations deserve self-tests.** A 10-line --self-test addition asserting
   ci.yml's sed lines extract non-empty, byte-matching constants would turn
   CI-only advisory drift into a pre-push hard failure (queued §f1).
3. **Quote-stripping capture is the most fragile byte in the change.** A
   `check-gosec.sh --emit-env` (or `--print-pin`) subcommand would give CI a
   machine-readable seam instead of scraping source lines. Only worth it if
   §f1's guard lands anyway.
4. **Run the cheap sim when you assert a guard.** §b1's one-liner costs less
   than the sentence I wrote explaining why I skipped it.
5. **Session ritual batch worked**: turn 1 ran log+status+stash, task-ID grep
   (no prior reports — true first dispatch), CONTRIBUTING read, in parallel.
   Keep the batch; it is what surfaced the clean-tree baseline that made the
   daemon-race diagnosis instant.
6. **Gate invocations: file + `$?`, always.** `cmd >/tmp/x.log 2>&1; rc=$?`
   from the first attempt, not the second. The AGENTS.md sentence is already
   exact; the only improvement is obedience.

## f) Up to 50 things we should get done next (honest list, window-scoped — not padded to 50)

1. NEW (queued): ci.yml gosec derivation drift guard — self-test/ci-local
   asserts the sed lines still extract byte-identical constants (§b2/§e2).
2. NEW (queued): .golangci.yml gosec.excludes derive-or-parity-assert — the
   last hand-synced copy of the triage (AGENTS.md promises scanner parity).
3. Row 132: check-gosec.sh --self-test absolute-path fix + foreign-CWD
   assertion (reproduced rc=127 on 2026-09-19).
4. Row 133: for-each-module zero-target consumers — six ci.yml loops catch
   neither failing nor empty enumeration (ci.yml:288 included).
5. Row 134: pin a SHORT module list as a failure (renamed sub-module drops out
   silently today).
6. Row 135: end-of-run summary lines for sibling gates (check-script-syntax,
   check-go-mods, check-dead-exports, check-guard-wiring).
7. Row 127: full ci.yml→check-gosec.sh convergence — owner-blocked; re-surface
   with the note that the pin/excludes half is DONE as of this window.
8. Row 124: cmd/tq gosec coverage — owner-blocked.
9. Row 40: gosec gate-vs-advisory flip — owner ruling O5.
10. Unstamped GOSEC provenance (02-52 §g q2) — every ci-local run still WARNs
    on this host's `Version: dev` binary; owner install clears it.
11. Daemon attribution gap, structural fix (03-00 §g1 carried): footer-less
    sweeps of task-attributed files keep happening (this window, third time).
12. Re-dispatch policy across gosec IDs: summary-line ID burned 3 windows on
    repeat dispatches; this ID was a clean first dispatch — the bug-or-expected
    question stays open (04-54 §g1).
13. Verify-window report format capping (05-04 §g2 carried): full battery
    re-runs cost minutes per window for zero-edit dispatches.
14. TODO row 136: Dead-SHA citation gate — my §d3 fork record uses the arrow
    form; that gate would enforce it.
15. TODO row 137: drop refs/original once the healed lineage is pushed —
    owner-blocked on push.
16. Prose pin mentions (ci.yml:260 "gosec v2.29.0" triage comment) now describe
    history, not config — harmless, but the comment could say "pin lives in
    check-gosec.sh" for the next reader. LOW.
17. CI dry-run of the derivation on a runner (workflow_dispatch scratch or
    next natural push) to observe the FAIL-free path end-to-end in the real
    environment — actionlint proves syntax, not runner semantics. MEDIUM.
18. .github/workflows/fuzz.yml has no gosec — correct today (fuzz campaign
    scope), worth one sentence in the AGENTS.md gosec note if the fleet grows
    another workflow. LOW.

## g) Questions I can NOT figure out myself

1. **Advisory blind spot**: the fail-closed derivation guard lives inside a
   continue-on-error job, so a red derivation can never block merge, and no
   local gate exercises ci.yml's shell. Do you want the §f1 drift guard built
   (I can do it without a ruling), and should anything in the gosec family be
   ALLOWED to fail a push, given O5 keeps the scans advisory?
2. **.golangci.yml gosec.excludes**: derive it from check-gosec.sh too, add a
   parity assertion, or is the hand-synced copy deliberate because the linter
   config format differs (CSV list vs `-exclude=` flag)? The AGENTS.md parity
   promise is currently enforced by nothing.
3. **Daemon attribution, third strike**: do you want the daemon changed (defer
   paths matching an in-flight task's footer, or append the footer itself), or
   is manual amend-on-race the accepted permanent cost? Each occurrence costs
   a history rewrite of an unpushed commit and one honest §d entry.

---
Verification battery (this window, HEAD=6c2211b at time of writing): actionlint
ok; sed extraction byte-identical; TestRepoTodoListParses ok; build+vet OK;
root race suite rc=0 (15 ok); footer count=1, trailers-visible; tree clean
pre-report. Full ci-local NOT run (no push; §b3).
