# Verify-and-stop (third dispatch): `tq tasks --band` CLI parity

Task 000001a0bd841527f58d252d81b4effaf4aa. THIRD dispatch of a DONE task:
shipped in the 08-43 window (commit db372ab, exact footer, TODO row 156
`[x]`, close-out indexed), verified again in the 08-47 window. This window
made zero source edits; verify → cite → stop per the session-start rule.

## Fresh battery at HEAD (rc captured directly, not from a pipeline)

- root `GOEXPERIMENT=jsonv2 go build ./... && go vet ./...` → rc=0
- `./scripts/test-cmd-tq.sh` → rc=0, `ok …cmd/tq 10.650s` (includes the
  pinning test `TestCmdTasksBandFilter`)
- work commit db372ab present with the parseable `Task-Queue-ID:` trailer;
  ID resolves in docs/status/README.md (2 prior rows) and TODO_LIST.md:156
  is `[x]`.

No fresh delta exists beyond this report: `git status` clean at start, zero
source changes since db372ab. Prior windows' verification scope stands.

## a) FULLY DONE

The work item (db372ab), TODO row `[x]`, two prior reports + this one,
all gates cited above.

## b) PARTIALLY DONE

Race suite / nix / ci-local not re-run — justified only because no source
has changed since the 08-43 window's full battery; cheap gates re-run fresh.

## c) NOT STARTED

Nothing; the task was complete on arrival.

## d) TOTALLY FUCKED UP

Nothing new: the documented command (scripts/test-cmd-tq.sh) was used
first this time; session-start ritual honored via the footer grep.

## f) NEXT

Unchanged: the 08-43 close-out's 11 items + 08-47 §f13 (queue-side
DONE-row re-dispatch suppression — now at THREE windows on this ID; every
further dispatch is pure spend).

## g) QUESTIONS

08-43 §g1–g3 and 08-47 §g1–g3 stand unchanged, owner-only.
