# Status Report — task 000001a08e71df95f81bb4c091cf4b28073b

**Date:** 2026-09-11 05:12
**Work item:** "`checkProjectsDir` should not require the projects dir when every `--repos` entry is absolute (02:00 f16; cmd/tq/agentpool.go + test)"
**Commit:** 9804683 (work) — report committed separately, same Task-Queue-ID footer.
**Scope discipline:** single work item, no drive-by refactors. Note: the HTML dashboard default from the status-report skill was overridden — the queue contract explicitly asked for `.md` at this path.

## a) FULLY DONE

1. **Guard bypass for fully-absolute `--repos`.** `runAgentPool` (cmd/tq/agentpool.go:232) now skips `checkProjectsDir` when `--repos` is non-empty AND every comma-separated entry is an absolute path (`filepath.IsAbs`). A risky (or default-resolved) projects root no longer blocks legitimate fully-absolute invocations.
2. **New helper `allReposAbsolute`** in cmd/tq/seeds.go: uses the existing `splitRepos` (no parsing duplication); empty spec → false (no repos = nothing to trust).
3. **Tests:** `TestAllReposAbsolute` table (empty / single absolute / mixed whitespace / one relative entry / all relative) in cmd/tq/seeds_test.go. Existing `TestCheckProjectsDir` untouched and green.
4. **Verification:** `GOEXPERIMENT=jsonv2 go build ./cmd/... && go vet ./cmd/...` clean; full `go test ./cmd/tq/ -count=1` green; `gofmt -l` clean.
5. **TODO_LIST.md closed out:** item marked `[x]` per the file's checkbox convention.
6. **Committed** with Task-Queue-ID footer; working tree clean afterwards; nothing pushed.
7. **In passing, verified:** the session-start ritual (`git log/status/stash`) showed no concurrent-agent surprises; repo was clean at start.

## b) PARTIALLY DONE

1. **Guard bypass covers only `agent-pool`.** The task item named agentpool.go explicitly, and I stopped there — but `checkProjectsDir` is also called from `tq worker --agents` (cmd/tq/main.go:463) and `tq audit` (cmd/tq/audit.go:42). Those two callers still refuse on a risky projects dir even with all-absolute `--repos`. Deliberate (smallest correct change; the item scoped agentpool), but the carry-parity question is now open — see (f).
2. **Skipped-comment placement:** the explanatory comment sits inside agentpool.go's flag-parse function; the helper's own doc comment in seeds.go is thinner than the repo's usual contract-documentation style. Functional, not exemplary.

## c) NOT STARTED

1. No CHANGELOG.md entry — this is a behavior change to a user-facing flag interaction (`agent-pool` now starts in a case where it previously refused). The repo convention is append-only CHANGELOG; a one-liner is warranted and was not added.
2. No AGENTS.md update — the guard-bypass semantics (when the risky-projects-dir guard applies) are the kind of non-obvious behavior AGENTS.md exists to record; the existing guard was never documented there either, but this change makes the asymmetry (agent-pool skips, worker/audit don't) harder to discover from code alone.
3. No docs/status harvest routing of new follow-up items into TODO_LIST.md (this report is the routing input — section f below).
4. Nothing else in scope was left undone: the work item itself is fully complete.

## d) TOTALLY FUCKED UP

1. **One botched edit, caught and fixed in-flight:** my first edit to agentpool.go consumed the `return agentPoolOptions{...}` opening lines (old_string included them, new_string didn't), leaving orphaned struct fields and a syntax error. LSP diagnostics flagged it immediately; I re-read the exact lines and restored the return block within one tool call. No damage shipped — the committed code builds and tests green — but a top-tier engineer's edit would have matched boundaries precisely the first time (`lsp_replace_symbol` on the function would have avoided the class of error entirely).
2. **Nothing else.** No reverted work, no broken state at any commit point, no unrelated files touched, no `.crushrc`/`.tq-verify` interference.

## e) WHAT WE SHOULD IMPROVE

1. **Prefer `lsp_replace_symbol` for whole-block edits** — the botched edit above was a textbook case for symbol-boundary replacement over string matching.
2. **Guard symmetry needs a decision, not drift:** agent-pool bypasses, `tq worker --agents` and `tq audit` don't. Either the bypass belongs in `checkProjectsDir` itself (callers pass their resolved repos) or the asymmetry should be documented as deliberate. Silently carrying three behaviors invites a split brain.
3. **Behavior changes should ship with a CHANGELOG line in the same commit** — cheap, and the repo already treats CHANGELOG as append-only; I under-scoped.
4. **Consider testing the bypass at the options level** (a `runAgentPool` flag-parse test asserting acceptance with `--projects-dir / --repos /abs/a,/abs/b`), not just the helper — the wiring, not the helper, is the contract.
5. **The lll baseline in agentpool.go** (flag help strings 120–207 chars) keeps tripping advisory lint on every touch; a one-time rewrap of that flag block would silence a recurring noise source — but per repo rules that's a dedicated small task, not a drive-by.

## f) Up to 50 things we should get done next

*(Brainstorm, not commitment — most items below the top ~10 are ROADMAP fuel; HARVEST should apply routing rigor. Items 1–3 follow directly from this task.)*

1. Extend the absolute-repos bypass to `tq worker --agents` (main.go:463) or document why agent-pool alone skips.
2. Same decision for `tq audit` (audit.go:42).
3. Add a `runAgentPool` flag-parse-level test: `--projects-dir / --repos /a,/b` accepted; `--repos a,b` with `/` still refused.
4. Add CHANGELOG entry for the guard-bypass behavior change (9804683).
5. Document the risky-projects-dir guard + bypass conditions in AGENTS.md operational contracts.
6. Rewrap agentpool.go flag help strings to ≤120 chars (kill recurring lll noise).
7. Audit whether `--repos` entries should be normalized (trailing slashes, `~` expansion) before `filepath.IsAbs` — `~/x` is NOT absolute and silently keeps the guard active.
8. Consider a `--` style escape or explicit `--projects-dir=-` to mean "no projects dir" rather than inferring from repos shape.
9. `splitRepos` trims whitespace? Verify and pin with a test (my `/srv/a, /srv/b` test passed, implying trim exists — pin it).
10. Error message of the guard could mention the all-absolute `--repos` escape hatch so users self-serve.
11. `tq doctor` exercises pool options — confirm doctor reports (not fails on) the bypassed-guard configuration.
12. Bootstrap (`bootstrap.go:286` appends positional args to repos) — check whether the same guard-bypass semantics should apply there.
13. Smoke test: `scripts/smoke/status-loop.sh`-style smoke for pool startup with fully-absolute repos against a scratch DB (`TQ_DB=` set — production journal trap!).
14. Verify the config-file path (`applyPoolConfigFile`) can't smuggle a projects-dir that re-triggers the guard post-bypass (flag precedence).
15. Windows: `filepath.IsAbs` on `C:\...` — confirm CI windows-latest covers the new test (it's not `//go:build unix` gated, so it runs; verify it passes).
16. Fuzz `allReposAbsolute`? Overkill, but `FuzzParseRepo`-adjacent seeds for comma/path shapes are cheap corpus additions.
17. Sweep status reports (docs-health ANNOTATE) for older claims that the guard "always refuses home dir" — now stale for the agent-pool path.
18. Review whether `expandRepoSpecs` callers (worker, doctor, audit) should share the "absolute entries skip projects dir" logic instead of each deciding.
19. Consider promoting the guard + bypass into `agentPoolOptions` construction with a returned `guardSkipped bool` for observability/logging.
20. Log line when the guard is skipped (repoRootDesc already prints repos provenance — keep them consistent).
21. `repoRootDesc(poolOpts.projectsDir, poolOpts.repos)` — verify its output stays truthful when projects dir is risky-but-bypassed.
22. TODO_LIST.md hygiene pass: the dogfood-round section (02:00 self-review §f) — how many items remain unchecked? Triage against (f) here.
23. gopls jsonv2 phantoms pollute every session's diagnostics — root-cause the LSP env (GOEXPERIMENT) instead of tolerating it.
24. Dead-export audit rerun after this change (`allReposAbsolute` has one importer by design; confirm no near-duplicate helper already existed elsewhere).
25. golangci-lint advisory on seeds.go flags err113 (dynamic errors) on pre-existing lines — triage whether the guard should return wrapped static sentinels.
26. Add `tq doctor` check: warn when `--repos` mixes relative and absolute entries (user probably meant all-absolute).
27. Unit test for `splitRepos` with empty segments (`"a,,b"`), trailing comma, only-comma — contract pinning.
28. Documentation: `docs/DOMAIN_LANGUAGE.md` — is "projects dir" vs "repos" vocabulary defined? The guard's refusal text invents phrasing; align.
29. README/FEATURES mention of the agent-pool flag behavior — likely stale once guard semantics change; verify.
30. Consider acceptance criteria doc for the guard: a tiny table test file doubles as spec.
31. `scripts/check-status-index.sh` — confirm this report passes the pre-commit index hook (it should; report naming follows convention).
32. Cross-check `--repos` in `tq serve`/webui surfaces: any UI hint about the guard?
33. Env var path: `TQ_PROJECTS_DIR` set to `/` with absolute repos — the bypass covers it; add that exact scenario as a test case.
34. Home-dir variant of #33 (TQ_PROJECTS_DIR=$HOME + absolute repos).
35. Defensive: what if `--repos` has hundreds of entries? `allReposAbsolute` is O(n) string checks — fine, but no cap means a pathological line parses; ignore, just confirm.
36. Review the 2026-09-10 "scan failed" pool-deploy incident: would the bypass have prevented or masked it? Write the conclusion into that report's appendix.
37. Search for other "refuse to start" guards in cmd/tq that could benefit from the same shape analysis (absolute-paths-are-explicit pattern).
38. Consider `--allow-risky-projects-dir` explicit opt-out flag instead of shape inference — more discoverable, more explicit; weigh against flag bloat.
39. ADR: is a one-page ADR warranted for the guard-bypass decision (precedent: ADR-0007/0011/0012 style)? Probably fold into AGENTS.md instead — decide once.
40. Test naming: `TestAllReposAbsolute` lives in seeds_test.go — if more repos-shape tests accrete, split into repos_shape_test.go.
41. Confirm race detector covers the changed path (parse is pre-goroutine; run `-race` on the package once for completeness — done in full gate? verify ci-local covers cmd/tq with -race).
42. Run `./scripts/ci-local.sh` fully before next push (I ran targeted gates only).
43. nix build after go.mod-untouched change should be unaffected — confirm vendorHash didn't drift (it shouldn't have; nothing touched go.mod).
44. Sweep for TODO items referencing "02:00 f16" to ensure no sibling item duplicates this one.
45. Check whether `tq harvest --repos` also consults projects dir and needs the same escape.
46. Docs: mention the bypass in `docs/status` close-out conventions so future reviewers know the guard isn't absolute.
47. Add the exact commit SHA + item ID cross-reference to TODO_LIST? Convention says checkbox only — confirm no footer is expected in the file.
48. LSP: report/ignore templ phantoms remain untouched — no action, just noted.
49. Consider `errors.Join`-style aggregated guard reporting when BOTH projects-dir risk AND mixed repos exist (single clear refusal).
50. Revisit (e).2 in a week: if no third caller needs the bypass, close the symmetry question with a doc line, not code.

## g) Questions I can NOT figure out myself

1. **Symmetry intent:** was the 02:00 f16 finding deliberately scoped to agent-pool only (because the systemd pool is the sole caller hit by the failing default resolution), or should `tq worker --agents` and `tq audit` get the identical bypass? Owner call: I did not want to widen the blast radius uninvited.
2. **Guard philosophy:** should the risky-projects-dir refusal become an explicit `--allow-risky-projects-dir` opt-out rather than inferred from the shape of `--repos`? Inference is implicit and undocumented in `--help`; a flag is discoverable but adds surface. Which does the project want long-term?
3. **CHANGELOG policy:** does a refusal-behavior relaxation count as CHANGELOG-worthy for a v0.x CLI, or is CHANGELOG reserved for releases (in which case the line lands with the next version cut via docs/release/RELEASE.md)?

---
*TQ_RESULT for the work commit (9804683) is recorded by the queue; this report closes the loop for task 000001a08e71df95f81bb4c091cf4b28073b.*
