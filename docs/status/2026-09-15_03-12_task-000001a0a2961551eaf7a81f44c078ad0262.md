# Status — task 000001a0a2961551 (review-pipeline hardening, lap 2, verification-only)

**When:** 2026-09-15 03:12 · **Session:** agent dispatch on this ID (2nd lap) · **Verdict:** DONE-ON-ARRIVAL NO-OP

Work item: "Review-pipeline hardening: review findings must anchor commit sha + quoted anchor
text; queue-side re-anchoring pre-flight for cited line ranges; disposition rule for
rejected/dangling commits; dual-footer convention."

## a) FULLY DONE

- The work item itself: shipped by the concurrent session in **97070c1** (feat(review): anchor
  findings to commit sha + verbatim text with re-anchor pre-flight) and its close-out **d3b79da**.
  `ReviewFinding` carries verbatim `anchor` (bare positions like "lines 12-18" / "x.go:34" /
  "L40" rejected at parse time) + `commit_sha` (backfilled from the review payload); fix prompts
  carry the dangling-commit disposition (`git cat-file -e` first) and the single-footer ruling.
  TODO_LIST.md row 79 is `[x]` with a dated DONE annotation.
- This lap's verification, cited: `go build ./...` green (GOEXPERIMENT=jsonv2);
  `internal/review` module tests `ok` (2.2s lap includes executor: `ok`); working tree clean at
  d3b79da before my session.
- The 03:10 report from the first lap (same ID) is written and indexed — I did not duplicate its
  content, only re-verified its claims at HEAD.

## b) PARTIALLY DONE

- Nothing partial from this lap: zero deliverable changes were needed or made.
- Standing partials noticed in passing (pre-existing, not mine to fix here): §d items in the
  prior reports — no mint-time `[x]`-check gate, so DONE items keep getting re-dispatched
  (this ID is now a paid 2-lap instance of that loop, after at least five other same-day
  instances).

## c) NOT STARTED

- Mint-side gate that refuses to mint/claim an already-checked TODO row (the re-dispatch
  loop's systemic fix — flagged in ≥6 reports today, still unbuilt).
- `reviews.sh`-style e2e smoke for the review pipeline (review → fix → verdict) — unit +
  sweep tests exist; no hermetic end-to-end script like the other smokes.

## d) TOTALLY FUCKED UP (honest list, this lap)

- **Second paid lap on a DONE item.** My session arrived after the item was already shipped,
  checked off, reported, and indexed — pure spend. Root cause is the queue/mint side, but my
  lap still contributed.
- **My check ordering was wrong.** I ran the first verify command before grepping TODO_LIST —
  had the row not been `[x]` I'd have started redundant implementation. The
  session-start/done-check ritual should run FIRST. First-command ordering fixed mid-session;
  flagging so the pattern is explicit.
- Nothing broken: no edits were made, so no build/test/type risk was introduced (cited: tree
  clean at HEAD, gates run green this session).

## e) WHAT WE SHOULD IMPROVE

1. Mint-time DONE check (unchecked-box gate at harvest/mint) — the single fix that would have
   saved every lap in §d today, mine included.
2. Dispatcher should pass the dedup-key → row-state through so agents fail fast instead of
   verifying after the fact.
3. Done-on-arrival reports are converging on a stable shape — a template would cut their cost
   further.
4. My own process: grep TODO_LIST state before any verification run.

## f) WHAT TO GET DONE NEXT (up to 50; honest tail, no filler — items 1-8 from this task's
orbit, 9+ are standing backlogs already tracked elsewhere)

1. Mint-time `[x]` check: harvester/refill refuses rows already checked (systemic re-dispatch fix).
2. Hermetic `reviews.sh` e2e smoke (stub reviewer emits valid verdict via `tq verdict`).
3. Extend anchor pre-flight coverage: dlqfix + cqa bridge payloads that quote findings.
4. Deciding-ruling: what happens when re-anchoring SUCCEEDS but content changed semantically
   (quoted text matches, sha dangling) — re-file vs auto-dismiss.
5. Render the anchor + sha in the webui task detail (findings are data now; the UI predates them).
6. Backfill-strictness ruling: should a reviewer-supplied conflicting sha be rejected rather
   than backfilled (today backfill only fires on omission).
7. Ledger the two-lap spend on this ID in the re-dispatch count for the mint-gate case.
8. Close-out convention: the 97070c1 close-out already documents this item — later laps should
   link, not restate (done here).
9-50. Standing, already-tracked: 2026-09-18 rate-limit 7-day re-check + owner DLQ triage of the
   8 pre-flip dead; vendorHash runner-only drift diagnosis; status-index daemon-hole (amend
   maneuver automation); lint-baseline shrink campaign; facade-parity postgres env job;
   TODO — the big rows all live in TODO_LIST.md and ROADMAP.md; do not duplicate here.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Re-dispatch sanction:** when the queue re-mints an already-DONE row (as happened here and
   ≥5 times today), should the lap agent answer with a no-op report, a cancel request, or
   silence? Who owns making the mint gate — pool code or harvest admission?
2. **Anchor semantics ruling:** if a `request_changes` finding's sha dangles but the quoted
   anchor text still exists at HEAD, is the finding still valid (re-anchor and fix) or expired
   (the change it targeted may have been reverted deliberately)?
3. **Lap-report index policy:** second-lap same-ID reports — new index row (as I did here) or
   append a lap note to the first lap's row?
