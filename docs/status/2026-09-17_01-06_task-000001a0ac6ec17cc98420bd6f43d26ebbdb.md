# Verify-Only Re-Dispatch Close-Out — claims-with-citations convention (row 100)

**Date**: 2026-09-17 01:06
**Task**: `000001a0ac6ec17cc98420bd6f43d26ebbdb`
**Verdict**: DONE — verify-only close-out, ZERO code/doc edits

## What this dispatch was

Third dispatch of the row-100 work item ("AGENTS.md
claims-with-citations convention: every filter-scope/behavior claim in
reports and handoffs carries file:line or a test name"). The first
dispatch (00-59) shipped it; the second (01-03) verified it; this window
ran the turn-1 ritual first, found the DONE state again, and stopped.

## Verification (cited, per the convention this task created)

- Convention live in `AGENTS.md` Conventions section: "Claims carry
  citations (2026-09-12)… Filter-scope/behavior claims… additionally
  cite file:line or a pinning test name in reports and handoffs",
  motivating case cited to docs/status/2026-09-12_01-47_per-project-ui-question.md
  (path exists — cited by grep hit, not a fresh line-number read this
  window).
- TODO row `[x]` at `TODO_LIST.md:100`.
- Prior close-outs on disk: docs/status/2026-09-17_00-59_task-000001a0ac6ec17cc98420bd6f43d26ebbdb.md
  (index row README.md:44) and docs/status/2026-09-17_01-03_task-000001a0ac6ec17cc98420bd6f43d26ebbdb.md
  (index row README.md:49).
- Landed commit chain at HEAD `be888de` (`docs: verify-only close-out for
  re-dispatched row-100 claims-citations task`), which carries the
  footer per `git log`; `git status` clean at turn 1.

## a) FULLY DONE

- The work item itself (first dispatch): convention written into
  AGENTS.md, TODO row ticked, report + index row landed, footer-bearing
  commit chain in history.
- This window: ritual → verify → stop, with citations above. Report +
  index row only; no code, no AGENTS.md edit, no TODO edit.

## b) PARTIALLY DONE

- Nothing for this task. Everything the contract asks (mark done,
  commit with footer, no push) was already true on arrival.

## c) NOT STARTED

- No new work discovered in passing; nothing appended to TODO_LIST
  (nothing beyond the queue-side short-circuit already carried by the
  prior reports' §f items).

## d) TOTALLY FUCKED UP

- **The re-dispatch itself is the process failure** (N+4 recurrence of
  this genre across the evening; this task ID alone re-fired 3x). The
  only structural fix remains the queue-side dedup-key → COMPLETED
  short-circuit requested in every prior close-out since 00-22 — still
  unimplemented and unanswered.
- CONTRIBUTING.md: not read at turn 1 (the documented N+5 recurrence of
  that miss). The ritual grep WAS first this time, so no duplicate work
  was done — but the read-skip stands, recorded honestly.
- Report-cost observation (carried from 01-03 §d): verify-only
  close-outs of already-done rows now dominate this loop's spend. A
  fixed short-form verdict template would cut this window's cost by
  roughly half and lose nothing.

## e) WHAT WE SHOULD IMPROVE

1. Mint-time/queue-side done-check (dedup key of a COMPLETED task →
   refuse re-enqueue or return the stored verdict) — retires this whole
   report genre.
2. Turn-1 ritual: actually READ CONTRIBUTING.md at turn 1, every
   window, no exceptions.
3. Owner-ruled short-form verdict format for verify-only re-dispatches.

## f) NEXT (deltas only — prior reports' items NOT duplicated)

1. Authorize + implement the queue-side dedup→COMPLETED short-circuit
   (carried since 00-22 §f1, now N+4; it is the ONLY item that matters
   for this loop).
2. Mint-time interim guard: `tq enqueue` warns when the dedup key
   matches a COMPLETED task (cheap, no fact-model change).
3. Re-fire-rate baseline: one SQL query over the journal counting
   re-dispatched task IDs per day, to size the waste with numbers.
4. Short-form verify-only verdict template (owner ruling needed).
5. CONTRIBUTING.md read as a hard, checked step in the turn-1 ritual
   (grep for it, read it, note it in the close-out).

## g) QUESTIONS (carried unanswered from prior close-outs)

1. Do you authorize the queue-side dedup-key→COMPLETED short-circuit?
2. May verify-only re-dispatches use a fixed short-form verdict instead
   of the full a)–g) close-out?
3. Should the auto-commit daemon hold footer-less commits open a few
   seconds to let an in-flight explicit commit win (the recurring
   footer-on-daemon-commit wart)?
