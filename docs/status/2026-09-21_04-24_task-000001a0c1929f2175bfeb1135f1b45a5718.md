# Task 000001a0c1929f2175bfeb1135f1b45a5718 — full a)-g) close-out (second window, verify→cite→stop repeat dispatch)

Window: 2026-09-21 ~04:10–04:24 CEST · HEAD at dispatch and at every work-turn gate run: `aaeae32`; HEAD at close-out battery: `504f651` (this window's own footer commit; delta since the work battery is docs-only by construction). Tree clean at start; clean again between incidents (§d1).

**Verdict up front: REPEAT DISPATCH, handled as verify → cite → stop.** The item (TODO row 163, Priority-system follow-ups: "`tq show` + webui detail: render derived session tokens/cost for status and prioritize tasks…") was implemented and `[x]` before my first tool call — the rendering landed in `dd920ac` (03:37, per the 04-07 report's record) and the first window on this ID (04-07) already wrote the canonical close-out. This window re-verified every implementation claim first-hand at file:line, re-executed the battery at the NEW head `aaeae32` (04-07's battery ran at `4b30826`), annotated the row, filed the three 04-07 §f items that had never become TODO rows, and wrote a compact second-window report (`2026-09-21_04-17_…`). THIS report is the owner-requested full a)-g) close-out of that window.

## a) FULLY DONE

All claims re-read at file:line AND re-executed by THIS window:

1. **`tq show` half of the item**: `resultDetail` decodes the completion fact into the typed result per task type — status case cmd/tq/main.go:2266-2269, prioritize case :2270-2273 (agent :2258-2261, review :2262-2265; function at :2250). The show payload marshals `Result any` wholesale (cmd/tq/main.go:1949-1956), so the `Session*` fields surface through the shared json tags: `internal/executor/status.go:84-87`, `internal/executor/prioritize.go:93-96` — same keys as `AgentResult` (result.go:35-38) and `ReviewResult` (review.go:113-116), the key convention the budget projection parses.
2. **webui half of the item**: `statusReportCard` usage line internal/webui/fragments.templ:706-708; `prioritizeResultCard` (verdict count + tokens/cost + log path) with usage at :722-724; both wired on the detail page at fragments.templ:649-654; shared helpers `sessionUsageEmpty`/`sessionUsageText` internal/webui/components.go:98-113; usage-less results render no line (render-nothing gate).
3. **Both halves are pinned by tests, and this window EXECUTED the pins fresh** (not just read them): `TestResultUsageRendersOnDetailPage` (internal/webui/webui_test.go:624) and `TestResultDetailDecodesTypedResults` (cmd/tq/main_test.go:352).
4. **Work-turn battery at `aaeae32`, all rc=0, file+rc capture (no PIPESTATUS)**: root build rc=0 + vet rc=0 under `GOEXPERIMENT=jsonv2`; `./scripts/check-doc-refs.sh` rc=0 ("doc refs ok"); internal/webui targeted pin rc=0 + FULL suite `ok 10.094s` (GOWORK=off in-module); `./scripts/test-cmd-tq.sh` ok 8.126s (includes the decode pin). Expensive tier (race suite, nix, ci-local, smokes) consciously skipped per the verify-window inheritance clause: zero source delta since 04-07's full battery — every commit between is docs-only.
5. **Close-out battery at `504f651`** (this report prep): build rc=0, vet rc=0, doc-refs rc=0; `./scripts/check-todo-list.sh` rc=0 (validating the three backlog rows that rode a daemon commit past the pre-commit hook — §d2).
6. **TODO row 163 re-verified annotation** appended in place (RE-VERIFIED 2026-09-21 04:17 … HEAD aaeae32, zero source delta, battery list). Content is on master (rode daemon `d9c3a0a`, §d1); attribution resolves via footer commits `aaeae32` (first window) + `504f651` (this window).
7. **Backlog hygiene**: the 04-07 report's §f1/f2-f3/f4 items existed NOWHERE in TODO_LIST.md — appended as three unchecked rows in the Priority-system follow-ups section: (a) e2e wire pin for derived usage in `tq show` JSON (the wholesale-marshal step is unpinned), (b) SessionMessageCount recorded-but-never-rendered + `sessionUsageEmpty` blind spot decision, (c) CHANGELOG/FEATURES entries check for the rendering work. Rows 164/165 were already tracked and were deliberately NOT re-filed (no mint-duplicates disease).

## b) PARTIALLY DONE

- Nothing of the ITEM is partial — it shipped in the first window and verifies green.
- This window's stop artifact was initially partial in attribution: two of the three content pieces (row annotation; report + backlog rows) rode daemon commits before the footer commit landed, so the footer commit carried only the index row. Attribution still resolves (footer-bearing commits exist for the ID), and no history rewrite was performed, but the window's OWN declared goal "one add+commit chain carrying the stop artifact" was met only for the index row. Healed in-window the only way the house allows: content on master + footer commit + this report; the fold CLASS stays open (§g1).

## c) NOT STARTED

- Nothing within the item's scope. Remaining usage-surface work is fully tracked: row 164 (PrioritizeResult-vs-AgentResult json-key pin), row 165 (score-cache TTL), this window's three new rows, plus the carried owner rulings (§g). No scope creep was committed.

## d) TOTALLY FUCKED UP

1. **The daemon fold hit TWICE in one window — now at sub-minute latency, against a window that had read the doctrine.** Fold 1: the row-163 annotation was swept into footer-less `d9c3a0a` in the seconds between my edit tool call and my add+commit chain (the chain correctly refused: nothing to commit). Fold 2: the compact report + three backlog rows were swept into `5124337` before my footer commit, which therefore carried only the README index row. The "commit immediately after the LAST write" doctrine (codified after at least four prior fold hits) is NECESSARY but PROVEN NOT SUFFICIENT: the daemon's sweep latency is shorter than one tool-call round trip. The remedy I did NOT take despite it being filed in the family's own reports (01-11 §f "heredoc+commit single-invocation option", 00-27 msg-filter precedent): performing the file writes and the commit inside ONE bash invocation so no round trip separates write from commit. For markdown (not Go source — the heredoc ban is Go-specific) that is fully available and I knew it. That is the window's single biggest self-inflicted loss: I turned a codified lesson into a footnote instead of a procedure.
2. **`tq show <task-id>` of the queue record was skipped again** — the 04-07 §d2c miss repeated verbatim. Read-only and safe under the inherited `TQ_DB`; the "why am I here" answer came from the report grep, not the queue. The ritual probe order in 04-07 §e2 listed it fourth of four; I executed two of four.
3. **Item-text grep came second.** session-start.sh surfaced the prior report by ID (correct, first action), but the cheapest DONE signal — `rg <item text> TODO_LIST.md` — ran only after the report was read. The 04-07 §d3 ask, half-adopted.
4. **My three new TODO rows bypassed check-todo-list.sh**: they rode daemon commit `5124337`, and the daemon bypasses pre-commit hooks (the KNOWN hole AGENTS.md documents for status indexing). I ran check-status-index.sh post-commit but initially forgot its TODO sibling — repaired at 04:24 (rc=0) and honestly, this was luck-of-recall, not procedure: the habit should be "any content that rode a daemon commit gets its skipped hook run manually in the same session".
5. **First footer-commit attempt was a no-op error** (`nothing to commit` after fold 1) — correct refusal by git, but it cost a round trip and momentarily left the window with zero artifacts while the compact report was still unwritten; the stop-artifact shape (report + index + rows) should have been assembled BEFORE the first commit attempt, not discovered to be missing mid-chain.

Fairness record — what this window did NOT fuck up: turn-1 ritual complete (session-start.sh first action, whole-repo git log/status/stash, CONTRIBUTING.md via the script, prior-report grep by ID BEFORE any edit); zero source edits; every prior claim re-read AND re-executed before the DONE verdict; battery captured to files with direct `$?` (no PIPESTATUS); no duplicate rows filed; expensive tier consciously skipped and disclosed, not silently omitted; no history rewrite under the live daemon; both no-op/fold incidents were disclosed in the same session they happened, not discovered later.

## e) WHAT WE SHOULD IMPROVE

1. **Make the fold remedy a PROCEDURE, not a principle.** Concretely, for every future window: file writes AND `git add` AND `git commit` in ONE bash invocation (heredoc/`cat >>` for markdown; the Go-source heredoc ban is untouched). The 01-11 report filed this option; nobody adopted it because it reads like an optimization — it is actually the only fold-proof shape at current daemon latency. This window is the datapoint that settles it: two folds, both inside single-digit-second gaps.
2. **"Daemon rode it" → run the skipped hooks.** Any content that landed via a daemon commit should get check-todo-list.sh / check-status-index.sh / check-doc-refs.sh manually in the same session (whichever apply). One line of procedure, closes the known hook-bypass hole at the individual-window level while the structural fix (§g1) pends.
3. **Assemble the full stop artifact BEFORE the first commit attempt.** Report → index row → row annotation → backlog rows → ONE chain. This window committed (or tried to) three times where one would do; each extra attempt is another fold window.
4. **Second windows on a reported ID need less paper, executed earlier.** With a canonical report on file, the fastest correct shape is: item-text grep (command 1) → prior-report grep (command 2) → `tq show <id>` (command 3) → re-cite at file:line → battery → single-chain stop artifact. This window's ordering made the verification rigorous but the discovery ~2× slower than the grep order implies.
5. **Keep the compact second-window template** (04-17 report): fresh battery + annotation + pointer to the canonical report. The owner-requested full a)-g) remains the exception, not the default, for repeat dispatches — else the report floor grows to meet the fold rate.

## f) Things we should get done next

Honest N: 4 new + carried pointers — the family's threads are nearly all tracked; padding to 50 refused.

New, discovered in THIS window:

1. **Codify the single-invocation write+commit chain** (heredoc+git add+git commit in one bash call) as the REQUIRED shape for stop artifacts and one-liner edits in agent windows; it is the only fold-proof pattern at sub-minute daemon sweep latency (evidence: this window, two folds, §d1; option first filed 01-11 §f). Agent-executable (an AGENTS.md Operational-contracts bullet).
2. **Manual re-run of hook-skipped checks for daemon-riding content**: after ANY daemon commit carrying your edits, run the applicable gates (check-todo-list.sh, check-status-index.sh, check-doc-refs.sh) before the session ends (this window's §d4 repair; agent-executable).
3. **`sessionUsageEmpty` message-count blind spot + `SessionMessageCount` rendered nowhere** — FILED this window as a TODO row (04-07 §f2/f3 finally became actionable); listed here for visibility, work belongs to that row, not a re-dispatch.
4. **E2e `tq show` JSON usage wire pin + CHANGELOG/FEATURES check for the rendering work** — FILED this window (04-07 §f1/f4); same pointer rule.

Carried by pointer (tracked rows / prior rulings — NOT re-filed):

5. Row 164: PrioritizeResult usage json-key pin — plus 04-07 §f10's note to reconcile it against the budget-side drift pin so no duplicate pin ships.
6. Row 165: score-cache TTL/eviction.
7. Daemon fold REMEDY (sweep-guard/lockfile/commit-window) — the structural fix behind §e1's procedure; most-repeated §g ask in the family (00-27/01-05/01-11/01-45/01-49/04-07, now +2 datapoints).
8. Re-dispatch suppression at mint time (consult TODO row state + prior reports per ID) — 04-07 §f6 POLICY item; this window is another datapoint (two windows on a row ticked ~10 and ~70 minutes earlier).
9. gopls cmd/tq phantom diagnostics treatment (04-07 §f5/§g3).
10. `tq show --commits` "MISSING FOOTER" wording for legit no-op ids (04-07 §f21) — relevant to this window's own record: the CODE has no footer-bearing commit on this ID by design; only docs commits do.
11. Budget token consumption of the non-agent result types + cap-unit ruling (09-52 §f1 lineage, row 160 family).
12. internal/e2e runtime vs the 180s gate cap (tracked row; 02-53/03-17).
13. owner-only `.tq-verify` vendor-scoped gofmt fix + dead-letter forensic sweep (03-17 §f; BLOCKED rows).
14. Score-cache / prioritize spend: `--prioritize` still default OFF pending the one-batch cost measurement (tracked row, T28).

## g) Questions I can NOT figure out myself

1. **Daemon fold ruling, sharpened by this window's data:** sweep latency is now demonstrably below one tool-call round trip (two folds in one window, both in single-digit-second gaps). Do you want (a) the structural remedy (sweep-guard/lockfile/commit-window script) prioritized, or (b) the single-invocation write+commit chain codified as window procedure (§e1) and the folds accepted as cosmetic (footer commit still lands, content still on master)? (b) costs nothing today; (a) fixes the CLASS. This is the most-repeated ask across the family and I can only keep documenting hits.
2. **Re-dispatch policy for `[x]` rows:** is periodic re-verification of DONE rows intentional spend, or should mint-time suppression (skip/flag/`verify-only` marker when the row is already ticked) land? Three task IDs on 09-21 alone show the treadmill (this ID: 2 windows ~40min apart; the f3/f4 IDs: 4+ windows each). If intentional: what cadence, and should re-dispatches carry a marker so windows skip discovery and go straight to the §e4 shape?
3. **Footer semantics for zero-source-delta windows** (carried unanswered from 04-07 §g2): re-dispatch windows mint docs-only footer commits, so the queue's code-level cross-reference will report no code commit for the ID — the actual implementation footer lives on the FIRST window's commits (`dd920ac` lineage here). Acceptable noise, or should re-verify annotations cite the original implementation commit so `git log --grep` chains to real work?

---

Battery evidence index (all run by this window, outputs in /tmp logs): work turn at `aaeae32` — build rc=0, vet rc=0 (GOEXPERIMENT=jsonv2), check-doc-refs rc=0, webui targeted pin rc=0, webui FULL suite ok 10.094s, cmd/tq gate ok 8.126s. Close-out at `504f651` — build rc=0, vet rc=0, check-doc-refs rc=0, check-todo-list rc=0 (04:24). check-status-index rc=0 (04:21, post-footer-commit). No ci-local/nix/race/smokes: zero source delta, inheritance clause per the verify-window convention, disclosed not silent.
