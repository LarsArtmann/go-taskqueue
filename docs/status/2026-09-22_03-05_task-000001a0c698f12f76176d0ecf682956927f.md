# Close-out: AGENTS.md identity-blind one-liner on the smoke list (row 95)

- **Task**: 000001a0c698f12f76176d0ecf682956927f
- **Window**: 2026-09-22 ~02:55–03:00 CEST (work) + 03:05 (this report)
- **TODO row**: 95 — "AGENTS.md one-liner near the smoke list: the release-gates
  smoke runs identity-blind via ci-local (`GIT_CONFIG_GLOBAL=/dev/null` +
  `user.useConfigOnly`) — so nobody 'simplifies' it away (03-46 §f5)" — now `[x]`
- **Lineage**: 03-46 §f5 minted the row; carried as top §f item through the
  02-30/02-33/02-41/02-47 windows on sibling task 000001a0c6820ca0 (row 94);
  this window executed it. No prior window exists for THIS task id
  (`rg -l 000001a0c698f12f docs/status/` empty at turn 1) — no duplicate dispatch.

**Verdict: DONE.** Deliverable landed, battery green, footer commit in place.

## a) FULLY DONE

1. **Deliverable** (AGENTS.md:98): the release-gates smoke-list entry now reads
   "…positive + negative; runs IDENTITY-BLIND via ci-local (GIT_CONFIG_GLOBAL=/dev/null
   + user.useConfigOnly — bare /dev/null is INERT, this host autodetects GECOS identity)
   and self-tests the property with a stripped-commit fixture — never 'simplify' the
   sanitizer or the -c user.* flags away". All three load-bearing facts from the
   source windows are in the line: the exact env (ci-local.sh:265-267), the
   inertness caveat that killed the first /dev/null-only attempt (03-46 §f5),
   and the self-test fixture pointer (release-gates.sh:42-57, row 94's work).
2. **TODO_LIST.md:95** marked `[x]` with a "— DONE:" annotation in the exact
   sibling format of rows 90/94 (the two other identity-blindness rows).
3. **Mechanism verified first-hand before writing the claim**: ci-local.sh:260-267
   (the sanitizer env wrapping the smoke invocation) and release-gates.sh:42-57
   (the stripped-commit self-test asserting "identity unknown") both read and
   matched the row's description — claims cite what the code actually does.
4. **Verification battery, each rc captured directly** (PIPESTATUS rule):
   root `go build` rc=0; root `go vet` rc=0 (both GOEXPERIMENT=jsonv2 +
   GOTOOLCHAIN=auto); `check-todo-list.sh` rc=0; `check-doc-refs.sh` rc=0;
   release-gates smoke under the EXACT ci-local sanitizer env rc=0 with 12/12
   ok lines including "ok fixture: stripped -c user.* commit fails (identity
   unknown)" — the identity-blind property proven green at HEAD with the doc
   change in place.
5. **Attribution handled per repo rails**: the auto-commit daemon folded both
   edits into footer-less 4426d7c, which ALSO carried an unrelated concurrent
   CHANGELOG hunk (the CI toolchain window's entries — not authored here).
   Handling: no history rewrite (policy), no amend (would have stamped this
   task's footer on the foreign hunk), instead empty footer-marker commit
   575e0c6 directly adjacent, whose message documents the fold, names the
   folded SHA, and explicitly excludes the CHANGELOG work from this task's
   claim. Commit-msg hook satisfied (exactly one well-formed footer).
6. **Session ritual**: session-start.sh run with the task id; CONTRIBUTING.md
   read (dev-setup section); git log/status/stash checked over the whole repo
   at turn 1.

## b) PARTIALLY DONE

- Nothing. The row's ask was one one-liner; it shipped whole. The only
  judgment call inside the deliverable was appending the self-test pointer to
  the line (the row text didn't mention the fixture — row 94 landed it later);
  including it makes the line more rot-resistant, not less complete.

## c) NOT STARTED (deliberate scope-keeping, all left in the backlog)

1. **Sanitizer single-ownership** (02-47 §g3, owner ruling pending): folding
   GIT_CONFIG_GLOBAL/useConfigOnly INTO release-gates.sh so the smoke is
   identity-blind standalone. Today the blindness lives in ci-local's env
   (ci-local.sh:265-267) and the self-test merely re-asserts it under that env.
2. **Known Issues entry for identity-blindness** (02-30 §f item 31): skipped —
   row 95's smoke-list one-liner already covers the don't-simplify goal, and a
   Known Issues paragraph would partially duplicate it. Ruling pending (§g2).
3. **CHANGELOG entry** for this docs-only change: skipped by judgment; the
   policy question is parked in §g3.
4. Row 96 (03-46 §f9) and row 97 (04-31 §f14): sibling backlog rows, untouched.
5. The concurrent agent's in-flight test edits (cmd/tq/main_test.go,
   cmd/tq/show_test.go, folding through daemon commits ee0cde5→6d31195):
   observed, never touched, per the build-on-never-revert rule.

## d) TOTALLY FUCKED UP

1. **First TODO_LIST.md edit rejected** — "you must read the file before
   editing it": I had read the exact target line via `sed`, not the View tool,
   so the tool contract failed and cost one wasted round-trip. The AGENTS.md
   edit (View-read) went through first try. Read-before-edit means the View
   tool, not "I've seen the bytes".
2. **session-start.sh rc swallowed by my own pipe**: invoked as
   `./scripts/session-start.sh <id> | tail -20` — the exact PIPESTATUS hazard
   the repo codifies, applied to the ritual itself. `$?` was tail's. The
   completion line was present so the gates almost certainly ran, but "almost
   certainly" is not rc=0; worse, `tail -20` may have cut the ritual's
   prior-windows output, which is why I re-ran the duplicate grep manually.
   Compensation worked; discipline failed.
3. **`tail -2` over four log files died** with "option used in invalid
   context -- 2" in this session shell, discarding the intended log tails.
   No blind spot (each gate's rc was already captured directly into the echo),
   but the verification OUTPUT was never re-examined; I judged gates on rc
   alone. Same session-shell-hazard family as PIPESTATUS; candidate for a
   one-line AGENTS.md note (§f9).

## e) WHAT WE SHOULD IMPROVE

1. **Rc-capture from command #1, not just the gates** — the battery commands
   got the redirect+`rc=$?` treatment; the ritual and log-tail commands didn't.
   The rule is one habit, not a gate-time special case.
2. **Daemon-fold handling is ad hoc** — the marker-commit maneuver worked but
   was invented mid-window (message boilerplate, folded-SHA citation, explicit
   foreign-hunk disclaimer). A tiny script would make it mechanical and
   consistent (§f1). Related: row 96 covers the QUEUE-RENDERING side of the
   same fold; nothing covers the AUTHORING side.
3. **One-liners encoding behavioral contracts have no rot-guard** — the smoke
   self-test guards the PROPERTY, but the AGENTS.md line's claims (env names,
   self-test existence) could silently rot if someone moves the fixture or
   renames the env. Low risk today; only worth guarding if the line grows
   more load-bearing (§f8).
4. **View-before-edit should be reflexive for every file, not just Go files**
   — d1 was pure tool-contract friction, but the same laziness on a file with
   real ambiguity would have produced a wrong edit, not just a rejection.

## f) Up to 50 things we should get done next (prioritized; this window's items first, then carried)

1. Mechanicalize the daemon-fold footer marker: `scripts/fold-marker.sh <task-id> <folded-sha> <subject>` creating the empty footer commit with standard boilerplate (fold disclosure + foreign-hunk disclaimer), so windows stop hand-inventing the message (found this window).
2. session-start.sh: surface a one-line prior-windows summary (count + any DONE-row hits) INSIDE the final completion line, so tail-truncated invocations still surface duplicates (found this window, d2).
3. Row 96 (03-46 §f9): `tq show --commits` renders footer-less daemon commits ADJACENT to a footer-bearing commit as "folded here" — 4426d7c/575e0c6 is the newest live render case.
4. Sanitizer single-ownership ruling + fold: make release-gates.sh self-sanitizing (set the env inside the script, ci-local keeps belt-and-suspenders), closing 02-47 §g3.
5. Vendor-gofmt verify-gate defect (owner-only): scope the gofmt stage to tracked files in `.tq-verify` + the mint template — still the deterministic dead-letter machine (5 dead since 09-20; AGENTS.md documents it; three sibling windows re-flagged it).
6. Mint-time dedup for DONE-row re-dispatches (02-47 §f): row 95 sat open while FOUR windows dispatched its DONE sibling — the harvest-side already-done skip (02-06 section) plus mint-time row comparison would kill the class.
7. Known Issues entry for identity-blindness (02-30 §f31) — pending §g2 ruling.
8. Session-shell hazard doc: one AGENTS.md line noting multi-file `tail -N` also misbehaves in the session shell ("option used in invalid context"), beside the PIPESTATUS bullet (d3).
9. CHANGELOG policy for docs-only changes (§g3): yes/no under a Documentation heading; this window skipped, concurrent windows add infra entries.
10. Row 97 (04-31 §f14): bank the gochecknoglobals shrink (root 9→7) in the same regen that resolves the baseline-triage item.
11. internal/e2e under -race is load-marginal vs the 180s stage cap (240s kill observed) — its own tracked row.
12. Carry: session-close opens (trigger automation crush #3146, daemon-commit attribution gap, budget bypass, postgres parity) — AGENTS.md session-close section.
13. Carry: Crush upstream-issue candidate — `crush run --reasoning-effort <typo>` fails with "does not support reasoning effort" instead of listing accepted values (AGENTS.md, verified 2026-09-14).
14. Carry (low value, keep skipped): upstream git docs note that bare GIT_CONFIG_GLOBAL=/dev/null is inert with GECOS autodetection (02-30 §f called it low-value).
15. Carry: row 89 session E2E dogfood proof — BLOCKED: owner-gated API spend.

## g) Questions I cannot figure out myself

1. **Daemon-fold maneuver**: is the adjacent empty footer-marker commit
   (575e0c6 over 4426d7c) the sanctioned workaround until row 96 ships, or do
   you prefer amending the index/content into the daemon commit for
   single-owner windows (the documented amend-maneuver class) and reserve
   markers for mixed-authority folds like this one (foreign CHANGELOG hunk
   present)?
2. **Known Issues coverage**: should the identity-blind run mode ALSO get a
   Known Issues entry (02-30 §f31), or is the smoke-list one-liner the
   intended and sufficient home for the don't-simplify guard?
3. **CHANGELOG for docs-only changes**: enter or not? A docs one-liner feels
   below the bar; the repo's CHANGELOG has no Documentation section convention
   I could find.

## Verification battery (fresh, this window, at 575e0c6)

| Gate | Command | Result |
| ---- | ------- | ------ |
| Build | `GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto go build ./...` | rc=0 |
| Vet | `GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto go vet ./...` | rc=0 |
| TODO gate | `./scripts/check-todo-list.sh` | rc=0 |
| Doc refs | `./scripts/check-doc-refs.sh` | rc=0 |
| Release-gates smoke | `GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=user.useConfigOnly GIT_CONFIG_VALUE_0=true ./scripts/smoke/release-gates.sh` | rc=0, 12/12 ok incl. identity-unknown fixture |

Commits: 4426d7c (daemon fold: AGENTS.md + TODO_LIST.md hunks + foreign
CHANGELOG hunk), 575e0c6 (footer marker, empty by design).
