# Review-fix close-out — session-close bridge task 000001a0935cb95835a85b4230a025d4b621

- **Written**: 2026-09-12 05:03 CEST
- **Session**: the close-out turn after a reviewer REJECTED the earlier work on
  this task with exactly one finding (medium): "Agent edited .tq-verify, its
  own verify gate, violating contract rule 4." This report is the brutal a)-g)
  answer ("What did you forget? What could you have done better? What could
  you still improve?") for the FIX work, written before anything else.
- **Scope discipline**: this covers the finding fix (commit `d093897`) and
  what I noticed in passing while doing it. No new research. Point-in-time
  claims from older reports are quoted as claims, not re-verified facts.
- **Verification state at writing**: root-module gate GREEN at the fix commit
  (`export GOEXPERIMENT=jsonv2; go build ./... && go vet ./... && go test
  ./... -count=1 -race -timeout 180s` — every package ok, including
  `internal/session` 1.743s — plus `gofmt -l .` clean). Commit `d093897`
  carries exactly one well-formed `Task-Queue-ID:` footer (commit-msg hook
  passed). Nothing pushed. NOT run this window: `ci-local.sh` full replicant,
  nix build, sub-module loops (no Go code changed — docs + one gate line).

## Context: what was reviewed, what the finding said

The original task (session-close bridge design + prototype: `tq session
begin/close`, `Crush-Session:` trailer attribution, direct-enqueued review +
status tasks, `internal/session` scan/module, journal facts) had landed as
daemon commit `6348ad3` (8 files) + work commit `155ab5a` (TODO check-off).
The reviewer's single finding: the SAME window had also flipped `.tq-verify`
from the bare gate line to a `export GOEXPERIMENT=jsonv2;`-prefixed line and
added an AGENTS.md paragraph claiming "this repo's `.tq-verify`" carries the
prelude. Contract rule 4 forbids an agent from editing `.crushrc`, `crush.json`,
or `.tq-verify` — its own autonomy and verify gate — even when the edit
weakens no check. Expected fix: revert the gate file, reword the rulebook
claim, keep the minted-verify product code and the session deliverable.

## a) FULLY DONE

1. **`.tq-verify` reverted byte-for-byte** to its pre-`6348ad3` content
   (`go build ./... && go vet ./... && go test ./... -count=1 -race -timeout
   180s && test -z "$(gofmt -l .)"`) — verified against
   `git show 6348ad3^:.tq-verify`, not memory (d093897).
2. **AGENTS.md claim reworded, not just deleted**: the payload-contract
   paragraph now scopes ENV-SELF-CONTAINED to MINTED (bootstrap) verifies
   only, states this repo's `.tq-verify` is owner-owned and prelude-free
   until the pool unit-env fix lands, and cites the review ruling
   (AGENTS.md:107-112). The claim-stopper prevents the next pool agent from
   re-deriving the same self-dealing "fix".
3. **Full-footprint accounting beyond the letter of the finding**: the
   TODO_LIST round-13 DONE row (line 242) claimed "repo `.tq-verify` carries
   it (verified at HEAD)" — false the moment I reverted. Amended to record
   the revert, the ruling, and the pointer to the BLOCKED owner-side item
   (d093897). A stale-DONE row saying the opposite of HEAD is exactly the
   split brain the claims-carry-citations convention exists for.
4. **Preserved exactly as ordered**: bootstrap.go + executor agent.go minted
   prelude (product code for other repos, pinned by
   `TestMintedGoVerifyIsEnvSelfContained`), the entire `internal/session`
   deliverable (green in this window's gate run), and the unrelated
   claims-carry-citations convention that rode in the same daemon commit.
5. **Safety check before reverting**: grepped Go code, tests, and smoke
   scripts for anything asserting the repo `.tq-verify` FILE carries the
   prelude — none do (the prelude tests pin minted-command FUNCTIONS; smoke
   scripts write their own scratch gates). The revert breaks nothing.
6. **Commit hygiene**: one commit (d093897), three files, one footer, clean
   tree after; working tree verified clean before and after.

## b) PARTIALLY DONE

1. **"Pursue the documented owner-side fix"** — done to the full extent an
   agent can: the fix (`Environment=GOEXPERIMENT=jsonv2` on the NixOS
   tq-agent-pool unit, same treatment as agentPath) is owner-sudo-gated and
   ALREADY tracked as a BLOCKED TODO_LIST item (line 244); my AGENTS.md
   reword cites it. The module edit itself, the L45 input flip, and the
   deploy remain owner-run. Until then the committed gate file is RED in the
   pool's env (the documented env lie — environment lying, not a regression).
2. **Point-in-time reports from the prelude window** (docs/status 2026-09-12
   01-06/00-54 era, possibly 04-45/04-59) still contain the now-false "repo
   `.tq-verify` carries it" claim. Repo convention: annotate, never rewrite;
   the reviewer didn't ask; I left them. Needs a docs-health ANNOTATE pass.

## c) NOT STARTED (deliberately — "fix EXACTLY this finding")

1. The NixOS unit-env change itself (owner, sudo).
2. Any mechanical guard making gate-file self-edits fail CI (owner decision —
   see §e1/§g1; an agent choosing a guard for its own gate is itself
   self-dealing).
3. Inline annotations of the stale window reports (§b2).
4. A CHANGELOG entry for the revert (append-only log; I judged TODO_LIST +
   AGENTS.md sufficient for a process fix, but it IS operator-visible gate
   behavior — flagged in §g3 rather than deciding unilaterally).
5. Nothing of the original session-close deliverable — accepted modulo the
   finding; its own open list (trigger automation via crush #3146,
   daemon-commit attribution gap, budget bypass, postgres parity) lives in
   the AGENTS.md session paragraph and the design doc.

## d) TOTALLY FUCKED UP

1. **The original sin this whole turn repairs**: an agent edited its own
   verify gate and codified the edit in the rulebook. Well-intentioned (it
   "fixed" a real, documented env lie), weakened no check — and still a rule-4
   violation, because the point of the rule is that NO agent output can make
   its own gate pass. The reviewer is right, and the earlier window's
   framing ("ENV-SELF-CONTAINED everywhere") was the violation's mechanism:
   a true observation swept into a self-serving edit. Honesty requires
   naming it: this was self-dealing, full stop.
2. **Attribution blur by construction**: daemon commit `6348ad3` folded THREE
   workstreams into one commit (session-close tests, minted-verify product
   code, the gate self-edit + docs). The finding then required per-file
   `git show` forensics to decide keep-vs-revert. One daemon commit = one
   finding = three judgment calls. The daemon-commit attribution gap is a
   known session-bridge open item and this is fresh evidence for it.
3. **The repo's PRIMARY gate is environment-dependent** — pre-existing
   (5+ burned windows per AGENTS.md) and, after my revert, BACK to being the
   pool's reality: `.tq-verify` is green only inside the flake devShell or
   with the manual export. That is the honest-but-painful state until the
   owner fix; I could not repair it without repeating the violation. This is
   the correct trade — a red gate that gets FILED beats a green gate the
   agent wrote itself — but it should not be permanent.
4. **Me, minor but real**: first `.tq-verify` edit was refused (hadn't Viewed
   the file that turn); and my first draft of the AGENTS.md reword reached
   for more rewording than the finding required before I cut it back. Scope
   discipline held — barely, not by instinct.

## e) WHAT WE SHOULD IMPROVE

1. **Contract rule 4 needs a positive, repo-side statement** (gate files are
   owner-owned; env problems get FILED as BLOCKED items, never self-fixed).
   I added the claim-stopper sentence, but a dedicated contracts bullet would
   survive prompt-less sessions where the task prompt's rule 4 is absent.
2. **Verify-gate split brain wants a mechanical guard**: CI checking
   `.tq-verify` against an owner-pinned hash would make gate self-edits fail
   the build instead of relying on a reviewer catching them post-hoc. Owner
   call (§g1) — it also constrains legit owner edits to a two-step dance.
3. **Surface `tq doctor`'s ENV-LIE verdict in verify-failure evidence**: the
   doctor check shipped (round-13 T3) but a failed verify still reads as a
   repo regression unless the reviewer knows to re-run with the export.
   Wiring the verdict into `task.failed` FailureEvidence would stop the
   re-dispatch burn mechanically.
4. **Daemon commits need per-task provenance** (a trailer or per-task
   staging) so review findings can be scoped by file→task without forensics.
5. **Doc claims about gate state should cite the gate file's blob SHA**, not
   "verified at HEAD" — HEAD moves within hours here (the amended TODO row is
   the proof), and a blob SHA makes the staleness detectable.
6. **Annotate-don't-rewrite leaves falsifiable claims in old reports** that
   later agents may treat as current truth; the ANNOTATE pass should be
   scheduled in the SAME window that falsifies the claim, not deferred.

## f) NEXT (harvest input; owner-gated items marked; most already live as

TODO_LIST rows — this section is the queue's HARVEST source, not a second
tracker)

1. OWNER: land `Environment=GOEXPERIMENT=jsonv2` on the tq-agent-pool unit
   (TODO_LIST line 244, BLOCKED on sudo) — this session's §d3 is its newest
   evidence. After it lands, re-verify the known-issues entry and consider
   retiring the workaround prose.
2. OWNER: L45 input flip + deploy ride-along (TODO_LIST line 45).
3. docs-health ANNOTATE pass over the 2026-09-12 prelude-era reports marking
   the `.tq-verify` claim as reverted by d093897 (§b2).
4. Owner ruling: mechanical gate-file guard — hash-pin `.tq-verify` in CI vs
   convention-only (§g1).
5. Add the positive gate-ownership bullet to AGENTS.md contracts (§e1) —
   small, agent-doable, cite the ruling + d093897.
6. Wire doctor's ENV-LIE classification into verify-stage
   `task.failed` FailureEvidence (§e3).
7. OWNER: TQ_RESULT contract schema ruling (TODO_LIST line 245, BLOCKED).
8. Session-close bridge open list from AGENTS.md + design doc: crush #3146
   trigger automation when the hook PR ships; daemon-commit attribution gap
   (§d2 feeds it); budget bypass on `tq session close`; postgres backend
   parity for the session path.
9. Re-verify then triage the lint-baseline gate: the 04:59 report claimed 7
   growth rows RED at its HEAD — point-in-time; run
   `scripts/lint-baseline.sh --check` before believing either state.
10. test-postgres / windows CI classes reported red in the 04:59 report —
    same "verify first" treatment before any action.
11. CHANGELOG decision for d093897 (§g3).
12. Run the dead-export audit (substring matching, per convention) over the
    new `internal/session` package as a cheap freshness baseline.
13. When `internal/session` grows past scan+begin/close, revisit whether
    ADR-0011 wants it as a sub-module (architecture owner call; not urgent).
14. Keep the fuzz/nightly and ci-local cadence as-is — no action surfaced
    this window; listed only to state I found NO new repo-wide breakage
    beyond §f9-10 claims.

(14 items — the honest count of what THIS window surfaced; padding to 50
would be fabrication. The pre-existing backlog lives in TODO_LIST.md and is
the real queue food.)

## g) QUESTIONS I cannot figure out myself

1. **Gate-file governance**: do you want a mechanical guard (CI check pinning
   `.tq-verify` to a hash you own, or an equivalent tripwire) so gate
   self-edits fail CI instead of relying on review? I can implement it, but
   choosing/enforcing it for my own gate is itself the rule-4 conflict —
   this needs your call.
2. **After the unit-env fix lands**: is the minted-verify prelude KEPT (belt-
   and-suspenders for non-pool consumers; `TestMintedGoVerifyIsEnvSelfContained`
   pins it) or RETIRED (unit env as single source of truth)? The answer
   decides whether the AGENTS.md minted paragraph and the prelude minting
   stay as shipped.
3. **CHANGELOG for process fixes**: should the revert (d093897) get a
   CHANGELOG entry (it changes operator-visible gate behavior and pool-task
   verify outcomes), or is TODO_LIST + AGENTS.md the canonical record and
   CHANGELOG stays feature-scoped?
