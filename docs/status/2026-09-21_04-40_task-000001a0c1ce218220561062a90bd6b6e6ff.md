# Task 000001a0c1ce218220561062a90bd6b6e6ff — PrioritizeResult usage-key pin

Full a)-g) close-out, 2026-09-21 04:40 CEST. Work item (TODO_LIST row 164, section
"Priority-system follow-ups"): "Pinning test: PrioritizeResult usage json keys must
equal AgentResult's (session_cost_usd/session_prompt_tokens/session_completion_tokens/
session_message_count) so the sink convention cannot drift (09-52 report f2;
internal/executor prioritize_test.go)".

FIRST window on this ID: turn-1 rg over docs/status/ found no prior reports for
000001a0c1ce, no BLOCKED marker on the row; session-start.sh, `git log -5` +
status + stash run; CONTRIBUTING.md read (74 lines). Dispatch HEAD 4b1ab71, tree
clean.

## a) FULLY DONE

- **The pin test.** `TestPrioritizeResultUsageKeysMatchAgentResult`
  (internal/executor/prioritize_test.go:101-164, file now 164 lines): one table
  over `AgentResult` and `PrioritizeResult`, each marshalled through the REAL
  type (encoding/json/v2), unmarshalled into a map, and pinned to EXACTLY the
  four shared usage keys — `session_cost_usd`, `session_prompt_tokens`,
  `session_completion_tokens`, `session_message_count` — plus
  `PrioritizeResult`'s ever-present non-omitempty `verdicts`. The shared
  `usageKeys` slice is declared once; both subtests consume it, so the canonical
  list cannot fork inside the test.
- **Both directions of drift fail.** A rename in `AgentResult` fails its own
  subtest; a rename in `PrioritizeResult` fails its own; a both-renamed-together
  drift fails BOTH (each type must match the canonical list, not just each
  other). Even an ADDED non-omitempty field that silently joins the marshal
  output fails the exact-set check — the pin forces a conscious update, which
  is the point.
- **Mutation-verified evidence** (the test's failure mode proven, not assumed),
  via `go test -overlay` so the tree was never touched: sed-renamed
  `session_cost_usd`→`usage_cost_usd` in prioritize.go → FAIL with the drifted
  key list; `session_prompt_tokens`→`prompt_tokens` → FAIL;
  `session_cost_usd`→`usage_cost_usd` in result.go (AgentResult side) → FAIL;
  unmutated → PASS. Failure message names the offending keys and why it
  matters ("a rename here breaks the budget projection and tq show").
- **TODO_LIST row 164 checked off** with full citation block (commit efd9348,
  exactly one well-formed footer, commit-msg hook green; check-todo-list rc=0
  after the edit).
- **Battery, all rc=0** (this window, GOEXPERIMENT=jsonv2 throughout):
  gofmt -l on the touched file clean; executor module `GOWORK=off go vet` +
  FULL `go test ./... -race -count=1` ok 10.386s; targeted `-run TestPrioritize
  -v` PASS incl. both sibling contract tests; root `go build ./... && go vet
  ./...`; scripts/check-doc-refs.sh; scripts/check-todo-list.sh.
- **Consciously NOT run, with reasons:** ci-local.sh (delta = one test file +
  two docs; module gate + root battery cover it — same tradeoff the 09-52
  window stated), nix build/flake check (no go.mod/deps/template/css change →
  vendorHash and webui-css unaffected), cmd/tq + webui suites (untouched),
  budget suite (untouched; its own pin re-verified by the executor suite's
  dependency graph only in the trivial sense — no budget code changed), root
  -race (executor is the only code-touched module; its FULL race suite ran).
- **No forbidden files touched** (.crushrc, crush.json, .tq-verify), no push,
  no drive-by refactors, no new backlog rows describing this task's own work.

## b) PARTIALLY DONE

- **The pin covers PrioritizeResult vs AgentResult only — by scope.**
  ReviewResult and StatusResult carry the same four keys
  (review.go:104-118, status.go:78-88) but have NO executor-level
  exact-marshal pin; the only all-four guard is budget_test's
  `TestUsageTodaySumsDerivedSessionUsage` (budget_test.go:144), which pins
  drift SEMANTICALLY (sums drop when a parsed key renames) but tolerates extra
  keys and lives in a different module. A parity row extending the exact-set
  pattern to review_test.go/status_test.go is FILED this window (see §f1) —
  the owner may also rule budget_test sufficient and reject it (§g1).
- **gofmt: file-scoped only.** The repo-wide `gofmt -l .` gate is structurally
  red on this dev host (walks gitignored vendor/, the 03-17 discovery);
  `gofmt -l prioritize_test.go` clean is the honest claim, not a green
  repo-wide gate.

## c) NOT STARTED

Nothing outside the single work item was attempted. Adjacent rows observed in
passing and deliberately left to the queue's gates: score-cache TTL/eviction
(row 165), e2e show-JSON usage wire pin (row 166), SessionMessageCount render
decision (row 167), CHANGELOG/FEATURES usage-rendering check (row 168), v0.3.0
pkg.go.dev spot-check, journal/cqrs facade ruling (BLOCKED owner), owner-only
.tq-verify gofmt-vendor fix + dead-letter forensic sweep (filed by the 03-17
window), e2e -race 180s headroom, mint-time `[x]`-row guard (recurring sibling
ask). None new except §f1.

## d) TOTALLY FUCKED UP

1. **FALSE-GREEN mutation check.** The first mutation run wrote
   /tmp/overlay.json but OMITTED `-overlay=/tmp/overlay.json` from the
   `go test` invocation — the harness reported `ok` on a MUTATED tree. Caught
   within one tool call by reading the output against the expected verdict
   (a rename must fail), then re-run correctly; all three renames failed as
   intended. No artifact had yet cited the bogus pass — the TODO check-off and
   this report were written after the corrected run — but one more step of
   momentum and the check-off would have carried a mutation proof that never
   ran. Same family as the PIPESTATUS hazard: a silently no-op evidence
   harness is worse than none.
2. **View-before-edit skipped twice** (grep/sed reads used as substitutes) —
   two failed `edit` calls (prioritize_test.go, TODO_LIST.md) before View.
   Tooling caught it; zero damage; pure process sloppiness.
3. **Daemon fold of the source change**: prioritize_test.go rode daemon commit
   93372d4 (footerless, hooks bypassed) before my check-off commit landed —
   the established ce935ba/b6b115d shape; attribution rests on the footer
   commit alone (efd9348). Mitigation applied THIS window: report + index row
   - backlog row + commit assembled in ONE bash invocation (the 04-24 §e
     fold-proof shape, adopted for the first time by this window).
4. **ci-local consciously skipped** (see §a) — stated, not hidden; if the
   owner wants the full replicant per window regardless of delta, that is a
   policy question (§g3 adjacent).

## e) WHAT WE SHOULD IMPROVE

- **Verify the verifier, as a step, not an instinct**: before running a
  mutation/overlay harness for evidence, state the expected verdict OUT LOUD
  (in the command description), and treat any `ok` on a should-fail run as a
  HARNESS failure, not a test failure. Would have caught d1 at zero cost.
- **Fold-proof write+commit as default**: this window used the one-invocation
  pattern for the close-out; future windows writing reports + rows should
  start there instead of discovering it in a §e.
- **Lessons candidate (cross-project, crush-config repo — NOT writable
  in-session)**: "A mutation check whose harness silently no-ops returns a
  false green; run the harness against a known-bad input before trusting any
  pass." Belongs in references/lessons.md by owner commit; flagged here
  because this window's d1 is the third member of the verify-the-verifier
  family (PIPESTATUS ×2, overlay ×1).
- **Line-number citations gathered BEFORE drafting** (done this window via
  one grep pass) avoided the cite-then-check inversion several prior windows
  confessed; keep the ordering.

## f) Up to 50 things we should get done next

Honest list; items 2-12 are tracked pointers from this window's reading, not
padding. 50-padding refused.

1. **FILED this window** — Usage-key pin parity: extend the exact-key-set pin
   to ReviewResult + StatusResult (same table pattern,
   prioritize_test.go:101) so all four result types carry an executor-level
   exact-marshal pin, not only budget_test's parse-sum check (new TODO row,
   internal/executor review_test.go/status_test.go).
2. Score-cache TTL/eviction pass (TODO row 165; internal/prioritize).
3. E2e wire pin for derived usage in `tq show` (TODO row 166; cmd/tq via
   scripts/test-cmd-tq.sh).
4. SessionMessageCount rendered-nowhere / sessionUsageEmpty blind-spot
   decision (TODO row 167; internal/webui components.go).
5. CHANGELOG/FEATURES entries check for the 2026-09-21 usage-rendering work
   (TODO row 168; docs-only).
6. v0.3.0 GitHub Release remaining step: pkg.go.dev render spot-check for the
   7 facade modules (persistent 404 = investigate).
7. Ruling: public `journal/cqrs` facade or internal-only (BLOCKED owner).
8. Owner-only: scope the minted gofmt stage to tracked files
   (`gofmt -l $(git ls-files '*.go')`) in `.tq-verify` + mint template — the
   structural vendor/ gate kill (03-17 report).
9. Dead-letter forensic sweep of the gate-killed tasks (agent-executable row
   filed 03-17).
10. e2e -race vs 180s stage-cap headroom (tracked row; load-marginal).
11. Queue-level: mint-time guard consulting TODO row state + prior reports per
    ID to stop DONE-row re-dispatches (recurring sibling §f ask, 04-07).
12. If §g3 rules yes: document the `go test -overlay` mutation-evidence
    pattern in CONTRIBUTING.md (docs-only).

## g) Questions (up to 3)

1. **Pin granularity**: is the executor-level exact-key-set pin wanted for all
   four result types (my §f1 parity row), or is budget_test's parse-sum pin
   the designated all-four guard, keeping PrioritizeResult's executor pin as
   the pattern's only instance? (Both guards have value; I filed the row, but
   duplication without a ruling is how split-brain pins start.)
2. **Unpushed daemon-commit amend ruling** (carried, most-repeated ask): when
   the daemon folds source into a footerless commit BEFORE the footer commit
   lands (93372d4 → efd9348 this window), is the footer-commit-only
   attribution still the accepted shape, or should windows `git commit --amend`
   the UNPUSHED daemon commit to consolidate code + footer into one commit?
   The history-rewrite policy forbids rewriting PUSHED commits and is silent
   on unpushed daemon commits.
3. **Mutation-evidence bar**: should drift-pin tests of this class come with a
   mutation run as standard evidence (via `go test -overlay`, tree-untouched),
   documented in CONTRIBUTING.md — or stay ad-hoc, cited per report when the
   pin is novel? (Costs ~2 tool calls per pin; catches pins that pin nothing,
   which review has caught at least once repo-wide.)
