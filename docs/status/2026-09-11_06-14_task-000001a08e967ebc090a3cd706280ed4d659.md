# Status Report — task 000001a08e967ebc090a3cd706280ed4d659 (third dispatch)

**Date:** 2026-09-11 06:14 CEST
**Work item:** dead-pool detection: alert via the PapDashboard bridge when a pool's scan-failed skip count covers every repo for N consecutive ticks (02:00 f6; cmd/tq agent-pool tick loop + internal/bridge)
**Scope:** this task's work only, plus what was noticed in passing. No unrelated research.

## Summary

**Third dispatch of an already-completed item.** The feature shipped in the first window (impl a3724d8, tests bd40b20, docs close-out 8b5a6be with the Task-Queue-ID footer), was independently verified by the second window (06-06 close-out report, committed 2dba3cc, which itself produced a second verification report committed 467aaf9). This window found the item marked `[x]` in TODO_LIST.md:82, re-verified the gate green for a third time, changed no code, and — before this report — noticed the 06-11 report had landed **unindexed** in `docs/status/README.md` (a repeat of the 05-38 pattern) and added both rows (06-11 + this one) while writing this report. Stale re-dispatch tally: **7** across 2 tasks (4x `…b61fa8c`, 3x here); the dispatcher keeps re-firing completed work.

## a) FULLY DONE

- **The entire work item** (first window, verified 2x since): `tq agent-pool --dead-pool-ticks N` (default 3, `0` = off) counts CONSECUTIVE all-repos scan-failed ticks (coverage = scan-failed skip count == `res.Repos`), fires ONE `alert.triggered` per streak via the bridge's direct `NotifyDeadPool` (stable `agent-pool-dead-pool` aggregate), WARNs even without `--alert-url`, resolves + resets on the first readable-repo tick.
- **Pinned by two test suites:** `TestDeadPoolDetectorStreakLifecycle` (6 subtests, cmd/tq) + `TestDeadPoolAlertTriggerAndResolve` (httptest, papdashboard).
- **Docs closed:** CHANGELOG, FEATURES.md, AGENTS.md, systemd sample, TODO_LIST:82 `[x]`.
- **This dispatch's verification:** session-start ritual (`git log`/`git status`); TODO cross-check; `GOEXPERIMENT=jsonv2 go build ./...` clean; `go test ./cmd/tq -run DeadPool -count=1` pass (all 6 subtests); `go test ./internal/bridge/... -run DeadPool -count=1` pass. Working tree clean before the docs fix below.
- **Drive-by docs hygiene (owner-sanctioned on-sight class):** the 06-11 close-out report landed unindexed; both its README row and this report's row added in this window's commit.

## b) PARTIALLY DONE

- **Ack-based alerting — still unfixed, re-confirmed at the source this window:** `cmd/tq/agentpool.go:413` sets `d.alerted = true` unconditionally BEFORE the notify result is known (`grep`-verified this window, third independent confirmation). A dashboard outage at fire time loses the streak's only alert until resolve + a fresh N-tick streak. Fix is small: notify returns acceptance; set `alerted` only on it. Carried twice already — it should be the very next code change on this file.
- **Everything in the 06-06/06-11 §b lists remains open**, deliberately untouched here (no scope creep on a verification dispatch): untested cmd/tq composition glue, streak not restart-surviving, `--once` making N≥2 unreachable, discovery-failure blindness not counted, watch-ticks advancing the streak faster than `--interval`, live-dashboard E2E never run (two never-posted paths: base bridge and `NotifyDeadPool`).
- **Status-index discipline is now a recurring manual patch** (05-38 → retro-indexed by 05-41; 06-11 → retro-indexed here). The pre-commit hook from `scripts/install-pre-commit.sh` evidently isn't active in this environment for docs-status commits; the loop keeps being closed by the NEXT window instead.

## c) NOT STARTED

- **Production deployment:** the 2026-09-10 blind-pool incident class this feature guards is STILL unprotected — `dead-pool-ticks` unset in production pool.conf and the SystemNix input predates 8b5a6be (owner-run flip + `nix run .#deploy`).
- **The re-dispatch root cause itself:** 7 stale re-fires across 2 tasks, no dispatcher-side fix ticketed beyond carrying item #24.

## d) TOTALLY FUCKED UP (this window's honest ledger)

- **Nothing broken — but also nothing advanced.** Three windows have now produced zero code changes on an item whose one real flaw (ack-based alerting) is known, small, and in a file I had open. The strict "do exactly this item" contract correctly barred it (fixing the flaw is arguably IN the item: "alert … when" implies the alert reliably fires — a third window COULD have argued this and shipped it). That is the actual miss: I verified the flaw a third time instead of fixing it under the same item's umbrella.
- **Minor:** I ran the bridge DeadPool test filtered (`-run DeadPool`, no `-race`); sufficient for verification but weaker than the full `./scripts/ci-local.sh` gate the first window ran.
- **Minor:** status-index check (`scripts/check-status-index.sh`) was not part of my first verification pass — I found the unindexed 06-11 report by reading the README, not by running the guard that exists precisely for this.

## e) WHAT WE SHOULD IMPROVE

1. **Dispatcher: stop re-firing `[x]` items** — 7 wasted windows; the TODO checkbox is machine-readable and the contract requires reading it first anyway.
2. **Ack-based `alerted` flag** — flip only on accepted post (agentpool.go:413); one-test addition, closes the only real defect in the feature.
3. **Make the status-index pre-commit hook actually present in agent environments** — two retro-indexing events in one day means the hook isn't installed where agents commit.
4. **Ship the SystemNix input flip + `dead-pool-ticks` in production pool.conf** — the feature is useless until the deployed pool runs it.
5. **Fix-on-sight vs. contract tension:** agents on verification-only dispatches should treat a known, in-item, small defect as in-scope rather than tripling its confirmation count.

## f) TOP NEXT ITEMS (condensed; full 50-item ledger in the 06-06 report — forward items unchanged, re-verify each before starting)

1. Ack-based `alerted` flag + regression test (cmd/tq/agentpool.go).
2. Dispatcher re-fire guard (skip `[x]` TODO items).
3. SystemNix input flip → deploy → set `dead-pool-ticks` in production pool.conf.
4. Live-dashboard E2E: verify a real `NotifyDeadPool` trigger/resolve posts against a running PapDashboard (both paths never observed posting).
5. Count discovery/scan errors that are NOT skip-rows (harvest crash itself) into dead-pool coverage.
6. Make the dead-pool streak restart-surviving (persist last-streak state or accept the documented gap).
7. Decide `--once` semantics for `--dead-pool-ticks N≥2` (currently unreachable).
8. Test the cmd/tq composition glue (main.go:702 wiring) rather than only the detector unit.
9. Handle watch-ticks vs `--interval` cadence (streak advances faster than configured).
10. Install/verify the status-index pre-commit hook in the agent runtime image.
11. Cap re-dispatch count per task (dispatcher-side, e.g. max 2) and route overflow to an owner ping.
12. Add `scripts/check-status-index.sh` to the verification checklist of verification-only dispatch templates.
13. Deeper sweep of the 06-06 report's items 13–50 (retry/backoff audit, bridge watermark docs, E2E smokes) — carried verbatim, still valid.

## g) OWNER QUESTIONS (carried + one new; none answerable from the repo alone)

1. **Authorization argument:** may a verification-only dispatch FIX a known in-item defect (here: the ack flag) when it is the sole remaining gap between shipped and correct? The contract's "smallest correct change" reads ambiguously on re-dispatches.
2. **Re-fire trigger:** why does the dispatcher re-enqueue items whose TODO checkbox is `[x]` with a DONE-dated evidence string? Is the harvester filtering on a stale snapshot?
3. **Deployment window:** when will the SystemNix input flip to a post-8b5a6be build happen, so dead-pool detection actually guards the production pool it was written for?
