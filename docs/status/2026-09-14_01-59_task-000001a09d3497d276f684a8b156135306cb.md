# Status Report — task-000001a09d3497d276f684a8b156135306cb

> **ANNOTATED 14 Sep 2026 (docs-health strikethrough pass)** — every numbered item below is resolved inline (`done`/`duplicate` markers) or left untouched (= still open). Unarchived on purpose: open items remain. Evidence verified against HEAD.

**Date:** 2026-09-14 01:59
**Work item (TODO_LIST row 149, section "Window follow-ups (harvested from docs/status/2026-09-10_08-25)"):** Pin the fullcore drain-deadline determinism mechanically: extend the planned hermetic fullcore smoke to exercise the deadline path N times (`16a15d8` was hand-verified only; 08:25 report b3)
**Work commit:** 73a4f34 (`test: pin the fullcore drain-deadline path in the hermetic smoke`, footer `Task-Queue-ID: 000001a09d3497d276f684a8b156135306cb`)
**Note:** the status-report skill's canonical format is a styled HTML dashboard; the dispatch contract explicitly demands a `.md` file at this exact path, so the override is honored per the skill's own rule.

## a) FULLY DONE

1. ~~**TODO row 149 closed, work committed.** `scripts/smoke/fullcore.sh` now pins the drain-deadline path mechanically: after the existing drain assertion, it runs the fullcore example **3 times** with `--timeout 50ms` and asserts each run (i) exits NONZERO and (ii) fails with exactly `deadline exceeded before the queue drained` — the message at `examples/fullcore/main.go:144`. A wrong-reason failure (or, worse, a drain inside 50ms) fails the smoke.~~ done — fullcore.sh:50 deadline x3 (verified at HEAD)
2. ~~**Determinism probed before pinning, not assumed.** Timed the example at `--timeout` 30/60/120/250ms: ALL trip the drain deadline (worker-start latency dominates). 50ms was chosen as far below that latency — the failure mode is deterministic by construction, not a timing race. This matters because 250ms still trips: a future OS-slow-CI regression would have to slow worker start below 50ms, which would be its own loud failure.~~ done — comment cites 250ms probe (verified at HEAD)
3. ~~**Smoke re-run green twice** (once after the edit, once immediately before commit). Scratch-DB hygiene intact: deadline runs share a separate scratch db under the same mktemp dir; the existing `TQ_DB` production-trap export still guards everything.~~ done — narrative (point-in-time process note, no artifact owed)
4. ~~**TODO_LIST.md updated per file convention** (row 149 `[x]` + one-line DONE closure with date and the determinism evidence).~~ done — row closed; DONE rows deleted (verified at HEAD)
5. ~~**Commit hygiene:** footer exactly one well-formed `Task-Queue-ID:` (commit-msg hook would reject duplicates); never pushed; tree clean after commit.~~ done — narrative (point-in-time process note, no artifact owed)

## b) PARTIALLY DONE

1. ~~**Verification breadth.** The gate run for this task was the fullcore smoke itself plus its wiring in `ci-local.sh` (confirmed present at ci-local.sh:161-162). I did NOT run the full `./scripts/ci-local.sh` replicant — the change is smoke-only (shell + TODO row), no Go source touched, so the standard gate was judged disproportionate. That judgment is defensible but is a judgment, not a proof: `bash -n` / shellcheck-style checks are not gated anywhere for this script.~~ resolved — routed ci-local end-to-end row (verified at HEAD)
2. **N is hardcoded (3), not parameterized.** The item said "exercise the deadline path N times". I fixed N=3 as a constant in the script. It could be an env knob (`FULLCORE_DEADLINE_RUNS`) for a beefier nightly run; not done — smallest correct change won.
3. ~~**50ms margin is host-calibrated.** On a heavily loaded 2-core runner the worker-start latency could approach 50ms someday. The failure then is the smoke going red on the drain run, not the deadline runs — annoying but safe-direction (fails closed, never false-green).~~ done — narrative (point-in-time process note, no artifact owed)

## c) NOT STARTED

1. **Postgres-backend deadline run.** The smoke (and the deadline path) exercise sqlite only. The deadline logic lives in the example's consumer loop, backend-agnostic by construction, but nothing mechanically proves it on postgres. (Row 136 already covers a manual postgres run incl. the drain; the deadline path there was never exercised either way.)
2. **Windows coverage.** ci.yml runs some smokes on windows-latest; `fullcore.sh` is a bash script — whether it runs (or is skipped) in the Windows job was not checked this turn.
3. **Nightly/deeper N.** No wiring of a higher-N deadline sweep into `scripts/fuzz/nightly.sh`-style nightly automation.

## d) TOTALLY FUCKED UP (what I forgot / got wrong)

1. **First deadline probe was a trap I nearly pinned.** `--timeout 1ns` fails at ENQUEUE (`enqueue demo 0: context deadline exceeded`) — not the drain deadline. Had I grabbed the first failing exit code as "the deadline path works", the smoke would have pinned the WRONG code path forever. Only reading `main.go`'s control flow (ctx wraps enqueue AND drain) and grepping for the actual fatal message caught it. Lesson applied: assert on the MESSAGE, not the exit code — the script now greps `deadline exceeded before the queue drained`.
2. **Pipeline exit-code masking in my own probe.** `fc --timeout Xms 2>&1 | tail -1; echo "exit=$?"` printed `exit=0` for every run — `$?` was tail's status, log.Fatal's exit 1 vanished. Exactly the documented `set -o pipefail` trap from AGENTS.md, and I re-committed it in a throwaway probe (caught because the deadline MESSAGE was visibly present; in the actual script I used `if cmd; then fail` — correct form). No damage landed, but it's a wasted-verification-class mistake.
3. **Edit tool first-call failure** ("you must read the file before editing"): I had read the script via `cat` in bash, not the View tool — a turn wasted on a known tool contract.
4. **gopls phantom diagnostics noise.** The 41 "errors" (encoding/json/v2 build constraints, invalid-type on `t.Payload`) are the documented GOEXPERIMENT phantoms; I correctly ignored them (CLI build green), but each view paying 40 lines of phantom noise is friction the project has accepted — noted, not actionable by me.

## e) WHAT WE SHOULD IMPROVE

1. **Smoke asserts should grep for behavior, not just exit codes** — now done here; the pattern is worth copying to other smokes that only check `$?`.
2. ~~**Deadline-path margin should be documented where it's pinned** — the script comment explains the 50ms choice (done this turn); if CI hardware changes, the comment is the first place to look.~~ done — 50ms rationale in comment (verified at HEAD)
3. **Shell smokes have no lint gate** (`bash -n`/shellcheck absent from ci-local) — three smokes have now each had a near-miss class of bug.
4. ~~**Re-dispatch waste remains unguarded** — this is the 5th same-window task report; three siblings in the index are same-ID re-dispatches that re-verified DONE work. An enqueue-time dedup against recently-closed rows would save whole windows.~~ resolved — routed status-append-cap row (verified at HEAD)
5. ~~**Determinism pins should cite their probe data** — done (comment cites 250ms upper bound); keep the habit.~~ done — comment cites probe data (verified at HEAD)

## f) Up to 50 things we should get done next

(Brainstorm, not commitments — docs-health HARVEST should route these; most are ROADMAP fuel.)

**Queue/smoke hygiene (highest impact, from this task's neighborhood):**
1. Parameterize `FULLCORE_DEADLINE_RUNS` (env knob) and consider a nightly higher-N run.
2. Add a postgres-backend deadline-path smoke (throwaway initdb pattern from row 136).
3. Wire `bash -n` + shellcheck into ci-local for `scripts/smoke/*.sh` and `scripts/*.sh`.
4. Add the "assert on failure MESSAGE, not exit code" pattern to smokes that only check `$?` (audit webui/status-loop/dogfood/bootstrap smokes).
5. ~~Full `./scripts/ci-local.sh` run on a quiet window to confirm no cross-smoke interference from the new deadline runs.~~ resolved — routed ci-local row (verified at HEAD)
6. ~~Re-dispatch dedup: enqueue-time rejection (or warning) when a TODO row was closed within the last hour (the 3 same-ID re-dispatches of row 148).~~ resolved — routed rows 93/148/157 (verified at HEAD)
7. Document the 50ms/250ms calibration in AGENTS.md fullcore smoke entry (one line).
8. Check whether `fullcore.sh` is exercised or skipped in the ci.yml windows job; gate or document.

**Carried from sibling reports this window (already surfaced 01-34 → 01-54):**
9. Scoped-lint smoke (`lint-cmd-tq.sh`) so cmd/tq raw-site lint runs are repeatable (row-148 cluster's open §b).
10. ~~Add cmd/tq to ci-local's per-module lint loop or document the exclusion intent (pending owner ruling).~~ done — ci-local.sh:44-46 documented (verified at HEAD)
11. ~~Ratify the daemon-attribution convention (footer commit vs daemon commit) as a written rule.~~ resolved — routed daemon-attribution row (verified at HEAD)
12. Commit-msg hook: reject commits whose footer ID duplicates one in an already-closed TODO row on the same item text (dedup at the git layer).
13. ~~`no-op commit_sha` semantics for DONE-on-arrival verdicts (define what the queue should record).~~ resolved — routed duplicate-claim row (verified at HEAD)
14. Formalize the archive-sweep "cluster-note" convention (vs strikethroughs) in docs-health references.
15. gopls viability inside the cmd/tq devmod module (phantom diagnostics there too).

**General repo (noticed in passing, pre-existing):**
16. `check-dead-exports.sh` is advisory — decide if it should gate.
17. ~~Status-index WARNING (f26) in check-status-index.sh is known-noisy — triage or encode.~~ done — WARNING flip condition encoded (verified at HEAD)
18. ~~Run the lint-baseline regen check after the next policy change (886/111 baseline is fresh as of 2026-09-13; keep it that way).~~ done — round-2 regen ran --check green (verified at HEAD)
19. Nightly fuzz campaign health check: confirm last nightly actually ran (fuzz.yml GOEXPERIMENT fix landed 2026-09-12).
20. ~~VendorHash: no drift this turn (no go.mod changes); keep the fakeHash dance in mind for the next dep bump.~~ done — narrative (point-in-time process note, no artifact owed)
21. ~~Pool unit env: `Environment=GOEXPERIMENT=jsonv2` on the NixOS tq-agent-pool module is STILL owner-pending (AGENTS.md ⚠️) — every root `.tq-verify` failure with build-constraints errors is that lie, not a regression.~~ resolved — routed SystemNix env row (verified at HEAD)
22. ~~Session-close bridge open items (trigger automation crush #3146, daemon-commit attribution gap, budget bypass, postgres parity) — all still open per AGENTS.md.~~ resolved — routed session-trigger rows (verified at HEAD)
23. ~~go-cqrs-lite durable-work-queue module proposal upstream — re-open tq storage adoption only when it reaches conformance parity (per ADR stance; do not churn now).~~ resolved — routed facade-scope row (verified at HEAD)

**Nice-to-haves:**
24. Extract the shared mktemp/trap/GOEXPERIMENT/TQ_DB preamble of the five smokes into `scripts/smoke/lib.sh`.
25. Add a `--json` output mode to the fullcore example so smokes can assert counts without grepping prose.
26. Consider `tq` self-test subcommand running the smoke suite hermetically (adopters get the same gate).
27. Deadline-path unit test inside `examples/fullcore` is impossible (example has no tests by convention) — a tiny `internal`-adjacent test reproducing the consumer-poll loop would pin it at unit speed too.
28. ~~README/FEATURES: confirm the fullcore example + smoke are mentioned for adopters (on-ramp story).~~ done — README:437 FEATURES:142 (verified at HEAD)
29. ROADMAP item: "determinism harness" — a shared pattern (N iterations + message assertion) for any future flake-prone path.
30. CI: cache go build artifacts for smoke jobs (five smokes each `go build` independently in ci-local).
31. Document in DOMAIN_LANGUAGE.md: "drain deadline" vs "task timeout" are different deadlines (the example has both: `--timeout` drain, 5s internal shutdown ctx at main.go:167).

**Housekeeping:**
32. Delete stale scratch DBs from /tmp probes (`/tmp/fc*.db`, `/tmp/fcx.db`) — mine from this session.
33. ~~Confirm row 149's closure text renders in TODO_LIST lint (`check-todo-list.sh`) — implicit via smoke, but the TODO gate is separate; next ci-local run confirms.~~ done — DONE rows deleted on closure (verified at HEAD)
34-50. Reserved: see the sibling 01-54 report's list (rows 148 cluster, lint-baseline, CI parity items) — deliberately not duplicated here; HARVEST should merge, not double-book.

## g) Questions I can NOT figure out myself (max 3)

1. **Is 3 iterations the right N, and should it scale in CI vs nightly?** The item said "N times" without a number. I picked 3 (deterministic path, each run ~sub-second). If the intent was statistical confidence in a timing race, 3 is weak — but I demonstrated there IS no race at 50ms. Is N=3 accepted, or did the 08-25 report's b3 intend a burn-in (N≥50)?
2. **Should the deadline smoke also cover the postgres backend?** It doubles smoke cost and needs a postgres fixture in CI (the env-gated TQ_TEST_POSTGRES job exists). My judgment: no — the deadline logic is backend-agnostic and the row-136 postgres run covers the drain. Confirm or overrule.
3. ~~**The re-dispatch problem is now 3-for-3 this window** (row 148 closed, then re-dispatched twice more; row 139 similar). Do you want an owner ruling on WHERE dedup belongs (queue enqueue-time vs sweep-time vs prompt contract "re-read the row before working"), so I can stop paying the tax in every window?~~ resolved — routed dedup ruling rows (verified at HEAD)

---

*Point-in-time snapshot, 2026-09-14 01:59. Re-verify claims against current code before acting on them.*
