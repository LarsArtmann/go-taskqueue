# Task 000001a09d3497d276f684a8b156135306cb — third dispatch, delta close-out

> **ANNOTATED 14 Sep 2026 (docs-health strikethrough pass)** — every numbered item below is resolved inline (`done`/`duplicate` markers) or left untouched (= still open). Unarchived on purpose: open items remain. Evidence verified against HEAD.

Date: 2026-09-14 02:04. This is the THIRD dispatch of TODO row 149 (fullcore
drain-deadline mechanical pin), which was already DONE and closed twice:

1. `2026-09-14_01-59` — the work itself (73a4f34: `scripts/smoke/fullcore.sh`
   runs the example 3× at `--timeout 50ms`, each asserted to exit nonzero with
   "deadline exceeded before the queue drained"; TODO row 149 `[x]`).
2. `2026-09-14_02-02` — close-out for the second (re-)dispatch.

## a) FULLY DONE

- Verification, not trust: `git log` confirmed 73a4f34 on master with this
  task's exact footer; TODO_LIST.md row 149 already `[x]` with a DONE
  citation (commit + probe evidence: 30–250ms all trip, 50ms is far below
  worker-start latency so the path is deterministic).
- Fresh gate run: `./scripts/smoke/fullcore.sh` → PASS ("fullcore drained
  4/4 on sqlite (scratch TQ_DB honored)") including the deadline path ×3
  with message assertion (`scripts/smoke/fullcore.sh:50`).
- Repo clean after verification — nothing to change, nothing to commit for
  the work turn.

## b) PARTIALLY DONE

- Nothing in THIS dispatch. The residual gaps from 01-59 §b stand unchanged:
  N=3 hardcoded (no `DEADLINE_RUNS` env knob), 50ms calibrated on this host
  (margin not asserted numerically), no postgres/Windows deadline coverage.

## c) NOT STARTED

- Nothing — and correctly so. The smallest correct change for a DONE-on-
  arrival dispatch is no change at all.

## d) TOTALLY FUCKED UP

- The dispatch-routing hole, again: this is now the third same-id dispatch of
  a row that was DONE, committed, and closed in the same window (01-59 work
  turn). The near-miss I personally own: my first action pattern matched
  "work item" → I began scoping an implementation before running the
  session-start ritual that would have shown 73a4f34 in `git log -5` (it was
  the third-newest commit). The ritual-first habit is the whole defense and
  I reached for it second.

## e) WHAT WE SHOULD IMPROVE

- Queue dispatchers should re-check row state at claim time, not enqueue
  time — a row marked `[x]` between enqueue and claim should not re-ship.
- For verifiably-DONE arrivals, the fastest correct response is: verify
  (log + gate run) → report → stop. I did this; make it the documented
  playbook so it costs one turn instead of a re-close-out report.

## f) NEXT (delta — the canonical 50-item list lives in `2026-09-14_01-59` §f)

Top of that list, still valid as of this dispatch:
1. Shellcheck gate for `scripts/smoke/*.sh` (bash -n at minimum).
2. `DEADLINE_RUNS` / `DEADLINE_TIMEOUT_MS` env knobs in fullcore.sh with
   committed defaults.
3. Numeric margin assertion on the 50ms probe (fail if drain lands < ~5ms —
   would catch CI hosts fast enough to make 50ms flaky-positive).
4. ~~Re-dispatch dedup ruling (see §g).~~ duplicate — see 01-59 f6/g3
5. ~~postgres-backend deadline coverage once the env-gated CI postgres job is~~ duplicate — see 01-59 c1/f2
   green again.

## g) QUESTIONS FOR THE OWNER

1. Should the dispatcher (or the close-out turn itself) hard-stop when the
   claimed row is already `[x]` with a DONE citation newer than the enqueue
   timestamp — instead of minting another no-op window? (Third occurrence
   this window; same question asked in 02-02 §g.)
2. Is a delta close-out like this one the preferred artifact for repeat
   dispatches, or should repeat dispatches be suppressed upstream with no
   report at all?
3. Do you want the fullcore deadline N/timeout turned into env knobs now
   (small), or held until the postgres/Windows deadline coverage decision?

— Not BLOCKED; task complete and verified.
