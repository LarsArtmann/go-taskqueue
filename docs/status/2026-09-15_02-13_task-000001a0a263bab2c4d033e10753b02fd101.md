# Close-out: session-snapshot ghost reported upstream (bafc720 incident)

- **Task**: `000001a0a263bab2c4d033e10753b02fd101`
- **Date**: 2026-09-15 02:13
- **Work commit**: `e6997d0` (TODO_LIST check-off)
- **Upstream artifact**: charmbracelet/crush#3842

## a) FULLY DONE

1. **Verdict delivered**: the ghost is a harness state-capture artifact, not a
   concurrent-agent duplicate. The two hypotheses in the TODO item are now
   resolved with repo forensics instead of remaining an open question.
2. **Forensics on the incident** (the part the 08:32 report couldn't do from
   inside its session):
   - `bafc7201ee28…` is a REAL commit, authored 2026-09-10 08:32:18 +0200 —
     and it is now DANGLING (`git log --all` does not reach it; only
     `git rev-parse` finds the object).
   - Its reachable twin `2574569` carries the SAME author timestamp
     (08:32:18) but a different parent (7f29f96 vs 191aaed) and a LATER
     committer timestamp (09:15:43). So `2574569` is a replay of `bafc720`
     onto a rebase fork — the 2026-09-10 reword-incident lineage fork is the
     thing that orphaned the original. That also explains why the 08:32
     report's `git log --all --grep ExitCause` saw "one commit" (it was
     reachable then) while today it sees a different one.
   - No concurrent duplicate ever existed: one authoring event, one replay.
3. **Root cause verified at Crush source level** (cloned charmbracelet/crush
   master 2026-09-15):
   - `internal/agent/templates/coder.md.tpl:378` hardcodes
     `Git status (snapshot at conversation start - may be outdated)`.
   - `internal/agent/prompt/prompt.go` `getGitRecentCommits` runs
     `git log --oneline -n 3` at prompt-build time.
   - `internal/agent/coordinator.go` `buildAgent` builds the system prompt
     once per coordinator ("one-time setup").
   - `internal/app/app.go` `initCoderAgent` constructs a fresh coordinator on
     EVERY app init — including session resume. Resume keeps conversation
     history but regenerates the system prompt from live git state, so a
     resumed session presents post-hoc commits as pre-existing. That is the
     exact mechanism behind the 08:32 agent seeing `bafc720` "at conversation
     start".
4. **Upstream filing discipline followed** (verify-before-filing + github-voice):
   - Source-level evidence in the issue (file:line for all four code points).
   - Gate 5 duplicate search ("git status snapshot", "stale git") — empty.
   - Local `crush v0.94.1` pinned as the incident version, latest release
     v0.94.2 checked; behavior confirmed on master.
   - Draft passed `check-draft.py --kind body-issue --ai-drafted`
     (0 FAIL, 0 WARN) before filing.
   - Filed: charmbracelet/crush#3842 with a one-line template fix suggested
     (timestamp the header) and a stronger fix (persist the snapshot at
     session creation).
5. **TODO_LIST closed**: item checked off with the verdict + issue link +
   one-line root cause (commit `e6997d0`); `check-todo-list.sh` green.

## b) PARTIALLY DONE

Nothing in the task itself. One adjacent bookkeeping gap:

1. **No CHANGELOG entry**: this is a docs/TODO-only change plus an upstream
   issue — no shipped behavior changed in THIS repo, so by the (still
   undecided) policy question in TODO_LIST row 66 nothing is owed. Flagging
   the judgment call rather than silently skipping.

## c) NOT STARTED

1. Watching charmbracelet/crush#3842 for maintainer response (a resume-path
   fix upstream would retroactively explain the other "stale snapshot"
   confusions agents have reported in passing).
2. The twin-commit situation itself: `bafc720` (dangling) vs `2574569`
   (reachable) is now KNOWN but the dangling object is still unreferenced
   garbage that `git gc` will eventually prune. No action needed — the
   history-rewrite policy already treats the fork as healed-by-merge-append —
   but nobody has recorded the specific bafc720/2574569 pair as a resolved
   instance of that policy. This report is that record.

## d) TOTALLY FUCKED UP

Nothing rose to that level. Honest worst-offenders:

1. **The 08:32 close-out's own claim was half wrong and I initially weighted
   the wrong hypothesis.** The report said "my commit then actually produced
   bafc720" — impossible to reconcile with the twin evidence unless the
   agent's `git log` readback was of the pre-existing commit (the replay
   proves the work was re-committed later). The natural reading of the
   incident ("SHA predicted before it existed") is a red herring; only the
   dangling-object forensics surfaced that. I spent the first searches on
   Crush's snapshot _generation_ mechanics before running `git rev-parse` —
   the single command that reframed everything. Cheap lesson, real cost: a
   few wasted tool calls.
2. **First TODO-item placement check was lazy**: I initially searched only
   unarchived `docs/status/` for the incident report and missed that it had
   been archived (needed the grep over the whole tree). Trivial, but it is
   the "verify provenance at pickup" rule from the 08:32 report §e1 biting
   again.

## e) WHAT WE SHOULD IMPROVE

1. **Run `git rev-parse` before any ghost-commit theory.** Reachability
   (`--all`) and existence (rev-parse) are different questions; every
   "impossible" hash sighting should be triaged with both in that order.
2. **Dangling twins are a recurring artifact of the rebase fork policy.** A
   one-line `git note` or a list in the history-rewrite section of AGENTS.md
   (old-SHA → new-SHA pairs) would prevent the next agent from re-deriving
   the pair. Cheap, prevents repeat forensics.
3. **Upstream issues deserve the same close-out rigor as code**: the
   issue (#3842) cites master file:line and versions; if Crush renames those
   internals the issue may rot — the TODO check-off links the issue, which
   is the durable pointer. Keep it that way for future filings.
4. **`crush --version` at incident time is unknowable post-hoc** (the
   08:32 report didn't record it). Pinned v0.94.1 as "the version running
   the incident" from the current deployment; the issue says so. Reports
   that involve harness behavior should record the tool version at write
   time — one line, saves the next investigation.

## f) NEXT (Pareto-ordered, this task's vicinity first)

1. Watch/subscribe charmbracelet/crush#3842; if the timestamp-label fix
   lands, note the fixed version in the TODO row.
2. Record the resolved bafc720 → 2574569 twin pair in the AGENTS.md
   history-rewrite section (or as git notes) so no one re-investigates.
3. Decide owner question 66 (CHANGELOG policy) — two close-outs have now
   tripped over it.
4. Sweep TODO_LIST for other items that assume session snapshots are
   trustworthy ground truth; annotate them with the #3842 caveat.
5. Consider a `scripts/smoke/` or audit helper that lists dangling commits
   with author dates (`git fsck --lost-found` + `git log` per object) as a
   periodic advisory — the rebase-fork policy creates these silently.
6. The 08:32 report §g3 (TODO-item accuracy bar at harvest time) remains
   open and BLOCKED on owner call; this task re-proved the rule (archived
   report mis-placed twice during pickup).
7. Upstream follow-up candidate: Crush could expose the snapshot capture
   time in the env block programmatically (`{{.CapturedAt}}`) rather than a
   hardcoded label — propose only if #3842 gets traction.

## g) QUESTIONS I COULD NOT FIGURE OUT MYSELF

1. **Which crush binary ran the 08:32 session?** The report didn't record
   it; I pinned v0.94.1 (current deployment) but the incident may have run
   an earlier 0.9x. Does SystemNix keep a version history for the pinned
   tq-agent-pool crush input that would let me confirm the label bug
   existed then too?
2. **Should resolved forensics like the bafc720→2574569 twin pair live in
   AGENTS.md, git notes, or nowhere?** git notes don't survive this repo's
   daemon/rewrite regime cleanly; AGENTS.md grows unboundedly. Owner call.
3. **Is #3842 worth a PR?** The one-line timestamp fix is trivial, but
   per repo policy contributions to charmbracelet/crush go through the
   jj-fork-PR workflow and the maintainer may prefer the session-persisted
   snapshot. Wait for maintainer steer, or open the draft PR now?

## Verification trail

- `git rev-parse bafc720` — resolves (object exists, unreachable)
- `git log -1 --format=... bafc720` vs `2574569` — same author ts
  08:32:18, different parent + committer ts 09:15:43 (replay proof)
- `git merge-base --is-ancestor 191aaed master` — false (old fork dangling)
- `git log --oneline master | grep "rename runactor"` — 2574569 reachable
- Crush source: coder.md.tpl:378, prompt.go getGitStatus/getGitRecentCommits,
  coordinator.go buildAgent, app.go initCoderAgent (cloned master 2026-09-15)
- `gh search issues --repo charmbracelet/crush` (3 queries) — no duplicates
- `check-draft.py --kind body-issue --ai-drafted` — 0 FAIL, 0 WARN
- `./scripts/check-todo-list.sh` — ok
- Issue filed: https://github.com/charmbracelet/crush/issues/3842
