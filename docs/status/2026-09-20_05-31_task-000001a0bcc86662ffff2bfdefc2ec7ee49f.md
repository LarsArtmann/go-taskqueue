# Status Report — dead-SHA citation gate, verify re-dispatch (task 000001a0bcc86662ffff2bfdefc2ec7ee49f)

**Window:** 2026-09-20 05:31 · **Type:** verify→cite→stop re-dispatch (fourth window on
this ID; work window = 2026-09-20_05-16, first verify close-out = 05-21) ·
**Source delta this window:** ZERO · **Tree at report time:** clean

## What this window did

Turn-1 ritual held: same-ID report grep FIRST (found the 05-16 work report + 05-21
verify close-out) → verified → stop. The dispatched close-out asked for this report.

Fresh battery at HEAD:

- `./scripts/check-dead-sha-refs.sh`: rc=0, `dead-sha refs ok (996 baselined, 0 new)`.
  Cite-site count moved 989→996 since the 05-21 window: the baseline is SHA-level, so
  NEW reports citing ALREADY-baselined dead SHAs add cite-sites without failing — no
  new dead SHAs (0 new), gate semantics intact.
- ci-local wiring confirmed present (scripts/ci-local.sh:281, hard step).
- Dispatch citation-check instruction executed: `dd543ec` confirmed NOT reachable from
  HEAD (`git merge-base --is-ancestor` → NOT-REACHABLE) but still a real object covered
  by the baseline (dead-sha-baseline.txt:220); TODO row 140 keeps its historical
  narrative mention (re-pointed to reachable d035911/ca514a8 in the 05-16 window) and
  the gate passes at HEAD — no source item fix needed beyond what shipped.

## a) FULLY DONE

1. The task itself (05-16 window, re-verified fresh): hard ci-local gate
   `scripts/check-dead-sha-refs.sh` + `scripts/dead-sha-baseline.txt`; TODO row 140
   `[x]` with citations.
2. This window: zero-delta verification with the battery above; gate green at HEAD.

## b) PARTIALLY DONE / carried

Unchanged from 05-21 §b: legacy baseline shrink (242 SHAs, ~1000 cite-sites, urgency =
before the next `git gc` orphanizes the objects), ci.yml parity (gate is ci-local-only).

## c) NOT STARTED

Nothing owned by this ID.
