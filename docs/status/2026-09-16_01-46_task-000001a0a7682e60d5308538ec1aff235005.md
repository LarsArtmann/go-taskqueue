# Close-out: CI retry-on-transient-foreign-break wrapper for ci-local

- **Task**: 000001a0a7682e60d5308538ec1aff235005 (TODO_LIST row 88; source: 15-39 report f9 / e2 / f-item 9)
- **Lap**: 2026-09-16 01:29–01:55 CEST, single session
- **Work commits**: content rode the auto-commit daemon — `a3b97f2` (scripts/ci-local.sh, +39/−16) and `1a71159` (TODO_LIST.md); footer marker `a1b4588` (empty, precedented by 37e83d4) carries the `Task-Queue-ID` and cites both daemon SHAs
- **Close-out lap adds**: this report + index row, AGENTS.md Commands one-liner, TODO follow-up row (smoke coverage + durable self-test pin)

## a) FULLY DONE

1. **The wrapper ships**: `scripts/ci-local.sh` gained `with_transient_retry` — on gate failure: WARN ("possible concurrent edit in flight; retry poll N/3 in 45s"), sleep, re-run; after 3 exhausted polls: hard FAIL with the "concurrent edit in flight" context message ("let it land (or re-run this gate on a quiet tree) before judging the work"). `TRANSIENT_POLL_SECS`/`TRANSIENT_MAX_POLLS` are env-overridable so the loop can be exercised without sleeping.
2. **11 wrapped call sites across 10 steps**, exactly the tree-reading Go gates — the failure class the report observed (`undefined: atomic` mid-edit reds): vet, build, windows build+vet (root), per-module windows cross-compile loop, cmd/tq windows gate, `-race` tests, per-module isolation loop, cmd/tq gate, embed example build, harvest-parse guard.
3. **Boundary decision made and documented in-script**: nix steps deliberately NOT wrapped (they measure the staged tree — a foreign break there is about to be pushed and must fail, not heal); smokes deliberately deferred (each owns its own cleanup; extending is a runtime-budget decision, not drive-by). The comment says both.
4. **Verification battery** (all cited runs from this session):
   - `./scripts/check-script-syntax.sh` green (bash -n 47 scripts + shellcheck severity≥warning, 0 findings) — one earlier exit-1 was the nix-shell fetch racing the gate's output, re-run green;
   - functional test of the REAL helper text (sed-extracted from the shipped file): heal-on-3rd-attempt → exit 0 with 2 poll WARNs; always-red → exit 1 after exactly 3 polls with the context FAIL; compound `bash -c 'cd "$1" …' _ dir` argument form → exit 0;
   - `env GOOS=windows go build ./... && env GOOS=windows go vet ./...` — the changed invocation form compiles and vets;
   - standard verify gate `GOEXPERIMENT=jsonv2 go build ./... && go vet ./...` green;
   - full root race suite `go test ./... -count=1 -race -timeout 120s` green (all packages ok);
   - `./scripts/check-todo-list.sh` green after the row flip.
5. **TODO_LIST closed per convention**: row 88 marked `[x]` with a DONE note (what shipped, what is deliberately unwrapped and why).
6. **Committed with the queue footer** (`a1b4588`); nothing pushed.

## b) PARTIALLY DONE

1. **Wrapper coverage of ci-local is partial by design — and the deferred half was untracked until this close-out lap.** Smokes are not wrapped (each smoke re-runs whole up to 3× and can take minutes; that worst-case budget is a ruling, not a drive-by). Deferring was correct; deferring WITHOUT a TODO row was the miss — fixed this lap (new row: smoke coverage + durable self-test pin).
2. **Durable regression pin for the helper: none.** The functional test was a throwaway /tmp exercise of the sed-extracted function — it verified the shipped bytes at authoring time and protects nothing going forward. If a future edit breaks the poll counter or the context message, no gate catches it. The repo has the precedent (`check-status-index.sh --self-test`, wired as a ci-local step) — but a ci-local self-test is self-referential and can't gate itself; the right shape needs the small design decision now tracked in the follow-up row.
3. **AGENTS.md capture lagged the work.** The wrapper changes operator-facing behavior of the pre-push gate (a red can now mean "wait ≤2.25 min and re-run"), and AGENTS.md's Commands block said nothing about it at work-commit time. Fixed in this close-out lap (one line), but the memory-maintenance rule says at the moment of discovery, not at close-out.
4. **The 45s×3 budget is report-inherited, not measured.** 15-39 e2 said "poll up to N×45s" — I implemented N=3 faithfully without any datapoint on how long a real foreign-edit window lasts. If concurrent sessions routinely hold the tree mid-edit for >2.25 min, the wrapper burns its polls and still fails (the context message still saves the diagnosis, but the "wasted 10-min run" problem is only partially solved for slow foreign sessions).
5. **No "healed on poll N" success marker.** After a successful retry the log shows the earlier red output, WARN lines, then more output and eventually ALL GREEN — a skimmer sees a build error and a green banner with no explicit heal line between. Cheap fix, not shipped.
6. **No wrapped gate was exercised through the real script end-to-end.** Mechanics are proven (extracted-text functional test covers the exact compound forms; bash -n + shellcheck cover the file), but full ci-local was deliberately not run for a script-only change (10+ min, stages the tree, nix build). Honest residue: the first REAL run is also the first end-to-end test.

## c) NOT STARTED

1. **Real-world soak**: the wrapper has never healed an actual foreign break — zero poll-hit-rate datapoints exist.
2. **Smoke coverage under the wrapper** (now tracked in TODO_LIST).
3. **Durable self-test pin** for `with_transient_retry` (same row).
4. **Calibration measurement** of actual foreign-edit window durations (from journal/git evidence of past mid-edit breaks) to validate or replace 45s×3.

## d) TOTALLY FUCKED UP

Honest answer: nothing in this class shipped. Two disclosures that stop short of it:

1. **Turn-1 ritual order violation**: the session-start addition "grep prior reports for the SAME task ID BEFORE doing anything" ran at commit time, not turn 1. Outcome was benign (zero prior reports for this ID — fresh dispatch, verified), but the ritual exists precisely for the case where a DONE row or prior report DOES exist; a repeat dispatch would have been detected only after the work was done. Sixth-recurrence class of miss, different flavor (late instead of skipped).
2. **Queue↔git cross-reference wart**: the footer lives on an EMPTY marker commit (`a1b4588`) while the content rides two footer-less daemon commits (`a3b97f2`, `1a71159`). The daemon swept both files before my explicit commit could (my `git status` check came back clean mid-flight). I chose the empty marker (precedented, zero history-rewrite risk) over scripted rewording of the unpushed daemon commits — but the result is that the queue's cross-reference lands on a no-diff commit, and forensics require reading the message to find the real diff. This is the known daemon-fold weakness (01-21 report §b1), now with one more instance.

## e) WHAT WE SHOULD IMPROVE

1. **A deferral without a tracking artifact is a loss.** "Smokes deliberately deferred" lived only in a code comment and the done-row's prose until this lap added the TODO row. Rule going forward: every deliberate deferral lands in TODO_LIST in the same lap.
2. **Behavior-gating script helpers deserve durable pins.** Throwaway /tmp verification proves the ship, not the future. The self-test precedent exists; use it (or a dedicated wired check) for any bash helper whose failure mode is silent.
3. **AGENTS.md updates at the moment an operator-facing behavior changes** — the close-out lap catch-up works but violates the stated immediate-update rule.
4. **Turn-1 means turn 1**: the grep-prior-reports step precedes every other tool call, including the session-start `git log`.
5. **Retry loops should announce their outcomes**: a "healed on poll N" line makes ci-local logs self-explaining; red-then-green without explanation reads as flakiness and invites misdiagnosis.
6. **Daemon-race protocol**: when a work item must carry its own footer, run the verify battery, then IMMEDIATELY re-check `git status --short` and commit before any other step (the daemon's sweep beat my commit by minutes). Where the diff is already daemon-folded, the empty marker commit is the honest fallback — keep citing the daemon SHAs in the message.

## f) UP TO 50 THINGS WE SHOULD GET DONE NEXT

Honest count: 20 real items. I refuse to pad to 50 (precedent: the 00-11 report's refusal).

**From this task's follow-ups:**
1. Extend the transient-retry wrapper to the smokes (needs the runtime-budget ruling, §g3) — tracked.
2. Durable self-test pin for `with_transient_retry` (tracked, same row).
3. Add the "healed on poll N" log line to the wrapper.
4. Measure real foreign-edit heal latencies (past incidents in git history / reports) and validate or recalibrate 45s×3.
5. Soak: first real ci-local run with the wrapper; record whether any poll healed (closes the zero-datapoint gap).
6. One-line rationale in the wrapper comment for why `check-ci` (master CI state) is unwrapped too — foreign master redness has its own semantics (it is currently implicit).

**Adjacent queue rows read in passing (legitimately next):**
7. Mint-time done-check for repeat dispatches — the loop's standing top fix (the 00-55/04-35/04-40 reports each burned a paid lap re-verifying an already-DONE row; this task was fresh, but the pattern is one flaky grep away).
8. Row 89 batch: small rate-limit/observability items (`--commits` on `tq facts`, parked×MarkOrphaned pin, closeout-resume flag in requeue fact, HTTP-date Retry-After fixture, stats parked JSON contract test, doctor earliest `not_before`, armed-provider log, NotifyDeadPool 429-transient, postgres stale-fact parity, parked count in nowband, WithoutCloseout gate-leak test, ratelimit-e2e second-claim assertion + ci-local wiring).
9. Row 90: `ci.yml` concurrency group to cancel superseded daemon-burst runs; `check-ci.sh` "red predates your tree" wording; go-mod verify GOMODCACHE pin.
10. Row 91: status-append cap/dedup + re-dispatch root cause (36/75 close-outs, up to 5× re-fires, a proven LOST TODO fix).
11. Row 92: delete dead `factLines` (`internal/webui/components.go:133`) + add the four missing DOMAIN_LANGUAGE terms.
12. Round-13 blocked row: `GOEXPERIMENT=jsonv2` on the tq-agent-pool unit (owner sudo; ends the pool verify env-lie class — directly relevant here, since that env lie is one of the two "gate red but code fine" classes the retry wrapper does NOT cover).

**Noticed in passing this session:**
13. The wrapper's per-gate retry means a foreign break can pass gate 1 and break gate 2 — fine — but the WARN lines don't name the failing FILES; consider grepping the failed output for the file list into the WARN (helps decide "wait vs coordinate" without re-reading the log).
14. `TRANSIENT_MAX_POLLS=0` silently disables the retry (immediate fail = pre-wrapper behavior) — harmless, but the AGENTS.md line could note the knob's escape-hatch semantics.
15. The shellcheck gate's nix-shell fetch path raced its own output once this session (exit-1 that wasn't a finding) — a `--quiet`-style retry or output separation in `check-script-syntax.sh` would kill the false red.
16. Empty marker commits as daemon-race fallback now have two instances (37e83d4, a1b4588) — if §g1 ratifies the pattern, document it in AGENTS.md; if not, codify the reword alternative.
17. The 15-39 report's e2 row said "N×45s" with N unspecified and f9 said "max 3" — the two source rows disagree in specificity; harvested TODO rows should pin the exact numbers at harvest time (this one took report archaeology to resolve).
18. ci-local's wrapped gates now total 11 call sites — the script is growing into a lib candidate; if one more mechanism lands in it (retry, then what?), split `scripts/lib/ci-local-lib.sh` (the `cmd-tq-devmod.sh` lib precedent exists).
19. The `env GOOS=windows` normalization (was `GOOS=windows` prefix) is now inconsistent with any other env-prefixed calls elsewhere in the script — sweep once if more appear.
20. §b6's honest residue: the first real ci-local run after this ship should be watched once (the in-situ `bash -c` forms have only ever run through the extracted-helper test).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Daemon-race footer policy**: when the auto-commit daemon sweeps a task's diff before the explicit footer commit, is the empty marker commit (current precedent: 37e83d4, a1b4588) the sanctioned answer, or should future sessions reword the unpushed daemon commits in place (content 1:1 with its footer, but scripted history editing of daemon commits)? I can pick per-instance; only you can make it policy.
2. **Is 45s×3 the right budget?** I implemented the report's numbers faithfully, but nothing in the repo measures how long a real foreign-edit window lasts. If you have observed typical concurrent-session edit durations, I should recalibrate; otherwise item f4 stays the plan.
3. **Smoke-retry runtime budget**: wrapping the smokes means a genuinely-red smoke could cost 3× its runtime (some smokes take minutes) before ci-local fails. Accept whole-smoke re-runs ×3, or do you want the two-phase design (cheap tree-quiet probe before each smoke, full re-run only when the probe saw a break)?

---

**Gates cited (this session)**: `check-script-syntax.sh` green ×2 (after one fetch-race false red); extracted-helper functional test 3/3; `env GOOS=windows` build+vet green; `GOEXPERIMENT=jsonv2` build+vet green; root race suite green; `check-todo-list.sh` green. Commits: `a3b97f2`, `1a71159` (daemon), `a1b4588` (footer marker), this report commit.
