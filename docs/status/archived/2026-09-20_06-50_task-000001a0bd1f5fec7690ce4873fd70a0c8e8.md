# Status Report — task 000001a0bd1f5fec7690ce4873fd70a0c8e8 — committed wrapper-path smoke leg (verify window, re-dispatch)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_06-45_task-000001a0bd1f5fec7690ce4873fd70a0c8e8.md`, which owns any open residue.

**Window:** 2026-09-20, ~06:45–06:50 (single verify window; REPEAT DISPATCH of the same task id as docs/status/2026-09-20_06-45_task-000001a0bd1f5fec7690ce4873fd70a0c8e8.md)
**Work item (TODO_LIST §"Done-prompt window harvest 2026-09-12"):** drive `tq crush --bin <stub child>` over a scratch repo (footer commit + `session_id:` output), assert id scan + 1 review + 1 status minted through `runSessionClose` + exit passthrough; wire into ci-local after session-close.sh.
**Resolution:** ALREADY DONE by the 06-45 window (commit d1290cf). This window ran the ritual: grep prior reports for the task id FIRST (hit: the 06-45 report), verify the claim against fresh gate output, cite, stop. No code changes; the only artifacts are this report and its index row.

## a) FULLY DONE

1. **Repeat-dispatch ritual executed in order**: `git log --oneline -5` + `git status --short` + `git stash list` (clean tree, d1290cf is the head of the smoke script's history), then `rg -l 000001a0bd1f5fec… docs/status/` BEFORE any work — hit the 06-45 report immediately, so the fastest correct response (verify → cite → stop) was chosen over re-implementing.
2. **Independent gate re-run green (fresh evidence, this window)**: `./scripts/smoke/session-close.sh` full run, `rc=0`, final lines "tq crush, committed path … / == session-close smoke passed" — the committed wrapper-path leg itself (stub child commits `Crush-Session: wrapsession0001`, prints `session_id:`, exits 5; wrapper scans the id, attributes 1 commit, mints exactly 1 review + 1 status into a fresh `--db`, 1 `session.closed` fact, exit 5 passes through). Per the citation rule: the gate output was in hand before this report was written.
3. **TODO_LIST row confirmed closed**: item is `[x]` at TODO_LIST.md:146 — closed by the prior window's commit d1290cf, nothing to re-close.
4. **ci-local wiring confirmed**: the smoke is the existing ci-local step at `scripts/ci-local.sh:248-249`, so the item's "wire into ci-local after session-close.sh" half is satisfied exactly as the 06-45 report documented (the leg rides the script ci-local already executes).
5. **§g1 ruling-gap confirmed moot**: the gate now covers the committed wrapper path directly, so the "gate-vs-cited-one-off" question the item text flagged is answered for this leg by construction (same verdict as 06-45 §g1 context).
6. **No code changes made** — verified state was not edited, reverted, or duplicated; working tree clean before and after (this report excluded).

## b) PARTIALLY DONE

1. **Freshness scope of the re-verification**: only the session-close smoke was re-run, not `./scripts/ci-local.sh` or the full test battery. Defensible for a verify-only window over an already-gated change (the smoke IS the gate for this leg), but a full ci-local pass remains the honest pre-push gate and was not re-executed here. The 06-45 window carried the same honest caveat for build/test gates (its §b1).

## c) NOT STARTED

1. Nothing from THIS task — the item is fully done and was so before this window started. The remaining §f rows below are carry-overs and observations, not work this task opened.

## d) TOTALLY FUCKED UP

Nothing this window. Honest misses worth recording:

1. **First command batch ran the session ritual and the script read in parallel without first grepping the task id** — the ritual ordering (grep prior reports FIRST) was satisfied within the same turn, but ideally the `rg -l <task-id> docs/status/` is the very first call; it was the second batch. Cost: one redundant full read of session-close.sh (~180 lines) before knowing the task was a repeat dispatch. Trivial cost, but the ritual exists to prevent exactly this ordering.
2. **Inherited, still-open issues noticed in passing (not mine to fix, not new)**: the 06-45 report's §f rows remain valid — notably shellcheck being unavailable in session shells (the `check-script-syntax.sh` CI step is the catcher) and the wrap.err-vs-stdout stream assumption that cost the prior window a retry (runSessionClose prints close output to stdout, not stderr).

## e) WHAT WE SHOULD IMPROVE

1. **Task-id dedup at the SOURCE, not the consumer**: this is the second dispatch of this exact item within the hour. The queue minted a repeat while the row was `[x]` in the same commit as the work. A sweep-side guard (skip minting when the dedup-keyed item is already checked off) would stop repeat windows entirely.
2. **Verify-window reports could be lighter**: a repeat dispatch's correct output is "already done, fresh gate rc=0, cite, stop" — a full a–g report re-inventories a state another report already describes. A short "REPEAT DISPATCH — supersede-verify" report format (or an appendix on the prior report) would keep docs/status/ noise down.
3. **Session-shell gate capture discipline keeps proving itself**: this window redirected to a file and captured `$?` directly (`rc=$?` after the redirect) — the codified PIPESTATUS hazard was avoided by construction. Keep this as the default shape for every agent-invoked gate.
4. **The §g1 "gate-vs-cited-one-off" question class is still unruled in general**: this leg is mooted, but the general ruling (which ad-hoc cited stub runs must be promoted into gates) remains open — see §g3.

## f) Up to 50 things we should get done next

Carry-overs from the 06-45 report (still valid, not re-derived) plus this window's observations. Most are ROADMAP fuel; harvest with routing rigor.

1. Sweep-side repeat-dispatch guard: don't mint a task whose TODO_LIST row is already `[x]` (root cause of this very window; the 06-45 report's §f carried its own variant).
2. A lightweight "repeat dispatch" report convention: short supersede-verify note instead of a full a–g re-inventory (see §e2).
3. Owner ruling on the general §g1 question: which ad-hoc cited stub runs must be promoted into gates (this leg closed the instance, not the policy).
4. §g carry-over: make close-failure visibility explicit in `tq crush` (what the operator sees when close itself fails after the child succeeded).
5. §g carry-over: decide whether smokes should share the main scratch DB or always use fresh per-leg DBs (the wrapper leg's fresh-DB counting is the current pattern).
6. CHANGELOG one-liner for the wrapper-path smoke leg (06-45 §f row 3, still open).
7. Get shellcheck into the devShell so session shells can run the syntax gate locally (06-45 §e1).
8. Two same-day reports for one task id (06-45 + 06-50) now both sit in the index — confirm `check-status-index.sh` and the README's newest-first convention handle same-day same-task rows cleanly (this commit is the test case).
9. Audit how many open TODO rows duplicate already-shipped work (this window proves at least one mint-vs-state race exists).
10. Consider a `tq doctor` check that flags RUNNING-adjacent anomalies like a task re-dispatched after its item checked off.

11–40. The 06-45 report's §f rows 4–40 stand as written (40 honest rows incl. 10 explicit refuses + 10 deliberately empty) — re-listing them here would be a duplicate ledger with two drift sources; that report is the canonical carry list for this task id, and this row intentionally counts as the pointer, not a gap.

41. Aspirational (ROADMAP): a "verify windows" metric in status reports — how many windows per task id end in verify-only, as a queue-health signal for mint quality.
42. Aspirational (ROADMAP): session-close smoke could assert the minted review payload's repo path points at the scratch repo (currently asserts counts only) — cheap, catches repo-blind dedup regressions early. (Deferred-Open items class from session-close design notes; needs the owner's multi-repo-close ruling first for the deeper version.)

## g) Questions I can NOT figure out myself

1. **Should the queue suppress re-dispatch of a TODO item that is already `[x]`?** The harvester's admission gates own pacing/budget/priority, but nothing checks item state at mint time. If suppression is wrong (re-verification is valued), then how often is re-verification WANTED vs accidental — is there an owner policy for verify-window frequency?
2. **For repeat dispatches, do you want full a–g reports or a short verify note?** This window wrote the full form for comparability with 06-45; a ruling would settle the format for all future verify windows.
3. **Is the general "gate-vs-cited-one-off" ruling still wanted** (which ad-hoc cited stub runs must be promoted into gates), now that this leg is the precedent that "cited one-off only" TODO rows get gate legs on first dispatch?
