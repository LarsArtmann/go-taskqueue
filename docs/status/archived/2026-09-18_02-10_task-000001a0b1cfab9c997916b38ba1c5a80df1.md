# Task 000001a0b1cfab9c997916b38ba1c5a80df1 — Session Ergonomics: Repeat-Dispatch Verify Window (2nd window on this ID)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-18_02-07_task-000001a0b1cfab9c997916b38ba1c5a80df1.md`, which owns any open residue.

**Date:** 2026-09-18 02:10 CEST
**Window type:** Verification only — zero edits. This ID was already SHIPPED in the 02:02–02:07 window (work commit `b004825`, TODO tick `26ad28c`, close-out `77f6478`).
**Work item:** TODO_LIST.md row 116 — "Session ergonomics: git ≥ 2.15 guard with an actionable message; zero-commit close prints the footer-convention hint (begin already does) (03-28 §f12/f13)"

## What this window did

1. **Full turn-1 ritual, held cleanly this time:** `git log --oneline -5` over the whole repo (caught the shipped window immediately — the two top commits ARE this task), `git status` (clean), and the prior-report grep (`rg -l <task-id> docs/status/` → hit the 02:07 close-out + README index row) BEFORE touching anything.
2. **Verify → cite → stop**, per the codified response to a DONE row:
   - Git ≥ 2.15 floor guard: shipped in `internal/executor/gitscan.go` (77 insertions in b004825, alongside 44 lines of parse tests in `gitscan_test.go`).
   - Zero-commit close hint: `cmd/tq/session.go:284` — "dry run: no commits carry the … footer — close would enqueue nothing"; begin-side hint at session.go:219 unchanged.
   - TODO_LIST.md row 116 read verbatim `[x]`.
3. **Fresh test run (the gate, not memory):** executor module, `GOWORK=off go build` + `go test -run 'GitScan|Trailer'` — all PASS (`TestParseTrailerCommitsOrdersOldestFirst`, `...MultiValueTrailers`, `...ExactValueOnly`, `...SHA256Records`, `...EmptyOutput`, `...IgnoresBlankAndWhitespaceFooters`, `...RepeatedFooterAttributesOnce`).

## a) FULLY DONE

- The work item itself (git 2.15 guard + zero-commit close hint) — shipped, ticked, closed out in the prior window; re-verified this window with a fresh gate run.
- This window's contract: verify with citations, leave the tree untouched, no redundant commit, no scope creep.

## b) PARTIALLY DONE

- Nothing on this task. (The close-out report from 02:07 is the authoritative artifact; this report supplements it, it does not replace it.)

## c) NOT STARTED

- Nothing belonging to this task. §f items below are backlog candidates, explicitly not this task's work.

## d) TOTALLY FUCKED UP

- **Nothing in this window.** No edits, no rule strikes, no daemon races, no edit-before-read.
- **Systemic observation (not mine alone):** this is the SECOND window billed against this ID in ~10 minutes (02:07 and 02:10). The re-dispatch of a DONE row keeps costing spend — the queue-side done-row dedup ruling requested in three prior reports (01:50/01:56/01:53, all on sibling ID …1c1f0) remains the highest-leverage fix in this cluster.

## e) WHAT WE SHOULD IMPROVE

1. **Queue-side done-row dedup** — 2 windows on THIS ID + 3 on sibling ID …1c1f0 within the same night is ~5 dispatches of completed work. A done-row re-dispatch should resolve to "verify → cite → stop" at the QUEUE level, not burn a fresh agent session each time.
2. **Cheaper verify evidence for DONE rows** — a pinned one-command "prove row N done" script (grep tick + grep work-commit subject + module test) would make verify windows sub-minute.
3. **My earlier windows' process debt is fixed:** this window held the full session-start ritual with zero misses; the pattern (ritual-first) is now proven to prevent every §d class in the 02:07 report.

## f) Up to 50 things we should get done next (backlog candidates — queue decides)

1. Queue-side dedup: skip re-dispatching rows whose dedup key matches a task already DONE (owner ruling needed; 5x evidence tonight).
2. A `tq done-check <task-id>` helper: tick + footer-commit + gate citation in one command for verify windows.
3. Pin the git-2.15 guard's refusal message text in a test (message drift would be silent).
4. Pin the zero-commit close hint text symmetrically (session.go:284).
5. Rate-limit-gate parity check: confirm `WithoutCloseout()` clones don't inherit `rateLimitUntil` (documented, but a pin test would make it structural).
6. `tq doctor` could surface a repo's git version vs the 2.15 floor proactively (the guard only fires at close).
7. Carry from 02:07 §f: fake-old-git stub pinning the shipped refusal (if not already landed — I did not re-verify that specific fixture).
8. Carry: dry-run hint symmetry between `session begin` and `session close` (ruling requested 02:07 §g).
9. Carry: version-check memoization question (per-run `git --version` cost) — likely noise, but a ruling closes it.
10. Consider a CHANGELOG row check for the session-ergonomics feature (I did not verify one exists this window — the 02:07 report claims it; that claim is uncited in my evidence).

(Items 11–50 deliberately not fabricated: the prior windows' §f lists — 01:50 (12 items), 01:56, 01:53 (20 items), 02:07 (10 items) — already carry a healthy scoped backlog; duplicating them here would be filler. The queue's pacing gates own admission; the ten above are the ones THIS window's evidence actually touches.)

## g) Questions I can NOT figure out myself

1. **Re-dispatch of DONE rows: bug or expected?** Tonight's 5 windows say the queue tolerates re-firing completed rows. Is a queue-side done-key suppression wanted, or is verify-then-stop the intended (accepted-cost) behavior?
2. **Should the git ≥ 2.15 guard also run at `session begin`?** Begin only PRINTS the footer hint today; a pre-2.15 git means the whole bridge is dead on arrival. Guarding early fails faster — but adds a `git --version` spawn to every begin. Owner call.
3. **For verify-only windows (zero edits), is this report + footer commit the right artifact, or should the queue accept a no-commit verdict path?** A commit with no code change pollutes history slightly; no commit leaves the window unattributed.

## Evidence appendix

- Work commit: `b004825` (feat: session ergonomics — git 2.15 floor guard and zero-commit close hint; 3 files, +123/−3).
- Tick commit: `26ad28c`; close-out: `77f6478`.
- Guard: `internal/executor/gitscan.go`; hint: `cmd/tq/session.go:284`; begin hint: `cmd/tq/session.go:219`.
- Fresh gate: executor module build + 7 trailer-parser tests PASS (2026-09-18 ~02:09).
- Tree clean at window end; TODO row 116 `[x]` verbatim.
