# Verify Window — `tq session list` + `tq session close --dry-run` (task 000001a0ad05d15bb890363a8fcf259f6829)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-17_03-45_task-000001a0ad05d15bb890363a8fcf259f6829.md`, which owns any open residue.

**2026-09-17 03:49 · repeat-dispatch verify-only window · verdict: already DONE, verified**

## What this window did

The queue re-dispatched the same work item that shipped in the 03-45 window
(commit `5fd7935`, close-out
`docs/status/2026-09-17_03-45_task-000001a0ad05d15bb890363a8fcf259f6829.md`).
Per the AGENTS.md repeat-dispatch ruling, the fastest correct response is
verify → cite → stop. No source changes were made; no new work commit exists
(the footered implementation commit from the prior window stands).

## a) FULLY DONE

1. **Prior-report grep at turn 1** — `rg -l <task-id> docs/status/` found the
   03-45 close-out immediately (the ritual miss of prior windows, fixed here).
2. **TODO row confirmed checked** — TODO_LIST.md:110 is `[x]`.
3. **Implementation confirmed on disk** — `internal/session/session.go`
   (`session.List`) and `cmd/tq/session.go` (`session list`, close
   `--dry-run`) both exist; help-text usage line carries `list`.
4. **Tests re-run green this window** — `cd internal/session && GOEXPERIMENT=jsonv2
   GOWORK=off go test ./... -count=1` → `ok ... 0.047s` (cites the actual gate
   run, not the 03-45 report's claim).
5. **Footer attribution confirmed** — `git log` shows `5fd7935` immediately
   below the prior close-out, matching the report's citation.
6. **No empty commit fabricated** — zero-diff verify windows do not earn a
   work commit; only this report carries the footer.

## b) PARTIALLY DONE

1. **Verification scope** — I re-ran the internal/session suite only, not the
   cmd/tq module gate or the full `./scripts/ci-local.sh`. Defensible for a
   zero-diff window (the prior window ran `./scripts/test-cmd-tq.sh` + the
   help-text smoke green), but the re-verify covered the library half only.
2. **Residue follow-ups** — the 03-45 report's §b/§f items (CHANGELOG line,
   dry-run replay notice, registry-blind `session list`) remain open in that
   report only; none were carried into TODO_LIST.md as backlog rows this
   window (see §e3).

## c) NOT STARTED

Nothing scoped to this task beyond the residue above — the work item itself
was complete before this window began.

## d) TOTALLY FUCKED UP

1. **CONTRIBUTING.md skipped at turn 1 AGAIN** (documented recurrence:
   00-52 d4 → 02-17 d3 → 03-45 d1 → now). I read it mid-window (03:49),
   after the first tool batch — nothing task-relevant (standard fork/PR
   flow), but the rule is the ritual. `git stash list` also skipped in the
   turn-1 batch (ran it only when preparing this report; it was empty).
2. **The 03-45 window's heredoc violations went unpunished by me** — I
   verified the shipped code but did not re-flag or normalize anything; not
   actionable now (code is clean, gofmt+tests green), but the pattern class
   stays open (see §e2).

## e) WHAT WE SHOULD IMPROVE

1. **Turn-1 ritual still not mechanical** — the same-ID grep finally
   happened this window and saved the entire session (it converted a would-be
   duplicate implementation into a 4-minute verify), which is the strongest
   argument yet for making it a script, not a memory exercise (third report
   in a row asking for §f6 below).
2. **Heredoc discipline needs a structural guard** — asking politely has
   failed four windows running; a hook or skill-level rule would end it.
3. **Close-out §f follow-ups should be harvested into TODO_LIST.md by the
   closing window itself** — the 03-45 report listed 10 real follow-ups; none
   became backlog rows, so this verify window had to re-discover them by
   re-reading the report. (I did not backfill them this window either — see
   the contract: append only NEW items discovered in THIS run's work, and
   re-listing another report's §f is mechanical carry-over that belongs to
   the docs-health pass; flagged here instead of silently done.)
4. **Repeat-dispatch detection could be cheaper** — the queue re-sent a DONE
   item ~4 minutes after its close-out landed. A queue-side done-row
   short-circuit (03-45 §e5) would have skipped this window entirely.

## f) Up to 50 things to get done next

Capped at 10 per precedent; items 1–10 mirror the 03-45 report's §f (still
real, still unaddressed — restated here so this report is self-contained;
the authoritative wording lives there):

1. Add the CHANGELOG entry for `tq session list` + `--dry-run`.
2. Surface replay state in `--dry-run` (export a closed-check from
   internal/session; print "close would be a replay (dedup holds)").
3. Design decision: registry-aware `session list` mode for sessions that
   never wrote journal facts.
4. Extend `scripts/smoke/session-close.sh` to exercise `session list` and
   `--dry-run`.
5. Owner ruling: amend maneuver for daemon footer-less commits carrying task
   code (03-45 §d3; `d786e99` case).
6. Turn-1 ritual script (`scripts/session-start.sh`) so the
   CONTRIBUTING/stash-list misses become structurally impossible.
7. `session list --json` for scripting/pool integrations.
8. `session list --all` including closed sessions with closed-at timestamps.
9. `tq crush` help text: one-line doc check that the wrapper subsumes
   `--dry-run` semantics.
10. Postgres session-close E2E smoke (list/dry-run ride it when it lands).

## g) Questions I cannot answer myself

1. **Done-row short-circuit**: can the queue mark a work item DONE at
   dispatch time so repeat dispatches like this one are never minted (or are
   minted as explicit verify-only dispatches)?
2. **Backlog harvesting ownership**: should the closing window convert its
   own report's §f items into TODO_LIST.md rows, or does that stay with the
   docs-health pass? Every report asks for the same follow-ups and none land.
3. **Verify-window verification depth**: for a zero-diff repeat dispatch, is
   re-running the targeted module suite sufficient, or is the full
   `./scripts/ci-local.sh` expected even when nothing changed?
