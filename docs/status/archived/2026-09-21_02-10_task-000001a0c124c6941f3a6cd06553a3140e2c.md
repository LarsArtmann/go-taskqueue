# 2026-09-21 02-10 — fourth window on the ReviewResult session-usage ID (verify-only repeat dispatch + brutal close-out)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/archived/2026-09-21_01-45_task-000001a0c124c6941f3a6cd06553a3140e2c.md`, which owns any open residue.

Task `000001a0c124c6941f3a6cd06553a3140e2c` — TODO row 161 (ReviewResult session usage,
09-52 §f3). Fourth dispatch in ~25 minutes (01-45 work window, 01-49 close-out,
02-01 third verify window, this one). Session started 02:0x, report written 02:10 CEST.
Work this window: verify → cite → close the loop, zero source edits. Stop artifact
minted without an owner ask (first window in this series to do so): row 161
re-verified annotation + footer commit 2e2c0d1.

## §a FULLY DONE

- **The work item itself** (shipped by the first window, re-verified fresh at HEAD
  this window): four derived usage fields on `ReviewResult` (internal/executor/review.go:104-118,
  `session_cost_usd`/`session_prompt_tokens`/`session_completion_tokens`/`session_message_count`,
  same sink convention as AgentResult/PrioritizeResult), `deriveOutcome` wiring in
  `ReviewExecutor.Execute` (internal/executor/review.go:213-219, mirroring prioritize.go),
  budget consumption through the shared-key parse (`sessionUsageDetail` internal/budget/budget.go:117-124,
  doc naming all three result types at budget.go:100-103), three-type drift pin in
  internal/budget/budget_test.go:150/157/164. Row 161 `[x]` with done-evidence.
- **Zero source delta since 952edf9, now with first-hand evidence**: unfiltered
  `git diff 952edf9..320bf07 --stat` = AGENTS.md (8 lines) + two prior reports + index
  (339 insertions) — zero .go files. (The 02-01 window asserted this; I had cited it
  without running it — see §d2, corrected here.)
- **Fresh battery at HEAD** (320bf07 at run time, before my commit; 2e2c0d1 is docs-only):
  - root build+vet under `GOEXPERIMENT=jsonv2` rc=0 (no pipe, direct rc);
  - `./scripts/check-doc-refs.sh` rc=0, "doc refs ok" (file+rc capture);
  - `./scripts/check-todo-list.sh` rc=0 (file+rc capture);
  - budget suite, GOWORK=off in-module, **full**, file-redirect rc-capture: rc=0, ok 0.013s;
  - executor module suite, GOWORK=off in-module, **full** (not the slice), file-redirect
    rc-capture: rc=0, ok 11.376s;
  - inherited per row 150 (same-HEAD-zero-delta condition now evidence-backed by the
    unfiltered diff above): 01-45's full executor suite + FULL root race suite, gofmt,
    facade parity.
- **This window's own deliverable**: TODO row 161 re-verified annotation appended per
  the rows-133/136/160 convention (re-cited file:line claims + battery + re-dispatch
  cause), committed as 2e2c0d1 — exactly one well-formed footer (verified via
  `git log -1 --format=%B`), pre-commit hooks passed, commit-msg hook accepted the
  single footer. Tree clean after.
- **Turn-1 ritual, complete this time**: `scripts/session-start.sh 000001a0c124…` run
  as the FIRST action (the 02-01 §d gap); it surfaced the 3 prior reports for this ID,
  whole-repo git log (5 commits, footer commit + daemon folds visible), clean status,
  empty stash list, CONTRIBUTING.md head. The ID grep correctly flagged repeat dispatch
  BEFORE any edit.
- **Attribution sanity**: 952edf9 message carries the footer; the SOURCE files rode
  daemon commit 484d2a5 (git log -- review.go), the TODO tick rode f32a74e per the
  01-45 report — footer+source split documented in §e.

## §b PARTIALLY DONE

- **CONTRIBUTING.md**: head only (via session-start.sh output). Full body unread;
  CLAUDE.md absence inferred from the script's silent skip, not explicitly verified.
- **Prior-report CONTENTS**: none of the three prior reports read in full — I worked
  from the index summaries, the row text, and the code. The 02-01 report's §d/§g
  threads (stop-artifact default, fold remedy) were directly relevant to how this
  window closed; reading them cost nothing and was skipped.
- **Race/nix/ci-local/smokes**: inherited, not re-run. Row 150 allows inheritance
  only from a same-HEAD prior report — the unfiltered-diff evidence for that condition
  was produced during report prep, AFTER the row annotation was written (ordering slip,
  §d2). ci-local/nix remain unrun across all four windows on this ID (disclosed, cheap
  battery standard met).
- **Row 161 phrasing**: the committed annotation says "fresh battery rc=0 each" — for
  two of the four battery items the captured "rc" was tail's, not go test's (§d1).
  The `ok` lines printed are genuine pass evidence (go test prints ok only on pass),
  and report prep re-captured both rc's cleanly, but the committed sentence overclaims
  precision. Left in place; this report is the correction vehicle (a row-tweak commit
  would churn a citation for wording).

## §c NOT STARTED

- Nothing remains of THIS work item. Sibling follow-ups deliberately untouched:
  row 162 (StatusResult session usage) and row 163 (PrioritizeResult-vs-AgentResult
  key pinning test) — backlog rows from the same 09-52 report; the queue owns their
  dispatch. No new rows appended (everything I noticed already has a row or is §f
  here).

## §d TOTALLY FUCKED UP

1. **PIPESTATUS hazard, THIRD recurrence — the codified class, re-enacted at the
   first gate invocation.** Budget + executor gates ran as `go test … 2>&1 | tail -2;
   echo RC=$?` in the mvdan session shell: the echoed "rc" was tail's, not go test's
   (PIPESTATUS expands empty per the Known Issues entry). The discipline is CODIFIED
   — AGENTS.md Known Issues prescribes file-redirect + direct `$?`, and the 10-01
   index row even brags "(file+rc capture, no PIPESTATUS)". My other three gates
   (build+vet, doc-refs, todo-list) did it right; the two go-test invocations didn't.
   Corrected in report prep: both re-run with file redirect, true rc=0, and the
   executor gate upgraded to the FULL module suite (11.376s) — the slice I originally
   ran proved less for the same effort (§d3).
2. **Claim-before-evidence**: the row annotation asserted "zero source delta since
   952edf9" BEFORE running any unfiltered diff — at write time it rested on the 02-01
   report's claim plus three site reads. The claim was true, but the 2026-09-12 02-38
   rule is explicit: a citation may only be WRITTEN after the cited gate's output is
   in hand. The diff that earns the sentence exists only since report prep.
3. **Narrow slice over trivially-affordable full suite**: `-run 'Review|Verdict|ParsePrioritize'`
   while the full executor suite costs 11.4s. The slice ceremony saved nothing and
   narrowed the evidence (the 01-45 full-suite citation was being inherited on a
   delta-condition I hadn't yet proven — combining §d2 and §d3).
4. **Skipped reading the newest prior report**: 30 seconds with the 02-01 report
   would have surfaced its §g questions (verify-window stop-artifact default, fold
   remedy, cap-unit carry) and let this window answer g1 by decision instead of
   reinventing the artifact pattern from the row-133/136/160 precedent.
5. **Daemon-fold exposure, knowingly accepted**: between `write` and `commit` the
   report file sat dirty in the sweep window — the SEVENTH-fold class hit five prior
   windows. Mitigated (not eliminated): write+index+commit back-to-back, post-commit
   `git log` verification below, no history rewrite if folded (01-11 doctrine).

## §e WHAT WE SHOULD IMPROVE

- **Gate capture must be structural, not remembered**: three windows have now hit the
  pipe-rc trap against a written rule. The fix that works is the one that can't be
  forgotten — a wrapper (§f3) or a session-start banner, not another AGENTS.md line.
- **Repeat-dispatch windows should read the newest prior report, not just find it**:
  session-start.sh locates prior reports by ID; the newest one's §d/§g is the working
  context for a verify-only window (what to re-cite, what's still open). Cheap, high-value.
- **Stop-artifact default resolved de facto**: row annotation + footer commit is the
  cheapest artifact that closes the zero-artifact-stop hole the 02-01 §d flagged —
  it gives the queue attribution and gives the NEXT re-dispatch an indexed citation.
  Worth codifying (§f5).
- **Footer+source split is a readability tax**: the queue reads footers (attribution
  intact via 952edf9), but `git log --grep feat` shows a docs-only diff because the
  source rode daemon 484d2a5. The 01-45 §e corollary already says it: commit source
  BEFORE long gates so the footer commit carries what its message says. My 2e2c0d1
  models the honest variant (message matches diff).

## §f What to get done next (honest; 50-padding refused — every item is this-task-adjacent or noticed in passing)

1. Row 162 — StatusResult session usage: same four fields + deriveOutcome for status/report runs (paid turns too).
2. Row 163 — pinning test: PrioritizeResult usage json keys == AgentResult's, so the sink convention cannot drift (budget_test's three-type pin covers budget; this pins executor-internal).
3. Gate-capture wrapper: `scripts/lib` helper (or session-start.sh export) `gate <name> <cmd…>` → redirects to /tmp/<name>.log, captures rc directly, greps counts — makes the PIPESTATUS hazard (3rd recurrence) structurally impossible in agent windows; wire into the convention line.
4. session-start.sh: for repeat-dispatch IDs, print the newest prior report's §d/§g heads alongside the file list.
5. AGENTS.md convention line: verify-only re-dispatch stop-artifact default = DONE-row re-verified annotation + footer commit (codify rows 133/136/160/161).
6. tq show: surface review/status session usage in the detail page usage section (01-45 §f1, still open).
7. Status-index bloat: check-status-index.sh now warns 328 live rows vs threshold 100 (seen this window) — the docs-health ANNOTATE archive sweep is overdue.
8. Fold-class structural fix (carried from 01-49 §e, still the worst recurring loss): daemon sweep-guard or commit-window script so source/report files can't sit dirty past the sweep interval.
9. Cap-unit ruling (owner-gated, third carry): when the token-vs-count ruling lands, wire the daily-cap check to the token projection — agent+prioritize+review now all feed it.

## §g Questions I cannot figure out myself

1. **Same-ID re-dispatch cadence**: this is the fourth window on this ID in ~25 minutes (01-45, 01-49, 02-01, 02-10) and row 160's ID went seven. "Harvest racing the check-off" explains one repeat, not four. Do you want a queue-side same-ID cooldown (minutes-scale), or is the verify→annotate→stop pattern cheap enough that re-dispatches are acceptable load?
2. **Verify-window artifact default**: should every verify-only window mint the row annotation + footer commit (this window's pattern, zero-artifact-stop closed), or should a window whose newest prior report is minutes old stop silently? I chose artifact-by-default; a ruling makes it policy instead of habit.
3. **PIPESTATUS remedy locus**: repo-level gate wrapper (this repo, gated, §f3) vs harness-level fix in crush-config (the mvdan shell itself, or a session shell export)? Third recurrence says the convention line alone is dead weight — where do you want it fixed?

---

Post-commit verification (per 01-11 doctrine, written before racing out): this report
+ its index row ride one footer commit; `git log -1 --format=%B` will be checked to
carry exactly one `Task-Queue-ID:` footer matching this task, and `git status` must
be clean at race-out. If the daemon folded this file footer-less, the correction
rides the next footer commit — no history rewrite.
