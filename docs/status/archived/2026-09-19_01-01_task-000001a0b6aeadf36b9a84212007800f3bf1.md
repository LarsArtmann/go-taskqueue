# Second window on the tq crush doc-gap task (re-dispatch verification) — task 000001a0b6aeadf36b9a84212007800f3bf1

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-19_00-49_task-000001a0b6aeadf36b9a84212007800f3bf1.md`, which owns any open residue.

2026-09-19 01:01. The queue re-dispatched the SAME work item ("Document
`tq crush` in README (short subsection under session triggers) +
FEATURES.md row (02-54 §f)") that window one closed at 00:49 (work commit
7c0d8b4, close-out 0d168bc). This window did the turn-1 ritual, caught the
DONE state before any action, and executed verify → cite → stop with ZERO
edits to the tree. Docs-only task; no Go code touched in either window.

## a) FULLY DONE

1. **Re-dispatch caught at turn 1.** The task-ID grep
   (`rg -l 000001a0b6aeadf36b9a84212007800f3bf1 docs/status/`) surfaced the
   00-49 close-out in the FIRST tool batch, before any edit attempt; TODO
   122 read `[x]` in the same batch. No duplicate docs were written, no
   empty commit was created (a re-dispatch failure class sibling windows
   have hit).
2. **Deliverables re-verified fresh, by citation (claims carry citations):**
   - README.md:310-331 — `### Session triggers: \`tq crush\`` subsection
     read in full this window: wrapper purpose, two invocations, id
     resolution order, exit-code passthrough + signal forwarding, no-id
     no-op guarantee, remaining flags, both hook-based trigger
     alternatives.
   - FEATURES.md:102 — the 🟢 `FULLY_FUNCTIONAL` wrapper row beside the
     session-bridge row, citing the cmd/tq/crush_test.go pins.
   - TODO_LIST.md:122 — `[x]` verbatim with the DONE note naming both doc
     artifacts.
   - Work commit 7c0d8b4 — `%B` + `--stat` re-checked: exactly one
     well-formed `Task-Queue-ID:` footer (this task's id), exactly three
     files (FEATURES.md +1, README.md +23, TODO_LIST.md row), no stray
     content. Close-out self-review shipped as 0d168bc.
3. **Fast doc gates re-run green at HEAD this window** (independent of the
   first window's runs): `check-todo-list.sh` ok ("no unblocked
   owner-gated items"), `check-doc-refs.sh` ok, `check-features-roadmap.sh`
   ok ("seed cross-check ok").
4. **Prior close-out read IN FULL before writing this report** — its
   §b/c/f/g were mined so this report carries rather than re-derives; its
   two NEW follow-up AGENTS.md lines were re-checked against the file
   rather than assumed.
5. **Follow-up state re-verified this pass** (cheap greps, so §c below is
   current truth, not the 00-49 snapshot): TODO rows 109/123/124/125/157/
   230/40 still open; the first window's proposed AGENTS.md daemon-tell and
   cmd/tq-gopls-phantom lines still ABSENT from AGENTS.md; README still has
   no Contents anchor for the new H3; the `…` placeholder still sits at
   README.md:318.

## b) PARTIALLY DONE

1. **The full test battery was cited, not re-run.** The 00-49 report
   captured `go test ./... -count=1 -race` exit 0 + `scripts/test-cmd-tq.sh`
   green at this exact HEAD (tree clean at 0d168bc since); this window
   re-ran only the three fast doc gates and cited the rest. Defensible —
   the point-in-time rule says re-run before CHANGING code, and no code
   changed — but the 00-38 window's execution-bar lesson has a verify-window
   variant I did not apply: the cheapest cited gate (root `go build`, ~30s)
   could have been executed fresh. Citation covered the expensive battery;
   it should not excuse running nothing.
2. **Turn-1 ritual was partial.** Task-ID grep + git log/status/stash all
   landed in batch one (correct); the CONTRIBUTING.md read did not land at
   turn 1 at all — see §d1.
3. **The first window's two §g rulings remain unanswered** (row-126 gate
   bar; FEATURES row flip). Nothing this window can do about them; re-asked
   in §g.

## c) NOT STARTED

Verified open this pass; nothing NEW was noticed in passing (the verify
surface was two docs, one TODO row, and one commit — there was no code to
trip over):

1. **FEATURES `tq session begin/close` gap text** — still says "trigger
   automation not wired" although triggers #1 (registry) and #2 (wrapper)
   shipped 2026-09-17. Pending the §g2 flip ruling (first-window §c1,
   carried).
2. **Committed wrapper-path smoke leg** (TODO row 126) — pending the §g1
   gate-vs-citation ruling. Still the strongest test gap on this surface.
3. **go 1.27.1 go.mod alignment sweep** (TODO row 230) — claims
   `check-go-mods.sh` red at HEAD since 09-17; I did NOT re-verify tonight
   (out of scope; cheap for whoever takes the row).
4. **Live-no-id smoke assertion** for the wrapper (TODO row 123).
5. **First-window §e1/§e2 AGENTS.md lines** (daemon-tell; cmd/tq gopls
   phantoms) — re-verified ABSENT tonight; still unowned harvest
   candidates.
6. **Doc-hygiene bundle** (first-window f7-f10, plus its §b1 date-stamp):
   README Contents anchor for the H3 (verified missing), `…` → concrete
   string (verified still at README:318), command-map pointer to the new
   subsection, design-doc link from the README subsection.
7. **TODO row 125** — check-gosec.sh should assert a pre-existing GOSEC_BIN
   is actually v2.29.0 (noticed in passing in the row dump).
8. **TODO row 40** — gosec gating owner ruling (owner-blocked).
9. **TODO rows 109 / 157** — SessionEnd hook adoption (blocked upstream on
   crush #3146) and daemon-fold attribution ruling (blocked owner, 4th+
   consecutive ask).

## d) TOTALLY FUCKED UP

Nothing shipped is broken — there were NO edits this window, the tree is
clean, all gates green, the work commit's footer verified. The honest
failures are process, and both are COUNTED recurrence classes:

1. **CONTRIBUTING.md skipped at turn 1 — again. N+7 in the counted series**
   (00-22 counted N+6; 00-38 landed it in batch 2; tonight: fully skipped
   until 00:59, during close-out prep). Zero damage THIS window — it was
   verify-only, and CONTRIBUTING's CI-gate content could not have changed a
   single decision — but that is exactly the excuse that will eventually be
   wrong: the ritual is either mechanical or it is a prayer. The fix
   already exists as TODO 128 (session-ritual script); it needs to ship,
   not be re-asked. That this miss keeps recurring across ≥7 windows while
   its remedy sits in the backlog is the actual indictment.
2. **qmd misselection — N+3 in its counted series** (00-22 and 00-32 both
   logged qmd dead-ends on repo docs). First tool call used `mcp_qmd_get`
   for an in-repo file; it failed ("context canceled") and `view` recovered
   in the same round. One wasted call; the class is "reach for the search
   index for a file whose path I already had". In-repo reads go through
   view, full stop.
3. Nothing else: no bad edits (none existed), no masked pipelines (gates
   run bare, outputs captured), no uncited claims (every §a item carries a
   file:line or gate name).

## e) WHAT WE SHOULD IMPROVE

1. **Ship TODO 128 (session-ritual script).** The counted miss series
   (CONTRIBUTING now N+7, qmd N+3) ends only when turn-1 reads are
   mechanical, not memorized. Highest-leverage process item in this
   report; its recurrence evidence now spans at least seven windows.
2. **Codify the two first-window AGENTS.md lines** (daemon-tell after own
   edits; cmd/tq gopls phantoms ⇒ trust scripts/test-cmd-tq.sh) —
   re-verified absent tonight. One line each; nobody has picked them up
   since 00-49 proposed them.
3. **Verify-window execution bar**: citation covers the expensive battery,
   not the cheap gate. A verify→cite→stop window should still execute the
   cheapest cited gate (root `go build` here) so the citation chain ends in
   tonight's own output somewhere. Pencil rule: if a cited gate runs in
   under a minute, run it.
4. **Done-row re-dispatch cost (systemic, multi-window ask).** TODO 122 was
   `[x]` with a same-ID close-out, yet the queue re-dispatched; tonight is
   one more instance of the pattern sibling windows have pushed a
   queue-side dedup ruling for. Each occurrence burns a window on pure
   verification. Either build the done-row short-circuit or rule that the
   cost is accepted — the current limbo is the worst option.
5. **Report policy for verify windows** (see §g3): tonight's report
   necessarily overlaps the 00-49 close-out; the 03-35 precedent logged a
   ~70%-overlap duplicate as "one wasted index slot". A dated append to the
   existing report would carry the same information at a fraction of the
   index cost — but that is an owner call, not mine.
6. **Keep refusing the 50-item padding.** §f has 16 honest items. The
   first window's §e6 said it; it stays true. Inflating the list to hit a
   quota would be noise dressed as thoroughness.

## f) NEXT — ranked (16 honest items, not 50; "tracked" = a TODO row owns it, NEW = harvest candidate)

1. **Ship TODO 128 session-ritual script** — ends the N+7 miss series —
   High / S / Process (tracked, row 128)
2. **Committed wrapper-path smoke leg** (`tq crush --bin <stub child>`,
   wired into ci-local after session-close.sh) — pending §g1 — High / M /
   Test (tracked, row 126)
3. **FEATURES session-bridge gap-text rewrite** — pending §g2 flip ruling —
   High / S / Documentation (NEW, blocked)
4. **go 1.27.1 go.mod alignment sweep** (row 230; re-verify the red claim
   first) — High / M / Build (tracked)
5. **Queue-side done-row dedup ruling** (§e4) — High / M / Process
   (owner; NEW)
6. **AGENTS.md daemon-tell line** (first-window §e1) — Medium / S / Process
   (NEW)
7. **AGENTS.md cmd/tq gopls-phantom line** (first-window §e2) — Low / S /
   Process (NEW)
8. **Doc-hygiene bundle as ONE window**: README Contents anchor, `…` →
   concrete prompt string, command-map pointer to the subsection,
   design-doc link, date-stamp on the "unreleased" claim (first-window
   f7-f10 + §b1) — Low / S / Documentation (NEW)
9. **Live-no-id smoke assertion** (row 123) — Medium / S / Test (tracked)
10. **check-gosec.sh GOSEC_BIN version assert** (row 125) — Medium / S /
    Build (tracked)
11. **check-gosec.sh cmd/tq coverage** (row 124) — Medium / S / Build
    (tracked)
12. **Re-verify crush #3146 state** before any doc repeats "unreleased"
    (row 109 cadence) — Low / S / Documentation (tracked)
13. **SessionEnd hook adoption** when #3146 lands — Medium / S / Feature
    (tracked, row 109, blocked upstream)
14. **Daemon-fold attribution ruling** (Crush-Session footers from the
    daemon) — High / M / Feature (tracked, row 157, blocked owner, 4th+ ask)
15. **CLI gosec gating ruling** (row 40) — Medium / S / Build (tracked,
    blocked owner)
16. **Re-verify the row-230 red claim** (`check-go-mods.sh` at HEAD) as a
    standalone cheap probe so §c3 stops carrying an unverified "still
    true?" — Low / S / Build (NEW)

Items 1-5 are the real queue-order candidates; 6-8 are one-window
micro-fixes; 9-16 are tracked or blocked.

## g) QUESTIONS (3; the first two are carried from the first window — both remain unanswered and both gate §f items)

1. **(carried, gates §f2/row 126)** Gate-vs-cited-one-off for the wrapper's
   E2E: does `tq crush` need a COMMITTED ci-local smoke leg before more
   wrapper surface accrues, or are the unit pins + the cited live
   stub-child run a permanent sufficiency standard? I cannot set that bar.
2. **(carried, gates §f3)** May the FEATURES `tq session begin/close` row
   flip/rewrite now that 2 of 3 triggers shipped (registry + wrapper), or
   does "when trigger automation lands" mean specifically the in-crush
   SessionEnd hook (#3146)? I can draft either text; only the owner can say
   which is honest.
3. **(carried from the 01-53 sibling window's "report policy for verify
   windows")** For a verify-only re-dispatch of a DONE row: is a fresh
   indexed report wanted each time, or should the verification be a dated
   append to the EXISTING same-ID report (row considered closed at first
   DONE)? Tonight's report duplicates the 00-49 close-out by construction;
   only the owner can weigh the audit trail against the index cost.
