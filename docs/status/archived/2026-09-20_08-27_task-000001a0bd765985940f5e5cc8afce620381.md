# Task 000001a0bd765985940f5e5cc8afce620381 — Capture-shape regression pin (third dispatch)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_08-21_task-000001a0bd765985940f5e5cc8afce620381.md`, which owns any open residue.

- **Window:** 2026-09-20, ~08:25–08:27 CEST
- **Work item:** TODO_LIST row 152 — regression pin for the row-133 capture shape (every `for m in …` loop in ci.yml + scripts/ci-local.sh fed by a `mods="$(./scripts/for-each-module.sh)"` capture).
- **Verdict: DONE (by a prior window). Third dispatch on this ID. Verify → cite → stop.**

## a) FULLY DONE

- The pin exists, is wired, and the TODO row is `[x]` (TODO_LIST.md:152) with a full DONE annotation. Primary work: commit ad85c09 (pin script + ci-local wiring); prior reports: docs/status/2026-09-20_08-21 + 08-24 (same task ID). No code touched this window.
- **This window's fresh battery at HEAD (a68153e), NOT inherited:**
  - `./scripts/check-module-loop-capture.sh` → rc=0 (ci-local.sh loops=4/captures=1, ci.yml loops=6/captures=6).
  - `./scripts/check-script-syntax.sh` → rc=0 (0 shellcheck findings).
  - `./scripts/check-doc-refs.sh` → rc=0.
  - Root `go build ./...` + `go vet ./...` under GOEXPERIMENT=jsonv2 → rc=0.
- **Fresh delta: NONE exists.** HEAD is byte-identical to the 08-24 window's HEAD (a68153e — the verify-only close-out commit), and that report already re-ran the same gates at this exact HEAD with identical loop counts. No source, wiring, or TODO change has landed between the two windows; there is nothing new to observe. Stated explicitly per the verify-window minimum battery row (TODO_LIST.md:150).

## b) PARTIALLY DONE

- Nothing under this work item.

## c) NOT STARTED

- Nothing under this work item. The 08-24 §f follow-ups (evidence-drift re-annotation pin, --self-test negative fixture, regression-pin indexing home) remain open in the backlog discussion of the prior report; none were mine to start this window.

## d) TOTALLY FUCKED UP

- Nothing broken. Honest critique:
  - This is the **third** dispatch on a terminal-fact task within ~30 minutes (08-21, 08-24, 08-27). The verify-only shape was again the correct response, but the queue's re-dispatch guard rows (TODO 182, 191) remain open, so each repeat still burns a window. I did not invent a new follow-up — the fix is already backloged and owner-gated.
  - Ritual: I re-ran the pin gate before grepping prior reports in a single batch, but I did read prior reports before writing anything, so no claim here rests on memory. Minor: my first battery message ran the gates in parallel with the ritual grep instead of the ritual first — converged correctly, again by construction of the gates rather than by discipline.

## e) WHAT WE SHOULD IMPROVE

1. The re-delivery guard (TODO 182/191) keeps graduating in urgency: three windows on one done task is the strongest signal yet — but it stays owner-gated, correctly.
2. Small observation in passing: ci-local.sh's loop count is now 4 vs the 08-21 report's 3 — the pin's R1/R3 rules absorb that drift silently and correctly, which is the design working; the DONE annotation on row 152 cites loops=3/1 and is now one count stale. Cosmetic only; the gate itself is the living truth.

## f) NEXT (honest count: 0 new)

- Nothing appended. Every follow-up this window could name is already backloged (TODO 182, 191) or noted in the 08-24 §f list. Padding this to 50 would be fabrication; per the citation row, refusing to pad is the correct output.

## g) QUESTIONS (owner-gated, ≤3)

1. **Duplicate-claim policy:** ratify verify-only as the sanctioned repeat-dispatch shape (TODO 182)? I cannot rule this myself.
2. Should the row-152 DONE annotation's loop counts (loops=3) be re-annotated on drift, or is the gate's live output the only truth that matters (evidence-freeze vs living-gate)?
3. Is a third verify report on the same HEAD worth an index row each time, or should repeat dispatches at unchanged HEAD skip the report and emit only the queue verdict?
