# Status — task 000001a0b20b2decdd8e46ded4221dcb43f9 (5th dispatch, 2026-09-18 03:13)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-18_03-07_task-000001a0b20b2decdd8e46ded4221dcb43f9.md`, which owns any open residue.

Verify-then-close re-dispatch (third verify window on a DONE row). The full
turn-1 ritual was held this time (git log over the whole repo, `git status`,
`git stash list`, `rg -l <task-id> docs/status/`, CONTRIBUTING.md — present
and honored) and the DONE row was caught at turn 1, before any edit attempt.

## a) FULLY DONE

- **The task's work item itself** — shipped in a prior window, re-verified
  this one with fresh instruments:
  - `crush` in the cmd/tq subcommand list: AGENTS.md:138.
  - Wrapper note in the session-close bridge bullet: AGENTS.md:353 and the
    dedicated `tq crush -- <crush args>` paragraph at AGENTS.md:372-373.
  - TODO_LIST.md:119 is `[x]`.
  - Work commit d6f5ad0 carries the exact Task-Queue-ID footer.
  - Cited per the claims-carry-citations rule (AGENTS.md Conventions).
- **This window's own deliverable**: this report + its docs/status/README.md
  index row, committed with the same footer.

## b) PARTIALLY DONE

- Nothing. The work item is binary (doc entry present or not) and it is
  present. No partial state exists to report.

## c) NOT STARTED

- Nothing in scope. All follow-up candidates live in §f as backlog rows,
  deliberately not started under this task's contract.

## d) TOTALLY FUCKED UP

- **Nothing this window** — the cleanest window on this task ID so far.
  The turn-1 prior-report grep was NOT skipped (the recurring miss codified
  into the session-start ritual after 09-12); it is what produced the
  verify→cite→stop outcome within one tool round.
- Systemic, not this window: this is the **fifth dispatch on this task ID**
  (03-06 ship via d6f5ad0, 03-07 and 03-11 verify reports, this one) and at
  least the seventh DONE-row re-dispatch tonight across task classes
  (02-10, 01-37, 01-20 reports document the same pattern on sibling IDs).
  The queue has no done-row short-circuit; every re-dispatch costs a full
  agent turn plus a report, for zero delta.

## e) WHAT WE SHOULD IMPROVE

1. **Queue-side done-row dedup** — before dispatching, the pool should
   check the TODO row / dedup-key state and skip (or downgrade to a cheap
   verify) tasks whose `[x]` row + footer commit already exist. Five
   dispatches on one ID is a queue bug, not an agent diligence problem.
2. **Verify-window report volume** — three reports now exist for this
   single doc-gap fix. A fixed report skeleton for verify-only windows
   (a-e in ten lines, f carried, g carried) would cut the noise the index
   is accumulating.
3. **README.md index bloat** (4th sighting this week): rows are added, none
   retire. Archive done 2026-09-1x reports with citation repointing
   (check-doc-refs.sh guards the move).

## f) NEXT (grounded, ≤50 — carried + deduped against 03-07/03-11)

In priority order; items 1-5 carried verbatim from the 03-11 report (nothing
new surfaced this window to displace them), 6-9 from 03-07:

1. Fix the internal/status ULID same-millisecond ordering flake
   (TestSweepPinsCloseoutReportPaths, TestSweepNthCompletionMintsOneReport).
2. Queue-side re-dispatch guard for `[x]`-completed items (the d-1 root
   cause; three windows of evidence on this ID alone).
3. BLOCKED-annotate the owner-gated gosec cmd/tq TODO row the
   check-todo-list gate keeps failing.
4. docs/status/README.md index bloat: archive done 2026-09-1x reports with
   citation repointing + archive counter update.
5. AGENTS.md subcommand-list parity script pinning the `cmd/tq` table row
   against registered subcommands (the doc-gap this task fixed existed
   because nothing compared them).
6. One-command verify-gate helper for verify-only windows (build+vet+race +
   the right module gate chosen automatically) so verify windows stop
   burning moves on wrong-gate invocations (01-20 §d pattern).
7. CHANGELOG row for the tq crush doc fix window (d6f5ad0 is docs-only;
   confirm CHANGELOG policy covers docs-only ships or record the ruling).
8. Consider archiving the 03-07/03-11 twins for this task ID together with
   this report once the queue closes it (single repoint sweep).

Items 10-50 deliberately not fabricated: no further grounded candidates
surfaced in this window, and padding an evidence report to an arbitrary
count violates the repo's evidence-over-volume rule.

## g) QUESTIONS (owner-only — carried, still unanswered)

1. Should a repeat dispatch on a task whose footer commit already exists
   auto-close at the queue layer (no agent turn at all), or is the
   verify-then-stop turn the intended behavior?
2. Is the internal/status ULID flake owner-aware, or should the next window
   treat it as its primary fix target?
3. For the gosec cmd/tq TODO row: BLOCKED-annotate now, or wait for the
   owner ruling requested in the 03-07 report?

## Verification

Docs-only window: no code changed, so no code gates were run and none are
claimed. Fresh turn-1 instruments this window: `git log --oneline -5`
(HEAD aec1ef8), `git status` clean, `git stash list` empty, `rg -n crush
AGENTS.md` (lines 138/353/372), `rg "crush.*subcommand" TODO_LIST.md` (row
119 `[x]`), `rg -l <task-id> docs/status/` (03-07 + 03-11 reports, both
indexed in docs/status/README.md).

Format note: user dispatch asked .md; the status-report skill's HTML default
is overridden for this report only (same note as the 01-20 report).
