# Task 000001a0c1929f2175bfeb1135f1b45a5718 — second window, verify→cite→stop repeat dispatch — close-out

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-21_04-07_task-000001a0c1929f2175bfeb1135f1b45a5718.md`, which owns any open residue.

Window: 2026-09-21 ~04:10–04:17 CEST · HEAD at dispatch AND at every gate run: `aaeae32` (docs-only close-out commit of the first window) · tree clean at start, between gates, and at the (attempted) stop commit.

**Verdict up front: REPEAT DISPATCH, verify → cite → stop.** The item (row 163, f5-extended usage rendering) was implemented and `[x]` before my first tool call; the first window on this ID (04-07) already did the heavy verification and wrote the canonical report. This window re-verified every claim first-hand at the NEW head `aaeae32` (the 04-07 battery ran at `4b30826`; two commits landed since, both docs-only), ran a fresh battery, appended a re-verify annotation to the row, and filed the three backlog rows the 04-07 §f items had never become.

## a) FULLY DONE

All claims re-read at file:line at HEAD `aaeae32` by THIS window:

1. **`tq show` half**: `resultDetail` decodes the completion fact per task type — status case cmd/tq/main.go:2266-2269, prioritize case :2270-2273 (agent :2258, review :2262); the show payload marshals `Result any` wholesale (cmd/tq/main.go:1949-1956), so the `Session*` fields surface via the shared json tags (`internal/executor/status.go:84-87`, `internal/executor/prioritize.go:93-96`; same keys as AgentResult/ReviewResult — the drift pin budget_test already owns).
2. **webui half**: `statusReportCard` usage line internal/webui/fragments.templ:706-708; `prioritizeResultCard` with usage at :722-724, both wired on the detail page at :649-654; shared `sessionUsageEmpty`/`sessionUsageText` helpers internal/webui/components.go:98-113; usage-less results render no line.
3. **Pins exist and were executed fresh** (not just read): `TestResultUsageRendersOnDetailPage` (internal/webui/webui_test.go:624) and `TestResultDetailDecodesTypedResults` (cmd/tq/main_test.go:352).
4. **This window's fresh battery at `aaeae32`, all rc=0** (file+rc capture, no PIPESTATUS): root build rc=0 + vet rc=0 (`GOEXPERIMENT=jsonv2`), `scripts/check-doc-refs.sh` rc=0 ("doc refs ok"), internal/webui targeted pin rc=0 + FULL suite `ok 10.094s` (GOWORK=off in-module), `scripts/test-cmd-tq.sh` ok 8.126s (includes the decode pin).
5. **Backlog hygiene**: the 04-07 report's §f1/f2-f3/f4 items existed nowhere in TODO_LIST.md — appended as three unchecked rows in the Priority-system follow-ups section (e2e show-JSON wire pin; SessionMessageCount blind spot + rendering decision; CHANGELOG/FEATURES check for the rendering work). No duplicates created; rows 164/165 were already tracked and were NOT re-filed.

## b) PARTIALLY DONE

- Nothing of the item. The window's own stop artifact initially risked being footer-less (§d1) — repaired in-window.

## c) NOT STARTED

- Nothing within the item's scope. All remaining usage-surface work is tracked: rows 164 (json-key pin), 165 (score-cache TTL) plus this window's three new rows, and the carried owner questions below.

## d) TOTALLY FUCKED UP

1. **The daemon fold hit AGAIN — now sub-minute.** The row-163 re-verify annotation was swept into footer-less `d9c3a0a chore: auto-commit 1 changed file(s)` in the seconds between my edit and my add+commit chain (the chain then correctly refused: nothing to commit). The "commit immediately after the LAST write" doctrine is NECESSARY but NOT SUFFICIENT — the daemon's sweep latency is shorter than one tool-call round trip. Attribution still resolves via this footer commit (report + index + new rows), and no history rewrite was performed; the fold-remedy ask (sweep-guard/lockfile) is the most-repeated §g item in this family and stands.
2. **`tq show <task-id>` of the queue record skipped again** — the 04-07 §d2c miss repeated verbatim; the "why am I here" answer came from the report grep, not the queue. Read-only and safe under the inherited `TQ_DB`, still skipped.
3. **Item-text grep came second.** session-start.sh surfaced the prior report by ID (good), but the cheapest DONE signal (`rg <item text> TODO_LIST.md`) should have been command #2, not part of the verification pass — the 04-07 §d3 ask, half-adopted.

What this window did right, for the record: turn-1 ritual complete (session-start.sh first action, whole-repo git log/status/stash, CONTRIBUTING.md via the script, prior-report grep by ID BEFORE any edit); zero source edits; every prior claim re-read AND re-executed before the DONE verdict; battery captured to files with direct `$?` (no PIPESTATUS).

## e) WHAT WE SHOULD IMPROVE

1. **Accept that edit→commit will lose the daemon race at current sweep latency.** Until the owner rules on the fold remedy, the practical pattern for one-line annotations is: write the annotation AND the report AND the index row in one tool burst, then ONE add+commit chain — which is what this window did — and treat a folded annotation as expected, healed by the footer commit's content. Stop burning windows on surprise.
2. **Verify-only windows on an already-reported ID can carry LESS paper**: with a canonical report on file, a second window needs the fresh battery + annotation + this compact close-out, not a full re-derivation. This report is the template.
3. **The §f-to-backlog handoff has a gap**: the 04-07 report listed three actionable rows that never became TODO_LIST lines. Convention fix: when a verify window's §f has agent-executable items, file them in the SAME commit as the report (done here).

## f) New follow-ups

Filed as rows this window (see a5): e2e show-JSON usage wire pin; SessionMessageCount render/empty-check decision; CHANGELOG/FEATURES check for the rendering work. Nothing else new — rows 164/165 and the owner-level asks (§g) carry the rest; 04-07 §f5-f22 stand by pointer. Padding refused.

## g) Questions carried (owner rulings, unchanged from 04-07 §g)

1. Re-dispatch policy for `[x]` rows (now N≥3 same-day datapoints across three IDs).
2. Footer semantics for zero-source-delta windows — this window again used the 02-10 pattern: annotation + footer commit carrying docs only; the CODE's footer lives on the original implementation commits.
3. gopls cmd/tq phantoms treatment; plus the daemon fold-remedy ruling (most-repeated ask in the family).

---

Battery evidence (this window, /tmp logs): build+vet rc=0/rc=0, check-doc-refs rc=0, webui targeted rc=0 + full ok 10.094s, cmd/tq gate ok 8.126s. Expensive tier (race/nix/ci-local/smokes) consciously skipped: zero source delta since the 04-07 full battery, per the verify-window inheritance clause; docs-only commits since are visible in `git log --stat`.
