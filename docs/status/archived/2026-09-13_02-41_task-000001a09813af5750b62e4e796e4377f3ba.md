# Toolchain-Drift Dispatch-3 Verification Close-Out (Brutal a)-g)

*(Archived 2026-09-13 docs-health pass: window complete: go-directive drift repaired (1cc32d6) re-verified; TODO row 117 [x]. Point-in-time snapshot — re-verify before treating any claim as current.)*

- **Task**: `000001a09813af5750b62e4e796e4377f3ba` (TODO_LIST row 117)
- **Dispatch**: n=3 for this task ID. n=1 (02:19 report; work commits
  `1cc32d6` + `bdd2ad5`) completed the item; n=2 (02:31 report, `74904e2`)
  re-verified zero-diff. This dispatch found the item **DONE-on-arrival**
  again, ran independent re-verification, and changed **zero deliverable
  files** — this report plus its index row are the only diff.
- **Window**: 2026-09-13 ~02:35–02:45 CEST
- **Verdict**: work item complete within agent authority. The single open
  criterion ("next master ci.yml run green") remains blocked on the owner
  push: origin/master is still at `b7aa049` (pre-fix), local master is
  **ahead 6** (`6560c74`, `108a4b5`, `1cc32d6`, `bdd2ad5`, `a50e0e8`,
  `74904e2`), and the newest ci.yml run on master is still the red
  34726154600. Pushing is contractually forbidden to me ("Never push") and
  is marked owner action in the row's own DONE annotation.

## a) FULLY DONE (all verified this dispatch, first-hand, unmasked exits)

1. **go.mod alignment**: `internal/journal/go.mod` and `internal/task/go.mod`
   both declare `go 1.26.7`, matching root (head over all three files).
2. **`./scripts/check-go-mods.sh` green** — `GOMODS_EXIT=0` with a clean
   `> log 2>&1; EXIT=$?` capture. This is the exact CI step that failed run
   34726154600.
3. **Fixed-module gates green**: `internal/task` and `internal/journal`
   `GOWORK=off` build + vet + `go test -count=1` — both **EXIT=0**, `ok`
   lines in the logs. The first loop attempt printed EXIT=1 — that was MY
   redirection bug, not a module failure (§d1); the re-run with fixed log
   paths is the evidence.
4. **Root module gate green**: `GOEXPERIMENT=jsonv2` build + vet +
   `go test ./... -race -count=1 -timeout 120s` — **ROOT_GATE_EXIT=0, 15
   packages ok, zero FAIL lines** (exit captured before any pipe; counts
   grepped from the raw log).
5. **Footer forensics re-verified first-hand this dispatch** (n=2's §a5
   accepted until now): `git log -1 --format=%B` over `1cc32d6`, `bdd2ad5`,
   `a50e0e8`, `74904e2` — exactly one well-formed
   `Task-Queue-ID: 000001a09813af5750b62e4e796e4377f3ba` footer each.
6. **AGENTS.md guard in place**: Known-Issues bullet at AGENTS.md:427
   (never lower a module go directive; zero-dep leaves have no floor; the
   `go mod edit` restore recipe; run check-go-mods.sh before the daemon
   sweeps).
7. **TODO row 117 closed correctly**: `[x]` with a DONE annotation carrying
   root cause (T38–T40 down-alignment; `go mod tidy` cannot re-floor a
   zero-dep leaf), the falsified suspect theory, the guard pointer, and
   gate evidence — including the honest push caveat.
8. **Both prior close-outs indexed**: `docs/status/README.md` carries
   exactly one row each for the 02-19 and 02-31 reports (grep count 2 total,
   1 per file) — the daemon-folding hole has not swallowed either.
9. **CI forensics**: `gh run list --workflow ci.yml --branch master` — the
   newest run is still 34726154600 (failure, 2026-09-12T23:43Z, executed
   against `b7aa049`, i.e. BEFORE the fix). `git fetch` + `git status -sb`:
   ahead 6, origin/master at `b7aa049`. No green run can exist until the
   push happens.
10. **CONTRIBUTING.md read in full** (74 lines) — ci-local is the pre-push
    gate (N/A: I never push); individual gates for isolation; dprint
    on-demand and non-gated for docs per the AGENTS.md decision. Timing sin
    logged in §d3.
11. **No work commit**: zero-diff dispatch → no empty commit (AGENTS.md
    rule, n=2 §a9 precedent); TQ_RESULT re-emits the work line verbatim
    citing the work commit `1cc32d6` per the 01-45 ruling.

## b) PARTIALLY DONE

1. **"Next master ci.yml run green"** — the only criterion I cannot close.
   Blocked on the owner push of the ahead-6 stack. The failing step is
   deterministic and green locally on the exact gate; the FULL run
   all-green is still an expectation, not a verification, and the row's
   annotation stays honestly split until a real green run exists
   (then: annotate row 117 with the run ID — trail §f1).
2. **Full `find internal -name go.mod` test loop skipped-declared**: this
   dispatch changed zero files, and check-go-mods.sh covers directive
   health across ALL modules (the actual drift class). n=2 §b2 precedent.
3. **nix build not re-run**: n=1 ran it green including the vendor-hash
   check; the change is directive-only; the tree is byte-identical since
   n=2's verification (clean status, no new commits). Skipped-declared.
4. **ci-local.sh not run** — skipped-declared: its first step
   (`scripts/check-ci.sh`) refuses while master is red on this very drift;
   running it to watch it refuse adds nothing.

## c) NOT STARTED

Nothing from the work item proper — all five deliverables existed on
arrival from dispatch n=1 and were re-verified in a1–a7. The push (b1) and
the row-117 green-run annotation are push-gated work outside my authority.

## d) TOTALLY FUCKED UP (this dispatch)

1. **Redirection-failure EXIT corruption — a NEW variant of the false-exit
   class, invented minutes after reading the 02-31 report's §d1/§e1.** My
   first module-gate loop wrote its log to `/tmp/$m.log` with
   `$m=internal/task`; `/tmp/internal/` did not exist, so the redirection
   failed BEFORE the build ran and the loop reported "internal/task
   EXIT=1" for two perfectly healthy modules. Not pipeline masking this
   time — redirection masking — but the same genus: the captured exit
   measured my shell, not the gate. I caught it same-command (the missing
   log file in the tail gave it away) and re-ran clean, but the first
   evidence line was garbage and would have shipped as a false "module
   broken" claim had I written the report from it. Sixth-plus occurrence
   of the genus across agents in 24h; the §e1 gate.sh/hook fix must cover
   redirection failures too, not just `| tail` pipelines.
2. **The per-dispatch close-out convention is convention-by-inference, and
   I bet against it — and lost.** n=1 and n=2 each wrote a close-out; I
   judged a third same-ID report to be spam against the repo's own
   report-flood concern (TODO row 139), shipped zero repo trace, and
   planned to close with TQ_RESULT only. The owner's explicit report
   request for this dispatch shows the expectation held and was not mine
   to waive. Cost: without the explicit ask, dispatch n=3 would have left
   no durable trace in the repo. The fix is not "Lars asks every time" —
   it is one written rule (§e2).
3. **CONTRIBUTING.md read mid-session, not at turn 1.** The contract says
   "read AGENTS.md (and CONTRIBUTING.md / CLAUDE.md if present) first". I
   read it before any gate run and before any report work — but after the
   go.mod checks, the fetch, and the gh queries. Third consecutive
   dispatch flagged on this exact line (02-19 §d2, 02-31 §d2; they cite
   00-19 and 01-38). Mine is the mildest form — only read-only status
   checks preceded it — but "first" means first, and I re-demonstrated
   that intent alone does not survive turn-1 momentum.
4. **The task-ID-first index grep was not my first action.** 02-31 §e2
   proposes grepping `docs/status/README.md` for the task ID as tool call
   #1; I reached the same dedup information via `git log --stat` plus an
   `ls | rg` two calls later. Coverage equivalent, latency not, ritual
   unadopted the very dispatch after it was proposed.
5. **Commit-metadata claims trusted before verified.** n=2's §a5 footer
   forensics was accepted on report authority while I re-ran every code
   gate; I only re-ran the footer grep while preparing THIS report (a5 —
   all clean, so no harm done). AGENTS.md's claims-carry-citations rule
   does not carve out metadata; the re-verify habit must not either.
6. **Todos tool unused this dispatch** — noted for completeness. For a
   verify-only window the list would have been two items, and the 02-31
   §d4 sin was FICTIONAL todos, not absence; still, done-on-arrival is
   exactly when a re-scoped two-item list (verify / close-out) would have
   made §d2's deviation visible to myself before the owner saw it.

## e) WHAT WE SHOULD IMPROVE

1. **Exit-capture enforcement, widened motivation**: my §d1 variant proves
   the false-exit class is bigger than `| tail` pipelines — redirection
   failures and any post-hoc `$?` capture measure the wrapper, not the
   gate. `scripts/gate.sh` (run + tee + emit the true exit) still fixes
   the whole genus; a Crush hook that flags `$?` captured after ANY pipe
   or redirect catches both variants at authoring time.
2. **Codify the close-out-per-dispatch rule**: one AGENTS.md line —
   "every dispatch of a queue task writes `docs/status/<ts>_task-<id>.md`,
   including zero-diff re-dispatches" — ends the §d2 debate and removes
   the owner from the loop. If volume is the worry, pair it with a
   same-ID supersede marker (§f19) rather than leaving the rule implicit.
3. **Task-ID-first dispatch ritual** (carry, 02-31 §e2): grep the status
   index for the ID as the first tool call — dedup + primary-source
   routing for one grep. I half-followed it by accident (§d4); put it in
   the dispatch contract next to the CONTRIBUTING line.
4. **"Skipped" stays a declared verdict** (carry, 02-31 §e3): b2–b4 are
   written as skipped-with-reason; keep that mandatory so scope honesty
   survives compression.
5. **Push-pending as a first-class state** (carry, 02-31 §e4): this
   report's header states the exact ahead-6 stack once, loudly. The
   recurring nightly shape is "fix green locally, master red, agent
   cannot push" — it deserves a name and one place to look.
6. **Provenance in minted rows** (carry, 02-19 §e/§g3, 02-31 §e5): this
   dispatch again started from the row summary; rows that embed evidence
   pointers (file + section) would route to primary sources by default.

## f) Things we should get done next (impact-ordered; brainstorm, not commitment)

Provenance: [C] carried from this task's own trail (02-19/02-31 §f, with
their numbering noted where stable), [N] new this dispatch. No unrelated
research — everything below was touched by or noticed during THIS window.

1. **[C, #1, STILL TOP] Owner: push the ahead-6 stack** (`6560c74`,
   `108a4b5`, `1cc32d6`, `bdd2ad5`, `a50e0e8`, `74904e2`) — flips the
   go.mod health step green on the next ci.yml run; then confirm the FULL
   run all-green (not just that step), and annotate row 117 with the green
   run ID to close its last criterion for real. Stack grew by `74904e2`
   since n=2's list.
2. **[N] Codify close-out-per-dispatch** (§e2/§d2): one AGENTS.md rule so
   the third dispatch does not have to guess the convention.
3. **[N, extends C #5] gate.sh + hook design covers redirection-failure
   exits** (§e1/§d1): the false-exit class now has two observed variants
   (post-pipe `$?`, failed redirect before the command); the mechanical
   fix must match the genus, not the first instance.
4. **[C, #2] Dispatch-time dedup in the queue minter** — this dispatch is
   n=3 on one task ID; the night's redundant-dispatch total keeps climbing.
   Every redundant run is a full verification window burned.
5. **[C, #3 + §e3] Task-ID-first dispatch ritual into the contract** —
   grep `docs/status/README.md` for the ID as tool call #1.
6. **[C, #4] TQ_RESULT sha semantics for re-dispatches** — settled in
   practice (01-45 ruling: re-emit work line verbatim, cite the work
   commit; followed here), still not written where the minter/queue can
   enforce it.
7. **[C, #5] Exit-capture enforcement** (now item 3's vehicle):
   `scripts/gate.sh` wrapper and/or the Crush pre-tool-use hook.
8. **[C, #6] f26 canonical-ID owner ruling** — unchanged, still blocks the
   status-index trailer cross-check going from WARNING to fail.
9. **[C, #7] Load-phase-sync barrier for stress-evidence jobs** (01-45/
   01-23 carry: background "load" was mostly its BUILD phase).
10. **[C, #8] Guard soak test — the AGENTS.md guard still has no real
    trial**: watch the next 2–3 windows for any go-directive lowering.
    n=3 confirms the text is in place; nobody has yet TRIED to lower a
    directive under it.
11. **[C, #9] Pre-commit hook rejecting go-directive edits that mismatch
    root** (humans/agents path; the daemon bypasses hooks — hence #12).
12. **[C, #10] Daemon post-commit checker** — chore commit touches a
    go.mod → run check-go-mods.sh, surface drift immediately; closes the
    daemon's own path that #11 cannot.
13. **[C, #11] check-go-mods.sh FAIL output should print the restore
    recipe and the zero-dep-leaf caveat** — the next recurrence becomes a
    30-second fix instead of a forensic session.
14. **[C, #12] Commit 19-04's blame classifier into scripts/** (still
    /tmp-only; dies on reboot).
15. **[C, #13] CONTRIBUTING-read-at-turn-1 ritualization** (§d3, newest
    datapoint): a hook, a contract reorder (it is currently instruction
    #1 but reads like boilerplate), or the read receipt pattern — pick a
    mechanical answer.
16. **[C, #14] Codify the literal CI-condition stress-load matrix** as a
    script (01-15/01-23/01-45 carry).
17. **[N] Same-ID report-volume policy** (§e2's flip side; feeds row 139):
    n=3 on one ID = three indexed reports for one deliverable set. If the
    per-dispatch rule stands, add a supersede marker for zero-diff
    re-dispatch reports (or fold their §a into the row annotation) so the
    index does not grow one full row per redundant run forever.
18. **[N] `gh run list` default output hides workflow identity** (noticed
    in passing; 02-31 §d5 near-miss formalized): green dependency-graph
    "Update" runs sit next to the red ci.yml run at the same timestamp —
    one `--workflow ci.yml` habit note in scripts/check-ci.sh (or a
    comment in AGENTS.md's CI section) prevents the next "CI green" misread.
19. **[C] TODO rows 120/121**: templ-components `KanbanBoard` +
    `PageProps.SEO`/`icons.Render` evaluations — open pool food, noticed
    again while reading row 117's neighborhood.
20. **[C] Two sqlite noctx OLD sites** (19-04 #6, one line each) — carried
    untouched.

## g) Questions I cannot figure out myself

1. **Push policy**: is autonomous push now sanctioned for agent sessions
   (TODO row 142 asks the same)? Until it is answered, this item's last
   criterion can NEVER be closed by an agent no matter how many times the
   queue re-dispatches — the fix is deterministic and green on the exact
   failing gate, but the evidence run requires the owner's push of the
   ahead-6 stack.
2. **Close-out convention**: is a per-dispatch report mandatory for
   zero-diff re-dispatches of an already-reported task? This dispatch
   defaulted to "no" (two same-ID reports existed) and the owner's request
   implied "yes" — which is the rule I should encode in AGENTS.md?
3. **Queue dedup**: the queue re-dispatched a row that was already `[x]`
   in TODO_LIST with two indexed same-ID close-outs — is that minter dedup
   gap visible/fixable from inside this repo at all, or is it queue-side
   infrastructure outside my reach? If queue-side, should push-blocked
   rows carry an explicit "PUSH-PENDING" state so the minter can park them?
