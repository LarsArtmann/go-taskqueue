# Verification-Claims Guidance — n=2 Same-ID Close-Out (brutal a)-g)

*(Archived 2026-09-13 docs-health pass: window complete: verification-claims guidance re-verified; TODO row 123 [x]. Point-in-time snapshot — re-verify before treating any claim as current.)*

_Task 000001a0987864f31177fd994d17309fe80d · 2026-09-13 03:58 · verification-only dispatch_

**HEADLINE: DONE-on-arrival, n=2 on this task ID.** The work item was already
completed and closed out in this very session's work window (deliverable
fa93087, close-out 12dbb47 + report `2026-09-13_03-55_task-000001a0987864f31177fd994d17309fe80d.md`).
This dispatch ordered a second close-out for the same ID. Zero deliverable
changes made. This report is the ordered n=2 verification pass, not new work.

## Independent re-verification (first-hand this turn)

| Claim                                     | Evidence (re-ran / re-read just now)                                                                                                                                                                                               |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Deliverable exists and is substantive     | `docs/planning/2026-09-13_verification-claims-guidance.md` re-read: rule, govulncheck root+sqlite cautionary tale (GO-2026-5970 in `internal/queue/postgres`), 4 required scope elements, good/bad examples, partial-scope honesty |
| TODO row 123 `[x]` with DONE note         | `TODO_LIST.md:123` re-grepped — checked, DONE 2026-09-13, deliverable named                                                                                                                                                        |
| Work commit carries the dispatch footer   | `git log --format=%B -1 fa93087` → exactly 1 `Task-Queue-ID:` line, well-formed                                                                                                                                                    |
| Close-out commit carries it too           | `git log --format=%B -1 12dbb47` → exactly 1 footer (report 12dbb47 indexed in `docs/status/README.md:17`)                                                                                                                         |
| Root gate green (scope: ROOT MODULE ONLY) | `GOEXPERIMENT=jsonv2; go build ./... && go vet ./... && go test ./... -count=1` → GATE_EXIT=0, 15 ok / 0 FAIL, clean capture (no `head`/pipe masking)                                                                              |

Scope honesty per the very guidance this task shipped: the gate above covers
the root module only. No `internal/*` sub-module gate was run — justified
because the deliverable and all changes this session are docs-only (`git status`
clean before this report; zero Go files touched by the work turn, verified by
diff at work time and re-confirmed: the only commits are the two docs commits).

## a) FULLY DONE

- Work item (TODO row 123): guidance doc shipped (fa93087), TODO checked,
  close-out committed (12dbb47). All re-verified first-hand this turn.
- n=2 re-verification itself (this report): independent gate re-run, footer
  count checks on both commits, deliverable re-read.

## b) PARTIALLY DONE

- Nothing on the deliverable. Verification scope is partial BY DESIGN and
  declared: sub-module gates, `-race`, ci-local, and nix gates were not
  re-run for a zero-code session; master-CI state was not re-checked via
  `gh` this turn (the work window verified its own gate; re-proving CI is
  owner-surface, and the change is docs-only).

## c) NOT STARTED

- Nothing within the dispatch scope. The carried §f/§g items from the n=1
  close-out (03-55) remain open and are carried below rather than duplicated.

## d) TOTALLY FUCKED UP

1. **The dispatch itself, again**: this is at least the FIFTH redundant
   same-ID dispatch cluster this window (03-44/03-47/03-49 on task …4e3,
   03-20/03-28/03-40 on task …b22e, now 03-55/this on …80d). The
   dispatch-time footer dedup guard remains unminted — harvesters keep
   re-dispatching checked items. Every n≥2 session burns a full close-out
   cycle (gate re-run + report + index row + commit) to conclude "already
   done".
2. **Same-ID close-out unread at turn 1** (3rd occurrence of the class): the
   03-55 report and its index row existed before I started; I found them
   only mid-turn. The turn-1 ritual (`grep docs/status for task ID`) was
   prescribed in at least two prior close-outs and was still not applied
   before acting.
3. Minor: my work-turn final TQ_RESULT used `commit_sha: 12dbb47` (the
   close-out commit) while the deliverable commit is fa93087 — both carry
   the footer, but "commit_sha" ambiguity across n≥2 reports is exactly the
   ruling requested in the 03-40 §g2 (still unanswered).

No repo damage: tree clean, no reverts, no fixture debris, both footers
well-formed (commit-msg hook passed on all commits).

## e) WHAT WE SHOULD IMPROVE

- Make the turn-1 checklist MECHANICAL for close-out dispatches: grep
  `docs/status/README.md` for the task ID and `git log --grep` the footer
  BEFORE any other action; if found, the session is an n≥2 verification
  pass from step one (cheaper gate, capped report).
- Mint the dispatch-time dedup guard (harvest skips rows already `[x]` with
  a DONE note citing this task's ID) — root-cause fix for the whole cluster.
- TQ_RESULT commit_sha semantics for n≥2: state which commit the sha refers
  to (deliverable vs close-out) or emit both.

## f) Up to 50 things we should get done next (brainstorm; most are ROADMAP fuel — HARVEST with rigor)

**Owner-gated / rulings**

1. Mint the dispatch-time same-ID dedup guard (top item; kills d1 class).
2. Rule: when a task ID is re-dispatched, should the n≥2 session skip the
   report entirely and emit a superseding TQ_RESULT note instead?
3. Rule: TQ_RESULT `commit_sha` semantics for n≥2 (deliverable vs close-out).
4. Rule: one-line hardening CHANGELOG policy (carried from 03-44 §g1, unanswered).
5. Owner push of the ~16-commit unpushed master stack (carried every close-out
   this window; not re-counted this turn).
6. Backfill scope statements into pre-guidance DONE notes (sweep proposed in
   n=1 §f, unstarted).
7. AGENTS.md one-line pointer from the "Claims carry citations" convention to
   the guidance doc (M104's other half; skipped per dispatch scope in n=1,
   still open, 2-line fix).

**Process/tooling**

8. Add `grep docs/status/README.md <task-id>` to the codified turn-1 ritual
   (AGENTS.md session-start ritual section).
9. Decide the build-vs-tests gate scope rule for docs-only sessions once
   (n=1 §g2, unanswered): is a root build enough, or no gate at all?
10. Mechanical turn-1 file check (CONTRIBUTING.md/CLAUDE.md existence) in the
    session ritual — the N-times-unread-CONTRIBUTING miss (interactive
    loophole noted 03-28) still has no guard.
11. Consider a `tq session close` improvement: attribute daemon `chore:` commits
    to the session that produced the files (attribution gap, session-close
    bridge "Open" list).

**Carried from n=1 close-out (03-55 §f), unverified this turn, likely still open**

12. Guidance-doc backfill: annotate older DONE notes only as reports are
    touched, not a bulk rewrite (n=1's softer alternative to item 6).
13. STATUS-index lint for glued rows (03-40 fixed one instance; a checker
    rule could catch the class).
14. Prompt-template hard-coded-file check (n=1 §g3, ruling requested).

**Rest of the 50**: the remainder would be filler for a docs-only n=2
verification session. Honest cap here: 14 substantive items. The prior
close-outs' §f lists (03-55, 03-49, 03-47, 03-44, 03-40, 03-28, 03-20) already
carry ~80 open brainstorm items; re-listing them here duplicates without
progress — HARVEST should sweep THOSE lists, not this one.

## g) Questions I can NOT figure out myself

1. **Same-ID dispatch intent**: is the queue's repeated re-dispatch of
   completed task IDs deliberate (a verification regime) or a harvest dedup
   bug? Item 1/2 depend on the answer — if deliberate, I should codify an
   n≥2 cheap path; if a bug, only item 1 matters.
2. **Report cap for n≥2**: for a done-on-arrival verification pass, is the
   full brutal a)-g) report wanted every time, or a capped note + superseding
   TQ_RESULT? Five near-identical close-outs exist already.
3. **TQ_RESULT sha**: which commit should `commit_sha` name for a multi-commit
   task (work commit, close-out commit, or both)? The queue cross-reference
   currently sees both cited interchangeably across this window's reports.
