# Status Report — for-each-module.sh extraction task (000001a0a23f1bb20d2c1df9d06e4c071c1a)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-15_01-31_task-000001a0a23f1bb20d2c1df9d06e4c071c1a.md`, which owns any open residue.

**Date:** 2026-09-15 01:33 · **Scope:** THIS task only (TODO_LIST §"Review-window follow-ups" —
"Extract the disk-derived module loop into `scripts/for-each-module.sh` and consume it from
ci.yml (4+ copies) and ci-local.sh"). This was a re-dispatch: the work was DONE-on-arrival.

## a) FULLY DONE

1. **`scripts/for-each-module.sh` exists** — disk-derived module list (find go.mod → strip suffix
   → sort), 15 modules listed, exit 0 on a fresh run this turn (executor, internal/* ×8, facade
   dirs journal/queue/task/worker, root executor/queue/task/journal facade shims).
2. **ci.yml consumption ≥ 4 copies: 6 loops** at lines 38, 51, 89, 138, 220, 249 all run
   `for m in $(./scripts/for-each-module.sh)`.
3. **ci-local.sh consumption** — `mods="$(./scripts/for-each-module.sh)"` at line 38.
4. **TODO_LIST.md row 62 marked [x]** with a dated, cited closure note (done 2026-09-15,
   ba1657e, 6 ci.yml loops + 3 ci-local consumers) — by 1d015e8 in a prior window; re-verified
   fresh this turn.
5. **Worktree clean** before and after; no new deliverable changes needed, none made.

## b) PARTIALLY DONE

- **Fresh gate evidence this turn:** root `go build` + `go vet` + `go test ./... -count=1` all
  green, and the script itself was executed (not just grepped). NOT run: the full per-module
  loop gates (vet/test per sub-module) and ci-local — overkill for a verify-only re-dispatch of
  an already-shipped, already-gated extraction, but the honest label is "root gates + script
  smoke", not "full CI".
- **Report commitment:** this report + index row committed explicitly with the task footer
  (the daemon footer-race is a known issue; the work commit itself already carries the footer
  via the prior close-out window, 1d015e8/ba1657e lineage).

## c) NOT STARTED

- Nothing for this item — zero remaining sub-work. The item is closed in TODO_LIST.md.

## d) TOTALLY FUCKED UP (this window's honest misses)

1. **First turn's TQ_RESULT cited `ba1657e` as commit_sha with an empty files_changed list** —
   defensible for a DONE-on-arrival verification (no new commit), but the sha points at the
   daemon heuristic commit that happened to carry the files; the _closure_ commit is 1d015e8 and
   the original delivery the daemon folded. Ambiguous provenance, should have been explicit.
2. **Session-start ritual incomplete:** ran `git log` + `git status` but skipped
   `git stash list` on the first turn (added it this turn: empty). Recurring miss across the
   2026-09-15 window (multiple prior reports flag the same skip).
3. **First turn verified by grep only** — no script execution, no build/test. The second-turn
   run (script executed, exit 0, root gates green) is what a first turn should have done.
4. **This is the ~6th DONE-on-arrival re-dispatch in this window** (help-text ×3, literal-leak
   ×3, errname ×2, lint-annotations ×2, now this). Each dispatch burns a full verify+report
   cycle for zero deliverable change — the mint side has no DONE check before dispatch.

## e) WHAT WE SHOULD IMPROVE

1. **Mint-time DONE check:** before minting a work task, check whether the TODO row is already
   `[x]` (and re-dispatch only if the closure lacks gate evidence). Would have killed most of
   this window's dispatches.
2. **Verify-only dispatch contract:** a lightweight "verification-only" verdict (already used
   informally in several reports) should be a first-class queue outcome with a shorter report
   format — DONE-on-arrival does not need a 50-item §f.
3. **Ritual as hook, not memory:** the stash-list/whole-repo-log ritual keeps getting skipped;
   a pre-flight script (`scripts/session-preflight.sh`) or a Crush session hook would enforce it.
4. **TQ_RESULT provenance fields:** when no new commit exists, the convention needs an explicit
   "verified_at_sha" instead of overloading commit_sha with an ancestor.
5. **Report index self-service works** (row added in the same commit) — keep doing that; the
   daemon-fold hole only bites when the index row is skipped.

## f) Up to 50 things to get done next (grounded: direct tail from this task + standing backlog)

**Direct tail (from this task / this window's pattern):**

1. Mint-time DONE check at enqueue (see e1) — biggest spend saver in the queue itself.
2. `verify-only` verdict class in the queue contract (e2).
3. Session-preflight script wired into agent prompts / pre-commit (e3).
4. Script smoke gate: `for-each-module.sh` has no test; a `--self-test` fixture (expected count,
   sorted, no root-module dupes) would pin it like help-text's smoke.
5. Unify the golangci-lint version pin (TODO row 63 — same "single source" class as this item).
6. CI topology one-pager (TODO row 64) — this task's extraction makes it half-written already.
7. Dedup remaining disk-derived loops: grep ci-local.sh + CI for any `find .* go.mod` copies not
   yet routed through the script (quick audit; the 08:47 report counted 4+, all 6 ci.yml now
   consume it — confirm zero stragglers).
8. Re-dispatch spend policy ruling (queue-side cap: how many re-dispatches per item).

**Standing TODO_LIST backlog (unchecked, in file order):**

9. SystemNix host-side deploy + round-9 cutover (row 22, owner-run, BLOCKED).
10. Proxy check + clean-room `go install` for backend tags (row 26 tail, owner push auth).
11. `internal/consumer` wire-or-delete decision (row 27, BLOCKED owner intent).
12. Three near-identical FactSource interfaces — unify or document (row 28, BLOCKED arch call).
13. Postgres CLI store wiring `--store postgres://…` (row 29, BLOCKED release timing).
14. Dependabot/renovate policy for the 8-module tree (row 30, BLOCKED policy).
15. `tq pool-health` one-shot liveness summary (row 34, unblocked).
16. CQA bridge live-instance verification (row 36, BLOCKED needs live CQA).
17. gosec gate-vs-advisory ruling (row 40, BLOCKED owner call).
18. `task.AllStatuses` export vs pinned twin-list test (row 41, BLOCKED public-API call).
19. Red-master pool policy (row 42, BLOCKED pool contract decision).
20. Closeout-report placement (root floods ~10/day; row 46, BLOCKED owner call).
21. TODO-append caps in close-outs (row 47, BLOCKED backlog policy).
22. Ticket 000001a08915cf… phantom-footer disposition (row 48, BLOCKED convention).
23. Master push-authorization policy answer (row 49, BLOCKED owner).
24. Module-fetch trust / vendor-for-hermeticity call (row 50, BLOCKED owner).
25. Canonical ID for the f26 three-ID cluster (row 54, BLOCKED queue convention).
26. Session-snapshot ghost upstream report (row 65).

**Items noticed in passing this window (candidates, not yet TODO rows):**

27. for-each-module.sh outputs facade dirs AND internal dirs — consumers that want only
    _sub-modules_ filter downstream; a `--submodules-only` flag could tighten CI loops.
28. ci.yml's 6 loops each repeat the same inner body pattern; a reusable composite workflow
    step could cut further — only worth it if a 7th consumer appears.
29. Several 2026-09-15 reports repeat the same §d ritual misses verbatim — a digest row or
    weekly meta-report would surface systemic agent-process debt instead of per-report noise.
30. The status index is past the scannability-cadence slope (~14 unarchived rows today alone);
    schedule the next archive sweep before the 100-row bloat warning fires.
31. help-text smoke's §b gaps (self-test fixture, derived command inventory, ci.yml mirror,
    CHANGELOG row) are still open per its own index rows — pick them up before they fossilize.
32. lint-annotations 01-21 report's re-verification was narrower than claimed (no negative-case
    re-run) — a negative-case fixture for that gate is the same shape as item 4.

_(Items 27–32 are the honest direct tail; beyond that the standing backlog is the list. Filling
to 50 would be fabrication — ROADMAP fuel belongs in docs-health HARVEST, not invented here.)_

## g) Questions I cannot figure out myself

1. **Who owns mint-time dedup** — should the queue itself refuse to mint a task whose TODO row
   is already `[x]` with a cited closure, or is the harvester the right place? (This determines
   whether the DONE-on-arrival spend fix is a queue or harvest change.)
2. **Is a verify-only re-dispatch supposed to produce a status report at all**, or should those
   collapse into an index-row-only convention? Six DONE-on-arrival reports in one window is
   noise the index now carries forever.
3. **For this task specifically:** which commit is the canonical "delivery" citation for the
   cross-reference — ba1657e (daemon commit carrying the files), 1d015e8 (closure), or should
   the queue accept an ancestor range when the daemon folded the real work?

—
Verification this turn: `./scripts/for-each-module.sh` exit 0 (15 modules); root
`GOEXPERIMENT=jsonv2 go build ./... && go vet ./... && go test ./... -count=1` green; ci.yml
lines 38/51/89/138/220/249 + ci-local.sh:38 consume the script; TODO row 62 [x]; `git stash list`
empty; worktree clean before report.
