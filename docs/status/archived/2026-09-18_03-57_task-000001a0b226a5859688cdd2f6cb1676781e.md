# Lint-baseline repair task — SECOND window (verify → cite → stop, zero edits)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-18_03-49_task-000001a0b226a5859688cdd2f6cb1676781e.md`, which owns any open residue.

- **Task ID**: `000001a0b226a5859688cdd2f6cb1676781e`
- **Window**: 2026-09-18 03:5x (this report 03:57); first window = `2026-09-18_03-49_task-000001a0b226a5859688cdd2f6cb1676781e.md` (shipped the repair)
- **Verdict**: work item was **DONE on arrival**. This window re-verified the gate fresh and confirmed the prior window's deliverables. Zero code/doc edits to the work itself; only this report + its index row.

## a) FULLY DONE

1. **The work item itself** (first window, re-verified this window):
   - All 8 growth rows repaired; `.golangci-baseline.txt` seventh regen recorded in AGENTS.md (1113 findings / 134 rows).
   - Repair commit `9c39220` + status-report commit `4f6782a` on master.
   - cmd/tq tagliatelle path-excluded for `cmd/tq/(main|top|journalaudit)\.go` (wire payloads snake_case by contract, pinned by `stats_test.go`); sqlite varnamelen 32→33 corrected to clean-cache truth.
2. **Fresh gate verification (THIS window)**: `golangci-lint cache clean` then `./scripts/lint-baseline.sh --check` → **exit 0, "within baseline (1113 findings vs baseline 1113)"**, run WITHOUT pipes after noticing the first run's `| tail` masked the exit code (the exact trap TODO 201 warns about — caught it, re-ran clean).
3. **Prior-report grep (the citation core)**: `rg -l <task-id> docs/status/` found the 03-49 report; TODO_LIST.md:30 confirmed `[x]` verbatim; work commit footers confirmed via `git log --format='%(trailers)'`.
4. **Working tree clean** at session start and end — nothing to revert, nothing dangling.

## b) PARTIALLY DONE

1. **Session-start ritual**: git log/status/stash ran in the FIRST tool batch, but the **prior-report grep and CONTRIBUTING.md check did not** — they happened only after I had already run the baseline check. Nothing was harmed (the check is read-only), but the ritual's ORDER is the contract, and this is the Nth documented recurrence across reports (03-49 §d listed the same two misses).
2. **Build/test verification**: the lint-baseline gate exercises every module's analysis, but I did not re-run `go build/vet/test -race` — defensible for a zero-edit verify window on a clean tree with master CI green, but the contract's "verify your work" was satisfied by citation rather than execution.

## c) NOT STARTED

1. Nothing belonging to THIS task. All deliverables existed on arrival.
2. The 03-49 report's open §f items (cache-clean inside `lint-baseline.sh`, `--since-baseline` triage mode, CHANGELOG policy for lint edits) — inherited, not mine to duplicate; they are already in the backlog/prior report.

## d) TOTALLY FUCKED UP

1. **Turn-1 ritual order miss, recurrence N+1**: the task-ID grep + CONTRIBUTING.md check should have been the first tool calls, before ANY gate run. This exact miss is listed in 03-49's §d from the same morning. The system keeps documenting it and the next window keeps repeating it.
2. **First gate run piped through `tail`**: exactly the pipeline-masking failure class AGENTS.md and TODO 201 call out by name (`set -e`/exit-code hiding, `head` truncation). Self-caught within the same window and re-run unfiltered (exit=0 verified), but the reflex should not exist at all.
3. **Redundant dispatch spend, 8th+ instance**: a fully-done row re-dispatched costs a window (this one ~10 min incl. a clean-cache lint pass over all 15 modules). The queue-side done-row dedup short-circuit proposed in at least four prior reports (01-53, 01-56, 03-00, 03-49 lineage) is still unbuilt.
4. Minor: initial `rg` for the TODO item returned nothing because I grepped for fragment text ("lint-baseline repair") while the row lives on line 30 verbatim — second search needed. Cheap, but the first grep should have anchored on the task ID, not prose.

## e) WHAT WE SHOULD IMPROVE

1. **Turn-1 ritual as a script**: `scripts/session-start.sh <task-id>` doing (git log -5 over whole repo, git status, stash list, `rg -l <task-id> docs/status/`, CONTRIBUTING.md/CLAUDE.md existence check) — fifth+ ask across reports; a hook or wrapper kills the recurrence class.
2. **Queue-side done-row dedup short-circuit**: before dispatching, the harvester checks `docs/status/` + TODO `[x]` for the item's ID and re-dispatches only on owner override. Eight windows of evidence now.
3. **`scripts/lint-baseline.sh` should run cache-clean (or verify clean-cache) itself** for `--check`, or print a loud WARM-CACHE warning — two warm-cache incidents (03-49's committed-baseline artifact, AGENTS.md 2026-09-17 note) in two days.
4. **Verify-window contract wording**: "run the project's build and tests" vs. a pure verify→cite→stop window — codify which depth a zero-diff re-dispatch owes so windows stop improvising (this window cited instead of executing; prior windows varied too).
5. **Gate-summary line convention** (03-49 §f8, third ask): first line of a verify window's DONE note = `within baseline N/N, exit 0` — I used it above; make it the pinned format.

## f) Next tasks (honest count: 12 — the 03-49 §f items remain primary; this window adds the top two and refuses padding to 50)

1. Bake `golangci-lint cache clean` (or a warm-cache guard) into `scripts/lint-baseline.sh --check` — kills the flip-flop class at the root (03-49 §f1, now n=2 incidents).
2. Queue-side done-row dedup: harvester short-circuits items whose ID already has a status report + `[x]` (n=8+ redundant windows).
3. `scripts/session-start.sh` turn-1 ritual script (or Crush hook) — order-enforced task-ID grep + CONTRIBUTING check.
4. `--since-baseline` triage mode in lint-baseline.sh: show only findings above committed counts, per row (03-49 §f2).
5. Un-addressed 03-49 §g1 owner ruling: daemon-race fold convention (amend unpushed daemon commit vs footer-only follow-up).
6. Un-addressed 03-49 §g2: should the committed baseline get a one-off clean-cache re-verification audit?
7. Un-addressed 03-49 §g3 / §f12: CHANGELOG policy for lint-policy edits (tagliatelle exclusion shipped without one).
8. 03-49 §f11: document the tagliatelle exclusion boundary (all of cmd/tq vs current three files) before the next NEW tagliatelle class mints.
9. 03-49 §f9: enforce the turn-1 prior-report grep queue-side or hook-side (this window proved it's still not holding).
10. 03-49 §f10: amend-maneuver policy note in AGENTS.md (daemon-fold convention).
11. Gate-summary line contract (§e5 here): pin the `within baseline N/N, exit 0` verdict format.
12. Verify-window depth contract (§e4 here): one AGENTS.md paragraph defining what a zero-diff re-dispatch must re-execute vs cite.

## g) QUESTIONS FOR THE OWNER

1. **Re-dispatch sanction**: this is the second window on this ID in under an hour (03-49 shipped, this re-verified). Is the repeat dispatch a queue bug (done-row dedup missing) or an intended double-check policy? If the latter, what verification depth do you want — gate re-run only, or full build+test too?
2. **Cache-clean policy**: should `scripts/lint-baseline.sh --check` always clean the golangci cache itself (adds minutes, guarantees CI-parity truth), or stay fast and rely on the documented manual ritual that has now failed twice?
3. **CHANGELOG for gate/policy edits**: the tagliatelle exclusion and seventh regen (03-49) carry no CHANGELOG row. Should scanner-policy changes get rows, or does the docs-only CHANGELOG policy deliberately exclude them?
