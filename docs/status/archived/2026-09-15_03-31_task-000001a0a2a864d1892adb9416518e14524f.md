# Status — task-000001a0a2a864d1892adb9416518e14524f — third re-dispatch lap (03-31)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-15_03-25_task-000001a0a2a864d1892adb9416518e14524f.md`, which owns any open residue.

**Work item:** "`golangci-lint config verify` in ci-local before the run —
schema-invalid keys silently disable linter settings (07-39 report f3)"

**Lap verdict: verification-only no-op.** The item was fully shipped in a
prior lap (commit 27df042, gate wired in `scripts/ci-local.sh:83-89`), the
TODO_LIST row is already checked off with the DONE note, and two earlier
reports (666454c close-out, a03a67c first re-dispatch) document that work.
This lap re-verified and changed nothing.

## a) Fully done

- The work item itself: `scripts/ci-local.sh` gained a hard-failure
  "lint config verify" step (line 85-89) immediately before the advisory
  lint run, using the local binary with a GOPATH fallback.
- TODO_LIST.md row marked `[x]` with DONE 2026-09-15 note citing the file.
- Re-verified this lap: `golangci-lint config verify` exits 0 on the root
  `.golangci.yml` (cite: run in this session, silent success).
- `GOEXPERIMENT=jsonv2 go build ./...` green (cite: this session).
- Working tree clean at a03a67c — no uncommitted residue from parallel
  agents in the touched area.

## b) Partially done

- Nothing in scope. The full item is complete; only report/commit
  bookkeeping remained for this lap (this file + commit).

## c) Not started

- No sub-parts of the item remain. Nothing was deferred.

## d) Totally fucked up

- Nothing. One honest nit: the prior laps (666454c, a03a67c) were
  verification-only re-runs of the same task — three reports for a
  one-gate change is report overhead the queue could avoid if
  re-dispatch checks TODO_LIST check-state FIRST and exits with a bare
  TQ_RESULT without a new report. That's a queue-dispatch policy
  observation, not a defect of this task's work.

## e) What we should improve

- Re-dispatch protocol: check the TODO_LIST row's done-state before
  writing any code or reports; emit a one-line no-op result instead of a
  full lap.
- The ci-local gate's fallback `$(go env GOPATH)/bin/golangci-lint` is
  host-layout-dependent; a `command -v` probe would be more honest than
  trusting GOPATH/bin placement.
- Session-start ritual was followed, but this lap could have cited the
  two prior reports' evidence instead of re-running gates — cheap either
  way, but re-verification was the safer call given concurrent agents.

## f) Next things to get done (queue-relevant, from what passed in view)

1. Confirm the config-verify gate survives the next golangci-lint
   upgrade (schema version drift can make `config verify` fail for NEW
   valid reasons).
2. Wire the same config-verify gate into ci.yml's lint job for runner
   parity with ci-local.
3. Add a smoke/negative fixture proving the gate FAILS on a schema-invalid
   key (the gate has no positive-negative pin test yet).
4. Sweep for other silently-disabled config classes: `templ` config,
   `treefmt`/dprint config validation equivalents.
5. Document the gate in docs/release/RELEASE.md's gate inventory if
   release.sh also lints.
6. Reduce redundant re-dispatch reports (queue policy change — owner).
7. Re-run `scripts/check-guard-wiring.sh` on the next ci-local edit to
   keep the orphaned-guard audit honest.
8. Consider `golangci-lint config verify` inside `scripts/lint-baseline.sh
   --check` so baseline regens can't ride a broken config.

(Stopped at 8 — nothing further belongs to this task's blast radius;
do NOT let a close-out lap invent filler items.)

## g) Questions I cannot resolve myself

1. Should re-dispatched already-done tasks skip the status report
   entirely (queue-side policy), or keep the paper trail?
2. Do you want the config-verify gate mirrored into ci.yml's lint job
   (runner parity) in a separate task, or is ci-local-only acceptable?
3. Is a negative-test fixture for the gate (deliberately invalid config
   under /tmp, assert non-zero exit) worth a TODO_LIST row of its own?

**Gates cited:** `golangci-lint config verify` (exit 0, this lap);
`GOEXPERIMENT=jsonv2 go build ./...` (green, this lap); tree clean at
a03a67c.
