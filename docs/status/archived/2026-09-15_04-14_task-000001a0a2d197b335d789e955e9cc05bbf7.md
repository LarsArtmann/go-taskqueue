# Task 000001a0a2d197b335d789e955e9cc05bbf7 — enqueue `$TQ_DB` guardrail — fourth-dispatch verification lap

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-15_04-09_task-000001a0a2d197b335d789e955e9cc05bbf7.md`, which owns any open residue.

- **Date:** 2026-09-15 04:14
- **Item:** "`tq enqueue` guardrail: warn when `$TQ_DB` points somewhere other than `./tasks.db` (the production-DB trap cost a live enqueue 2026-09-10) (05-29 report c4/d2/e2)"
- **Dispatch:** Fourth dispatch of this task id. The work itself shipped in dispatch one (`9139917`), close-out in dispatch two (`e0f3853`), verification lap three (`83aaa42`). This lap re-verified and gathered three facts lap three did not.

## a) Fully done

- **The work item itself** — shipped and closed since lap one: stderr warning at `cmd/tq/main.go:305-311` (fires only when `--db` is NOT passed and `$TQ_DB` is set, naming the target DB and the `--db` escape hatch), TODO_LIST.md row 83 checked, feature commit `9139917`.
- **This lap's verification (cited, fresh):**
  - Guard code re-read at the source this lap (grep + `sed -n 295,315p`), not from lap-three memory: the guard is `--db`-silenced (`if *db == ""` wraps the env check) — that nuance is now on record.
  - cmd/tq module gate: `./scripts/test-cmd-tq.sh` → PASS (`ok github.com/larsartmann/go-taskqueue/cmd/tq 7.890s`).
  - Working tree clean; TODO row 83 `[x]`; commits `9139917`/`e0f3853`/`83aaa42` all carry the correct footer.
- **Two open questions answered with evidence this lap:**
  - Lap-three §f9 asked whether v0.3.0 on the proxy fixed the cmd/tq plain-build trap: NO. `cd cmd/tq && GOEXPERIMENT=jsonv2 go build .` still dies with `ambiguous import` (proxy copy of `cmd/tq` under `go-taskqueue@v0.3.0` vs the local module). The ADR-0017 "until the next root tag" clause is now known-stale: the tag landed and did not fix it. The devmod shim remains mandatory.
  - Lap-two §f3 (warning conditions) is now pinned by source: unconditional per enqueue run, silenced by an explicit `--db`, no `TQ_DB_WARN` opt-out, no once-per-process counter (enqueue is one process anyway).

## b) Partially done

- **Nothing inside the item itself** — it is binary and done.
- **Adjacent, noticed in passing:** the guardrail has NO unit test pinning the warning text (grep over `cmd/tq/*_test.go` for `TQ_DB` finds only a comment in session_test.go:22). This was flagged in lap two §f4 and lap three §f4 and still exists nowhere in TODO_LIST.md — the follow-up was never harvested. The warning is exactly the string-literal class AGENTS.md warns about (rename sweeps deadened `q.Get("query")` and `--max-attempts` help the same way).

## c) Not started

- Warning-text unit test (above) — not started, and unharvested into the backlog.
- Same env trap guard for the other `defaultDB()` consumers (`tq worker`, `tq harvest`, `tq agent-pool`): still enqueue-only today (grep confirms `os.Getenv("TQ_DB")` appears once, in the enqueue path, plus the defaultDB resolution itself).
- README/SECURITY documentation of the warning: both files mention `$TQ_DB` (README.md:84, SECURITY.md:14) but neither mentions the new warning.

## d) Totally fucked up

- **Pipeline-masked exit code in my own verification.** My plain-build probe was `cd cmd/tq && GOEXPERIMENT=jsonv2 go build . 2>&1 | head -5; echo "exit=$?"` — the `$?` measured `head`, not `go build`, so it printed `exit=0` under a failing build. I got the right conclusion (the error text is unambiguous), but this is precisely the pipeline-masking lesson AGENTS.md already encodes (`set -o pipefail`, verify raw summaries, not filtered tails). An ad-hoc one-liner is not exempt from the discipline.
- **Four dispatches of a DONE item and my laps keep finding new work** rather than driving it to zero: the missing warning test was flagged twice (laps 2 and 3) and is STILL not a TODO_LIST row. A verification lap that ends with an unfixed, unharvested known gap is a report mill. The honest fix was either to add the test this lap (rejected: contract says smallest change, item is closed — and a fourth lap adding feature code would itself be scope creep without an owner ruling) or to ensure the row landed in TODO_LIST. I did neither until this report.
- **Incomplete session ritual:** I ran `git log` + `git status` but skipped `git stash list` (AGENTS.md session-start ritual). Zero consequence this time; zero excuse.
- **(carried, still unfixed queue-side)** lap three's §d: no clean no-op-lap commit story; each re-dispatch of a done id re-pays a full agent session for three lines of verification.

## e) What we should improve

1. **Stop re-minting done ids.** Four laps now. The cheap dispatch pre-flight (`git log --grep=Task-Queue-ID: <id>` before minting) converts laps 2-4 from sessions into a boolean.
2. **Harvest lap findings into TODO_LIST.** §f items from laps 2/3 exist only inside archived-bound status reports. The status sweeper's docs-health pass should lift still-open items into rows; this lap's §b gap (unpinned warning text) is the concrete example — twice flagged, zero rows.
3. **Pipefail discipline for ad-hoc probes**, not just scripts: every verify one-liner I run should survive the "what did `$?` actually measure" question.
4. **Answer stale AGENTS.md claims with evidence, then edit the doc.** The ADR-0017 "until the next root tag lands" caveat is now proven stale (v0.3.0 on the proxy did not fix the ambiguous-import trap); AGENTS.md still phrases it as pending. One-line correction candidate for the next code-touching session (docs-only edit tonight would be a daemon-commit race for zero functional gain — flagged, not done).

## f) What we should get done next (scoped to this task's residue; honest list, not padded to 50)

1. Add `TestEnqueueWarnsTQDB`-style unit test pinning the exact warning text and the `--db` silencing; and harvest the row into TODO_LIST.md (it currently exists in no backlog).
2. Extend the same test (or the help-text smoke) to pin `main.go:88` help wording so rename sweeps cannot deaden either surface.
3. Decide + implement the guard's scope: log-line (not warning) at startup for `tq worker`/`tq harvest`/`tq agent-pool` when `$TQ_DB` is set — the 2026-09-10 trap generalizes to any bare `tq` command, not just enqueue (owner ruling needed on loudness).
4. Document the warning in README.md:84 and SECURITY.md:14 (one sentence each; SECURITY already calls the DB file "the real" asset — the warning is its natural footnote).
5. Decide `TQ_DB_WARN=off` opt-out vs permanent loudness (owner taste; trap argues loud).
6. Minor polish: suppress the warning when `$TQ_DB` resolves to the same path as `./tasks.db` (equal-path noise case).
7. Fix the stale ADR-0017 caveat in AGENTS.md (lap-verified: v0.3.0 did NOT fix the cmd/tq plain-build trap; the shim is permanent until a root tag ships cmd/tq in-module or the shim changes).
8. Queue-side: dispatch pre-flight guard (restate from laps 2/3 — now at four laps of evidence).
9. Queue-side: `noop`/`already-done` TQ_RESULT verdict convention (restate).
10. `scripts/test-cmd-tq.sh` `-run` passthrough so targeted re-runs don't tempt raw `go test` (restate).
11. `tq doctor` should report "TQ_DB points at X" — natural home for the env-trap check, read-only surface.
12. Consider pinning the guard in the help-text smoke (`scripts/smoke/help-text.sh`) alongside the enqueue help audit.
13. Verify and document scope ruling: harvest/agent-pool mints bypass the enqueue CLI entirely (store-level), so the env trap is CLI-only — one paragraph in DOMAIN_LANGUAGE.md or the payload-contracts section.
14. Sweep the surviving lap-2/lap-3 §f items into TODO_LIST.md rows so the backlog owns them (items 3-6 above overlap; the rest die as done or owner-rejected).

(15 items, each concrete. Filling to 50 would mean researching unrelated backlog, which this lap was told not to do; TODO_LIST.md remains the authoritative pool.)

## g) Questions I cannot figure out myself

1. **Guard scope ruling:** should the `$TQ_DB` surface stay enqueue-only (warning), or extend to worker/harvest/agent-pool as a startup log line (or banner)? I can implement either; loudness on every pool start is a taste call.
2. **Suppression policy:** keep the warning unconditional-with-`--db`-silencing (today), add a `TQ_DB_WARN=off` knob, or accept the equal-path polish in §f6 and leave it? The trap argues loud; scripted enqueues argue silence.
3. **Re-dispatch cutoff:** after four laps, should this task id be closed against further re-minting (pre-flight + noop verdict), or are verification laps intended dogfood behavior worth one session each?
