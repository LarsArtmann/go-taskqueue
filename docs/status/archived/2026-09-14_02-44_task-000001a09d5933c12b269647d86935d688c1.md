# History-rewrite policy task — third dispatch delta (02-44)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-14_02-38_task-000001a09d5933c12b269647d86935d688c1.md`, which owns any open residue.

Task: TODO_LIST row 168 (a09d5933c12b269647d86935d688c1) — draft the
history-rewrite policy line for AGENTS.md (08-25 report f21; 07-49 f22).

**Verdict: DONE-on-arrival, third consecutive no-op dispatch.** The work
shipped in c9d36ed; rows 21-22 of this README document the two prior
close-outs. Nothing new was changed this window.

## a) FULLY DONE

- AGENTS.md history-rewrite policy bullet (AGENTS.md:596-606): pushed
  commits (incl. daemon commits on origin/master) are NEVER
  reworded/amended/rebased; owner-approved exception only, for defects a
  follow-up commit cannot fix; scripted per the daemon rules; four
  mandatory fork records (old/new SHAs + fork point in a status report,
  tag/remote-twin ancestry note, `git tag --no-merged master` release
  check, no citations of dead SHAs). 07-49 incident is the embedded case
  study; row 170's merge-append healing is noted as the exception that
  proves permanence.
- TODO_LIST row 168 checked `[x]` with DONE note citing the bullet.
- Queue cross-reference: c9d36ed carries the Task-Queue-ID footer
  (single, well-formed — commit-msg hook contract holds).

## b) PARTIALLY DONE

Nothing on this task. The broader ask ("what could still improve")
accumulates in §e/§f below.

## c) NOT STARTED

Nothing — this dispatch's scope was fully covered before it arrived.

## d) TOTALLY FUCKED UP

- **The dispatch loop itself is the failure.** This is the THIRD re-run
  of an item the queue already had as DONE with a checked row, a DONE
  note, and two indexed close-out reports. The dispatcher (and any agent
  session) must grep TODO_LIST for the task id / DONE note AND
  docs/status/README.md for the task id BEFORE doing work — my first
  window's session-start grep covered TODO_LIST but the dedup signal I
  actually acted on was the git log subject line, which is fragile.
- Re-running gates for a docs-only no-op is cheap, but each no-op still
  burns a window, a report, an index row, and a commit on master.

## e) WHAT WE SHOULD IMPROVE

1. **Done-on-arrival guard seam**: a cheap scripted pre-flight
   (`scripts/check-task-done.sh <task-id>`: TODO_LIST checkbox + DONE
   note + status-index grep) that any session runs before picking up a
   row — turns the §d lesson into a gate, mirroring how
   check-status-index.sh turned the daemon-fold hole into a catcher.
2. **Session-start ritual grep must include the task id**, not just
   paths: `git log --grep <id>` + README.md index grep.
3. **Dispatcher feedback loop**: whatever mints the re-dispatch (manual
   TODO row vs queue re-arm) should read the prior TQ_RESULT note — this
   and the 02-40 window both said "already DONE" in the note field.
4. Report/index bloat: three rows for one docs bullet is honest history
   but consider marking prior rows superseded-by in the newest row.
5. (carried) A mechanical pre-rewrite guard (release-gate style check
   for `git tag --no-merged master` before any release) would make
   policy §(c) enforced rather than remembered.

## f) NEXT (delta list — canonical list = 02-38 §f / 02-40 §f, not duplicated)

1. Ship `scripts/check-task-done.sh` + wire into the pool claim path.
2. Add task-id grep to the AGENTS.md session-start ritual line.
3. Surface prior TQ_RESULT notes to the dispatching side (queue CLI or
   TODO row annotation).
4. Release gate: `git tag --no-merged master` check before release.sh.
5. Decide whether no-op re-dispatches get a one-line report instead of a
   full a)-g) file (owner ruling needed — see §g3).
6. (carried) commit-msg hook vs ci-local catcher parity for daemon
   commits (hook bypasses remain the known hole).
7. (carried) CONTRIBUTING.md pointer to the history-rewrite policy —
   policy lives only in AGENTS.md today.

## g) QUESTIONS (self-answerable=no)

1. Should a proven DONE-on-arrival re-dispatch be allowed to answer with
   a bare TQ_RESULT and NO new report file/index row (fifth ask of the
   same question — the 02-40 report asked it too)?
2. Owner ruling: is the §f1 done-guard a gate (blocking) or advisory in
   the pool claim path?
3. Where does the public-facing policy pointer live — CONTRIBUTING.md
   (if it should exist for this repo) or README?
