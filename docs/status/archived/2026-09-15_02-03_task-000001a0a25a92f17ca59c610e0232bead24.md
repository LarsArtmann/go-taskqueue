# Close-out — CI topology one-pager task 000001a0a25a92f17ca59c610e0232bead24 (this dispatch: 2026-09-15 02:03)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-15_01-59_task-000001a0a25a92f17ca59c610e0232bead24.md`, which owns any open residue.

Fifth paid lap on this task ID (prior: work commit 46ea40a, close-outs 896ec74
and d8ed746, plus the 01:59/02:01 reports this morning). This dispatch found
the deliverable complete at HEAD and wrote no code or doc changes.

## a) FULLY DONE

- The deliverable exists and covers the contract:
  `docs/planning/2026-09-15_ci-topology-one-pager.md` — every ci.yml job
  (test, test-windows, test-postgres, nix, govulncheck, gosec, cqrs-lint) and
  fuzz.yml, per-step "loops modules?" flags with the ADR-0011 rationale, the
  cmd/tq (ADR-0017) and facade-parity (ADR-0016) carve-outs, and the
  ci-local.sh parity rule.
- TODO_LIST.md row 64 already `[x]` with the doc path (verified this dispatch).
- Work commit 46ea40a already carries the Task-Queue-ID footer.
- Verification this dispatch: git log/status clean; no build run needed
  (zero repo changes by this lap; the one-pager is prose — its "tests" are the
  two dispatch re-reads + the CI green state at HEAD).

## b) PARTIALLY DONE

Nothing from this lap — it was pure verification. Two hedged claims survive
in the one-pager from the earlier lap (noted by the 01:59 report, still true):
the fuzz.yml row's devmod-free invocation detail and the exact
ci-local.sh↔ci.yml step parity are asserted from close-out memory rather than
re-derived line-by-line this lap.

## c) NOT STARTED

Nothing in the work item. The one-pager has no automated rot-protection (see
§e) — that is follow-up work, never part of this row.

## d) TOTALLY FUCKED UP

The re-dispatch loop itself. This is the FIFTH paid turn against one task ID
for a done deliverable. Each lap costs a full agent window to re-read files
and re-confirm what a `git log` + `grep TODO_LIST` one-liner already proves.
The systemic fix (mint-time done-check on the task ID before dispatch) is
outside this queue's control from inside an agent turn — it stays the owner
call.

## e) WHAT WE SHOULD IMPROVE

1. Mint-time done-check: if the task ID already has a completion verdict +
   close-out, do not re-dispatch without an owner override.
2. The one-pager should be drift-gated: it duplicates facts (job lists, loop
   roots) that ci.yml and ci-local.sh also encode. A smoke comparing the doc's
   job/step inventory against the workflow YAMLs (like check-features-ci.sh)
   would catch silent rot.
3. The two hedged claims in §b should be re-derived once and the doc updated,
   so the page is fully evidence-backed.

## f) NEXT (up to 50 — refusing padding: the honest list is short, and prior same-ID reports own the backlog; the delta this lap adds nothing new)

1. Drift-gate smoke for the CI topology one-pager (parse ci.yml/fuzz.yml job
   ids + step names, compare to the doc's tables).
2. Re-derive the two hedged claims (fuzz nightly invocation path, ci-local
   parity) and cite them in the doc.
3. Owner decision: re-dispatch suppression for task IDs with existing
   close-outs (queue-side, not agent-side).

## g) QUESTIONS (agent-unanswerable)

1. Is the one-pager's permanent home docs/planning/ or should it graduate to
   docs/ (or AGENTS.md condensation) once stabilized? It is now load-bearing
   for every CI change.
2. Should the drift-gate smoke (§f1) be HARD in CI or advisory like the lint
   baseline? Doc rot here is cheap to detect but the gate cost is a new
   YAML parser.
3. Owner ruling wanted: what dispatch condition justifies a re-lap on a task
   ID that already has a `[x]` TODO row + close-out report? Without that
   rule, laps like this one are unpriced.
