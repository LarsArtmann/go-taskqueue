# Close-out — gosec 0-target guard, third dispatch (verify window)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-19_03-56_task-000001a0b75806e7ce18883bf1d063d176f2.md`, which owns any open residue.

Task `000001a0b75806e7ce18883bf1d063d176f2` · 2026-09-19 04:32 CEST · third window on this ID (03-56 work, 04-16 verify+heal, this one) · single agent, tree clean at turn 1 and at close-out, no concurrent collisions observed.

**One-line verdict:** already-shipped work re-verified fresh at HEAD with the full battery; zero code delta; the window's one real finding — the mvdan-shell PIPESTATUS bashism — is a REPEAT of the 02-38 report's prose-only confession and is now codified in AGENTS.md instead of living only in report prose.

---

## a) FULLY DONE

1. **Turn-1 protocol, executed in order.** The task-ID grep (`rg -l <id> docs/status/`) was the literal first action (batched with `git log`/`git status`/`git stash list`), surfacing both prior reports and the DONE row before any other tool ran; the TODO_LIST grep in the same batch confirmed row 129 already `[x]`. CONTRIBUTING.md was READ (not `ls`'d) before any edit — the first window in this ID's lineage to close that specific hole at turn 1 (the 03-27/03-39 windows on the sibling ID confessed N+12/N+13; 04-16 confessed N+14).
2. **Code-level re-verification of the deliverable at HEAD.** d035911 confirmed in history via path-scoped `git log -- scripts/check-gosec.sh`; the guard re-read in the file (scripts/check-gosec.sh:222-231): enumerate-first capture `modules="$("$enum_cmd")"` with a `||` handler (catches the enumerator exit status that process substitution silently drops), the `-z` zero-target guard, and the module loop fed from the captured list (`<<<"$modules"`). Both failure shapes `exit 1` BEFORE the first scan — the fail-fast property the item demanded.
3. **Fresh battery at HEAD, all green.** `--self-test`: 24/24 checks ok, including both enumeration-failure branch groups ("empty enumeration: 0 module targets hard-fail", "broken enumeration: non-zero exit hard-fail", each with fail-before-any-scan `== (root)`-absent assertions). Real gate: exit 0 (captured by redirect-to-file + direct `$?`, 16 `== ` scan lines, 16 `ok: Files=… Issues=0` lines, 0 FAIL, 1 WARN — the documented unstamped-binary provenance WARN). Root `go build` + `go vet` green under `GOEXPERIMENT=jsonv2`. Tree clean before and after; HEAD at 31842a0.
4. **Correct no-op disposition.** Zero code changes, no empty commit, zero footer work commits (the documented no-op re-dispatch derivation), no duplicate backlog mints for already-rowed follow-ups.
5. **The window's lesson codified ON SIGHT, not queued.** The mvdan PIPESTATUS hazard now has a Known Issues bullet in AGENTS.md (this window's commit), per the 2026-09-06 no-two-line-doc-fix-to-backlog ruling and the Memory Maintenance mandate — closing the loop the 02-38 report §d opened in prose only (see d1).

## b) PARTIALLY DONE

Nothing about the work item itself is partial (it is binary and done). What stays partial is environmental, carried forward unchanged by this window:

1. **Gate green carries the provenance asterisk.** The 16-scan green run WARNs because the discovered gosec binary is UNSTAMPED (`Version: dev`); the v2.29.0 pin cannot be PROVEN on this host until the owner installs a stamped binary (02-52 §g q2, standing).
2. **The lineage is still unpushed.** The 04-16 window measured origin 34 commits behind; no push has happened since, so remote CI has still never run the new gate. Owner-side action.
3. **Zero new signal produced.** The battery and the guard re-read reproduced the 04-16 coverage exactly. That is the expected shape of a verify-only window and the running argument for a lighter certificate format (g1).

## c) NOT STARTED

All observed in passing from TODO_LIST.md and the digest row; none of it this window's scope:

- Row 133 — `for-each-module.sh` consumer loops (ci.yml ×6, ci-local.sh:73) catch neither enumeration failure shape. Same silent-skip family one layer up; highest-value sibling.
- Row 134 — short-list enumeration guard (non-zero-but-short list scans survivors, exits 0).
- Row 132 — `--self-test` foreign-CWD rc=127 under stock bash (the session shell masks it for agents; ci-local is the only catcher — this window's self-test run inherits that blind spot).
- Rows 130, 131, 135, 136 — scan summary line; ci.yml pin derivation; citation-order note; wrapper smoke leg.
- Rows 124 + 127 — BLOCKED on owner rulings (CLI gosec coverage; CI convergence).
- Row 253 — CONTRIBUTING gate-list sweep.
- Owner push of the unpushed lineage; stamped gosec binary install; status-index ANNOTATE sweep (check-status-index WARNING this window: 242 live rows vs the 100 threshold — the digest row's ~143 estimate has diverged, as the 03-27 report already noted; this report adds one).

## d) TOTALLY FUCKED UP

1. **The window's first real-gate invocation shipped in the pipeline-masking shape**: `./scripts/check-gosec.sh 2>&1 | tail -30; echo GATE_RC=${PIPESTATUS[0]}`. Two hazards in one line — it relied on `PIPESTATUS`, which the session (mvdan) shell does not provide (expands EMPTY, proven empirically this window: `true | false; echo $?` prints 1, `PS0=` prints nothing), and the common reflex alternative (`$?` after the pipe) would have reported tail's 0 and MASKED the gate. It failed loud by luck, not by design; the honest re-run (redirect to `/tmp/gosec-verify-window.log`, capture `$?` directly, grep counts from the file) cost a second full gate scan. Worst part: this exact bashism is the 02-38 report's §d confession ("claim-before-evidence in the DONE note (PIPESTATUS bashism came back empty)") — the lesson had been sitting in report prose for ~2 hours and was re-hit verbatim because it was never codified. Then my own generalization sat un-interpreted until this report's drafting (04-16 §d3 disease, miniature scale).
2. **Pipeline reflex everywhere**: even the read-only guard-location search used `grep | head`. Exit-code-irrelevant there, but in a session whose own close-out documents the masking family, redirect-then-inspect should be the default shape for anything that could ever have a gate on its left side.
3. **Nothing else.** No read-before-edit violations (every edit target was Viewed first; TODO_LIST was deliberately left untouched, removing the 04-16 §d1 class entirely); exactly-one-footer and footer-as-last-line held at commit time (verified against `git log -1 --format=%B`); explicit-path `git add` with a pre-add `git status --short` re-check held (no daemon race this window).

## e) WHAT WE SHOULD IMPROVE

1. **The gate-invocation form is now written down; the remaining half is reflex.** AGENTS.md carries the bullet (redirect to file, `$?` directly, counts via grep); the next window should not need it written down. Candidate follow-on: grep ci-local/agent-facing snippets for any remaining `${PIPESTATUS` reliance (none known — ci-local runs stock bash where it works — but a sweep is cheap).
2. **Third verify-only close-out on this ID in one day** (03-56 changed code; 04-16 and this window only verified). Each paid a full a)-g) report for roughly ten lines of real content. The minimal-certificate format keeps asking for a ruling (g1).
3. **Report-prose lessons demonstrably do not propagate.** PIPESTATUS lived in 02-38 §d, reached no AGENTS.md surface, and was re-hit. Proposal for g2: a mechanical-gotcha §d confession must end the window either codified in AGENTS.md or minted as a TODO row — prose alone is where lessons go to die.
4. **Keep doing (held this window):** turn-1 ID grep as the literal first action; CONTRIBUTING read at turn 1; fresh-at-HEAD over inherited citations; explicit-path add with the pre-add status re-check; footer as the literal last line.
5. **The gosec guard family is closed at the gate layer for both enumeration failure shapes** — confirmed by reading the guard, not by trusting the DONE note. The residual exposure is entirely in the consumers (row 133) and list-length (row 134); the next window on this family should start at row 133, not re-derive the gate.

## f) Up to 50 things we should get done next — honestly scoped at 14

The ceiling is 50; padding to it would mint noise. The one candidate new mint (PIPESTATUS codification) was executed directly this window instead per the fix-on-sight ruling, so the table below is the standing queue, re-ranked after this window's verification.

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | Row 133: for-each-module.sh consumers (ci.yml ×6, ci-local.sh:73) catch neither enumeration failure shape — same silent-skip family one layer up | High | M | Bug |
| 2 | Row 134: pin a SHORT module list as failure (expected-count or canary-module assertion) | High | M | Quality |
| 3 | Owner push: the unpushed lineage means remote CI has never run the new gate; every "master CI green" claim is local-only until then | High | S | Process (owner) |
| 4 | Install a stamped gosec v2.29.0 on this host (02-52 §g q2): removes the provenance WARN from every green gate claim | Medium | S | Cleanup (owner) |
| 5 | Row 132: `--self-test` absolute self-path before the top-level `cd` + foreign-CWD assertion (rc=127 under stock bash) | Medium | S | Bug |
| 6 | Row 253: CONTRIBUTING.md gate-list sweep (check-gosec.sh, lint-baseline, transient-retry, go-mods, ci, facade-parity, status-index) | Medium | S | Documentation |
| 7 | Row 131: derive GOSEC_VERSION + excludes in ci.yml from check-gosec.sh (kill the second hand-pinned copy) | Medium | S | Cleanup |
| 8 | Row 136: committed wrapper-path smoke leg for `tq crush --bin` | Medium | M | Quality |
| 9 | g1 ruling: minimal-certificate format for verify-only re-dispatch windows | Medium | M | Process |
| 10 | g2 ruling: mechanical-gotcha confessions must be codified or minted same-window (prose-only demonstrably fails) | Low | S | Process |
| 11 | Row 130: end-of-run "N scans ok, M failed" summary line in check-gosec.sh | Low-Med | S | Quality |
| 12 | Rows 124 + 127 (BLOCKED): CLI gosec coverage; ci.yml gosec-loop convergence — park on owner rulings | Medium | M | Quality |
| 13 | Status-index ANNOTATE sweep (check-status-index WARNING: 242 live rows vs the 100 threshold; ~243 after this report) | Low | M | Documentation |
| 14 | Owner-side: the global pipeline-masking lesson in the global AGENTS.md could absorb the mvdan PIPESTATUS data point (cross-project hazard) | Low | S | Documentation (owner) |

## g) Top 3 questions I can NOT figure out myself

1. **Minimal-certificate format for verify-only re-dispatches — third ask** (04-16 §e3/§f15/§g3, converging with 03-39's battery row). Three windows in one day have now paid full close-out ceremony for "already done, verified". If you ratify the certificate: what are its REQUIRED fields, so it cannot silently shrink into "trusted the prior report"?
2. **Should a mechanical-gotcha §d confession be REQUIRED to end the window either codified in AGENTS.md or minted as a TODO row?** PIPESTATUS needed two windows and one wasted full-gate run because it existed only as report prose. If yes, where does the convention live — the claims-carry-citations note, or the close-out prompt itself?
3. **Battery depth for zero-delta windows:** this window re-ran everything (self-test + real gate + build/vet, then doc-refs/syntax/guard-wiring/todo at commit time). Is the full battery mandatory every dispatch, or is one fresh real-gate run + re-read of the guarded lines enough when the self-test was green in the immediately-prior window?

---

*Gates cited (all fresh this window at HEAD 31842a0): `check-gosec.sh --self-test` 24/24 rc=0; `check-gosec.sh` rc=0 / 16 scans / 0 FAIL / 1 documented WARN (`/tmp/gosec-verify-window.log`); root `go build` + `go vet` ok (GOEXPERIMENT=jsonv2); `check-doc-refs.sh`, `check-script-syntax.sh`, `check-guard-wiring.sh`, `check-todo-list.sh`, `check-status-index.sh` re-run green before the close-out commit. Commits: d035911 (work, 03-56 window — verified intact), this window's close-out commit (report + index row + the AGENTS.md PIPESTATUS bullet).*
