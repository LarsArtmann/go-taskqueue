# KanbanBoard Evaluation Dispatch-3 Verification Close-Out (Brutal a)-g)

*(Archived 2026-09-13 docs-health pass: window complete: KanbanBoard REJECTED re-verified n=3; TODO row 120 [x]. Point-in-time snapshot — re-verify before treating any claim as current.)*

Task: 000001a098384e6a6033b938936163fc7a98 — third dispatch (n=3) of this task
ID. Window: 2026-09-13 ~03:00–03:10 CEST. Session type: **verify-only** — the
work was DONE on arrival (again).

## Arrival state (turn 1)

Turn-1 ritual ran in full and in the mechanical order n=2 §e3 prescribed
(git log whole-repo + status + stash, task-ID-first grep of
docs/status/README.md, TODO row state, CONTRIBUTING/CLAUDE presence):

- 819012d (evaluation) + 439f539 (n=1 close-out) + c684f73 (n=2 close-out)
  all present at turn 1; tree clean; no stash.
- TODO row 120 `[x]` with the cited DONE annotation; AGENTS.md:392 rejection
  note present; both prior reports indexed (one row each).
- CONTRIBUTING.md EXISTS, CLAUDE.md absent — checked at turn 1 this time
  (n=2's §b3 miss, not repeated).
- Library still pinned v1.16.0 in go.mod — the re-evaluation guard
  ("only on a library API change") holds; no re-litigation triggered.

## a) FULLY DONE (this dispatch's actual work: closing n=2's verification gap)

1. **The Wire-nil read-only claim — the one claim NO session had verified
   first-hand (n=2 §b1) — now verified first-hand by me**, module-cache
   `display/kanban.templ` v1.16.0: `wired := len(moveAttrs) > 0` (line 127,
   from `kanbanWireAttributes(props.Wire, ...)`) gates EVERYTHING interactive:
   the hidden move form AND the `aria-live` live region sit inside
   `if wired {` (lines 137–157), the prev/next move buttons inside
   `if wired && ...` (line 241), and card draggability is
   `draggable := wired && col.ID != ""`. The doc comments state it outright
   (lines 58, 64): a board without Wire "is a read-only view". With Wire=nil
   the component renders a bare section wrapper + columns + cards — which the
   custom markup already matches. The rejection's central claim now has
   first-hand citations from TWO independent sessions (02-47 and this one).
2. **Three hard blockers re-confirmed first-hand** (citations mine):
   `data-tc-kanban-column` (kanban.templ:163) vs board_test.go's five
   `data-status` pins (pending/running/completed/dead/cancelled, both the
   fragment test and the stream-snapshot pin); count badge
   `strconv.Itoa(len(col.Cards))` (kanban.templ:174–176, truncated slice
   length); custom-only semantics with no library hook —
   `statusAccentClass` top rule (fragments.templ:412) and the
   `"+N older →"` escape hatch (fragments.templ:431).
3. **Verify gate re-run with CLEAN exit capture**: file redirect, no
   pipeline — `GATE_EXIT=0`, 15 packages ok, 0 FAIL. The exit-1 the tool
   reported is `grep -c FAIL` finding zero; the capture itself is sound
   (build/vet short-circuit would surface in `$?`).
4. **Per-claim citation ledger** (n=2 §e2 discipline): rejection claim 1
   (Wire-nil inert) — verified THIS session; claims 2–4 (badge, hooks,
   data-status) — verified THIS session; library pin v1.16.0 — verified
   THIS session. Trusted from prior sessions: nothing material.
5. **CI in passing**: origin shows only Dependabot Update runs in the
   recent window; the newest ci.yml run remains the red PRE-fix
   34726154600; local stack now ~11 commits ahead, unpushed — push stays
   owner action, unchanged from n=2's finding.
6. **Constraints held**: no edits to .crushrc/.crush.json/.tq-verify, no
   pushes, no reverts, no empty commit, TODO row untouched (already [x]).

## b) PARTIALLY DONE

1. **Gate scope**: root module only (build/vet/-race). The internal/*
   sub-module gates were not re-run this dispatch — proportionate for a
   docs-only session with zero deliverable changes, but stated, not hidden.
2. **ci-local.sh (the full pre-push replicant) not run** — contract asks
   for "the project's build and tests"; the AGENTS.md standard verify gate
   is what was run, matching n=2's scope.

## c) NOT STARTED (correctly out of scope)

1. FEATURES.md KanbanBoard-rejected row — owner-ruling-gated (02-47 §g1,
   reconfirmed by n=2 §c1).
2. TODO 121 (PageProps.SEO + icons.Render eval), 122, 123 — separate items.
3. Re-evaluation itself — closed-with-guard on library API change; v1.16.0
   unchanged today.

## d) TOTALLY FUCKED UP

1. Honestly: little. The post-gate `grep -c FAIL` chained a cosmetic exit-1
   into the tool result (noise, not masking — GATE_EXIT was already
   captured clean and the counts printed). A trivial variant of the
   exit-capture genus, but this time it could NOT have lied about the gate.
2. **Same-ID report volume continues**: this is the third close-out report
   for one queue item. n=2 §f already raised the supersede/same-ID policy
   question; it remains unresolved, and this dispatch adds row three
   rather than resolving it. Defensible (each dispatch's verification is
   genuinely additive — this one closed the last unverified claim), but
   the pattern is now self-aware.

## e) WHAT WE SHOULD IMPROVE

1. The exit-capture enforcement mechanism (hook or gate.sh) remains
   unbuilt — n=2 §e1 stands; this session merely avoided the sin manually.
2. Same-ID dispatch policy: after n=3, the marginal close-out adds near
   zero unless it verifies a NEW claim (as this one did). A fourth
   dispatch should re-emit TQ_RESULT citing this report WITHOUT a fourth
   report, unless the library version moves.

## f) Backlog deltas

- Carried (owner-gated, led by): push of the unpushed stack; FEATURES.md
  rejected-eval ruling; exit-capture mechanism.
- New: none — this dispatch deliberately adds no items.

## g) Owner questions

None new. The task is closed; AGENTS.md:392 is the durable record.
