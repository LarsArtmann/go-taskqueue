# Task 000001a0bd765985940f5e5cc8afce620381 — Capture-shape regression pin (repeat dispatch)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_08-21_task-000001a0bd765985940f5e5cc8afce620381.md`, which owns any open residue.

- **Window:** 2026-09-20, ~08:15–08:24 CEST
- **Work item:** TODO_LIST "Done-prompt window harvest (2026-09-12 …)" section, row: pin the row-133 capture shape — every `for m in …` loop in ci.yml + scripts/ci-local.sh must be fed by a `mods="$(./scripts/for-each-module.sh)"` capture.
- **Verdict: DONE (by a prior window). This window was a repeat dispatch. Verify → cite → stop.**

## a) FULLY DONE

- The regression pin already exists and is wired:
  - `scripts/check-module-loop-capture.sh` — audit-style gate pinning three rules over `scripts/ci-local.sh` + `.github/workflows/ci.yml`: (1) every `for m in <list>` must iterate `$mods`, (2) every `mods=` assignment must be the `mods="$(./scripts/for-each-module.sh)"` capture, (3) any file with loops must carry at least one such capture.
  - Wired as a ci-local step after guard-wiring (`scripts/ci-local.sh:307`, "module-loop capture check"); its wired-ness is itself caught by `check-guard-wiring.sh`.
  - This landed in commit ad85c09 ("ci: pin the for-each-module capture shape of per-module loops") with a status report (3c6dafd) — both already on master before this window.
- The TODO_LIST row is already checked `[x]` with a full DONE annotation citing gate evidence (TODO_LIST.md:152).
- **This window's fresh verification** (not inherited):
  - `./scripts/check-module-loop-capture.sh` at HEAD → rc=0: ci-local.sh loops=4 captures=1, ci.yml loops=6 captures=6, "every module loop is fed by the for-each-module capture".
  - Note: the gate reports loops=4 for ci-local.sh vs the prior report's loops=3 — ci-local gained one more `for m in $mods` loop since (concurrent forward progress, correctly fed by the single capture), which incidentally re-proves the pin's live value.

## b) PARTIALLY DONE

- Nothing. The work item is fully closed; this window added only the fresh re-run of the gate.

## c) NOT STARTED

- Nothing under this work item. All feasible follow-ups for it were done by the prior window (gate + wiring + TODO check-off + prior status report).

## d) TOTALLY FUCKED UP

- Nothing in this window. Honest self-critique:
  - My first tool call grepped a nonexistent path (`ci.yml` at repo root) in the same invocation as the correct ones — harmless (the correct hits still came back) but sloppy; the workflow file is `.github/workflows/ci.yml`.
  - I could have run the session-start ritual (`scripts/session-start.sh` + `rg -l <task-id> docs/status/`) _before_ touching anything. I happened to converge on the right behavior immediately anyway, but the ritual exists precisely so this is not luck.

## e) WHAT WE SHOULD IMPROVE

1. Repeat dispatches are routine (the AGENTS.md turn-1 rule says so) — the grep-prior-reports step should be the FIRST action for queue-fed tasks, not an afterthought; this window got lucky, the 00-52/02-17 windows did not.
2. The capture-shape gate is a good template: cheap, mechanical, self-describing output (loops/captures counts per file) that makes drift a one-glance diagnosis. Similar shape-hazard pins (e.g. any future `$(find …)` enumerations sneaking into gates) could reuse the same audit style.
3. The gate counts ci-local.sh loops=4/captures=1 — correct by design (one capture feeds four loops), but the message could state "loops fed by the single capture" explicitly to avoid future readers mistaking 1 for a near-miss.

## f) Up to 50 things we should get done next

Only NEW, agent-executable follow-ups observed in passing — none duplicate this task:

1. Add a negative-test mode to `check-module-loop-capture.sh` (`--self-test` style, like check-gosec.sh) that builds a temp fixture with a BAD inline-`$(find …)` loop and asserts rc=1, so the gate's rejection path is exercised in CI instead of relying on the prior window's manual negative test.
2. Same audit treatment for any direct `find … -name go.mod` enumerations in scripts/ that bypass `for-each-module.sh` (advisory scan; confirm whether any exist outside the pinned fixtures).
3. Sweep TODO_LIST for other rows harvested from the same 2026-09-12 04-45 window that may be repeat-dispatch candidates, and annotate each with its DONE evidence pointer if already closed.
4. Consider having `check-module-loop-capture.sh` also assert the five pinned canary sub-modules appear in the enumeration output (mirroring the check-gosec canary rule), so a renamed sub-module dropping out of `for-each-module.sh` itself is caught at this layer too.

(Stopped at 4 — inventing filler beyond what this task's surface surfaced would violate the no-scope-creep contract.)

## g) Questions I can NOT figure out myself

1. ci-local.sh now has 4 loops fed by 1 capture (prior report said 3 loops) — the 4th loop arrived via a concurrent window. Purely informational, no ruling needed, but: do you want repeat-dispatch windows to re-annotate the TODO row's DONE evidence when the counts it cites drift (loops=3→4), or leave the original evidence frozen as point-in-time?
2. Should `check-module-loop-capture.sh` grow the `--self-test` negative fixture (f1) in this repo, or does that belong in a dedicated backlog row for the gate-hardening family (check-gosec-style self-tests) so they land together?
3. Is there an owner preference for where "audit-style gates derived from one-off verify-window lessons" get indexed (the 2026-09-12 harvest section vs a dedicated regression-pin section in TODO_LIST), so future harvest rows are consistently findable?

## Gate evidence (fresh, this window, at HEAD)

- `./scripts/check-module-loop-capture.sh` → rc=0 (ci-local.sh loops=4 captures=1; ci.yml loops=6 captures=6).
- TODO_LIST.md:152 row already `[x]` with prior window's battery cited there (negative fixture re-execution, shellcheck, guard-wiring 35/0, root build+vet+race 15 ok / 0 FAIL under GOEXPERIMENT=jsonv2).
