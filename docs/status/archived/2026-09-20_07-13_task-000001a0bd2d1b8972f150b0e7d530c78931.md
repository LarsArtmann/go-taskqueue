# Status Report — Dead-SHA Residue Positional Mapping (FOURTH dispatch, verify-only close-out)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_07-07_task-000001a0bd2d1b8972f150b0e7d530c78931.md`, which owns any open residue.

- **Task:** 000001a0bd2d1b8972f150b0e7d530c78931 (TODO row 148)
- **Window:** 2026-09-20 ~07:10–07:15 CEST (single short window)
- **Work commits (prior windows):** c1cb0a9 (verdicts in `scripts/dead-sha-baseline.txt`) + 292b265 (close-out report), both carrying this task's footer
- **Scope this window:** none — verify → cite → stop. Zero source delta.

## a) FULLY DONE

1. **Turn-1 task-ID grep over docs/status/** caught the DONE state immediately: the 07-07 close-out report for this exact ID exists, and TODO row 148 is `[x]` with the full DONE note.
2. **Fresh gate run at HEAD after the work was confirmed in hand:** `./scripts/check-dead-sha-refs.sh` rc=0 — **110 baselined, 0 new** (count drift 107→110 since 07-07: new reports citing already-baselined dead SHAs, the documented STALE-count semantics).
3. **Footer cross-reference verified mechanically:** `git log -3` trailers show BOTH c1cb0a9 and 292b265 carry `Task-Queue-ID: 000001a0bd2d…` — the queue can derive the work from the cited commits.
4. **Working tree clean**, no stash, nothing to commit besides this report.
5. **CITATION CHECK from the task text honored:** the cited dead SHAs dd543ec/e9ef358 were already re-resolved by the prior window (dd543ec→d035911, e9ef358→ca514a8, exact patch-id) — no reliance on unreachable SHAs this window, nothing to fix in the source item (row 148's own note already cites the reachable twins).

## b) PARTIALLY DONE

Nothing is partially done from THIS window — it was fully a verify-and-stop window. The task itself ships complete: 11/28 residue SHAs resolved content-exactly, 15 ruled citation-lost with per-SHA anchors, verdicts recorded durably in the baseline header. Rows drop when the refs/original drop lands (deliberately deferred to the dependent row).

## c) NOT STARTED (dependent rows, out of scope by design)

1. **The refs/original drop itself** (`refs/original/refs/heads/master` → e9ef358 still pinned) + subsequent purge of all baseline rows — owner-gated (irreversible), queued as the dependent row.
2. **Post-drop gc cycle + gate re-verification.**
3. The §e improvements of the 07-07 report (patch-id-first playbook note in AGENTS.md, baseline-validation self-test, premature-gc guard) — none started this window; deliberately not drive-by'd.

## d) TOTALLY FUCKED UP

Honest misses, this window:

1. **The queue dispatched a closed ID a FOURTH time** (07-07 ship → 07-13 verify dispatch, ~6 minutes). Four total dispatches on this ID; the turn-1 ritual was again the only guard. Each re-dispatch costs a full context load for a two-command verification. Nothing I control can suppress it — it is a dispatch-layer defect, flagged repeatedly now (05-57 §g, 06-04 §g, 06-07 §g).
2. **No self-review was requested on the 07-07 ship window's own report** — its §d honestly lists the stale-read round-trips and the daemon amend-race; I have nothing new to confess on the work itself, and this window's only process miss is nil. I refuse to pad this section (the 06-55 window set the precedent: padding refused).
3. **Micro-miss, mine:** my first command batch ran `rg -l` and `git log`/`git status` as a chain that also included `git stash list` — fine — but I initially piped the report read through `cat` in bash instead of the View tool. Zero damage here (read-only), but it is the same View-before-write discipline the 07-07 window burned round-trips on. Reading only, so no cost paid — noted for the next editing window.

## e) WHAT WE SHOULD IMPROVE

1. **Repeat-dispatch suppression at the source** — the single highest-leverage fix on this ID's history: the queue should refuse (or downgrade to a cheap echo) a dispatch whose row is already `[x]` with a DONE note. Flagged in four consecutive windows; still no structural guard.
2. **Verify-window minimal format** — a re-dispatched closed ID needs no a–g report at all; a three-line verdict + gate rc would do. This report follows the mandated a–g format per the close-out ask, but the standing convention question (06-50/06-55) deserves an owner ruling.
3. **Baseline-count semantics** — 973→110 cite-sites across passes, but 105→110 RESOLVED-era growth shows reports keep minting cite-sites for already-baselined SHAs; a one-line trend note per report is cheap and would make shrink progress visible (07-07 §f22).
4. **Carry the 07-07 improvement list forward, don't re-derive it** — patch-id-first matcher codification, baseline self-test, gc guard are all queued in that report's §f1–4; this window adds nothing to them.

## f) Up to 50 things to get done next

Most are already recorded in the 07-07 report §f1–25 — I point at that list rather than duplicate it, and add only what THIS window surfaced:

1. **Execute the refs/original drop + baseline purge** (07-07 §f1) — the actionable core; verdicts are recorded, the drop is mechanical, owner approval outstanding (07-07 §g1).
2. **Baseline-validation mode in `check-dead-sha-refs.sh --self-test`** (07-07 §f3): rows real+unreachable, RESOLVED twins reachable — would have caught a typo'd twin mechanically.
3. **Premature-gc guard in ci-local** (07-07 §f4): "refs/original exists OR baseline empty, else fail".
4. **`scripts/session-start.sh`** (existing row 149) — still unchecked; would have automated this window's turn-1 grep.
5. **Verify-window minimum-battery convention** (existing row 150) — would standardize what this window ran (gate rc + footer trailer check + tree state).
6. **Repeat-dispatch suppression** (§e1) — flag again with four-window evidence.
7. **AGENTS.md note: patch-id-first twin matching is the canonical resolver** (07-07 §f7, §e1).
8. **One-line baseline-count trend note per report** (§e3).
9. **Convert tolerated RESOLVED citations to `old→new` arrow-form** (07-07 §f9/§g2) — shrinks the baseline ~11 rows without the drop.
10. **Structural fix for the daemon amend-race** (07-07 §f10) — the generator of ALL residue classes.

(10 offered; items beyond this duplicate 07-07 §f11–25 verbatim — padding refused, same refusal the shrink pass and the 06-55 window made.)

## g) Questions I can NOT figure out myself

1. **Repeat-dispatch policy:** this ID has now been dispatched FOUR times (05-35 ship, then verify dispatches at ~06-00, ~07-07-window follow-ups, and this one). Should the dispatcher stop minting tasks for rows already `[x]` with DONE notes — and if the mint is intentional (re-verification), should the prompt say so explicitly so the verify window can skip the full a–g report?
2. **Refs/original drop approval + executor:** owner-run or agent-run once approved? The verdicts make it mechanical, but gc afterwards makes the 15 LOST SHAs permanently underivable — the approval is yours alone (carried from 07-07 §g1, still unanswered).
3. **Arrow-form conversion ruling:** may RESOLVED cite-sites whose prose tolerates it be rewritten to `old→new` fork-record form now (shrinking the baseline ~11 rows immediately), or do they stay untouched until the drop? (carried from 07-07 §g2)
