# Status: dead-sha gate --self-test pin (task 000001a0bd03) — re-dispatch, verify-only window

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_06-17_task-000001a0bd03e8a51dff61e137aa26e541a0.md`, which owns any open residue.

**Date:** 2026-09-20 06-21
**Task:** check-dead-sha-refs.sh `--self-test` mode: pin the gate's semantics durably
**Outcome:** ALREADY DONE by prior window — this dispatch was a repeat; verified, cited, stopped. No new commit (tree clean, nothing to commit).

## What this window did

Repeat dispatch of task 000001a0bd03. Session-start ritual (AGENTS.md turn-1 rule: grep
prior reports for the same task ID) caught the prior completion immediately:

- Prior work commit: 0822025 "test: pin dead-sha citation gate semantics via --self-test"
- Prior report: docs/status/2026-09-20_06-17_task-000001a0bd03e8a51dff61e137aa26e541a0.md
- TODO_LIST.md row 142 already `[x]` with DONE note.

Independent re-verification this window (not trusted from the report):

- `./scripts/check-dead-sha-refs.sh --self-test` → rc=0, "dead-sha self-test ok (clean rc=0,
  exact file:line on rc=1, arrow old→new passes, baselined silent unless TQ_DEAD_SHA_VERBOSE=1)"
- `./scripts/check-dead-sha-refs.sh` (live gate) → rc=0 at HEAD
- `bash -n scripts/check-dead-sha-refs.sh` → clean
- ci-local wiring confirmed present: `scripts/ci-local.sh:283-284`, step right after the gate
- All four pinned semantics read directly from the script's self-test section:
  1. clean/baselined run rc=0 with hit count `dead-sha refs ok (2 baselined, 0 new)`
  2. baselined hit silent unless `TQ_DEAD_SHA_VERBOSE=1`, then exact `file:line cites '<sha>'`
  3. non-baselined dead SHA → rc=1 with exact `DEAD SHA: docs/status/report.md:1 cites '<sha>'`
  4. arrow fork-record line (`old→new`) passes

## a) FULLY DONE

- The self-test pin itself (prior window, re-verified here): run_gate extraction (baseline +
  files as caller state), /tmp fixture git repo with a dangling commit built via
  `commit-tree 'HEAD^{tree}' -p HEAD` (no git reset), all four branches pinned.
- ci-local wiring next to the gate (mirror of check-gosec.sh --self-test pattern).
- TODO_LIST row closure with DONE note and verification citations.
- Status report indexed (prior window's 06-17 report + this one, appended by this commit).

## b) PARTIALLY DONE

- Nothing for THIS task. The task's own row is fully closed.

## c) NOT STARTED

- The follow-up row that the baseline-shrink window left behind (TODO_LIST row 148, deliberately
  out of scope here): positional mapping / ruling for the 28 remaining baselined SHAs. It is a
  separate backlog row and correctly NOT part of this dispatch.

## d) TOTALLY FUCKED UP

- Nothing in this window. The only "failure" is upstream: the queue re-dispatched a task whose
  DONE state was already committed ~4 minutes before this window started (prior report 06-17,
  this window 06-21). Zero wasted work, but the re-dispatch itself is queue-side pacing noise —
  the fastest correct response (verify → cite → stop) was followed.

## e) WHAT WE SHOULD IMPROVE

- Nothing in the gate or its wiring. In passing: the self-test's fixture uses `mktemp -d` +
  trap-cleanup, which is correct and hermetic; no fixture material ever lands inside the gated
  tree (the AGENTS.md daemon-food hazard does not apply here).

## f) NEXT (already in backlog — not duplicated here)

This task added no follow-ups. The relevant existing residue is:

1. TODO_LIST row 148: resolve-or-rule the 28 baselined dead SHAs positionally while
   refs/original + reflog still hold the old lineage (time-boxed by any future `git gc`).

Beyond that, everything this task touched is closed; new items belong to other windows.

## g) QUESTIONS (owner-only)

1. The queue re-dispatched this task minutes after its DONE commit landed. Is that a deliberate
   retry policy (e.g. the 06-17 close-out hadn't recorded its verdict yet when this was enqueued),
   or a sweep bug worth a `tq audit` look?
2. Should the baseline shrink's 28-SHA residue (row 148) be prioritized before the refs/original
   drop, or is the drop consciously deferred until after it?
3. None other — the task left no open design surface.

---
Task-Queue-ID: 000001a0bd03e8a51dff61e137aa26e541a0
