# Task 000001a0c1fbe85b1c43d84a48539f16c43e — Fourth verify-window re-verification record

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-21_05-39_task-000001a0c1fbe85b1c43d84a48539f16c43e.md`, which owns any open residue.

Verify→cite→stop repeat dispatch, 2026-09-21 05:58 CEST. FOURTH artifact on
this ID, 7 minutes after the third (05-51). Zero source edits — the work
shipped in the 05-39 work window (footer commit 90ed386), reviewed in
05-43 (f138eba), re-verified in 05-51 (30df914, the only delta since:
docs-only). Compact form by choice pending the owner ruling 05-51 §g3
asked for; every load-bearing claim below was re-derived FIRST-HAND this
session at HEAD 30df914.

## Delta statement

Tree at dispatch HEAD 30df914 == the tree 05-51 verified (f138eba) plus
its own report file. `git status` clean, no stash. Nothing to do on the
work item; TODO row 166 already `[x]` with the full citation block.

## First-hand verification at HEAD

- **tq show run — the third-recurrence gap (05-43 §d5, 05-51 §d2) CLOSED
  this window.** The queue record reads: attempts 2 / max 3, attempt 1
  FAILED the verify gate with the row-203 vendor/gofmt structural defect —
  `test -z "$(gofmt -l .)"` walked the gitignored `vendor/` tree while the
  test tail shows every package `ok` (internal/httpapi through webui) —
  exactly the documented owner-only dead-letter machine, not a work
  defect. Full failure log on disk
  (`~/.local/state/tq/logs/<id>.verify-failure.log`). This dispatch (the
  attempt-2 run) will hit the same structural failure after I stop;
  the fix is the owner's, the defect is already row 203.
- **Footer trailers grepped fresh**: 90ed386, f138eba, 30df914 all carry
  the ID; the three daemon folds (458df68 and older) footerless as
  documented.
- **Code grepped fresh with line numbers** (cite-by-grep doctrine):
  aging — `DefaultScoreTTL` 30d (internal/prioritize/sweep.go:78),
  `SweeperConfig.ScoreTTL` zero→default / negative→disable (:100-105,
  :153-154), `verdictStale` (:541-544), mint-time stale re-join
  (:446); eviction — `pruneCache` (:559) + `CachePruned` stat (:125-129);
  store surface `PriorityScores`/`DeletePriorityScores` in the queue
  contract, mirrored both backends.
- **Row 166 `[x]` confirmed by grep.**

## Gate battery, rc captured per the PIPESTATUS rule (file + `$?`, no pipes)

| Gate | rc |
| --- | --- |
| root build+vet (GOEXPERIMENT=jsonv2, chained) | 0 |
| prioritize suite `-count=1 -race` GOWORK=off | 0 |
| sqlite `TestPriorityScoresListAndDelete` targeted | 0 |
| check-doc-refs.sh | 0 |
| check-todo-list.sh | 0 |
| gofmt over tracked files only (vendor-safe form), empty output | 0 |

Expensive tier (nix, ci-local): consciously skipped — delta since the
05-51 verification is one docs commit; no source, dep, template, or css
surface changed.

## Carried items

All 18 carried items from 05-43 §f + the 2 new from 05-51 §f remain open,
untouched (no owner rulings landed). §g questions carried a fourth ask:
the `DefaultScoreTTL` value ruling (3× asked) and the refresh-burst spend
shape (3× asked) now include this record; the compact-form ruling itself
(05-51 §g3) is answered in practice by this file's existence — owner may
retroactively bless or reject it.
