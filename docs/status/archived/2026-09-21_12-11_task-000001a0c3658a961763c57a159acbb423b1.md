# Fourth window — dead-export audit re-run over internal/session (task 000001a0c3658a961763c57a159acbb423b1)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/archived/2026-09-21_11-58_task-000001a0c3658a961763c57a159acbb423b1.md`, which owns any open residue.

Window opened 2026-09-21 12-11 at HEAD d4234d4. This is the FOURTH window on the
same task ID (11-58 work window closed TODO row 179; 12-01 a-g close-out; 12-05
duplicate re-verification; this window was a third repeat dispatch onto an
already-closed row).

## a) FULLY DONE

- **Repeat-dispatch detection at turn 1**: `rg -l <task-id> docs/status/` fired
  immediately (three prior reports found) and TODO_LIST.md:179 confirmed the row
  already checked with a DONE citation. The fastest correct response — verify →
  cite → stop — was chosen before any editing.
- **Independent re-run of the audit** (`scripts/check-dead-exports.sh`, rc=0,
  advisory): same 7 internal/session zero-importer flags as every prior window
  (RegistryEntry, LoadRegistry, RewriteRegistry, ProcessOwner, PgrepOwner,
  SweepOutcome from registry.go; CloseResult from session.go) plus the known
  webui flags (ErrTokenRequiredOnLAN, HealthDashboardPath, HealthSSEPath).
  Verdict unchanged.
- **First-hand substring re-verification** (not just inherited claims):
  `Trailer` and `SyntheticTaskID` confirmed live via outside-file substring hits
  (cmd/tq/session.go, internal/executor/gitscan.go, webui/crush/stats tests);
  RegistryEntry/SweepOutcome/CloseResult confirmed structurally alive inside
  `internal/session` behind `Sweep` (registry.go:225 feeds `[]SweepOutcome`,
  appends `RegistryEntry`). Nothing to prune. Verdict matches all three prior
  reports.
- **Cheap verify battery run BEFORE writing this report's claims** (the
  verify-window minimum battery from AGENTS.md conventions): root
  `go build ./...` + `go vet ./...` under GOEXPERIMENT=jsonv2 + check-doc-refs,
  all rc=0, executed before any DONE-class sentence above was committed to text.
- No source delta, no TODO change needed (row 179 already checked correctly).

## b) PARTIALLY DONE

- Nothing. The work item is a re-run audit; it was re-run and re-verified.

## c) NOT STARTED

- The follow-up backlog row created by the 12-01 window (dead-export heuristic
  advisory annotation for return/parameter-type false positives) remains
  unchecked in TODO_LIST.md:180 — deliberately untouched here; it is separate
  queue work.

## d) TOTALLY FUCKED UP

1. **Fourth window on one ID is itself the finding.** Three prior windows had
   already produced verdict + close-out + duplicate re-verification for this
   exact ID, and the queue re-dispatched it again. Either the row's DONE
   annotation is not suppressing re-harvest (reworded duplicates are the known
   14-01-loop class), or the dispatcher does not consult checked rows. This
   window added no information a fourth time — the marginal value of window 4
   over window 3 was near zero. Worth an owner look at WHY the ID keeps
   re-entering the pool.
2. **Battery ordered after the stop decision, again (pattern recurrence).**
   This window initially intended to stop on inherited citations alone; the
   cheap battery was only run because the closeout contract forced a report.
   The 12-01 window documented the same sequence failure (claim-then-verify);
   the ritual is: verify-only windows run the minimum battery BEFORE citing.
   The saving grace here: no claim was written before the gates ran, so
   nothing needs retracting.
3. **Inherited line-number citations used in the stop decision.** My stop
   message cited cmd/tq/session.go:114/308 from the 11-58 report without
   personally confirming those exact lines (I confirmed the structural liveness
   by grep, but not the line numbers). Per the claims-carry-citations rule, a
   scope claim cites file:line I verified — the 12-05 report independently
   confirmed :325 for CloseResult consumption, but I leaned on it rather than
   re-deriving.

## e) WHAT WE SHOULD IMPROVE

- The audit's false-positive class (7/7 internal/session flags) is now
  confirmed by FOUR independent windows. The backlog annotation row (TODO 180)
  should be prioritized — every re-run burns a full agent window on the same
  manual triage.
- Repeat-dispatch suppression: the pool should not mint work for a task ID that
  already has DONE-cited status reports; this ID has now cost 4 windows.
- The stop-then-verify ordering keeps recurring (02-38 → 12-01 → this window);
  the closeout contract catching it is working, but a turn-1 habit would be
  cheaper.

## f) NEXT (up to 50 — the high-value ones; the queue gates the rest)

Already in TODO_LIST (do not duplicate): the heuristic-annotation row (180) is
the direct follow-up. Candidate NEW observations from this window, in priority
order (the first three are the only genuinely new, in-scope ones — appended to
TODO_LIST below; the rest are recorded here as report-only observations, most
already covered by existing rows):

1. (appended) — see TODO_LIST addition below: investigate why task
   000001a0c3658a96 re-dispatched 3x after its DONE row + 3 cited reports
   (harvest dedup vs re-dispatch path — the 14-01 loop class again).
2. (appended) — see TODO_LIST addition below: the audit's webui flags
   (ErrTokenRequiredOnLAN, HealthDashboardPath, HealthSSEPath) recur every run;
   verify whether they belong in the same annotation class or are genuinely
   dead.
3. (appended) — see TODO_LIST addition below: extend the repeat-dispatch ritual
   into session-start.sh itself (grep the task ID if provided) so turn-1
   detection is mechanical, not memory-dependent.
4-50: everything else observed in passing is ALREADY tracked (lint baseline
   growth policy, index-bloat sweep row 261, internal/e2e race-margin row, and
   the dead-export annotation row 180) — re-listing them here would be duplicate
   backlog noise, which the 12-01 window already flagged as index bloat.

## g) QUESTIONS (cannot figure out myself)

1. Why did this task ID re-enter the pool three times after row 179 was checked
   with a DONE citation — is the re-dispatch coming from harvest rewording, the
   retry ladder, or a manual re-enqueue? (Queue-side fact trail only you can
   rule on; I can trace the journal if given the go-ahead as its own task.)
2. Should the dead-export audit get a strict mode at all, or is the advisory
   raw-report + manual triage the permanent design? (The annotation row 180
   assumes annotation-only; an owner ruling would settle it.)
3. Is the fourth window's cost acceptable under the current pacing gates, or
   should verified-DONE task IDs get a hard mint-block (a `done:` dedup class)
   rather than relying on prompt-level "check for prior windows" rituals?
