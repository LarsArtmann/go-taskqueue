# CI Topology One-Pager (row 64) — close-out RE-dispatch report (02:01)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-15_01-59_task-000001a0a25a92f17ca59c610e0232bead24.md`, which owns any open residue.

**Task ID:** 000001a0a25a92f17ca59c610e0232bead24
**Dispatch type:** verification-only close-out of an already-shipped item. This is the SECOND
close-out for the same task ID (the first is `2026-09-15_01-59_task-000001a0a25a92f17ca59c610e0232bead24.md`,
commit 896ec74) and the THIRD dispatch overall (work commit 46ea40a).

## a) FULLY DONE

- Deliverable verified live at HEAD: `docs/planning/2026-09-15_ci-topology-one-pager.md`
  exists and covers what row 64 asked for — every ci.yml job (test, test-windows,
  test-postgres, nix, govulncheck, gosec, cqrs-lint) and every fuzz.yml job, per-step
  "loops modules?" column, the why (ADR-0011 multi-module: root `./...` never descends
  into nested modules), the `for-each-module.sh` single source, the parity rule, and
  cmd/tq's out-of-loop ADR-0017 treatment.
- TODO_LIST.md row 64: `[x]` with the doc path and a dated done note. Gate-clean.
- Working tree clean at session start; nothing to build or test (docs-only session,
  zero code touched).

## b) PARTIALLY DONE

- Nothing for this task itself. Two hedged claims inherited from the one-pager remain
  spot-check-only (not re-verified this session): the fuzz.yml row's claim that targets
  are invoked "devmod-free from root", and the parity rule's claim that ci-local.sh
  runs the advisory loops identically to ci.yml. Both look right on read; neither got
  a mechanical check.

## c) NOT STARTED

- Nothing owed by this task.

## d) TOTALLY FUCKED UP (honest accounting)

- The close-out itself was already done at 01:59, ~2 minutes before my dispatch. I am
  the fourth paid lap on this task ID (work → close-out → this). In my work turn I
  checked `git log` + TODO state FIRST (correct), which caught it — but I did not
  check the status index for the prior same-ID close-out until this close-out turn.
  The 01-49 golangci report documents the same re-dispatch loop; it persists because
  the mint-time [x]/prior-report check does not exist.
- Cost of this session: one report + index row for zero new information.

## e) WHAT WE SHOULD IMPROVE

- The systemic fix is not per-session diligence — it is a mint-time gate: before
  enqueueing a harvested TODO item, run the same `git log`/grep check the dispatching
  agent will do, and refuse to mint over an already-shipped row.
- Status reports for done-on-arrival sessions should be tiny (this one is) — the
  template should say so explicitly so future agents stop padding.
- The one-pager has no drift protection: nothing fails CI when a workflow step changes
  without a doc edit. A light grep-pair check (every job name in ci.yml appears in the
  one-pager, and vice versa) would be cheap; whether it earns its gate is an owner call.

## f) WHAT WE SHOULD GET DONE NEXT

Top honest items (this report refuses to pad to 50; the earlier same-ID report's
§f 11-row backlog plus the golangci reports' backlogs already cover the CI-adjacent
work — do not duplicate that here):

1. Mint-time done-check (TODO row + same-ID report grep) in the harvester — kills the
   re-dispatch loop that produced this session.
2. Drift gate for the one-pager (job-name cross-check vs workflows), if owner wants it.
3. Spot-verify the one-pager's two hedged claims (fuzz target invocation path, ci-local
   advisory-loop parity) once, mechanically.
4. Sweep the backlog of near-duplicate re-dispatch tasks already in flight (the
   golangci and for-each-module items show the same DONE-on-arrival pattern).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. Should a verification-only close-out of an already-closed task mint a NEW close-out
   at all, or is a single TQ_RESULT line sufficient for the queue when the prior
   close-out report exists and cites the same evidence?
2. Is the one-pager's home (`docs/planning/`) final, or should CI-topology reference
   material live somewhere more discoverable (e.g. `docs/ci/` or linked from AGENTS.md
   Commands section)?
3. Do you want the drift gate (e)3, given every gate has a maintenance cost and the
   workflows change weekly under the pool?
