# Task 000001a0bd9af8a3c9aa05b5776c51557538 — Status Report 2026-09-20 09:10

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_09-06_task-000001a0bd9af8a3c9aa05b5776c51557538.md`, which owns any open residue.

**Window type:** THIRD dispatch of this ID, verify-only re-dispatch (verify → cite →
stop). Zero source delta. Row 157 (webui priority provenance) shipped by the 09-06
window via footer commit 459e3da + close-out 849c1c8.

## a) FULLY DONE

- The deliverable itself (prior window): static priority-provenance DefinitionList
  card on the task detail page — current priority + band, harvest item key +
  marker level, cached AI verdict, repri history from the fact trail
  (render.go, mirroring `tq show`'s buildPriorityProvenance). Row 157 `[x]` in
  TODO_LIST.md:157. Nothing re-shipped, nothing touched this window.
- Fresh battery at HEAD this window, exit codes captured directly (no pipes):
  - root `go build ./...` + `go vet ./...` under GOEXPERIMENT=jsonv2 — rc=0
  - `go test ./internal/webui/ -run Provenance` — rc=0 (first battery)
  - `go test ./internal/webui/ -count=1` full suite — rc=0, ok 10.566s
  - `./scripts/check-doc-refs.sh` — rc=0
- Turn-1 ritual executed this window: `scripts/session-start.sh <task-id>` rc=0
  with the ID grep hit (2 lines), whole-repo git log/status, CONTRIBUTING.md
  read IN FULL (74/74 lines — first time in this ID's lineage; the ship and
  close-out windows both disclosed skipping it).
- Provenance verification: commit 459e3da message carries the exact footer ID
  (read %B directly), TODO row confirmed `[x]`, prior report
  2026-09-20_09-06_…md found via `rg -l` before any work.

## b) PARTIALLY DONE

- Nothing for the deliverable — it is binary done/not, and it is done.
- Battery scope: race suite, nix build, and the webui smoke were NOT re-run.
  Justification: zero source delta since 459e3da; the 09-06 window declared the
  same evidence scope. Stated, not hidden.

## c) NOT STARTED

- Nothing new was started, by design. This window's job was verification and
  this report.

## d) TOTALLY FUCKED UP (self-review, honest)

1. **First battery was narrower than the family bar.** My opening battery ran
   only the `-run Provenance` slice; the full webui suite (10.5s) came only
   because I extended it for this report. The 09-06 window's own battery (full
   suite) should have been the verbatim re-run target from the start —
   re-running the lineage's pinned battery is the codified rule, not a
   report-drafting afterthought.
2. **Session-start script not run at turn 1.** `scripts/session-start.sh` shipped
   07:30 TODAY (task 000001a0bd48…, row 149) and I still did the ritual by hand
   piecemeal (git log + TODO grep, no stash list, no CONTRIBUTING). Ran it
   retroactively for this report; the N+1 recurrence family continues.
3. **The dispatch layer re-fired a closed ID a third time** (09-06 ship + close-out,
   ~09:08 verify, 09:10 this report). Every guard so far is the agent-side
   ritual; the queue has no DONE-row suppression. Not my work item, but it is
   the structural cause of this window existing.
4. **No fresh delta was produced and that is fine, but I nearly stopped without
   a citation.** The first assistant turn ended with a one-line stop. Per the
   verify-window minimum battery rule a verify window must state why no fresh
   delta exists: none exists because HEAD is unchanged since 459e3da and the
   battery re-confirms it green.

## e) WHAT WE SHOULD IMPROVE

1. **Re-run the pinned battery verbatim, first thing.** Every verify window
   should copy the prior report's gate list and execute it in order before any
   prose. (Recurring §d1 in this family.)
2. **`session-start.sh` as the literal first command**, not a reconstructed
   ritual — it is shipped, cheap, and greps the ID for me. Mechanical: alias it
   into the task prompt contract if the queue can carry that.
3. **Queue-side re-dispatch suppression**: a DONE row with a fresh verify
   citation should not mint again within hours (see §g1).
4. **Verify-window report format**: this family now has ~10 reports saying the
   same three sentences (row [x], battery green, zero delta). A short-form
   verify template (row, battery, delta-reason, carried §f/§g) would cut the
   index bloat that rows 137-157 are visibly causing.

## f) WHAT WE SHOULD GET DONE NEXT (carried from 09-06 §f + this window; honest, not padded)

1. Provenance on task TABLE rows (badge/cell), not just detail page — 09-06 §g1
   owner question, unresolved.
2. Repri-history cap/collapse for tasks with long fact trails — 09-06 §g2.
3. Cached-verdict → minting batch-task link on the provenance card — 09-06 §g3.
4. DONE-row re-dispatch suppression at the queue source (row 157 fired 3×).
5. Short-form verify-window report template to slow status-index bloat.
6. Owner push obligation: the repo carries an unpushed lineage; refs/original
   cleanup after push remains gated on it (carried family-wide).
7. lint-baseline: `--check` never run live this week by any window I read
   (carried 08-11 §f).
8. AGENTS.md baseline-count split (~400 in CONTRIBUTING vs 1113 in AGENTS) —
   one-line fix, still open (carried).
9. session-start.sh could absorb the verify-battery: a `--verify` flag re-running
   the cheap gates would make §e1 mechanical.
10. Everything else from 09-06 §f stands; this window found no NEW work items
    and refuses to pad the list to 50.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Re-dispatch policy**: should the queue skip minting when the row is `[x]`
   AND a same-ID verify report exists within N hours — or is repeated
   verification desired (drift insurance)?
2. **Verify-window commit policy**: should zero-delta verify windows ship a
   footer commit (this report) every time, or fold into the daemon and stay
   commit-less? The 04-25 window called the footer commit the KEY DELTA; the
   04-19 window shipped none. I shipped one here.
3. **Battery bar for zero-delta windows**: full prior battery verbatim (10.5s +
   here) or the minimum battery (build/vet/doc-refs) plus a delta statement?
   The 03-27/03-39 precedent reads as "full battery only for verify-ONLY
   re-dispatch windows" — is a third dispatch still that, or a lighter class?
