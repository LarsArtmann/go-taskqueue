# Task 000001a0c124c6941f3a6cd06553a3140e2c — ReviewResult session usage (09-52 §f3)

> **ARCHIVED 2026-09-21 (docs-health archive sweep)** — every forward-looking item verified resolved at HEAD (per-item triage): review-card usage line shipped (fragments.templ sessionUsageText, 4 render sites) + routed row 326; §g: none new. No open residue.

Window: 2026-09-21 ~01:20 – 01:50 CEST. Work item: add the four derived usage
fields + deriveOutcome wiring to the review executor so paid review turns
surface in the budget token projection (09-52 report §f3, TODO_LIST row 161).

## a) FULLY DONE

- **`ReviewResult` usage fields** (internal/executor/review.go): `SessionCostUSD`
  / `SessionPromptTokens` / `SessionCompletionTokens` / `SessionMessageCount`
  with the exact shared json keys (`session_cost_usd`,
  `session_prompt_tokens`, `session_completion_tokens`,
  `session_message_count`), placed between `SessionID` and `LogPath`,
  mirroring PrioritizeResult's shape and doc convention.
- **Derivation wired** in `ReviewExecutor.Execute` (internal/executor/review.go):
  `deriveOutcome(ctx, repoDir, result.SessionID, t.ID)` after the session-id
  extraction, before the sidecar write — the same insertion point and
  best-effort comment contract as prioritize.go. Reviews make no commits
  (read-only rule), so only the Session* fields are surfaced, exactly like
  the prioritize executor.
- **Budget consumption needed ZERO budget-code change**: `Guard.usageSince`
  parses completion-fact details into `sessionUsageDetail` by json key only
  (internal/budget/budget.go), so review completions count as soon as the
  result carries the shared keys. The two enumerating doc comments were
  updated to name ReviewResult alongside AgentResult/PrioritizeResult.
- **Drift pin extended** (internal/budget/budget_test.go): a ReviewResult
  completion fact joins `TestUsageTodaySumsDerivedSessionUsage` (marshalled
  through the REAL type; want now Runs 3 / $0.65 / 2300+4300 tokens / 18
  messages), so a json-key rename in any of the three result types fails
  the budget suite in both directions.
- **TODO_LIST row 161 ticked in place** with full text preserved + a
  verification citation (the claims-carry-citations convention).
- **Verification battery, all fresh at this HEAD**:
  - executor module: `GOWORK=off` build + vet + `go test -count=1` rc=0
    (ok 288.045s).
  - budget package: root-module package, so the stale-`vendor/` trap fired
    first (vet: unknown field SessionCostUSD); `go mod vendor` + build +
    vet + `go test -count=1` rc=0 (ok 0.008s).
  - root: build rc=0, `go vet ./...` rc=0, `go test ./... -race -count=1`
    rc=0 (full suite, RACE_RC captured to file per the PIPESTATUS rule).
  - `gofmt -l` clean on both modules; `scripts/check-facade-parity.sh` OK
    (field additions are not new package symbols).

## b) PARTIALLY DONE

- Nothing in the item's scope. The remaining usage-derivation surface
  (StatusResult, TODO row 162; executor-side json-key pin, row 163) was
  deliberately left out — the contract said smallest correct change.

## c) NOT STARTED

- Nothing within the item's scope.

## d) TOTALLY FUCKED UP

- **LOST the daemon race on BOTH commits — seventh fold in the report
  family, and this time the SOURCE files went footer-less, not just docs**:
  the executor + budget edits were swept into `484d2a5` (chore, 3 files)
  while the 288s executor suite ran in the background, and the TODO tick
  into `f32a74e` (chore) before the explicit commit could happen — the
  window committed NOTHING footer-carrying until this report. No history
  rewrite (both commits unpushed, but the filter/rebase remedy is
  owner-gated while the fold ruling is pending; policy: never rewrite under
  the live daemon). Sanctioned default remedy applies: this fold record,
  the index row and the new follow-up row all ride the footer-carrying
  commit, so the queue's footer derivation covers the window. Structural
  confirmation of the 01-11 doctrine: the loss variable is wall-clock
  DIRTY time — long background gates between edit and commit guarantee a
  fold. New corollary recorded in §e.
- **LSP phantom red across the edit window**: gopls served stale-vendor
  `MissingLitField` errors on budget_test.go (and golangci_lint_ls the same
  typecheck) for the entire post-edit period until an `lsp_restart`; the
  CLI gates were green throughout. Known class (trust the CLI), first time
  recorded for the root-module vendor staleness variant.

## e) WHAT WE SHOULD IMPROVE

- **Commit source BEFORE running long gates** (corollary to the 01-11
  critical-section doctrine): for executor-class windows the suite is
  minutes long, so edit → commit → gate beats edit → gate → commit — the
  gate run is exactly the window the daemon sweeps in. Verification gates
  do not need the commit to wait; a failed gate is followed by a fix
  commit, which is normal dogfood flow.
- **Root-module packages + internal edits = mandatory `go mod vendor`
  before ANY root gate** — already documented as a Known Issue; this
  window is one more datapoint that it fires on TEST files too, not just
  builds.

## f) Up to 50 things we should get done next

1. `tq show` review-task detail: render session tokens/cost for review
   turns the way agent and prioritize tasks already show theirs (mirrors
   09-52 §f5's prioritize treatment; internal/webui). (appended to
   TODO_LIST this window)

## g) Questions (up to 3)

- None new; the cap-unit ruling (09-52 §g1) already governs whether
  review spend joins the daily-cap math.
