# Status Report — task 000001a0bd1f5fec7690ce4873fd70a0c8e8 (third window, close-out)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_06-45_task-000001a0bd1f5fec7690ce4873fd70a0c8e8.md`, which owns any open residue.

**Date:** 2026-09-20 06:55 CEST · **Window type:** verify→cite→stop re-dispatch (THIRD on this ID in ~10 minutes) · **Source delta:** zero (docs-only window: this report + index row)

The work item (committed wrapper-path smoke leg for `tq crush`) shipped at
06:45 in commit d1290cf and was already verify-confirmed at 06:50. This
window re-ran the gate fresh at HEAD and produced this close-out.

## a) FULLY DONE

- **The work item itself** (shipped 06:45, d1290cf): `tq crush --bin <stub>` committed-path leg in `scripts/smoke/session-close.sh:136-181` — stub child makes a `Crush-Session` footer commit, prints `session_id:`, exits 5; the wrapper must scan the id, attribute exactly 1 commit, mint exactly 1 review + 1 status into a fresh `--db`, append exactly 1 `session.closed` fact, and pass the child's exit code through. Rides the existing ci-local session-close step (line 248-249); no ci-local edit needed.
- **TODO_LIST.md row closed** (`[x]` at line 146) — closed in the shipping window, not this one.
- **This window's verification:** built `tq` fresh (`scripts/build-tq.sh`), ran `./scripts/smoke/session-close.sh` → all legs green, `rc=0` (no-id path: child code 7 preserved, no DB touched; committed path: id scan, 1+1 mint, exit 5 passthrough; replay-safe second close). Evidence in this window's shell transcript; gate name cited per the claims-carry-citations rule.
- **§g1 of the original item** ("gate-vs-cited-one-off ruling pending") is moot: the cited ad-hoc stub run is now a committed gate leg.

## b) PARTIALLY DONE

- Nothing in the work item. The only partial surface is meta: the
  re-dispatch suppression question (§g of 06-50) remains open — this is
  the third dispatch on a closed ID, and the only guard that fired is the
  session-start ritual (task-ID grep), not anything in the queue.

## c) NOT STARTED

- Nothing carried from this item. The §f backlog below is candidate fuel,
  not owed work.

## d) TOTALLY FUCKED UP

Nothing in this window broke. Honest misses, this window and inherited:

1. **Third dispatch on a closed task ID in ~10 minutes** (06-45 ship → 06-50 verify → 06-55 close-out). Each window's ritual caught it, but the dispatch layer itself has no suppression — pure queue-side inefficiency, ~3× window cost for one shipped item.
2. **This window initially answered with a bare "done, verified" and no report** — the close-out obligation (report + index + footer commit) only fired when explicitly asked. The 06-50 verify window DID write its report; inconsistent verify-window behavior across windows.
3. **No fresh `go test ./... -race` in this window** — the smoke exercises the CLI end-to-end, but the session-start ritual and the contract's verify gate would have wanted the standard battery cited too for a *close-out* claim (the 06-45 shipping window ran its gates; inherited, not re-proven here).
4. **No re-dispatch note back into the queue** — a window that detects "already DONE, dispatch N+1" records it in reports but has no mechanism to mark the task consumed at the source.

## e) WHAT WE SHOULD IMPROVE

1. **Verify-window report format**: when the ritual detects a repeat dispatch of a DONE row, the window should still close out (report + index + footer commit) — make that the default, not explicit-ask-triggered.
2. **Verify windows should cite the standard battery** (build/vet/test or explicitly state scope exclusion) so close-out claims are uniformly evidenced.
3. **Dispatch-side dedup**: the queue should refuse re-dispatch of a task whose ID has a DONE verdict + footer commit in git (a `git log --grep Task-Queue-ID` check at enqueue time would catch this class).
4. **Gate quality is good** — session-close.sh now covers both wrapper paths hermetically; keep the pattern (committed stub, fresh scratch DB per leg, exit-passthrough pin) as the template for future CLI E2E legs.

## f) Up to 50 things we should get done next

Carried by pointer from the 06-45/06-50 reports (their §f lists remain
canonical) plus this window's additions. Bounded list; padding refused.

1. Queue-side repeat-dispatch guard: reject enqueue of a Task-Queue-ID already carrying a DONE verdict (root fix for §d1).
2. Verify-window close-out convention: ritual says "already DONE" → still write report+index+commit by default (§e1).
3. Close-failure visibility in `tq crush` (carried 06-45 §g): surface a close that failed after the child succeeded.
4. Fresh-vs-shared smoke DB policy for wrapper legs (carried 06-45 §g) — current fresh-DB approach works; pin it in a comment or AGENTS.md.
5. General gate-promotion ruling (carried §g): which cited one-off verifications MUST be promoted to committed gates, and on what cadence.
6. ci.yml parity check for the session-close smoke (it runs in ci-local; confirm the CI workflow runs it too — noticed in passing, unverified this window).
7. Baseline shrink follow-ups from the sibling dead-SHA-gate windows (residue policy ruling, gate --self-test, refs/original drop gating) — owned by those windows, listed here only as cross-reference.

Items 8-50: **deliberately empty.** This item is closed end-to-end; inventing 43 more rows would be padding. Real backlog growth belongs to the docs-health HARVEST pass over the sibling windows' reports, not to this one.

## g) Questions I can NOT figure out myself

1. **Repeat-dispatch suppression policy (owner ruling needed):** should the queue hard-block a re-enqueue whose Task-Queue-ID already has a DONE verdict/commit, and should agents auto-close (report+commit) without an explicit close-out ask when the ritual detects dispatch N>1?
2. **Verify-window report format:** is a short verify-style report (like 06-50 and this one) acceptable as the standing format for re-dispatch windows, or should they be folded into an amendment of the shipping window's report?
3. **Gate-promotion rule:** is there a standing rule for which ad-hoc verifications must be promoted into ci-local gates (the original §g1), or does it stay case-by-case owner judgment?
