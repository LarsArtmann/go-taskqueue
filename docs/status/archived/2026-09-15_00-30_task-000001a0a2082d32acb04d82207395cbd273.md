# Status Report — errname scoped-lint re-dispatch (task 000001a0a2082d32acb04d82207395cbd273)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-15_00-27_task-000001a0a2082d32acb04d82207395cbd273.md`, which owns any open residue.

Written: 2026-09-15 00:30. Scope: THIS task only (the errname scoped-lint
confirmation). No unrelated research.

## What this turn did

Re-dispatch of the same TODO row (already `[x]` since commit 0e70385).
This turn was an independent re-verification, zero deliverable changes:

1. Read repo state: TODO_LIST.md:58 already `[x]`; report
   `2026-09-15_00-27_...md` exists and is indexed; tree clean; no
   errname-related code changes anywhere to make.
2. Re-ran the scoped lint myself: `cd internal/runactor && GOWORK=off
   golangci-lint run ./...` — **zero errname findings** (grep count 0).
   12 remaining findings, all known advisory/policy-disabled classes
   (paralleltest 6, mnd 2, err113 2, containedctx 1, nilerr 1 — mnd and
   paralleltest were POLICY-DISABLED per the round-13 baseline note, yet
   still surface here: see d2).
3. Did NOT commit anything for the "work" — nothing changed. The prior
   session's commits (0e70385 confirmation, 6700911 report) stand.

## a) FULLY DONE

- Independent re-verification that errname is quiet on internal/runactor
  (citation: the scoped lint run above, this turn).
- Confirmed the TODO row, status report, and index row all exist —
  DONE-on-arrival, no rework needed.
- Working tree left clean; repo not broken.

## b) PARTIALLY DONE

- Nothing. The work item is complete; this turn added only a second
  verification data point.

## c) NOT STARTED

- Nothing owed by this task. (The 12 residual findings belong to the
  standing advisory lint baseline, not to this item.)

## d) TOTALLY FUCKED UP (honest)

1. **First-turn dedup miss (partially excusable):** the re-dispatch
   arrived after the work was already done; I spent a full turn before
   noticing the `[x]` at TODO_LIST.md:58 and the 00-27 report. The
   session-start ritual (git log + grep the item text FIRST) would have
   deduped in one tool call.
2. **Pipe-masking in my own verification:** my first lint invocation used
   `| tail -20` and read the summary from filtered output — exactly the
   pipeline-masking trap this repo's own memory warns about. I corrected
   with an explicit `grep -ci errname` + exit-code read, but the first
   read was not the evidence; the second was.
3. **Unverified toolchain identity (carried from 00-27 §d, still true):**
   "golangci-lint run" reports errname=0, but the installed binary and
   its linter set were never pinned, so "quiet" is version-relative.

## e) WHAT WE SHOULD IMPROVE

1. Re-dispatch protocol: grep the TODO row text BEFORE any gate run —
   DONE-on-arrival should cost one grep, not a lint campaign.
2. Verification reads must bypass filters: run the check, capture exit
   code, then filter for display. Never judge from `tail`/`head` output.
3. Pin linter provenance for confirmations: record the golangci-lint
   version alongside a "linter X is quiet" claim.

## f) NEXT (delta items only — the 00-27 report §f and the standing backlogs remain authoritative)

1. Record the golangci-lint version used for this confirmation in the
   00-27 report or AGENTS.md (one line; closes d3).
2. Investigate why mnd/paralleltest findings surface in a scoped
   internal/runactor run despite the 2026-09-14 policy-disable — either
   the disable scoping is narrower than remembered or the local binary
   ignores the root config section (would also affect the baseline gate).
3. Nothing else minted from this task — do not inflate the backlog with
   derived busywork.

## g) QUESTIONS I CANNOT RESOLVE MYSELF

1. Which golangci-lint binary/version is canonical for "linter X is
   quiet" claims (devShell-pinned vs PATH-resolved), so confirmations are
   reproducible?
2. Are mnd/paralleltest expected to be quiet under the current
   `.golangci.yml` policy-disable for scoped sub-module runs, or were
   they disabled only for the baseline count?
3. Should a no-change re-dispatch of an already-done task mint a report
   at all, or is a TQ_RESULT citing the prior commit the intended
   spend-free close?
