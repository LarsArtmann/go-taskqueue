# Task Close-Out Report — RELEASE.md (round-2 multi-module release flow) + brutal self-review

- **Written**: 2026-09-10 06:55 CEST
- **Task**: `000001a089a3864d95e427c8af75793a37be` — TODO_LIST item f26:
  "Document the round-2 multi-module release flow as docs/release/RELEASE.md
  (sub-tag cutting, sibling-replace allowlist, two-phase --tag/--push;
  scripts/release.sh owns the code) (23:47 f26)"
- **Work commit**: `41b817b` (master, not pushed — agents never push).
  > **ANNOTATION 2026-09-10 08:25 (docs-health, non-destructive):** the
  > work commit was subsequently reworded for a footer fix (07:49 session):
  > the corrected twin is `cd09c1c` on local master (message-only rewrite,
  > empty tree diff); `41b817b` survives on `origin/master` and in the
  > v0.2.0 tag lineage, which forked at `c2eb01c` as a result. See the
  > 07-49 close-out report for the full fallout. Cross-links added in the
  > same pass: RELEASE.md ↔ VERSION-SURFACES.md + AGENTS.md release-doc
  > pointer.
- **Method**: the code was read before the doc was written —
  `scripts/release.sh` (all 177 lines), `scripts/lib/release-gates.sh`,
  `scripts/smoke/release-gates.sh`, the archived v0.1.0 checklist, and the
  round-2 status report's release sections. Every doc claim traces to a
  specific script behavior; no flag, phase, or policy is documented from
  memory.

## TL;DR

| Area             | Verdict                                                                                                               |
| ---------------- | --------------------------------------------------------------------------------------------------------------------- |
| The work item    | Done in one file: `docs/release/RELEASE.md` (99 lines), TODO item checked with a DONE note                            |
| Doc truthfulness | Every claim sourced from the script/lib/smoke this session; nothing documented from memory                            |
| Gates            | Root build/vet/test green; release-gates smoke green; doc-refs green; TODO parse gate green                           |
| Honest gaps      | Doc is not cross-linked from any living doc (half a ghost system); release.sh never executed in any mode this session |
| Self-verdict     | Clean scope discipline, one deliberate boundary call (no cross-links) that needs an owner nod                         |

---

## a) FULLY DONE

1. **RELEASE.md written, exactly the three named subjects.**
   `docs/release/RELEASE.md` covers: (1) the two-phase `--tag`/`--push`
   split with the safe gates-only default and the resume-at-HEAD semantics,
   (2) sub-tag cutting — disk-derived via `find internal -name go.mod`,
   one annotated `internal/<mod>/vX.Y.Z` per module including
   zero-consumer modules, idempotent, plus the dev-side rule (bump the
   require line AND cut the sub-tag), (3) the sibling-replace allowlist —
   the exact `gate_gomod` rules (one nesting level, no pseudo-versions,
   requires must have cut sub-tags) and the round-2 lesson that the
   single-segment regex silently stopped matching nested modules, hence the
   fixture smoke. Framed throughout as "scripts/release.sh owns the code,
   this doc explains it", per the work item.
2. **No invented content.** The doc's phases, gate order, version-sync
   checks, proxy verification (5 attempts), clean-room `go install` proof,
   and owner decisions (no squash, v0.x prerelease, immutable tags) were
   each verified against `release.sh`/`release-gates.sh` or the archived
   v0.1.0 checklist this session. The gates-only → `--tag` → `--push`
   sequence, resume path, and the auto-commit-daemon tag-timing warning are
   quoted from the script's own logic and comments.
3. **In-passing cross-checks held.** All seven `internal/*/v0.2.0` sub-tags
   exist; root `go.mod` carries real `v0.2.0` requires for all seven
   internal modules; the v0.2.0 release already happened, so the doc
   documents a proven flow, not a planned one.
4. **Gates green for a docs change.** `go build ./...`, `go vet ./...`,
   `go test ./... -count=1` (root module, all packages ok);
   `scripts/smoke/release-gates.sh` (real tree + 5 poison fixtures, all
   green); `scripts/check-doc-refs.sh` (no ghost references);
   `scripts/check-todo-list.sh` green after the checkbox flip.
5. **Loop closed, contracts honored.** TODO_LIST f26 is `[x]` with a DONE
   note in the file's own convention; commit `41b817b` carries the
   `Task-Queue-ID` footer; nothing pushed; `.crushrc`/`crush.json`/
   `.tq-verify` untouched; zero drive-by changes — the diff is +99 doc
   lines and one TODO line.

## b) PARTIALLY DONE

1. **The doc is not linked from anywhere living.** Nothing in FEATURES.md
   (whose release-runner row at line 120 is the natural anchor), README, or
   AGENTS.md points at `docs/release/RELEASE.md`; discovery is TODO_LIST
   history + directory listing only. A doc nothing links to is half a ghost
   system. I chose strict scope (the item named the file, not the
   cross-links) — a deliberate call, owned here, one-line fix proposed in
   (f).
2. **Doc accuracy rests on reading, not executing.** `release.sh` was never
   run this session: gates-only mode triggers the full CI replicant (nix
   build + webui smoke — heavy for a docs-only change) and `--tag` would
   cut real tags. The v0.3.0-derivation and resume paths were tested by the
   round-2 session (per its report), not re-verified now. The doc will get
   its real test the next time a release is actually cut.
3. **Scope boundaries left as separate open items, deliberately:**
   the version-surface inventory (who must move when: flake attr +
   ldflags + root tag + sub-tags + CHANGELOG) is TODO f27, adjacent but
   distinct; the "first multi-module release retrospective" (round-2 report
   item 50) is a different artifact and, with v0.2.0 shipped, now due.

## c) NOT STARTED

1. **f27 version-surface inventory doc** — untouched (RELEASE.md sets up
   its vocabulary but does not do its job).
2. **Cross-linking RELEASE.md into FEATURES/README** — not done (see b1).
3. **Round-2 item 50: multi-module release retrospective** — not started.
4. **Remaining Post-split follow-ups seen in passing** (f28/f29 lint
   slice-triage, f30 `ExitCause`→`ExitError`, f32 per-module golangci in
   ci.yml, f35 multi-repo smoke on the nix binary) — untouched, not
   re-verified this session.

## d) TOTALLY FUCKED UP

1. **Nothing.** The diff cannot break anything: no code, no go.mod/go.sum,
   no flake inputs, no tags, no config, no pushes. Master's pre-existing
   red state (runner-only nix FOD mismatch, known class) is unrelated to
   this change and disclosed, not hidden.
2. **Two warts worth owning, neither shipped damage:**
   (a) My `flake.nix` version grep returned nothing and I had to re-derive
   why (the script's gate matches `version = "0.2.0"` without the `v`; my
   grep pattern was `v`-prefixed). The trap was real: "empty grep output =
   missing version line" would have been a falsehood in a release doc. The
   doc only states what the gate enforces, so nothing false shipped.
   (b) My first TODO_LIST edit was rejected because I hadn't `view`ed the
   file that session (grep is not view). One wasted round trip — and with
   concurrent agents committing constantly, view-immediately-before-edit is
   the rule that prevents clobbering a stranger's forward progress, not a
   formality.

## e) WHAT WE SHOULD IMPROVE

1. **Docs that mirror code need drift guards, like code does.** The repo
   already learned "regex gates without fixtures rot"; RELEASE.md is a
   prose mirror of `release.sh` with no equivalent pin. A small smoke (grep
   the doc's cited modes/flags/gate names against the script) would fail
   the suite when they diverge — same spirit as
   `TestAdoptionTableCoversTemplates`.
2. **`release.sh` ergonomics noticed in passing:** the release-notes
   tempfile is a fixed `/tmp/tq-release-notes.md` (collision-prone for
   concurrent sessions; `mktemp` + trap would fix — the `--push` verify dir
   already does this); no `--help` (usage only on error); a fixed
   `sleep 10` before the proxy poll loop. All minor; touch at the next real
   release, not before.
3. **Owner-decision provenance is now duplicated** (archived v0.1.0
   checklist + RELEASE.md). RELEASE.md declares the checklist historical,
   which is the right direction — future owner decisions should land in
   RELEASE.md only, so the archived file stays frozen.
4. **Self-review checklist:** Did I lie? No — every claim was executed or
   read this session, and the two near-miss reasoning errors above are
   disclosed. Ghost systems? None introduced; b1's unlinked doc is the
   half-case, flagged with a fix. Split brains? None — the doc explicitly
   defers to the script as the code owner. Removed something useful? No.
   Scope creep? Held the line twice (no cross-links, no version-surface
   doc) and recorded both as follow-ups instead of doing them.

## f) NEXT (prioritized; 1–7 are this task's direct follow-ons, 8+ read from

TODO_LIST.md / the round-2 report in passing today 06:55 — not re-verified
per item, [BLOCKED] = owner-gated)

1. One-line cross-link: FEATURES.md release-runner row →
   `docs/release/RELEASE.md` (closes b1).
2. TODO f27 version-surface inventory doc — natural companion; RELEASE.md
   already names every surface it must cover.
3. Doc↔script agreement smoke (see e1): pin RELEASE.md's cited surface
   (modes, gate order, sub-tag derivation, allowlist rules) to
   `release.sh`/`release-gates.sh`.
4. Round-2 item 50: v0.2.0 multi-module release retrospective (now due —
   v0.2.0 is shipped).
5. `mktemp` the release-notes tempfile in `release.sh` (e2).
6. Next real release: execute the flow BY following RELEASE.md, then fix
   the doc where reality disagrees — the execution test b2 lacks.
7. `release.sh --help` + `sleep 10` → backoff (e2), same touch.
8. f28/f29 lint-baseline slice-triage (wrapcheck, then varnamelen).
9. f30 `ExitCause` → `ExitError` rename (needs the v0.3 window).
10. f32 per-module golangci runs in ci.yml (ci-local already loops).
11. f35 multi-repo smoke against the nix-built binary.
12. Master-CI red state (runner-only nix FOD mismatch, 06-25 report d1):
    differential-dump the runner's module fetch [BLOCKED — needs runner
    access].
13. TODO 98 test-windows red (bare-repo-name expansion separators).
14. TODO 99 release-gates runner git identity (`-c user.email/name`).
15. TODO 100 x/text ≥v0.39.0 bump sweep (GO-2026-5970).
16. TODO 101 gosec exclusion config encoding the FP triage.
17. TODO 102 govulncheck continue-on-error → hard gate (after 16).
18. TODO 103 ci-local: sanitized GIT_CONFIG_GLOBAL for release-gates.
19. TODO 104 master-CI state gate in ci-local.sh (`gh run list`).
20. TODO 105 changed-lines line-length gate (lll/golines, 120 cols).
21. TODO 125 backend-tag proxy check + clean-room `go install` (overlaps
    what `release.sh --push` already does — reconcile the two).
22. Postgres store CLI wiring (ROADMAP) — resolves the zero-consumer
    honesty the sub-tag policy carries.
23. Round-2 leftover: regenerate/annotate the round-1 D2 diagrams for the
    split queue modules.
24. Round-1 leftover: verify `nix run .#test` per-module coverage claim.
25. docs-health HARVEST over this report's section f into TODO_LIST/
    ROADMAP with routing rigor (skill contract: f is input, not commitment).

## g) QUESTIONS (cannot figure out myself)

1. **Discoverability policy for living reference docs:** should a new
   reference doc like RELEASE.md be REQUIRED to be cross-linked from its
   FEATURES.md/README row at creation time, or is TODO_LIST + the status
   index the only mandatory discovery surface? This decides whether b1 was
   a scope violation or correct discipline, and applies to every future
   doc-task.
2. **Release authority:** is `scripts/release.sh --tag/--push` strictly
   owner-run forever, or may a trusted agent run the read-only gates-only
   mode as a pre-release rehearsal (it is safe but runs the full CI
   replicant — minutes of runner time)? Policy + cost call, not mine.
3. **Sub-module versioning at the next release:** release.sh cuts ALL
   internal sub-tags at the release version (shared versioning per
   ADR-0011). Confirm this stays the policy even when only one sub-module
   changed semantically — the root `require` lines and the require-tag gate
   both assume it, and a per-module bump scheme would need different tooling.

TQ_RESULT: {"files_changed": ["docs/release/RELEASE.md", "TODO_LIST.md"], "commit_sha": "41b817b"}
