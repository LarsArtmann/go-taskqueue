# Task 000001a0c0d6f2f74e46e4d524207638cccd — Full close-out: third (verify-only) window on the budget token/cost projection

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-21_00-27_task-000001a0c0d6f2f74e46e4d524207638cccd.md`, which owns any open residue.

Window: 2026-09-21 ~00:55 – 01:15 CEST. THIRD dispatch on this ID; owner
requested this full a)-g) close-out after the verify-only pass that produced
`docs/status/archived/2026-09-21_01-05_task-000001a0c0d6f2f74e46e4d524207638cccd.md`
(its §d/e3 corrections ride `232bf0b`). Prior windows: the 00-27 work window
(full close-out `docs/status/2026-09-21_00-27_task-000001a0c0d6f2f74e46e4d524207638cccd.md`)
shipped the item; the 00-39 window
(`docs/status/archived/2026-09-21_00-39_task-000001a0c0d6f2f74e46e4d524207638cccd.md`)
was the first verify-only re-dispatch. This window's SOURCE delta: zero.

Work item (TODO row 160, `[x]` since the 00-27 close-out): "Budget
projections: sum derived session tokens/cost from completion facts
(AgentResult + PrioritizeResult usage) into the daily-cap projection so spend
is measurable in tokens, not only task count (09-52 report f1;
internal/budget)".

## a) FULLY DONE

- **Turn-1 ritual with the task ID**: `scripts/session-start.sh
  000001a0c0d6f2f74e46e4d524207638cccd` FIRST — the prior-report grep
  surfaced BOTH sibling windows before any edit (repeat dispatch identified
  pre-edit; zero wasted re-implementation). `git log`/`git status`/`git
  stash list` over the WHOLE repo: clean tree at 59d525f, no stashes.
  CONTRIBUTING.md head read via the script output (CI gate list; local gate
  is ci-local.sh). No CLAUDE.md (ls'd — only CONTRIBUTING.md exists).
- **Every implementation claim re-verified against code at HEAD, file:line**
  (HEAD 59d525f at verification time; the only commits since are docs-only —
  see §b):
  - `SessionUsage` internal/budget/budget.go:99; `Guard.UsageToday`
    internal/budget/budget.go:129 (VALUE receiver — my first grep patterned
    a pointer receiver and missed it; a symbol-check miss, not a code gap).
  - `FactsSince` contract: internal/queue/queue.go:196 (Store interface
    method); sqlite internal/queue/sqlite/sqlite.go:1645; postgres
    internal/queue/postgres/postgres.go:1553.
  - Drift pins: `TestFactsSinceByType`
    internal/queue/sqlite/store_test.go:1745;
    `TestCheckExhaustedReasonCarriesUsage` internal/budget/budget_test.go:193;
    `TestStatsJSONSessionUsageContract` cmd/tq/stats_test.go:102.
  - AGENTS.md's budget row is IN SYNC with the shipped code (UsageToday sums
    derived session tokens/cost; surfaced in the Check refusal reason +
    `tq stats` `budget.session_usage`; cap semantics stay task-count pending
    the owner ruling) — no doc drift found on this item.
- **TODO row 160 confirmed `[x]` in place** (TODO_LIST.md:160) with the
  00-39 re-verification annotation. No edit needed; re-annotating per window
  would grow the row without new information. THE CONTRACT'S LOOP IS CLOSED:
  item done, row checked, queue attribution intact.
- **Fresh cheap battery at HEAD, rc's captured to files and read back**
  (never PIPESTATUS — the documented session-shell hazard):
  - root `go build ./...` rc=0; `go vet ./...` rc=0 (GOEXPERIMENT=jsonv2)
  - `scripts/check-doc-refs.sh` rc=0; `scripts/check-todo-list.sh` rc=0
  - internal/budget in-module `GOWORK=off`: build rc=0, full test suite
    rc=0 (ok 0.009s — includes TestCheckExhaustedReasonCarriesUsage)
  - internal/queue/sqlite in-module `GOWORK=off`, targeted
    `-run TestFactsSinceByType -v`: `--- PASS` rc=0 (ok 0.005s)
  - `./scripts/test-cmd-tq.sh` rc=0 (ok 17.259s — full cmd/tq suite incl.
    TestStatsJSONSessionUsageContract)
- **Queue attribution re-verified**: all SEVEN prior-window commits carry
  exactly one well-formed `Task-Queue-ID:` footer for this ID (87fc781,
  328732d, a5bc36a, 8d12eb0, 2d5c01e, 6d1f4ef, 3ecccdd — counted via grep
  over `git log --format=%B`, one each). This dispatch is a third
  HARVEST-RACED-THE-CHECKOFF delivery: the row was `[x]` at 6d1f4ef
  (00-27 close-out), which PRECEDES the mint. Fresh evidence for the BLOCKED
  duplicate-claim ruling row (§g1).
- **Reports shipped and indexed**: the 01-05 verify-only report + this
  close-out, both with README.md index rows. Closing gates after the final
  commit: `check-status-index.sh` rc=0, `check-doc-refs.sh` rc=0,
  `check-todo-list.sh` rc=0, footer count on 232bf0b = 1, working tree
  clean.
- **Daemon folds handled per the sanctioned remedy, no history rewrite**:
  the sweep folded the 01-05 report footer-less into `50ff934` (126 lines)
  while only the index row rode `1fbd68d`; corrections + fold record rode
  `232bf0b` (both files, footer verified). The §g2 heal ruling is pending;
  the filter-branch precedent stays owner-gated. AND IT HAPPENED AGAIN to
  THIS close-out: the index row for 01-11 was swept footer-less into
  `087e353` while the report itself was still being generated (~4 min), so
  the report rode `628060c` alone. SIXTH fold; correction rides this
  commit.

## b) PARTIALLY DONE

- **Verification tier: CHEAP battery only** — the documented verify-window
  minimum is met (build+vet+doc-refs+todo fresh at HEAD plus several fresh
  deltas: budget suite, sqlite FactsSince pin, full cmd/tq gate), but the
  expensive tier was NOT re-run: no race suite, no ci-local, no nix, no
  GOOS=windows, no smokes, no advisory lint, no local postgres twin.
  Justification: zero source edits; every commit since the 00-27
  fully-batteried tree (6d1f4ef) is docs-only (5b329a9 report+TODO+AGENTS,
  59d525f fold record, 50ff934 report fold, 1fbd68d index row, 232bf0b
  corrections) — the convention permits inheriting expensive-tier evidence
  when HEAD's code is unchanged. Disclosed, not implied.
- **One narrowing vs the 00-39 battery**: the full sqlite suite was not
  re-run this window — only the targeted TestFactsSinceByType. Same
  justification (sqlite code untouched since the 00-39 full-suite green at
  3.302s), but it IS a smaller fresh delta than the sibling window's.
- **CONTRIBUTING.md coverage**: read via the session-start script's excerpt
  (the CI/local-gate section), not the full file. The task contract asks
  for it at turn 1; the excerpt covers the operative gates. Marginal, but
  the honest ledger records the depth.
- **No live-queue interaction**: deliberately none — every bare `tq`
  command touches the production journal via inherited `TQ_DB`, and a
  verify window needs no production reads. Not a gap; a scope decision
  recorded so its absence is not misread later.

## c) NOT STARTED

- **No code work, BY DESIGN** (DONE-row re-dispatch; three windows in ~65
  minutes, one shipping, two verifying). Everything under the 00-27 §c
  ledger stays open and unchanged:
  - ReviewResult / StatusResult session-usage derivation (TODO rows
    161/162) — paid turns the projection cannot see yet.
  - Executor-side json-key drift pin (TODO row 163; the consumer-side pin
    exists in budget_test).
  - Webui budget card token/cost line; `tq show` prioritize usage;
    completion-cost sparkline (09-52 §f5/§f6).
  - Calibration/G1 sizing revisit (09-52 §f7/§f8).
  - Local postgres twin execution for FactsSince (env-gated; CI owns the
    proof so far).
  - Full ci-local run on this lineage.
  - Cap-unit ADR (measurement shipped; gate semantics owner-only).
  - Duplicate-claim mint fix (BLOCKED ruling row).
- Nothing was silently dropped from THIS window's scope (verification +
  report); nothing from the item's original scope was discovered undone.

## d) TOTALLY FUCKED UP

1. **The window exists at all — the systemic lead.** Two prior windows
   closed the item (one shipping with a full battery, one verifying); the
   queue minted a third inside the hour. Every re-dispatch costs a full
   ritual + battery + report (~30 tool calls). The fix candidates are
   written (00-39 §e2: row-state re-check at mint/claim time, or
   prior-report consult at mint); they sit in a BLOCKED row awaiting an
   owner ruling while the treadmill spins. The process owns this one, not
   the window.
2. **FIFTH daemon-race loss in this report family — and the first-pass
   report then contained a FALSE claim about it.** Timeline: I wrote the
   01-05 report, then edited the README index row (two tool calls,
   ~2 minutes), then ran add+commit — by then the daemon had already swept
   the report footer-less into `50ff934` (01:05:53), so my commit `1fbd68d`
   (01:07:10) carried only the index row. Worse: §a/§e3 of that first-pass
   report declared "the daemon race was finally WON (no fold this window)"
   — written BEFORE the outcome was knowable, while the sweep could still
   land (and did). That is the premature-citation class the
   claims-carry-citations rule exists for: a race outcome is only claimable
   AFTER `git log` verifies it, never from the intent of the chain. I
   corrected the report and index same-session (`232bf0b`) before any
   external reader could be misled, and the correction itself is cited
   evidence — but the claim should never have been written.
3. **The critical section was structured to lose — TWICE, and the first
   fix was falsified the same window.** Fold one (01-05): the write→commit
   gap held two avoidable calls (README view + edit). Fold two (this
   report): I applied the obvious ordering fix — index edit BEFORE the
   primary write — and lost anyway, because the real variable is WALL-CLOCK
   DIRTY TIME, not tool-call count: generating a 243-line report takes
   ~4 minutes, and the daemon's ~1-2 min sweep eats ANY file left dirty
   that long, regardless of ordering. No write-then-commit ordering
   survives a multi-minute generation inside the critical section. The
   honest conclusions: (i) either the content is composed BEFORE the tree
   is touched (not practical with the write tool) or the single-invocation
   heredoc+commit pattern (§f3) is the only local defense; (ii) until the
   daemon lockfile/delay lands (00-27 §f3), each window should BUDGET for
   one fold-record correction commit and stop there — the remedy loop must
   converge, not recurse. Fold mapping this window: `50ff934` (01-05
   report) + `1fbd68d` (01-05 index row) + `232bf0b` (corrections) +
   `087e353` (01-11 index row) + `628060c` (01-11 report) + this
   correction commit.
4. **A pointer-receiver grep burned one round trip** (first symbol check
   for `UsageToday` matched nothing because it is a value receiver).
   Trivial, but the fix is free: grep the symbol NAME, not a guessed
   signature, when the goal is existence verification.

## e) WHAT WE SHOULD IMPROVE

1. **Critical-section doctrine (rewritten by fold six, same window)**:
   the loss variable is WALL-CLOCK DIRTY TIME (~daemon sweep 1-2 min), not
   call adjacency — fold one lost on a 2-min multi-call gap, fold two lost
   on a 4-min report GENERATION with perfect call ordering. Doctrine for
   AGENTS.md: budget one fold-record correction per window; prefer the
   single-invocation write+commit pattern for daemon-critical markdown;
   verify `git log` after every commit BEFORE writing commit-outcome
   prose. This belongs in the session-start bullet — the current
   "add+commit must be ONE chain" line has failed six times because it
   names the wrong invariant.
2. **Post-commit verification precedes claims**: the "race won" prose was
   written from intent. Rule: no commit-outcome claim in any report until
   a post-commit `git log` (or the commit's own echo) is in hand. Same
   class as citing a gate that is still running (2026-09-12 02-38).
3. **Collapse the write→commit gap mechanically**: for markdown reports,
   a single bash invocation of `cat > file <<'EOF' … EOF && git add … &&
   git commit …` reduces the window to seconds (the heredoc ban is for Go
   source; a quoted heredoc is expansion-safe even with backticks).
   Tradeoff: less tooling support than the write tool; adopt as default
   only for daemon-race-critical writes.
4. **Measure the daemon before engineering around it**: five folds now
   carry timestamps; one paragraph of analysis (sweep interval vs
   tool-call latency) would turn the lockfile/delay proposal (00-27 §f3)
   from a hunch into a sized fix.
5. **The duplicate-claim ruling is the highest-leverage open item in this
   family** — three windows in ~65 minutes on one ID. Until ruled, every
   verify-only window is planned waste.
6. **Verify-only playbook**: the shape is now stable across two windows
   (prior-report grep → claim re-citation at file:line → cheap battery →
   footer check → report → index → one-chain commit). A script or skill
   would remove the per-window re-derivation (carried 00-39 §e4/§e5).
7. **What worked (keep)**: ritual-first with the task-ID grep (repeat
   dispatch caught pre-edit); rc-to-file capture with explicit reads;
   in-module GOWORK=off gates; todo-gate in the FIRST battery (00-39 §d4
   not repeated); no history rewrite while the heal ruling is pending;
   same-session correction of the false claim; honest §f counts; refusing
   duplicate backlog rows.

## f) Up to 50 things we should get done next

Honest count: **4 new**; padding to 50 refused — everything else this
window touched already has a row, and the carried ledgers ride by pointer
(00-27 §f: 32 items; 00-39 §f: 8 items). New, ranked:

1. Codify the critical-section doctrine (§e1) + post-commit-verification
   rule (§e2) into the AGENTS.md session-start ritual — one edit, stops
   the sixth fold before it happens. Impact: Critical. Effort: S.
   Category: Quality.
2. Empirical daemon measurement: sweep interval + trigger from the five
   fold timestamps, sizing the lockfile/short-delay proposal (00-27 §f3).
   Impact: High. Effort: S. Category: Quality.
3. Decide the heredoc+commit single-invocation pattern for daemon-critical
   markdown writes (§e3): adopt, reject, or scope it to reports only —
   write the decision wherever the doctrine lands. Impact: Medium.
   Effort: S. Category: Quality.
4. Duplicate-claim mint fix implementation (the ruling from §g1 unblocks
   it; candidates already specced in 00-39 §e2). Impact: High. Effort: M.
   Category: Feature.

Carried (not restated): ReviewResult/StatusResult usage (rows 161/162),
executor-side json-key pin (row 163), webui budget card, `tq show`
prioritize usage, sparkline, calibration/G1, postgres twin, full ci-local,
cap-unit ADR, verify-cheap script, verify-only-report-by-default doctrine.

## g) Questions (up to 3)

1. **Duplicate-claim re-delivery ruling (BLOCKED on owner; now THREE
   dispatches in ~65 minutes on this one ID, attribution intact each
   time)**: harvest keeps minting for rows already `[x]`. Pick the fix:
   (a) queue-side row-state re-check at mint/claim time, (b) harvest-side
   prior-report consult (skip mint when the item's ID has a docs/status
   report), or (c) ratify verify-only second windows as the standing
   shape. The cost of not deciding is now the dominant term in this
   family.
2. **Daemon-fold remedy ruling (carried 00-27 §g2, sharpened by the fifth
   fold)**: the msg-filter heal worked once (tree byte-equal, verified)
   but ran without explicit owner approval; the follow-up-commit remedy
   is safe but leaves footer-less chore commits in the lineage that the
   queue's git attribution cannot see. Should scripted msg-filter of
   footer-less DAEMON commits on the UNPUSHED range become the sanctioned
   standard remedy (with the five-step verification baked in), or is the
   follow-up commit the permanent answer?
3. **Cap-unit ruling (carried 09-52 §g1, owner-only)**: the projection
   now MEASURES tokens/cost (`budget.session_usage`); should the daily
   CAP gate on them (cost USD? tokens? which fact types once
   ReviewResult/StatusResult land?) or stay task-count? Blocks the Check
   semantics change and the webui budget card.
