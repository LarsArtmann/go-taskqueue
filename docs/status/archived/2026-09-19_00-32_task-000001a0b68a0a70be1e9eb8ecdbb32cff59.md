# Third window on the smoke re-cite ID — execution-based re-verification (task 000001a0b68a0a70be1e9eb8ecdbb32cff59)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-19_00-10_task-000001a0b68a0a70be1e9eb8ecdbb32cff59.md`, which owns any open residue.

- **Window**: 2026-09-19 00:26–00:35 CEST (third window on this ID; 00-10 did the work, 00-22 verified by citation + path drift)
- **Work item**: "Re-cite `scripts/smoke/session-close.sh` green over the `runSessionClose` extraction AS USED BY the `tq crush` wrapper (one smoke run + citation; unit tests cover the wrapper path, the smoke hasn't been re-cited since the refactor) (02-51/02-54 §f)"
- **Verdict**: DONE row re-verified — this time BY EXECUTION, not drift inference; TODO row 121 untouched; zero code edits

## Verification legs

1. Turn-1 ritual held in batch 1: whole-repo `git log`/`git status`/`git stash list` (clean/empty) + task-ID grep over `docs/status/` — both prior reports found BEFORE any action.
2. TODO row 121 `[x]` verbatim with the full citation (smoke green + LIVE wrapper E2E + cmd/tq gate).
3. Work commit 0aee604 body-verified: exactly one well-formed `Task-Queue-ID: 000001a0b68a0a70be1e9eb8ecdbb32cff59` footer; single-file diff (TODO_LIST.md).
4. Path drift `git log 0aee604..HEAD -- cmd/tq internal/executor internal/worker internal/session scripts/smoke/session-close.sh scripts/test-cmd-tq.sh` → EMPTY.
5. **NEW this window — both cited gates re-RUN at HEAD** (00-22 deliberately skipped execution, its §b3):
   - `./scripts/smoke/session-close.sh` → exit 0: 1 footer commit attributed → exactly 1 review + 1 status minted → replay close `known (dedup)` on both → journal facts asserted.
   - `./scripts/test-cmd-tq.sh` → `ok github.com/larsartmann/go-taskqueue/cmd/tq 9.897s` (includes the crush_test.go wrapper pins).
6. Carried asks checked against existing rows before minting anything: done-row dedup already TODO 165, wrapper smoke leg already TODO 126 — no duplicates. One NEW row minted (session-ritual script), recurs across ≥7 reports with no row.

## d) This window's own misses

1. **qmd-cv dead-end, again (N+2 on this exact miss)**: called `mcp_qmd_get` on the repo report although my own context states qmd indexes only the `cv` collection; one wasted call (00-22's d3 wasted two). The constraint is in-context every session — repo files go through `view`, full stop.
2. **CONTRIBUTING.md read in batch 2, not batch 1**: it rode the drift-check batch instead of sitting beside the git ritual. Read late is not skipped (00-22's d1), and its contents changed nothing (verify window, no push → no ci-local), but the ritual's ORDER exists precisely so a gate-relevant CONTRIBUTING rule can never arrive after action has started.

## e/f/g

- **e**: the ritual-script row below is this window's only mint; done-row dedup (TODO 165) remains the systemic fix that would prevent this window class from existing at all (three paid windows on one DONE row).
- **f**: carried by reference — 00-22 §f1–§f20 all open, none advanced here.
- **g**: carried — 00-22 §g1–§g3 (wrapper-leg gate scope, live-vs-unit citation bar, close-without-begin policy) unchanged.

## Gate evidence index

| Check | Result |
| --- | --- |
| Turn-1 ritual batch 1 | whole-repo log/status/stash + task-ID grep (2 prior reports under this ID) |
| TODO row 121 | `[x]` verbatim, citation intact |
| Work commit 0aee604 | exact footer, single-file diff |
| Path drift over cited paths | EMPTY |
| `scripts/smoke/session-close.sh` at HEAD | PASS, exit 0, full assertion chain |
| `scripts/test-cmd-tq.sh` at HEAD | ok 9.897s |
| `git status` / `git stash list` | clean / empty before report |
| Edits this window | none on code; report + index row + one new backlog row |
