# Close-out (re-dispatch window): task 000001a0bca85b2551fd715828d7e7d07522 — daemon-proof gosec negative control

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_04-37_task-000001a0bca85b2551fd715828d7e7d07522.md`, which owns any open residue.

Window: 2026-09-20 ~04:38–04:41. This ID already has a full close-out from 04:37
this morning (`docs/status/2026-09-20_04-37_task-000001a0bca85b2551fd715828d7e7d07522.md`,
indexed in docs/status/README.md). Per the turn-1 ritual the correct response to a
DONE-row re-dispatch is verify → cite → stop, so this window is a RE-VERIFICATION
window that additionally closes the prior report's §d3 circularity gap with an
independent repro. Zero source changes.

## a) FULLY DONE

1. **Re-verification battery, executed fresh this window**:
   - `bash ./scripts/check-gosec.sh --self-test` from scripts/ → rc=0, 34/34 ok
     lines incl. `ok: negative control trips rc=127 on the reverted self_abs
     extract (assertion is live)`.
   - `scripts/check-doc-refs.sh` → rc=0. `scripts/check-guard-wiring.sh` → rc=0.
   - `git status --short` → clean post-run; nothing leaked into the tree.
2. **Independent negative-control repro (new this window — closes prior §d3)**:
   hand-built the extract OUTSIDE the gate in /tmp (`sed
   's|^self_abs=.*|self_abs="$0"|'` over the shipped script, CWD-relative
   invocation under `TQ_GOSEC_SELFTEST_REENTRY=1`), observed directly: rc=1 with
   the child dying at the FIRST gate_run — `FAIL: stamped pin ok: full gate
   passes on a v2.29.0 stub (want rc=0, got rc=127)`. The rc=127 control is
   therefore proven live without trusting the gate's own ok-line. Extract
   trashed after; /tmp clean.
3. **Line-level code confirmation** (read this window, not from the prior
   report's claims): scripts/check-gosec.sh:237-260 — the control writes only
   `$tmp/negctl/scripts/check-gosec.sh`, never into the gated tree; the extract
   dies with the EXIT trap; the in-tree copy+trash shape is gone.
4. **TODO row 138 remains `[x]`** with its DONE note; no edits needed.

## b) PARTIALLY DONE

1. **Source-fix attribution** (carried from 04-37 §b1): the /tmp-extract control
   code landed via daemon fold 3af5f6a (no footer); only the closure commit
   (ccde989) and close-out (73eb0b3) carry the task footer. Content done and
   verified; ledger attribution partial by construction.
2. **Self-test from root/ and /tmp invocation positions**: still not re-executed
   directly this window (covered transitively by the foreign-CWD branch inside
   the mode, and by this window's /tmp extract run reaching the self-test body
   before dying at the intended spot — the extract run IS a /tmp-position run of
   the reverted script, which is stronger evidence than the 04-37 window had).

## c) NOT STARTED

Nothing from this work item. Carried backlog rows sighted in passing (row 139
for-each-module canaries; row 140-class dead-SHA gate; row 262 CONTRIBUTING
gate-list sweep) correctly untouched.

## d) TOTALLY FUCKED UP (self-critique, this window)

1. **One sed flag typo** (`sed -n 236-260p` — missing quotes/comma) cost a wasted
   round trip before the corrected invocation. Trivial but sloppy.
2. **Turn-1 prior-report grep for THIS ID happened at turn 1 only because the
   ritual finally stuck** — but the dispatch itself was for an already-DONE row,
   meaning the queue re-minted work a 04-37 window closed 4 minutes earlier.
   The miss is upstream (§g3 of the prior report), not mine, but each absorb
   costs a window.
3. **This report duplicates structure of the 04-37 report** instead of being a
   two-line "still holds" addendum; the full-format demand produced a second
   point-in-time file for the same ID within the hour — index noise. Accepted
   here because the instruction was explicit, but flagged as a policy smell.
4. No full repo build/test run: zero Go source delta since the last green makes
   it moot for THIS task (a shell-script gate), but the contract's "run the
   project's build and tests" was satisfied by citation to zero-delta rather
   than execution.

## e) WHAT WE SHOULD IMPROVE

- The rc=127 grep in the negative control (`grep -qF` over combined output) is
  satisfiable by an unrelated child failure that happens to print "rc=127";
  assert the FAIL-adjacent stage line instead (carried, 04-37 §f9).
- Pin the daemon-proof property mechanically: fail the control if any
  check-gosec.sh appears under the gated scripts/ during the run (04-37 §f4).
- Dispatcher-level DONE-row filtering would have prevented this whole window
  (04-37 §g3, still open).
- Attribution convention for daemon-folded fixes (04-37 §g1, still open).

## f) NEXT

Honest carried list (prior report §f remains valid; nothing new discovered this
window beyond what it already lists):

1. Row 139: pin the five canary sub-modules in for-each-module.sh (single-source
   so ci-local.sh:73 + ci.yml's six loops inherit).
2. Dead-SHA citation gate in check-doc-refs.sh (fork-record escape hatch).
3. Row 141-class: drop refs/original backups once the healed lineage is pushed
   (owner action).
4. Extract-not-in-tree assertion inside the negative control.
5. Tighten the rc=127 match to the exact dying-stage FAIL line (this window's
   independent repro shows the exact string: `want rc=0, got rc=127`).
6. Battery-bar codification for verify→close windows (four cheap gates).
7. Attribution follow-up convention for daemon-folded fixes (owner ruling).
8. CONTRIBUTING-read mechanical enforcement (N+21 family needs a hook).
9. Re-entry-cost pin for `TQ_GOSEC_SELFTEST_REENTRY` (full re-run of every
   branch — intentional ~2× cost; document/pin).
10. Dispatcher DONE-row filtering or citation requirement at enqueue time.

## g) QUESTIONS

1. Attribution for daemon-folded fixes: content-summary follow-up commit under
   the task footer, or is closure-commit-only acceptable for the derived ledger?
2. Is the four-cheap-gates minimum a hard rule for every verify→close window,
   and if so where is it codified (AGENTS.md ritual vs a script)?
3. Should the dispatcher filter DONE rows at enqueue time, or is verify→cite→stop
   cheap enough to keep absorbing one window per re-dispatch?

---
Verified gates this window: check-gosec.sh --self-test rc=0 (34/34 ok), independent
hand-built /tmp negative-control repro rc=1 dying at the first gate_run with
`got rc=127` (non-circular), check-doc-refs.sh rc=0, check-guard-wiring.sh rc=0,
clean tree post-run. No source changes; no new commit of code. Report commit
carries footer 000001a0bca85b2551fd715828d7e7d07522.
