# Close-out self-review — for-each-module consumers, fourth window (turn 4)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/archived/2026-09-19_07-00_task-000001a0b7ef16d70d5c3039ce2dec113cd1.md`, which owns any open residue.

Task `000001a0b7ef16d70d5c3039ce2dec113cd1` · 2026-09-19 07:28 CEST · verify-only re-dispatch over an already-shipped row; work commit for this window = 1e4b7e7 (TODO_LIST third re-verify citation). HEAD at writing: 1e4b7e7, tree clean. Fourth dispatch on this ID (prior: 07:00, 07:09, 07:16).

**One-line verdict:** row 133's fix (cc68b25) re-verified fresh at HEAD for the third time — both enumeration-failure branches re-executed WITH branch attribution, all consumers re-read, gate battery green, zero source delta — and the window's headline is that I repeated the 07:16 §d2 mis-built-fixture mistake VERBATIM one read after its documentation, caught only by refusing to trust rc=1 alone.

---

## a) FULLY DONE

1. **Turn-1 protocol, batched, and the CONTRIBUTING read was FULL (75/75 lines)** — first complete read in this ID's lineage after four consecutive misses (60/74 → 0/74 documented across the prior windows). Whole-repo `git log`/`git status`/`git stash list` + task-ID grep (three prior reports found) + CONTRIBUTING/CLAUDE check, all in the first batch, before any state judgement. The grep routed the window onto verify→cite→stop immediately.
2. **Fix verified ON MASTER, not from the DONE note.** `scripts/for-each-module.sh` re-read fresh: `set -euo pipefail`, the `-z` zero-target guard, the exact FAIL-to-stderr line, exit 1. All six ci.yml loops re-confirmed in capture form (`mods="$(./scripts/for-each-module.sh)"` + `for m in $mods`) at :45/:59/:98/:161/:255/:294; ci-local.sh capture re-read at :73 with its loops fed from it.
3. **Both enumeration-failure branches RE-EXECUTED with committed script bytes and BRANCH ATTRIBUTION** (fail-line hits, find-error hits, stdout bytes checked separately, not rc alone): clean-empty (all six find roots present, zero go.mods) → rc=1, FAIL line ×1, stdout 0 bytes (the `-z` guard); missing-roots → rc=1 via pipefail, find errors present, FAIL line absent. Happy path in-repo: rc=0, 15 targets. Fixtures under /tmp, trashed in the same shell chain.
4. **Proportionate fresh battery at HEAD:** check-script-syntax.sh rc=0, ci.yml YAML-parses, `go build ./...` rc=0, `go vet ./...` rc=0, `go test ./... -count=1 -race -timeout 180s` rc=0 (15 ok / 0 FAIL) under `GOEXPERIMENT=jsonv2`. Gate rc's captured via redirect-to-file + `$?` directly (the codified PIPESTATUS-hazard shape — no pipelines).
5. **Zero source delta since the fix commit** confirmed by path-scoped diff (`*.go *.sh .github/ flake.nix` — empty), cc68b25 existence confirmed.
6. **Row citation appended with the honesty boundary:** the third re-verify text on row 133 claims ONLY this window's executions, and records its own §d2-repeat inside the citation rather than hiding it.
7. **Fold-assume commit path worked as designed (first clean run in the lineage).** Status checked immediately after the edit-tool write, add+commit in ONE chain, no daemon fold occurred, footer verified via the grep form (`grep -c '^Task-Queue-ID: …$'` = 1), tree clean after. The 07:16 e1 proposal is validated — keep as default.
8. **This close-out written, indexed, gate-checked, and committed** with the exact task footer.

## b) PARTIALLY DONE

1. **Battery scope is root-module + scripts + named gates only.** Per-module suites, ci-local's full gate, and `nix build` were not run — defensible for a zero-source-delta verify window, but "green" here means exactly a4's list, nothing wider.
2. **check-gosec.sh --self-test was NOT re-run this window.** Its green is inherited from the 07:00 window's evidence; the row citation does not claim it, but a full-certificate reader should know which greens are fresh (a4) vs inherited (this bullet).
3. **The rewired ci.yml loops have STILL never executed on a runner.** The entire fix lineage (cc68b25 and now 60+ commits) is unpushed; remote CI cannot have run any of it. Every capture-form claim in this report is local-green, runner-unverified.
4. **Happy-path verification checked count (15) and rc, not content equality** against an expected module list — a corrupted-but-nonempty enumeration of the right size would pass this window's check (row 134's short-list guard territory, unexercised here).
5. **`date` and the close-out happened only on dispatch order** — the 07:16 d5 miss repeated; the report was not written unprompted at the end of the verify pass.
6. **Carried environmental partials:** unpushed lineage (owner-side); live status-index rows now ~256 (threshold 100; this report adds one); the ANNOTATE sweep remains owed.

## c) NOT STARTED

None of this window's scope. Observed in passing:

- **Minted in prior windows, untouched (correctly):** rows 146 (residual inline-find consumers) and 147 (capture-shape regression pin).
- **Standing family:** row 134 (short-list guard); rows 124 + 127 (BLOCKED on owner rulings — CLI gosec coverage, ci.yml gosec-loop convergence); row 142 (wrapper-path smoke leg); rows 144/145 (session-start ritual script; report-format ratification); rows 253/258 (CONTRIBUTING gate-list sweep; finding-count drift); row 140 (dead-SHA citation gate); row 141 (BLOCKED on the owner push); ANNOTATE/digest sweep.
- **Split-brain scan (unchanged):** the 0-target/failing-enum guard exists as TWO implementations (shared `for-each-module.sh` and `check-gosec.sh`'s own enum+guards, per row 129) — convergence is row 127's territory. Row 133's harvest-provenance line numbers stay stale-by-design.
- **Ghost-system scan:** none new. Every consumer (6 ci.yml + 3 ci-local loops) verified consuming the shared enumerator, again.

## d) TOTALLY FUCKED UP

1. **I repeated the 07:16 §d2 mis-built-fixture mistake VERBATIM — one window after reading the playbook that confessed it.** My first "clean-empty" fixture omitted the top-level `executor`/`worker` roots (I created `internal/executor` and `internal/worker` instead), so find errored and the script exited 1 via pipefail — NOT via the `-z` guard — while my command labeled it clean-empty. This is now a TWO-STRIKE documented pattern (07:16 original + this recurrence). The save was real: `fail-line-hits=0` together with rc=1 is impossible for the guard branch, so the wrong-branch attribution was caught before any claim — but the catch was reading output, and the error was repeating a documented disease its documentation had just been read aloud. Strongest evidence yet that the fixture-branch-contract rule (07:16 e2) must become mechanical, not aspirational.
2. **First battery chain wrapped the negative branches in `set -e`** — the script's CORRECT exit 1 killed my own harness before the rc capture, producing a bare `exit status 1` with no data. A negative test whose success path (the failure firing) aborts the test harness is a harness bug: one wasted round trip, and had the echoes printed before the abort, a transcript reader would have seen "clean-empty" claimed with zero evidence behind it. The fix is the already-codified shape: never errexit around branches whose expected outcome is nonzero, capture `$?` immediately, redirect to files.
3. **Edit tool refused the first TODO_LIST write** (grep output ≠ a read for the tool contract): one wasted call on the read-before-edit rule, at the tool level this time.
4. **`date` + close-out only on dispatch order** — d5 of 07:16 recurred exactly; `date` belongs in the turn-1 batch and the close-out should be the default tail of every verify pass, not a follow-up instruction.

## e) WHAT WE SHOULD IMPROVE

1. **Fixture-branch contract goes MECHANICAL (two strikes, same disease).** Before any negative fixture: write one line naming the guard, the branch, and what the fixture must contain for THAT branch to fire (clean-empty = all six roots present + zero go.mods; missing-roots = a root absent); build the fixture FROM that line; label the transcript FROM the same text. Candidate content for row 144's session-start/ritual script scope.
2. **Negative-test harness rule:** `set -e` never wraps branches whose success is a nonzero exit; rc captured immediately; branch attributed by OUTPUT MARKERS (fail-line count, find-error count, stdout bytes), never by rc alone — rc=1 is three different branches in this one script.
3. **Branch-attribution marker triple is the standard claim shape.** Every negative-branch citation should read "rc=N, <guard-marker>=k, <competing-marker>=0" — this is what caught d1 in-flight and what the 07:16 §d2 window lacked.
4. **Keep what finally worked:** full CONTRIBUTING read (wc -l → read-to-end) in the turn-1 batch; fold-assume (status → add → commit in one chain, grep-form footer check); fresh-vs-inherited greens labeled (a4 vs b2); unpushed-lineage marker inline (b3). Add `date` to the batch.
5. **Close-out is the default tail of a verify pass** — write it unprompted, run `date` first (kills d4 permanently if adopted).

## f) WHAT WE SHOULD GET DONE NEXT (16 honest — refuses 50-padding; no new mints: a verify-only fourth window produced nothing generalizable beyond strengthening existing rows)

| #  | Task                                                                                                                                                                               | Impact      | Effort | Source            |
| -- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------ | ----------------- |
| 1  | Owner push: the row-133 fix and 60+ commits are unpushed; remote CI has never run the rewired loops — every CI-surface green claim stays local-only until then                     | High        | S      | owner             |
| 2  | Row 146: convert lint-baseline.sh + lint-annotations.sh + check-go-mods.sh to captured, guarded enumeration preserving each script's exact module set                              | Medium-High | M      | minted 07:00      |
| 3  | Row 147: capture-shape regression pin — every `for m in $mods` in ci.yml + ci-local.sh must be fed by a for-each-module.sh capture                                                 | Medium      | S      | minted 07:00      |
| 4  | Row 134: short-list guard — non-zero-but-short enumeration silently scans survivors (canary-module assertion survives module additions better than a count)                        | High        | M      | standing          |
| 5  | Rows 124 + 127 (BLOCKED): CLI gosec coverage ruling; gosec-loop convergence (unblocks folding check-gosec's duplicate enum guard onto the shared enumerator)                       | Medium      | M      | owner             |
| 6  | Row 144: committed session-start ritual script — scope now ALSO carries the fixture-branch-contract line and the `date` step (d1/d4 of this window are its strongest evidence yet) | Medium      | M      | standing          |
| 7  | Row 145: ratify report-format clauses — the de-facto ruling that verify-only windows write FULL a)-g) is now three windows old                                                     | Medium      | S      | standing          |
| 8  | Amend/fold playbook: codify fold-assume (validated clean this window, a7) + tree-hash capture + grep-form footer verification into the standing playbook doc                       | Medium      | S      | 07:16 f7          |
| 9  | Row 142: committed wrapper-path smoke leg for `tq crush --bin`                                                                                                                     | Medium      | M      | standing          |
| 10 | Status-index ANNOTATE sweep (~256 live rows vs threshold 100; this report adds one)                                                                                                | Low         | M      | standing          |
| 11 | Row 253: CONTRIBUTING gate-list sweep                                                                                                                                              | Low         | S      | standing          |
| 12 | Row 258: CONTRIBUTING.md:41 "~400-finding" vs the 1113-finding seventh-regen truth                                                                                                 | Low         | S      | standing          |
| 13 | Row 140: dead-SHA citation gate (no doc may cite SHAs abandoned by an approved rewrite)                                                                                            | Low-Med     | M      | standing          |
| 14 | Row 141: backup-ref drop after the owner push lands (destructive, BLOCKED)                                                                                                         | Low-Med     | S      | standing          |
| 15 | Verify-external-claims habit for CI-behavior claims in reports: fetch the GHA shell-semantics doc or mark "from memory, unverified"                                                | Low         | S      | carried 07:09 d2  |
| 16 | Row 133 provenance hygiene (optional, low): decide once whether harvest-line numbers ever get an "as-of-harvest" marker                                                            | Low         | S      | carried 07:16 f16 |

## g) Open questions (3)

1. **Push timing (owner-only, carried):** the fix commit and 60+ more are unpushed — remote CI has never executed the capture-form loops. When does the lineage ship, and is local-green acceptable for closing queue rows whose only runtime surface is CI itself?
2. **Fixture-contract codification (new, owner preference):** the mis-built-fixture disease now has two strikes in two windows (07:16 §d2, this §d1). Should it become a committed mechanical artifact (a check or a documented contract inside row 144's ritual script), or stay a process note in close-outs? I cannot decide whether it warrants a new row or folds into 144.
3. **Row-A convergence shape (carried, coupled to rows 124/127):** should lint-baseline/lint-annotations/check-go-mods move onto the shared enumerator — which forces the cmd/tq-coverage ruling — or keep local guarded enumerations with their exact module sets?
