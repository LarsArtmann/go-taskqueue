# Task 000001a092a10a7edf3a3836f61deba98eed — close-out (re-dispatch verification window)

- **When**: 2026-09-12, 00:46–00:58 CEST (work turn 00:48–00:51, this close-out 00:52–00:58)
- **Dispatch**: 2nd window for this ID. First window (00:40–00:46 claim) shipped
  the deliverable completely: work `c5992e0` 00:42:26, close-out `a8c3906`
  00:46:22, report 00-43. The pool then failed the task's minted verify at
  00:46:29 and re-claimed at 00:46:31 — my window. This report is the brutal
  self-review of MY window; the deliverable itself was reviewed in 00-43 and
  independently re-verified in my work turn (report 00-49).

## a) FULLY DONE

1. **Re-dispatch root-caused with primary evidence — and it is NOT what my
   work turn guessed.** `tq show` (read-only, production journal): fact
   timeline 2538 enqueued 00:40:22 → 2539 claimed 00:40:23 → (work + close-out
   commits land 00:42:26 / 00:46:22) → 2540 `task.failed` 00:46:29 → 2541
   claimed 00:46:31 = me. `lastError` is the documented pool-env lie verbatim:
   root-module verify without `GOEXPERIMENT=jsonv2` dies on
   `encoding/json/{v2,jsontext}` build constraints regardless of repo state.
   This is AGENTS.md's known issue (sibling task 000001a08ebf burned 3
   attempts on it; 00-19 predicted the next burn). My 00-49 §d2 guess
   (unresolved 07-03 §g1 terminal-state semantics) is DISPROVEN as the cause
   for this instance — the queue never saw a passing verify.
2. **Reproduced the lie live this turn**: bare `go build ./...` (no export)
   prints the identical build-constraints errors (confirmation only — but see
   §d5 for how badly I instrumented that check). Consequence, stated plainly:
   the pool's verify of THIS close-out will fail the same way and likely
   mint a 3rd window. The failure is environmental, not a regression.
3. **Stale-dispatch verification completed before any write** (00-49 report
   §a, all re-checkable): AGENTS.md bullet complete + accurate vs the 06-41
   primary source (§d self-criticism + §e1-2 procedure lines), TODO item `[x]`
   with DONE note, both prior commits carry exactly one well-formed
   `Task-Queue-ID` footer, gates green fresh (check-todo-list, check-doc-refs,
   check-status-index; root `go build` + `go vet` + `go test ./... -race`
   with manual export).
4. **Hook gap found and closed by hand**: the installed pre-commit hook runs
   ONLY check-status-index.sh + check-todo-list.sh (read
   scripts/install-pre-commit.sh to confirm) — check-doc-refs.sh is NOT in
   it, so I re-ran it manually AFTER creating the 00-49 report and README row:
   green.
5. **Inherited-claim gap closed**: CLAUDE.md existence (claimed absent by the
   00-43 report) verified myself this turn — absent, claim true. It should
   never have been trusted unverified in the work turn (§d3).
6. **Contract compliance**: `.crushrc`/`crush.json`/`.tq-verify` untouched;
   nothing pushed; TODO loop already closed; deliverable untouched — zero
   drive-by changes; daemon sat idle (no heuristic commits during my edits).

## b) PARTIALLY DONE

1. **Content-accuracy verification depth**: my "accurate vs the 06-41 primary
   source" check rests on three grep one-liners (06-41 lines ~98/113/142) plus
   the 00-43 window's before-writing read — not a full-section read of §d1/§e1-2
   by me. Adequate for a no-write window; thinner than the claim sounds.
2. **Gate scope**: root verify + 3 doc gates; full `ci-local.sh` replicant and
   the per-sub-module gates consciously skipped (docs-only window, agents
   never push) — but this declaration belongs in the 00-49 report itself, and
   isn't there (§d4 adjacent).
3. **This close-out's own verification**: gates re-run green this turn
   (doc-refs/status-index/todo-list), but the pool-side verify of the close-out
   commit cannot pass until the env fix lands — the terminal state of this task
   remains "complete but unverifiable in the pool environment".

## c) NOT STARTED (noticed in passing, deliberately untouched)

1. TODO 88: harvest /tmp-hot same-session-priority flag (06-41 §e3).
2. TODO 89: `tq pool-health` per-repo skip-streak summarizer.
3. TODO 90: worktree-per-agent design doc.
4. TODO 91: session-close bridge design + prototype.
5. Unpushed pile grew by 2 this session (aa71951 + this close-out); still
   nothing surfaces the count (00-43 §c5).
6. f26 TRAILER WARNING fires on every check-status-index run (pre-existing,
   owner ruling pending, exit 0).
7. The `attempts` field says 1 while the fact stream shows claim → failed →
   claim for this ID — attempt-lineage opacity (fresh evidence for 00-35's
   `tq show` attempt-lineage item).
8. My re-dispatch prompt carried no hint that a close-out report for this ID
   already existed; the discovery pass cost real minutes and is fully
   mechanical (see §f27).

## d) TOTALLY FUCKED UP

1. **Fabricated a tally into the permanent record.** The 00-49 report and its
   README row say "4th ask of this ID". Reality: 2nd window (first = the
   completing 00:43 window). I conflated the SIBLING task's 3-attempt lineage
   with this ID. Root cause: asserted a count from memory instead of running
   `tq show` — the one command that settles it, which I then ran LAST instead
   of FIRST. Index row corrected in place this turn with a marker; the 00-49
   report body stays untouched per annotate-never-rewrite.
2. **Published a wrong root-cause guess.** 00-49 §d2 attributed the
   re-dispatch to the open terminal-state ruling; `tq show` proves it was the
   env-lie verify failure, case N+1 of a documented known issue. Every prior
   window that hit this class (06-49, 00-19) ran `tq show` first; I wrote
   prose first and evidence later.
3. **Repeated the previous window's confessed mistake.** The contract says
   read AGENTS.md AND CONTRIBUTING.md/CLAUDE.md FIRST; I read CONTRIBUTING.md
   after the discovery and verification passes, hours after reading 00-43 §d1
   confess the identical lapse. No damage (only read-only operations
   preceded), which is luck, not discipline.
4. **Re-offended on invented TQ_RESULT fields** (`work_commit`,
   `outcome: "verified-existing"`) — the exact thing 00-19 §d and 00-35 §f
   flag as needing an owner ruling, flagged in index rows I read this same
   session.
5. **Pipeline-masked exit check in my own diagnostic**: bare-verify probe ran
   `go build ./... | head -5; echo $?` — `$?` reported head's status, not the
   build's. The printed errors made the conclusion sound anyway, but the
   instrument was the 00-35 §d pipeline-masking class, again read and then
   repeated. On the REPORT gate itself I ran unfiltered commands; this was
   confined to the throwaway probe.
6. Minor: the 00-49 report's When window guessed an end-time ("00:55") at
   write time; actual last commit 00:50:43. Timestamps in reports should be
   actuals.

## e) WHAT WE SHOULD IMPROVE

1. **Re-dispatch protocol, step 0**: `tq show <id>` before any verification
   prose — root cause first, narrative second. Would have prevented §d1/§d2
   entirely.
2. **Counts only from the queue record** — never from memory, sibling
   lineages, or report-summary proximity.
3. **Report placement**: three files now exist for one task ID (00-43, 00-49,
   this). Verification-only windows should append to the task's existing
   report until the placement ruling lands (00-35 §b flagged the variant;
   I picked the unruled option again).
4. **Declarations at write time**: gate scope and verification depth belong
   in each report's §a/§b as written, not reconstructed in the next close-out.
5. **Minted verifies must be env-self-contained**: this task's stored verify
   has no `GOEXPERIMENT=jsonv2`, so every root-module agent task from this
   repo re-burns windows until the minting layer (harvest / `.tq-verify`
   template — NOT agent-editable, self-dealing rule) emits the export prefix,
   or the owner env fix lands.
6. **Owner env fix is now 4 windows of evidence old** (000001a08ebf ×3, this
   ID ×1 so far): `Environment=GOEXPERIMENT=jsonv2` on the NixOS
   tq-agent-pool unit.
7. **Report timestamps: actuals only**, captured from `date`/git at write
   and commit time.

## f) NEXT (grounded in this window's observations; stopping at honest items

per the 07-03 precedent — padding to 50 would be noise)

Queue/process:

1. Bake "run `tq show <id>` first on any re-dispatch" into the dispatch
   contract text and AGENTS.md gotchas (§d1/§d2 root cause).
2. TQ_RESULT schema ruling: field allowlist + what the gate does with extras
   (§d4; carried 00-19/00-35).
3. Report-placement ruling: append-per-task vs file-per-window (§e3; carried).
4. `tq show` attempt-lineage: reconcile the `attempts` field with the fact
   stream (§c7; carried 00-35).
5. Env-self-contained minted verify commands at the minting layer (§e5).
6. Owner: `Environment=GOEXPERIMENT=jsonv2` on the tq-agent-pool unit (§e6).
7. Owner: home-manager go-env jsonv2 fix (carried 07-03 §g2).
8. `tq doctor` env check: warn when GOEXPERIMENT/toolchain can't build the
   root module (carried 00-35).
9. Evidence-copy helper script `scripts/archive-evidence.sh` (check-ignore →
   copy → intended-set `--stat` diff) — makes the new AGENTS.md bullet
   mechanical (carried 00-10/00-43 proposals).
10. Mirror the evidence-copy gotcha as a one-liner in docs/status/README.md's
    header (carried 00-43 proposal).
11. Cross-reference the new gotcha from AGENTS.md Known Issues, not only
    Conventions (carried 00-43 proposal).
12. f26 trailer-cluster owner ruling (carried; WARNING fires on every index
    check).
13. Surface the unpushed-pile count (`tq doctor` or stats) (carried 00-43 §c5).
14. Push-cadence policy for master (carried 00-10 §g).
15. CHANGELOG policy for docs-only evidence tasks (carried 00-10 §g).
16. Daemon pause/scoping knob for evidence-copy windows (carried 00-10 §g).
17. Gate-scope declaration convention for docs-only dispatches (§b2).
18. Verification-claim checklist: every §a claim names the command that proved
    it (carried 00-35).
19. Wire check-doc-refs.sh into the pre-commit hook alongside status-index +
    todo-list (§a4 — it is currently absent and manually remembered).
20. Archive-README verify snippet in the ghost-gate docs (carried 00-35).
21. Queue-side: re-dispatch prompts should link the task's existing close-out
    reports (§c8) — the ID is known at enqueue time.
22. Annotate the 00-49 report's §d2 wrong guess with a pointer here (next
    docs-health pass; body stays untouched).

TODO siblings — the remaining items of this task's own section:
23. TODO 88: /tmp-hot harvest same-session-priority flag.
24. TODO 89: `tq pool-health` summarizer.
25. TODO 90: worktree-per-agent design doc.
26. TODO 91: session-close bridge design + prototype.

Report hygiene:
27. §f items harvested from index-row summaries (9-11, 20) should be
re-grounded against their source reports before filing — I saw only the
index prose, and this window is evidence of what summary-distance does
to facts (§d1).
28. Decide whether verification-only windows may cite `work_commit` in
TQ_RESULT after all — the queue↔git cross-reference currently has no
field for "the sha that did the work" vs "the sha of this window's
record" (pairs with item 2).

## g) QUESTIONS FOR THE OWNER (cannot answer myself)

1. **Terminal state under the env lie**: until the unit env fix lands, every
   completed root-module task requeues and burns a verification window. Is
   the env fix the ONLY accepted remedy (my recommendation — prose band-aids
   like "skip if a close-out report exists" invite lying gates), or do you
   want an interim queue-side short-circuit?
2. **TQ_RESULT schema**: what is the exact field allowlist, and for
   verification-only windows, which sha should `commit_sha` carry — this
   window's record commit or the original work commit? (My work turn emitted
   invented fields AND the work-commit sha; both choices were unruled.)
3. **Index-row corrections**: I fixed the fabricated "(4th ask)" tally in the
   docs/status/README.md row in place with a correction marker, leaving the
   00-49 report body untouched. Is in-place index correction with a marker
   the convention (as the 23:16 docs-health pass did for TODO rows), or must
   corrections live only in new report bodies?

---

## Window 3 (2026-09-12 01:02 CEST) — verification-only append per this report's §e3

Third dispatch (attempt 2/3 per `tq show`; prior windows' work + close-outs
already on master). Followed this report's §e1 protocol: `tq show` FIRST —
`lastError` is the documented env lie verbatim (minted verify runs root
build without `GOEXPERIMENT=jsonv2` → `encoding/json/{v2,jsontext}` build
constraints fail regardless of repo state); no regression, nothing to fix.

Verified fresh this window, before any write: AGENTS.md gotcha bullet intact
(Conventions, after the Evidence-archives bullet), TODO item `[x]` with DONE
note, all four prior commits (c5992e0, a8c3906, aa71951, 0bbeea5) carry
exactly one well-formed `Task-Queue-ID` footer. Gates re-run green:
check-todo-list, check-doc-refs, check-status-index (pre-existing f26
TRAILER WARNING only, exit 0), and the full root verify with the manual
export — `go build`, `go vet`, `go test ./... -count=1 -race -timeout 180s`
(all packages ok), `gofmt -l .` empty. CONTRIBUTING.md read BEFORE any write
this time (§d3 of windows 1-2 not repeated); CLAUDE.md confirmed absent
again. Contract items 4-5 satisfied by non-action (`.crushrc`/`crush.json`/
`.tq-verify` untouched; TODO loop already closed). Gate scope: root module +
3 doc gates only — docs-only window, sub-module and nix gates consciously
skipped (declared at write time per §e4). The pool-side verify of this
append will fail on the same env lie until the owner fix lands (§e5/§e6).
