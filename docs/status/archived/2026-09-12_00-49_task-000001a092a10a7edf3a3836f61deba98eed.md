# Task 000001a092a10a7edf3a3836f61deba98eed — re-dispatch verification (evidence-copy gotcha)

- **When**: 2026-09-12, 00:48–00:55 CEST
- **Dispatch**: STALE RE-DISPATCH (4th ask of this ID). The work is already
  shipped: work commit `c5992e0` (AGENTS.md gotcha bullet + TODO close),
  close-out commit `a8c3906` (the 00:43 report). Per the 06-49 stale-re-dispatch
  precedent this window is VERIFICATION-ONLY: independently re-verify the prior
  window's claims fresh, change nothing about the deliverable, record the
  outcome. No scope creep, no drive-by refactors.

## a) Re-verified fresh this window (not inherited from the 00:43 report)

1. **AGENTS.md Conventions bullet present and complete** (lines 266–272),
   directly after the Evidence-archives bullet: `git check-ignore -v <targets>`
   FIRST (copy second), diff the auto-commit daemon commit's `--stat` against
   the intended file set, "clean `git status` is not a complete archive",
   silent-loss triangle named, f9 near-miss + 06-41 §d1/§e1-2 cited.
2. **Content accuracy vs the primary source**: spot-checked the 06-41 report's
   own §d self-criticism ("`git check-ignore` on the target paths should have
   been …") and §e procedure lines ("check-ignore -v <targets> first, cp
   second"; "check-ignore first, verify daemon --stat") — the bullet reflects
   the incident, not a paraphrase of the TODO line.
3. **TODO_LIST.md loop stays closed**: item is `[x]` with `— DONE 2026-09-12:`
   note per the file's convention; nothing to re-mark.
4. **Footer integrity**: both commits carry EXACTLY ONE well-formed
   `Task-Queue-ID:` footer matching this task ID (work `c5992e0`, close-out
   `a8c3906`); the commit-msg hook's shape requirements hold.
5. **Gates green in THIS window** (fresh runs at 00:49, tree clean beforehand;
   session-start ritual done): `check-todo-list.sh` ok, `check-doc-refs.sh` ok,
   `check-status-index.sh` ok (pre-existing f26 TRAILER WARNING only, owner
   ruling pending, exit 0), and the root-module verify with
   `GOEXPERIMENT=jsonv2`: `go build` + `go vet` + `go test ./... -race` all ok.

## d) Honest notes

1. This window produced zero deliverable changes — by design. The only new
   artifacts are this report and its index row.
2. The re-dispatch loop itself remains unresolved: the owner question in the
   07-03 report §g1 (what makes a re-dispatched, already-done task register
   COMPLETE) is still open; this report is the 06-49-style answer — verify,
   record, point at the original work commit.

## f) Next

Nothing new opened. The deliverable is done and now verified by a second
window; the sibling items in this TODO section (88 harvest /tmp-hot flag,
89 pool-health, 90 worktree doc, 91 session-close bridge) remain the next
work in line.
