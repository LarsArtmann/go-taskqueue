# Status Report — dead-sha gate self-test re-dispatch window (2026-09-20 06-23)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_06-17_task-000001a0bd03e8a51dff61e137aa26e541a0.md`, which owns any open residue.

Task: `000001a0bd03e8a51dff61e137aa26e541a0` — TODO row "check-dead-sha-refs.sh
`--self-test` mode: pin the gate's semantics durably …".

## Summary

**This window did zero new work — by design.** The item was already fully
shipped in a prior window: commit 0822025 ("test: pin dead-sha citation gate
semantics via --self-test") carries the exact task footer, the self-test
passes on this host, ci-local wires it (scripts/ci-local.sh:283-284), and
TODO_LIST.md:142 was already marked `[x]` with a DONE note. Per the
session-start ritual ("repeat dispatches are routine; fastest correct
response is verify → cite → stop"), this window verified and stopped.
Two prior daemon reports for this same task ID already exist (80d2d8e,
a47e548) — this is now the THIRD report covering the same DONE row, which is
itself a finding (see §e).

## a) FULLY DONE (this task's scope, verified this window)

1. Self-test exists and passes: `./scripts/check-dead-sha-refs.sh --self-test`
   → "dead-sha self-test ok (clean rc=0, exact file:line on rc=1, arrow
   old→new passes, baselined silent unless TQ_DEAD_SHA_VERBOSE=1)". All four
   pinned semantics from the TODO row are exercised against /tmp fixtures:
   clean/baselined run rc=0 with hit count "(2 baselined, 0 new)"; verbose
   flag surfaces the exact `TODO_LIST.md:1` citation; non-baselined SHA
   fails with exact `docs/status/report.md:1` file:line; arrow line exempted.
2. Wiring: gate + self-test both run as ci-local steps
   (scripts/ci-local.sh:280-284), mirroring the check-gosec.sh pattern.
3. Work commit 0822025 carries the exact `Task-Queue-ID:` footer → the
   queue↔git cross-reference resolves.
4. TODO_LIST.md row closed with a DONE note before this window.
5. This window's verification: build + vet green (GOEXPERIMENT=jsonv2),
   tree clean, no uncommitted drift.

## b) PARTIALLY DONE

Nothing in this task's scope. (If one insists: the DONE note in TODO_LIST
predates this window and was not re-verified against the run_gate extraction
line-by-line — but the self-test passing IS that verification, so it is
covered.)

## c) NOT STARTED

Nothing in this task's scope. All TODO-row sub-requirements were already
satisfied on arrival.

## d) TOTALLY FUCKED UP

Nothing this window — but honest observations, not failures:

1. **This window was arguably unnecessary.** The 05-16 window shipped and
   pinned the work; 80d2d8e and a47e548 are already two reports about the
   SAME DONE row. A third report spends a live-pool dispatch on prose. The
   queue-side fix is dead-letter/cancel semantics for re-dispatch of rows
   whose footer already resolved to DONE commits — not more reports.
2. No defects found in the shipped self-test: fixture repo construction
   (commit-tree child of HEAD, no `git reset`) is clean, and the test does
   not depend on host git state.

## e) WHAT WE SHOULD IMPROVE

1. **Re-dispatch loop (proven, 3× now)**: this task ID re-fired twice after
   DONE. Root cause: the harvester sees the unchecked→checked transition only
   at scan time; if a dispatch was minted before the checkoff, it re-runs.
   Candidate fixes: (a) enqueue-time footer→git-log check that stamps the
   task DONE at claim time; (b) harvest skip when `git log --grep
   Task-Queue-ID:<id>` already resolves; (c) closeout prompt rule "if the row
   is already [x] with a DONE note, do not mint another report".
2. **Self-test coverage gap (minor)**: the self-test does NOT exercise the
   40-hex (full-SHA) upper bound or a non-commit hex token (queue task IDs,
   CI run numbers) — the "hex that is NOT a commit object is ignored" branch
   is unpinned. The arrow-`<-` reverse-direction form is also unpinned
   (only `→` is exercised). One fixture line each would close this.
3. **run_gate extraction note**: the gate is now a function with
   caller-set `baseline`/`files`; the real-mode invocation below the
   self-test block duplicates the file-glob loop (docs/status/*.md). Fine at
   this size; worth a glance if the scope ever grows.

## f) Up to 50 things to get done next (task-adjacent only; not researched)

1. Pin the ignored-non-commit-hex branch (fixture with a 16-hex task-ID-like
   token that is not a commit object → rc=0).
2. Pin the full 40-hex SHA upper bound in the fixture.
3. Pin the reverse arrow form `old<-new` (the gate's grep allows both
   directions; the self-test only uses `→`).
4. Add a TQ_DEAD_SHA_VERBOSE-count assertion (verbose run prints exactly 2
   STALE lines).
5. Self-test should assert the gate's summary line count/format so a
   regression in "(N baselined, 0 new)" phrasing is caught.
6. Re-dispatch dedup: harvest-time footer→git resolution to stop DONE-row
   re-dispatches (the §e.1 fix — highest value).
7. Consider a `tq audit` check: task IDs whose footer resolves on master but
   which remain PENDING/RUNNING in the queue (the re-dispatch detector).
8. Gate guard-wiring: confirm check-guard-wiring.sh sees the self-test step
   (it is wired via ci-local, so likely fine — verify once).
9. dead-sha baseline: 28 rows remain (TODO_LIST.md:148 tracks the positional
   mapping) — that row is the active follow-up, already filed; do not
   duplicate it here (pointing to it instead).
10. Window count discipline: three reports for one DONE row suggests the
    re-dispatch cost model should include report-writing spend.

(10 items — the honest count of task-adjacent follow-ups found in passing;
padding to 50 would be noise.)

## g) Questions for the owner

1. **Re-dispatch policy**: for a task whose `Task-Queue-ID` footer already
   resolves on master and whose TODO row is `[x]`-DONE, should the pool
   auto-cancel the re-dispatch at claim time instead of running the
   verify-and-stop pass (saves a dispatch + report, costs a trust boundary
   on "footer = done")?
2. **Self-test scope**: are the three unpinned branches (non-commit hex,
   40-hex bound, reverse `<-` arrow) worth one more dispatch now, or fold
   into the next time the gate is touched?
3. **Report dedup**: when a re-dispatch legitimately ends in verify-and-stop,
   should the close-out SKIP minting a status report when one for the same
   task ID already exists in docs/status/ (append a one-line "re-verified"
   note to the existing report instead)?

## Verification citations

- Self-test green: `./scripts/check-dead-sha-refs.sh --self-test` run this
  window (rc=0, output quoted in §a.1).
- Wiring: scripts/ci-local.sh:283-284.
- Work commit: 0822025, footer `Task-Queue-ID:
  000001a0bd03e8a51dff61e137aa26e541a0` verified via `git log -1 --format=%B`.
- Build/vet green with `GOEXPERIMENT=jsonv2` this window.
- Prior windows: 80d2d8e (self-test pin), a47e548 (first re-dispatch report).
