# Task 000001a097a2c7953ab120778fc256de25ab — required-checks proposal close-out (brutal a)-g) self-review)

*(Archived 2026-09-13 docs-health pass: window complete: required-checks proposal shipped (790813f); residue = TODO row 118 [x]. Point-in-time snapshot — re-verify before treating any claim as current.)*

Written 2026-09-13 00:10 CEST. Work item: TODO_LIST.md §"Window f20–f24
follow-ups", row 115 — "Write the required-checks proposal (test-windows +
release-gates smoke first, then the scan jobs) into docs/planning/ for the
next release window". Work commit: `790813f` (docs: required-checks proposal
for CI gates (next release window)). Window type: docs/proposal, zero Go
code changed.

## a) FULLY DONE

1. **The proposal exists**: `docs/planning/2026-09-13_required-checks-proposal.md`
   (133 lines). Contents: verified problem statement (master UNPROTECTED —
   `gh api repos/LarsArtmann/go-taskqueue/branches/master/protection` → 404
   "Branch not protected", run 2026-09-13), six-job inventory table with
   per-job gate status and the runner evidence actually at hand (run
   34670475702 for test/test-windows; govulncheck's round-13 T7 evidence;
   gosec marked O5-gated; test-postgres honestly marked "no dedicated green
   citation at hand"), the step-vs-job mechanical wrinkle (required checks
   attach to JOB names; the smoke is a step inside `test`), recommendation A
   (extract a standalone `release-gates` job) with option B rejected for a
   stated reason, phase 1 (require `test-windows` + `release-gates`,
   evidence-gated per the round-13 T7 flip discipline), phase 2
   (`govulncheck` next; `gosec` ONLY after owner-ruling O5, carrying O5's
   recorded one-green-week condition verbatim instead of re-deciding it),
   a deliberately-deferred set (test/test-postgres/nix as owner question),
   an enforcement-scope section (classic protection gates PR merges only;
   rulesets extend required checks to direct pushes; admin-bypass nuance),
   failure modes (orphaned check names on job rename, emergency bypass,
   flaky-windows policy: fix the flake, never silently relax), a companion
   sanity-audit sketch (`scripts/check-required-checks.sh`, from the 09-33
   report item 24), and 4 packaged owner questions.
2. **External claims verified before encoding**: the proposal's load-bearing
   GitHub-mechanics claims (classic protection = PR merges only; rulesets can
   gate pushes) were checked against GitHub docs BEFORE writing (2026-09-13).
   The repo's own 404-unprotected state was pulled live, not assumed.
3. **TODO_LIST.md:115 flipped [x]** with a DONE note citing the deliverable
   path, the phase structure, the O5 dependency, the 404 verification with
   date, and the owner questions.
4. **Gates**: `scripts/check-todo-list.sh` ok; root `go build` + `go vet` +
   `go test ./... -race -count=1` green; `gofmt -l .` clean. One failure
   investigated before moving on (see §d4/§e5). Scope honestly root-only —
   see §b1.
5. **Commit hygiene**: the daemon folded my two files into footer-less
   `d876710` mid-session; stat-verified it contained EXACTLY my two files
   (no concurrent work swept in) and that it was unpushed, then applied the
   documented amend maneuver → `790813f` with exactly one well-formed
   Task-Queue-ID footer (commit-msg hook contract). Exactly the two intended
   files in the commit. `.crushrc`/`crush.json`/`.tq-verify` untouched.
   Nothing pushed.
6. **Scope held**: no drive-by refactors, no unrelated TODO rows closed, no
   speculative scripts written alongside the proposal (the sanity-audit
   script is DESCRIBED, not prematurely built — its home is phase 1).

## b) PARTIALLY DONE

1. **Gate scope is root-module-only** (build/vet/test -race + gofmt +
   todo-parse). The per-sub-module loop and `ci-local.sh` (nix, smokes,
   lint-baseline) did NOT run. Defensible — zero Go code changed, docs
   cannot break a module build — but the contract's "never leave the repo
   broken" deserves the strict reading only when code moves; declared here
   per the verification-claims-scope principle (TODO row 120 is literally
   this guidance, still open).
2. **The deliverable is a proposal, not enforcement**: nothing is required,
   nothing is extracted, nothing is flipped. That is what the item asked for
   ("proposal ... for the next release window") — but the row's DONE must
   not be read as "required checks exist". They do not; master remains
   unprotected until the owner acts.
3. **§f below is a brainstorm, not backlog**: per the status-report skill's
   routing rule, items beyond the TODO-ready core are ROADMAP fuel; the
   docs-health HARVEST pass that mints TODO rows from this report has NOT
   run (it is the next pass's job, not this window's — see §e6).

## c) NOT STARTED

1. Phase-1 step 1: extract the release-gates smoke into its own ci.yml job
   (deliberately deferred to the next release window by the item's framing).
2. The branch-protection flip itself (owner-run, admin-only — an agent
   literally cannot do it).
3. `scripts/check-required-checks.sh` sanity audit + ci-local wiring.
4. O5 ruling (gosec hard vs advisory), the ruleset-vs-classic decision, and
   the deferred-jobs decision (test/test-postgres/nix) — all owner.
5. Phase-2 flips (require govulncheck; require gosec post-O5).
6. Fresh runner-green citation pull for `test-postgres` (flagged in the
   proposal's own inventory table as a gap).
7. Noticed in passing, pre-existing, untouched (their rows live in
   TODO_LIST): the AGENTS.md release-gates identity-blind one-liner (row
   ~283), KanbanBoard evaluation (117), PageProps.SEO + icons.Render (118),
   example-server hardening (119), verification-claims guidance doc (120),
   CQA live verification (100, BLOCKED owner), the three BLOCKED owner rows
   (124-126 cluster), fullcore example run + hermetic fullcore smoke, the
   six unarchived 2026-09-09 reports, session-close bridge open items
   (crush#3146 triggers, daemon attribution, budget bypass, postgres parity).

## d) TOTALLY FUCKED UP

1. **Reached for grep instead of View before the first edit** — the edit
   tool refused with "must read the file first" and I burned a round trip
   re-reading TODO_LIST.md:112-118. Trivial, mechanical, and exactly the
   kind of ritual slip this section exists for: the file was ALREADY in
   context from the session-start grep, but grep output is not a read.
2. **The daemon beat me to my own commit and I fed the attribution gap
   again.** I wrote files → ran the (multi-minute) gate → found my work
   already folded into footer-less `d876710` → patched reactively via the
   amend maneuver. The repair was correct and verified, but the SEQUENCE was
   backwards: for a docs deliverable I should have committed the deliverable
   immediately after writing (the hook validates; the gate re-run would then
   amend only on red). Every reactive amend is one more instance of the
   already-tracked daemon-attribution problem (O7) instead of one fewer.
3. **The proposal's GitHub-mechanics section rests on an AI-mediated docs
   pull, not a primary-source read.** The claims match my prior knowledge
   and the fetch returned doc phrasing, but no doc URLs are cited IN the
   proposal, so a future reader cannot re-verify the "classic gates PR
   merges only / rulesets gate pushes" claims without redoing the research.
   For a repo whose culture is claims-carry-citations, that is a real gap —
   the fetch happened, the citation didn't land in the artifact.
4. **First failure response mixed gates into one background chain and read
   only the tail.** `check-todo-list && build && vet && test -race | tail -5`
   reported exactly one failing package — fine this time, but the tail-only
   read is the pipeline-masking pattern (04-13, 04-31 reports) one step away
   from a green lie. I recovered correctly (isolated 3x re-run, then a fresh
   full unfiltered pass), but the first instinct was the documented trap.
5. **Nothing else forgotten that I can find**: planning dir was listed
   before writing (no prior required-checks doc existed), CHANGELOG
   convention was checked before skipping it (planning docs land without
   CHANGELOG rows — verified against the dlq-autopsy design commit), the
   doc-refs exemption for docs/planning/ was read from the script itself,
   and the footer/hook contract was honored on the first commit. Listing
   the clean checks so the next reader doesn't re-audit them.

## e) WHAT WE SHOULD IMPROVE

1. **Commit-early ritual for docs deliverables**: write → `git add` +
   commit (hook validates index/todos) → run gates → amend only on red.
   Inverts this window's order, avoids the daemon race, and shrinks the
   attribution gap instead of growing it. The repo already proved amend is
   safe pre-push; the missing piece is just doing it BEFORE the long gate.
2. **Primary-source URLs belong in the artifact**: when an external
   mechanics claim (GitHub, Go, Nix behavior) carries a recommendation, cite
   the doc URL in the doc. AI-mediated fetches are secondhand evidence.
3. **Gate-scope declaration as a standing close-out line** (done here in
   §b1) — feeds the still-open verification-claims-guidance row (120): the
   govulncheck "clean" scope lesson keeps repeating in milder forms.
4. **TODO_LIST section headers rot**: this section's header still says
   "harvested from docs/status/2026-09-10_04-09" but that report moved to
   `docs/status/archived/` (check-doc-refs deliberately exempts TODO_LIST,
   so the stale path persists silently). One header-provenance sweep would
   fix the class.
5. **SSE heartbeat flake**: `TestSSEHeartbeatStopsBeforeHandlerExit` failed
   once under full-suite `-race` load (0.19s into the test, deadline
   exceeded), then passed 3x isolated and in a fresh full run. One flake is
   noise budget, but it has a tight internal deadline under race load —
   candidate for widening or a documented retry, before it costs a future
   window a false red.
6. **Harvest discipline**: §f below must be routed (TODO vs ROADMAP vs
   BLOCKED) by the next docs-health pass, not silently entombed in this
   file — the status-index makes the report discoverable; only HARVEST makes
   it actionable.

## f) NEXT (up to 50 — 32 items: 11 new from this window, the rest noticed in passing; tags: [todo] TODO-ready, [owner] BLOCKED, [road] ROADMAP fuel)

Required-checks follow-through (new, from the proposal itself):

1. [todo] Extract the release-gates smoke into its own ci.yml job (delete the
   `test`-job step in the same change; rename discipline: required-check
   names are job names).
2. [todo] Pull a fresh runner-green citation for `test-postgres` (inventory
   table gap; one `gh run list` away).
3. [todo] Add the GitHub docs source URLs to the proposal's
   enforcement-scope section (§d3 repair).
4. [todo] At next window start: owner flips branch protection — require
   `test-windows` + `release-gates` (evidence-gated: new job green on a
   runner run at HEAD first).
5. [todo] `scripts/check-required-checks.sh` (gh api → diff contexts vs the
   intended set from the proposal) wired into ci-local next to check-ci.sh.
6. [owner] O5 verdict: gosec hard vs advisory (proposal inherits it verbatim;
   one-fully-green-runner-week trigger recorded there).
7. [owner] Classic protection only, or a ruleset extending required checks to
   direct pushes (bypass rules, include/exclude patterns — bigger policy
   step, packaged separately on purpose).
8. [owner] Do `test` / `test-postgres` / `nix` join required checks in a
   later phase?
9. [road] Phase 2 flips once phases bed in: require `govulncheck`, then
   `gosec` per O5.
10. [road] Emergency-bypass + job-rename discipline note for the release
    runbook (orphaned required check = merges blocked forever).
11. [road] If rulesets are adopted: bypass policy for the owner account
    (visibility-vs-lock tradeoff is a ruling, not a default).

Session observations (new):

12. [todo] Widen/document `TestSSEHeartbeatStopsBeforeHandlerExit`'s internal
    deadline (one observed -race load flake this window; §e5).
13. [todo] TODO_LIST section-header provenance sweep: repoint
    "harvested from docs/status/2026-09-10_04-09…" to the archived path
    (§e4; TODO_LIST is doc-refs-exempt so nothing else catches it).
14. [todo] Docs-health HARVEST pass over THIS report's §f (route
    todo/owner/roadmap; the skill's anti-pattern list applies to the
    [road] tail).
15. [road] Task-closeout helper: one script that stamps the report path,
    index row, and footer-carrying commit together (the §e1 ritual,
    mechanized) — probably overkill; write only if the daemon race recurs.

Noticed in passing, pre-existing open rows (cite, not re-file):

16. AGENTS.md one-liner near the smoke list: release-gates smoke runs
    identity-blind via ci-local (row ~283, small, agent-executable).
17. templ-components `KanbanBoard` evaluation vs custom board columns/cards
    (row 117).
18. templ-components `PageProps.SEO` + `icons.Render` evaluation (row 118).
19. Example servers hardening beyond ReadHeaderTimeout, SSE WriteTimeout=0
    exemption documented (row 119).
20. docs/planning/ verification-claims guidance — gate-SCOPE lines in DONE
    notes (row 120; this report's §b1 is a live specimen).
21. CQA bridge live-API contract verification (row 100 — BLOCKED owner).
22. gosec gating owner call (row ~124 — same ruling as item 6 above; the
    proposal gives it a concrete trigger).
23. `task.AllStatuses` public re-tag API call (BLOCKED owner; export rides
    the local replace until then).
24. Pool policy on red master: refuse DONE vs disclose-only (BLOCKED owner;
    required checks make the GitHub side of this enforceable).
25. fullcore example run against throwaway postgres (fullcore section).
26. Hermetic fullcore smoke `scripts/smoke/fullcore.sh`.
27. Status-task window payload: carry the window's report paths.
28. Docs-health continuation: annotate + archive the six 2026-09-09 reports.
29. Session-close bridge open items: crush#3146 trigger automation,
    daemon-commit attribution gap (this window added one amend to its
    history), budget bypass, postgres parity.
30. Round-13 T26 policy-drafts batch: dependabot policy, CI topology
    one-pager, history-rewrite policy line, etc. (this proposal was its
    M127; siblings remain).
31. [road] Once required checks exist: extend `check-ci.sh` to diff master's
    latest run against the REQUIRED set (not just "latest run failed") —
    the pool's green-traffic assumption then matches the platform's.
32. [road] Push cadence: master sits N commits ahead of origin between
    owner pushes; runner proof (and required checks' raw material) only
    exists at push time — a stated cadence or push-on-green-ci-local rule
    would keep the CI evidence fresh (owner policy).

## g) QUESTIONS FOR THE OWNER

1. **Phase-1 mechanics**: should the `release-gates` job extraction land on
   master NOW (so the required-check name exists before the window opens),
   or ride the window's first push? And does the window-start flip include
   "Do not allow bypassing" (admins included) or keep the pragmatic
   admin-override default the proposal assumes?
2. **Push-side enforcement**: classic branch protection (PR merges only) or
   a ruleset that also gates direct pushes to master? This decides whether
   the owner's own push flow — the repo's primary write path — is gated at
   all; everything else in the proposal only governs PRs.
3. **Unpushed-commit cadence**: master accumulates local commits between
   owner pushes (ahead by 2 at this window's end), and runner proof +
   required checks only materialize at push time — is there a wanted
   cadence/policy (e.g. push after every green ci-local), or is the current
   owner-paced push intentional?
