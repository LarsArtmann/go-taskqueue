# Help-text smoke re-dispatch (verification-only) — task 000001a0a223a4728f43bb54d054ba8fc987

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-15_01-01_task-000001a0a223a4728f43bb54d054ba8fc987.md`, which owns any open residue.

- **Date**: 2026-09-15 01:05
- **Task**: Help-text smoke: run `tq` subcommand help (minimum `tq dlq -h`) asserting no
  parenthesized-identifier artifacts in flag strings (rename-leak class, 08:42 report e3/f2)
- **Work commit**: none this dispatch — the item was already DONE-on-arrival. Implementation
  shipped as 67905f9 (smoke + TODO checkoff) with daemon commit eaadb1a; full close-out at
  `2026-09-15_01-01_task-000001a0a223…md`. This report records THIS dispatch's re-verification.
- **Files changed**: this report + index row only.

## TL;DR

Fourth dispatch of a DONE task this week (§f17 class). Verification confirmed the work is
complete and healthy: TODO_LIST.md:60 checked `[x]` with evidence, smoke re-run PASS
(18 subcommands), commit 67905f9 carries the exact Task-Queue-ID footer, worktree clean.
The §b gaps from the 01-01 close-out (unpinned negative fixture, hardcoded subcommand list,
no ci.yml mirror) remain open — they are follow-up items, not defects.

## a) FULLY DONE

1. **Re-verified the deliverable end-to-end.** `./scripts/smoke/help-text.sh` run fresh:
   `help-text smoke: PASS (18 subcommands checked)`, exit 0. The smoke checks `tq -h` plus
   every subcommand help (grouped leaves `watermarks`/`session`, exit-1 `tq verdict`) for
   `[A-Za-z0-9]\([a-zA-Z][a-zA-Z0-9]+\)` artifacts (`task(store)` class).
2. **Confirmed the TODO loop is closed.** `grep help-text TODO_LIST.md` → row 60 is `[x]`
   with the 2026-09-15 done annotation citing the script and ci-local wiring.
3. **Confirmed the queue↔git cross-reference.** `git show 67905f9 --stat` carries the exact
   footer `Task-Queue-ID: 000001a0a223…`; nothing pushed; worktree clean at session start.
4. **Index row added before committing** (check-status-index gate honored proactively).

## b) PARTIALLY DONE

1. **Nothing partial belongs to this dispatch** — the only in-scope artifact was
   verification, and it is complete. The underlying task's known partials are inherited
   from the 01-01 close-out and restated in §c/§f, not re-done here.

## c) NOT STARTED

1. **All three 01-01 §b follow-ups remain unstarted** (correctly — separate scope):
   a pinned `--self-test`/negative fixture in help-text.sh; machine-readable subcommand
   inventory (the hardcoded 21-entry list can shrink coverage silently); ci.yml mirror of
   the smoke (f32 parity precedent).
2. **§f17 spend-policy item unstarted**: DONE-on-arrival re-dispatches keep costing a full
   turn each; the harness has no cheaper path (see §g3).

## d) TOTALLY FUCKED UP

Nothing is broken, but this dispatch's own fumbles:

1. **I did not check for a prior close-out before re-verifying.** The 01-01 report for this
   exact task ID was sitting in docs/status/ and I found it only after running the gate.
   One `ls docs/status/ | grep 000001a0a223` at the start would have oriented the whole
   dispatch (still would have re-run the smoke — independent verification is worthwhile —
   but the report would have been framed as delta, not discovery).
2. **Skill-view miss (standing):** the status-report SKILL.md view failed
   (`crush://skills/status-report/SKILL.md` not found by the View tool), and I proceeded on
   the dispatch prompt's explicit a)–g) skeleton + repo convention instead of chasing an
   alternative path to the skill body. Recorded honestly; the skeleton here matches every
   prior close-out in this directory.
3. **No new work fumbles because no new work was attempted** — the smallest correct action
   on a DONE item is verification + this report, and that is all that happened.

## e) WHAT WE SHOULD IMPROVE

1. **Dispatch pre-flight should include the status index.** Agents get re-dispatched on
   DONE items repeatedly this week; the first action for any task should be
   `grep <task-id> docs/status/README.md TODO_LIST.md` so the dispatch knows it is a
   verification pass before any tool runs.
2. **Verification-only dispatches should be cheap.** Re-running the gate + confirming
   footer + checkoff is ~3 commands; the remaining cost is report ceremony. A short-form
   report template for "DONE-on-arrival, re-verified" would cut this turn roughly in half.
3. **The 01-01 §e items stand** (self-test every regex gate; machine-readable command
   inventory; observed-output-first). Not re-argued here.
4. **Builtin skill loading has a failure mode with no fallback documented** — the
   `crush://` view errored; note it and move on, but harness owners may want it logged.

## f) Up to 50 things we should get done next

Direct tail of THIS task (1–3 are inherited 01-01 §f items, restated as still-open):

1. Pin a negative fixture / `--self-test` into help-text.sh (seeded fake tq with
   `task(store)`; smoke must FAIL).
2. Derive the subcommand list machine-readably (hidden `tq __commands` or go/parser) so
   drift fails instead of silently shrinking coverage.
3. Mirror help-text smoke into ci.yml (f32 precedent).
4. Owner ruling: CHANGELOG convention for smoke/gate-only additions (01-01 §g3, unresolved).
5. `grep <task-id>`-first pre-flight in the dispatch harness (§e1) — kills the
   DONE-on-arrival rediscovery cost.
6. Short-form report mode for verification-only dispatches (§e2).
7. §f17 spend policy: rule for DONE-on-arrival re-dispatches (four this week; each costs a
   full turn).
8. Builtin-skill load failure (`crush://` view error): log/route to harness owners (§d2).

Standing TODO_LIST neighbors observed in passing (verified present earlier sessions; not
re-verified this turn): 9. lint-annotations sub-module loop decision; 10. extract
`scripts/for-each-module.sh`; 11–12. unit-env GOEXPERIMENT owner fix + regression test;
13. client/server-mode experiment or formal park; 14. templ-components sweep wave 2.
Broader ROADMAP fuel (route through docs-health HARVEST): 15. postgres parity pass for
post-conformance surfaces; 16. pin canonical golangci-lint version; 18. batch-scorer
live-pool trial; 22. sweep for other rot-prone hardcoded dispatch lists; 24. `pipefail`
audit across scripts/_.sh; 25. shared smoke preflight lib; 34. gate `bash -n` for
scripts/_.sh in ci-local; 35. `self-test-all.sh` runner for every smoke's negative mode;
38. verify smoke under `TQ_BIN=result/bin/tq` once; 39. timeout guard for the smoke loop.

## g) Questions I can NOT figure out myself

1. **Should DONE-on-arrival re-dispatches get a cheaper contract?** The queue keeps
   re-sending completed items (four this week). Is re-verification the intent (keep paying
   a turn), or should the dispatch carry a "verify-only, short report" flag the harness
   could honor?
2. **CHANGELOG for gate-only additions** — carried from 01-01 §g3: does a new smoke script
   get a CHANGELOG row, or is that surface reserved for user-visible behavior? I cannot
   derive the convention from the existing entries alone.
3. **Is the hardcoded subcommand list acceptable until `tq __commands` exists, or should
   the smoke hard-fail when `tq -h`'s usage prose no longer matches the list?** A count
   assertion (01-01 §f8) is a guess at intent; an owner ruling picks the right tripwire.

—
Verification evidence: smoke PASS output in this session's transcript; footer + checkoff via
`git show 67905f9 --stat` and `grep -n "help-text" TODO_LIST.md`.
