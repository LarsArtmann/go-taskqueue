# Status Report — Dead-SHA Residue Positional Mapping (FIFTH dispatch, FULL close-out)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_07-07_task-000001a0bd2d1b8972f150b0e7d530c78931.md`, which owns any open residue.

- **Task:** 000001a0bd2d1b8972f150b0e7d530c78931 (TODO row 148, `[x]` since the 07-07 ship)
- **Window:** 2026-09-20 ~07:15–07:19 CEST
- **Work commits (prior windows):** c1cb0a9 (baseline verdicts) + 292b265 (close-out) + 57deb90 (fourth-dispatch verify) + 9466b59 (this window's verify report)
- **Scope this window:** none — verify → cite → stop. Zero source delta.

## a) FULLY DONE

1. **Turn-1 task-ID grep over docs/status/** caught the DONE state immediately: the 07-07 ship plus two verify-only successors (07-13, 06-23-class) exist; TODO row 148 is `[x]` with the full DONE note. The ritual worked as designed — no work was re-derived, no verdict re-litigated.
2. **Fresh gate run at HEAD:** `./scripts/check-dead-sha-refs.sh` rc=0 — **111 baselined, 0 new** (count drift 110→111 since the 07-13 window: another report cites an already-baselined dead SHA; documented STALE-count semantics, not residue growth — the residue itself is still 28 SHAs per the baseline header).
3. **Footer cross-reference verified mechanically:** `git log` trailers show c1cb0a9, 292b265, and 57deb90 all carry `Task-Queue-ID: 000001a0bd2d…`.
4. **Working tree clean**, no stash, session-start ritual (git log -5 over the whole repo, status, stash list) executed in the first command batch.
5. **CITATION CHECK from the task text honored:** dd543ec/e9ef358 were already re-resolved by the 07-07 window (→d035911 / →ca514a8, exact patch-id, recorded in the baseline header). No reliance on unreachable SHAs this window; nothing to fix in the source item — row 148's own note cites the reachable twins.
6. **Self-caught trailer defect:** my first work commit (451de93→9466b59) buried the Task-Queue-ID footer ABOVE the attribution block, making it invisible to git's trailer parser — the exact failure class documented in AGENTS.md (the 2026-09-19 heal). Caught it by re-running `git log -1 --format='%(trailers:key=Task-Queue-ID,valueonly)'`, confirmed empty output, and amended (unpushed) so the footer is the last paragraph → 9466b59 parses correctly. The queue↔git cross-reference was never at risk of a silent miss, but only because I checked.
7. **Verify report written AND indexed** (`docs/status/README.md` row prepended); `check-status-index.sh` rc=0 before commit.

## b) PARTIALLY DONE

1. **The task itself is fully shipped** (07-07 window: 11/28 residue SHAs resolved content-exactly, 15 ruled citation-lost with per-SHA anchors, verdicts recorded durably in `scripts/dead-sha-baseline.txt`). What remains incomplete is the DEPENDENT work, deliberately out of scope: the baseline rows drop only when the refs/original drop lands, and that drop is owner-gated and still unanswered (07-07 §g1). Until then the gate silences a steadily growing silent-count (973→110→111 cite-sites across passes).
2. **This window's own deliverable** is a verify-only close-out, which by prior precedent (06-50/06-55) needs no full a–g report — one was written anyway per the explicit close-out ask in the dispatch. The standing convention question (§g) is unresolved.

## c) NOT STARTED (dependent rows, out of scope by design)

1. **The refs/original drop itself** — `refs/original/refs/heads/master` still pins e9ef358; dropping it + purging all baseline rows is the actionable core (07-07 §f1), owner-gated because gc afterwards makes the 15 LOST SHAs permanently underivable.
2. **Post-drop gc cycle + gate re-verification.**
3. **The 07-07 §e improvement list** — patch-id-first playbook note in AGENTS.md, `check-dead-sha-refs.sh --self-test` baseline-validation mode, premature-gc guard in ci-local, arrow-form conversion, structural fix for the daemon amend-race. None started; none drive-by'd (correct).
4. `scripts/session-start.sh` (existing TODO row) — would have automated this window's turn-1 grep; still unchecked.

## d) TOTALLY FUCKED UP

1. **My first commit's footer was INVALID** (451de93→9466b59, amended to 9466b59): I ran the commit through the standard attribution HEREDOC and did not notice the template places `Task-Queue-ID:` two paragraphs above the trailer-parser's reach. In THIS repo that is a known, documented, previously-burned failure mode — the footer must be the LAST paragraph. I caught it myself within one command (trailer-format check), and the amend is legal (unpushed), but the miss should never have happened: the repo's own AGENTS.md warns about exactly this shape, and the commit-msg hook (install-pre-commit.sh) guards it — the daemon bypasses hooks, but I ran `git commit` directly through a hook-respecting client, so why did the hook not catch it? Unverified — worth a check (§f). A footer that parses is the ONLY mechanism by which this queue can derive the work; shipping it broken, even for minutes, is a real defect.
2. **Report filename timestamp drift:** the prior turn wrote the verify report as `07-21_*` when the wall clock read 07:16–07:19 — the timestamp was guessed, not run through `date` first. Cosmetic (the index row matches the filename, both gates rc=0), but it puts a future timestamp in the archive, which breaks the sort-order convention the directory relies on. This full report uses `date`-derived `07-19` — one minute BEFORE its own predecessor's filename.
3. **FIFTH dispatch on a closed ID** — not mine, but must be named again: five windows now (05-35-class ship, then four verify re-dispatches) on one DONE row. Each costs a full context load for a two-command verification. The dispatch layer is the defect (flagged in 05-57 §g, 06-04 §g, 06-07 §g, 07-13 §d).
4. **Did not run the `-race` test suite this window** — defensible (zero source delta; the gate battery + trailer checks were the proportionate battery), but the verify-before-filing instinct says a "verified" claim should state the scope: this window's green is gates + tree + trailers, NOT build/tests. Prior verify windows did run build+vet; I skipped even that. For a docs-only, zero-delta window the risk is genuinely nil, but the citation-carrying principle ("cite the gate you rest on") cuts both ways — the DONE claim this window rests on gates only, and the report must say so. It now does.

## e) WHAT WE SHOULD IMPROVE

1. **Repeat-dispatch suppression at the source** — the single highest-leverage fix on this ID's entire history: the queue should refuse (or downgrade to a cheap echo) a dispatch whose row is already `[x]` with a DONE note. Five windows of evidence now. Nothing I control can suppress it.
2. **Commit-template footer placement:** the attribution HEREDOC used by agents puts the Task-Queue-ID above the Crush/Assisted-by block. In this repo that breaks trailer parsing. Either the footer goes into the final paragraph (below Assisted-by), or the commit-msg hook must catch the placement — today I cannot confirm which guard exists (§g3/§f). A one-line AGENTS.md correction to the footer template would prevent the next agent from repeating my 451de93→9466b59 miss.
3. **Verify-window minimal format** — a re-dispatched closed ID needs no a–g report; a three-line verdict + gate rc + trailer check would do. Owner ruling still outstanding (carried since 06-50/06-55).
4. **Baseline-count trend note per report** (07-13 §e3, carried): 973→110→111; shrink progress is invisible without the one-liner. Included here in §a2.
5. **Timestamp discipline:** always run `date` before writing a report filename — the 07-21-vs-07-19 drift in the same window proves the guess-first habit produces out-of-order archives. (The close-out ask itself now mandates `date`; this window is the proof it was needed.)
6. **Carry the 07-07 improvement list forward, don't re-derive it** — §f1–4 of that report remain the canonical next-work list; this window adds the footer-template fix (e2) and timestamp discipline (e5) as the only genuinely new items.

## f) Up to 50 things we should get done next

1. **Execute the refs/original drop + baseline purge** (07-07 §f1, carried) — the actionable core; verdicts are recorded in the baseline header, the drop is mechanical once approved; owner approval outstanding since 07-07 §g1.
2. **Post-drop gc + gate re-verification** (dependent on f1).
3. **Baseline-validation mode in `check-dead-sha-refs.sh --self-test`** (07-07 §f3): rows real+unreachable, RESOLVED twins reachable — would catch a typo'd twin mechanically.
4. **Premature-gc guard in ci-local** (07-07 §f4): "refs/original exists OR baseline empty, else fail" — protects the LOST verdicts until f1 lands.
5. **Check whether the commit-msg hook (install-pre-commit.sh) enforces footer PLACEMENT** (last-paragraph rule), not just count/well-formedness; if not, extend it — that hook would have caught my 451de93→9466b59 miss mechanically.
6. **Fix the attribution HEREDOC convention** so the Task-Queue-ID footer is the trailer-parser-reachable last paragraph in every future agent commit (AGENTS.md one-liner + template change).
7. `scripts/session-start.sh` (existing row) — automate the turn-1 grep.
8. **Verify-window minimum-battery convention** (existing row 150) — standardize gate rc + footer trailer check + tree state for verify-only windows.
9. Repeat-dispatch suppression (§e1) — flag with five-window evidence.
10. Arrow-form conversion of tolerated RESOLVED citations (07-07 §f9/§g2) — shrinks the baseline ~11 rows without the drop.
11. Structural fix for the daemon amend-race (07-07 §f10) — the generator of ALL residue classes.
12. AGENTS.md note: patch-id-first twin matching is the canonical resolver (07-07 §f7/§e1).
13. Retro-fix the 07-21 filename → rename to a ≤07-19 timestamp + repoint the index row + citations (check-doc-refs gates the move), OR accept the sort-order blemish with a one-line archive note — owner's call, tiny either way.

(13 offered; items beyond this duplicate 07-07 §f1–25 verbatim — padding refused, same refusal every window on this ID has made.)

## g) Questions I can NOT figure out myself

1. **Repeat-dispatch policy (fifth ask):** should the dispatcher stop minting tasks for rows already `[x]` with DONE notes — or, if re-verification is intentional, mark the prompt as such so the window can skip the full a–g report?
2. **Refs/original drop approval + executor** (carried unchanged from 07-07 §g1): owner-run or agent-run once approved? The verdicts make it mechanical; the point of no return is the post-drop gc, which permanently destroys the 15 LOST SHAs. Your approval, your timing.
3. **Footer-placement guard:** is the commit-msg hook supposed to catch a non-last-paragraph Task-Queue-ID footer (in which case my 451de93→9466b59 slipped through a hook bug worth filing), or does the hook only check count/well-formedness (in which case should the placement rule be added)? I can read the hook source next window if you want it fixed rather than answered.
