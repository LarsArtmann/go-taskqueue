# Task 000001a0ace13249179222ae9ed9deccc8d5 — Session trigger #3 (crush #3146 adoption): second verify window

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-17_03-04_task-000001a0ace13249179222ae9ed9deccc8d5.md`, which owns any open residue.

**Date:** 2026-09-17 03:05–03:12
**Work item:** TODO_LIST row 108 — "Session trigger #3 — adopt crush #3146 SessionEnd when released: point the hook at `tq session close` (enqueue-only fits the ~2s budget) (03-28 §f3)"
**Window lineage:** second window on this task id today; prior window
03-04 (same id) established the block; this window re-verified per the
re-check command the row carries, and CLOSED one of that window's §b gaps.

## Summary

Verify-cite-stop re-dispatch. The upstream gate (crush #3146 SessionEnd)
still has not landed; the smallest correct change was a fresh re-verify, a
re-check stamp on the row, and one line committed (58175a7). During the
close-out self-review I noticed the prior window's release-body evidence had
an empty-body ambiguity and ran the stronger compare check NOW — the gap is
closed in this window, not left open.

## a) FULLY DONE

1. **Re-verify per the row's own re-check command** (the ritual the row was
   designed for): `gh api repos/charmbracelet/crush/pulls/3146 --jq
   .merged` → `{"merged":false,"state":"open"}`. Evidence: command output
   this window (03:06).
2. **Release-surface re-check:** latest crush release is still v0.95.0;
   release body carries zero hook/SessionEnd/#3146 mentions (grep count 0).
3. **Compare check — prior window's §b1 gap CLOSED:** `compare/v0.94.2...v0.95.0`
   = 13 commits, ZERO matching hook/SessionEnd/#3146. This upgrades the
   release-body leg of the triad from "no output, possibly empty body" to a
   commit-level negative — the block is now evidenced at all three legs with
   no ambiguity (command output this window, 03:09).
4. **Installed-binary check:** crush v0.94.2 on this host (unchanged since
   the 03-04 window's "Unknown command" for `crush hooks`) — no SessionEnd
   surface to adopt.
5. **TODO_LIST row 108 updated** with a second-verify-window stamp carrying
   the fresh evidence + the re-check command, left unchecked per contract;
   committed as 58175a7 with the task footer (1 file, 1 line;
   `check-todo-list.sh` → "TODO_LIST gate ok"; commit-msg hook accepted the
   footer; `git status` clean after).

## b) PARTIALLY DONE

1. **No build/test gate cited this window:** the diff is one markdown line;
   the executed gates were `check-todo-list.sh` + the commit-msg hook.
   Standing convention wants a gate citation even on docs-only windows; the
   root `go build` was not run. (Prior windows were dinged for exactly this
   class; carrying it honestly rather than claiming exemption.)
2. **§f follow-ups from the 03-04 window still un-actioned:** its items
   (watch cadence for #3146, hook-config draft shape, sweeper-demotion
   decision after #3 lands) are backlog-level and none were appended as new
   TODO rows this window — they live in the 03-04 report only. Row 108
   itself carries the re-check command, which is the load-bearing one.

## c) NOT STARTED

1. **The actual adoption** (the item's payload): writing the SessionEnd hook
   config pointing at `tq session close --db <journal>`, the ~2s-budget
   verification, and the sweeper/registry demotion decisions. Hard-gated on
   upstream: PR #3146 is open with merge conflicts and absent from v0.95.0
   (13 commits between v0.94.2 and v0.95.0, zero matching). No code or
   config was written against an imaginary hook surface.
2. Everything downstream of it (see the 03-04 report §c) — correctly not
   started; the queue will re-fire the row when the gate changes or on
   cadence.

## d) TOTALLY FUCKED UP

Nothing this window — no misfires, no reverted edits, no daemon race, no
wrong-first-approach (the gh api route was used first, unlike the prior
window's agentic_fetch×2 detour). Honest near-miss: I almost re-ran the
release-body grep and stopped there again; the self-review caught that the
compare command (named in the 03-04 report as the cheap follow-up) was
runnable RIGHT NOW, and it was run before writing this report. The §b1
ambiguity from the prior window existed for ~5 minutes longer than it
should have because the first pass settled for the weak evidence.

## e) WHAT WE SHOULD IMPROVE

1. **Verify windows should execute their own prior window's "cheap
   follow-ups" when they are still runnable** — the compare check took one
   command and closed a documented evidence gap; leaving a known-unverifiable
   leg standing when the stronger check is one `gh api` away is lazy.
2. **The row's BLOCKED note is now the evidence store, not the reports** —
   good: a future window needs only the row, not a report crawl. Keep
   stamps compounding there.
3. **Docs-only windows should still run one cheap gate** (todo-parse +
   commit-msg hook here) and CITE them, which this window did — preserve
   that pattern.
4. **Second same-day windows on one id should open by diffing against the
   prior window's §b list**, not just the row — that is how the compare gap
   got caught.

## f) NEXT (up to 50, most impactful first; starred = new this window)

1. ★ When #3146 merges or a crush release carries it (compare-check, not
   release-body grep): write the SessionEnd hook config pointing at
   `tq session close` enqueue-only; verify the ~2s budget (row 108, the
   item itself — unblocks on upstream).
2. ★ (carried) Decide the re-check cadence for row 108: leave it to queue
   re-dispatch (current, proven working — 2 windows today) vs a scheduled
   cron row; the queue re-dispatch cost today was trivial, so default =
   no change until it isn't.
3. (carried, from 03-04 §f) When the hook ships: decide what the session
   registry sweeper becomes (primary → crash-recovery net) and record it
   in the design doc (OQ3 fully answers then).
4. (carried) Hook config format risk: #3146 has 29 review comments — the
   config shape may change before merge; re-read the PR diff at adoption
   time, do not draft against today's shape.
5. (existing row 110) `tq session list` (opened-without-closed, crash
   recovery) + `tq session close --dry-run`.
6. (existing row 109) Postgres session bridge: AppendFact + conformance, or
   fail-fast error.
7. (existing row 150) Owner ruling: daemon-fold attribution.
8. Rows 111+ of the harvest section remain queue food — no new backlog rows
   appended this window (nothing discovered that is not already a row or a
   carried item above).

(Short list is deliberate: this window produced no new findings; padding
to 50 would fabricate work.)

## g) QUESTIONS (up to 3, cannot self-answer)

1. **Cadence owner for the block:** is queue re-dispatch (two windows today)
   the intended re-check mechanism for row 108, or do you want it
   de-duplicated via a slower explicit cadence so windows stop re-firing
   same-day on a known-external gate?
2. **At adoption time, does the hook replace `tq crush` wrapper usage** for
   interactive sessions, or do both stay (hook = primary, wrapper = manual
   fallback)? The demotion decision changes what `tq crush` docs promise.
3. **When #3 lands, should the sweep default `--allow-dirty` stance be
   revisited in the same change** (02-51 §f48 flagged close minted via
   `tq session close` defaults RequireClean=true, which requeues-until-DLQ
   on a dirty multi-agent repo), or is that a separate row?

## Verification citations

- `gh api repos/charmbracelet/crush/pulls/3146 --jq .merged` →
  `{"merged":false,"state":"open"}` (03:06 this window)
- `gh api .../compare/v0.94.2...v0.95.0` → 13 commits, 0 matching
  hook/SessionEnd/#3146 (03:09 this window)
- `crush --version` → v0.94.2 (03:06 this window)
- `./scripts/check-todo-list.sh` → "TODO_LIST gate ok" (before commit 58175a7)
- Commit 58175a7 carries the task footer, accepted by the commit-msg hook
