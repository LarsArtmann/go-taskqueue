# Close-out self-review — canary single-sourcing re-dispatch (verify window)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_04-51_task-000001a0bcb616c4382e67e8a034cadc4e83.md`, which owns any open residue.

Task `000001a0bcb616c4382e67e8a034cadc4e83` · 2026-09-20 04:56 CEST ·
SECOND window on THIS task ID, first was the work+closeout window (04-51
report, work commit 094b22d). This window: verify→cite→stop, zero source
edits.

## a) FULLY DONE

- **The task's deliverable was already shipped** before this window: the
  five-canary guard lives in `scripts/for-each-module.sh:26-36` (canary
  list after the 0-target guard, `grep -qxF` exact-line membership, FAIL
  to stderr naming every missing canary + the conscious-update hint,
  exit 1). Commit 094b22d carries this task's `Task-Queue-ID` footer.
- **TODO row 139 already `[x]`** — no TODO edit needed, no closure edit
  made.
- **Fresh verification battery this window** (independent of the 04-51
  evidence):
  - Happy path: `./scripts/for-each-module.sh` rc=0, 15 targets
    (executor, internal/executor, internal/journal, internal/journal/cqrs,
    internal/queue, internal/queue/postgres, internal/queue/sqlite,
    internal/task, internal/worker, journal, queue, queue/postgres,
    queue/sqlite, task, worker).
  - Negative control RE-EXECUTED: /tmp copy of the repo with
    `internal/task/go.mod` removed → rc=1 with the exact line
    "FAIL: module enumeration is missing canary module(s): internal/task —
    …" (scratch trashed after; no fixtures in the gated tree).
  - Consumer inheritance re-confirmed by read: ci-local.sh:73
    `mods="$(./scripts/for-each-module.sh)"` under
    `set -euo pipefail` (:5), loops fed from `$mods`; ci.yml's six
    capture-form loops (checked in prior windows at
    :45/:59/:98/:161/:255/:294) inherit the same exit status through the
    `mods="$(…)"` assignment under `bash -e`.
- **This report written + indexed** in docs/status/README.md.

## b) PARTIALLY DONE

- **Verification depth is asymmetric**: the happy path and the
  missing-one-canary shape were RE-EXECUTED this window, but the
  all-canaries-missing shape (empty-ish find output that still passes the
  0-target guard — e.g. internal/* renamed wholesale) was covered only in
  the 04-51 window's /tmp evidence, not re-run here. The guard code is
  one loop, risk is low, but a full two-branch negative re-run would be
  the complete battery.

## c) NOT STARTED (correctly out of scope)

- The 04-51 report's own follow-ups stand untouched: durable negative-
  control pin for the enumerator's guards (check-transient-retry.sh
  pattern), canary-list dedup with check-gosec.sh's array, lint-baseline/
  lint-annotations/check-go-mods inline-enumeration rows (147), the
  capture-shape regression pin (148). All are separate backlog rows;
  this window's contract forbids scope creep.
- No build/test re-run beyond the enumerator itself: zero source files
  changed this window, so a root gate re-run would only re-certify
  someone else's HEAD. Prior windows' green gate runs stand.

## d) TOTALLY FUCKED UP

- **Nothing this window.** No edits, no stray fixtures, no
  PIPESTATUS-class capture errors (rc captured directly, counts read
  from output, not filtered pipelines). One honest miss below in §e.
- Near-miss avoided: the 094b22d diff is TODO_LIST.md only — the script
  itself rode the d115f37 auto-commit. Verified by
  `git log --follow -- scripts/for-each-module.sh` before citing, so the
  report attributes the code to the right commit family instead of
  crediting 094b22d with the script hunk.

## e) WHAT WE SHOULD IMPROVE

1. **Check the DONE-on-arrival signal BEFORE any work planning**: the
   task-ID grep over docs/status/ (turn-1 ritual item) was done, but the
   verification battery could have been assembled from the 04-51
   report's evidence section + deltas only, saving the state re-reads.
   It was — but only after re-reading the script and TODO row in full.
2. **The negative control used a full `cp -r` of the repo** (~seconds,
   heavyweight). A /tmp scratch tree with just the script + a fake
   internal/ layout would be faster and immune to repo-side concurrent
   commits landing mid-copy (harmless here, but the fixture is a moving
   target).
3. **The all-canaries-missing branch** (§b) should join the standard
   verify battery — it is the only canary-guard branch not re-executed
   this window.
4. **Close-out reports for verify-only windows** still carry the full
   section template; a lighter verify-variant (a/b/d/e only) would cut
   boilerplate — though the fleet convention currently wants the full
   form, so this is a ruling question (see §g3).

## f) TOP NEXT WORK (backlog, not commitments)

1. Durable negative-control pin for for-each-module.sh guards (0-target
   + canary branches; check-transient-retry.sh pattern, ci-local step) —
   the 04-51 report's top follow-up, still open.
2. Dedup the canary list: check-gosec.sh's array and
   for-each-module.sh's `canaries=` string pin the same five modules in
   two places; a triage change should be one edit.
3. Row 147: lint-baseline.sh:35 + lint-annotations.sh:69 +
   check-go-mods.sh:31 inline/unguarded enumerations — silent-narrow
   family, switch to captured guarded enumeration.
4. Row 148: capture-shape regression pin — every `for m in $mods` in
   ci.yml + ci-local.sh must be fed by a for-each-module.sh capture.
5. check-guard-wiring-style audit that no consumer script enumerates
   modules by hand (04-51 §f4).
6. ci.yml pipefail audit of the six loops' exact capture forms (04-51
   §f6).
7. Smoke that renames a fixture sub-module in a scratch copy and asserts
   every gate trips (04-51 §f20-class).
8. cmd/tq gosec coverage row 124 (BLOCKED owner ruling).
9. Dead-SHA citation gate (TODO row: check-doc-refs extension).
10. Drop refs/original backups once the healed lineage is pushed
    (BLOCKED owner push).
11. AGENTS.md Conventions one-liner: citations written only after gate
    output is in hand.
12. Committed wrapper-path smoke leg for `tq crush --bin` (existing row).
13. Session-start ritual script (existing row 2026-09-19 00-32).
14. Surface module-enumeration failures in the webui health page
    (04-51 ROADMAP fuel).
15. Review whether TQ_GOSEC_ENUM override should validate output shape
    (04-51 §f).

(15 items — the honest remainder of this seam's open surface; padding to
50 would mint noise. The §f cap is a ceiling, not a quota.)

## g) QUESTIONS FOR THE OWNER

1. **Canary-list dedup shape**: should check-gosec.sh STOP carrying its
   own array and instead source the list from for-each-module.sh (e.g.
   a `--canaries` flag or a parse of the script), or keep two copies
   with a parity assertion in its --self-test? First is one source of
   truth but couples the gate to enumerator internals.
2. **Durable pin for the enumerator's guards**: ci-local step
   (check-transient-retry.sh pattern, new small script) vs folding a
   branch into check-gosec.sh --self-test (which already stubs the
   enumerator via TQ_GOSEC_ENUM and could assert the REAL enumerator's
   negative branches directly)?
3. **Verify-window report weight**: is the full a-g template required
   for verify-only close-outs, or may future windows use a reduced
   variant (§e4)?
