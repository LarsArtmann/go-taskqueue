# Rate-Limit Retro Task (000001a0a27176572e0bcf9c305950d445a5) — 3rd-Lap Self-Review — 2026-09-15 02-34

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-15_02-28_task-000001a0a27176572e0bcf9c305950d445a5.md`, which owns any open residue.

**Task:** 000001a0a27176572e0bcf9c305950d445a5 — "Post-deploy retro: one week
after the input flip, count 429 requeues vs pre-deploy dead-letters and record
the number in a status report" (13:29 report f50, M124).

**Lap context:** this is the THIRD paid dispatch on this task ID. Lap 1
(a8a84e0, 02:27) did the actual retro. Lap 2 (dbe152c, 02:29) and lap 2.5
(dca880a, 02:32) were verification close-outs. Lap 3 (this one, 02:32
dispatch) found DONE-on-arrival, added zero deliverable changes, and now
writes its own brutal review. The honest headline: the queue paid three
concurrent sessions for one task that one session completed in minutes.

## a) FULLY DONE

- The retro itself: production journal counted, numbers recorded — pre-flip
  8 429-class dead-letters (28 failed attempts, 24 attempts burned) vs
  post-flip 0/0/0; zero 429 requeues because zero 429s occurred
  (docs/status/2026-09-15_02-30_post-deploy-rate-limit-retro.md).
- TODO_LIST.md row 73 checked [x] with DONE note and the 2026-09-18
  7-day re-check carried forward.
- Index rows in docs/status/README.md for both prior close-out reports.
- Footer-bearing commits: a8a84e0, dbe152c, dca880a — all three greppable by
  the Task-Queue-ID footer; cross-reference intact.
- This lap's verification: fresh greps + `git log` trailer check + report
  re-read at HEAD. All claims re-verified, nothing taken from memory.

## b) PARTIALLY DONE

- The runbook scheduled the retro for flip + 7 days (2026-09-18); it ran at
  day +4. The report argues window-insensitivity honestly (zero post-flip
  429s means the comparison cannot change), but the FINAL one-week number
  still needs the 09-18 re-check. Interim, by design — noted in the report.
- The mechanism itself (rate-limit requeue without burning an attempt) is
  deployed-but-unproven in production: the retro proves no 429s happened,
  not that the requeue path works when one does. That proof awaits the
  first live 429.

## c) NOT STARTED

- Nothing within this task's scope. The 7-day re-check is scheduled for
  2026-09-18 and is correctly OUT of this dispatch's scope (the task says
  "one week after the input flip" — the final lap).
- The first-live-429-window production proof (open observation, not a
  deliverable of this task).

## d) TOTALLY FUCKED UP

1. **Three paid laps for one task (the big one).** The dispatch pipeline has
   no mint-time done-check: this task was closed by lap 1, yet laps 2 and 3
   were both minted and both billed a full session. ~6x the necessary spend
   (3 laps, and lap 2 itself split into two close-out commits). §g asks the
   owner about the gate.
2. **Filename clock drift (recurring).** Lap 1's report is named
   `02-30` while git records commits at 02:27–02:32 — the report filename
   clock ran ahead of the commit clock. Same drift lap 2 flagged in itself
   (`02-28` file, 02:29 commit). Third occurrence this window; nothing
   fixes it except a mint-time convention (report timestamp = git clock).
3. **Footer verified late, not first.** This lap checked `%(trailers)` on
   the prior commits — confirming the footer lives in commit BODIES
   (grep hit) — only after deciding DONE-on-arrival. The cross-reference
   claim should have been verified BEFORE the verdict, not after.
4. **Index-grep ordering.** The prior lap's index-row check came after the
   close-out was drafted. Cheap checks first, verdict second — the verify-
   before-filing order was inverted twice on this task ID.
5. **No gate run on a docs-only lap is fine, but lap 2 explicitly logged
   "no -race gate run" as a self-criticism while ALSO adding zero code.
   The self-review machinery keeps inventing process guilt for no-op laps
   instead of concluding the honest thing: no code, no gate needed.

## e) WHAT WE SHOULD IMPROVE

- Mint-time done-check: before paying a session, grep TODO_LIST + footer
  commits for the task ID. This single gate kills the entire class of
  paid re-dispatch laps (this task, plus the session-snapshot ghost task
  earlier the same evening — that's TWO same-day instances).
- Report-filename convention: derive the timestamp from `date` at write
  time AND note the git clock, or name reports by the closing commit SHA.
- "DONE-on-arrival" laps should be one small commit (this review) with the
  footer — laps 2 and 2.5 each produced a full close-out report + index
  row for zero deliverable value. The review of a no-op lap should be
  shorter than the work it reviews.
- Keep the interim/final distinction visible: the 09-15 retro report says
  "interim" in TODO row 73 but the report title says just "Post-Deploy
  Retro" — the 09-18 final should supersede with an explicit pointer back.

## f) NEXT (up to 50, focused on what this lap noticed; standing backlogs referenced once, not itemized)

Direct follow-ups from THIS task window:

1. 2026-09-18: run the 7-day re-check and write the FINAL retro
   (supersede 02-30 report, repoint citations per archiving rules).
2. Owner: add a mint-time done-check to the harvest/pool path (grep TODO
   - footer commits) — the 3-lap waste above is the evidence.
3. First live post-flip 429: verify DetectRateLimit + no-attempt-burn
   requeue end-to-end in production (the mechanism is still unproven).
4. Consider journaling fast-refuse events (in-process `rateLimitUntil`
   gate refusals are invisible to the journal — the retro's requeue count
   would be blind to them even if 429s had occurred).
5. Standardize report-filename clocking (filename vs git clock drift,
   3 hits this window).
6. Close-out-report scope rule: a no-op verification lap emits ONE commit
   with the footer, not a full report + index row.
7. Consider marking the 02-30 report title "(interim, day +4)" for
   consistency with the TODO note.
8. When the first 429 arrives, extend the retro method with a
   requeue-path assertion (not just absence-of-429 counting).

Standing backlogs this lap re-confirms but did NOT work (avoid duplicate
rows — see TODO_LIST sections): docs-health harvest rows below row 73;
templ-components consumer sweep (ADR-0018 waves); go-cqrs-lite `queue/`
module parity watch; synthetic.new upstream issue for the misleading
"does not support reasoning effort" error; crush #3146 session-trigger
automation. Do not re-harvest these from this report.

Deliberately NOT suggested (would be scope creep): any code change to the
executor/queue from a docs task; new smokes for unobserved failure modes;
baseline churn; touching .crushrc/.tq-verify.

That's 8 real items — padding to 50 would be the same disease as lap 2's
close-out: volume instead of value.

## g) QUESTIONS (cannot self-answer)

1. **Mint gate:** should the harvester refuse to mint a task whose ID
   already has a footer-bearing commit AND a checked TODO row (hard gate),
   or only warn (soft gate, since close-outs themselves carry the footer
   and would self-match)? Owner call on false-positive tolerance.
2. **2026-09-18 final lap:** when the 7-day re-check runs, should it
   SUPERSEDE the 02-30 report in place (annotate + rename to final) or
   archive 02-30 and write a fresh final report? Citation repointing cost
   differs (one backtick citation vs several).
3. **Fast-refuse observability:** is the in-process `rateLimitUntil` gate
   worth a journal fact (a new `agent.ratelimit_refused` fact type), or is
   process-local logging sufficient given the gate is best-effort and
   per-executor-instance by design?

---

_3rd lap, DONE-on-arrival, zero deliverable changes. Report generated by
the dispatched agent per the a)-g) close-out contract; the retro numbers
itself rest on lap 1's a8a84e0 (journal read via `tq facts -json` at HEAD,
method in docs/status/2026-09-15_02-30_post-deploy-rate-limit-retro.md)._
