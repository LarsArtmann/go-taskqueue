# Task 000001a0b1cfab9c997916b38ba1c5a80df1 — Session Ergonomics: Third Verify Window (Repeat Dispatch #2)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-18_02-07_task-000001a0b1cfab9c997916b38ba1c5a80df1.md`, which owns any open residue.

**Date:** 2026-09-18 02:14 CEST
**Window type:** Verification only — zero source edits. This ID was SHIPPED in the 02:07 window (work commit `b004825`, TODO tick `26ad28c`) and already re-verified once in the 02:10 window.
**Work item:** TODO_LIST.md row 116 — "Session ergonomics: git ≥ 2.15 guard with an actionable message; zero-commit close prints the footer-convention hint (begin already does) (03-28 §f12/f13)"
**Report format:** Markdown per explicit harness instruction (status-report skill names HTML canonical; the user's explicit format wins — one-off override, not propagated to the skill).

## What this window did

1. Located the work item and the implementing code fresh (grep, no memory reliance): guard in `internal/executor/gitscan.go:60-84` (`checkGitVersion` → actionable "trailer attribution needs git >= 2.15 — upgrade git" message, fails open on unparseable `--version`); zero-commit close hint at `cmd/tq/session.go:317-323` (three-line footer-convention hint; begin side at session.go:219); dry-run variant at session.go:284.
2. TODO_LIST.md row 116 read verbatim `[x]`; prior ticks: commits `26ad28c` (check-off), `869a8a6` (verify-window report), `77f6478` (close-out).
3. Ran gates fresh: root `go build ./...` green; `internal/executor` `go test -run 'GitScan|Version'` green; `scripts/test-cmd-tq.sh` (devmod shim gate) green in 9.0s.
4. Committed nothing for the work itself (nothing to commit) — correct for a verify window.

## a) FULLY DONE

- **The work item** — git ≥ 2.15 guard + zero-commit close hint: shipped, tested, ticked, closed out in the 02:07 window; re-verified this window with fresh gate runs and file:line citations.
- **This window's contract items 1-4**: smallest correct response (verify → cite → stop), no scope creep, TODO row state respected, no push.

## b) PARTIALLY DONE

- **Turn-1 ritual, partially honored again.** I did `git log`/`git status` implicitly and verified code, but I did **not** run the prior-report grep (`rg -l <task-id> docs/status/`) before starting, and did not open CONTRIBUTING.md until writing this report. Both are codified turn-1 additions (AGENTS.md, 2026-09-15) with N documented recurrences. The grep would have ended this window in one step instead of five tool calls.
- Nothing on the task itself — it is fully done.

## c) NOT STARTED

- Nothing belonging to this task. §f items below are backlog candidates for the queue to pace, explicitly not this run's work.

## d) TOTALLY FUCKED UP

- **Repeat-dispatch burn, now 3 windows on THIS ID** (02:07 ship + 02:10 verify + this one) and 3 more on sibling ID …1c1f0 the same night. The queue-side done-row dedup ruling requested in at least five prior reports remains unmade. Each re-dispatch costs a full session + a report + a commit.
- **My own misses this window** (honest ledger): (1) skipped the turn-1 prior-report grep — the exact miss the ritual exists to prevent; (2) burned two gate round trips on the KNOWN cmd/tq module ambiguity (`go test ./cmd/tq/...` from root and bare `go test` inside cmd/tq both fail setup by design; AGENTS.md documents `scripts/test-cmd-tq.sh` as the only working invocation — I read the doc and still tried the dead paths first).
- Nothing in the repo is broken; tree state untouched by this window apart from this report.

## e) WHAT WE SHOULD IMPROVE

1. **Queue-side done-row dedup** — the single highest-leverage fix in this cluster: a DONE row's re-dispatch should resolve at claim time to "verify → cite → stop" (or be skipped outright), not mint a fresh agent window. Six windows of evidence across two IDs in one night.
2. **Pinned one-command verify for DONE rows** — a script that greps the tick, greps the work commit by subject/footer, and runs the module gate would make verify windows sub-minute and uniform.
3. **Turn-1 ritual compliance** — the prior-report grep must be the FIRST command, not an afterthought; my window is the Nth recurrence. Consider a checklist echo at session start.
4. **Gate-invocation memory** — cmd/tq has exactly one working test path (the shim script); trying known-dead invocations wastes round trips. This belongs in every session's first-minute knowledge (it IS in AGENTS.md; the failure was execution, not documentation).
5. **Report-format policy** — skill says HTML, harness says `.md`; every session resolves it ad hoc. One line in AGENTS.md would end the divergence.

## f) Up to 50 things we should get done next (backlog candidates — the queue decides; NOT this task's work)

1. Queue-side done-row dedup: claim-time check that the TODO row is already `[x]` → resolve to verify-only or skip (owner ruling needed, §g1).
2. `scripts/verify-done-row.sh <row-pattern>`: tick grep + work-commit lookup + module gate in one command, for verify windows.
3. Pin the shipped git-version refusal with a fake-old-git stub test (2.14 `git --version` → assert the actionable error text; carries from the 02:07 report).
4. Decide fail-open vs fail-closed for read-only trailer consumers on unparseable `git --version` (carried §g from 02:07).
5. CHANGELOG row for the session-ergonomics feature (guard + hint) if not already appended this release cycle.
6. Decide dry-run hint symmetry: zero-commit close prints the convention hint; zero-commit DRY-RUN prints only "would enqueue nothing" — align the two (carried §g from 02:07).
7. Memoize the `git --version` probe per process so per-close cost is one exec (carried §g from 02:07).
8. Re-dispatch semantics ruling: is re-claiming a DONE row a queue bug or expected pacing? (carried, now 3× evidence on this ID alone).
9. Verify-window report policy: should a verify-only window write a full report + commit, or a one-line appendix on the prior report? (carried from 01:50).
10. No-commit verdict path for verify windows so reports don't each pay the daemon-race risk (carried from 02:10).
11. Session-ritual script: one command that runs `git log -5` + `git status` + `git stash list` + prior-report grep + CONTRIBUTING check, so compliance is mechanical.
12. `wasClosed` payload-schema hedge for the session-close one-close-per-session model (carried from 01:53).
13. Permanent-model ruling for the first-close-wins session/repo model (carried §g from 01:47).
14. Sanction (or reject) one-session-per-repo multi-repo close redesign sketch (carried §g from 01:47).
15. Docs-only gate policy: is ci-local required for a docs-only diff? Define the minimal gate (carried §g from 01:47).
16. dlqfix-autopsy prune-semantics ruling (carried from 01:29).
17. Mint-time done-check: sweepers skip minting follow-ups whose source row is already `[x]` (generalization of item 1 into the sweeper layer).
18. Done-row dedup telemetry: count re-dispatches of `[x]` rows per week so the spend is visible in `tq stats`.
19. Cheap `tq verdict`-only path for verify windows: record the verify evidence as a verdict instead of a committed report.
20. Cross-reference TODO row IDs in work-item payloads so the queue can self-audit done-row dispatches.
21. Add the close-hint + guard to `tq session --help` text so operators discover the footer convention without a failed close.
22. Batch verify windows: if a dispatch lands on a DONE row, sweep sibling `[x]` rows in the same section and verify them all in one window.
23. Report-index size budget: docs/status/README.md index rows are growing verbose; consider a cap or an archived-summary convention.
24. Deduplicate the three reports on this ID into a single canonical close-out with append-only verify annotations (docs-health ANNOTATE pass).
25. Template a minimal "verify window" report skeleton (sections a-g, three lines each) to cut drafting cost.
26. Pin `checkGitVersion` output text in a golden test so rewordings fail loudly (agents grep for the message).
27. Audit other exec sites that assume modern git for similar silent-incompatibility classes (e.g. `git diff-tree`, `git cat-file -e` usage in depbump/review paths).
28. Consider `git version` failure telemetry: a WARN when the floor check fails open, so exotic environments surface instead of silently best-effort scanning.
29. Write the pool-unit `Environment=GOEXPERIMENT=jsonv2` fix ruling into a tracked backlog row if the NixOS module change is still owner-pending (AGENTS.md says owner fix required).
30. Carry-over: crush `--reasoning-effort` misleading-error upstream issue (AGENTS.md flags it as an upstream-issue candidate — needs verify-before-filing pass).
31. Carry-over: tq ask prompt-teaching decision — agents still won't discover `tq ask` without an owner ask-policy ruling (AGENTS.md §g, 21-04 report).
32. Carry-over: session-close trigger automation (crush #3146 SessionEnd hooks) — recheck upstream status.
33. Add a smoke that exercises the zero-commit close END-TO-END through `tq session close` against a scratch repo (02:07 did it live/ad-hoc; make it durable).
34. Guard the guard: `check-script-syntax`-style gate that every `%(trailers)` log invocation goes through GitLogScanner (no ad-hoc `git log` in cmd/).
35. Windows CI coverage for the version-parse path (parseGitVersion is pure, but the exec path is POSIX-flavored; assert the windows behavior explicitly).
36. Document the cmd/tq devmod shim gate invocation in CONTRIBUTING.md — external contributors will hit the same ambiguous-import wall I did.
37. Reduce report-writing cargo: sections f/g at 50/3 items invite padding; consider skill-level guidance for verify windows (10 items max unless material).
38. Dedup burn ledger: track "windows per TODO row" as a metric in the next brutal-self-review.
39. Ensure the footer-convention hint text and `checkGitVersion` message are covered by the help-text smoke (parenthesized-identifier artifact class).
40. Consider surfacing the git version actually scanned into `session.closed` facts (forensics value when attribution silently finds zero commits).
41. Zero-attribution forensics: `tq show` could note "close found 0 commits; git floor check passed/failed" for diagnosis.
42. Add the session-begin side a git-floor preflight so a sub-2.15 git fails at BEGIN (before work), not only at close — question for the owner (carried from 02:10 §g).
43. Align `shortSHA` truncation with the review extra's full SHAs (cosmetic consistency check).
44. Sweep for other "would enqueue nothing" messages lacking actionable next steps (user-feedback quality pass over cmd/tq output).
45. Consider a `tq session status` subcommand: is a session id attributed, closed, reviewed? (operator ergonomics adjacent to this row).
46. Backfill unit test for the begin-side footer hint printing (only close-side paths are exercised today).
47. Move the `Trailer` constant usage audit: confirm no site hardcodes "Crush-Session" as a literal (rename-leak class guard).
48. Estimate and document the cost delta between a verify window and a full work window to justify the dedup investment in the ruling doc.
49. Retire or automate the "amend maneuver" doc hole: a daemon-vs-hook reconciliation script for unindexed status reports.
50. ROADMAP fuel: a first-class "verify mode" for the queue (`tq claim --verify-only`) so dispatches can carry their own disposition instead of the agent guessing.

## g) Up to 3 questions I can NOT figure out myself

1. **Done-row re-dispatch: bug or intended?** Three windows (including this one) have now billed against this completed ID tonight, plus three on sibling …1c1f0. Is this a queue pacing bug to fix in claim logic, or intended redundancy I should keep answering with verify-only windows? This decides items §f1/§f8 and whether the fix is queue-side or prompt-side.
2. **Verify-window reporting policy:** should a verify-only window commit a full status report (current practice, at daemon-race + index cost), or append a short verify annotation to the existing close-out / record a `tq verdict`? I cannot rule on the evidence-vs-spend tradeoff for the queue's own accounting.
3. **Begin-side git guard:** the ≥ 2.15 floor currently guards the close-side trailer scan only. Should `tq session begin` also preflight the git version (fail before work, not after), or is a close-side-only refusal the intended minimal surface?
