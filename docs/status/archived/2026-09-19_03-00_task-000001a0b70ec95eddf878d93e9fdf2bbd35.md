# Close-out self-review — check-gosec.sh --self-test, third window (verify; trailer anomaly escalated)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-19_02-38_task-000001a0b70ec95eddf878d93e9fdf2bbd35.md`, which owns any open residue.

**Task:** 000001a0b70ec95eddf878d93e9fdf2bbd35 (TODO_LIST row 126 —
check-gosec.sh `--self-test`: pin the three version branches plus the
Files:0 parse with canned stub binaries)
**Window:** 2026-09-19, ~02:45–03:00 CEST
**Nature:** THIRD window on this ID, second re-dispatch — arrived after the
work was at HEAD (`fc6e03d`, footer exactly once in the raw message, verified
via `git show` + `cat -A`). Work commit + two prior reports read in full:
`docs/status/2026-09-19_02-38_…` and `docs/status/2026-09-19_02-45_…`. This
window's contract response was verify → cite → stop, zero code edits — but
the close-out investigation produced the biggest delta of the whole arc
(§a DELTA, §d, §e, §g1).

---

## a) FULLY DONE

- **Re-dispatch caught at turn 1** (batch one, before any action): whole-repo
  `git log --oneline -5` + `git status` + `git stash list` (clean at
  `1c8daa1`, 22 ahead of origin), same-ID grep over docs/ + TODO_LIST.md →
  row 126 already `[x]` with the DONE note, two prior reports found.
  Fastest correct response executed: verify, cite, stop. No duplicate work.
- **Deliverable re-verified LIVE at HEAD**: `./scripts/check-gosec.sh
  --self-test` → 18/18 `ok:` assertions + final summary, exit 0 captured
  bare. All five branches green on this run: stamped-ok full gate, stamped-
  mismatch fail-fast (zero scans), unstamped WARN + proceed, Files:0
  silent-skip FAIL, Issues>0 FAIL.
- **The two gates 02-45 skipped on the immutability argument were re-run
  fresh this window, closing that §b gap**: `check-script-syntax.sh` → bash
  -n 52 scripts + shellcheck 0 findings (severity ≥ warning);
  `check-guard-wiring.sh` → ok. ci-local wiring re-cited from source:
  self-test step at scripts/ci-local.sh:110-111 before the real gate at
  :113-114.
- **Tree hygiene**: `git status` clean after all verification — the stub
  battery runs in `mktemp -d` outside the gated trees, nothing leaked.
- **Queue↔git cross-reference (raw message)**: `fc6e03d` carries
  `Task-Queue-ID: 000001a0b70ec95eddf878d93e9fdf2bbd35` exactly once —
  BUT see §a DELTA: the commit is INVISIBLE to git's trailer machinery,
  which is what the queue's own scanner uses.
- **Daemon-fold repaired per playbook (post-report amend)**: the daemon
  folded the three close-out files into footer-less `fddad84` minutes
  before the footer commit landed; scripted squash (`pick fddad84` /
  `fixup 1b56e1b` via script-file GIT_SEQUENCE_EDITOR, then
  `git commit --amend -F`) produced ONE footer commit. Fork record
  (policy a-d): fork point `1c8daa1`; dead side `fddad84` + `1b56e1b`
  superseded by the amended commit carrying this file; no remote twins
  (unpushed throughout); `git tag --no-merged master` empty; tree hash
  `42ecc71a` byte-identical across the rewrite. The repaired commit
  ends with the footer as the LAST line — `%(trailers)` now sees it,
  the live proof of the DELTA above.
- **DELTA — the trailer anomaly escalated into a live derivation blind spot.**
  Evidence chain, every step captured this window:
  1. `git log -1 --format='%(trailers:key=Task-Queue-ID,valueonly)' fc6e03d`
     returns EMPTY, while the raw message clearly carries the footer.
  2. Byte-level cause (`cat -A`): the message ends
     `Task-Queue-ID: …$` / `$` / `Assisted-by: Crush:glm-5.3-flash$` — the
     blank line splits the footer into the second-to-last paragraph, and git
     parses ONLY the final paragraph as the trailer block
     (`%(trailers)` on this commit yields just the Assisted-by line).
  3. The queue's own attribution is trailer-based:
     internal/executor/gitscan.go:143-146 runs
     `git log --format=%H%x1f%s%x1f%(trailers:key=<key>,valueonly)` —
     exactly the machinery that misses this shape.
  4. Repo-wide measurement (last 400 commits, both counts over the same
     range): 180 commits carry a well-formed `^Task-Queue-ID: <16+ hex>$`
     line; only 74 are trailer-visible. **106 footer commits (59%) attribute
     to ZERO for the queue's derived-outcomes channel**
     (`AgentResult.Commits`, shipped 2026-09-14).
  5. Shape rule confirmed by byte comparison: footer as the message's LAST
     line (e.g. `ee4711a8`) parses fine; anything after a blank line
     following the footer (the harness commit template's Assisted-by tail,
     e.g. `fc6e03d`) kills attribution.
  6. Why no test caught it: internal/executor/gitscan_test.go:16-19 `rec()`
     fixtures simulate git's OUTPUT (SHA ␟ subject ␟ trailers), so the suite
     pins the PARSER while git-side trailer DETECTION is untested — the
     suite stays green while production under-attributes. Classic
     mock-blind-spot; the pin belongs at the real-git level.
  - Two backlog rows minted (fix decision + end-to-end pin); owner question
    in §g1. Deliberately NOT fixed this run — separate work item, and the
    fix direction (scanner fallback vs commit-shape rule) is an owner call.

## b) PARTIALLY DONE

- **Real gosec gate (16 scans) not re-run this window**: the script's bytes
  are unchanged since 02-38's green full-gate run (only docs commits landed
  after `fc6e03d`), so inputs are identical and the stub battery exercised
  the parse paths live. Reasoned skip, owned — but stated so it is not a
  silent skip.
- **CONTRIBUTING.md read in batch two, not batch one**: existence-checked in
  the first batch, content read alongside the footer check. The ritual says
  "check for AND read" at turn 1 — this is the N+12 recurrence in its softer
  form (N+11 was 02-45 §d2).
- **The 106/180 measurement is scoped to the last 400 commits**, not whole
  history; the ratio + the byte-confirmed shape rule is the finding, an
  exact all-history census was not worth the runtime for a backlog row.

## c) NOT STARTED (tracked, open — re-verified open this pass)

The gosec cluster this task grew out of (all re-read in TODO_LIST.md):

- Row 132 — self-test absolute-`$0` + foreign-CWD assertion (first-hand
  confirmed by the 02-45 window).
- Row 130 — end-of-run "N scans ok, M failed" summary line.
- Row 129 — hard-fail when the module loop enumerates 0 targets.
- Row 131 — derive ci.yml's GOSEC_VERSION/excludes from the script.
- Row 127 — converge ci.yml's per-module gosec loop onto the script (owner).
- Row 124 — cmd/tq gosec coverage (owner).
- Row 128 — AGENTS.md gosec note → canonical entry pointer.
- Row 40 — permanent gate-vs-advisory ruling.
- Owner action — install a stamped v2.29.0 gosec binary (kills the per-run
  WARN).
- NEW this window, by design not started: the attribution fix decision and
  the end-to-end gitscan pin (§a DELTA).

## d) TOTALLY FUCKED UP

Nothing destructive (zero edits, no reverts, no duplicate commits), but the
close-out turn caught me in four process-grade failures, two of them
repeat-offenses with named prior confessions:

1. **I noticed the anomaly and moved on.** The empty `%(trailers)` output
   was IN my batch-two capture, next to the `git show` that contradicted it.
   I filed the footer as "verified" and proceeded; only close-out prep
   forced the investigation. A verify window's deltas must come from the
   verify pass — the 02-45 report confessed this exact phase miss (its §d3
   "pencil-rule-late recurrence") and I repeated it one window later.
2. **I nearly shipped a false verification-method claim.** Draft wording
   echoed 02-45's "footer verified exactly once via trailers" — which is
   FALSE as stated (the trailer parse misses this commit; 02-45's
   verification must have actually been the raw-message grep). My own
   captured output caught it at report time. Two lessons: prior reports'
   method claims are claims, not evidence; and a one-word citation error
   here would have permanently poisoned the record for the finding that
   matters most.
3. **My first evidence pipeline lied positively.** The first trailer-scan
   awk used a display format with spaces around the field separators, so
   empty trailer fields became `" "` and ALL 400 commits "counted" as
   attributed — the exact OPPOSITE of the truth. Caught only because the
   sample lines looked visually empty. A count that surprising must be
   re-derived with a second method before it is believed (it was: clean
   format, 74).
4. **The second pipeline lied negatively.** The raw-footer awk (percent-B
   with record separators) returned 0 raw footers — impossible, `fc6e03d`
   visibly carries one. The correct number (180) came from the dumbest
   possible tool (`git log --format=%B | grep -c`). Three attempts for two
   numbers; the grep-first discipline would have been one.
5. **Lost the daemon race again — the exact 02-38 §d2 mode.** The report +
   index sat uncommitted through the gate runs and the daemon folded all
   three files into footer-less `fddad84`; the atomic status→add→commit
   chain was moot for the same reason as 02-38 (dirty window, not commit
   command). The repair worked (§a fork record), but two micro-failures
   inside it cost extra round trips: the sequence-editor script's
   `#!/bin/bash` shebang (no /bin/bash on NixOS) and a `rebase | tail`
   pipeline that masked the first rebase failure — the exact
   pipeline-masking class the memory file names, repeated within minutes.

No lies detected on self-audit: every §a claim re-checks against captured
output (18/18 exit=0, trailer-empty capture, cat -A bytes, 180/74/106 counts
with method stated, gitscan.go:145, gitscan_test.go:16-19).

## e) WHAT WE SHOULD IMPROVE

1. **Fix the attribution blind spot** (minted): either the scanner gains a
   raw-message fallback (parse `%B` lines when the trailer field is empty —
   queue becomes robust to ANY message shape), or a shape rule lands
   (footer must be the message's final line; harness template + agent
   prompts + the commit-msg hook enforce it), or both. 59% silent
   under-attribution of the derived-outcomes channel is the worst
   signal-to-noise defect found in this arc. §g1 asks the direction.
2. **End-to-end test at the real-git level** (minted): commit a
   harness-template-shaped message into a temp repo and run GitLogScanner
   against it — red today, and it stays green after either fix. The `rec()`
   fixtures cannot see this failure class by construction.
3. **Anomalies-in-passing get investigated in the same pass** — the empty
   trailer output was a free delta sitting in a capture; the verify pass is
   where deltas are born, not the report turn.
4. **Dumbest-tool-first for evidence counts**: `grep -c` before awk record
   games; re-derive any surprising count with an independent method before
   citing it (this window's counts disagreeing with themselves is §d3/§d4).
5. **Correct the record when a sweep touches it**: 02-45's "verified via
   trailers" line is wrong-as-stated; docs-health should fix the sentence
   when it next touches that report (it is an archived-in-place report, so
   annotation, not rewrite — same treatment as prior stale-citation fixes).
6. **CONTRIBUTING content-read joins the existence-check batch** — the
   mechanical session-start script (row 136) keeps gaining justification.

## f) What we should get done next (prioritized; refuses 50-padding per the
00-32/01-01/02-09/02-18/02-38/02-45 precedent — 18 real items, no filler)

1. NEW — attribution fix (§e1): scanner `%B` fallback and/or footer-last
   shape rule; owner direction via §g1. Highest impact-per-line in the arc.
2. NEW — end-to-end gitscan pin (§e2): real temp-repo commit with the
   harness shape; red today.
3. Row 132 — self-test absolute-`$0` + foreign-CWD assertion (three lines +
   one assertion; stock-bash rc=127 repro is the acceptance test).
4. Row 136 — session-start ritual script (kills a ≥12-recurrence miss
   class; §b/§d1/§e6 all feed it).
5. Row 130 — gosec scan-count summary line.
6. Row 129 — zero-target module-loop guard.
7. Row 131 — single-source the pin (ci.yml derives GOSEC_VERSION/excludes).
8. Row 127 — CI convergence onto the script (needs §g3).
9. Row 124 — cmd/tq gosec coverage (needs the owner ruling).
10. Row 128 — AGENTS.md gosec note → canonical entry pointer.
11. Row 40 — permanent gate-vs-advisory ruling.
12. Owner — install a stamped v2.29.0 gosec binary (after that the dev-stub
    branch stays the only WARN exerciser; keep it).
13. After 8: decide whether CI runs `--self-test` too (today ci-local-only).
14. Row 250 — CONTRIBUTING "individual gates" sweep (+ the stale
    ~400-finding number).
15. Micro — self-test scan-COUNT assertion (`== (root)` + one per module;
    pairs with row 129).
16. Micro — stub-pin the ANSI-strip path (loud failure today, priority-low).
17. Docs-health — stale "17/17 scans" citations in old reports (02-38 §f14)
    PLUS the 02-45 trailer-method sentence (§e5).
18. Process — verify-window report policy ruling (carried five windows
    deep; §g3 of 02-38/02-45 asked it, the dispatch contract keeps
    demanding full reports, so the ruling should be made once).

## g) Questions I cannot figure out myself (three; the first is new and
gates the arc's biggest open defect)

1. **Attribution fix direction**: the queue's GitLogScanner sees only 41%
   of footer commits (74/180, last 400) because the harness commit
   template's trailing `Assisted-by:` line demotes `Task-Queue-ID:` out of
   git's trailer block. Fix scanner-side (raw `%B` line fallback when the
   trailer field is empty — robust to any shape, no upstream dependency),
   shape-side (rule: footer is the final line; harness/agents/hook change),
   or both? Scanner-side is my recommendation (self-contained, no
   behavior change for correct commits); but it is your call because the
   shape-side rule touches the harness contract this queue itself defines.
2. **Unstamped-binary provenance (carried three windows, now four)**:
   gosec cannot be `go install`ed from agent sandboxes, so every real gate
   run warns about binary provenance until you install a stamped v2.29.0
   binary or grant one-shot install rights. Is the indefinite WARN
   acceptable, and if not, which fix?
3. **CI independence vs dedup (rows 127/131, carried)**: converge ci.yml's
   gosec job onto check-gosec.sh (one tool, one pin, self-test coverage) or
   keep the hand-rolled loop as an independent second layer? The
   counter-risk — a broken script would silence both layers — is real and
   named in row 127.
