# Task 000001a0a27f — Orphaned-guard audit (NO-OP DUPLICATE CLOSE-OUT)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-15_02-41_task-000001a0a27f31f2369a5ff74a1f8488a08c.md`, which owns any open residue.

**Date:** 2026-09-15 02:46 · **Verdict:** NO-OP — work already shipped; nothing to do or fix.

## Context

Third+ paid dispatch of this work item on the same queue ID. The item was fully
delivered by the 02-41 session (commit a02f72f / 28ffe7e) and re-verified as a
no-op by the 02-44 session. This dispatch verified again:

- `scripts/check-guard-wiring.sh` exists and passes: "guard wiring ok: every
  check-*/smoke script is referenced"
- Wired into `scripts/ci-local.sh:214` (step "guard-wiring check")
- `TODO_LIST.md:77` already `[x] — DONE 2026-09-15` with full evidence
- Working tree clean at `9d21abf`; nothing to commit for the work itself

## a) FULLY DONE (pre-existing, re-verified this session)

1. ci-local meta-check `check-guard-wiring.sh` — every `scripts/check-*.sh`
   and smoke script must be referenced by ci-local/ci.yml/flake or be deleted.
2. Dead duplicate `check-lint-baseline.sh` deleted (superseded by
   `lint-baseline.sh --check`).
3. Orphaned smokes `multi-repo`, `papdashboard-e2e`, `ratelimit-e2e` wired
   into ci-local as gating steps, verified passing.
4. TODO_LIST row 77 checked off with citations; gate runs green.

## b) PARTIALLY DONE

Nothing attributable to this task. Carried-over scope notes from the 02-41
report stand: the gate is ci-local-only (not a separate CI job) and uses
substring matching for reference detection — both deliberate smallness.

## c) NOT STARTED

Nothing. Every element of the work item is shipped.

## d) TOTALLY FUCKED UP (mint-side)

- The queue minted (at least) three paid dispatches against one work item
  that was already done before enqueue. The gate is green and the row is
  `[x]`; the mint side did not check. Cost: this report + two before it.

## e) WHAT WE SHOULD IMPROVE

1. Mint-side `[x]`-check: harvest/claim should refuse to mint an item whose
   TODO_LIST row is already checked (this is now the 4th+ dispatch — the
   02-44 report already asked for this).
2. Dispatch dedup by Task-Queue-ID: a second claim on the same ID with a
   shipped-artifact trail should downgrade to a cheap verify, not a re-run.
3. The no-op close-out ceremony is itself the cost; a one-line
   `tq verdict '{"verdict":"noop"}'`-style outcome would be cheaper.

## f) NEXT (capped at 5 — this is a duplicate close-out, not a planning pass)

1. Fix the mint-side `[x]`-check (repeat of e1 — still not done).
2. Consider promoting `check-guard-wiring.sh` into ci.yml as a standalone
   job (currently only runs inside ci-local on some paths).
3. Negative-fixture test for check-guard-wiring.sh (an unwired script must
   fail the gate) — currently only the positive path is exercised in CI.
4. Sweep the backlog for other items minted against already-shipped rows.
5. Consider a `TQ_RESULT: noop` convention so duplicate dispatches cost one
   line instead of a report + commit.

## g) QUESTIONS (cannot self-answer)

1. Why did the queue re-mint this ID after two prior close-outs — is the
   dispatcher ignoring recorded TQ_RESULT/verdicts, or is this a manual requeue?
2. Should duplicate dispatches of a shipped ID be auto-cancelled at claim
   time, or is a human expected to intervene in the DLQ?
3. Is ci-local runtime budget still a constraint for adding the negative
   fixture test (item f3), or is the gate time no longer watched?

— TQ_RESULT file channel not used (no code change); legacy stdout line only.
