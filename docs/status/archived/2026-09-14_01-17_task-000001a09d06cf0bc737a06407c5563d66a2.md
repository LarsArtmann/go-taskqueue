# Status report — task 000001a09d06cf0bc737a06407c5563d66a2 (status-window closeout report paths, re-dispatch)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-14_01-12_task-000001a09d06cf0bc737a06407c5563d66a2.md`, which owns any open residue.

> **ANNOTATED 14 Sep 2026 (docs-health strikethrough pass)** — every numbered item below is resolved inline (`done`/`duplicate` markers) or left untouched (= still open). Unarchived on purpose: open items remain. Evidence verified against HEAD.

2026-09-14 01:17. Dispatched to do the TODO_LIST item "Status-task window payload: include the window's closeout/status report paths". Found the work already shipped.

## a) Fully done

- Work item verified COMPLETE before touching anything. Commit 4a6f504 ("feat: status window payload carries closeout report paths") implements it:
  - `StatusCompletion.Report` field (repo-relative closeout report path), resolved by the sweeper via `closeoutReportPath` glob `docs/status/*_task-<id>.md`, newest match, best-effort empty (internal/status/sweep.go:285).
  - Status prompt lists "(closeout report: <path>)" per entry and instructs the reporter to read those files first, never re-derive from task ids (internal/executor/status.go:251,258).
- TODO_LIST.md line 138 already marked `[x]` with DONE evidence — closed the loop before I started.
- Verification gate run this dispatch: root-module `go build ./...` green; `internal/status` tests pass; `internal/executor` sub-module tests pass (`GOWORK=off`, `-count=1`, GOEXPERIMENT=jsonv2).
- No changes made this dispatch, nothing to commit for the work itself. No repo breakage.

## b) Partially done

- Nothing — the item was binary and it was done.

## c) Not started

- Nothing within this item's scope.

## d) Totally fucked up

- Nothing broken. The one honest miss this dispatch: I initially ran `go test ./internal/executor/` from the root module and got the expected "does not contain package" failure (AGENTS.md documents that `./...` never descends into nested modules). I caught it immediately and re-ran the per-module gate correctly — but I should have applied the documented multi-module rule on the first try.
- Known hole I did NOT close: the broader per-module test loop (all internal/* modules) and ci-local were not run — justified as a no-op verification dispatch, but a skeptic could argue a re-dispatch should re-run the full gate, not just the touched packages.

## e) What we should improve

1. ~~**Re-dispatch dedup**: this task was re-dispatched after already being DONE with evidence in TODO_LIST. The queue/sweeper minted a duplicate work item for work already shipped. A cheap guard: harvest/mint path could check whether a commit already cites the item's Task-Queue-ID (git trailers exist for exactly this) and skip or downgrade the re-dispatch.~~ resolved — routed rows 148/157 (verified at HEAD)
2. ~~**TQ_RESULT on no-op**: my work dispatch reported a no-op TQ_RESULT with the original commit SHA. If the queue treats that as success, fine; if it expects a fresh commit, re-dispatch loops like this one recur. The contract should say explicitly what a no-op re-dispatch must report.~~ done — AGENTS.md:193 no-op derivation (verified at HEAD)
3. ~~**Session-start ritual helped**: reading AGENTS.md + git log first is what made this a 3-command dispatch instead of a duplicated implementation. Keep it.~~ done — narrative (point-in-time process note, no artifact owed)
4. **Per-module gate reflex**: the multi-module test rule is documented; agents (including me) still trip on it once. Consider a tiny `scripts/test-module.sh <dir>` wrapper so the correct invocation is one command, not a documented incantation.

## f) Next up to 50 things (only items noticed in passing on THIS task — not a researched backlog)

1. ~~Add re-dispatch dedup: skip minting a work item whose Task-Queue-ID already appears in a git trailer on master.~~ resolved — routed rows 157/148 (verified at HEAD)
2. ~~Document the no-op re-dispatch TQ_RESULT contract in AGENTS.md payload section.~~ done — AGENTS.md:193 (verified at HEAD)
3. Add `scripts/test-module.sh <dir>` for one-command per-module gates.
4. ~~Confirm 4a6f504's closeout report exists and is indexed (I saw 4a6f504/3de902d/d593908 in log; did not verify the report file's index row).~~ done — indexed README row 50 (verified at HEAD)
5. ~~Run the full per-module test loop as a periodic health check — I only ran executor + status.~~ done — ci-local module loop; TODO 143 (verified at HEAD)
6. Verify `closeoutReportPath` glob behavior when two reports match the same task id (tie-break is "newest" — pinned by a test? not checked).
7. ~~Verify the empty-Report path renders cleanly in the prompt (no dangling "(closeout report: )" when best-effort resolution fails) — not checked this dispatch.~~ done — status.go:250 Report guard (verified at HEAD)
8. ~~Sweep for other prompt contracts that re-derive context from task ids and could carry direct paths the same way (the general pattern this item fixed once).~~ done — prompt contracts reworked 12-52 (verified at HEAD)
9. ~~Consider a facade-parity/lint check that `StatusCompletion` is aliased if it crosses a facaded package boundary (it lives in internal/executor — parity gate should already cover it; worth a spot check).~~ done — check-facade-parity ci-local (verified at HEAD)

(Items beyond 9 would require research beyond this task; not fabricated to fill a quota.)

## g) Questions I cannot answer myself

1. ~~Should a no-op re-dispatch report the ORIGINAL commit SHA in TQ_RESULT (as I did), or omit `commit_sha` and let the queue link via the trailer? The gate may accept either; owner preference unknown.~~ done — AGENTS.md:193 (verified at HEAD)
2. ~~Is re-dispatch of DONE items coming from the sweeper minting against a stale TODO_LIST snapshot, or from a manual re-queue? Determines where the dedup guard belongs (harvest vs queue ops).~~ resolved — routed TODO 93 (verified at HEAD)
3. Should re-dispatch verification be the FULL per-module gate, or is touched-package verification acceptable when the tree shows zero new changes since the original DONE?
