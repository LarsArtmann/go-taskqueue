# Status report: status-window closeout report paths — RE-DISPATCH verification close-out, n=2 (task 000001a09d06cf0bc737a06407c5563d66a2)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-14_01-12_task-000001a09d06cf0bc737a06407c5563d66a2.md`, which owns any open residue.

> **ANNOTATED 14 Sep 2026 (docs-health strikethrough pass)** — every numbered item below is resolved inline (`done`/`duplicate` markers) or left untouched (= still open). Unarchived on purpose: open items remain. Evidence verified against HEAD.

**Date:** 2026-09-14 01:14 CEST
**Scope:** Same TODO item as the 01-12 close-out ("Status-task window payload: include the window's closeout/status report paths", 05-30 report f25). This is a re-dispatch of an already-done task ID. No unrelated research.

## a) FULLY DONE

- The work item is DONE-on-arrival, first-hand re-verified this turn:
  - Work commit **4a6f504** re-read (`git show --stat`): exactly the five expected files — `internal/executor/status.go` (+11, `StatusCompletion.Report` + prompt listing), `internal/status/sweep.go` (+28, `closeoutReportPath` glob/resolver wired into `maybeMint`), both test files (+61), `TODO_LIST.md` closure. Message carries exactly one `Task-Queue-ID:` footer matching this dispatch ID.
  - **TODO_LIST.md** row 138 confirmed `[x]` with a DONE note citing the mechanism and the commit.
  - Prior close-out `2026-09-14_01-12_task-000001a09d06cf0bc737a06407c5563d66a2.md` exists and is indexed in `docs/status/README.md` (commit 3de902d).
  - Independent gate re-run at HEAD (fresh, this turn, not quoted from the prior report): `internal/executor` and `internal/status` — `GOWORK=off go build && go vet && go test -race -count=1` → **ok / ok** (3.29s / 1.20s).
- This turn's deliverable: this report, indexed and committed with the dispatch footer. Zero code changes — correctly, because the deliverable already exists and is green.

## b) PARTIALLY DONE

- Nothing in the assigned item. Carried observations from the 01-12 report remain valid follow-ups (webui does not render `StatusCompletion.Report` as links; `tq session close` mints bypass the sweeper's path resolution; `closeoutReportPath` depends on the `_task-<id>.md` filename convention) — all deliberately unexpanded, see (f).

## c) NOT STARTED

- Nothing assigned was skipped.

## d) TOTALLY FUCKED UP

- **The re-dispatch itself.** This is the Nth same-ID redundant dispatch this window (the 2026-09-13 close-outs documented a three-deep cluster on one task; this is n=2 on 000001a09d06…, one day later). The queue's dispatch-time footer dedup remains unminted — every redundant dispatch pays a full session, a gate re-run, and a report.
- **Turn-1 response shipped without reading CONTRIBUTING.md / CLAUDE.md** — the contract explicitly says "and CLAUDE.md if present". (They were checked for existence this turn; the turn-1 miss is on record as the recurring class.) Turn 1 also did not re-run the gates itself — it trusted the prior session's claim plus git/TODO evidence. Acceptable for a DONE-on-arrival declaration (the gates were re-run here in n=2), but the ordering was report-ready-first, verify-second; the wrong reflex.
- No shipped defect this dispatch; no unexpected working-tree state (`git status` clean before writing this report).

## e) WHAT WE SHOULD IMPROVE

1. ~~**Dispatch-time dedup is the root surface.** The queue can check before minting: does a report with this `Task-Queue-ID` already exist in `docs/status/` / is the TODO row `[x]` with that footer? If yes, either skip the dispatch or mint it as an explicit `verify` mode so the session knows it is a re-dispatch (the 2026-09-13 close-outs asked for the same; still nothing).~~ resolved — routed TODO 157 (verified at HEAD)
2. **Turn-1 mechanical checklist should be a script**, not memory: `git log --grep <id>`, TODO row state, `ls docs/status/*<id>*`, `ls CONTRIBUTING.md CLAUDE.md`, then read. The 03-40 report proposed this; it remains unbuilt.
3. **Verify-before-claim ordering**: even a DONE-on-arrival turn should run the cheapest gate (`go build`) before its first output. Codify in AGENTS.md's session-start ritual rather than relying on each close-out's self-criticism.
4. ~~**TQ_RESULT semantics for n≥2**: this turn's sha is still 4a6f504 (no new work commit), so two different reports for the same ID carry the same `commit_sha` — ambiguous for the queue's cross-reference. A `report_only` marker or the report commit sha would disambiguate (owner ruling candidate, see (g)).~~ done — superseded TQ_RESULT retired (verified at HEAD)

## f) UP TO 50 THINGS WE SHOULD GET DONE NEXT

Honesty note: items 1–14 are carried/derived from the 01-12 close-out and this window's observations, priority-ordered. Items 15+ are filler only where labeled; padding past what genuinely surfaced would violate the smallest-correct-change contract — most of these are ROADMAP fuel, not commitments.

1. ~~Queue: dispatch-time footer dedup — skip or downgrade re-dispatches of done task IDs (§e1, the root surface).~~ resolved — routed TODO 157 (verified at HEAD)
2. Carry `"report"` in the closeout turn's `TQ_RESULT` contract; parse into `AgentResult`; sweeper prefers fact over glob (kills the re-derivation class at emit time).
3. Surface `StatusCompletion.Report` in the webui status window strip as per-entry links.
4. Extend report resolution into `tq session close`'s direct status mints (bypasses the sweeper today).
5. ~~TQ_RESULT report-vs-work commit sha disambiguation for n≥2 dispatches (§e4, owner ruling).~~ done — superseded TQ_RESULT retired (verified at HEAD)
6. Turn-1 verification checklist script (task-ID grep + TODO row + report glob + CONTRIBUTING/CLAUDE existence).
7. AGENTS.md: add the "run the cheapest gate before declaring DONE-on-arrival" line to the session-start ritual.
8. `docs/DOMAIN_LANGUAGE.md`: entry for "window entry report".
9. AGENTS.md status payload contract section: mention the new `report` field (docs-health pass material).
10. Negative test: `closeoutReportPath` with repo dir absent entirely returns `""`.
11. Pin the glob-metacharacter assumption for hex task ids (paranoid check or comment).
12. `tq show` status tasks: list per-entry reports in the payload view.
13. Review tasks' payloads could cite the reviewed task's closeout report path (same pattern as this fix).
14. Debug log line when `filepath.Glob` errors (currently silent `""`).
15. ~~— filler boundary — nothing else surfaced in passing this dispatch that clears the "small, concrete, agent-executable" bar. Items 16–50 intentionally not fabricated.~~ done — narrative filler boundary (verified at HEAD)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **What is the intended contract for a re-dispatch of an already-done task ID?** Should the queue skip them entirely, mint an explicit cheaper `verify-only` mode, or is redundancy accepted as crash-tolerance cost — and if the latter, should the n≥2 close-out be allowed to skip the full a)–g) report when nothing changed?
2. **Should `StatusCompletion.Report` be REQUIRED once the closeout `TQ_RESULT` carries it (f2), with the glob demoted to a legacy fallback?** The 01-12 report listed the fact-carry as an improvement; making it contractual changes the review/status prompt gates and deserves an owner call.
3. **For report-only dispatches, which sha should `TQ_RESULT` carry** — the original work commit (4a6f504), the report commit, or both?

— Close-out by the n=2 dispatch agent. Work provenance: 4a6f504 (code) + 3de902d (01-12 report) + this report's commit. Gates re-run fresh this turn, both modules `-race` ok.
