# Status report — task 000001a0b1afa081ef9275788875e1388462

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-18_01-29_task-000001a0b1afa081ef9275788875e1388462.md`, which owns any open residue.

**When:** 2026-09-18 01:37 CEST · **Scope:** session-bridge safety pins (TODO row 114, section "Done-prompt window harvest (2026-09-12…)") · **Repo:** go-taskqueue @ master, clean tree

## 0. Executive summary

This window was a **re-dispatch of an already-complete task**. The turn-1 ritual (`rg -l <task-id> docs/status/`) found the work landed in commit cd1694f (2026-09-18 01:29) with TODO row 114 closed and a DONE note. The correct response was verify → cite → stop: all four pinning tests re-run green at HEAD, zero code changes, zero new commits for the work itself. Two prior reports for this same task ID already exist (01-29 and 01-33); this is the third and should be the last for this ID.

## a) FULLY DONE

| Item | Evidence |
| --- | --- |
| f15 — prune-stale provenance guard pin | `TestPruneStaleNeverTouchesSessionMintedTasks` (internal/harvest): the sweep lists only the harvester's own task type, so pending `review:session:` / `status:…:session:` tasks survive any sweep. Green at HEAD this pass (`-count=1`). |
| f16 — sweeper-namespace cross-dedup pin | `TestSessionAndSweeperReviewKeysStayDisjoint` (internal/session/session_test.go): `review:session:<id>` can never collide with sweeper `review:<task-id>`. Pre-existing pin, verified at HEAD this pass. |
| f17 — trailer-parser edge pins | `TestParseTrailerCommitsIgnoresBlankAndWhitespaceFooters` + `TestParseTrailerCommitsRepeatedFooterAttributesOnce`: empty, whitespace-only, and repeated session footers can neither double-attribute nor wrongly attribute a commit. Green at HEAD this pass. |
| TODO loop closed | Row 114 checked `[x]` in commit cd1694f with a DONE note citing all four test names. |
| Work commit + footer | cd1694f carries the exact `Task-Queue-ID: 000001a0b1afa081ef9275788875e1388462` footer; queue cross-reference intact. |

## b) PARTIALLY DONE

Nothing in scope. All three sub-items (f15/f16/f17) are pinned; none is stubbed or deferred.

## c) NOT STARTED

Nothing in scope. The contract's optional step — appending NEW follow-up items to TODO_LIST.md — was deliberately exercised as a no-op: this window surfaced no novel, dedup-safe follow-up work worth minting.

## d) TOTALLY FUCKED UP

Nothing. Honest accounting of near-misses:

- **First grep miss, self-corrected in the same turn**: `rg "session-bridge safety" TODO_LIST.md` returned exit 1 (the row text says "Session-bridge safety pins" — case/prefix difference). A second, looser grep found row 114 immediately. Lesson already codified; no damage.
- **Report duplication (process smell, not breakage)**: three status reports now exist for one task ID (01-29, 01-33, this one). Each was its own dispatch's correct output, but the queue minting repeat dispatches for a DONE row is exactly the re-dispatch loop the 2026-09-17 dedup-gating work targeted for status-task *content* — dispatch-level repeats still cost a report each.

## e) WHAT WE SHOULD IMPROVE

1. **Verify-first responses should be cheap to recognize downstream.** This window's entire contribution is a re-verification citation. A short `re-verified at HEAD by <task-id>` note (this report) is the artifact; nothing more was needed — resist the urge to "improve" pinned tests that are green.
2. **Repeat dispatches on DONE rows still burn a close-out + report each.** The content-level dedup (14-01 report) closed the reword-duplicate loop; dispatch-level repeats for already-footed work remain a cost center. A queue-side check — "does a fact with this Task-Queue-ID footer already exist in `git log`?" — could short-circuit mint-time or claim-time.
3. **Grep hygiene in the session-start ritual**: match on the task ID (exact hex), not on subject phrases — the ID never rewords; subjects do.

## f) Up to 50 things we should get done next

Brainstorm-mode list, grounded ONLY in what this window touched or noticed in passing (no unrelated research). Most are owner-gated; the queue's pacing/budget gates own admission. Ordered by impact:

1. Owner ruling on **ask-policy** (§g of the 21-04 report): whether agent prompts should teach `tq ask` — the questions channel is shipped but undiscoverable to agents until ruled on.
2. **Repeat-dispatch short-circuit**: at claim time (or mint time), check whether the target TODO row's task ID already has a footer commit; skip with a fact instead of a full verify window.
3. **Postgres parity for the session-close bridge** (`tq session begin/close`) — AGENTS.md lists it as the known open gap.
4. **Session-bridge trigger automation** (crush #3146) — close is currently enqueue-only via SessionEnd-hook budget; wiring the hook end-to-end is owner-gated.
5. **Daemon-commit attribution gap** for the session-close bridge (listed open in the bridge design doc).
6. **Budget bypass audit** for session-close minted tasks (listed open in the bridge design doc).
7. NixOS unit env: add `Environment=GOEXPERIMENT=jsonv2` to the tq-agent-pool module so `.tq-verify` stops lying to agents outside the devShell (owner-run, known since 2026-09-11).
8. Crush `--reasoning-effort` misleading error message ("does not support reasoning effort" instead of listing accepted values) — upstream-issue candidate, verified 2026-09-14.
9. Archive the two earlier reports for this task ID (01-29/01-33) via docs-health ANNOTATE/archive flow once this report lands, so the index doesn't grow three rows per task.
10. Consider a `check-status-index.sh`-adjacent guard that flags a THIRD report for an already-DONE task ID (cheap dedup signal at index time).

Items 11-50: nothing further without researching unrelated work, which this window's contract forbids. The honest count of *newly surfaced* next work is ten, and of those, nine predate this window (observed in passing, already tracked in AGENTS.md or design docs). Minting filler to reach 50 would poison the harvest pipeline.

## g) Questions I can NOT figure out myself

1. **Ask-policy ruling**: may work prompts now teach agents `tq ask` for parked decisions (the shipped questions channel), or does it stay human-surface-only? This gates whether the questions feature ever fires in the dogfood fleet.
2. **Repeat dispatches**: is the queue deliberately re-minting DONE-row tasks (e.g., because the row was still unchecked at harvest time in a race with cd1694f), or is this a harvest-window race worth a dedup guard at mint time? I can't see the mint-side timing from the repo alone.
3. **Report archival**: should the two earlier 01-29/01-33 reports for this ID be archived now, or do you want all three kept live for the cross-reference audit trail?

— *Verified gates this pass: the four pinning tests above, `go test -count=1`, clean `git status` at report time.*
