# Status report — dead-SHA baseline shrink pass, verify-only re-dispatch (task 000001a0bce3dd86f7ddbc604c21b4b66781)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-20_05-57_task-000001a0bce3dd86f7ddbc604c21b4b66781.md`, which owns any open residue.

Window: 2026-09-20 05:58–06:04 CEST. This was a RE-DISPATCH of the row-141
shrink work item that a prior window (report:
docs/status/2026-09-20_05-57_task-000001a0bce3dd86f7ddbc604c21b4b66781.md —
same task ID, ended 05:57) had already completed minutes earlier. Per the
session-start ritual (grep prior reports for the task ID FIRST), this window
correctly identified the DONE state and performed verify → cite → stop
instead of re-doing the pass. No code, baseline, or doc content changed in
this window; zero commits until this report.

## Summary

The 05:57 window shrank the dead-SHA baseline 242→28 SHAs (cite-sites
973→~88) via staged signature twin matching over `git log --all --reflog`.
This window re-verified the end state at HEAD and found it intact:

- `./scripts/check-dead-sha-refs.sh` → `dead-sha refs ok (100 baselined,
  0 new)`, rc=0 (gate green).
- `TQ_DEAD_SHA_VERBOSE=1` STALE count: 100 (was 82 at the shrink pass's own
  final gate, 88 in the prior report's prose — see §e1).
- TODO_LIST row 141 `[x]` with gate evidence; sibling row 143 (AGENTS.md
  re-points) absorbed and `[x]`.
- Working tree clean before this report; baseline file at 41 lines
  (22 intermediates + 4 wrong-twin folds + dd543ec/e9ef358 fork-note pair
  - header comments).

## a) FULLY DONE

1. **The task itself (row 141), by the prior dispatch**: 214/242 SHAs
   re-resolved to reachable twins; 971 cite-sites repointed across
   AGENTS.md + TODO_LIST.md + docs/status/*.md; baseline 242→28; gate
   rc=0 after every baseline edit. Footer carrier b00dda6; report at
   docs/status/2026-09-20_05-57_task-<id>.md; TODO_LIST closed.
2. **Verification pass (this window)**: gate re-run clean (rc=0, 0 new
   dead citations), verbose count taken, working tree confirmed clean,
   prior report + TODO rows confirmed present and consistent with the
   baseline file on disk. Claims in the DONE row rest on a fresh gate
   run at 86e2a88, not memory.

## b) PARTIALLY DONE

1. **Residue explanation (rows in flight)**: the remaining 28 baselined
   SHAs are documented (22 no-twin squashed intermediates, 4
   wrong-twin-rejected folds, dd543ec/e9ef358 sanctioned fork-note prose)
   but NOT resolved — backlog row 148 owns the positional-mapping ruling.
   Unchanged this window by design.
2. **Gate self-test (row 142)**: check-dead-sha-refs.sh still has no
   `--self-test` mode; semantics are execution-verified only. Not started,
   not this window's scope.

## c) NOT STARTED

1. Row 148: positional mapping (or conscious citation-loss ruling) for the
   28 residue SHAs — time-sensitive while refs/original + reflog still
   hold the old lineage, and a hard prerequisite for any `git gc` /
   refs/original drop (row above it).
2. Row 142: `--self-test` pin for the gate, wired into ci-local.
3. The "drop refs/original" row itself — BLOCKED behind rows 148 + the
   owner ruling.

## d) TOTALLY FUCKED UP

Nothing this window — but one honest process observation: this task was
DISPATCHED TWICE for the same ID within ~20 minutes. The first dispatch
did the work and closed the row; the second (this one) re-ran it. The
only thing that prevented wasted duplicate work (and possible merge
chaos with the daemon commits) was the turn-1 ritual of grepping
docs/status/ for the task ID. Had that ritual been skipped, this window
would have re-derived twin mappings over already-repointed citations —
a no-op at best, a falsifying "repoint" at worst (the pass itself
caught exactly that falsification class: "replaced X with X" edits).
The ritual works; the queue re-dispatching a closed row is the hole.

## e) WHAT WE SHOULD IMPROVE

1. **Verbose STALE count drift (82 → 88 → 100)**: three different
   baselined cite-site counts across one morning. Cause is almost
   certainly NEW status reports (05-57 and this one) citing the dead
   SHAs while describing the pass itself — reports ABOUT the heal cite
   the dead side. Consider whether report prose that describes a dead
   SHA (forensics) should use the `old→new` arrow form (the gate's
   sanctioned escape) instead of growing the silent baseline count.
2. **Queue-side: don't re-dispatch a row already closed with the same
   footer in TODO_LIST** — or at least, the work prompt could carry the
   prior dispatch's report path. Cost of the miss this time was one
   cheap verify window; on a heavier task it doubles spend.
3. **The gate counts, but doesn't trend**: `check-dead-sha-refs.sh`
   prints the baselined count only in the ok line; nothing records it
   over time. A one-line append of `date count` to a scratch trend file
   (or just the discipline of quoting it in every report, as done here)
   would make drift visible.

## f) What we should get done next (focused, this task's blast radius)

1. Row 148: rule the residue — positional mapping attempt vs
   conscious citation-loss acceptance for the 22+4 (owner ruling needed,
   see §g1).
2. Row 142: `--self-test` for check-dead-sha-refs.sh (mirror
   check-gosec.sh --self-test; /tmp fixtures; wire into ci-local).
3. Convert dead-SHA _forensics prose_ in future reports to the
   `old→new` arrow form so the silent baseline count stops growing.
4. After 1–3: cut the refs/original drop + `git gc` only when row 148's
   ruling is recorded and the baseline file is either empty or
   consciously terminal.
5. If row 148 resolves the 22 intermediates: drop them from the
   baseline, gate rc=0 each drop (the original work item's letter).
6. Sweep the ~12 newer cite-sites (the 82→100 drift) to arrow form or
   twins, whichever §e1's ruling picks.
7. Add the baselined-count line to the queue's derived-outcome channel
   (`tq verdict` JSON) so future close-outs carry it mechanically.
8. Consider a docs/status/README.md index convention for
   re-dispatch reports (this report's row should say "verify-only
   re-dispatch" so the lineage of the ID is readable).
9. (Explicitly NOT padding to 50: the prior dispatch already refused
   padding rows 26–50 as dishonest; this window found nothing new
   outside this blast radius, and inventing items would violate the
   no-research-outside-scope instruction.)

## g) Questions (cannot figure out myself)

1. **Residue policy**: for the 28 baselined SHAs — do you want a real
   positional-mapping attempt (reflog-tip anchoring, high effort,
   uncertain yield), or do you rule them citation-lost and drop the
   rows when the refs/original drop lands, accepting the loss?
2. **Forensics-citation form**: should reports describing a dead SHA
   (like the 05-57 report and this one) be REQUIRED to use the
   `old→new` arrow form, so the gate's escape covers prose-about-rewrites
   and the silent count stops inflating? (Would need an AGENTS.md
   convention note + possibly a gate tweak to accept arrow pairs that
   reference baselined SHAs.)
3. **Re-dispatch guard**: is the double dispatch of this task ID a
   queue-side bug (row checked but still enqueueable) or intended
   at-least-once delivery? If the former, it belongs in the tq backlog,
   not here — say the word and I (or a future window) will file it.
