# Close-out — gosec 0-target enumeration guard, verify + residue-heal window

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-19_03-56_task-000001a0b75806e7ce18883bf1d063d176f2.md`, which owns any open residue.

Task `000001a0b75806e7ce18883bf1d063d176f2` · 2026-09-19 04:16 CEST · re-dispatch window over the 03-56 work window · single agent, no concurrent collisions observed (`git status` clean at turn 1 and before commit).

**One-line verdict:** the work item was already shipped and verified by the 03-56 window; this window re-verified the deliverable fresh, found and healed the window's one policy violation (two dead-SHA citations), and completed the verify-window battery the work run had skipped.

---

## a) FULLY DONE

1. **The work item (TODO row 129, the 02-18 §e6 mint): `check-gosec.sh` hard-fails on a broken or empty module enumeration.** Shipped by the 03-56 window in d035911 with this exact footer; row 129 marked `[x]` with a full DONE note (TODO_LIST.md:129). This window's fresh verification: `./scripts/check-gosec.sh --self-test` green — the new branches all pass ("empty enumeration: 0 module targets hard-fail", "broken enumeration: non-zero exit hard-fail", both with fail-before-any-scan `== (root)`-absent assertions); the guard enumerates FIRST via `modules="$(enum)"` with a `||` handler for the enumerator exit and a `-z` guard for the empty list (scripts/check-gosec.sh:222 area, `TQ_GOSEC_ENUM` stub hook).
2. **Fresh real-gate run at HEAD: exit 0, 16 scans ok (1 root + 15 module targets), Issues 0 everywhere.** Carries the documented unstamped-binary WARN (see b2). This is the battery leg the work run skipped (see d4).
3. **Dead-SHA citation heal (131ea17).** The 03-56 window's msg-filter heal replaced `dd543ec` with `d035911`, leaving TODO_LIST rows 133/134 citing the pre-rewrite SHA as a live reference — unreachable from every local AND remote-tracking branch (`git branch -a --contains d035911` empty), violating history-policy clause (d) "no doc may cite the dead SHAs". Both rows repointed to `d035911`; AGENTS.md:408 and the 03-56 report keep their citations as the sanctioned fork record. Gates at the fix commit: check-todo-list ok, check-doc-refs ok, root build+vet ok under GOEXPERIMENT=jsonv2.
4. **Verify→cite→stop discipline held.** Turn-1 ID grep (`rg -l <id> docs/status/`) surfaced the 03-56 work report + index row immediately; the re-dispatch correctly did NOT re-implement the guard — it verified and moved to residue handling. This is the protocol working as designed (the 03-27/03-39 windows on the sibling gosec-pointer ID did the same).

## b) PARTIALLY DONE

1. **The heal is local-only.** origin/master sits at 662e76d, **34 commits behind** local master — the entire window (the abandoned dd543ec side never pushed, the healed d035911/ca514a8 lineage, and 131ea17) is unpushed. Nothing remote is poisoned (origin never saw `d035911`), but remote CI has never seen the new gate either; master-CI-green claims and local master state have diverged until the owner pushes. refs/original is correctly still retained (policy: keep until push lands).
2. **Gate green carries a provenance asterisk.** Every `check-gosec.sh` run on this host WARNs: the discovered binary is UNSTAMPED (`Version: dev`), so "v2.29.0 pin verified" is not proven — the 02-52 §g q2 owner ruling is still open. Until a stamped binary is installed, every green gate claim in every report inherits this caveat.
3. **The gosec/CI-gate TODO family is open downstream of this fix.** Rows 124 + 127 BLOCKED on owner rulings (CLI gosec coverage; ci.yml convergence), rows 130-136 open (scan summary line, ci.yml pin derivation, self-test foreign-CWD bug, consumer loops, short-list guard, citation-order note, wrapper smoke leg), row 253 open (CONTRIBUTING gate-list sweep). The 0-target guard this task shipped is one layer of a family the remaining rows close.

## c) NOT STARTED

All listed in TODO_LIST.md, none of it this task's scope to execute:

- Row 133 — for-each-module.sh consumer loops (ci.yml ×6, ci-local.sh:73) catch neither enumeration failure shape. **The same silent-skip family this task fixed one layer up; highest-value sibling.**
- Row 134 — short-list enumeration guard (non-zero-but-short list scans survivors, exits 0).
- Row 132 — `--self-test` foreign-CWD rc=127 (stock bash; the mvdan shell masks it, so agent-run self-tests can NEVER catch this class — ci-local is the only catcher).
- Rows 130, 131, 135, 136, 253 — scan summary line; ci.yml pin derivation; citation-order one-liner; wrapper-path smoke leg; CONTRIBUTING gate sweep.
- Rows 124, 127 — BLOCKED on owner rulings (standing since 02-18/02-52).
- NEW rows minted this window (see e2/e3): dead-SHA reachability gate; backup-ref drop after push (BLOCKED on owner push + ruling).

## d) TOTALLY FUCKED UP

Zero code damage, zero broken gates — but four process failures, honestly:

1. **Read-before-edit violation in my own window.** My first `multiedit` on TODO_LIST.md failed outright (I had only grep/sed'd the file, never Viewed it) — one wasted round trip on a rule I am explicitly bound to. Root cause: I treated grep output as having "read" the file. Fix is behavioral, not tooling: View before any edit, no exceptions, even when the edit target text is already on screen.
2. **CONTRIBUTING.md skipped at turn 1 — N+14 in the documented recurring-miss series.** I `ls`'d it (so the turn-1 checklist technically "ran") but did not READ it until this close-out. The 03-27 (N+12) and 03-39 (N+13) windows confess the identical failure; my window proves the confession-and-repeat loop is still live. The checklist item needs to be "read", not "check for existence" — that wording upgrade is the actual fix and belongs in the AGENTS.md session-ritual text.
3. **Evidence in hand, interpretation deferred.** During the work run I ran `git branch --contains dd543ec -a` (empty — which already proved the SHA is on NO branch, local or remote) but only registered "not reachable from master" and moved on. The full remote story — origin/master 34 commits behind, heal unpushed, remote never poisoned — was pieced together only at close-out with explicit `merge-base --is-ancestor` checks. No harm done (the citation fix was correct either way), but the first-class evidence was on disk and uninterpreted for the whole work run. Interpret what the command told you when it tells you.
4. **Verification rested on inherited gate evidence until close-out.** The work run verified the deliverable via `--self-test` only; the "real gate re-run green (16 scans)" claim in row 129's DONE note was the 03-56 window's evidence, not this HEAD's. For a docs-only delta that is defensible (the 03-15 window's stated deviation), but the 03-39 window minted the minimum-battery rule precisely to stop this drift: cheap gates re-run at HEAD + fresh delta or explicit none. Battery completed at close-out (real gate exit 0 / 16 scans, build+vet, doc-refs, todo-list), but it was close-out-completed, not claim-time-present — row 135's disease, one generation later.

## e) WHAT WE SHOULD IMPROVE

1. **Dead-SHA citation policy has an internal tension worth one sentence.** Clause (a) of the history-rewrite policy REQUIRES the fork record to carry old + new SHAs; clause (d) bans citing dead SHAs. The boundary (fork note in AGENTS.md, the healing report, and index rows referencing them = sanctioned; TODO rows and everything else = violation) is currently convention, not text. One clarifying parenthetical in the policy would stop the next agent from either mass-scrubbing legitimate fork records or re-citing dead SHAs as live refs. Owner-voice policy edit — flagged, not done unilaterally.
2. **Policy (d) has no mechanical enforcement.** This window's violation survived one full window (03-56 healed the history but its own harvest rows re-cited the dead side). A reachability checker (extract hex tokens from docs, fail on unreachable ones, fork-record escape form) makes the policy self-enforcing — minted as a TODO row this window (§c). Shape decision (hard gate vs advisory, extension vs sibling script) deliberately left open.
3. **Verify-only re-dispatch windows burn a full close-out each.** This is the third such window today across two sibling IDs, each producing a full report whose §a is "already done, verified". The turn-1 ID grep is perfect; what follows it has no lighter protocol than a full report. A minimal certificate format (deliverable re-read at HEAD + delta check + battery result, ~10 lines) would keep the evidence and drop the ceremony.
4. **The mvdan-shell blind spot is structural.** Row 132's rc=127 bug is invisible to every agent session by construction (our shell masks it) — only ci-local's stock bash can catch it. Any gate whose failure mode is shell-dependent must be exercised by ci-local, never by agents claiming it green. Worth a line in the claims-carry-citations convention: agent-run evidence for shell-sensitive gates cites ci-local, or runs in an explicit `bash --noprofile` child.
5. **Keep doing:** turn-1 ID grep (saved a full re-implementation); pre-commit `git status --short` re-check (no daemon race this window); footer-as-last-line (the ea73e45 lesson held — commit-msg hook + derivation both satisfied first try).

## f) Up to 50 things we should get done next — honestly scoped at 15

The user's ceiling is 50; padding to it would mint noise. 15 real items, ranked by impact. Items 1-2 are this window's mints; 3-12 are existing verified rows; 13-15 are standing observations. HARVEST: items 1-2 are now in TODO_LIST.md; the rest already have rows.

| #  | Task                                                                                                                                                                     | Impact  | Effort | Category      |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ------ | ------------- |
| 1  | **NEW (minted):** Dead-SHA citation gate — docs must not cite SHAs unreachable from master; fork-record escape form; decide hard-gate vs advisory                        | Medium  | M      | Quality       |
| 2  | **NEW (minted, BLOCKED):** Drop refs/original + filter-branch backups after the healed lineage is pushed                                                                 | Low     | S      | Cleanup       |
| 3  | Row 133: fix for-each-module.sh consumers (ci.yml:45,58,96,158,251,280 catch neither failure shape; ci-local.sh:73 misses empty) — same silent-skip family, one layer up | High    | M      | Bug           |
| 4  | Row 134: pin a SHORT module list as failure (expected-count or canary-module assertion)                                                                                  | High    | M      | Quality       |
| 5  | Row 132: `--self-test` absolute self-path before top-level `cd` + foreign-CWD assertion (rc=127 under stock bash)                                                        | Medium  | S      | Bug           |
| 6  | Owner push: 34 unpushed commits incl. the entire gosec window + heal — until then remote CI has never run the new gate                                                   | High    | S      | Process       |
| 7  | Row 253: CONTRIBUTING.md gate-list sweep (7 gates missing from the turn-1 inventory)                                                                                     | Medium  | S      | Documentation |
| 8  | Row 130: end-of-run "N scans ok, M failed" summary line in check-gosec.sh (kills hand-count drift — row 129's DONE note miscount already proved the disease)             | Low-Med | S      | Quality       |
| 9  | Row 131: derive GOSEC_VERSION + excludes in ci.yml from check-gosec.sh (two hand-pinned copies drift)                                                                    | Medium  | S      | Cleanup       |
| 10 | Row 136: committed wrapper-path smoke leg for `tq crush --bin`                                                                                                           | Medium  | M      | Quality       |
| 11 | Row 135: AGENTS.md one-liner — citations written only after the cited gate's output is in hand                                                                           | Low     | S      | Documentation |
| 12 | Rows 124 + 127 (BLOCKED): CLI gosec coverage; ci.yml gosec-loop convergence — both park on owner rulings recorded 02-18/02-52                                            | Medium  | M      | Quality       |
| 13 | Standing (02-52 §g q2): install a stamped gosec v2.29.0 on this host so gate green stops carrying the provenance WARN                                                    | Medium  | S      | Cleanup       |
| 14 | Standing (digest row): status-index ANNOTATE sweep overdue — live rows ~143 vs threshold 100                                                                             | Low     | M      | Documentation |
| 15 | e3: minimal-certificate protocol for verify-only re-dispatch windows (converges with the suppression-signal question in the 03-27/03-39 reports)                         | Medium  | M      | Process       |

## g) Top 3 questions I can NOT figure out myself

1. **Codify the fork-record exception?** History policy (a) requires recording old+new SHAs; (d) bans citing dead SHAs. I resolved the tension by convention (fork note + healing report may cite; TODO rows may not) — should the policy text say so explicitly, naming the sanctioned locations? Tried: reading both clauses, checking how the 03-56 report/index row/AGENTS.md:408 treat dead SHAs — the line between "record" and "violation" is nowhere written.
2. **Dead-SHA enforcement: hard or advisory?** If the reachability checker (§f1) gets built, should it gate ci-local (hard, like doc-refs) or run advisory? A hard gate needs the fork-record escape form decided FIRST (arrow syntax vs allowlist file) — that's a convention choice only you can ratify.
3. **Report weight for verify-only re-dispatches?** Three windows today (two IDs) each paid a full close-out for "already done, verified". Do you want the minimal-certificate format (§e3/§f15) as the standard for windows whose turn-1 grep finds the row DONE — full report reserved for windows that change something? This converges with the re-dispatch-suppression-signal question the 03-27/03-39 reports asked; answering it once would close both.

---

_Gates cited: `check-gosec.sh` exit 0 / 16 scans (fresh at HEAD, /tmp/gosec-closeout.log); `--self-test` green incl. both enumeration-failure branches; root `go build` + `go vet` ok (GOEXPERIMENT=jsonv2); `check-doc-refs.sh` ok; `check-todo-list.sh` ok. Commits: d035911 (work, 03-56 window), 131ea17 (citation heal, this window)._
