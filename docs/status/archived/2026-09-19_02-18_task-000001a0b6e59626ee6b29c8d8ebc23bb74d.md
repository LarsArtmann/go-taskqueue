# Close-out self-review — third dispatch, check-gosec.sh pin verification

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-19_01-58_task-000001a0b6e59626ee6b29c8d8ebc23bb74d.md`, which owns any open residue.

**Task:** 000001a0b6e59626ee6b29c8d8ebc23bb74d (TODO_LIST row: "check-gosec.sh: verify a
pre-existing GOSEC_BIN is actually v2.29.0 … (02-52 gosec-gate report §b)")
**Window:** 2026-09-19, ~02:10–02:18 CEST — THIRD dispatch on this ID; the first window
shipped the work (`e913d7b` + 01-58 close-out), the second verified it (02-09).
**Nature:** verify → cite → stop, zero repo edits until this report + 3 new backlog rows
(third re-dispatch of a row already `[x]`; no work commit — nothing changed).

---

## a) FULLY DONE

- **Repeat-dispatch caught at turn 1**: whole-repo `git log`/`git status`/`git stash list`
  PLUS the task-ID grep over `docs/status/` in the FIRST tool batch — found work commit
  `e913d7b`, close-outs `e41b9b9` (01-58) and `f5c3dd4` (02-09), and TODO row 125 already
  `[x]` with a DONE note. Verify→cite→stop chosen per the AGENTS.md ritual; no empty
  commit (git rules + precedent from both prior windows).
- **Implementation re-read fresh from source**: whole of `scripts/check-gosec.sh` (96
  lines), not just the pin block — discovery order (`GOSEC` override → GOPATH → PATH,
  :21-33), pre-existing-binary gate BEFORE any scan (:40-63), three-way case, install
  path exempt (v2.29.0 by construction), Files:0 / Issues>0 scan gates (:65-83). The
  DONE note matches the code exactly.
- **Full three-branch version-gate coverage, all observed FRESH this window** — closes
  the 02-09 §b gap ("WARN branch output line never observed"):
  - FAIL branch: `v2.28.1` stub → exact FAIL line + `go install …@v2.29.0` fix line,
    exit 1, ZERO scan lines (nothing scanned).
  - OK branch: `v2.29.0` stub → `ok: … is v2.29.0`, exit 0, 16/16 scans green.
  - WARN branch: `Version: dev` stub → loud WARN line verbatim (pin UNVERIFIED,
    provenance-ruling pointer), exit 0, 16/16 scans green.
    All stubs under /tmp, created+asserted+trashed in ONE chain each (no daemon food).
- **WARN-branch input confirmed live on the host**: `/home/lars/go/bin/gosec -version`
  → `Version: dev` — every ci-local gosec run today prints the WARN line.
- **Scan arithmetic DERIVED, not eyeballed**: `./scripts/for-each-module.sh | wc -l` = 15
  → 16 scans (1 root + 15 module targets). This settles the ok-line arithmetic (OK run:
  17 `^ok:` lines = 1 version-ok + 16 scan-ok; WARN run: 16 = 16 scan-ok) and exposes
  the 02-09 report's "(1 root + 16 module scans)" as an off-by-one (its 17 ok lines
  included the version-ok line). The row's own "16/16" figure was correct all along.
- **Gates green at HEAD f5c3dd4**: `check-script-syntax.sh` (bash -n over tracked
  scripts + shellcheck 0 findings) and the full root gate `GOEXPERIMENT=jsonv2 go build
  ./... && go vet ./... && go test ./... -race -count=1` — all green.
- **Tree clean at stop; TODO row 125 left `[x]` verbatim** (every DONE-note claim
  re-checked against fresh evidence; all hold); no false BLOCKED, no duplicate row.
- **The set -e probe-chain trap from 02-09 §d2 avoided by construction**: FAIL branch
  captured via `|| rc=$?` inside one chain; branches never chained with `&&`.

## b) PARTIALLY DONE

- **Turn-1 ritual, again imperfect**: CONTRIBUTING.md read in FULL — but in the fourth
  tool batch, not the first. Read before any verification ran, which is better than
  02-09 (existence-check only, read at close-out) but still not the literal "turn 1"
  the codified ritual demands. Counted series continues: N+10 (02-09's row says N+9).
  CLAUDE.md was never even existence-checked (see §d2) — it turned out to be absent.
- **Cited-not-rerun**: the real-binary full-scan green (minutes of gosec) still rests on
  the 01-58 window's evidence; this window's stubs prove the VERSION gate and the
  Files/Issues parse path, not today's real scan outputs. Full ci-local (lint loop,
  smokes, nix legs) not run — deliberate and stated: zero edits, the row's object was
  verified at branch level + the full root Go suite.
- **The 01-58 (first-window) report never read**: the ID's evidence chain was walked
  02-09 → e913d7b but not 01-58 → e913d7b; 02-09's summary of 01-58 was trusted. Low
  risk (02-09 is the later, self-critical record that re-verified 01-58's claims) but
  the chain has an unwalked link.
- **`e913d7b`'s diff never inspected**: verified the final STATE of the script, not the
  shape of the change (smallest-change/scope review of the work commit skipped). The
  final state is what gates, so impact is nil; the review step was still skipped.
- **Follow-up rows appended: 3** (§f items 2/3/4 below → TODO_LIST). Judgment applied
  about what to queue vs. merely report; the 02-09 count-error correction itself was
  NOT queued (policy fence, see §g2).

## c) NOT STARTED

- Nothing on the row itself — by design. Third dispatch on a `[x]` row; zero edits, no
  work commit, TODO row already closed.
- Open owner actions carried: stamped gosec v2.29.0 install (clears the WARN on every
  gosec run), 02-52 §g q1/q2/q3 rulings (provenance, mismatch policy, CI independence).
- Sibling rows untouched, their own dispatches: 124 (cmd/tq coverage, BLOCKED owner),
  126 (--self-test), 127 (ci.yml convergence, BLOCKED owner), 128 (AGENTS.md pointer),
  40 (gate-vs-advisory, BLOCKED owner).

## d) TOTALLY FUCKED UP

1. **Repeated a documented miss from THIS ID's own close-out**: `mcp_qmd_get` with a
   guessed `cv/`-prefixed repo path → "Document not found" — the 02-09 report's §d3 is
   the SAME mistake class (qmd retrieval misselect for a local repo file), and I did it
   again with a different wrong prefix. Repo docs are local files; `view` was always
   the right tool. One wasted call, recovered immediately — but close-outs exist so
   documented misses don't recur, and this one did, window-over-window, on the SAME ID.
2. **CLAUDE.md never existence-checked**: the ritual says "check for and read
   CONTRIBUTING.md (and CLAUDE.md if present)" — I never ran the `ls`. It happens to be
   absent (verified while writing this report), so no harm THIS time, but "true by
   luck" is exactly the failure mode the 01-37 close-out named on another ID. The
   check is the gate, not the outcome.
3. **CONTRIBUTING.md read at batch 4, not batch 1** → miss N+10 by the series
   convention. Third consecutive window on this ID to fumble the ritual's timing
   (01-58: clean; 02-09: existence-check + late read; 02-18: full read, late). The
   codified rule has now failed its purpose three times running — the fix is
   mechanical (row 131's session-start script), not more vigilance.
4. **Near-miss on inheriting the 02-09 miscount**: my stub runs produced 17 vs 16
   `ok:`-lines and the framing pull was to cite "17 scans" matching the prior report;
   only the fresh `wc -l` (15 targets → 16 scans) caught it before anything was
   asserted. The derivation happened BEFORE my final claims (good), but it was
   reactive to a felt confusion, not a planned gate — one step from shipping a wrong
   count into a third report.
5. **Sat on the policy fence instead of resolving it**: the 02-09 count error + the
   fix-trivial-doc-staleness-on-sight ruling (2026-09-06) vs "reports are point-in-time
   records" — noticed, cited, then neither fixed nor queued; ends as a §g2 question.
   Two consecutive windows have now deferred the same class (02-09 deferred 02-52's
   stale "17/17" the same way). Deferral-with-a-citation is still deferral.

## e) WHAT WE SHOULD IMPROVE

1. **Make the ritual mechanical, not judged**: batch 1 should literally be (git ritual +
   ID grep + `ls CONTRIBUTING.md CLAUDE.md` + read CONTRIBUTING.md). Row 131's
   `session-start.sh` is the structural fix — demanded by ≥8 windows of close-outs and
   still not existing. Prioritize it above any further gosec polish.
2. **Stop hand-rolling the same stub harness per dispatch**: three windows have now
   re-derived near-identical /tmp stubs. Row 126 (`--self-test`) pins the three
   branches AND the Files:0 parse inside the script — it converts this entire verify
   ritual into one command. Highest-leverage gosec row left.
3. **Derive every count at claim time**: `wc -l`/`grep -c` before asserting any
   "N scans / M lines" claim. The 02-09 miscount came from eyeballing; this window
   nearly repeated it (§d4).
4. **qmd is for the knowledge base, not repo files**: repo docs → `view`/`grep`,
   always. Two windows, two misselects; stop reaching for the wrong accessor.
5. **Pin duplication is a drift trap**: the v2.29.0 pin + exclude set now live in BOTH
   `scripts/check-gosec.sh:18-19` and `.github/workflows/ci.yml:277` — a future pin
   bump is a two-place edit waiting to half-land. ci.yml can derive the version from
   the script with one sed; queued (§f item 4) without forcing row 127's convergence.
6. **New silent-narrowing hole found while re-counting**: if `for-each-module.sh` ever
   returns empty or fails, `check-gosec.sh`'s module loop silently runs ZERO module
   scans and exits 0 on the root scan alone (the process substitution's exit status is
   ignored). Same "silent skip" family as Files:0, one level up. Queued (§f item 2).
7. **Verify windows should state the proportionality ruling up front** (which gates
   ran, which are cited-not-rerun, and why) — both prior windows did this well; keep it.

## f) UP TO 50 THINGS TO GET DONE NEXT

Ordered by impact/effort within this task's orbit. **21 honest items — padding to 50
would be noise** (thirded from 01-58/02-09).

Gosec gate:

1. Owner: install stamped gosec v2.29.0 (`go install
   github.com/securego/gosec/v2/cmd/gosec@v2.29.0`) — one command, clears the WARN on
   every ci-local/CI gosec run, makes "green" mean green (02-52 §g q2 dissolves).
2. NEW row appended: check-gosec.sh — hard-fail when the module loop enumerates 0
   targets (silent root-only narrowing today).
3. NEW row appended: check-gosec.sh — end-of-run "N scans ok, M failed" summary line
   (kills hand-counted citations; the 02-09 miscount is the proof of need).
4. NEW row appended: ci.yml — derive GOSEC_VERSION/excludes from check-gosec.sh instead
   of the second hand-pinned copy (ci.yml:277).
5. Row 126: `--self-test` pinning the three version branches + the Files:0 parse (the
   three windows of /tmp stubs are the design).
6. Row 128: AGENTS.md gosec note → point at check-gosec.sh as the canonical entry.
7. After a stamped install: re-run the real-binary end-to-end, then flip TODO row 125's
   "until the owner installs…" parenthetical and the AGENTS.md "this host's 02-52
   scratch build" wording (both go stale the moment the WARN clears).
8. Fold the 15-target/16-scan arithmetic into the eventual --self-test assertions
   (structural fix for citation drift, not another eyeball).

Citations & docs (docs-health sweep material):
9. Correct the 02-09 close-out + its index row: "17 ok lines = 1 root + 16 modules" →
16 scans = 1 root + 15 targets (the 17 ok lines included the version-ok); same pass
fixes 02-52's stale "17/17" (pending the §g2 policy call).
10. Walk the ID's unwalked evidence link: read 01-58 and confirm its claims against
02-09/02-18 (three reports should tell one story).
11. AGENTS.md claims-carry-citations bullet: add "counts are derived at claim time
(wc -l / grep -c), never eyeballed".

Process:
12. Row 131: scripts/session-start.sh (the turn-1 ritual script) — now demanded by
≥8 windows; this window's §d2/§d3 are two more exhibits.
13. Add CLAUDE.md to the ritual's existence check (absent today; the check is the gate).
14. Rule on re-dispatch close-out weight (02-09 §g q3, now five windows deep across
IDs): does a third verify window need the full a-g ceremony, or a) + delta only?

Carried rows needing dispatches (unchanged, listed for completeness):
15. Row 124: cmd/tq gosec coverage (BLOCKED: owner ruling, 03-07 §g3).
16. Row 127: converge ci.yml's gosec loop onto check-gosec.sh (BLOCKED:
CI-independence ruling; §f4 above is the non-converging subset).
17. Row 40: gosec gate-vs-advisory ruling (BLOCKED: owner).
18. 02-52 §g q1/q2 rulings (mismatch fail-vs-force-reinstall; unstamped provenance).

Not queued, deliberately:
19. The 02-09 count fix itself — behind the §g2 policy question, not queued
unilaterally.
20. Any ci-local run-time reduction for the gosec step — no evidence of pain yet.
21. Anything outside this ID's orbit (per dispatch contract).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Unstamped-binary provenance (02-52 §g q2, carried twice, now load-bearing)**: is a
   `Version: dev` binary acceptable for the gate — WARN + continue as shipped — or must
   ci-local hard-FAIL until a stamped v2.29.0 binary is installed? Every gosec run
   today prints the WARN; "green" currently carries unproven provenance, and only the
   owner can rule whether that is an acceptable resting state.
2. **Prior-report evidence corrections**: when a close-out's evidence line is factually
   wrong (02-09's scan count; older: 02-52's "17/17"), what is the sanctioned fix —
   annotate in place, follow-up report, or docs-health sweep? The fix-on-sight ruling
   (2026-09-06) and the point-in-time-record norm point in opposite directions; two
   consecutive windows have deferred rather than guess.
3. **Pin dedup vs CI independence (refines 02-52 §g q3)**: may ci.yml derive
   GOSEC_VERSION/excludes from scripts/check-gosec.sh (single source, agent-doable), or
   does the CI-independence concern (a broken script then silences both layers) apply
   to the pin constant too, making the second hand-pinned copy deliberate?
