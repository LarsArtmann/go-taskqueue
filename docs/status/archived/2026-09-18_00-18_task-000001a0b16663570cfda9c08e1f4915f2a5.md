# Task 000001a0b16663570cfda9c08e1f4915f2a5 — stale re-dispatch, verify-only window

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-18_00-14_task-000001a0b16663570cfda9c08e1f4915f2a5.md`, which owns any open residue.

**Verdict: DONE-on-arrival, no code changes.** The queue paid a THIRD window for
this row: the 2026-09-18 00-14 report covers the same task ID (verify window,
E2E on a scratch DB), and the implementation itself shipped even earlier.
Nothing was wrong with the shipped work — the miss is queue-side (see §d).

## §a — FULLY DONE (this window)

1. **Session-start ritual caught the re-dispatch**: `git log` + grep for the
   task ID under `docs/status/` found `2026-09-18_00-14_task-…5.md` (the
   00-14 verify window) before any edit attempt.
2. **Independent re-verification (trust nothing, verify fresh)**:
   - Doctor check exists with the exact ruling-required semantics:
     `doctorOpenSessions` at cmd/tq/doctor.go:535 — reports open sessions
     (repo/`-` fallback per entry) and its detail states close never happens
     without an explicit operator ruling; never auto-closes (03-28 §f9).
   - Stats `open_sessions` JSON field + human row: pinned at
     `cmd/tq/stats_test.go:96` (`TestStatsOpenSessions`), including the
     omitted-when-none contract (bare store → no key at all).
   - Doctor pin: `TestDoctorOpenSessions` at `cmd/tq/doctor_test.go:705`.
3. **Gate run FRESH this session**: `./scripts/test-cmd-tq.sh` →
   `ok github.com/larsartmann/go-taskqueue/cmd/tq 9.670s`. (First attempt ran
   the tests from the repo root without the devmod shim — setup failed on the
   replace-free cmd/tq module; corrected to the sanctioned gate script. §e2.)
4. **TODO row confirmed closed**: row 111 already `[x]` with evidence cited
   (doctor.go:535, stats field, E2E scratch-DB run). No TODO edit needed.
5. **Working tree clean** — nothing to commit for the work itself; this
   report is the only change, committed with the task footer.

## §b — PARTIALLY DONE

Nothing. The row's full contract (doctor + stats visibility, no auto-close)
is shipped and pinned.

## §c — NOT STARTED

Nothing new this window. Sibling session-bridge rows remain open and are
already tracked (rows 112+ / §f below).

## §d — TOTALLY FUCKED UP

1. **Queue-level, not this session's, but it must be said: a THIRD paid
   window on a DONE row.** The 00-14 report's own §d was "the queue paid a
   full window for a DONE row (no prior report under this ID)". Now the row
   was re-minted HOURS later with a prior report under the exact same ID in
   the index. The harvest-time dedup cannot see DONE-ness; the agent-side
   ritual can and did (one grep), but the session still burned a full
   dispatch. The cheapest systemic fix remains the owner-blocked row 68
   (pickup-time stale-row check in the harvester).
2. **Minor: I ran the pinning tests from the repo root first** and hit the
   documented "missing go.sum entry … exactly one module" failure before
   switching to `scripts/test-cmd-tq.sh`. The AGENTS.md documents that
   `cmd/tq` is its own replace-free module (ADR-0017); I should have gone
   straight to the sanctioned gate. Cost: one wasted tool round-trip.

## §e — WHAT WE SHOULD IMPROVE

1. **Same-ID re-dispatch is a distinct recurrence class from stale-row
   re-dispatch.** The 00-14 report checked "no prior report under this ID"
   and passed — yet the queue re-minted the same ID anyway. Whoever owns
   the mint path should check whether the enqueue dedup key collided on
   reworded item text (the key hashes repo + text; the row was reworded/
   re-verified between windows, which forks the key — that is the likely
   mechanism, and it means reworded DONE rows re-fire forever).
2. **Agent-side fix is already cheap and I applied it**: grep
   `rg -l <task-id> docs/status/` BEFORE any code work. It worked. Codify
   harder: the 00-52/02-17 class of ritual misses is shrinking; keep it so.
3. **Gate script muscle memory**: per-module gates should always be reached
   via `scripts/test-cmd-tq.sh` (cmd/tq) or the per-module loop
   (internal/*), never bare `go test` from root. Known, documented, still
   tripped — worth a line in every close-out until it stops happening.

## §f — Up to 50 next items (new/observed; existing rows not duplicated)

1. Session forensics: CloseDetail records AllowDirty/RequireClean + review
   range; `tq facts --type session.opened,closed`; `tq show session:<id>`
   graceful degradation (existing row, still open).
2. Session-bridge safety pins: prune-stale never cancels session-minted
   tasks; sweeper-namespace cross-dedup; trailer-parser edge tests
   (existing row, still open).
3. Investigate the §d1/§e1 hypothesis: confirm the harvest dedup key
   forking on row rewording is what re-minted this ID, and either pin the
   behavior in a test or mint a harvester-side guard row.
4. `tq session sweep` already exists (2026-09-17 02-21 report); consider a
   smoke script for it alongside `session-close.sh` (sweep → registry
   empty → exactly one review + one status).
5. `factTone` still falls back to neutral for SessionOpened/SessionClosed
   (components.go:114, 2026-09-17 00-43 report item 3) — the fact feed
   could tone-tag the two session lifecycle facts.
6. Owner-blocked: pickup-time stale-row check in the harvester (row 68) —
   this window is its 4th-ish data point; worth re-pinging the owner with
   the recurrence count.
7. `tq session list`/`--dry-run` surface (2026-09-14 14-17 report item 47)
   — sweep preview without closing anything.

(7 items; the remaining 43 would be padding — the honest next-work list
for this row's neighborhood is exactly these.)

## §g — Questions for the owner

1. Ask-policy (open since the 02-54 report, owner ruling still pending §g):
   should `tq session begin` ever auto-fire from a wrapper when
   `$CRUSH_SESSION_ID` is set but no `session.opened` fact exists, or is
   begin strictly operator/hook-owned?
2. Do you want DONE-row re-mints handled by the harvester (dedup against
   row text VARIANTS, e.g. fuzzy or section-scoped), or is the agent-side
   grep ritual deemed sufficient and the third window accepted as cost?
3. The doctor open-sessions check surfaces IDs but has no drill-down
   (e.g. `tq doctor --open-session <id>` → the session's facts + attributed
   commits). Worth a row, or is `tq show session:<id>` (row in §f1) the
   intended surface?
