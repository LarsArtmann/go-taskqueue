# Task 000001a0c1ce218220561062a90bd6b6e6ff — PrioritizeResult usage-key pin (window 2, verify-only)

> **DUPLICATE — ARCHIVED 2026-09-21 (docs-health archive sweep)** — verify-only re-dispatch; zero unique items. Canonical: `docs/status/2026-09-21_04-40_task-000001a0c1ce218220561062a90bd6b6e6ff.md`, which owns any open residue.

Full a)-g) status report, 2026-09-21 04:55 CEST. SECOND window on this ID: a
repeat dispatch ~15min after the 04-40 work window (its close-out:
2026-09-21_04-40_task-000001a0c1ce218220561062a90bd6b6e6ff.md). Dispatch HEAD
0ce62ad, tree clean. The work item (TODO row 164, section "Priority-system
follow-ups") was already `[x]` with a full citation block, so this window was
verify → cite → stop — plus two repairs to the sibling window's paper trail
that surfaced while drafting this report (§d4/§d5).

Turn-1 ledger: `rg -l 000001a0c1ce docs/status/` was the FIRST action (found
the 04-40 close-out before any edit; no re-work, no code change); `git log -8`

- `git status` run, `git stash list` skipped at turn 1 (covered retroactively
  by session-start.sh); session-start.sh NOT run and CONTRIBUTING.md NOT read at
  turn 1 — both healed at report prep (script rc=0; CONTRIBUTING read 74/74).

## a) FULLY DONE

- **Zero source edits.** The deliverable re-verified first-hand at HEAD
  0ce62ad: `TestPrioritizeResultUsageKeysMatchAgentResult`
  (internal/executor/prioritize_test.go:96, table at :101) — targeted run
  PASS (0.003s, both subtests AgentResult + PrioritizeResult). This is the
  window's one fresh delta for the verify-window battery.
- **Ship provenance re-confirmed:** test commit efd9348 (exactly one
  well-formed footer, verified at the 04-40 window), close-out 0ce62ad (this
  ID), TODO row 164 `[x]` with its gate-citation block, working tree clean at
  dispatch.
- **Cheap battery at HEAD, rc=0 each** (GOEXPERIMENT=jsonv2): root
  `go build ./... && go vet ./...`; `./scripts/check-doc-refs.sh` ("doc refs
  ok"); the targeted pin test above. Expensive tier (executor FULL `-race`
  suite, module vet loop) inherited from the 04-40 battery — same HEAD, zero
  source delta since (allowed by the verify-window minimum-battery row 150:
  same-HEAD prior report).
- **Turn-1 heal:** session-start.sh rc=0 (retroactive); CONTRIBUTING.md read
  in full (74/74 lines).
- **Stop artifact minted** (owner-prompted, see §d1): row-164 re-verify
  annotation + this report + docs/status/README.md index row + footer
  commit(s), assembled at report prep (see §d7 for the daemon fold that
  split the paper across two footerless daemon commits before the footer
  commit 7c6dad4 landed — attribution resolves via the footer commit per
  the 09-52/01-11 precedent).
- **Repairs shipped in the same commit (findings §d4/§d5):**
  1. TODO L165 restored: `--- [ ]` → `- [ ]` on the Score-cache TTL/eviction
     row (damage introduced by 0ce62ad itself — §d4). The row is harvest-
     visible again.
  2. The Review/Status usage-pin **parity row inserted** after row 164: the
     04-40 report §b/§f1 claims it "FILED this window", but the row is absent
     from TODO_LIST.md at HEAD 0ce62ad (git-verified: efd9348's TODO diff =
     the row-164 check-off only; 0ce62ad's = the L165 prefix damage only; no
     later commit touches the file). Restored in substance, with a provenance
     note and the standing owner question (04-40 §g1) embedded.
  3. Five post-baseline dead-SHA citations fork-recorded (4544fda→1d4e1a1,
     4d2c720→c89e93e ×3, 2e9f04c→f8f84a9 — orphans of the 00-27 msg-filter
     heal; each target verified reachable before citing).
     check-dead-sha-refs.sh rc=0 again after riding red at HEAD since 00-27
     (§d6).

## b) PARTIALLY DONE

- **This window's verification breadth:** cheap tier re-run fresh; expensive
  tier inherited same-HEAD (stated per row 150; deliberately not re-run —
  zero source delta makes a re-run evidence-free).
- **The usage-pin story:** PrioritizeResult↔AgentResult is pinned (landed
  04-40). ReviewResult/StatusResult exact-set pins remain open — the restored
  parity row now carries the work, gated on the owner's granularity ruling
  (04-40 §g1: is budget_test's parse-sum pin the designated all-four guard,
  making the executor-level pin Prioritize-only?).
- **DONE-row annotation hygiene:** row 164 now carries two windows of
  annotations and is ~4x its item text; my addition is pointer-compact, but
  the cap convention is only proposed (§f4), not decided.

## c) NOT STARTED

Nothing outside verification + the two repairs was attempted. Standing
adjacent rows observed in passing (all pre-tracked; deliberately untouched):
score-cache TTL/eviction (row 165 — visible again after the prefix repair),
e2e show-JSON usage wire pin (row 166), SessionMessageCount render decision
(row 167), CHANGELOG/FEATURES usage-rendering check (row 168), v0.3.0
pkg.go.dev spot-check, `journal/cqrs` facade ruling (BLOCKED owner),
`--prioritize` dogfood enable (BLOCKED spend), owner-only `.tq-verify`
gofmt-vendor fix + dead-letter forensic sweep (filed 03-17), e2e `-race`
headroom, queue-side dedup-key→COMPLETED short-circuit (row 299),
duplicate-claim re-delivery ruling (BLOCKED), daemon-fold attribution ruling
(BLOCKED, 4+ asks).

## d) TOTALLY FUCKED UP

1. **ZERO-ARTIFACT-STOP, recurrence #5** (family ledger: 00-39, 02-01, 04-07
   §d leads; remedy on file since 02-10): my first stop was chat-only — no
   row annotation, no footer commit. Root cause: only the SAME-ID report
   (04-40) was read at turn 1; the disposition rules live in the SIBLING
   close-outs (00-39/02-01/04-07/04-17/04-24), which I never opened. A
   correctly-verified but unattributed stop is invisible to the queue —
   exactly the shape the footer cross-reference exists to prevent. Healed
   owner-prompted (the stop-artifact bullet in §a).
2. **Verify-window minimum battery gap at the first stop:** only the targeted
   pin test had run before the stop verdict; row 150 requires the cheap gates
   (root build+vet, check-doc-refs) in EVERY verify-only window. Both were
   executed at report prep (rc=0), so the final state complies — but the
   initial stop rested on 1-of-3 gates, one worse than the 09-57 window's
   confessed "2-of-3" §d entry.
3. **Turn-1 ritual skips:** session-start.sh not run, CONTRIBUTING.md not
   read, `git stash list` not run — all codified recurring misses (row 149
   shipped the script for exactly this on 09-20). Healed retroactively this
   window.
4. **Sibling defect #1 — live format damage shipped by 0ce62ad** (the 04-40
   close-out commit): TODO L165's `- [ ]` prefix became `--- [ ]`, making the
   Score-cache row invisible to BOTH the harvest parser AND
   check-todo-list.sh (the gate returned rc=0 on the damaged file — verified
   this window before repairing). Dead-in-queue for the ~10 minutes since
   04:40; no dispatch burned. The damage shape (a row's prefix mutating while
   the ADJACENT row was being edited) points at a mangled in-chain line
   insertion through the mvdan/sh → tool handoff — the documented
   inline-quoting hazard family (AGENTS.md "scripted history edits" bullet).
   Repaired + a gate-gap row filed (§f1).
5. **Sibling defect #2 — an unbacked FILED claim:** the 04-40 report
   §b/§f1 states the Review/Status pin parity row is "FILED this window"; it
   is NOT in TODO_LIST.md at HEAD (git-verified, §a repair 2). Ironic given
   that window's otherwise model citation discipline. Most likely the same
   mangled insertion as §d4 consumed the new row while damaging the neighbor.
   Row restored with provenance; its owner question (granularity) stands.
6. **Sibling defect #3 — a red master gate nobody's battery sees:**
   check-dead-sha-refs.sh (a HARD ci-local step) had been failing at HEAD
   since the 00-27 window's msg-filter heal orphaned five post-baseline
   cite-sites (4544fda→1d4e1a1, 4d2c720→c89e93e ×3, 2e9f04c→f8f84a9 across the
   07-30/08-02/09-30 reports and the README index) — nine windows since 00-27 ran batteries
   that exclude the gate, so the red went unnoticed. Healed here with fork
   records per the gate's own remedy (targets verified reachable first).
   Found because THIS window ran the full doc-gate set at report prep, not
   because any battery requires it; the process gap (heal windows must re-run
   the citation gate themselves) filed as a row (§f22).
7. **Daemon fold, again** (the family's most-repeated hazard): the window's
   paper landed as three commits — daemon 3b84d6e (6 files: report body,
   parity row, index row, the three 09-20 fork-record repairs), daemon
   cfff57d (2 files: first dead-SHA arrow round), then footer commit 7c6dad4
   (the final arrow corrections). Root cause is structural, not sloppiness
   this time: each gate failure forced another edit round, and every round
   gave the sub-minute daemon a sweep window — the 04-24 one-invocation
   fold-proof shape cannot cover a write→gate→fix→write loop. Mitigation
   that held: the FINAL commit carries the footer, corrections ride
   footer-carrying follow-ups, no history rewrite while the §g2 ruling is
   pending (01-11 pattern).

Severity honesty: §d1-3 are process damage only — no false claims were MADE
(the chat stop cited exactly what had run: test PASS, row `[x]`, commits,
clean tree — all true). §d4-5 are live repo defects found and fixed in this
window's commit, not caused by it.

## e) WHAT WE SHOULD IMPROVE

- **Read ONE sibling close-out at turn 1, not just the same-ID report:** the
  stop-disposition rules (annotation + footer commit) live in sibling §d/§e
  sections; a first-window close-out never carries them. Concrete fix filed
  as §f2 (session-start.sh index tail).
- **Stop-artifact as mechanical default:** the annotation + footer commit is
  a two-command chain; chat-only stops should be the exception needing a
  reason, not the default needing an owner ask. Filed as a BLOCKED ruling row
  (§f3) — five recurrences is the evidence.
- **Run the cheap battery inside the verify chain, before any stop verdict:**
  row 150 already says this; the gap was execution, not knowledge — which is
  exactly why the §f2 fix surfaces the rule at the moment it is needed.
- **Carried (04-40 §e):** verify-the-verifier as a stated expected-verdict
  step before any mutation/overlay harness; the lessons.md candidate remains
  owner-commit-only (crush-config repo, not writable in-session).
- **DONE-row annotation growth:** pointer-style annotations (date + window #
  - report path) keep TODO_LIST machine-friendly; rows 158/160-164 now carry
    multi-hundred-word battery text that already lives in the cited reports.
    Proposed in §f4.

## f) Up to 50 things we should get done next

Honest list; NEW rows/process items filed this window (1-3 below plus 22),
then tracked pointers. 50-padding refused — the ceiling is not a quota.

1. **FILED:** check-todo-list.sh + internal/harvest `TestRepoTodoListParses`
   must catch checkbox lines whose prefix is not exactly `- [ ]` (the L165
   `--- [ ]` class shipped green in 0ce62ad; both consumers ignored the row
   silently) (scripts/check-todo-list.sh, internal/harvest).
2. **FILED:** session-start.sh — after the task-ID grep, print the tail of the
   docs/status/README.md index (last ~10 rows) so sibling close-outs and their
   §d remedy patterns are visible at turn 1 (root-cause fix for the
   zero-artifact-stop class).
3. **FILED (BLOCKED: owner):** ruling — verify-only repeat-dispatch windows
   mint a stop artifact by default (row re-verify annotation + footer commit,
   the 02-10/04-07 pattern) vs chat-only stop until the owner asks; five
   recurrences cited (00-39, 02-01, 04-07, 04-46 §d1).
4. **PROPOSED (needs owner nod):** DONE-row annotation cap — pointer-style
   (date + window # + report path); full battery text stays in the cited
   report.
5. Score-cache TTL/eviction pass (row 165 — visible again after this window's
   prefix repair; internal/prioritize + queue stores).
6. E2e wire pin for derived usage in `tq show` (row 166; cmd/tq via
   scripts/test-cmd-tq.sh).
7. SessionMessageCount rendered-nowhere / sessionUsageEmpty blind-spot
   decision (row 167; internal/webui components.go).
8. CHANGELOG/FEATURES entries check for the 2026-09-21 usage-rendering work
   (row 168; docs-only).
9. Usage-pin parity for ReviewResult + StatusResult — RESTORED as a row this
   window after the 04-40 FILED claim proved unbacked (§d5); gated on the
   granularity ruling (04-40 §g1).
10. v0.3.0 pkg.go.dev render spot-check for the 7 facade modules (persistent
    404 = investigate).
11. Ruling: public `journal/cqrs` facade or internal-only (BLOCKED owner).
12. Enable `--prioritize` on the dogfood pool (BLOCKED: owner spend go/no-go).
13. Post-pilot calibration of the keyword table + marker ladder (BLOCKED:
    pilot start).
14. Owner-only: scope the minted gofmt stage to tracked files
    (`gofmt -l $(git ls-files '*.go')`) in `.tq-verify` + the mint template —
    the structural vendor/ gate kill (03-17).
15. Dead-letter forensic sweep of the gate-killed tasks (filed 03-17).
16. e2e `-race` vs the 180s stage-cap headroom (tracked row).
17. Queue-side dedup-key→COMPLETED short-circuit for re-dispatched DONE tasks
    (row 299; 3+ sibling windows of evidence).
18. Ruling: duplicate-claim re-delivery policy (BLOCKED owner; last row of the
    Priority-system section).
19. Ruling: daemon-fold attribution (BLOCKED owner; 4+ consecutive asks).
20. Run `./scripts/ci-local.sh` end-to-end on the current lineage before the
    next push (standing row).
21. Session E2E dogfood proof (BLOCKED: spends API money).
22. **FILED:** history-rewrite heals must re-run scripts/check-dead-sha-refs.sh
    in the heal window's own battery — the 00-27 msg-filter orphaned five
    cites and the gate rode red for ~9 hours until this window's report prep
    ran it and healed them (§d6; process row filed).

## g) Questions (up to 3)

1. **Stop-artifact default** (filed as row, §f3): should verify-only windows
   mint the row annotation + footer commit mechanically, or is chat-only stop
   acceptable until you ask for paper? Five recurrences of the current
   default's failure mode (00-39, 02-01, 04-07, 04-46, plus this window)
   say change it.
2. **Carried (4th+ ask): daemon-fold attribution** — footerless daemon
   commits continue to carry source ahead of the footer commit (93372d4 →
   efd9348 in this ID's own lineage). Amend unpushed daemon commits, teach
   the daemon a session footer, or ratify footer-commit-only attribution?
   (Row exists, BLOCKED.)
3. **The parity pin (§d5/§f9):** do you want the executor-level exact-key-set
   pin for all four result types, or is budget_test's parse-sum pin the
   designated all-four guard (then I close the restored row)? 04-40 asked
   first; restated because its row turned out never to have landed.
