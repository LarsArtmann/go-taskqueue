# Status report 2026-09-26 05-19 — task 000001a0dbb5f5eec2e3f7021239fd38e0e9

Verify-only window closing TODO row 110: "Check whether scripts/lint-lll-changed.sh excludes `*_templ.go`: generated long lines make every templ regen a latent gate trip (04-31 §f16)". Verdict: the exclusion ALREADY EXISTS and WORKS — zero code change. Row closed `[x]` with citations; work commit c460e758 (amended onto daemon commit eb48b416 while unpushed, footer attached). This file is the task's close-out turn: brutal a)–g) self-review.

## a) FULLY DONE

1. **Row-110 verdict, code-inspection leg**: the changed-lines line-length gate excludes generated templ files twice over — the awk guard `file !~ /_templ\.go$/` (scripts/lint-lll-changed.sh:35) skips any added line whose file ends `_templ.go`, and the header comment (scripts/lint-lll-changed.sh:8) documents the rationale ("templ fmt owns their formatting"). The diff pathspec `-- '*.go'` does match `*_templ.go`, so the awk filter is the operative exclusion — confirmed by reading the whole 45-line script, not the header alone.
2. **Row-110 verdict, probe leg (with control)**: at HEAD (base b1d6a7d9), a 200-char added line appended to internal/task/task.go trips the gate — rc=1, reported `internal/task/task.go:81: 203 chars` — while the IDENTICAL line appended to internal/webui/layout_templ.go stays green (rc=0, "no added line exceeds 120 chars"). Working tree restored after each case (`git restore`, files I modified myself in the same chain); `git status --short` clean after. The control case is what makes the templ green meaningful (see §d2 — the first, control-less run proved nothing).
3. **TODO_LIST row closure**: row 110 marked `[x]` with a VERIFIED annotation carrying the guard line number, both probe outcomes, and the two prior confirming windows (docs/status/2026-09-22_03-37_ci-toolchain-green-todolist-sweep.md closed it as "lint-lll verified already-excluded"; re-affirmed by the 11-30 brutal self-review). This window is the third independent confirmation.
4. **Gate battery at HEAD** (docs-only change; verify-window minimum battery): `check-todo-list.sh` rc=0, `check-doc-refs.sh` rc=0, root `go build ./...` + `go vet ./...` rc=0 (GOEXPERIMENT=jsonv2, GOTOOLCHAIN=auto per AGENTS.md; note vendor/ is currently ABSENT — trashed by the 05-02 row-109 window — and the root build is green without it, consistent with that window's post-trash proof). Fresh delta exists: the valid control+exclusion probe pair above. Race suite deliberately not run: zero Go code changed, no same-code prior battery needed.
5. **Attribution preserved**: the auto-commit daemon swept the TODO edit into footerless eb48b416 (05:18:30) before my commit ran (~4 min edit-to-daemon latency). Amended in place with the exact `Task-Queue-ID: 000001a0dbb5f5eec2e3f7021239fd38e0e9` footer AFTER verifying the commit was local-only (`git merge-base --is-ancestor HEAD origin/master` → "HEAD not pushed"; branch 117 ahead of origin). Amend kept the daemon's tree byte-identical (still 1 file, 1 insertion, 1 deletion — TODO_LIST.md only). Final commit c460e758, tree clean, nothing pushed.
6. **Session ritual**: session-start.sh with the task ID (no prior windows found for this ID — first dispatch), git log/status/stash over the whole repo, prior-report grep for the ID (empty).

## b) PARTIALLY DONE

1. **Turn-1 CONTRIBUTING.md read**: existence confirmed at turn 1 (`ls CONTRIBUTING.md`), content READ only during this close-out. The ritual's intent ("check for and read") was half-met; nothing in the file changes this window's verdict (docs-only, no push), but the miss is real — see §d1.
2. **TODO_LIST follow-up appends**: 3 new rows in a new section ("Row-110 verify-window follow-ups", file tail) — done during THIS close-out, not the work turn; they are appended but unbuilt by definition.
3. **Close-out completeness**: this report + README index row are being shipped in the same commit as the rows; the §g questions are asked but of course unanswered.

## c) NOT STARTED

1. **Mechanical self-test pin for the templ exclusion** (new row 1) — proposed, not built. Three windows have now manually verified the same one-line property; that is the signature of a missing pin, not a habit to continue.
2. **Row-110 checkbox-regression investigation** (new row 3) — how a twice-verified row resurfaced unchecked is unexplained; I could bisect `git log -p TODO_LIST.md` but deliberately did not scope-creep it into this window.
3. **AGENTS.md printf-quirk documentation** (new row 2) — first hit recorded in §d2; the PIPESTATUS precedent codified at the second hit, so this waits for the bar or an owner override.
4. **Everything else in the backlog** — rows 111–114 (status oracles const, commit-msg final-trailer gate, internal/task re-tag, ci-local end-to-end), companion extraction, and the rest: untouched by design. This window's contract was exactly one row.

## d) TOTALLY FUCKED UP

1. **CONTRIBUTING.md ls'd, not read** (work turn). The documented recurring miss (00-52 d4, 02-17 d3) — now a third occurrence of the same shape from this agent class: confirming a file EXISTS is not reading it, and the ritual says read. Blast radius this window: zero (docs-only task, no push, no contributor-facing surface touched). Blast radius in general: the file carries the push gate, templ/tailwind regen duties, and the harvester-parse constraint — any of which could have been violated unnoticed by a heavier window.
2. **First probe battery was invalid and nearly cited as evidence.** The fixture builder `printf 'x%.0s' $(seq 1 200)` fails in the mvdan session shell ("invalid format char: ." — this printf does not support precision on %s), so BOTH fixture lines were short `// ` comments and both runs reported rc=0. Without the embedded expectation echoes (`expect 1`, got 0) I would have "probe-proven" the exclusion with a probe that tested nothing — a fabricated gate citation, the exact failure class the claims-carry-citations rule exists to stop. Caught it only because the control case was EXPECTED to fail and didn't. Secondary finding: the two stderr errors surfaced AFTER later stdout echoes — output ordering in this shell is not reliable evidence of execution order.
3. **Daemon race handled reactively, not structurally.** I edited TODO_LIST, ran the full battery, then committed — leaving the edit uncommitted for ~4 minutes in a repo whose daemon commits within minutes. Recovery (amend on verified-unpushed HEAD) is documented, legal, and worked, but amend IS a history rewrite; committing the machine-consumed file immediately after its own format gate (check-todo-list rc=0) would have made the race unwinnable-by-the-daemon and avoided any rewrite. The 04-43 window made the identical mistake (§d of that report: "losing the work diff to three footerless daemon commits"). Twice in one morning on adjacent windows is a pattern, not bad luck.

## e) WHAT WE SHOULD IMPROVE

1. **No probe without a control.** An exclusion/allowlist probe is only evidence when the same fixture provably trips the gate in the non-excluded case. Default shape: negative control FIRST (expect FAIL), then the excluded case (expect PASS), same fixture string, one chain, restore between. This window's §d2 is the cautionary tale; new row 1 mechanizes it as a permanent --self-test.
2. **N manual verifications of one property = N-1 wasted windows.** The templ exclusion has now been verified by hand three times (09-22 sweep, 11-30 self-review, this window). check-transient-retry.sh already established the repo pattern (marker-guarded extraction + fixture, sub-second); porting it to lint-lll-changed.sh is ~15 lines and would have made this dispatch a 30-second cite-and-close.
3. **One session-shell hazard bullet, not one per quirk.** PIPESTATUS (codified), printf `%.0s` (this window), stderr/stdout ordering (this window, §d2) are the same class: agent-session probes whose failure mode is silent-green evidence. A single AGENTS.md bullet listing all three (with the `head -c N /dev/zero | tr` fixture recipe) beats three fragmented bullets that each understate the class — and avoids the split-brain of the same guidance living in evolving shapes.
4. **DONE-row durability.** session-start.sh already surfaces prior windows per task ID; the missing half is TODO-side: when a dispatched row's text already carries a DONE/VERIFIED annotation while unchecked, that contradiction should be LOUD at dispatch (check-todo-list warning or session-start flag), because it means either a write race (this window's regression, new row 3) or a stale annotation — both are cheaper to resolve at turn 1 than after a battery.
5. **Commit machine-consumed files at their own gate, not after the whole battery.** TODO_LIST edits need check-todo-list, not build+vet, to be safely committable. Front-load that gate for doc-only edits; the daemon sets the commit cadence whether we like it or not (§d3).

## f) NEXT (up to 50 — honest count: 3 new rows filed + 5 highest-leverage carried pointers; padding to 50 would be fabrication)

New rows appended this close-out (TODO_LIST tail, "Row-110 verify-window follow-ups" section):

1. Pin the lint-lll-changed.sh `*_templ.go` exclusion with a `--self-test` probe (control FAIL + templ PASS over a marker-guarded awk extraction).
2. Document the agent-session printf `%.0s` empty-fixture quirk in AGENTS.md next to the PIPESTATUS hazard.
3. Investigate the row-110 checkbox regression (twice-verified row resurfaced unchecked; git log -p TODO_LIST.md over 09-22..09-26; propose a mechanical guard if it is a write race).

Highest-leverage carried rows observed at dispatch (restated as pointers only — they own their own follow-ups):

4. Row 114: run `./scripts/ci-local.sh` end-to-end on the current lineage before the next push — with 117 unpushed commits on master, this is the single highest-leverage next window.
5. Row 112: commit-msg hook requiring the Task-Queue-ID footer in the FINAL trailer block — mechanical prevention of the footer-before-attribution mistake; directly relevant to this window's footer handling.
6. Row 113: re-tag internal/task + bump the root require so proxy consumers receive `task.AllStatuses()`.
7. Row 111: share the two hardcoded status oracles in internal/task status_test.go as one test const.
8. Companion extraction (TODO tail) per docs/planning/2026-09-26_companion-extraction-design.md — flips MIRROR_CLONES_STRICT=1 and retires the 31 mirror-clone groups.

## g) QUESTIONS (cannot self-answer)

1. **Re-verification policy for cited-DONE rows**: when a dispatched TODO row is unchecked but its text (or a prior report) already carries a DONE/VERIFIED annotation for exactly that work, should the queue treat re-dispatch as INTENDED re-verification (status quo — this window's cost: a full battery + probe, ~15 min) or should session-start/check-todo-list hard-flag it so the dispatcher cites-and-stops mechanically? I can build either; which is wanted is an owner call (§e4).
2. **Quirk codification bar**: AGENTS.md codified the PIPESTATUS hazard at the second hit. printf `%.0s` is a FIRST hit whose blast radius is the same class (silent-green probe evidence, i.e. potentially fabricated citations). Codify now as part of a combined session-shell-hazards bullet (§e3), or hold the two-hit bar?
3. **lll gate semantics near the boundary**: awk's `length()` counts bytes or characters depending on locale, while golangci's lll counts runes — the two gates can disagree on non-ASCII lines just under/over 120 (the repo's prose is full of `→`, `§`, `—`). Pin `LC_ALL=C` byte semantics (documented) or make the awk count runes for linter parity? Taste call, noticed in passing while reading the script; no probe run, no row filed pending the ruling.
