# Task 000001a0c0d6f2f74e46e4d524207638cccd — Verify-only third window on the budget token/cost projection

Window: 2026-09-21 ~01:00 – 01:07 CEST. THIRD dispatch on this ID. History: the
00-27 work window shipped the full item (close-out:
`docs/status/2026-09-21_00-27_task-000001a0c0d6f2f74e46e4d524207638cccd.md`);
the 00-39 window was the first verify-only re-dispatch
(`docs/status/2026-09-21_00-39_task-000001a0c0d6f2f74e46e4d524207638cccd.md`).
This window: verify → cite → stop, ZERO source edits.

## a) FULLY DONE

- **Turn-1 ritual via `scripts/session-start.sh` WITH the task ID**: the
  prior-report grep surfaced BOTH sibling windows before any edit; repeat
  dispatch identified pre-edit, zero wasted work. CONTRIBUTING.md head read
  (script output); no CLAUDE.md (ls'd).
- **Every implementation claim re-verified against code at HEAD (59d525f),
  file:line**:
  - `SessionUsage` internal/budget/budget.go:99; `Guard.UsageToday`
    internal/budget/budget.go:129 (value receiver — the first grep for a
    pointer receiver missed it; symbol check, not a code gap).
  - `FactsSince` contract internal/queue/queue.go:196 (interface method);
    sqlite internal/queue/sqlite/sqlite.go:1645; postgres
    internal/queue/postgres/postgres.go:1553.
  - Pins: `TestFactsSinceByType` internal/queue/sqlite/store_test.go:1745;
    `TestCheckExhaustedReasonCarriesUsage` internal/budget/budget_test.go:193;
    `TestStatsJSONSessionUsageContract` cmd/tq/stats_test.go:102.
- **TODO row 160 confirmed `[x]` in place** (TODO_LIST.md:160) with the
  00-39 re-verification annotation — no edit needed this window (the row is
  already annotated per the file's re-dispatch convention; re-annotating per
  window would grow the row without new information).
- **Fresh cheap battery at HEAD, rc captured directly** (file-redirect +
  `$?`, never PIPESTATUS):
  - root build rc=0 + vet rc=0 (GOEXPERIMENT=jsonv2)
  - `scripts/check-doc-refs.sh` rc=0; `scripts/check-todo-list.sh` rc=0
    (both in-battery this time — 00-39 §d4's skip is not repeated)
  - internal/budget suite, in-module `GOWORK=off`, build rc=0 + test rc=0
    (ok 0.009s)
  - internal/queue/sqlite `TestFactsSinceByType`, in-module `GOWORK=off`,
    targeted -run: PASS rc=0 (ok 0.005s)
  - `./scripts/test-cmd-tq.sh` rc=0 (ok 17.259s, incl
    TestStatsJSONSessionUsageContract)
- **Queue attribution re-verified**: all SEVEN prior-window commits carry
  exactly one well-formed footer (87fc781, 328732d, a5bc36a, 8d12eb0,
  2d5c01e, 6d1f4ef, 3ecccdd → this ID). This dispatch is again a
  HARVEST-RACED-THE-CHECKOFF delivery, third in ~25 minutes on one ID —
  fresh weight for the BLOCKED duplicate-claim ruling row (00-39 §g2).
- **Report commit shipped in one chain right after the last write** (the
  inverted order the two prior windows confessed to losing): write → add +
  commit with footer → gates. Gates ran after; amend only if forced.

## b) PARTIALLY DONE

- **Verification tier: CHEAP battery only** (the documented verify-window
  minimum, met: build+vet+doc-refs+todo-gate fresh at HEAD plus several
  fresh deltas). No race-suite re-run, no ci-local/nix/GOOS=windows/smokes/
  advisory-lint — permitted because zero source edits and HEAD is
  byte-identical to the fully-batteried 00-27 tree plus two docs-only
  commits (5b329a9 daemon fold, 59d525f fold record).

## c) NOT STARTED

- No code work, BY DESIGN (DONE-row re-dispatch). Everything under the
  00-27 §c ledger stays open and unchanged: ReviewResult/StatusResult usage
  derivation (TODO rows 161/162), executor-side json-key pin (row 163),
  webui budget-card token/cost line, `tq show` prioritize usage,
  completion-cost sparkline, calibration/G1 revisit, local postgres twin
  execution, full ci-local run, cap-unit ADR, duplicate-claim ruling.
- Nothing was silently dropped; the window's scope was verification.

## d) TOTALLY FUCKED UP

1. **A third dispatch exists at all** — this window is itself the systemic
   failure. Two prior windows closed the item (one shipping, one
   verifying); the queue minted a third inside the hour. The cheapest
   sufficient fixes are already on the table (00-39 §e2: row-state
   re-check at mint/claim time, or skip minting when the item's ID already
   has a docs/status report). Until that ruling lands, verify-only windows
   on this ID remain pure treadmill cost. No new individual mistake here —
   the process owns this one.
2. **Nothing else.** The battery was complete on the first pass (todo-gate
   included), rc's captured to files, in-module gates used, footer check
   done pre-stop, report committed before gates instead of after. The
   prior windows' lessons were applied; recording that they worked is part
   of the honest ledger.

## e) WHAT WE SHOULD IMPROVE

1. **The duplicate-claim ruling is now the single highest-leverage open
   item in this report family**: three windows in ~65 minutes on one ID,
   each costing a full ritual + battery + report. Every additional
   re-dispatch before the ruling is bought-and-paid-for waste.
2. **Verify-only report template**: this is the second consecutive
   verify-only window in the family; the structure (prior-report grep →
   claim re-citation → battery → footer check → one-chain commit) is now
   stable enough to be a script or skill rather than re-derived prose.
3. **What worked (keep)**: ritual-first with the task-ID grep (repeat
   dispatch caught pre-edit), rc-to-file capture, in-module GOWORK=off
   gates, todo-gate in the first battery, commit-before-gates one-chain
   discipline (the daemon race was finally WON — no fold this window).

## f) Up to 50 things we should get done next

Honest count: **0 new**; padding to 50 refused. Every thread this window
touched already has a backlog row or ruling slot: duplicate-claim fix shape
(00-39 §g2, BLOCKED on owner), cap-unit ruling (00-39 §g3 / 09-52 §g1,
owner-only), ReviewResult/StatusResult usage (TODO rows 161/162),
executor-side json-key pin (row 163), postgres twin + full ci-local (open
rows), verify-cheap script (carried). Appending duplicates would be the
exact mint-duplicates disease this family is suffering, in documentation
form. The 00-27 §f ledger (32 items) + 00-39 §f (8 items) are carried BY
POINTER.

## g) Questions (up to 3)

1. **Duplicate-claim re-delivery ruling (carried, BLOCKED on owner; now
   three data points in one hour)**: harvest keeps minting dispatches for
   rows already `[x]` with intact attribution. Pick the fix: (a) row-state
   re-check at mint/claim time, (b) harvest-side prior-report consult, or
   (c) ratify verify-only second windows as the standing shape. The cost
   of deciding is now empirically the dominant term.
2. **Cap-unit ruling (carried 09-52 §g1, owner-only)**: the projection
   MEASURES tokens/cost (`budget.session_usage`); should the daily CAP
   gate on them or stay task-count? Blocks Check semantics + webui card.
3. No third question — the remaining open threads (verify-only
   report-commit default, §g1 of 00-39) are covered by 1/2 above once the
   standing shape is ruled.
