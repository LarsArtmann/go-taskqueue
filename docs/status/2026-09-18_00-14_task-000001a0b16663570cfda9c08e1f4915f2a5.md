# Status Update — task 000001a0b16663570cfda9c08e1f4915f2a5 (2026-09-18 00:14)

Work item: "Stale-open session visibility in doctor/stats: sessions that began
and never closed; never auto-close without a ruling (03-28 §f9)".

## Executive summary

**The feature was already fully implemented in shipped code** — this window was
a verify-cite-stop dispatch that nobody recognized as such (the turn-1
`rg -l <task-id> docs/status/` grep found NO prior report for this ID, so the
done-row dedup short-circuit could not fire). I verified both surfaces end-to-end
(doctor check + stats row/JSON on a scratch DB, CLI test gate green), ticked the
TODO row with evidence, and committed. Total code delta: **one TODO_LIST.md line**.

## a) FULLY DONE

1. **Doctor stale-open session check** — pre-existing, verified live this window:
   `open-sessions` check in cmd/tq/doctor.go:535 via `session.List`; a stale-open
   session renders `WARN open-sessions  1 session(s) began and never closed —
   their close-out never ran; close explicitly with tq session close (doctor
   never auto-closes): <id> (opened <ts>, repo <path>)`. The never-auto-close
   ruling (03-28 §f9) is encoded in the check's own detail text, not just docs.
2. **Stats stale-open session count** — pre-existing, verified live:
   `tq stats` prints `open sessions     1 (began, never closed — tq session list)`
   and emits `open_sessions` in the `--json` payload (statsPayload,
   cmd/tq/main.go, OpenSessions field). Comment cites 03-28 §f9 explicitly.
3. **E2E verification on a scratch DB** (`TQ_DB=/tmp/tq-sess.db`, honoring the
   inherited-TQ_DB contract): `session begin --id testsess12345` → stats shows
   the open-sessions row → doctor WARNs with the id → cleanup. No production
   journal touched.
4. **Test gate**: full cmd/tq module suite green via `scripts/test-cmd-tq.sh`
   (9.6s) — includes session/stats/doctor tests.
5. **TODO row 111 closed** with DONE evidence + file:line citation (claims-carry-
   citations convention).
6. **Commit with footer**: the auto-commit daemon raced my explicit commit
   (known case); per the repo's amend maneuver the footer was amended into the
   daemon's unshipped commit → `2cb4e13 docs: verify and close stale-open
   session visibility TODO row`, `Task-Queue-ID:` footer verified present in
   `%B`. Not pushed.

## b) PARTIALLY DONE

1. **Turn-1 ritual, half-executed**: `git log/status/stash` + the task-ID grep
   ran correctly in batch 1 (the grep correctly found no prior report), but
   CONTRIBUTING.md was `ls`'d in batch 1 and only actually READ in batch 2 —
   the N+5-th recurrence of the documented read-at-turn-1 miss (00-52 d4,
   02-17 d3, and every report since). Reading it costs one command; batching it
   with an `ls` is how it keeps slipping.
2. **Full pre-push gate NOT run**: only the cmd/tq module gate + root builds.
   `./scripts/ci-local.sh` unrun this window (delta was one doc line, so risk is
   near-zero — but "near-zero" is exactly the 2026-09-16 lesson's opening line).
   58 commits now sit unpushed on master.
3. **Verification used a guessed flag first**: `session begin --note` failed
   (no such flag) — one wasted round trip before consulting `--help`. Small,
   but the pattern (guess, fail, then read) is the anti-pattern.
4. **Report format divergence**: user instruction asked for this `.md` file;
   the status-report skill's canonical format is HTML. Flagged per the skill's
   own rule, not propagated as a new default.

## c) NOT STARTED

1. Nothing from the work item itself — it shipped in an earlier window (per
   AGENTS.md session-bridge history, the doctor/stats surfaces predate this
   task's dispatch; the queue re-dispatched a DONE row with no done-row marker
   the dispatcher could see).
2. No follow-up items were appended to TODO_LIST.md — nothing NEW was
   discovered that isn't already a row (dashboard session lamp = existing row
   113; sweep scheduler = 02-21 report's top item).

## d) TOTALLY FUCKED UP

Nothing this window rose to fucked-up. The honest near-misses, ranked:

1. **The queue paid a full window for a DONE row** — the single biggest waste.
   The row sat unchecked-looking actionable but was shipped code; no
   `docs/status` report existed under this task ID (the work had ridden
   earlier footerless daemon commits per the 02-21 report's known pattern:
   "feature rode footerless daemon commits — only the TODO tick carries the
   footer"). The done-row dedup short-circuit proposed in the 03-35 report
   §f remains unbuilt, and this is now the Nth paid verify window.
2. **Daemon race burned one commit attempt** — `git add && git commit` hit
   "nothing to commit" because the daemon committed TODO_LIST.md between my
   edit and my commit. Recovered correctly via the documented amend maneuver
   (commit is unpushed, so no history fork), but the runbook-known race
   happened anyway: I did not re-check `git status --short` immediately before
   `git add` as AGENTS.md's commit-per-task lesson prescribes.
3. **Footer is not git-trailer-parseable**: `git log --format='%(trailers)'`
   on 2cb4e13 yields only `Assisted-by:` — the blank line + emoji block after
   `Task-Queue-ID:` terminates git's trailer block. Harmless today (the
   executor's GitLogScanner greps raw `%B`, which matches), but any future
   tooling using `%(trailers)` will silently miss the queue footer. The
   attribution block ordering (`💘 Generated with Crush` between footer and
   Assisted-by) is the cause.

## e) WHAT WE SHOULD IMPROVE

1. **Queue-side done-row dedup**: before dispatching, check whether the row's
   DONE evidence already exists (the 03-35 §f #1 proposal). This window is its
   strongest exhibit yet: zero code, full window, purely because the row still
   read `- [ ]` to the dispatcher.
2. **Footer-commits for verified work**: when an agent verifies-and-ticks a
   DONE row, the tick itself should be recognizable as terminal (row text or
   dispatch payload carries "shipped in <sha>" markers) so verify-cite-stop
   windows become seconds, not sessions.
3. **Turn-1 ritual as a script**: `scripts/session-ritual.sh <task-id>` running
   git log/status/stash + task-ID grep + CONTRIBUTING/CLAUDE read in ONE batch
   would kill the N+5 recurrence class outright (proposed in 02-27 §f, still
   unbuilt).
4. **Amend-maneuver discipline**: re-run `git status --short` in the same
   command as `git add` (`git status --short && git add …`) so the daemon race
   is caught before the failed commit, not after.
5. **Trailer hygiene in the commit template**: put `Task-Queue-ID:` as the
   literal last line (after the attribution block) OR drop the blank line so
   `%(trailers)` sees it — one-line template change, preserves the greppable
   footer either way.
6. **Unpushed-commit backlog**: 58 commits on master ahead of origin. Every
   report since 09-16 carries this warning; the risk compounds (a single bad
   push now moves 58 commits through push protection and CI at once).

## f) TOP THINGS TO GET DONE NEXT (sorted by impact; brainstorm, not commitment)

1. **OWNER: push master** (58 unpushed commits; every gate that matters ran
   green across the windows that produced them, but the ledger is large).
2. **Queue-side done-row dedup short-circuit** (dispatcher consults
   TODO_LIST `[x]` + docs/status index before minting a window).
3. **Session-ritual script** killing the turn-1 miss class (N+5 and counting).
4. **Sweep scheduler**: `tq session sweep` exists (02-21) but NOTHING runs it
   on a cadence — wire it into agent-pool ticks or a systemd timer (owner
   ruling on cadence/budget pending, 02-21 §g).
5. **webui session lamp + session-fact counts** (existing TODO row 113) —
   natural companion to the doctor/stats surfaces this task verified.
6. **TODO row 112 session forensics** (CloseDetail records
   AllowDirty/RequireClean; `tq facts --type session.opened,closed`; `tq show
   session:<id>` graceful degradation).
7. **TODO row 114 session-bridge safety pins** (prune-stale provenance guard,
   namespace cross-dedup, trailer-parser edge tests).
8. **Stats open-sessions hint update**: text says `tq session list`; with the
   sweep shipped, consider `tq session list` → list + sweep mention, or a
   `--stale` threshold (see §g Q3).
9. **Footer trailer template fix** (§e item 5) — one line, do it in the next
   footer commit.
10. **CHANGELOG**: no row was added this window (doc-only tick — correct call),
    but the session-bridge features verified here (doctor check, stats count,
    sweep) may deserve a CHANGELOG line if they never got one; audit.
11. **`tq stats` alignment nit**: the open-sessions row uses `%5d` vs other
    rows' `%6d` — pad consistently (cosmetic, noticed in passing output).
12. **ci-local full run** before the next push (the standing §b item of every
    recent report).
13. **Postgres parity smoke for the session bridge** (AppendFact conformance
    shipped 4f4bd62; a smoke exercise remains unbuilt — 03-49 §g item).
14. **Budget routing for session-close minted tasks** (OQ2, 02-21 §g) —
    close is enqueue-only by design but mints review+status tasks; owner
    ruling on which budget pays.
15. **Hook script wiring**: hook-session-registry.sh is shipped but unwired
    (owner-gated crush config change); document the install step or a
    bootstrap flag.
16. **Registry bounding**: session registry JSONL grows unbounded between
    sweeps — cap or compact on sweep.
17. **pgrep ownership check is substring-not-pid** (02-21 §b) — tighten to a
    pid match or document the false-positive tolerance.
18. **Done-row DONE-annotation convention**: require every `[x]` tick to carry
    `— DONE <date>: <evidence>` (this window's tick follows it; enforce via
    check-todo-list.sh) so future dispatchers can dedup mechanically.
19. **`docs/status/README.md` index**: add this report's row (done in the same
    commit as this file — verify the hook/daemon didn't drop it, the known
    silent-loss triangle).
20. **Retire `/tmp` scratch binaries** (`/tmp/tq-verify`) — trivial hygiene;
    E2E smokes should build into a session-scoped mktemp dir.

(Stopped at 20 honest items rather than padding to 50 — items 11-20 are
already micro-scale; the remainder of a 50-list would be ROADMAP fuel, not
actionable rows. Precedent: the 09-16 12-03 and 09-17 03-35 reports refused
padding for the same reason.)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Push sanction**: 58 commits sit unpushed on master (multiple windows'
   work, gates green per-window, full ci-local unrun as a batch). Push now,
   run ci-local first, or wait for a scheduled push window?
2. **Is `tq session sweep`'s quiet-close "auto-close without a ruling"?** The
   03-28 §f9 ruling says never auto-close; the sweep (02-21) closes
   quiet+unowned sessions after ownership checks. If the sweep is sanctioned,
   doctor/stats wording ("close explicitly with tq session close") should
   mention it; if not, the sweep needs a re-examination. This is a policy
   boundary only the owner can draw.
3. **Done-row dedup: dispatcher-side or row-side?** Should the queue mint
   nothing for rows whose text carries `— DONE` (dispatcher change, out of
   this repo), or should harvested DONE rows simply leave TODO_LIST.md
   immediately (row-side, but loses the audit trail)? The queue has now paid
   4+ windows on shipped rows.

---

_Point-in-time snapshot; superseded by any later report. Written as .md per
explicit user instruction (skill default is HTML — not propagated)._
