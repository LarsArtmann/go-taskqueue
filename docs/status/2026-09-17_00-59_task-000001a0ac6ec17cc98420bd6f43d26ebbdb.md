# Close-out: AGENTS.md claims-with-citations convention (filter-scope/behavior claims)

**Task**: `000001a0ac6ec17cc98420bd6f43d26ebbdb`
**Window**: 2026-09-17 00:5x, single dispatch, docs-only.
**TODO row**: 100 (`TODO_LIST.md:100`), section "Round-13 planning additions" — marked `[x]`.

## a) FULLY DONE

- **Convention shipped**: the existing "Claims carry citations" bullet in
  `AGENTS.md:453-461` now requires that filter-scope/behavior claims (what a
  view, filter, or code path displays/restricts) carry **file:line or a
  pinning test name** in reports and handoffs, in addition to the existing
  gate-run/commit-SHA citation requirement. Motivating case cited inline:
  the 2026-09-12 01-47 §d3 budget-display wrong-scope answer
  (source: `docs/status/2026-09-12_01-47_per-project-ui-question.md`,
  verified on disk before citing it — the path in the TODO row's §d3
  reference was abbreviated; I resolved and used the full filename).
- **TODO row closed**: `TODO_LIST.md:100` ticked `[x]` via exact-line
  `sed` (verified by grep after).
- **Landed in commit `c888be6`** carrying exactly one well-formed
  `Task-Queue-ID:` footer (verified: `git log -1 --format=%B | grep -c` = 1).
- **Verification**: root-module gate green at HEAD
  (`GOEXPERIMENT=jsonv2; go build ./... && go vet ./... && go test ./...`,
  all packages ok) + `scripts/check-doc-refs.sh` ("doc refs ok") — both
  appropriate for a docs-only change.

## b) PARTIALLY DONE

- **Verification depth**: I ran the ROOT-module gate only, not the
  per-sub-module loop or `ci-local.sh`. For a 6-line docs diff this is
  proportionate (no Go source touched), but the full pre-push gate remains
  owner/next-window territory (branch is ~15 commits ahead of origin; pushes
  are owner-run).
- **Scope of the convention**: the bullet covers reports and handoffs, but
  nothing mechanically enforces it (no gate can grep judgment). It is a
  prompt-level convention only — same enforcement class as the rest of the
  claims-citations rule (by design; noted, not a defect).

## c) NOT STARTED

- Nothing from THIS item. No follow-up work was discovered that belongs in
  the backlog beyond what §f lists (none of it is new scope invented for
  this task).

## d) TOTALLY FUCKED UP (near-misses, honestly)

1. **CONTRIBUTING.md not read at turn 1** — the codified turn-1 ritual
   (AGENTS.md "Session-start ritual", turn-1 additions) requires checking
   `CONTRIBUTING.md`/`CLAUDE.md` first; I ran the task-id prior-report grep
   and TODO context read but skipped CONTRIBUTING.md. This is the documented
   N+2-th recurrence of that miss (see 2026-09-16_04-21 §d). Mitigation:
   docs-only task, no contract interaction — but the ritual is not
   conditional on task type.
2. **Daemon out-committed me (again)** — my explicit `git commit` found a
   clean tree: the auto-commit daemon had swept AGENTS.md + TODO_LIST.md
   into footerless `chore: auto-commit` (`1537fd2`) between my edit and my
   commit. Recovered via the sanctioned unpushed-daemon-commit amend
   maneuver (footer added, exactly-one-footer verified). This is at least
   the 4th documented daemon-race of this shape; it cost one failed commit
   round-trip.
3. **First AGENTS.md citation was path-imprecise** — I initially wrote the
   source as `docs/status/2026-09-12_01-47` (no extension); caught it
   myself before commit and replaced with the full filename. The new
   convention I was shipping would flag my own first draft.

## e) WHAT WE SHOULD IMPROVE

- **Mint-time done-check** (standing systemic item): this task was first
  dispatch and NOT done-on-arrival, so it dodged the re-dispatch loop — but
  the loop's fix (harvester checks `[x]` at mint time) remains unbuilt and
  keeps burning paid laps on other IDs (three same-ID repeat close-outs
  landed in the last hour alone, per the index rows 00-22/00-33/00-37/00-50).
- **Daemon-vs-explicit-commit arbitration**: a pre-commit hook that detects
  "staged files just landed in an unpushed daemon commit with no footer"
  and amends automatically would remove the manual maneuver (and its
  failure modes) entirely.
- The convention extension itself is the improvement this task shipped;
  adoption is now on every future report/handoff (including this one — all
  claims above carry file:line, test/commit, or gate citations).

## f) What to get done next

Honest list, no padding (up to 50 requested; 16 real items below — the
repo's standing backlog in TODO_LIST.md already carries the rest):

1. Owner: push master (branch ~15 ahead; check-ci gate on ci-local requires
   master-green context anyway).
2. Mint-time `[x]` done-check in the harvester (top systemic burn).
3. Daemon footer-arbitration hook (§e above).
4. Turn-1 ritual enforcement: a hook or prompt-side gate that fails loud
   when CONTRIBUTING.md wasn't read (recurring miss class).
5. Consider extending claims-citations mechanically where cheap: a
   rename-hygiene-style advisory scanner flagging DONE rows in reports
   lacking any `file:line`, test name, or SHA citation.
6. Round-13 row 1 (SystemNix GOEXPERIMENT env) still BLOCKED on owner sudo —
   5-window burn continues until the unit env is fixed.
7. Round-13 row 3 (webui UX rulings: unknown-project 404 vs empty-state +
   budget display) still BLOCKED on owner calls — now doubly relevant since
   the new convention makes such claims citable once ruled.
8. Index bloat: live row count keeps growing (~10/day); the next ANNOTATE
   archive sweep targets the 2026-09-15 morning re-dispatch triplets.
9. nix vendorHash runner divergence (iQv0f2 vs dtC0Y6, 2026-09-16_12-03
   report) — still the sole red CI job, owner-grade pin/mirror fix.
10. Append a one-line pointer in docs/DOMAIN_LANGUAGE.md or CONTRIBUTING.md
    to the extended citation convention so non-AGENTS readers find it.
11. `tq audit --journal` run against the production journal (standing
    advisory, secrets-evidence half) — unchanged priority.
12. Re-check in ~2 weeks whether the filter-scope citation convention
    actually appears in reports (adoption audit, one grep).
13. Consider citing the convention from the review-prompt quoting rules
    (reviewers judge footer/contract claims; scope claims would benefit
    from the same file:line bar).
14. The 01-47 §g1/§g2 owner UX rulings (row 98) overlap row 7 — consolidate
    the two rows into one on the next TODO_LIST pass.
15. Session-close bridge open items (postgres parity, budget bypass) remain
    documented-open in AGENTS.md — no change this window, still open.
16. Nothing else observed in passing this window: tree was clean at start
    beyond the daemon's own traffic; no concurrent-edit conflicts hit my
    two files.

## g) Questions I cannot figure out myself

1. **Enforcement depth for the new convention**: is a prompt-level
   convention sufficient (my assumption), or do you want the advisory
   scanner (§f-5) built so filter-scope claims without citations become a
   visible (non-gating) finding?
2. **Daemon-race policy**: when the daemon beats an explicit footer commit
   (this window, again), is amend-the-daemon-commit still the sanctioned
   maneuver, or should the rule flip to "always leave the daemon commit and
   add an empty footer commit on top" (the 9648a19 precedent used by the
   04-21 window)? I chose amend because the commit was unpushed and mine
   were the only changes in it — but the two precedents now disagree.
3. **Should the claims-citations rule apply to the close-out reports
   themselves mechanically** (i.e., a review/verdict gate that rejects a
   close-out whose §a rows lack any citation pattern), or is self-compliance
   plus spot adoption audits (§f-12) the intended end state?
