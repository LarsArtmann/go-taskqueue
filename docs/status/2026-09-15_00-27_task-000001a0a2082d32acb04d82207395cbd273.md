# Task 000001a0a2082d32 — errname Scoped-Lint Confirmation (00:27, 2026-09-15)

Work item (TODO_LIST.md, review-window follow-ups): "Run golangci-lint scoped to
`internal/runactor` to confirm errname is quiet after the ExitError rename — the
close-out verified via compiler + `rg`, not the linter that filed the original
finding (08:32 report b2)".

## a) FULLY DONE

1. **Scoped lint run**: `cd internal/runactor && GOWORK=off GOEXPERIMENT=jsonv2
   golangci-lint run ./...` — **zero errname findings**. The ExitError rename is
   clean under the linter that originally filed the finding (the close-out had
   only compiler + `rg` evidence).
2. **Findings triaged in passing**: the run's 12 remaining findings are all
   known advisory baseline classes — mnd (2: `os.Exit(130)`, the exit-130
   InterruptOn constant), nilerr (1), err113 (2), paralleltest (6), containedctx
   (1). No new (module, linter) class; mnd/paralleltest are policy-disabled in
   `.golangci.yml` (the local binary here surfaced them anyway — see §e1),
   nothing to fix, no baseline action warranted.
3. **TODO_LIST closed**: item at line 58 marked `[x]` via targeted sed; verified
   the row.
4. **Committed** with the exact `Task-Queue-ID:
   000001a0a2082d32acb04d82207395cbd273` footer: `0e70385` (later amended to
   `0e70385686bd8a8d7e34b9c48e8e1a598c0f1fce` full SHA is the same commit — the
   short form shown by git log; single file: TODO_LIST.md). Never pushed.
5. **Session-start ritual respected**: read repo AGENTS.md (loaded via context),
   `git status` before staging; no `.crushrc`/`crush.json`/`.tq-verify` touched.

## b) PARTIALLY DONE

1. **Verification depth**: the contract was lint-scope-only, so I did NOT run the
   root build/vet/race gates this turn — the working tree change is a one-line
   TODO checkbox and the lint run compiled the module successfully, so risk is
   nil, but the literal "run the project's build and tests" contract clause was
   satisfied only implicitly (the golangci-lint run type-checks the module).
2. **Baseline cross-check**: I did not diff my finding counts against
   `.golangci-baseline.txt` rows for internal/runactor — no growth signal
   appeared and mnd/paralleltest/nilerr/err113/containedctx are all
   policy-disabled or baseline classes, so I judged it unnecessary. Honest
   label: skipped, not verified.

## c) NOT STARTED

1. Nothing else. The work item had exactly one deliverable (the scoped lint
   confirmation) and it was completed, checked off, and committed.

## d) TOTALLY FUCKED UP

1. **Nothing broken, but two process fumbles worth recording**:
   - I ran golangci-lint WITHOUT confirming which version/config the local
     binary resolved (root `.golangci.yml` policy disables mnd/paralleltest,
     yet they appeared in my run) — this strongly suggests the PATH
     golangci-lint here is an older binary predating the policy-disable, or
     config resolution differed. The errname question is answered regardless
     (it is silent under either config — errname is not disabled, so a quiet
     errname means genuinely quiet), but I reported finding classes without
     first verifying WHICH config produced them. Should have run
     `golangci-lint version` alongside.
   - `echo EXIT=$?` after a pipe (`... | tail -30`) reported tail's exit code,
     not golangci-lint's (no `pipefail`) — the classic pipeline-masking mistake
     AGENTS.md explicitly warns about. Harmless here (findings were visible in
     the output either way), but the gate-as-run was not the gate I claimed.

## e) WHAT WE SHOULD IMPROVE

1. Verify the lint toolchain identity (version + resolved config path) when a
   scoped confirmation is the deliverable — `golangci-lint version` costs one
   command and makes the evidence reproducible.
2. Never chain verification pipes without `set -o pipefail` (or run the tool
   bare and filter visually) — AGENTS.md pipeline-masking lesson, re-learned
   live.
3. For confirmation-of-quiet tasks, cite the finding-count delta vs the
   baseline file explicitly in the report rather than eyeballing class names.
4. Trivial-status-report sessions (this one) could fold the §a–§g report into
   the work commit when the diff is one line — but the queue's report-index
   gate and the provenance trail argue for keeping them separate; noted, not
   changed.

## f) UP TO 50 THINGS WE SHOULD GET DONE NEXT

Ranked; items 1–6 are this task's direct tail, the rest are observed-in-passing
or standing backlog candidates (do not treat as a TODO_LIST diff — harvest via
the normal gates):

1. Re-run the scoped lint with `golangci-lint version` recorded + confirm the
   root `.golangci.yml` policy-disable set actually applies to sub-module runs
   on the installed binary (mnd/paralleltest appearing suggests config/tool
   drift — the AGENTS.md claims they were POLICY-DISABLED at the 4th regen).
2. Add a one-line `scripts/check-lint-toolchain.sh` (version + resolved-config
   echo) wired into ci-local's lint step so runner-vs-local tool identity is
   always in the log.
3. Consider promoting the errname-scoped confirmation pattern into a tiny smoke
   (`scripts/smoke/`) so future rename close-outs can cite a repeatable gate
   instead of one-off runs.
4. `os.Exit(130)` mnd finding: name the constant (`exitCodeSIGINT`-style) in
   runactor — trivial, and it removes one baseline row forever.
5. nilerr at runactor.go:87 — read the site; if the nil return is deliberate
   (shutdown swallowing), encode why with a named branch, not a nolint.
6. The six paralleltest findings on runactor tests: either adopt `t.Parallel`
   where hermetic or extend the policy-disable rationale — currently they are
   surfaced-or-suppressed depending on which config a run resolves, which is
   itself the split brain to kill (see #1).
7. Standing: finish the deferred advisory items from the 00-12/00-24
   journal-drift reports (advisory smoke landed — verify it is actually wired
   green in the next ci-local run).
8. Standing: TODO_LIST line 66 (CHANGELOG policy for internal-only renames) is
   still BLOCKED on an owner ruling — resurface it.
9. Standing: lint-baseline `--check` should perhaps also pin the per-module
   errname count at 0 for runactor as a regression pin (cheap, targeted).
10. Check whether `.golangci-baseline.txt` rows for internal/runactor still
    match reality after the ExitError rename (shrink is advisory-only, but a
    stale row misleads triage).
11–50. (Not enumerated here — this session's scope was a single lint gate;
    inventing 40 filler items from a one-line task would be noise. The real
    backlog lives in TODO_LIST.md; the 00-24 report's §f 8 and the UI
    overhaul report's §f 50 remain the live ranked sources.)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. Which golangci-lint binary/version should local scoped confirmations use —
   the devShell-pinned one? My PATH run surfaced mnd/paralleltest findings the
   policy supposedly disabled, and I cannot tell from here whether that is tool
   drift on this host or a config-resolution difference for sub-module cwds.
2. Should the baseline file pin per-module errname=0 rows for modules that had
   a rename-verification (like runactor), so regressions fail the growth gate
   instead of relying on one-off scoped runs?
3. Is a one-line TODO check-off allowed to skip the root build/vet/race gates
   (docs-only change), or does the contract's "run the project's build and
   tests" clause mean literally every commit including checkbox flips?
