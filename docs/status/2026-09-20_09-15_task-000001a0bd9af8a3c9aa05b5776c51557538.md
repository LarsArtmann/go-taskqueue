# Task 000001a0bd9af8a3c9aa05b5776c51557538 — Status Report 2026-09-20 09-15

**Window type:** FOURTH dispatch of this ID, verify-only re-dispatch (verify →
cite → stop). Zero source delta. Row 157 (webui priority provenance) shipped by
the 09-06 window via footer commit 459e3da; two verify windows followed (09-10
close-out 849c1c8, third window bb2ef4a).

## a) FULLY DONE

- The deliverable itself (09-06 window, 459e3da): static priority-provenance
  card on the task detail page — current priority + band, harvest item key +
  marker level, cached AI verdict, repri history from the fact trail
  (internal/webui/render.go, mirroring `tq show`'s buildPriorityProvenance,
  cmd/tq/main.go:2076). Row 157 `[x]` at TODO_LIST.md:157. Nothing re-shipped.
- Fresh battery at HEAD this window, exit codes captured directly:
  - root `go build ./...` + `go vet ./...` under GOEXPERIMENT=jsonv2 — rc=0
  - `go test ./internal/webui/ -count=1` full suite — rc=0, ok 10.090s
  - `go test ./internal/webui/ -run Provenance -count=1` — rc=0, ok 0.006s
  - `./scripts/check-doc-refs.sh` — rc=0
- Turn-1 ritual: `scripts/session-start.sh <task-id>` rc=0 (ID grep hit on the
  prior reports); whole-repo git log/status clean at bb2ef4a; CONTRIBUTING.md
  already read in full by the third window at the same HEAD (74/74) — inherited
  per the same-HEAD rule, not re-cited as fresh.

## b) Fresh delta or why none exists

No source delta exists to report: HEAD (bb2ef4a) touches no code since
459e3da, `git status` clean at window start. This window's output IS the fresh
battery above plus this report and its index row; the feature tests re-ran
green at HEAD under the current toolchain.

## f) Honest omissions

Race suite and nix build not re-run this window — the third window's
scope declaration stands and no code changed since. Re-dispatch suppression
for a four-times-minted closed ID remains an owner surface (carried §g).

## g) Open questions (carried, unchanged)

1. Re-dispatch suppression: should the queue stop minting this ID outright?
2. Verify-window commit policy for zero-delta re-checks.
3. Zero-delta battery bar (promoted to convention 2026-09-20, still owner-gated).
