# Task close-out: golangci-lint version-pin unification (TODO row 63)

- **Work item**: "Unify the golangci-lint version pin (duplicated in ci.yml and ci-local.sh) into one source so the two can never drift (08:47 report f22/f23)"
- **Verdict**: DONE-ON-ARRIVAL no-op — the unification already shipped in 550fec1 ("lint pin dedup"); this session re-verified it fresh and closed the TODO row.

## a) FULLY DONE

1. **Fresh verification of the unified pin**: `grep -oE 'golangci-lint/v2/cmd/golangci-lint@v[0-9.]+' .github/workflows/ci.yml` resolves to exactly one hit — ci.yml:82, `@v2.13.2`. The grep ci-local.sh:96 runs (its fallback install path) derives the same `v2.13.2`. The two can no longer drift because ci-local owns no literal.
2. **Repo-wide pin sweep**: grepped `.github/`, `scripts/`, `flake.nix`, root `*.md` for `golangci-lint@`/`2.13.2`. Exactly one literal pin (ci.yml:82); ci-local.sh's single hit is the grep-derivation line itself; fuzz.yml has none; lint-annotations.sh references the version only in a comment. No third copy anywhere.
3. **TODO row 63 closed** with evidence inline (ship commit 550fec1, re-verify date, single-source location): TODO_LIST.md row 63 now `[x]`.
4. **Gates run fresh**: `GOEXPERIMENT=jsonv2 go build ./...` + `go vet ./...` green; `./scripts/check-todo-list.sh` green ("no unblocked owner-gated items"); `bash -n scripts/ci-local.sh` green (script untouched, but the gate that reads ci.yml's pin parses it).
5. **Commit**: 620023a `docs(todo): mark golangci-lint pin unification done (already shipped in 550fec1)` with the task footer; not pushed (per contract).

## b) PARTIALLY DONE

Nothing — the item was binary (pin unified or not) and it is.

## c) NOT STARTED

The work item itself (as originally written) was already done before pickup; no new engineering was needed. All other TODO rows untouched (scope discipline).

## d) TOTALLY FUCKED UP

Nothing. Honest near-misses declared:

1. **No code change means no test exercised the fallback path**: I verified the grep derivation as a shell one-liner, but never actually ran ci-local.sh's install branch (golangci-lint IS on PATH here, so `lint()` takes the pre-installed branch). If the grep regex ever stops matching a future ci.yml edit (e.g. a wrapping line break inside the go install invocation), the fallback would install `@unknown` — a degraded, not broken, state, but the drift-protection's whole point is the install path. Mitigated by: the regex is anchored on the single-line `go install …@v[0-9.]+` shape, and the pin sweep confirms one clean hit. Risk: low; not falsified, just not exercised.
2. **`grep -c` sweep used `2>/dev/null` on a glob that could miss files**: harmless here (all listed files existed and counted 0), but a silent-zero is the pipeline-masking pattern this repo keeps flagging. The positive assertion (one exact hit in ci.yml) carried the proof; the zero-counts were corroboration.

## e) WHAT WE SHOULD IMPROVE

1. **The pin's single source is a grep over a YAML file — brittle by design.** A `scripts/lint-pin.sh` (or an env/config constant sourced by both ci.yml is impossible, but ci-local.sh + lint-baseline.sh + any future consumer) would give the derivation one named home with a test. Cheapest robust version: a tiny test or smoke asserting `grep` finds exactly one match, so an innocent ci.yml edit that adds a second mention fails loudly instead of `head -1` silently picking.
2. **Done-on-arrival pattern keeps recurring** (this is at least the third: for-each-module extraction d919e19/6d4436b, varnamelen re-dispatch 01-51/01-54 reports, now this). The queue harvests TODO rows from close-out §f lists faster than agents pick them up; a stale-row check at pickup time (grep the ship evidence before doing work) would save a session per row. The harvester can't do it (row 68 is BLOCKED on the owner process call) but each agent can — this report records that I did it here.
3. **TODO row 63's phrasing said "duplicated in ci.yml and ci-local.sh"** — by pickup time it wasn't anymore. Rows should carry the report section that minted them plus a "verify before work" norm; rows 61-63 in this section all needed the same no-op dance.
4. **The fallback install message prints `${pin:-unknown}`** — good — but doesn't fail the step when the pin is unparseable; it installs `@unknown` and lets `go install` error. An explicit `[[ -n "$pin" ]] || { echo FAIL; exit 1; }` inside the advisory block would turn silent degradation into a loud advisory failure (still not a gate, but visible).

## f) NEXT (up to 50, sourced from this task's visibility + passing observations)

1. Add the exactly-one-match pin assertion (grep count == 1) as a smoke or ci-local step — closes §e1.
2. Fail the ci-local fallback install loudly when the derived pin is empty (§e4).
3. CI topology one-pager (TODO row 64) — directly adjacent, same 08:47 report.
4. `golangci-lint config verify` in ci-local (TODO row 80).
5. Owner: changelog policy for non-release-boundary changes (row 66, BLOCKED).
6. Owner: CI-time budget number for module loops (row 67, BLOCKED).
7. Owner: TODO-item accuracy bar / harvest-time verification (row 68, BLOCKED).
8. Report the session-snapshot ghost upstream (row 65).
9. Consider vendoring the pinned golangci-lint into flake.nix so hermetic gates stop depending on PATH luck (the 00-53/01-01 reports flagged unpinned local linter identity).
10. Record which golangci-lint version is canonical in AGENTS.md's golangci section (standing §g1 from the 00-27 report; partially implied by ci.yml:82 now — one sentence would close it).
11. Re-check `.golangci-baseline.txt` rows for internal/runactor after the ExitError rename (00-27 report follow-up 10).
12. Verify root `.golangci.yml` policy-disables actually apply to `GOWORK=off` sub-module runs (00-33 report: mnd/paralleltest surfaced despite policy) — this task's §d1 lives in the same ambiguity.
13. Follow-up 1 from the 01-33 report: pin unification ✅ (this task) — its siblings 2-5 from that list are still open.
14. lint-annotations.sh runtime: 16 sequential passes (01-15 report §f3) — parallelize or cache.
15. Check golangci-lint v2.14+ for a native github-actions output format (recurring §f item, 01-15/01-18 reports).
16. `.golangci-baseline.txt` regen-procedure comment (00-48 §f34).
17. gochecknoglobals facade exclusion: per-file list → path-glob (00-48 §f35).
18. Scoped-lint config resolution root-cause note in AGENTS.md (00-33 §f1).
19. Sweep other grep-derived single-sources in scripts/ for the same head-1 fragility class (this task's pattern generalized).
20. Add a "verify-ship-before-work" line to the queue's agent prompt contract or AGENTS.md session ritual (§e2) — cheapest done-on-arrival killer.
21. Baseline shrink round on the 500-finding sea (standing, policy-change-owned).
22. gosec exclude-list parity check: `.golangci.yml` vs ci.yml env (AGENTS.md claims they match; a smoke would pin it).

(22 items; the remaining 28 would be padding — the honest list from THIS task's vantage is these.)

## g) QUESTIONS (cannot self-answer)

1. **Owner**: is the `head -1` grep derivation acceptable as the permanent mechanism, or should the pin move to a dedicated file (e.g. `.golangci-lint-version`) that ci.yml reads via a shell step and scripts source directly? (§e1 — I can build either; the choice is policy.)
2. **Owner**: for advisory-only steps like the lint fallback, should silent degradation (`@unknown` install) be allowed, or should every unparseable-pin path fail loudly even inside `continue-on-error` blocks? (§e4.)
3. **Queue/harness**: row 68 (harvest-time item verification) is BLOCKED on the owner, but done-on-arrival keeps costing sessions — would you accept a contract change where any agent finding a row already shipped closes it with evidence WITHOUT re-running full gates (this task ran build/vet/TODO-gate; arguably more than a no-op needed)?
