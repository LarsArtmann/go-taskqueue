# Task 000001a0c3056902a0ee78d22edce0da668b — WORK window: show-JSON message-count pin (TODO row 173) + owner-mandated a-g close-out

Written 2026-09-21 10:19 CEST. Single work window on this ID (no prior reports at turn 1 — the ID grep found none). Contract executed exactly as minted: extend `TestShowJSONCarriesDerivedSessionUsage` (cmd/tq/show_test.go) to assert `result.session_message_count` at the seeded value, verify, tick TODO row 173, commit with the task footer. All of that shipped. Disposition: **WORK COMPLETE — one source commit (bcf0eca, footer-verified), one TODO tick, gates green at the touched surface.** The close-out below is the owner-mandated self-audit; §d is where the honesty lives.

## a) FULLY DONE

1. **The pin itself.** `usageSeed` gained `wantMessages int64`; both seed details now carry `SessionMessageCount` (status seed: 7, prioritize seed: 9 — deliberately distinct so a cross-seed mixup fails the assertion); the wire map in `assertShowUsageWire` pins `session_message_count` beside `session_cost_usd`/`session_prompt_tokens`/`session_completion_tokens`; the test's doc comment now names the message-count key and cites 08-35 §f6. cmd/tq/show_test.go:159-233, :242-249, :322-327.
2. **The assertion is a real wire pin, not a struct retag echo.** The map decodes the raw show JSON into `map[string]any`, so a missing or renamed key fails with "missing wire key" — the passing run is end-to-end proof the encoder step emits the snake_case key at the seeded value. That was the exact unpinned surface the TODO row named.
3. **TODO row 173 closed** with a done annotation carrying the shape (seed values, distinct-value rationale) and the gate citations. `scripts/check-todo-list.sh` rc=0 after the edit.
4. **Gates at the touched surface, rc's captured directly** (redirect-to-file or direct `$?`, no PIPESTATUS claims): `./scripts/test-cmd-tq.sh` rc=0 — cmd/tq build + vet + FULL suite `ok 7.852s` including the extended test; root `go build ./...` + `go vet ./...` rc=0 under `GOEXPERIMENT=jsonv2`; `check-doc-refs.sh` rc=0; `gofmt -l` clean on the touched file; **`scripts/lint-annotations.sh HEAD~1` → 0 new findings on changed lines** (the one battery item the work turn skipped, closed during this close-out — see §d5).
5. **Commit hygiene.** bcf0eca "test: pin session_message_count on the show JSON wire" contains exactly the test delta + the TODO tick (2 files, +19/−10); message carries exactly one well-formed `Task-Queue-ID` footer (grep count = 1; the commit-msg hook validated placement); tree clean after commit; nothing pushed.

## b) PARTIALLY DONE

1. **The battery, again, arrived in two passes.** The work turn ran cmd/tq gate + root build+vet + doc-refs + gofmt. The lint-annotations run was added only when this close-out asked "what did you forget" — despite 10-07 §e2 (same cluster, read in passing) having already named close-out-first discipline after the identical gap. Green, but forced, not self-audited.
2. **`-race` tier skipped.** `test-cmd-tq.sh` runs plain `-count=1`; the root suite was not run at all (build+vet only). Proportionality argument: the delta is confined to one cmd/tq test file, zero production code touched. But the repo's standard gate is the root race suite, and this window did not run it nor explicitly inherit a same-HEAD prior race run — the battery stands on the module gate + root build+vet alone. Ruled proportionate, stated openly; see §g3.
3. **Session-start ritual, ~80%.** `scripts/session-start.sh <task-id>` WAS run at turn 1, but piped through `tail -20`, so most of its output was never read; CONTRIBUTING.md was read (head −50) and the ID grep ran clean, so the codified turn-1 additions were covered — by side effect, not by consuming the ritual's output.
4. **§g rulings below are not advanced** — owner rulings, not agent work; no evidence changed this window.

## c) NOT STARTED

Nothing of row 173's scope remains. Untouched on purpose (different rows, not this dispatch's contract): row 165 (usage-key pin parity for Review/Status — owner-ruling-gated), row 174 (v0.3.0 pkg.go.dev render check), row 175/176 (BLOCKED on owner). The carried §f table from the 10-07 sibling-cluster report is untouched except row 7 (= this row), now done.

## d) TOTALLY FUCKED UP

1. **Two wasted round trips hand-rolling the devmod shim** when the canonical gate was in-context. AGENTS.md's Commands section names `scripts/test-cmd-tq.sh` explicitly; I read the script AND still tried `source scripts/lib/cmd-tq-devmod.sh && cmdtq_devmod && cd cmd/tq && go test …` twice — both failed with nonsense paths (`/home/lars/cmd/tq/dev.mod`; the shim resolves the repo root in a way an interactive `source` breaks) before I ran the shipped script, which worked first try. Root cause: optimizing for a targeted `-run` pass over running the documented gate. The "smallest correct change wins" contract cuts against re-deriving a gate's plumbing.
2. **Committed the documented PIPESTATUS-class hazard once.** In the first (broken) chain I wrote `go test … | tail -8; rc=$?` in the session shell — `$?` after a pipeline reports the LAST stage, the exact codified mvdan-shell trap. No false claim rode on it (the chain had visibly failed), but the codified failure was one `echo` away from being cited. Every rc quoted in this report comes from direct capture or a redirected file instead.
3. **Lost the daemon race.** The test edit sat uncommitted through the gate runs; the auto-commit daemon folded show_test.go into footer-less 0f0c1bf→66d65ef ("1 changed file(s)", amend-healed). Healed correctly via the repo's amend maneuver (unpushed daemon commit + TODO fold + footer → bcf0eca, verified single footer), but the race was avoidable: gate first, then add+commit in ONE chain after the last write — the 08-52 lesson, re-hit anyway. Attribution survived only because the heal is precedented; per-window vigilance is not a control.
4. **Ritual output swallowed.** Running `session-start.sh … | tail -20` is compliance-shaped: the script ran, its output didn't land in context. The 10-07 §d1 "clean outcome was compliance-shaped luck" confession applies with the roles inverted — here the steps ran but the reading was skipped.
5. **Lint delta unchecked by the work turn.** "Don't add new findings in functions you touch" is a hard rule; the work turn never ran `lint-annotations.sh`, and the edit added blank lines inside a struct (wsl-class risk). Closed in close-out: 0 new findings. Rule followed by audit, not by habit.
6. **One edit-before-read round trip on TODO_LIST.md** (sed-read the row, then the edit tool correctly refused; View, then edit). Trivial, but it is the N+1 of a family every recent report confesses.

## e) WHAT WE SHOULD IMPROVE

1. **Targeted-run passthrough for `scripts/test-cmd-tq.sh`.** The gate hardcodes the full suite; any window wanting `-run` must hand-roll the shim (this window proves how that goes, §d1). A `$@`/env passthrough (`TQ_CMD_TQ_TEST_ARGS="-run '^TestX$' -v"`) appended to the script's `go test` line ends the whole hand-rolled class. Backlog row appended this window.
2. **One-chain commit after the last write — mechanically, not from memory.** Concretely for this flow: run gates on the dirty tree, then `git add <files> && git commit` as a single command, before ANY other tool call. The daemon's sweep interval is the clock; every intervening call is a lottery ticket.
3. **The verify/close-out battery script demand stands** (05-51 §e1, third+ cluster datapoint): one command = build+vet+doc-refs+module suite+lint-annotations with rc capture built in. This window's §d2/§d5 would both have been prevented by it.
4. **Lint-annotations belongs in the default per-window battery**, not the close-out's conscience. It is seconds, it is the exact "new findings in touched functions" gate, and it caught nothing here only because the close-out ran it.
5. **Ritual tools should print less or be consumed whole**: piping a checklist script through `tail` defeats its purpose. Either trust the (long) output into context or have the script emit a compact PASS/FAIL summary line that survives tailing.

## f) Things to get done next

Honest count, not 50-padding: 1 new (§e1), 14 carried from the sibling cluster's canonical table (10-07 §f) re-verified still-open, minus row 7 which THIS window closed. New item also appended to TODO_LIST.md per the follow-ups clause.

| #  | Task                                                                                                                                                                            | Impact   | Effort | Category       | Status                                     |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | -------------- | ------------------------------------------ |
| 1  | Add a targeted-run passthrough to scripts/test-cmd-tq.sh (TQ_CMD_TQ_TEST_ARGS or "$@", default = full suite) so windows stop hand-rolling the devmod shim                       | Medium   | S      | Tooling        | NEW — appended to TODO_LIST.md this window |
| 2  | OWNER: scope the minted gofmt gate to tracked files in BOTH .tq-verify and the mint template — queue-side verifies keep dying on gitignored vendor/ (5+ dead tasks since 09-20) | Critical | S      | Bug            | owner-only (rows 204/326/328)              |
| 3  | Verify-battery script (build+vet+doc-refs+suite+lint-annotations, rc capture built in) then make it the ritual battery                                                          | High     | S      | Process        | 05-51 §e1 demand, unbuilt                  |
| 4  | Review card: render the full agent-output log-path line like the other three cards                                                                                              | Low      | S      | Feature        | minted (09-52 §f1)                         |
| 5  | Agent-outcome table badge + loadSnapshot AgentResults parity — after the §g2 ruling                                                                                             | Medium   | M      | Feature/ruling | minted (09-52 §f2)                         |
| 6  | DLQ autopsy session usage in the budget token projection — after the §g3 ruling                                                                                                 | Medium   | M      | Budget/ruling  | minted (09-52 §f3)                         |
| 7  | Verify tq api mirrors the webui's agent/review usage surface                                                                                                                    | Low      | S      | Quality        | minted (09-52 §f4)                         |
| 8  | CHANGELOG/FEATURES entries for the agent-run card + review usage line                                                                                                           | Medium   | S      | Documentation  | minted (09-52 §f5; rides row 336)          |
| 9  | Usage-key pin parity for Review/Status results (exact-key pins beyond Prioritize) — owner-ruling-gated                                                                          | Medium   | S      | Quality        | row 165                                    |
| 10 | FEATURES placement ruling for usage-rendering facts                                                                                                                             | Low      | S      | Documentation  | row 336                                    |
| 11 | Run ./scripts/ci-local.sh end-to-end before the next push                                                                                                                       | High     | M      | Process        | row 190                                    |
| 12 | Daemon-fold attribution ruling (session footers in daemon commits vs tq-side fallback)                                                                                          | Medium   | —      | Process        | row 194                                    |
| 13 | Harvest anti-race: checkbox re-read at claim/dispatch time                                                                                                                      | High     | M      | Process        | 08-35 §f4 row                              |
| 14 | Commit-msg hook: require the Task-Queue-ID footer in the FINAL trailer block                                                                                                    | Medium   | S      | Quality        | 04-13 §d3 row                              |
| 15 | v0.3.0 release tail: pkg.go.dev render check                                                                                                                                    | Medium   | S      | Release        | row 174                                    |
| 16 | tq tasks payload/verify exposure (stale-verify hygiene)                                                                                                                         | Medium   | M      | Tooling        | 09-39 §e1 row                              |
| 17 | tq doctor --hygiene stale-verify gate                                                                                                                                           | Medium   | M      | Quality        | 09-39 §e3 row                              |
| 18 | Consciously decide factTone cases for the 6 newer fact types                                                                                                                    | Low      | S      | Quality        | 08-35 §e5 row                              |
| 19 | Drop detailItems' unused now param (unparam)                                                                                                                                    | Low      | S      | Cleanup        | 08-35 §e6 row                              |
| 20 | internal/e2e -race headroom vs the 180s stage cap                                                                                                                               | Medium   | M      | Quality        | row 162 note                               |
| 21 | agentResultEmpty helper extraction + review-card log-path in one micro pass                                                                                                     | Low      | S      | Cleanup        | 09-52 §e4 + §c1                            |

## g) Questions I cannot figure out myself

1. **Amend-heal policy, standing ask (08-52 §g2 carried):** is amending an unpushed footer-less daemon commit into the footer-carrying work commit the approved default heal, or should the daemon fold stay untouched and the work ride in a follow-up footer commit? This window heeled by amend (bcf0eca over 0f0c1bf→66d65ef) on precedent alone; the queue's derivation saw one clean footer commit either way, but the ruling changes what the next race does.
2. **Battery depth for module-confined deltas:** when a window's diff is one test file in cmd/tq (zero production code), is the required battery module-gate + root build+vet (what this window cites), or must the root `-race` suite (or cmd/tq under -race) run regardless? The verify-window minimum battery names cheap gates + one fresh delta and lets expensive tiers inherit — but no same-HEAD race run exists to inherit here.
3. **Re-dispatch suppression (cluster-wide standing ask):** this ID's row is now `[x]` with citations; the cluster's history shows DONE rows re-dispatched up to 5×. Is queue-side suppression of just-DONE IDs an owner decision pending, or is the current answer "agents verify→cite→stop and eat the cost" — in which case should the verify-window compact report become the STANDARD first artifact so repeats stay cheap?

---

Scope note: based ONLY on this window's work (the show_test.go edit, its gates, the TODO tick, the commit heal) and what was read in passing (AGENTS.md rows, CONTRIBUTING.md, scripts/test-cmd-tq.sh, the 10-07 sibling-cluster report, stats_test.go:144 and the executor result types while grepping the key). No unrelated research. Deltas this window produces: bcf0eca (already landed), this report + its index row, one new TODO_LIST.md backlog row.
