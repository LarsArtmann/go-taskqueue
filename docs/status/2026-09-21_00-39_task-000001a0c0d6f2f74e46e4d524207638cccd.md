# Task 000001a0c0d6f2f74e46e4d524207638cccd — Verify-only re-dispatch of the budget token/cost projection

Window: 2026-09-21 ~00:35 – 00:45 CEST. SECOND window on this ID. The first was
the 00-27 work window (full close-out:
`docs/status/2026-09-21_00-27_task-000001a0c0d6f2f74e46e4d524207638cccd.md`;
work commits 87fc781/328732d/a5bc36a/8d12eb0/2d5c01e + close-out 6d1f4ef, all
footer-verified below). This dispatch minted roughly 40 minutes after the row
was checked — harvest raced the check-off. Verify→cite→stop window: ZERO
source edits.

## a) FULLY DONE

- **Turn-1 ritual via `scripts/session-start.sh` WITH the task ID**: the
  prior-report grep surfaced the 00-27 close-out immediately — repeat
  dispatch identified before any edit. CONTRIBUTING.md head read (script
  output); no CLAUDE.md exists (ls'd). No prior window was wasted on
  re-implementation.
- **Every prior-window claim re-verified against code at HEAD (6d1f4ef),
  file:line**:
  - `SessionUsage` type + `Guard.UsageToday` — internal/budget/budget.go:99
    and :129
  - `FactsSince` — contract internal/queue/queue.go:196; sqlite
    internal/queue/sqlite/sqlite.go:1645; postgres
    internal/queue/postgres/postgres.go:1553
  - `TestFactsSinceByType` — internal/queue/sqlite/store_test.go:1745
  - `TestCheckExhaustedReasonCarriesUsage` —
    internal/budget/budget_test.go:193
  - `TestStatsJSONSessionUsageContract` — cmd/tq/stats_test.go:102
- **TODO row 160 confirmed `[x]` in place** (TODO_LIST.md:160); annotated
  with this window's re-verification per the file's own re-dispatch
  convention (row 133/136 style).
- **Fresh cheap battery at HEAD, rc's captured directly** (file-redirect +
  `$?`, never PIPESTATUS — the documented session-shell hazard):
  - root build+vet (GOEXPERIMENT=jsonv2) rc=0
  - `scripts/check-doc-refs.sh` rc=0 ("doc refs ok")
  - internal/budget full suite, in-module `GOWORK=off`, rc=0 (ok 0.010s)
  - internal/queue/sqlite full suite, in-module `GOWORK=off`, rc=0 (ok
    3.302s, incl. TestFactsSinceByType)
  - cmd/tq `TestStatsJSONSessionUsageContract` via `./scripts/test-cmd-tq.sh`
    rc=0 (ok 15.808s)
- **Queue attribution re-verified**: all six prior-window commits carry the
  exact Task-Queue-ID trailer (87fc781, 328732d, a5bc36a, 8d12eb0, 2d5c01e,
  6d1f4ef → 000001a0c0d6f2f74e46e4d524207638cccd). The re-dispatch is
  therefore a HARVEST-RACED-THE-CHECKOFF dispatch, not lost attribution —
  evidence for the BLOCKED duplicate-claim ruling row (the minted dispatch
  quotes the item text while the row was already `[x]` at HEAD).
- **TODO_LIST loop closed** per the file's conventions (re-verified
  annotation). No new backlog rows appended — every thread this window
  discovered already has a row: the duplicate-claim re-delivery ruling row
  covers suppression, the open ci-local row covers the expensive-tier gap,
  and the executor-side json-key pin row (row 163) is untouched.
- **AGENTS.md from-root nested-module note corrected on sight** (the
  "FAILS by design" claim was empirically half-wrong — see §d2): the claim
  now carries the observed nuance and names the in-module GOWORK=off run as
  the only canonical gate.
- **This report + index row + TODO annotation + AGENTS.md tweak committed in
  ONE chain with the footer** (daemon race respected: add+commit
  immediately after the last write — the 00-27 §d lead lesson applied).

## b) PARTIALLY DONE

- **Verification tier: CHEAP battery only** (the documented verify-window
  minimum). No race-suite re-run, no ci-local/nix/GOOS=windows/smokes/
  advisory-lint. Justification: zero source edits, HEAD byte-identical to
  the 00-27 window's fully-batteried tree (6d1f4ef IS the close-out
  commit), and the convention allows inheriting expensive-tier evidence
  from a same-HEAD prior report. Stated explicitly, not implied.
- **The first test invocation ran from ROOT** (`go test ./internal/budget/
  ./internal/queue/sqlite/`): budget "ok 0.003s" while sqlite setup-failed.
  The from-root budget pass rode the root module's require+replace
  resolution — it is NOT canonical evidence; the in-module GOWORK=off runs
  above are. One round trip wasted on the documented form (§d2).

## c) NOT STARTED

- No code work, BY DESIGN (DONE-row re-dispatch). Everything under the
  00-27 §c ledger stays open and unchanged: ReviewResult/StatusResult usage
  derivation (TODO rows 161/162), webui budget-card token/cost line,
  `tq show` prioritize usage, completion-cost sparkline, calibration/G1
  revisit, local postgres twin execution, full ci-local run, cap-unit ADR.
- Nothing was silently dropped; the window's scope was verification.

## d) TOTALLY FUCKED UP

1. **ZERO-ARTIFACT STOP (the lead)**: the first response ended chat-only —
   all verification evidence lived in the ephemeral reply; no footer commit,
   no report. The queue had NO record this window answered until the owner
   explicitly requested this report. This is the documented, twice-confessed
   shape (07-47 §d1, 07-51 §d1: "same trap, second consecutive hit — stub
   script is the fix") and the very next family hit it again. The
   verify→cite→stop ritual is only complete when its citation lives in a
   footer-bearing commit; chat is invisible to the queue.
2. **First test invocation ignored the documented rule**: AGENTS.md says
   plainly "`go test ./internal/foo` from root FAILS by design (cd into the
   module instead)" — I pattern-matched the root form anyway and burned one
   failed round trip (sqlite setup-failed). Worse, the failure revealed the
   rule text was UNDER-specified: a from-root directory pattern can
   silently PASS via the root's require+replace (budget did), which is more
   dangerous than failing. Rule text fixed on sight (§a).
3. **Footer attribution not checked in the first pass**: I verified the
   CODE existed but not the WORK COMMITS' trailers before stopping — the
   one check that distinguishes "harvest raced the check-off" (what
   happened) from "attribution lost" (what the 00-27 heal was for). Done
   now; the distinction decides which systemic fix the duplicate-claim
   ruling row needs.
4. **`check-todo-list.sh` skipped in the first battery** — it would have
   been the window's one cheap extra delta (07-51 §d made the identical
   confession). It IS part of the closing battery for this commit.

## e) WHAT WE SHOULD IMPROVE

1. **Verify-only stops must ship their report commit by default.** The
   artifact IS the attribution; a bare "verified, stopping" reply leaves
   the window unrecorded and feeds the re-dispatch treadmill. One line in
   the session-start ritual bullet or the verify-window battery convention
   makes this mechanical (candidate wording: "a verify-only stop is not
   done until its report commit exists").
2. **Mint-time row-state freshness**: harvest minted this dispatch while
   the row was already `[x]` at HEAD (proven: row 160 checked + close-out
   6d1f4ef precede the dispatch; footers intact). The BLOCKED
   duplicate-claim ruling row should weigh the cheapest sufficient fix:
   re-read the row state at mint/claim time, or skip minting when the
   item's ID already has a docs/status report. The sibling
   usage-derivation ID (000001a0bdc8bf…) burned SEVEN windows; this ID is
   at two in 40 minutes.
3. **In-module GOWORK=off is the only trustworthy nested-module gate** —
   root-level runs resolve through the root's require+replace and can
   half-pass (one module ok, another setup-failed, §d2). AGENTS.md now
   says so; no verify script should ever trust a from-root green.
4. **Cheap-battery script** (carried 08-52 §e): build+vet+doc-refs+todo-gate
   as one command with rc capture — removes the per-window improvisation
   where gates get skipped (§d4).
5. **What worked (keep)**: ritual-first with the task-ID grep (repeat
   dispatch caught pre-edit — zero wasted work); rc-to-file discipline;
   in-module canonical gates; honest no-commit over an empty commit;
   refusing to append duplicate backlog rows (every thread mapped to an
   existing row); AGENTS.md fix-on-sight instead of a report-only note.

## f) Up to 50 things we should get done next

Honest count: **8**; padding to 50 refused (same ruling as every sibling
window in this family). The 00-27 §f ledger (32 items) is carried BY
POINTER for this ID's scope — not restated here.

1. Codify verify-only-report-by-default (§e1) into the session-start /
   verify-window convention. Impact: High. Effort: S. Category: Quality.
2. Mint/claim-time row-state freshness check (§e2) — input to the BLOCKED
   duplicate-claim ruling row. Impact: High. Effort: M. Category: Feature.
3. `scripts/verify-cheap.sh` (§e4, carried 08-52 §e). Impact: Medium.
   Effort: S. Category: Quality.
4. Run the local postgres twin for `FactsSince` (TQ_TEST_POSTGRES + scratch
   container; 00-27 §f6). Impact: Medium. Effort: S. Category: Quality.
5. Run full ci-local once on this lineage (open TODO row; 03-46 §f6; 00-27
   §f7). Impact: Medium. Effort: S. Category: Quality.
6. ReviewResult usage fields + derivation (open TODO row 161; 00-27 §f4).
   Impact: High. Effort: S. Category: Feature.
7. StatusResult usage fields + derivation (open TODO row 162; 00-27 §f5).
   Impact: High. Effort: S. Category: Feature.
8. Executor-side json-key drift pin (open TODO row 163; 09-52 f2; the
   consumer-side pin already exists in budget_test). Impact: Medium.
   Effort: S. Category: Quality.

## g) Questions (up to 3)

1. **Verify-only attribution policy**: is a footer-bearing report commit
   the REQUIRED terminal artifact of a verify-only window (chat-only stop =
   the window never happened)? Third family to hit the zero-artifact stop
   (07-47/07-51, this window). Proposed default: yes — but it should be a
   ruling, since the alternative reading is "checked rows shouldn't re-mint
   at all" (§g2's territory).
2. **Duplicate-claim re-delivery ruling (carried, BLOCKED on owner)**:
   harvest minted this dispatch with the row ALREADY `[x]` at HEAD and
   attribution intact. Is the sanctioned fix (a) queue-side row-state
   re-check at mint/claim time, (b) harvest-side prior-report consult, or
   (c) ratify verify-only second windows as the standing shape with the
   §g1 report-commit requirement? The 7-window sibling says the cost of
   deciding is now the dominant term.
3. **Cap-unit ruling (carried 09-52 §g1, owner-only)**: the projection now
   MEASURES tokens/cost (`budget.session_usage`). Should the daily CAP gate
   on them (cost USD? tokens? both? which fact types once ReviewResult/
   StatusResult land?) or stay task-count? Blocks the Check semantics
   change and the webui budget card.
