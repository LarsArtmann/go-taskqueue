# Status Report — varnamelen raw-site re-dispatch (row 148), task 000001a09d26da747e9e15320a27942c9dfe

> **ANNOTATED 2026-09-14 (docs-health strikethrough pass)** — every numbered item below is resolved inline (`done`/`duplicate` markers) or left untouched (= still open). Unarchived on purpose: open items remain. Evidence verified against HEAD.

**Window:** 2026-09-14 ~01:50–01:55 CEST
**Work item:** "Rename the two varnamelen raw sites (`o` cmd/tq/agentpool.go:278, `f` cmd/tq/poolconfig.go:26) or extend the ignore list, so scoped `--enable-only` diagnostics agree with full-config runs (2 vs 0 discrepancy, 08-25 report b2)"
**Verdict up front:** the item was ALREADY DONE before this window started — commit d867c72 (2026-09-14 01:45) landed both renames (`o`→`opts`, `f`→`configFile`) and marked TODO_LIST row 148 `[x] DONE`. This dispatch was a no-op re-dispatch; my job reduced to verifying the claim at HEAD and closing the loop honestly. Third dispatch of the same row (the 01-46 and 01-51 reports carry the same task ID).

## a) FULLY DONE

1. ~~**State verification at HEAD**: root `go build ./...` and `go vet ./...` pass with `GOEXPERIMENT=jsonv2`; the full cmd/tq module suite via `scripts/test-cmd-tq.sh` (the sanctioned ADR-0017 path) is green (`ok … 8.352s`). No regressions from the prior window's renames.~~ duplicate — see 01-51 a3
2. ~~**Diff archaeology**: confirmed via `git show d867c72` that the renames are text-only (36+6 lines) and the fix's own commit message cites the scoped-0-issues verification.~~ duplicate — see 01-51 a1
3. ~~**TODO_LIST cross-check**: row 148 already `[x] — DONE` with the rename detail; no edit needed. Contract step 5 was already satisfied.~~ duplicate — see 01-46 a4
4. ~~**Hygiene**: removed the untracked `cmd/tq/tq` binary left behind by the test run (trash, not rm).~~ done — narrative (point-in-time process note, no artifact owed)

## b) PARTIALLY DONE

1. ~~**Independent re-run of the scoped `--enable-only varnamelen` gate**: NOT achievable raw. Three attempts, each with a distinct failure mode:~~ duplicate — see 01-51 b1
   - Plain `golangci-lint run` in cmd/tq dies on `ambiguous import` (module-cache copy of go-taskqueue v0.3.0 also contains cmd/tq).
   - `-modfile=dev.mod` as a golangci-lint flag: unknown flag.
   - `GOFLAGS=-modfile=dev.mod`: "build flag -modfile only valid when using modules" (golangci's go/packages load path drops module mode).
   The 01-51 report already documented this exact papercut; I add the two NEW data points above (flag-level attempts), which narrow the fix space: the shim needs `go list -modfile`-compatible plumbing or a `go.work`-style overlay for golangci, not flag hacks. The 2-vs-0 discrepancy itself is closed by the renames regardless — this is tooling debt, not open work-item risk.

## c) NOT STARTED

Nothing from the work item itself. The 01-51 report's follow-up list (items 11–16) remains unowned, most notably **#13: a smoke that runs the scoped varnamelen gate over cmd/tq** — which is precisely the verification this dispatch could not perform independently.

## d) TOTALLY FUCKED UP

Nothing destructive. Honest failures, ranked:

1. **Wasted cycles rediscovering the ambiguous-import wall**: the 01-51 report (same task ID, 3 minutes earlier) already said the raw scoped run is not re-runnable. I did not re-read the sibling report before attempting — I found it only after three failed invocations. The session-start ritual exists for exactly this; reading the other open dispatch's report for the SAME task ID should be step zero.
2. **First devmod attempt sourced the lib with a broken BASH_SOURCE resolution** (mvdan/sh + `source` chain resolved root to `$HOME`), producing garbage paths and an exit-3 before any real check ran. Manual inline regeneration of dev.mod was the working fallback, but two of three attempts produced zero signal.

## e) WHAT WE SHOULD IMPROVE

1. ~~**Re-dispatch dedup**: the queue re-sent a row already closed 9 minutes prior (row 148 was `[x]` and d867c72 pushed). The pool should check whether the task's TODO row is already `[x] DONE` and short-circuit instead of minting a re-dispatch. (This mirrors 01-51 item 16: dogfood re-verification of DONE rows.)~~ duplicate — see 01-51 e1
2. ~~**Scoped-lint smoke (01-51 #13) is now doubly justified**: two consecutive windows could not independently re-verify the gate. A `scripts/smoke/lint-scoped.sh` (devmod shim + `--enable-only varnamelen`, asserting 0 issues) would make both the original fix and any regression mechanically checkable.~~ duplicate — see 01-51 f13
3. ~~**golangci-lint + nested-module story**: document in AGENTS.md that raw scoped lint runs inside cmd/tq are NOT a supported invocation; the supported path is the (yet-to-exist) smoke or the full-config root run.~~ duplicate — see 01-51 f6
4. ~~**Session-start ritual addition**: when a dispatch carries a task ID with prior status reports (`docs/status/*_task-<id>.md`), read them FIRST — they encode exactly which walls exist.~~ done — narrative (point-in-time process note, no artifact owed)

## f) Next up to 50 things (this task's blast radius + noticed-in-passing; most duplicate 01-51's list deliberately so they stay visible)

1. ~~Smoke that runs scoped `--enable-only varnamelen` over cmd/tq via the devmod shim (01-51 #13) — top priority, closes this report's §b1 permanently.~~ duplicate — see 01-51 f13
2. ~~Investigate/fix the `ambiguous import` for golangci inside cmd/tq (maybe `go mod edit -droprequire` trick or a lint-specific overlay go.mod).~~ duplicate — see 01-51 f2
3. ~~Queue-side short-circuit: don't re-dispatch TODO rows already `[x]` with a landed commit citing the row (dogfood enforcement, 01-51 #16).~~ duplicate — see 01-51 f3
4. ~~`scripts/test-cmd-tq.sh`: add `--count=1` (01-51 #12).~~ duplicate — see 01-51 f12
5. ~~Document the cmd/tq test/lint path canonically in AGENTS.md commands block (01-51 #11).~~ duplicate — see 01-51 f11
6. ~~Sweep other report follow-up lists (01-51 items 14–16: baseline regen policy doc; go.mod-health self-check at gate top; claims-carry-citations enforcement in the queue itself).~~ duplicate — see 01-51 f14-16
7. From TODO_LIST row 150: lint-baseline round 2 (goconst, mnd, paralleltest, testpackage) — same triage discipline.
8. ~~Confirm row 150's baseline numbers are still current after the 2026-09-13 regen (886 findings / 111 rows).~~ done — row 105: gate green (verified at HEAD)
9. Consider teaching `scripts/lint-baseline.sh` to tag per-row provenance so "growth = my window" attribution is mechanical.
10. ~~Add `git status --short` cleanliness assertion to the end of `test-cmd-tq.sh` so test binaries like `cmd/tq/tq` never linger for the daemon to sweep. (This window created one; I trashed it.)~~ done — trap cleanup test-cmd-tq.sh:12 (verified at HEAD)
11. Verify the leftover `cmd/tq/tq` binary class is gitignored, not just untracked (it showed as `??`, so it is NOT ignored — one bad `git add .` from shipping it).
12. Add cmd/tq build artifacts to .gitignore explicitly.
13. ~~NixOS-module fix still pending from 2026-09-11: `Environment=GOEXPERIMENT=jsonv2` on tq-agent-pool (owner-run; pool verify env lie).~~ resolved — routed row 98 (verified at HEAD)
14. ~~Session-close bridge open items (trigger automation crush #3146, daemon-commit attribution gap, budget bypass, postgres parity) — per AGENTS.md design doc.~~ resolved — routed rows 107-110 (verified at HEAD)
15. ~~Postgres parity for the session-close bridge's `AppendFact` path.~~ resolved — routed row 110 (verified at HEAD)
16. Re-check whether the `statix` LSP disable (SystemNix, 2026-09-12) can be upstreamed as a repo-local override so fresh clones don't hit the 900-failure class.
17. Evaluate whether `execWithTransientRetry` should wrap any NEW exec sites noticed since (kernel ETXTBSY anomaly watch).
18. ~~Check whether the dev.mod shim lib's `BASH_SOURCE` root resolution is robust under mvdan/sh (this window's §d2); fix `cmdtq_devmod` to hard-fail with a clear message if `$HOME`-rooted.~~ done — root from BASH_SOURCE lib:12 (verified at HEAD)
19. Add a comment/test pinning the `cmdtq_devmod` root derivation.
20. ~~Consider `golangci-lint` version pin consistency between the CI-pinned v2.13.2 (baseline gate) and whatever this host ran — version drift changes finding counts.~~ resolved — routed row 64 (verified at HEAD)
21. ~~When the go-cqrs-lite durable-work-queue module reaches parity, open the ADR to re-evaluate the hand-rolled stores (AGENTS.md verdict; long-range).~~ duplicate — see 01-51 f50
22. Sweep TODO_LIST rows 149–151 neighbors for other stale DONE claims lacking commit citations (claims-carry-citations, 2026-09-12).
23. ~~Confirm the f26 three-ID-cluster guidance is encoded where reviewers actually read it (payload contracts section) — noticed while reading AGENTS.md.~~ done — AGENTS.md:298 cautionary note (verified at HEAD)
24. ~~Check `docs/status/README.md` index rows for the three reports under this task ID (01-46, 01-51, this one) are all present — the daemon-folded-report hole has bitten twice.~~ done — README rows 44-46 (verified at HEAD)
25. ~~Amend-maneuver check: this report must be indexed at creation; verify `check-status-index.sh` passes before the daemon folds it.~~ done — indexed README row 44 (verified at HEAD)
26. ~~CI_CHECK state: did not run `scripts/check-ci.sh` this window (verify was scoped to build/vet/cmd-tq tests); full `ci-local.sh` remains the pre-push gate for the next real change.~~ done — narrative (point-in-time process note, no artifact owed)
27. Fuzz nightly: confirm last night's campaign committed seeds cleanly (fuzz.yml had the 2026-09-12 setup-failure class).
28. Review whether the two "docs:" commits (cda9541, 8b7de61) both landed with index rows, given the daemon bypasses hooks.
29. Consider a `make`-free `scripts/verify-scoped.sh <module> <linter>` helper encapsulating the devmod + golangci dance for ANY module, killing the ad-hoc incantation class this window tripped over.
30. Audit other nested-module dirs (internal/*) for the same ambiguous-import lint wall — do scoped lint runs work there? They ascend to the root config, so probably yes; pin that with a smoke.
31. ~~`queue/sqlite varnamelen 32→33` growth row from the round-13 note: confirm the 2026-09-13 regen absorbed it.~~ done — row 105 regen 895de1e8 (verified at HEAD)
32. ~~Wrap this window's verification (build+vet+test evidence) into the task's DONE citation per claims-carry-citations — done in this report; ensure the queue's review reads it.~~ done — narrative (point-in-time process note, no artifact owed)
33. Consider making `test-cmd-tq.sh` print artifact paths it creates and clean them itself (self-trashing shim).
34. Check whether `GOFLAGS=-modfile` interacts badly with anything else when exported session-wide (I unset it, but a careless future session might not).
35. ~~Long-term: replace the dev.mod shim with `go.work` — rejected by decision (replace-only), so instead document WHY near the shim for future re-litigation attempts.~~ done — AGENTS.md:71 NO go.work (verified at HEAD)
36. ~~Baseline policy: write the "when is shrink a policy change vs drift" doc (01-51 #14).~~ duplicate — see 01-51 f14
37. ~~Add go.mod-health self-check (`check-go-mods.sh --quick`?) to the top of agent verify runs (01-51 #15).~~ duplicate — see 01-51 f15
38. Sweep for other places `-modfile` is assumed valid (scripts, docs) and annotate the golangci exception.
39. Track golangci-lint upstream issues for nested-module/gopls workspace support; file once verified per verify-before-filing.
40. Confirm 148's DONE line cites the smoke once #1 ships (upgrade citation from "dev.mod shim run" to "gated smoke").
41. ~~Consider queue-side task-type "verify-only" for re-dispatches so a no-op verification window costs minutes, not a full agent turn.~~ resolved — routed row 148 (verified at HEAD)
42. ~~Check TODO_LIST row 148's `DONE` note against the actual variable names now in HEAD (`opts`, `configFile`) — verified this window; keep in sync if renames happen again.~~ done — verified this window (verified at HEAD)
43. ~~The `.golangci-baseline.txt` regen cadence: confirm the next regen is deliberate (policy-owned), per round-13 rules.~~ done — regen-policy row 105 (verified at HEAD)
44. Postgres backend: confirm no varnamelen-equivalent raw sites (row 148 was cmd/tq only, but the growth rows mentioned sqlite).
45. ~~Consider adding this dispatch pattern (re-dispatch of done work) to the AGENTS.md known-issues so future windows skip the archaeology.~~ done — AGENTS.md:193 no-op derivation (verified at HEAD)
46. Verify `go vet ./...` coverage actually descends cmd/tq or whether it's root-module-only by design (I ran it from root; cmd/tq got its own test run — make the boundary explicit in AGENTS.md).
47. Trim TODO_LIST row 148's verbose DONE note once the smoke exists (the shim-run citation becomes stale).
48. Check whether the ambiguous import also breaks `go vet`/`go test` invoked by third-party tooling in cmd/tq (tests passed via script, but raw invocation from cmd/tq cwd is probably broken too — worth one probe).
49. ~~Archive window-five report follow-ups already superseded by round-13 work (rows citing 08-25 numbers that regen superseded).~~ done — superseded by round-13 (verified at HEAD)
50. ~~Owner decision needed (see g): whether re-dispatch dedup (#3) should be a pool-side fix or a harvest-side `[x]` scan.~~ resolved — routed rows 148/157 (verified at HEAD)

## g) Questions I cannot figure out myself

1. ~~**Re-dispatch policy**: was row 148's third dispatch intentional (e.g., testing the queue's DONE-detection) or a harvest bug? If bug: does the pool read TODO_LIST `[x]` state at all, and should dedup key on the row text hash?~~ resolved — routed row 93 (verified at HEAD)
2. **Scoped-lint smoke placement**: should the varnamelen-over-cmd/tq smoke live in `scripts/smoke/` (CI-safe, runs in ci-local) or as a `check-` script gated on golangci presence? I can't decide the failure semantics you want (hard gate vs advisory) without your ruling.
3. **golangci + devmod**: do you want a durable `lint.dev.mod` committed for cmd/tq (mirroring dev.mod's replace set, gitignored vs committed is your call), or is the smoke-supplied ephemeral shim the accepted endgame for the ambiguous-import wall?

---
*Verification cited: `GOEXPERIMENT=jsonv2 go build/vet ./...` green; `scripts/test-cmd-tq.sh` green (ok 8.352s); `git show d867c72` confirms renames; HEAD 8b7de61 clean except trashed `cmd/tq/tq` artifact.*
