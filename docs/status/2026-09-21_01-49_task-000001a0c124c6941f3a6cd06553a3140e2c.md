# Task 000001a0c124c6941f3a6cd06553a3140e2c — Brutal close-out: ReviewResult session usage (09-52 §f3)

Window: 2026-09-21 ~01:20 – 01:55 CEST. SECOND (close-out) commit on this
ID. The work window shipped the item and recorded a first close-out
(`docs/status/2026-09-21_01-45_task-000001a0c124c6941f3a6cd06553a3140e2c.md`);
THIS report is the brutal a-g self-review pass, plus the AGENTS.md
truth-hygiene the work window forgot (see §d4). Skill: brutal-self-review.

## a) FULLY DONE

- **The work item itself** (verified against code, file:line):
  - `ReviewResult` gains the four derived usage fields with the shared json
    keys — internal/executor/review.go:112-118 (`session_cost_usd` at :113),
    placed between `SessionID` and `LogPath`, PrioritizeResult's exact
    shape.
  - Derivation wired in `ReviewExecutor.Execute` — review.go:213-219
    (`deriveOutcome(ctx, repoDir, result.SessionID, t.ID)` at :215), same
    insertion point as prioritize.go (between session-id extraction and the
    sidecar write), same best-effort comment contract.
  - Budget consumption rides the shared key parse with ZERO projection-code
    change: `usageSince` (internal/budget/budget.go:134) unmarshals into
    `sessionUsageDetail` (budget.go:119) by json key only, so review
    completions count as soon as the result carries the keys. Enumerating
    doc comments updated (SessionUsage block budget.go:93-98).
  - Drift pin widened: `TestUsageTodaySumsDerivedSessionUsage`
    (internal/budget/budget_test.go:144) now marshals the REAL
    AgentResult + PrioritizeResult + ReviewResult through a completed fact
    each (want Runs 3 / $0.65 / 2300+4300 tokens / 18 messages) — a key
    rename in any of the three fails the budget suite in both directions.
- **AGENTS.md truth hygiene (this pass)**: the `internal/budget` table row
  now names all three result types, and the `review` payload-contract
  bullet records the 2026-09-21 derivation (the work window shipped code
  but left both stale — §d4).
- **TODO_LIST row 161 ticked in place** (full text preserved + gate
  citation); one NEW follow-up row appended (tq show review-usage display).
- **Turn-1 ritual complete**: session-start.sh WITH the id, whole-repo
  git log/status/stash, prior-report grep (no prior windows on this ID —
  first dispatch), CONTRIBUTING.md read, no CLAUDE.md.
- **Verification battery, all rc=0**: executor module GOWORK=off
  build+vet+`go test -count=1` (ok 288.045s); budget package after
  `go mod vendor` (build+vet+test ok 0.008s); root build + `go vet ./...`;
  FULL root `go test ./... -race -count=1` (RACE_RC captured to file per
  the PIPESTATUS rule); gofmt clean; check-facade-parity OK; post-commit
  check-doc-refs + check-status-index + check-todo-list all rc=0.
- **Honesty check (skill §5)**: no lies found; one imprecision — the 01-45
  citation "zero budget-code change" meant the projection LOGIC, but
  budget.go (comments) and budget_test.go (pin) WERE modified. Corrected
  wording here; no factual claim was wrong.

## b) PARTIALLY DONE

- **"Surface in the budget token projection" is the MEASUREMENT half**:
  `UsageToday`/`Check` refusal reasons now include review spend, but the
  daily-cap SEMANTICS remain task-count by the standing owner ruling
  (token-vs-count, 09-52 §g1) — reviews raising the token number does not
  yet gate minting. Out of this item's scope; ruling pending (§g3).
- **Test coverage pins the KEY CONTRACT, not the WIRING**: nothing asserts
  that Execute actually populates the Session* fields from derivation —
  hermetic tests see zeros (crushdata + git side effects are untestable as
  written). The same gap exists for agent and prioritize (pre-existing);
  a tiny seam would close it for all three (§e5, §f5).
- **Battery tier: strong but not full**: module suites + FULL race suite
  green, but no ci-local, no nix build, no GOOS=windows cross-compile, no
  smokes, no cmd/tq gate. Defensible for the surface (pure-Go field
  additions, no build tags, no cmd/tq payload change) but it IS an
  inference, not a gate run.

## c) NOT STARTED

- Sibling rows left deliberately (smallest-correct-change contract):
  StatusResult usage (TODO row 162), executor-side json-key pin
  (row 163), tq show review-usage rendering (new row from this window).

## d) TOTALLY FUCKED UP

- **SEVENTH daemon fold in the report family — and the worst variant:
  the SOURCE files went footer-less, not just docs.** The executor +
  budget edits were swept into `484d2a5` (chore, 3 files) and the TODO
  tick into `f32a74e` (chore); the window had NO footer-carrying commit
  until `952edf9` (this window's docs-only fold record at the time). The
  brutal part: I READ the 01-05 report's fold doctrine at turn 1, then
  sequenced edit → 288s executor suite → background race suite → tick →
  commit — handing the daemon an ~8-minute dirty window against its
  ~2-minute sweep. The knowledge was in context; the sequencing ignored
  it. The §e corollary is now a rule, not a note (§e1).
- **Edit-tool discipline slipped twice**: two `edit` calls bounced with
  "read the file first" (review.go struct, budget_test.go) because I had
  read content via bash cat/grep instead of View. The second bounce came
  AFTER the first should have taught it. Two wasted turns on a rule I
  know.
- **First-pass comment quality**: my initial budget.go doc rewrap left a
  dangling short line and an orphaned type name; needed two fix-up edits
  to reach clean 80-col wrapping. Four-line comment, three edits.
- **Memory-doc lag (caught only by THIS review)**: shipping derivation
  for a new executor changes the payload-contract surface — the AGENTS.md
  budget row and review bullet were stale for the entire gap between
  952edf9 and this pass. The aggressive-update protocol says update at
  discovery; the discovery was mine and I deferred it to a later pass.
- **LSP phantom red sat untriaged for several turns**: gopls served
  stale-vendor `MissingLitField` errors (and golangci_lint_ls the same
  typecheck) against a green CLI for the whole post-edit stretch; the
  correct move — `lsp_restart` immediately after `go mod vendor` (vendor
  refresh invalidates the LSP's module view) — happened only late. Known
  class, known fix, slow application.

- **Eighth fold, post-report-write (this window's single sanctioned correction)**: the AGENTS.md hygiene edits were swept footer-less into a7e0dda in the gap between the edit tool call and the commit chain — the exact race §d1 documents, re-proven on the same invocation pattern. Content verified intact on master (git show a7e0dda -- AGENTS.md, both hunks present); this correction rides the footer-carrying commit per the fold-record remedy. No further corrections this window regardless of what the daemon does next.

## e) WHAT WE SHOULD IMPROVE

1. **Promote the sequencing corollary to a hard rule**: source commits are
   the FIRST gate, not the last — edit → add+commit (footer) → long gates
   → fix-commit if red. Cost: an occasional fix commit on red (normal
   dogfood flow). Benefit: footer attribution + fold immunity for every
   executor-class window. The current order structurally donates the work
   to the daemon.
2. **Pair `go mod vendor` with `lsp_restart` as ONE step** for root-module
   packages after internal edits — the phantom-red stretch is pure noise
   cost and it has now bitten in two consecutive windows.
3. **Kill the fold CLASS, not the fold RECORD**: seven folds in one family
   means the remedy is a recurring tax. Either (a) the daemon learns a
   sweep guard (skip files touched < N minutes ago — owner-side change),
   or (b) agents get a sanctioned one-shot `scripts/commit-window.sh
   <task-id> <subject>` (stage + commit with the footer in one
   invocation). Either removes the race instead of documenting it.
4. **Update memory docs in the SAME commit as the code** that changes
   their truth (AGENTS.md rows naming result types, payload bullets) —
   not in a later review pass. Cheaper than a second commit and immune to
   the review pass being re-dispatched.
5. **Make derivation testable via a seam**: extract
   `applySessionUsage(target, derived)` (or a usage-source interface) so a
   unit test can pin "Execute populates usage from derivation" for review,
   prioritize and agent in one table — closes the §b wiring gap without
   hermetic-tube theater.

## f) Up to 50 things we should get done next

1. `tq show` review-task detail: render session tokens/cost for review
   turns like agent/prioritize tasks (NEW row appended this window).
2. StatusResult usage fields + deriveOutcome wiring (row 162 exists).
3. Executor-side json-key pin (row 163 exists) — once written, decide
   whether budget_test's marshal-pin stays or retires in its favor (one
   canonical pin, not two drifting ones).
4. Usage-only derivation variant for read-only executors (skip the
   footer git-log scan whose Commits/Files are discarded on the
   review/prioritize paths) — OR keep it deliberately as an integrity
   check (§g2 decides).
5. Derivation-wiring test seam (§e5).
6. Daemon sweep guard or commit-window script (§e3; owner-side).
7. Fold-ruling: scripted fold of unpushed footer-less daemon commits
   (carried from 01-05 §g2; this window is the strongest case yet —
   §g1).
8. AGENTS.md "Derived outcomes" bullet still narrates agent-only
   derivation; after row 162 lands, enumerate all result-carrying types
   in one place (deferred to avoid double-editing the same paragraph).

## g) Questions (up to 3)

1. **Fold remedy ruling (carried, now sharpened)**: may agents script a
   rebase that folds UNSHIPPED footer-less daemon commits into the
   window's footer commit (unpushed range only, per the daemon playbook)?
   Seven folds later, source-carrying folds corrupt per-commit attribution
   in both directions (work under chore, footer under docs); the
   fold-record remedy works but taxes every window with a second commit.
   If yes, a checked `scripts/` utility beats per-agent improvisation.
2. **Derivation scan semantics for read-only executors**: the footer
   git-log scan inside `deriveOutcome` runs on review/prioritize turns
   whose Commits/Files are discarded. Strip it (usage-only variant) or
   keep it as a cheap integrity check — a footer-bearing commit from a
   READ-ONLY run would itself be a contract violation worth attributing?
3. **Cap-unit ruling (carried 09-52 §g1, third window)**: with agent +
   prioritize + review now feeding the token projection and status
   pending (row 162), does `--daily-cap` stay task-count permanently, and
   if not, which result types count toward a token cap?
