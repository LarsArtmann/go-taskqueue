# Status Report — task-000001a0a2b62072f2a632ab7d060c74c309 (rename-hygiene scanner) — 2026-09-15 03:45

## a) FULLY DONE

- **Rename-hygiene scanner**: `scripts/check-rename-hygiene.sh` shipped in commit 194c864 (prior
  dispatch lap, not this one). Flags double-quoted string literals on added diff lines whose content
  exactly matches an identifier the diff removed — the `q.Get("query")` / `task(store)` class caught
  twice by hand (da8f331/9b7c46b incidents). `--strict` hard-fails; default advisory.
- **CI wiring**: step added to `scripts/ci-local.sh:74-75` right after the dead-export audit
  (verified this session: step present, script passes `bash -n`).
- **TODO_LIST.md close-out**: item marked `[x]` with DONE note and gate citation
  (TODO_LIST.md:81).
- **Docs loop**: close-out report (1c9f5c9) and a verification-lap report (0320234) already
  committed with the same Task-Queue-ID footer.
- **This session's re-dispatch**: verified deliverable intact (script exists, wiring present,
  syntax valid), confirmed item already checked off, made no code change — correct smallest outcome.

## b) PARTIALLY DONE

- **Scanner coverage vs. the rule**: the script only catches literals that EXACTLY match a removed
  identifier. Variants (case-changed, quote-wrapped, suffixed like `query` → `"query"` inside a
  larger literal such as `q.Get("query_value")`) pass silently. Acceptable v1 scope per the work
  item, but the "corruption class" is wider than the detector.

## c) NOT STARTED

- `--strict` is not the default anywhere in CI (advisory by design; a policy decision for the owner).
- No self-test/fixture mode in the script itself (unlike e.g. fanout-libdive.sh `--self-test`).
- No mention of the scanner in README/FEATURES (arguably fine — it's an internal guard).

## d) TOTALLY FUCKED UP

- Nothing this task. The only inefficiency: this dispatch re-ran verification of already-committed
  work — the third such lap for this task ID. The queue re-dispatched a DONE item; no daemon or
  agent fault, but each lap costs a session for a no-op.

## e) WHAT WE SHOULD IMPROVE

1. When a work item is completed and checked off, the dispatching side should skip re-claiming it
   (the harvest-side `[x]` filter should already do this — worth confirming why this item was
   re-dispatched twice after checkoff).
2. Add a tiny fixture-based self-test to `check-rename-hygiene.sh` (positive + negative case) so
   future edits to the scanner are gated, not eyeballed.
3. Consider matching identifier-shaped literals (e.g. `[A-Za-z_][A-Za-z0-9_]*`) against removed
   identifiers as a SECOND advisory tier, distinct from the exact-match tier, to catch near-miss
   corruptions — keep both advisory until FP rate is known.

## f) Next up to 50 things (task-adjacent first; no research done beyond this task's scope)

Scanner / guard family:

1. Fixture self-test mode for check-rename-hygiene.sh (`--self-test`, pinned positive/negative).
2. Second-tier fuzzy matching (identifier-shaped literal vs removed identifiers) behind a flag.
3. Evaluate promoting rename-hygiene scan from advisory to `--strict` in ci-local after a week of
   zero FPs (needs baseline evidence, owner call).
4. Wire the rename-hygiene scan into ci.yml (currently ci-local only — parity question like the
   lint loop had).
5. Document the scanner in AGENTS.md's script list (the rule is there; the script list in the
   smokes section could name it explicitly — check for drift next docs pass).
6. Sweep whether other hand-caught literal-corruption incidents (e.g. `tq dlq --max-attempts`
   help mangling) would have been caught by the scanner; add regression fixtures if not.

Process:
7. Investigate why the harvest loop re-dispatched a checked-off item three times (dedup or
checkpoint issue).
8. Confirm the checkoff's DONE note format satisfies check-todo-list.sh conventions (it passed,
but the long inline note is unusual — verify no gate churn).

(Generic backlog lives in TODO_LIST.md; items above are the ones visible from THIS task's vantage
point — per instructions no unrelated research was done to inflate this list. The repo's real next-
work source of truth remains TODO_LIST.md + the watermarks/sweeper output.)

## g) Questions I cannot figure out myself

1. Was this re-dispatch intentional (e.g. a deliberate verification lap policy) or is the
   done-item dedup filter leaking? If intentional, should agents skip work entirely when
   `git log` already shows a close-out for the same Task-Queue-ID?
2. Should the scanner ever become `--strict` (hard gate) in ci-local, and after what evidence
   period of zero false positives?
3. Do you want the same scan wired into the GitHub CI workflow (ci.yml), matching the advisory
   lint loop's parity precedent, or keep it local-only?

---

Task-Queue-ID: 000001a0a2b62072f2a632ab7d060c74c309
