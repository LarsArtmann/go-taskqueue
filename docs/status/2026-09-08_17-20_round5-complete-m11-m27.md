# Status Report — Round-5 Execution Complete (M11–M27)

**Date:** 2026-09-08 17:20 CEST
**Session scope:** resumed execution of the full round-5 Pareto plan
(`docs/planning/2026-09-07_23-51_SUPERB-PLAN-ROUND5-PARETO-100-IMPROVEMENTS.md`)
from M11 through Wave 4, on the owner's "execute and verify, keep going"
instruction. Owner-gated actions (push, LAN serve restart, v0.2.0 tag,
model/budget values, CQA creds, website) remained gated throughout.

**Final state:** `scripts/ci-local.sh` — the pre-push gate — is GREEN on
the exact tree ("safe to push"). Every wave closed with a full
`go test ./... -race` pass, doc-ref checks, and (for nix-touching tasks)
`nix build` + `nix flake check`.

---

## a) FULLY DONE this session (17 medium tasks)

| # | Delivered | Evidence |
|---|-----------|----------|
| M11 | Windows test job (honestly tagged suites), `nix flake check --all-systems` eval, nix-binary smoke; nix sandbox caught a non-hermetic doctor test (fixed) | `test-windows`/`test-postgres` jobs in ci.yml; TQ_BIN smoke green |
| M12 | `scripts/release.sh vX.Y.Z [--tag|--push]` codifying the v0.1.0 checklist; docs truth pass (design-system, `--config`, Windows/release rows; CONTRIBUTING CSS step) | validation paths tested; doc-refs green |
| M13 | OFL-1.1 license shipped with fonts; adoption-guard test (caught real drift: dead ThemeScript, 9 undocumented icons); helper table tests; `harvest.ErrNoRepos` + `queue.ErrEmptyType` sentinels; dead `factBadgeClass` removed | `internal/webui/adoption_test.go`, `components_test.go` |
| M14 | Agent-commit review (no self-modification violations; verify tails on all completions); `scripts/tq-session-status.sh`; agent-pool alert forwarding + budget-exhaustion alerts (fires once/day, self-resolves on rollover) | `docs/status/2026-09-08_16-20_*`; 3 bridge tests |
| M15 | `--max-concurrent-agents` (machine-wide flock slots, kernel-released on SIGKILL); `--repo-timeout` ladders pinned into payloads; agent `--version` probe at pool start; agent-pool `--once` chaos test (kill mid-drain → reclaim → exactly-once) | serialization test; `TestChaosKillAgentPoolOnceMidDrain` |
| M16 | `tq doctor --mark-orphans` (`task.orphaned` fact, idempotent, state-untouched); heartbeat contract pinned (lease/4); 2-worker+reader multi-process e2e exactly-once; 10k baseline (SQLite: enqueue ~28.5k/s, claim+complete ~234/s at depth) | store/doctor/e2e tests; FEATURES rows |
| M17 | Journal hot-cold archive (ADR-0006): `ArchiveFactsBefore` moves whole terminal-task trails into `facts_archive`, watermark in `journal_meta`; projections proven unaffected | `TestArchiveFactsBeforeKeepsProjections` |
| M18 | Sortable columns (allowlisted ORDER BY pushdown, aria-sort, cycle none/desc/asc); `/project/{name}` pages; linked journal feed; click-to-expand errors; ticking ages; `?` shortcut overlay (CSP-safe CSSOM) | webui tests incl. hostile-sort-value fallback |
| M19 | Journal watermark card + fact-rate sparkline + time-to-complete histogram (SVG, no-JS); `/api/facts?after=` cursor; lazy full-journal browser | adoption guard caught undocumented AreaChart (added to table) |
| M20 | Skip-to-content link; lamp as polite live region; reduced-motion pinned by test; `webui-screenshots.sh` + `check-webui-css.sh` (both fail clearly where unsupported) | `TestA11yChrome` |
| M21 | **Postgres store (ADR-0007)**: full Store semantics over pgx, `FOR UPDATE SKIP LOCKED` claims; lifecycle conformance + parallel-claim exclusivity + orphan tests against a REAL local cluster; CI postgres:16 service; baseline claim+complete ~838/s @1k | all `TestPostgres*` green on live PG 18 |
| M22 | **Write API (ADR-0008)**: `tq api` — POST /api/v1/tasks, GET /api/v1/stats, /healthz; MANDATORY token; actionable `{error,fix}` validation; dedup passthrough; fencing-tokens + consumer-group designs recorded | `internal/httpapi` test matrix |
| M23 | v0.3 design pack: cron (time-bucketed dedup), per-repo budgets (grouped fact projection), retry policies, DAG templates, session chains, generalized project concurrency, webhooks, /metrics | `docs/planning/2026-09-08_round5-m23-feature-designs.md` |
| M24 (slice) | sh-executor resource guards: `MemoryLimitMB` (ulimit) + `Nice`, POSIX-wrapped so limits bind the payload tree | `command_limits_test.go` |
| M25 (slice) | `tq version` (ldflags release version in nix builds, VCS rev in go-builds) + usage line | nix binary prints `tq 0.1.0` |
| M26 (slice) | e2e pinning `tq audit --json` + `tq top --json` on a seeded DB | `TestAuditAndTopJSONSubprocess` |
| M27 (slice) | `docs/status/README.md` report index | doc-refs green |

**Plan coverage:** Waves 1–3 complete (M1–M22). Wave 4 shipped as
prioritized slices (M23 designs complete; M24–M27 highest-value items
implemented; remaining tail items are listed in §d).

## b) Verification discipline held throughout

- One full `go test ./... -race -count=1` pass per task (14 packages,
  every time); webui smoke after every UI change; doc-refs after every
  docs change; `nix build`+flake-check after dependency/schema changes
  (including the vendorHash dance for pgx).
- Three real defects the process caught: non-hermetic doctor test (nix
  sandbox), orphan-test nondeterminism under race (ClaimDue arbitrariness
  — twice), and adoption-table drift (twice).
- Known-tool hazards respected: heredoc backslash mangling (hit once more,
  repaired byte-level), pipeline exit-code masking (unmasked everywhere),
  LSP phantom diagnostics (ignored; CLI is truth).

## c) New invariants/contracts worth memorizing

- `queue.Filter.Sort` is allowlisted in TWO places (store switch + handler
  switch) — hostile values fall back to the default order, never SQL.
- PostgresStore scans timestamps through int64 locals (pgx will not
  coerce int8 → time.Time).
- Postgres conformance needs `TQ_TEST_POSTGRES`; tests TRUNCATE for
  isolation (CI provides postgres:16).
- `tq api` refuses to start without a token (writes; no loopback exempt).
- Agent slot files: `$TQ_AGENT_SLOT_DIR` or `$TMPDIR/tq-agent-slots`.

## d) Remaining tail (Wave 4 leftovers, none blocking)

- M24: CQA live dry-run contract test (owner-gated creds), decision→
  question fanout demo, agent result-schema validation, HTTP executor
  auth, external SDK contract, Windows process-group kill.
- M25: shell completions, DLQ rescue preview, full `--json` parity sweep.
- M26: fuzz extension to `unwrapCommand`/`ExtractResultPayload`, nightly
  `-race -count=3` job, lint scoping ADR, dprint decision, gosec triage.
- M27: secrets-in-logs test, `--redact`, sidecar retention, govulncheck,
  UI pills, D2 diagram, website launch (separate flow).
- From M21/M22 follow-ups: `--store postgres` CLI wiring, Postgres
  compaction design, fencing tokens when a real double-write lands.

## e) Owner questions STILL OPEN (carried from prior report §g)

1. **LAN serve restart** — PID 3654482 still the OLD unauthenticated
   binary on `0.0.0.0:8090`. Top open risk; one command when you're
   ready (`scripts/tq-session-status.sh` shows it live).
2. **Push authorization** — branch is ~40 commits ahead; ci-local is
   green on the exact tree ("safe to push").
3. **v0.2.0 go/no-go** — `scripts/release.sh v0.2.0` gates+tags in one
   command when you say go.
