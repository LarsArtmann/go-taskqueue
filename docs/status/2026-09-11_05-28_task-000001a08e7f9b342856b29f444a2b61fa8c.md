# Task 000001a08e7f9b342856b29f444a2b61fa8c — stale re-dispatch verification close-out

**Date:** 2026-09-11 05:28
**Work item:** harvest skip-log change detection — log a skip class only when its
example changes (or on first tick) instead of every 5m tick flooding journald
(02:00 f17; cmd/tq/main.go)
**Work commit:** 783f4bf (shipped by the prior dispatch, 05:2x window)
**This window:** verification only — zero code diff

## a) FULLY DONE (this window)

- Session-start ritual: `git log --oneline -5` + TODO_LIST.md check immediately
  showed the item pre-shipped — 783f4bf implements the gate and fb7a6fa is the
  prior dispatch's full close-out self-review (docs/status/2026-09-11_05-26
  report for this exact task ID).
- Implementation verified present and correct: `skipLogExamples
  map[string]string` at cmd/tq/main.go:790-836 persists across ticks via the
  `runTick` closure; a class logs on first sight or when its (truncated)
  example reason changes; per-tick `skipped=N` summary keeps steady state
  observable.
- TODO_LIST.md:80 already `[x]` — loop closed by the prior window; nothing to
  re-check.
- Standard verify gate re-run fresh (trust-nothing): `GOEXPERIMENT=jsonv2 go
  build ./... && go vet ./... && go test ./... -count=1` — all green.
- Indexed two UNINDEXED concurrent-session reports (2026-09-11_05-20,
  2026-09-11_05-26) in docs/status/README.md — required to pass the shared
  status-index pre-commit gate (same move as the 04-51 window).

## b) PARTIALLY DONE

- Nothing in scope. The item is a bounded logging change; it shipped whole in
  the prior window.

## c) NOT STARTED

- Nothing in scope. Adjacent but out-of-scope items remain owned by
  TODO_LIST.md: `tq doctor` PATH warning (02:00 f18), dead-pool PapDashboard
  alert (02:00 f6), and the scan-fail gating semantics decision the 05-26
  report escalated to the owner (§d/§g there).

## d) TOTALLY FUCKED UP

- Nothing broke. Honest ledger of this window's own shortcomings:
  1. **I am the third dispatch of this exact task ID** (after 783f4bf and
     fb7a6fa). The queue's dedup does not prevent re-enqueue of an item whose
     harvest-derived DedupKey hashes repo+text — the item text was presumably
     re-added or the completion never propagated to the enqueuer before my
     dispatch. Each stale dispatch burns a full agent window on verification
     theater. (Same systemic gap already named in the 04-27 report.)
  2. **I initially reported "no delta to commit" without writing a report or
     indexing the unindexed concurrent reports.** The first TQ_RESULT closed
     the loop mechanically but left the two 05-2x reports one hook-failure
     away from being lost (the exact 2026-09-08 §d6 failure class). This
     window fixes that, but it should have been reflexive.
  3. **The root-module gate I ran is not the full CI replicant.** The
     standard verify gate covers the root module only; the per-sub-module
     loop (`./scripts/ci-local.sh` or the disk-derived loop in AGENTS.md)
     was not run this window. For a no-diff verification window the risk is
     ~zero (master was green at fb7a6fa), but "green" claims in this repo
     have been repeatedly dinged for exactly this qualification gap (05-17
     report §e).

## e) WHAT WE SHOULD IMPROVE

1. **Stale-dispatch terminal state** (third occurrence across tasks
   …8073b/…862a/…fa8c): a dispatch that finds its work already shipped should
   be able to mark the queue item terminally done, not re-fire. Options: a
   `tq result` write-back command, or harvest skipping `[x]` items at
   re-enqueue time (it may already — needs a root-cause look at why THIS id
   re-fired after fb7a6fa closed it).
2. **Indexing is still a manual reflex, not a default.** Two of the last
   three windows' reports went unindexed within minutes. The pre-commit hook
   catches it at the NEXT commit, but only if someone commits. Consider a
   post-write hook or making the report-writing flow always touch README.md
   in the same edit.
3. **The 05-26 report's e1-e5 findings are still open** and worth acting on
   rather than re-deriving: truncated-example suppression collision, per-tick
   `count` semantics ("count 1" after 12 quiet ticks is misleading), the
   untestable in-closure gate (extract a helper), map-growth bound proof.
4. **Scan-fail gating semantics needs an owner decision** (carried from
   05-26 §g1): steady scan-failed repos now log ONCE ever; a dead deployment
   is silent after tick one except for `skipped=N` summaries. Either accept
   and document, or add re-warn-every-Nth-tick.
5. **Verification vocabulary**: "green" in close-outs should state WHICH gate
   (root-only vs ci-local vs per-module loop) — the 05-17 report flagged this
   for the previous window and I almost repeated the ambiguity here.
6. **Re-dispatch reports could be cheaper**: a fixed "re-verification
   close-out" template (like this one) that references the parent report
   instead of restating it would cut redundant prose from the third-plus
   dispatches.

## f) Up to 50 things we should get done next

Seeded from TODO_LIST.md unchecked items, this task's residue, and things
noticed in passing. Tiered by impact/effort.

### Tier 1 — do next (high impact, bounded)

1. Root-cause why task …fa8c re-fired after its 05-26 close-out commit landed
   (harvest dedup vs completion propagation) — kills the whole stale-dispatch
   class. (02:00 f-series meta)
2. `tq doctor`: warn when crush/git/go are missing from PATH — the exact
   failure class that killed the deployed pool for 20h. (02:00 f18;
   cmd/tq/doctor.go)
3. Dead-pool detection: PapDashboard alert when scan-failed skips cover every
   repo for N consecutive ticks. (02:00 f6; agent-pool tick loop +
   internal/bridge)
4. Owner decision + implementation: scan-failed re-warn cadence (every Nth
   tick vs once-ever) — the 05-26 §g1 escalation is still unanswered.
5. Extract the skip-log gating decision from the runTick closure into a
   testable helper + unit test (05-26 report e3).
6. `tq pool-health`: one-shot per-repo skip-streak + last-harvest liveness
   summary. (02:00 f27)
7. Dogfood smoke: scripts/smoke/dogfood-once.sh (env-gated TQ_DOGFOOD=1;
   stub-agent variant ungated). (02:00 f10)
8. Archive the 2026-09-10 dogfood proof journal from /tmp before reboot loses
   it. (02:00 f9)
9. Fix the truncated-example collision: either key the gate on the FULL
   reason or document why truncation-keyed suppression is correct
   (05-26 report e1).
10. Fix skip-log `count` semantics: log count-since-last-log, not per-tick
    count (05-26 report e2).

### Tier 2 — quality debt

11. Prove or bound `skipLogExamples` map growth (class set = reason-text
    prefix); add a cap if unproven. (05-26 report e4)
12. Run `./scripts/ci-local.sh` once on master post-05-2x burst to confirm
    the multi-module gate over the concurrent windows' combined diff.
13. `tq doctor --repos` + expandRepoSpecs: reconcile agent-pool's inline join
    with expandRepoSpecs (cwd-relative pass-through divergence flagged in the
    04-43 report — intent still unknown).
14. Verify the CQA bridge against a live CQA instance (BLOCKED on owner:
    URL + token — carried from TODO_LIST).
15. Worktree-per-agent design doc. (02:00 f15; docs/planning/)
16. Session-close bridge: prototype the PreToolUse session-registry trigger
    (rank 1 in the researched lineage). (TODO_LIST session-close item)
17. Dead-letter retry storm guard: cap how often the same dedup-keyed item
    can be re-enqueued per day even when the text changes trivially.
18. `tq tasks --stale` helper: list queued tasks whose TODO_LIST item is now
    `[x]` (would have caught this re-dispatch in seconds).
19. Add a `TQ_RESULT` outcome field convention ("already-done" vs "fixed" vs
    "failed") so the queue can distinguish no-op verifications from real
    work statistically. (04-18 report §g3 overlap)
20. Status-index: fail CLOSED — make the pre-commit hook reject when a NEW
    report is in the diff without an index row, rather than only checking
    existing reports. (05-17 §e item, still open)

### Tier 3 — hardening / hygiene

21. Per-tick harvest summary: include per-class counts (not just aggregate
    skipped=N) at debug level for forensics.
22. Docs: document the skip-log gate semantics (once-per-example) in
    FEATURES.md or the agent-pool section of AGENTS.md.
23. Consider journald rate-limit friendliness: document the flood fix in the
    deploy/systemd README so operators know the old flood is fixed.
24. Smoke: assert the gate behavior end-to-end — a two-tick stub-agent smoke
    that counts skip-class log lines (regression net for helper extraction).
25. `tq watermarks show`: add per-consumer lag-human durations (noticed the
    surface while reading AGENTS.md watermarks contract).
26. Review whether any OTHER per-tick logs flood the same way (grep the tick
    loop for unconditional slog calls — the f17 fix was class-by-class).
27. FuzzParseRepo nightly: confirm last campaign committed seeds and stayed
    green (routine check, cheap).
28. check-dead-exports.sh: run it once over this week's diff surface
    (advisory, cheap).
29. TODO_LIST: annotate the two long design items (session-close,
    worktree-per-agent) with current-owner/next-action so they stop
    re-surfacing in every harvest without moving.
30. CHANGELOG: confirm 783f4bf is represented in the unreleased section
    (append-only file, one line).
31. Docs-health pass on docs/status/README.md index descriptions (several
    rows exceed reasonable width; readability).
32. Cross-repo: verify sibling project-discovery-sdk's "Open Work" section
    parses under the current harvest rules (its queue-facing section is
    special-cased; cheap drift check).
33. `tq audit`: surface skip-class streaks in audit output (reuses the f27
    grouping already built).
34. Agent payload contract test: extend TestAgentExecutorArgvContract to pin
    that close-out sessions never regress to `--continue` (regression net
    for the documented --session invariant).
35. Budget guard: alert (not just skip) when daily budget is exhausted —
    operators see "why is the pool idle" faster. (adjacent to f6)
36. Sweeper watermark: document the head-bootstrap behavior next to the
    watermarks table docs (one-liner, prevents repeat confusion).
37. webui: surface harvest skip classes on the dashboard (data already
    exists in the tick; steady-state liveness becomes visible).
38. Verify `go-retry` usage parity note in AGENTS.md still matches code
    (documented exceptions list — cheap re-verify).
39. Add the report-writing flow (report + index row + commit) to AGENTS.md's
    session ritual so it is reflexive, not rediscovered.
40. e2e: run `TQ_BIN=result/bin/tq ./scripts/smoke/status-loop.sh` once this
    week against the current binary (last documented run predates the f17
    change).

### Tier 4 — worth considering (not committed)

41. Named skip-class constants instead of ad-hoc prefix strings (strong
    typing for the class set).
42. `tq doctor --json` for machine consumption by the dead-pool alert.
43. Consider emit-once ledger as journal facts (`harvest.skipclass.logged`)
    instead of in-memory map — makes the gate crash-restart-safe (currently
    a restart re-logs every class once).
44. Skip-class cardinality metric in the tick summary (detects class
    explosion early).
45. Re-dispatch budget: cap agent-task re-enqueues of the same DedupKey per
    day (needs f1's root cause first).
46. Multi-writer docs problem: revisit dprint gating ONLY if plugin pinning
    lands (AGENTS.md says don't re-litigate — listed for completeness, not
    an ask).
47. Pool `--dry-run` harvest mode: show what WOULD be enqueued (aids the
    stale-dispatch forensics).
48. `tq show <task>`: render payload contracts (sh/agent/review/status) with
    type-aware pretty-printing.
49. SSE: heartbeat flake (two-agent corroborated, 04-05 report) — add a
    client-side reconnect-backoff assertion to the webui smoke.
50. Retire the retired repo-root tasks.db to an archive path (still on disk;
    AGENTS.md says orphan serve stopped, the file itself lingers).

## g) Questions I could NOT figure out myself

1. **Why did THIS task re-fire after fb7a6fa closed it?** The TODO item was
   `[x]` before my dispatch, yet the queue handed me the ID again. Is there
   a re-enqueue path (harvest race, manual enqueue, or the close-out
   close-out turn itself) that ignores checked items? I can fix the symptom
   (§f1) but not name the trigger from inside one window.
2. **Scan-fail gating semantics (carried from 05-26 §g1, needs an owner
   verdict):** for a persistently failing repo with a stable reason, is
   once-ever logging + `skipped=N` summaries acceptable steady-state, or
   should scan-failed re-warn every Nth tick? This decides §f4.
3. **Should stale-dispatch windows produce a full close-out report at all?**
   A verification-only window adds an indexed report + a commit per no-op
   dispatch. Cheaper alternative: a `tq result` write-back with no report
   file. That changes the close-out contract for the pool — owner call,
   since it trades auditability for queue noise.
