# Status Report — task 000001a0ac8eccc0a28fe8d84d67f29e481b

**2026-09-17 01:40 CEST · Work item: `scripts/smoke/session-close.sh` (TODO_LIST "Done-prompt window harvest 2026-09-12") · Scope: THIS task's work only + what was noticed in passing.**

---

## a) FULLY DONE

1. **The smoke script exists and passes live.** `scripts/smoke/session-close.sh` (75 lines, same conventions as `reviews.sh`: mktemp scratch dir, `TQ_BIN` override, `build-tq.sh` otherwise, exports `TQ_DB` — never touches production). Verified green twice at HEAD (`== session-close smoke passed`), plus `bash -n` and the repo's `check-script-syntax.sh` (shellcheck 0 findings).
2. **Every contract assertion from the TODO row is covered**: begin confirms the opening; a commit carrying `Crush-Session: <id>` gets attributed at close (`1 attributed commit`); exactly one review + one status task minted (counted via `tq tasks --type X --json`); second close reports `known (dedup)` and mints nothing; journal facts pinned at 1× `session.opened` + 2× `session.closed` (one per close — discovered during development that replay appends a fact even though it mints nothing; the script now documents that behavior).
3. **Wired into ci-local** as a hard step right after the review-loop smoke (`scripts/ci-local.sh` "session-close bridge smoke" step). `check-guard-wiring.sh` passes: no orphaned smoke.
4. **TODO_LIST row closed with citations**: line 105 marked `[x]` with a DONE note carrying the live gate run + the cmd/tq alignment discovery.
5. **AGENTS.md updated**: smoke added to the commands list (line 92).
6. **Committed with the exact footer** `Task-Queue-ID: 000001a0ac8eccc0a28fe8d84d67f29e481b`. Not pushed.
7. **Wider verify at HEAD**: root `go build ./... && go vet ./...` green; full `go test ./... -race` green; `./scripts/test-cmd-tq.sh` (cmd/tq module gate) green; `check-todo-list.sh` green after the row edit.

## b) PARTIALLY DONE

1. **The go 1.27.1 go.mod alignment is fixed for cmd/tq ONLY.** My smoke could not build tq at all until `cmd/tq/go.mod`'s `go` directive went 1.26.7 → 1.27.1 (root/journal/queue-top were bumped by a 2026-09-16 daemon commit f457375 without the rest). `check-go-mods.sh` STILL FAILS at HEAD: `task` (declares `go 1.26`), `worker`, `queue`, `queue/sqlite`, `queue/postgres` (all 1.26.7) vs root 1.27.1. I deliberately did not sweep ~15 module files — that touches release/version surfaces outside this task's scope and belongs to one conscious change. A backlog row exists (see f1).
2. **Footer attribution is split across two commits.** The auto-commit daemon raced me and committed the smoke script + cmd/tq go.mod as heuristic commit `0f495e1` BEFORE my footer commit; my message + footer landed on the amended head `c870555` (AGENTS/TODO/ci-local). The queue's git cross-reference therefore sees the ci-local wiring under the footer, but the smoke script file itself rides a `chore: heuristic` commit. Both unpushed; multiple commits per task are sanctioned, so I chose amend-only over a rebase that would rewrite the daemon commit (scripted-history-rewrite incident class). Consequence: `git log --grep` by task ID misses 0f495e1.

## c) NOT STARTED

1. Nothing in the task contract. The task was one smoke + ci-local wiring + TODO close-out — all delivered.
2. The follow-ups discovered in passing were routed to the backlog per contract (not started, correctly): the alignment sweep (minted this pass), and everything already sitting in TODO_LIST untouched (session triggers #1/#2, mint-time done-check, transient-retry calibration, etc. — not this task's scope, listed for completeness in f).

## d) TOTALLY FUCKED UP!

Nothing at the "fucked up" level. Honest near-misses, all self-caught before landing:

1. **The first version of the smoke asserted the WRONG journal invariant** (`exactly 1 session.closed fact` after a replay close — actually 2, one per close). The first live run caught it; fixed before any commit. Lesson already on file: run the thing, don't trust the spec in your head.
2. **I burned two dead-end build attempts on a host/toolchain rabbit hole** before root-causing the dev.mod failure to the cmd/tq go.mod misalignment: first blamed the missing flake devShell, then tried PATH-privileging the 1.27.1 go binary (the error text "running go 1.26.7" was real — the -modfile build selects the toolchain from dev.mod's OWN go directive, and GOTOOLCHAIN=auto does not re-switch for a REPLACED module's newer requirement). The pinned-`GOTOOLCHAIN=go1.27.1` experiment was what isolated the cause. Cost: ~3 extra tool calls; the diagnosis itself was worth having (it's not in AGENTS.md — added to e2).
3. **Session-start ritual items all done** (git log over whole repo, status, stash, `rg -l <task-id> docs/status/`, CONTRIBUTING.md read) — no miss to report. This one's clean.

## e) WHAT WE SHOULD IMPROVE

1. **Daemon-commit vs footer-commit racing is now the default failure mode for every task.** Two of my two file-groups got daemonized mid-task. The amend maneuver works but only fixes the HEAD commit; earlier daemon commits keep unattributed diffs. Improvement idea already implied by the format: agents should `git add` + commit each logical unit IMMEDIATELY after verifying it, shrinking the daemon's window — the 2026-09-13 lesson "commit per task" wants an upgrade to "commit per task, immediately".
2. **Document the -modfile toolchain quirk**: "a -modfile build does not auto-switch toolchains for replaced modules' newer go directives; a root go directive bump must bump EVERY module (cmd/tq included) in the same commit, or all devmod gates die with a misleading 'running go <old>' error." This cost a concurrent window real time and is pure AGENTS.md material (concurrent agent should harvest it — not mine to self-claim this pass).
3. **`check-go-mods.sh` is the gate that KNEW**: it fails loudly right now, but nothing forced the 2026-09-16 bump window to run it before the daemon swept the drift up. The gate lives in ci-local (pre-push), so unpushed drift sits silently in the tree until the next push. A cheap pre-commit or daemon-aware check would catch go.mod drift within minutes, not windows.
4. **The smoke's replay-close fact semantics** (2× `session.closed` for one logical close) were only discoverable empirically. A one-line doc in the session-close design doc would save the next author the same discovery — minor.

## f) Up to 50 things we should get done next

Sorted by impact. Items 1–2 minted into TODO_LIST this pass where the contract allowed; the rest are backlog fuel for docs-health HARVEST (most extra items are roadmap-grade, not commitments).

**Immediate (this repo, blocking or near-blocking):**

1. Finish the go 1.27.1 go.mod alignment sweep (task, worker, queue, queue/sqlite, queue/postgres + audit for stragglers) so `check-go-mods.sh` is green again — currently a guaranteed red master at next push. *(minted to TODO_LIST 2026-09-17)*
2. Verify master CI state (`gh run list`) before the next push — 27 unpushed commits carry the go bump drift and my window; ci-local's own master-CI check will refuse until verified.
3. Run the full `./scripts/ci-local.sh` once post-alignment-fix (my window ran targeted gates, not the full replicant — the smoke is wired but has not survived a complete gate lap yet).
4. Harvest the -modfile toolchain quirk into AGENTS.md Known Issues (see e2).
5. Confirm the smoke's TQ_BIN path works against the nix-built binary (`TQ_BIN=result/bin/tq ./scripts/smoke/session-close.sh`) — ci-local smokes the nix tq where the build uses build-tq.sh; both paths should be exercised at least once.
6. Add the CI (ci.yml) counterpart if policy wants CI parity for the new smoke — ci-local wiring alone satisfies check-guard-wiring; decide whether GitHub CI needs the same step (precedent: other smokes are ci-local-only or CI-looped inconsistently).

**Session-close bridge follow-ups (my smoke's neighborhood, all pre-existing backlog):**

7. Session trigger #1 — PreToolUse session registry sweeper (TODO row 106).
8. Session trigger #2 — `tq crush` wrapper close fallback (TODO row 107).
9. Postgres parity for the session bridge's `AppendFact` path.
10. Budget-gate decision for session-close mints (known bypass class from the bridge design doc).
11. Daemon-commit attribution gap for session-close mints (bridge design doc OQ list).
12. `tq session list` + `close --dry-run` (M10 hardening batch, still open).
13. gosec re-scan of internal/session (M10 batch).
14. Dead-export re-run (M10 batch).
15. Review dedup interplay test: sweep-minted reviews vs session-close namespaces (M16 in the sweep plan).

**Repo hygiene noticed in passing:**

16. CHANGELOG entry for the session-close smoke + ci-local wiring (CHANGELOG is append-only; this window wrote none).
17. FEATURES.md check: whether the smoke warrants a features-list mention per its conventions.
18. Decide a policy for the smoke's fact-count pin (2× session.closed) — if replay semantics ever change, the smoke is the pin that breaks; make sure the design doc references it.
19. The lint-baseline gate: my window added no lint growth (verified by check gates I ran), but the pending mnd/varnamelen/lll deliberate-regen items from row 104's neighborhood are still open in TODO_LIST.
20. Docs-health HARVEST of this report's f-section into TODO_LIST/ROADMAP with routing rigor (items 7–15 are already-harvested backlog; 16–20 are the new candidates).

**Beyond this task (standing work seen in TODO_LIST, unordered, roadmap-grade):**

21. Mint-time done-check for repeat dispatches (standing top fix, row minted 2026-09-17).
22. Calibrate transient-retry 45s×3 budget from real evidence.
23. go-cqrs-lite queue-module re-open check when upstream ships parity.
24. The templ-components consumer-sweep M-batch leftovers.
25. Prioritize-scorer adoption decision (`--prioritize` still default-OFF AI spend).
26. Depsweep adoption follow-through (`--dep-sweep` real-run calibration).
27. Session-close bridge trigger automation (crush #3146 upstream dependency).
28. Postgres backend conformance in CI (TQ_TEST_POSTGRES job exists — confirm it still runs green on runners).
29. website-launch/docs-site work if still desired for the project's public surface.
30. Release cadence: the go 1.27.1 directive bump has release implications (consumers need go ≥ 1.27.1 for the next tag) — plan the next release's CHANGELOG wording and sub-tag cutting with that in mind.

*(Items 31–50 deliberately not padded out: the standing TODO_LIST already holds the repo's real queue; duplicating it here would be noise. Sections a–e + 1–30 are the honest inventory.)*

## g) Questions I can NOT figure out myself

1. **Should the go 1.27.1 sweep be one commit over every module, or per-module?** The bump already sits unpushed on master (root/journal/queue-top via daemon f457375) — do you want the remaining ~15 go.mods swept now by a follow-up task, or rolled into the next release-prep window with its sub-tag cutting? (My backlog row assumes "sweep now"; confirm or reroute.)
2. **Do you consent to amending UNPUSHED daemon commits to attach Task-Queue-ID footers** (what I did for c870555), or should agents instead leave daemon commits untouched and add a follow-up footer-carrying commit? The amend path rewrites an existing commit (though unpushed, so no fork); the follow-up path leaves unattributed diffs in history forever. A standing ruling would remove the judgment call from every future window.
3. **Is the new smoke's scope right — enqueue-only, no pool lap?** The TODO row specified begin → close → dedup assertions, which is what ships. A deeper variant would drain the minted review/status tasks through a stub-agent pool lap (proving the minted payloads actually execute), roughly doubling runtime. Want that as a separate backlog row, or is the payload-contract test coverage in internal/session sufficient?

---

*Point-in-time snapshot. Citations: live gate runs this session (smoke ×3, root build/vet/race, test-cmd-tq, check-guard-wiring, check-script-syntax, check-todo-list, check-go-mods FAIL output), commits c870555 + 0f495e1, TODO_LIST.md:105, AGENTS.md:92, scripts/ci-local.sh session-close step. Repo state at writing: 27 unpushed commits ahead of origin/master, tree clean.*
