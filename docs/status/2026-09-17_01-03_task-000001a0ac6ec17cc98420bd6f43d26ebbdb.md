# Verify-Only Re-Dispatch Close-Out — claims-with-citations convention (row 100)

**Date**: 2026-09-17 01:03
**Task**: `000001a0ac6ec17cc98420bd6f43d26ebbdb`
**Verdict**: DONE — verify-only close-out, ZERO edits made

## What this dispatch was

Second (observed) dispatch of the row-100 work item ("AGENTS.md
claims-with-citations convention: every filter-scope/behavior claim in
reports and handoffs carries file:line or a test name"). The first
dispatch shipped it; this window ran the turn-1 ritual FIRST, found the
DONE row, verified the shipped state, and stopped.

## Verification (cited, per the convention this task created)

- Convention live in `AGENTS.md:456-458` ("Claims carry citations (2026-09-12)… filter-scope/behavior claims… cite file:line or a pinning test name").
- Prior close-out on disk: `docs/status/2026-09-17_00-59_task-000001a0ac6ec17cc98420bd6f43d26ebbdb.md`; index row at `docs/status/README.md:44`.
- TODO row `[x]` at `TODO_LIST.md:100`.
- Footer verified present on the landed commit `c888be6` (the daemon-raced commit recovered via unpushed-amend per the prior report) — `git log -1 --format=%B 9aaa7fc` carries exactly one well-formed `Task-Queue-ID:` footer; the follow-up report commit `06c1c5d` also carries one.
- `git status` clean at HEAD `06c1c5d`; no repo state touched this window.

## a) FULLY DONE

- The work item itself (first dispatch): convention written into AGENTS.md Conventions, TODO row ticked, report + index row landed, footer-bearing commit chain in history.
- This window: ritual → verify → stop, with citations above. No duplicate report content, no re-edit of shipped work.

## b) PARTIALLY DONE

- Nothing for this task. The prior report's own §f (16 items, led by owner push / mint-time done-check / daemon footer arbitration) remains open by design — backlog, not this run.

## c) NOT STARTED

- No new work discovered this window; nothing appended to TODO_LIST (contract forbids items describing THIS task, and nothing else surfaced).

## d) TOTALLY FUCKED UP

- **The re-dispatch itself is the process failure** (N+3 recurrence of the class): the queue re-fired a DONE row. Nothing an agent prompt can do fixes this — the structural fix is the queue-side dedup-key → COMPLETED short-circuit already requested in the 00-22/00-33/00-37 reports (still unanswered).
- This window itself: near-miss on the ritual order — I checked `git log`/`rg` for the task ID in the FIRST tool call, so nothing was missed; but I did not READ `CONTRIBUTING.md` at turn 1 (the documented N+4 recurrence of that miss). Recorded honestly here rather than silently skipped.
- Cost observation: this is at least the fourth verify-only close-out of an already-done row across two task IDs in one evening; report overhead now dominates actual work in this loop.

## e) WHAT WE SHOULD IMPROVE

1. Queue-side done-check at MINT time (dedup key of a COMPLETED task → refuse re-enqueue or return the stored verdict) — the only structural fix for this loop.
2. Turn-1 ritual: actually READ CONTRIBUTING.md, not just note its existence (owner contract, recurring miss).
3. Lightweight verdict format for verify-only re-dispatches (a fixed short template) so close-out cost drops an order of magnitude.

## f) NEXT (deltas only — prior reports' items NOT duplicated)

1. Authorize + implement the queue-side dedup→COMPLETED short-circuit (carried from 00-22 §f1, now N+3).
2. Same short-circuit applies to the sibling loop (task 000001a0ac3c674a…, three dispatches) and 000001a0ac3c677a… (two) — one fix covers three loops.
3. Mint-time done-check as a cheaper interim: reject re-enqueue when the dedup key maps to a non-PENDING task and surface "already done" in the dispatch log.
4. Measure the re-fire rate properly (3-sample baseline over the journal: distinct task IDs with >1 report file) to size the fix's payoff.
5. Mechanical close-out citation gate (prior report's §g3): a check that every new report's claims carry file:line/test-name patterns — would also catch future scope-claim regressions.

## g) Questions (carried, unanswered from prior windows)

1. Owner: authorize the queue-side dedup→COMPLETED short-circuit? (Three loops now demonstrate the cost; design sketch in 00-22 §d.)
2. Owner: is a SHORT-FORM verdict acceptable for verify-only re-dispatches, so the loop stops paying full-report cost?
3. Owner: the daemon footer-arbitration hook (prior report's top §f) — approved to design, or does the unpushed-amend recovery stay the sanctioned path?
