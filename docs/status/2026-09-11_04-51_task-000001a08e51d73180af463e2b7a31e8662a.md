# Task 000001a08e51 close-out — `tq doctor --repos` bare-name expansion

**Work item:** "`tq doctor --repos`: expand bare repo names via expandRepoSpecs like harvest/audit — last raw splitRepos consumer; bare names stat against the cwd in doctorRepoAutonomy (02:00 f8 residue, 04:05 verify session; cmd/tq/doctor.go:355 + repospec_test.go row)"

**Session:** 2026-09-11 04:48–04:51 UTC, autonomous queue dispatch, unsupervised.

---

## Headline

**Stale re-dispatch. The work already shipped.** Commit `2fa97ea` ("fix(doctor):
resolve bare --repos names against --projects-dir") landed in a prior session,
TODO_LIST.md:77 was already `[x]`, and two close-out reports existed
(docs/status/2026-09-11_04-43 for this exact task ID, plus the 04-27 and 04-05
sweep-task reports that flagged the residue). This session's entire job was
trust-nothing re-verification, which passed in full. Zero new diff, zero new
commits of code.

This is the THIRD dispatch of this task ID on record — the 04-43 report was
itself the fix session. A session-start ritual (`git log --oneline -5` +
grepping TODO_LIST for the item before touching anything) caught it in under
a minute and prevented a trophied no-op commit.

## What was actually verified this session (from scratch, not from the reports)

1. **Wiring check (grep, not trust):** `cmd/tq/doctor.go:357` reads
   `for _, repo := range expandRepoSpecs(opts.ProjectsDir, splitRepos(opts.Repos))`.
   Doctor is no longer a raw `splitRepos` consumer.
2. **Pinning test exists:** `TestCmdDoctorResolvesBareRepoNamesAgainstProjectsDir`
   at cmd/tq/repospec_test.go:63 — runs `cmdDoctor` from a foreign cwd and
   asserts a bare `--repos` name resolves against `--projects-dir`.
3. **TestExpandRepoSpecs table green:** all four cases (bare joins projects
   dir, absolute untouched, existing cwd-relative wins, no-projects-dir
   passthrough).
4. **Full `cmd/tq` suite green with `-race`** (7.975s, ok) plus root
   `go build ./...` with `GOEXPERIMENT=jsonv2`.
5. **Working tree clean** at session start — nothing landed mid-session from
   concurrent agents to trip over.

---

## a) FULLY DONE

- **The work item itself** (shipped 2fa97ea, prior session; re-verified this
  session): `doctorEnvironment` routes `--repos` through `expandRepoSpecs`;
  doctor gained `--projects-dir` (default `$TQ_PROJECTS_DIR` or `~/projects`,
  same as harvest/audit); pinned by the repospec_test.go row.
- **Stale-dispatch detection:** session-start ritual correctly identified the
  pre-existing fix before any edit; honored the "build on, never revert"
  contract by touching nothing.
- **Re-verification:** build + pinning tests + full module race suite, all
  green; results reported honestly as a re-verification, not as new work.
- **TODO_LIST.md:77** correctly marked `[x]` with an accurate DONE annotation
  (audited this session — claims match the code).

## b) PARTIALLY DONE

- **Nothing from this task.** The only partial angle: the verification was
  test-suite-deep, not live-binary-deep — I did not rebuild `tq` and run a
  real `tq doctor --repos go-taskqueue` from a scratch cwd against a scratch
  DB. The pinning test already does exactly that in-process (foreign cwd,
  bare name), so the marginal value was low; noting it for honesty.

## c) NOT STARTED

- **Nothing assigned to this task.** Adjacent open items noticed in passing
  (all correctly left alone per the no-scope-creep contract, and already
  tracked in TODO_LIST.md): flake module-eval PATH assertion;
  `checkProjectsDir` relaxing for all-absolute `--repos`; harvest skip-log
  change detection. See (f).

## d) TOTALLY FUCKED UP

- **Nothing.** No edits, no reverts, no broken state, no wasted commit. The
  queue's dispatch layer, not this session, is the wobble: the same task ID
  was dispatched again after its own close-out existed. Not fatal — the
  ritual caught it in ~1 minute — but each stale dispatch costs a session
  and muddies the journal.

## e) WHAT WE SHOULD IMPROVE

1. **Dispatch-side dedup:** the enqueuer should refuse (or warn) when the
   TODO item's key is already `[x]` with a DONE annotation referencing a
   commit. This is the second 3-dispatch cluster this week (04-27 report was
   another).
2. **Close-out → TODO_LIST feedback loop latency:** the fix session completed
   the item, yet the queue still held it as pending work. If close-outs append
   the TQ_RESULT's task ID to the item text (or the harvester greps
   docs/status for `task-<id>`), staleness becomes detectable mechanically.
3. **Verification depth for re-dispatches:** a re-dispatch's cheapest honest
   proof is (ritual check) + (pinning tests verbose) + (module race suite) —
   exactly what ran. Could be a tiny scripted recipe so future stale sessions
   take 2 minutes instead of 5.
4. **`--projects-dir` flag parity table:** doctor/harvest/audit/agent-pool/
   bootstrap now have five expansion stories (expandRepoSpecs ×3, agentpool
   inline join, bootstrap resolveRepos). Divergence is documented in three
   status reports but still lives only in tribal memory. One consolidation
   item should own it (see f).

## f) Up to 50 things to get done next

Ranked by impact; most are ROADMAP fuel (harvest with routing rigor — do not
blind-pipe 30–50 into TODO_LIST).

**High impact / small (do soon):**

1. Unify repo-spec expansion: one policy helper behind
   expandRepoSpecs / agent-pool join / bootstrap resolveRepos (ADR-worthy;
   three status reports already flagged it).
2. Dispatch dedup: refuse/stale-mark queue items whose TODO_LIST entry is
   already `[x] … DONE … <sha>`.
3. `checkProjectsDir`: don't require the projects dir when every `--repos`
   entry is absolute (TODO_LIST, 02:00 f16) + test.
4. Harvest skip-log change detection: emit a skip class only on change/first
   tick (02:00 f17) — journald flood fix.
5. Flake module-eval: assert tq-agent-pool `Environment` PATH non-empty
   (02:00 f11).
6. Doctor binary E2E smoke: a scripts/smoke case exercising
   `tq doctor --repos <bare>` from a foreign cwd against a scratch
   `TQ_DB` (closes this session's only honesty gap).
7. Sweep the report corpus (2026-09-11 03-34/04-05/04-27/04-43 §"next" lists)
   for items still unharvested into TODO_LIST — the 04-43 report alone lists
   ~19 numbered follow-ups.

**Medium (tracked, not urgent):**

8. Consider `expandRepoSpecs` returning a resolution report (resolved vs
   passed-through vs unresolved) so sweeps can log provenance.
9. Document "repo spec" (bare name vs path vs cwd-relative) in
   docs/DOMAIN_LANGUAGE.md.
10. ADR consolidating the seven splitRepos/expand call-sites once unified.
11. Add doctor to the smoke suite's TQ_BIN parity list if not covered.
12. Re-check the runner-only vendorHash FOD mismatch class (2026-09-10 d1)
    next time nix CI flakes locally-green — differential module fetch dump.
13. Quarantine pattern for stale re-dispatches: allow an agent to append
    `— DONE-ELSEWHERE: <sha>` to a queue item so the sweeper prunes it.
14. `tq doctor` output: surface the resolved repo path per repo so users see
    what a bare name became.
15. Vet whether `--projects-dir` default (`~/projects`) should be
    config-file-backed rather than env/flag only.
16. Test naming audit: repospec_test.go now carries three test families for
    one helper — consider a table-of-tables or split file by command.
17. Add a `go test ./cmd/tq -run TestExpandRepoSpecs` line to AGENTS.md's
    quick-verify list for repo-spec work.
18. Harvest the 04-27 report's "doctor --repos left to its own dispatch"
    trail into a docs index cross-link so the next auditor finds all four
    related reports from one entry point.
19. Consider making the session-start ritual a hook (crush hooks: block Edit
    until `git log --oneline -5` ran this session).
20. Trim: splitRepos trims; confirm string-literal safety of any future
    renames near flag text (`--max-attempts` incident class).

**Low / exploratory (ROADMAP fuel):**

21. Provider-pluggable expansion (e.g. `--repos @all` meaning every
    projects-dir child).
22. Config file for doctor defaults (recurring flags: projects-dir, timeout).
23. `tq doctor --json` for machine consumption (dogfood: the pool could
    self-diagnose).
24. Doctor check for journal-consumer watermark lag (currently visible only
    via `tq watermarks show`).
25. Doctor check for DLQ depth threshold warning.
26. Per-repo doctor parallelism (currently sequential autonomy checks).
27. Track expandRepoSpecs coverage with a lint-time assertion (grep-gate in
    ci-local: no raw `splitRepos(...)` outside main.go's expand helper and
    agentpool/bootstrap with an inline justification comment).
28. Renamed status-report index: status README table is growing; consider
    monthly archive rotation.
29. Retired root `tasks.db`: document its final disposition (delete? archive?)
    in AGENTS.md once confirmed nothing reads it.
30. `.tq-verify` contract doc: one page explaining why agents may never edit
    it (self-dealing) — exists in spirit, not as a doc.
31. Doctor: warn when `TQ_DB` env points at production while operating on a
    scratch cwd (the 2026-09-10 stray-enqueue class).
32. Smoke for `tq watermarks set` rewind safety (replay idempotence).
33. Nightly fuzz campaign dashboard link in README.
34. Dead-export audit: make check-dead-exports.sh gated (non-advisory) once
    substring-matching confidence is proven over a month.
35. golangci baseline decay: budget a session to re-triage and shrink the
    ~400-finding advisory set.
36. gosec baseline: same treatment (48 findings, all triaged FP — encode the
    triage as nolint-with-reason where the linter supports it).
37. Windows CI: agent-pool/doctor paths are POSIX-flavored; add a windows
    smoke if the commands claim cross-platform.
38. `tq top`: consider SSE-driven live refresh instead of poll loop, if not
    already.
39. WebUI: write-route lockout metrics (failed CSRF attempts) as a
    doctor-visible signal.
40. Bootstrap: offer `--projects-dir`-relative default repos suggestion in
    help text (discoverability of the expansion semantics).
41. Docs: single-page "repo name resolution" cheat sheet for all five
    commands with examples.
42. Consider `errors.AsType[E]` migration sweep residue check (erraudit) on
    cmd/tq if not run since the modernization push.
43. Perf: expandRepoSpecs stats each spec serially; fine at n≈10, note the
    ceiling in the doc comment.
44. Journal forensic view: `tq facts` filter by fact type in the webui.
45. Version surfaces doc: add doctor's new flag to the surfaces checklist if
    flags are one of the seven surfaces.
46. Release notes template: remind the cutter to check AGENTS.md "Known
    Issues" for entries that a release resolves.
47. Add `docs/status/README.md` row for THIS report (creation-time indexing
    is a pre-commit gate — confirm it fired).
48. Consider naming the dispatch dedup idea (e1/e2) as a single TODO item so
    it survives this report (HARVEST candidate #1).
49. Review whether stale-queue hygiene belongs in `tq doctor` itself
    (self-referential but cheap: parse TODO_LIST `[x] … DONE` vs live queue).
50. Fun-size: AGENTS.md's expandRepoSpecs consumer list (currently "harvest,
    audit") should now read "harvest, audit, doctor" — one-line doc fix;
    verify current text first (concurrent agents).

## g) Questions I can NOT figure out myself

1. **Dispatch layer ownership:** who/what re-enqueues completed items, and is
   there a dedup surface (DedupKey reuse at harvest time, or a dispatch-side
   TODO_LIST check) I could wire without touching the queue's contract? The
   harvest provenance key would suppress re-enqueue of an _edited_ item but
   clearly didn't suppress this re-dispatch — was the item text changed
   between dispatches, or is the enqueuer bypassing dedup intentionally?
2. **Is a 3-dispatch cluster acceptable dogfood noise,** or do you want a
   mechanical rule (e.g. agent MAY mark a queue item cancelled/dead when its
   TODO anchor is already `[x] DONE <sha>`)? I can implement the rule; I
   can't decide the policy.
3. **Verification depth policy for stale re-dispatches:** is
   ritual + pinning tests + module race suite (what I ran) the agreed floor,
   or do you want the binary E2E smoke (item f6) built so re-dispatches get
   full parity proof cheaply?

---

_Point-in-time snapshot. Code claims verified live at 2026-09-11 04:51 UTC
against master @ b65cffd. Status reports go stale — re-run the pinning tests
before trusting this file over the code._

TQ_RESULT: {"files_changed": [], "commit_sha": "2fa97ea", "note": "stale re-dispatch: doctor bare-name expansion pre-existed (2fa97ea, TODO_LIST:77 already [x]) and was re-verified from scratch — expandRepoSpecs wiring + pinning tests + full cmd/tq race suite all green, zero new diff"}
