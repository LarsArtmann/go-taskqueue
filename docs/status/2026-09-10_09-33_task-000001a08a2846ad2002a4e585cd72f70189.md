# Status Report — task 000001a08a2846ad2002a4e585cd72f70189

**Date:** 2026-09-10 09:33
**Work item:** "Measure CI time impact of the disk-derived `-count=1` module loops; tune if it dominates the job" (23:47 f49; `.github/workflows/ci.yml`)
**Commit:** 674320f (docs/todo close-out), report commit follows with the same Task-Queue-ID footer.

---

## a) FULLY DONE

- **Measurement executed from real CI data, not local guesses.** Pulled per-step timings via `gh api …/actions/runs/<id>/jobs` for the latest green run (34406684629). Earlier green runs (34362629686, 34308077581) predate the module-loop step entirely — their "Module isolation gates" step array is empty — so they served as a natural before/after: the loop is a NEW cost, not a regression on something measured before.
- **Numbers:** `test` job — module-gates step 26s of a 271s job (~10%); `test-windows` — 39s of 173s (~23%). For context in the same run: root `Test` step 82s, Windows `Test` 89s, golangci-lint 84s, `Windows build (cross-compile gate)` 44s.
- **Verdict per the item's own condition:** the loop does NOT dominate either job, so no tuning was applied. Smallest-correct-change principle held: zero lines of workflow YAML touched.
- **Rationale recorded:** `actions/setup-go` v5 caches the shared GOCACHE by default, and `GOWORK=off` per-module builds reuse that same cache — the loop's incremental cost on a warm runner is mostly test execution, not compilation.
- **TODO_LIST loop closed:** line 71 checked off in the file's own convention (`[x] … — DONE 2026-09-10: <numbers + verdict>`); `scripts/check-todo-list.sh` gate passed; committed with the Task-Queue-ID footer.

## b) PARTIALLY DONE

- Nothing in this work item itself. The measurement is point-in-time: it reflects one green run on one commit. CI timings drift (runner load, Go version bumps, new modules adding a loop iteration), so the numbers are a snapshot, not a permanent property. The TODO item as written is fully discharged; only the *durability* of the measurement is partial.

## c) NOT STARTED

- **No tuning work** — deliberately, because the precondition ("if it dominates") is false. If a future run shows the Windows loop crossing ~50% of its job, tuning ideas exist (below in §e/§f) but none were implemented.
- **No status-report-driven TODO harvest from this item** — the item closed without spawning follow-ups; none were warranted by the data.

## d) TOTALLY FUCKED UP!

- Nothing. No broken builds, no reverted work, no production-journal accidents (the `TQ_DB` trap from AGENTS.md was avoided — no bare `tq` invocations were made at all). Two near-misses worth naming honestly:
  1. The first `gh run list` returned Dependabot runs, not CI runs — filtering by `workflowName=="CI"` was required; a less careful session might have "measured" the dependabot job.
  2. `grep count=1 TODO_LIST.md` matched the item only via the literal string `-count=1` in its text; the queue ID cross-reference in the prompt is what anchored the right line, not the grep.

## e) WHAT WE SHOULD IMPROVE

- **Self-review: what I forgot / could do better.**
  - I measured a single run. A more rigorous answer would average 3–5 green runs; the one-run numbers carry runner-noise I did not quantify. The conclusion is robust (10%/23% is nowhere near "dominates"), but the precision was false.
  - I did not break the 39s Windows module-gates step down per module (7 sub-module iterations × build+vet+test). If it ever matters, that per-module timing is the first thing to collect — the step has no `time` instrumentation, so it would need a local reproduction or `set -x`-style timing echoes in CI.
  - I did not compare the pre-loop green runs' total job durations (205s/251s for `test`) against the post-loop 271s rigorously — different commits have different work, so this would be suggestive at best. Noted, not done.
  - The measurement lives only in TODO_LIST.md and this report. If CI timing ever becomes a real concern, a small timing-echo in the workflow steps (`echo "::notice::step took Ns"`) would make every future measurement free.
- **Process observations (in passing):**
  - The four most recent runs on the default branch are all `failure` (34436225021, 34437536993, 34438944055, 34439012027, all ~04:17–05:00 on 2026-09-10). I did NOT investigate — out of scope — but a red master streak on a repo that dogfoods itself is exactly the signal the pool's review sweeper should have caught. Flagging, not fixing.
  - `update-go_modules-graph` (Dependabot) runs on every push and took 40s of the last window — harmless, just observed.

## f) Up to 50 things we should get done next

**Ordered by impact. Items 1–8 are directly adjacent to this task; 9+ are harvested from what I saw in TODO_LIST.md / ci.yml in passing (NOT verified this session — treat as leads, per the point-in-time rule).**

1. Investigate the 4 consecutive red runs on master from ~04:17–05:00 2026-09-10 (`gh run view 34439012027 --log-failed`) — a red streak on a self-dogfooded repo is pool food gone wrong or a real regression.
2. Add step-level timing echoes (`::notice`) to ci.yml's module loops so the next "measure CI time" question costs one `gh api` call instead of an investigation.
3. Average the module-loop measurement over the last 5 green runs to replace the one-run snapshot with a defensible number.
4. Per-module breakdown of the 39s Windows module-gates step — only if item 2 shows it growing.
5. Consider `GOFLAGS=-count=1` removal candidates: audit which CI steps genuinely need `-count=1` versus which pay test-cache invalidation for nothing (the root `Test` step and module loop both force it; a cached-green path could skip re-running unchanged module tests).
6. If the Windows loop ever crosses ~50% of its job: split the loop into a job matrix (one job per module) for parallelism — GitHub runners are free in minutes, serial loops are not.
7. Document the "earlier green runs predate the step" trick (empty steps array = step didn't exist yet) in AGENTS.md's CI notes — it's a free before/after control for any future step-cost question.
8. Decide a CI wall-clock budget (e.g. "test job ≤ 5 min") so future "does X dominate" questions have a threshold to measure against instead of eyeballing.

**From TODO_LIST.md in passing (unchecked as of 09:33, unverified this session):**

9. Dogfood hygiene: stale queued tasks in the live pool may verify with the OLD single-module default command — drain or cancel (`tq tasks`, `tq cancel`) (23:47 f46).
10. cwd-dependence sweep: audit every repo-name consuming command (`tq audit`, prune, harvest) for bare-name resolution (02:00 f8).
11. module-eval hardening: assert the tq-agent-pool unit's Environment carries non-empty PATH (02:00 f11).
12. `checkProjectsDir` should not require the projects dir when every `--repos` entry is absolute (02:00 f16).
13. harvest skip-log change detection: log a skip class only when its example changes instead of every 5m tick (02:00 f17).
14. `tq doctor`: warn when crush/git/go are missing from PATH (02:00 f18) — directly relevant to the pool-deploy failure mode in AGENTS.md.
15. dead-pool detection: PapDashboard alert when a pool's scan-failed skip streak covers every repo for N ticks (02:00 f6).
16. dogfood smoke: `scripts/smoke/dogfood-once.sh`, env-gated `TQ_DOGFOOD=1` (02:00 f10).
17. Archive the 2026-09-10 dogfood proof journal from /tmp before a reboot loses it (02:00 f9) — time-sensitive, /tmp is ephemeral.
18. `tq pool-health`: per-repo skip streaks + last harvest activity (02:00 f27).
19. worktree-per-agent design doc: claim → worktree → verify → merge (02:00 f15).

**Improvement leads from this session's observations (new, small):**

20. The `test` job runs `Vet` and `Build` as separate steps (11s + 1s) — `go test` compiles anyway; folding Build into Test could shave a step without losing signal (vet stays, it's a different checker).
21. `Windows build (cross-compile gate)` (44s) is the second-largest Linux-job step and rebuilds everything with GOOS=windows that test-windows then rebuilds again on a real runner — consider trimming the Linux cross-compile to `go vet`-only or merging it into test-windows' own build.
22. golangci-lint (84s) is the single largest Linux-job step and advisory-only — caching `~/.cache/golangci-lint` via actions/cache keyed on GOGCFLAGS/config hash would likely halve it.
23. `gh run list --workflow ci.yml` returned nothing while `--workflow "CI"` worked — the workflow is referenced by display name in at least one place; pin an id/name convention for scriptability.
24. The recent-successes query had to filter by conclusion — consider a branch-protection-required-checks sanity pass so the pool's green-traffic assumption is explicit.

25–50: I am deliberately NOT padding this list. The skill guidance is explicit that extra items are brainstorm/ROADMAP fuel, and the repo's own convention (TODO_LIST items must be bounded and agent-executable) argues against inventing 25 more leads from a single-measurement session. Items 20–24 above are the honest residue; the TODO_LIST §"Dogfood round" (lines 74–85) already carries a vetted backlog for the rest.

## g) Questions I can NOT figure out myself

1. **Why are the four most recent master runs red (~04:17–05:00 today)?** I was told not to research unrelated work; if that streak is already known/triaged by another agent, say so and I'll drop it. If not, it should be the very next task (item f1).
2. **Is there an agreed wall-clock budget for CI jobs?** Without a threshold, "dominates" stays an eyeball judgment; if the owner has a number in mind (e.g. ≤5 min for `test`), the measurement style for future timing items changes from "is it the biggest step?" to "what fraction of budget?"
3. **Should the module loops' `-count=1` be considered sacred?** If test-cache reuse between CI runs on unchanged modules is acceptable, the Windows loop (39s, the biggest single module-loop cost) could drop substantially; if `-count=1` is a deliberate freshness guarantee for all modules in every run, that tradeoff should be written down next to the loop so nobody re-litigates it (mirroring the docs-formatting precedent).

---

*Point-in-time snapshot: measurements from green run 34406684629 (2026-09-10 ~03:41 UTC); repo state at commit 674320f. Everything in §f items 9–19 is unverified by this session and inherited from TODO_LIST.md.*
