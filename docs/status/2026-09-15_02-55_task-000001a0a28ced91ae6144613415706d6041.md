# Status — task 000001a0a28ced91: wire check-webui-css.sh into ci.yml

**When:** 2026-09-15 02:55 CEST · **Agent:** GLM-5.3-Flash pool dispatch (Crush)
**Scope:** TODO_LIST row 78 ("Docs-health harvest 2026-09-11 evening" section) only — no drive-bys.
**Commits:** `b8fcdb5` (daemon-folded: ci.yml step + canonical app.css) + `ece58e2` (footer commit: TODO row closed). Not pushed.

---

## a) FULLY DONE

1. **ci.yml wiring (the work item).** `Web UI CSS canonical check` step added to the `nix` job, after `Nix flake check` — the only CI job with nix on PATH, which `check-webui-css.sh` requires (it shells `nix run .#webui-css`). Runs as a HARD gate (no `continue-on-error`). YAML validated post-edit.
2. **REAL drift caught and fixed.** Running the gate locally before declaring green exposed that HEAD's committed `internal/webui/static/app.css` was UNMINIFIED (6,361 lines) — a bad regen rode daemon commit `5512bd9`/earlier "heuristic" commits and every local ci-local css step since must have been red or bypassed (master CI has no such step until now — exactly the hole this task closes). Regenerated byte-canonical via `nix run .#webui-css` (minified, 2 lines); gate re-run: GREEN twice, deterministic.
3. **TODO_LIST row 78 closed** with DONE note + evidence (gate verified green ×2, drift origin).
4. **Guard-wiring gate still green:** `check-guard-wiring.sh` passes (the new ci.yml reference satisfies its substring check).
5. **Build verified:** `GOEXPERIMENT=jsonv2 go build ./...` green; working tree clean; app.css is go:embed'ed so the rebuilt binary embeds the canonical file.

## b) PARTIALLY DONE

1. **Nothing in the work item itself.** One caveat: the footer commit `ece58e2` touches only TODO_LIST.md — the actual ci.yml + app.css changes were swept by the auto-commit daemon into footerless `b8fcdb5`. The queue↔git cross-reference therefore points at a docs-only commit; the code diff carries no footer. Sanctioned by policy (no history rewrites; amend maneuver applies to unshipped daemon commits but I chose not to fold `ece58e2` down via rebase under a live daemon).
2. **Local verification is nix-green but runner-unproven.** The new step runs on GitHub runners for the first time on next push; runner-only nix failure modes exist in this repo's history (2026-09-10 vendorHash FOD class). Risk is low (same action, same runner config as `Nix build`) but unverified until the first push.

## c) NOT STARTED

1. Push (never push — owner-run).
2. `ci-local.sh` step ordering note: `check-webui-css.sh` remains in ci-local; no change needed there (verified it already runs it at line ~150).
3. Nothing else — the item was atomic.

## d) TOTALLY FUCKED UP

1. **I raced the daemon and lost, silently at first.** I ran `git status --short && git add ... && git commit` in ONE chain; the daemon had already committed ci.yml + app.css 80 seconds earlier (`b8fcdb5`), so my footer commit captured only TODO_LIST.md. The session-start ritual explicitly warns about this; the rule is "re-check `git status --short` immediately before `git add`" AND `git log` for fresh daemon commits. I checked status but not the log inside the chain window. Cost: the code diff is footerless (see b1). Not data loss, but the known failure mode, replayed.
2. **Pre-existing, found in passing (not mine):** the unminified app.css sat on master unnoticed — the pre-commit nix byte-equal hook evidently didn't stop the daemon regen, confirming "the daemon still bypasses hooks; ci-local remains the catcher", and master CI had NO catcher until this task. The drift window length (commits `4c2fde9`, `3176712`, `679ce93`, `99e8658` all touched app.css) is unmeasured.
3. **Minor:** first gate run emitted a ~6k-line diff with `diff: standard output: Resource temporarily unavailable` and I briefly misread the diff direction (before/after) — wasted one read cycle before `wc -l`/`git show HEAD:` settled it.

## e) WHAT WE SHOULD IMPROVE

1. **Byte-canonical pins belong in CI, not just ci-local** — this task is the proof: the pin existed locally for days while unminified css shipped. Same audit should run for every other local-only gate.
2. **Daemon + staging race**: chain status→add→commit is not atomic enough; check `git log -1` right before `git add`.
3. **Pre-commit app.css hook appears ineffective against daemon regens** — worth a root-cause look (was the hook bypassed, or did the rebuild output differ?).
4. **`check-webui-css.sh` error output** should diff with `--brief` + write the full diff to a temp file path instead of stdout (the ETXTBSY-adjacent "Resource temporarily unavailable" on huge diffs makes failures noisy).

## f) Next (up to 50; 1–8 direct tail, 9+ pointers/brainstorm)

1. **[P1] Push so the new CI step's first runner run executes** — the gate is only real once runner-green (runner-only nix failure class exists).
2. **[P1] Root-cause the unminified app.css regen**: which commit introduced it, and did the pre-commit hook run and pass (bad) or get bypassed (expected)? If a bad regen can pass the byte-equal hook, the hook is lying.
3. **[P1] Mint-time done-check for re-dispatched work items** (recurring §d across today's reports — 5 no-op dispatches on the sibling task ID): enqueue-side `grep TODO_LIST` for `[x]` + "already shipped" evidence before minting a paid agent task.
4. **[P2] Audit every remaining ci-local-only guard for the same CI-hole** (lint-baseline --check, go-mods, dead-exports, doc-refs…): which others would let drift ship to master?
5. **[P2] Footer-attribution for daemon-folded code diffs** (b1): consider a `tq` convention where the follow-up docs commit cites the daemon SHA, or teach the daemon to read an in-repo footer hint file.
6. **[P2] `check-webui-css.sh` failure ergonomics**: diff to file, print path + first hunk only.
7. **[P2] Fix `diff` stderr noise under big diffs** (ETXTBSY-adjacent oddity observed).
8. **[P2] Add a negative fixture for the new CI step** (deliberately drifted css fails it) — the release-gates pattern; currently only the positive path is exercised.
9. TODO row 79: review-pipeline anchoring hardening (sha + quoted anchor, re-anchoring pre-flight, dual-footer convention).
10. TODO row 80: `golangci-lint config verify` in ci-local before the run.
11. Sweep remaining 2026-09-11-evening harvest rows for other unwired-guard lessons.
12. Index scannability: README.md warns past-100 unarchived rows — today's report cadence (30+ files on 09-15 alone) is sprinting toward it; run a docs-health archive pass.
13. Today's ~10 repeat-dispatch report triples (00-12/16/24, 00-27/30/33, …) are daemon-mint noise — harvest §d lessons into one mint-gate design note rather than per-report restatements.
14. Consider `git log --grep Task-Queue-ID` coverage check: footerless daemon commits holding real code (b8fcdb5) weaken queue analytics; quantify how many exist.
15. Runner-nix differential playbook (AGENTS 2026-09-10 vendorHash note) — document the escape hatch once the new step's first run lands.
16. `check-guard-wiring.sh` substring match means a mere comment mention counts as "wired" — tighten to step-name or run-line matching eventually.
17. CHANGELOG entry for the css pin (append-only policy; not done this session — it's a CI change, arguably out of surface; owner call).
18. FEATURES.md row for the byte-canonical pin (CI-freshness gate will want a green run id — after push).
19. Budget note: this task spent one nix rebuild (~2min) + one verify rebuild; fine, but the 5-dispatch sibling pattern is where the money leaks.
20.–50. Standing backlogs own the rest: go-cqrs-lite `queue/` parity re-open ADR, session-close-bridge trigger automation + budget bypass, crush client/server per-repo experiment, closeout 429 closeoutPending cross-restart persistence, postgres parity for session bridge, agentPath/GOEXPERIMENT NixOS module env fix, hpks of today's mint-gate ideas into docs/planning.

## g) Questions I cannot answer myself

1. **Was the unminified app.css regen produced by a pool agent running `build-webui-css.sh` OUTSIDE nix** (tailwind devShell missing → no minify)? I can't reconstruct which session produced commits 99e8658→5512bd9; git authorship is all daemon. If yes, the fix is env-gating that script, not just the pin.
2. **Should the amend maneuver have folded `ece58e2` into `b8fcdb5`** (unpushed daemon commit, footer restored, single clean commit)? Policy reads both ways; owner ruling wanted.
3. **Is a `chore:` daemon commit carrying a CI-workflow change a CHANGELOG/FEATURES surface** in this repo's convention, or are CI-only changes exempt from both?
