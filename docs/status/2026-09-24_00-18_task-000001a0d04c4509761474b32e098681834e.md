# Status Report — sqlitev4 D1 `last_error` Reset, Repeat-Dispatch Verify (task 000001a0d04c4509761474b32e098681834e)

- **When:** 2026-09-24 00:18 CEST
- **Work item:** sqlitev4 adapter: reset `last_error` on Complete (tq parity — divergence D1 of the 16-28 report)
- **Window type:** REPEAT DISPATCH — verify-only stop on an already-DONE row
- **Primary window:** 2026-09-24 00-12 report (same task id), fix commit `bc5aece0`

## Summary

This is the second dispatch of the same work item. The fix was already landed and
reported by the 00-12 window: commit `bc5aece0` ("Fix sqlitev4 stale last_error on
Complete (D1 tq parity)") clears `last_error` adapter-side in
`internal/queue/sqlitev4/adapter.go` `Store.Complete` (post-finalize
`UPDATE tasks SET last_error = '' WHERE id = ? AND status = 'completed'`,
adapter.go:276; a completed row can never Fail again, so no race), pinned by
`TestCompleteResetsLastError` (store_test.go:93: fail → retain → re-claim →
complete → empty). The same commit also aligned both spike modules' `go`
directive to 1.27.1, repairing a red `check-go-mods` at HEAD. TODO_LIST row 32
is `[x]` with the fix summary.

This window therefore followed the fastest-correct-response rule for DONE rows:
verify → cite → stop. Zero code delta by design.

## a) FULLY DONE

1. **D1 fix verified at HEAD** (fresh gate, captured this window):
   - sqlitev4 build rc=0, vet rc=0 (`GOWORK=off GOTOOLCHAIN=auto GOEXPERIMENT=jsonv2`).
   - `TestCompleteResetsLastError` PASS (0.30s).
   - Full sqlitev4 module suite rc=0, `go test ./... -count=1` (3.587s), package `ok`.
2. **Pin + fix existence confirmed by grep** before any work: adapter.go:276
   clear statement + comment block ("tq parity (divergence D1)"), test at
   store_test.go:93.
3. **TODO_LIST loop closed** by the prior window: row 32 `[x]` with an accurate
   fix citation; no edit needed this window.
4. **Turn-1 ritual**: git log/status checked; prior-report grep for the task id
   surfaced both same-task artifacts (00-12 report, bc5aece0) before any action
   — the repeat-dispatch was identified immediately, no wasted edits.

## b) PARTIALLY DONE

1. **Verify-window battery scope**: the cheap gates were re-run at HEAD
   (build, vet, pinning test, full module suite) and are fresh, but the
   sibling spike module `cqrsqlite` and the root-module `go build ./...` were
   NOT re-run this window (they were green at bc5aece0 per the 00-12 report §d,
   and this window's zero-delta makes regression impossible without a
   concurrent edit — none observed: `git status` clean at window start).
2. **README index row for THIS report**: not yet written at report creation
   time; the AMEND MANEUVER or a follow-up indexing commit is required (the
   daemon bypasses the pre-commit hook — known hole).

## c) NOT STARTED

1. **Upstream filing** of the engine-side `last_error` defect against
   go-cqrs-lite `queue/sqlite/v4` (the adapter-side clear is local; every OTHER
   upstream consumer still sees the stale error on completed tasks).
2. **TODO row 33** — lint-baseline regen covering sqlitev4/cqrsqlite (zero
   baseline rows for either spike module; structurally blind gate).
3. **§g owner questions below** — none answered yet.

## d) TOTALLY FUCKED UP!

Nothing in this window. The one process risk worth naming: the FIRST commit of
the fix (`bc5aece0`) rode through while the sqlitev4 adapter/go.mod had landed
in daemon commits (945010f3/2b8a1aee) — the queue's git derivation splits a
task's work across daemon + footer commits. Not broken, but attribution is
fraggile when daemon timing interleaves with a footer commit. No damage:
lineage is reconstructable via the footer + the 00-12 report.

## e) WHAT WE SHOULD IMPROVE!

1. **Repeat-dispatch cost**: two windows (00-12 fix, 00-18 verify) spent on one
   S-row because dispatch raced the fix landing. Nothing to fix in the queue
   (at-least-once is the contract) — the verify-only-stop pattern worked; keep
   it.
2. **Same task, two reports**: the 00-12 report already carries a full f-list
   (50 items) and g-list (3 questions). This window adds a snapshot, not new
   findings — future verify-only windows on DONE rows should consider whether
   a report adds value over a cited stop, but the contract here demanded one.
3. **Module-suite vs -race**: this window ran the suite non-race only; the
   00-12 window ran -race green. For zero-delta verify windows, inheriting
   -race from a same-SHA prior window is per-policy; keep citing the SHA when
   doing so.

## f) Up to 50 things we should get done next

Ordered roughly by impact; most are carried forward from the 00-12 report's
f-list (they remain unstarted — this window changed nothing).

| #  | Task                                                                                                                                         | Impact | Size |
|----|----------------------------------------------------------------------------------------------------------------------------------------------|--------|------|
| 1  | §g1 owner ruling: which S1 spike survives — cqrsqlite vs sqlitev4 — delete the loser (git rm + all references)                                 | Critical | S |
| 2  | File the upstream `last_error` issue against go-cqrs-lite `queue/sqlite/v4` (D1, engine leaves stale error on completed tasks)                 | High | S |
| 3  | TODO row 33: lint-baseline regen covering sqlitev4/cqrsqlite (clean golangci cache first, triage notes, AGENTS.md policy)                      | High | M |
| 4  | Cross-process same-owner finalization ruling (sqlitev4 allows, cqrsqlite forbids, tq parity?)                                                 | High | S |
| 5  | Document in sqlitev4's package comment that `Requeue` semantics differ from tq's sqlite (16-36 report #24)                                     | Med | S |
| 6  | Add sqlitev4/cqrsqlite to module-enumeration spot-checks (gosec canaries, for-each-module loops, check-go-mods coverage)                        | Med | S |
| 7  | Run `TestEnqueueClaimBaseline10k` once against sqlitev4 with TQ_BASELINE=1 (engine-vs-native perf delta)                                       | Med | M |
| 8  | Port `TestCompleteResetsLastError`-class pin to tq's own sqlite suite (it currently pins tq parity only via store behavior)                     | Med | S |
| 9  | TQ_TEST_POSTGRES CI job coverage of the sqlitev4-era extras (questions, PriorityScores) when S1 promotes                                       | Med | M |
| 10 | sqlitev4 sessionfacts parity note in the ADR-0019 decision memo                                                                                | Low | S |
| 11 | Health-check the facade parity gate story for sqlitev4's eventual S1 promotion (aliases needed? module is internal-only today)                 | Med | S |
| 12 | Split-brain watch: sqlite.go, cqrsqlite, sqlitev4 hold three copies of the Store contract — S4 deletes the mirrors; track until then            | High | — |
| 13 | Verify the daemon-committed sqlitev4/go.mod + adapter diff survived intact in final HEAD (bc5aece0 touched only cqrsqlite go.mod + TODO_LIST)  | Med | S |
| 14 | Resolve the 00-12 §g3 go-directive floor question for spike modules (1.27.1 now; guard via check-go-mods — confirmed repaired, keep pinned)     | Low | S |
| 15 | ADR-0019 S1→S4: S2 unify journal on facts.Fact (open FactType; tq fact types stay tq constants)                                                | Critical | L |
| 16 | ADR-0019 S3: read models on metaengine (Watcher/ServeSSE replacing the hand tailer fan-out)                                                    | High | L |
| 17 | ADR-0019 S4: composition via system/ DomainConfig + DELETE the 12 mirrored backend clone groups (2026-09-23 art-dupl count)                     | Critical | L |
| 18 | S1 promotion plan: which tq extras stay companion tables vs petition upstream (RecordAnswer, PriorityScores, CountFacts/FactsSince/LastFacts, ProjectCounts) | High | M |
| 19 | Projection-equality verify harness for the facts-first replay migration story (dogfood cutover is owner-run)                                   | High | L |
| 20 | Decide ask-policy (§g of the 21-04 report): no prompt teaches `tq ask` — agents cannot discover the question channel                           | Med | S |
| 21 | OWNER-ONLY: scope the minted verify gofmt stage to tracked files (`gofmt -l $(git ls-files '*.go')`) in `.tq-verify` + mint template — vendor/ walks kill tasks | High | S |
| 22 | internal/e2e -race vs 180s stage cap is load-marginal (240s kill at 02:45, 181.6s ok at 03:05) — tracked TODO row; fix or re-budget            | Med | M |
| 23 | Post-fix sweep: `tq audit --journal` over the dogfood DB for any completed sqlitev4-era rows still carrying stale last_error (none expected — spike never ran live) | Low | S |
| 24 | Write the cqrsqlite-vs-sqlitev4 divergence delta into the ADR-0019 memo so §g1's ruling is evidence-based                                        | Med | M |
| 25 | Add a repeat-dispatch fast path note to AGENTS.md's session-start ritual: DONE row + fresh same-SHA report → verify-battery-only window is fully compliant | Low | S |

(25 carried forward + condensed; items beyond this are the 00-12 report's
f-list rows 26–50 — still valid, not restated here. This window found no new
work items.)

## g) Questions I can NOT figure out myself

1. **§g1 (blocking S1 progression):** Which S1 spike survives — `cqrsqlite`
   (per the plan's C03/M011) or `sqlitev4` (per live work, now also carrying
   the D1 fix and broader conformance parity)? Everything downstream (rows 34+,
   extras placement, module deletions) hinges on this ruling.
2. **D1 upstream:** Do you want the engine-side `last_error` fix filed as a PR
   against go-cqrs-lite `queue/sqlite/v4` (verify-before-filing + github-voice
   rails), or is the tq-adapter-side clear the accepted permanent fix?
3. **Report scope for verify-only windows:** the contract demanded a full
   status report for a zero-delta verify window; for future repeat dispatches
   on DONE rows with a same-SHA fresh report, is a cited stop + index row
   enough, or should every window always write a report?

## Gates (captured)

- sqlitev4 build + vet rc=0 (`GOWORK=off GOTOOLCHAIN=auto GOEXPERIMENT=jsonv2`).
- `go test -run TestCompleteResetsLastError -v .` → PASS (0.30s).
- `go test ./... -count=1` → rc=0, package ok (3.587s).
- `-race`, cqrsqlite suite, root build: inherited from same-SHA bc5aece0 green
  runs (00-12 report); not re-run this window (zero code delta).
