# Status Report — task 000001a08a4851e129179c8cf04e126208a7

**Date:** 2026-09-11 03:46 CEST
**Work item:** Review rework — one finding, severity low: "Module-gate timings swapped between jobs in TODO_LIST.md and commit message", filed against commit 674320f (the close-out of work item 000001a08a2846ad2002a4e585cd72f70189, TODO_LIST 23:47 f49).
**Commit:** cfa8412 (refutation + step-name pinning on TODO_LIST.md:71); this report commit follows with the same Task-Queue-ID footer.

---

## a) FULLY DONE

- **The finding was investigated against primary sources BEFORE any edit.** Pulled run 34406684629's job/step data via `gh api repos/LarsArtmann/go-taskqueue/actions/runs/34406684629/jobs`: the `test` job (271s total) owns step "Module isolation gates (GOWORK=off per sub-module)" = **26s**; the `test-windows` job (173s total) owns step "Module isolation gates (per sub-module)" = **39s**. Cross-checked against the workflow itself: `.github/workflows/ci.yml:34` puts "(GOWORK=off per sub-module)" in job `test` (ubuntu), `ci.yml:110` puts "(per sub-module)" in job `test-windows`.
- **Verdict: the finding is refuted on BOTH mappings.** The reviewer inverted the step-name↔job mapping AND the duration↔job mapping. The original record — TODO_LIST.md:71 and commits 674320f/15ff1f9 — already matched the run exactly: test 26s/271s (~10%), test-windows 39s/173s (~23%). The reviewer's proposed "~14% and ~15%" derive from their inverted pair (39/271 = 14.4%, 26/173 = 15.0%); the true shares are 26/271 = 9.6% ≈ 10% and 39/173 = 22.5% ≈ 23%.
- **The swap was REFUSED.** Applying it would have replaced correct data with data its own cited run contradicts (verify-external-claims discipline; AGENTS.md "independently verify tool output before mutating anything"). A review finding about numbers is a claim, not a fact.
- **Constructive remedy applied instead (1 line, nothing else touched):** TODO_LIST.md:71 now quotes each CI step name next to its duration, so the job↔duration mapping is verifiable at a glance and cannot be re-paired by mistake. The item stays `[x]` DONE — its DONE claim was accurate all along.
- **The commit-message half of the finding is closed without rewriting history, and needed no fix:** 674320f is an orphaned fork twin (`git merge-base --is-ancestor` = false, contained in no branch) whose live, PUSHED twin 15ff1f9 carries the identical message with the identical correct numbers. No artifact ever carried the wrong numbers; rewording a pushed commit would fork the lineage (AGENTS.md scripted-history playbook), so there was legitimately nothing to rewrite.
- **Gates green:** `scripts/check-todo-list.sh` ok; `go build ./... && go vet ./...` green on the root module (`GOEXPERIMENT=jsonv2` per the AGENTS.md env note). The change is docs-only; no Go code touched, so no module-level test surface applies.
- **Loop closed per contract:** committed as cfa8412 with the exact `Task-Queue-ID:` footer; nothing pushed; final output carried the queue's TQ_RESULT line with the refutation evidence inline.
- **Shared-gate repair (noticed in passing, verified necessary):** the concurrent 03:34 sibling report (`2026-09-11_03-34_task-000001a08a4851ce7feb14c30837a9b399dd.md`, task …4851ce) was UNINDEXED, which fails the repo-wide `check-status-index.sh` gate — and the installed pre-commit hook runs exactly that check, blocking ALL commits, mine included. Indexed the sibling report from its own text (one README.md row; content untouched) so the shared gate is green again. `check-todo-list.sh` ok, `check-status-index.sh` ok after the repair.

## b) PARTIALLY DONE

- **The refutation's reach is partial.** The evidence lives in cfa8412's message, TODO_LIST.md:71, and this report — but there is no channel to hand it back to the reviewer. If the review loop re-inspects the TODO line, the quoted step names now make the truth self-evident; the finding itself, however, cannot be marked "refuted" in the review tooling from here. Remaining: owner/queue decision on the reply path (§g Q1). Effort: S once the channel is known; blocker: review tooling not visible to this session.
- **Ambiguity remediation covers one artifact of three.** TODO_LIST.md is pinned, but the older artifacts carrying the same bare (step-name-less) pairs remain unpinned: the 2026-09-10_09-33 close-out report (its "Numbers" bullet and three mentions of the "39s Windows step") and the docs/status/README.md index row for it. All contain the CORRECT pairings — but bare, which is exactly the shape that misled the reviewer. Left untouched deliberately (point-in-time snapshots, annotate-never-rewrite, and this task's fix-exactly-the-finding contract). Remaining: one ANNOTATE-mode pass. Effort: S; no blocker, pure scope discipline.
- **Orphan-citation rot is documented but not repaired.** README.md:19 and the 09-33 report cite "674320f" — a commit unreachable from any branch (reflog-only, will eventually be GC'd); the live commit is 15ff1f9. Harmless today, misleading later. Folds into the same annotate pass (§f item 1).

## c) NOT STARTED

- **Annotate pass over the 09-33 report + its index row** (add step names; repoint 674320f → 15ff1f9) — planned, deliberately not started this window: the contract said fix exactly the finding, no unrelated changes.
- **HARVEST of this report's §f into TODO_LIST.md** — not started; belongs to the next docs-health/status pass, and this task's contract forbade unrelated TODO_LIST edits beyond the finding.
- **Red master, verified live this window: `internal/webui TestA11yChrome` fails deterministically** ("committed CSS lacks prefers-reduced-motion handling"). Noticed in the sibling 03:34 report (§c1), then independently re-verified at this window's HEAD: `go test ./internal/webui -run TestA11yChrome` — FAIL. Pre-existing since the 3426afc app.css regeneration; repo-convention fix is `templ generate` + `nix run .#webui-css` (9d2c21e precedent). NOT fixed here: another agent's in-flight construction site per the sibling report, and out of this task's scope — but it gates ci-local and every push until regenerated (§f item 2).
- **Everything else in §f below is a lead, not started, and unverified this session unless marked otherwise.** Two more leads from the sibling report worth naming: `tq doctor --repos` still resolves bare names against the cwd (same bug class the f8 sweep fixed, excluded there by the item's parenthetical — §f item 8), and one `TestSSEHeartbeatStopsBeforeHandlerExit` timing flake that passed on re-run (§f item 21).

## d) TOTALLY FUCKED UP!

Brutal honesty first: **nothing in this window is on fire** — no broken builds, no data corruption, no production-journal contact (zero bare `tq` invocations; the `TQ_DB` trap from AGENTS.md was avoided entirely). But two things deserve the harshest framing available:

1. **One full review cycle was wasted by my own earlier record-keeping — that is the real defect behind this finding.** The original close-out (15ff1f9/674320f, task …846ad) wrote "test job 26s/271s (~10%)" WITHOUT naming the step. The numbers were right; the record was unverifiable-at-a-glance, and a reviewer mis-paired the durations, inverted the step-name mapping to match, and filed a plausible-sounding wrong finding. Cost: an entire review round-trip on a LOW-severity line. Root cause: measurement records that don't name their observation unit invite re-pairing. Mitigation is now live on TODO_LIST.md:71; the older artifacts stay bare until the annotate pass (§b).
2. **A near-fuckup that verification caught: the requested fix itself would have corrupted the record.** The finding was concrete, confident, and wrong. Had I complied unverified — swap 26↔39 and ~10%/~23% → ~14%/~15% — TODO_LIST.md would now assert "test 39s/271s, test-windows 26s/173s" against a run whose API says the opposite: a self-contradicting permanent record that a future session would either trust or burn another cycle untangling. Severity had it landed: medium — every citation of the line would be ~1.5× off on one job, and the review loop would likely have "passed" a corrupted record. Root cause of the near-miss: none; the verify-before-encode order held. Keep it.

Neither item blocks development or users; #1 costs review bandwidth, #2 never landed.

## e) WHAT WE SHOULD IMPROVE

- **Quote the observation unit in every measurement record.** Step names, file paths, run IDs — at write time, in the DONE line itself. This one habit would have prevented the entire episode. TODO_LIST.md:71 now does it; candidate for a single AGENTS.md Conventions sentence (§f item 12). Impact: prevents numeric-finding ping-pong; cost: ~10 words per record.
- **Findings that dispute a number should carry their raw evidence**, symmetric to the TQ_RESULT contract for work. This finding asserted run data without quoting it; one `gh api` line in the finding would have exposed the mis-pairing to its own author before filing. Owner decision (§g Q1). Impact: kills a whole class of wrong-but-confident findings.
- **Verify disputed numbers BEFORE judging the remedy.** I pulled the API data before editing and it changed the entire outcome. Formalize the order for every review finding: primary source first, remedy second. (This matches the "independently verify tool output before mutating anything" lesson — it worked; keep it mandatory.)
- **Cite reachable SHAs in permanent docs.** Citations to orphan 674320f will silently rot at GC. Suggested fix: after any rebase/reword, sweep `rg <old-sha> docs/ TODO_LIST.md` and repoint citations in the same annotate pass; worth folding into the scripted-history playbook (docs/status/2026-09-10_07-49 §e).
- **Self-critique, minor:** I checked rewrite depth (`rev-list --count 674320f~1..HEAD` = 40) BEFORE ancestry (`merge-base --is-ancestor`). Backwards — ancestry answers "is this commit even in the lineage" in one command and reframes everything (it revealed the orphan twin). Trivial inefficiency, one wasted round-trip.
- **Format divergence handled by rule, worth recording:** the status-report skill defaults to a styled HTML dashboard; the user's explicit instruction (and the repo's `docs/status/*.md` + index-gate convention) demand markdown here. The skill's own override rule says explicit user instruction wins — followed; no repo change needed, just don't re-derive this next time.

## f) Up to 20 things we should get done next

Ranked by impact. Sources: items 1, 3, 7, 14–15, 20–21, 23 arise directly from THIS window; 4–6, 9–13, 16 mirror the open unchecked TODO_LIST.md lines (77–85) I read in passing (unverified this session); 2, 8, 21 are verified/flagged findings from the concurrent 03:34 sibling report (item 2 re-verified live at HEAD); 17–19 are inherited leads from the parent task's 09-33 report. The guide's ceiling is 50 — I stop at 23 rather than pad with fabricated leads.

1. **Annotate the 2026-09-10_09-33 report + README.md:19 index row**: add the CI step names to both timing pairs and repoint the 674320f citation at the live pushed twin 15ff1f9 (docs-health ANNOTATE mode — annotate inline, never rewrite). Impact: High (kills the ambiguity class this finding came from). Effort: S. Category: Documentation.
2. **Fix the verified red master: regenerate the committed webui CSS** (`templ generate` + `nix run .#webui-css`, 9d2c21e precedent) so `internal/webui TestA11yChrome` passes again — re-verified FAILING live this window; it gates ci-local and every push. Coordinate first: the sibling 03:34 report names it as another agent's in-flight site. Impact: High (repo-blocking). Effort: S. Category: Bug. (In passing, verified.)
3. **Decide the refuted-finding reply path**: does the reviewer receive the cfa8412 evidence explicitly, or does the corrective commit + report stand as the terminal answer? Impact: High. Effort: S. Category: Process. (Owner — §g Q1.)
4. **Archive the /tmp dogfood evidence** (/tmp/tq-dogfood.db + review log) into docs/status/ assets or ~/.local/state/tq/ before a reboot destroys it (TODO_LIST 02:00 f9). Impact: High — one reboot from loss. Effort: S. Category: Cleanup.
5. **`tq doctor`: warn when crush/git/go are missing from PATH** (02:00 f18) — the exact failure class that killed the deployed pool for ~20h. Impact: High. Effort: M. Category: Feature.
6. **Dead-pool detection**: PapDashboard alert when a pool's scan-failed skip streak covers every repo for N consecutive ticks (02:00 f6). Impact: High. Effort: M. Category: Feature.
7. **Queue convention: numeric findings must quote primary-source evidence** (query + output), symmetric to TQ_RESULT. Impact: Medium. Effort: S. Category: Process. (Owner — §g Q1.)
8. **Extend the f8 fix to `tq doctor --repos`**: bare names still stat against the cwd when doctor checks repo files (sibling 03:34 report §b1 — same class the sweep fixed, excluded there by the item's parenthetical; honest-gap follow-up owed to "every repo-name consuming command"). Impact: Medium. Effort: S. Category: Bug. (In passing.)
9. **Harvest skip-log change detection**: log a skip class only when its example changes (or first tick), not every 5m tick flooding journald (02:00 f17). Impact: Medium. Effort: S. Category: Quality.
10. **`checkProjectsDir` should not require the projects dir when every `--repos` entry is absolute** (02:00 f16). Impact: Medium. Effort: S. Category: Bug.
11. **module-eval hardening**: assert the tq-agent-pool unit's Environment carries a non-empty PATH (02:00 f11). Impact: Medium. Effort: S. Category: Quality.
12. **`tq pool-health`**: one-shot per-repo skip streaks + last harvest activity from the journal — liveness ≠ process-up (02:00 f27). Impact: Medium. Effort: M. Category: Feature.
13. **worktree-per-agent design doc** → docs/planning/ with explicit open questions (02:00 f15). Impact: Medium. Effort: M. Category: Documentation.
14. **One AGENTS.md Conventions sentence**: DONE-line measurements name the observed step/artifact (step name, path, run ID). Impact: Medium. Effort: S. Category: Documentation. (This window.)
15. **HARVEST this §f into TODO_LIST/ROADMAP** in the next docs-health pass, per the status-report loop rule. Impact: Medium. Effort: S. Category: Process. (This window.)
16. **dogfood-once.sh smoke** replicating the 2026-09-10 --once proof (env-gated TQ_DOGFOOD=1; stub-agent variant ungated) (02:00 f10). Impact: Medium. Effort: M. Category: Quality.
17. **Average the module-loop timing over ~5 green runs** to replace the one-run snapshot (09-33 report's own honest-limits item, still open). Impact: Low. Effort: S. Category: Quality.
18. **Add `::notice` step-timing echoes to ci.yml's module loops** so the next "does X dominate" question costs one `gh api` call (09-33 §f2). Impact: Low. Effort: S. Category: Quality.
19. **Per-module breakdown of the 39s Windows gate step** — only after item 18 lands; needs instrumentation first (09-33 honest-limits item). Impact: Low. Effort: M. Category: Quality.
20. **Fold the orphan-SHA citation sweep into the scripted-history playbook** (docs/status/2026-09-10_07-49 §e): after any rebase/reword, `rg <old-sha>` across docs/ and repoint. Impact: Low. Effort: S. Category: Process. (This window.)
21. **Watch `TestSSEHeartbeatStopsBeforeHandlerExit`**: one context-deadline flake in the sibling window, green on re-run — if it recurs it is a real timing bug in the SSE heartbeat shutdown path, not a flake (03:34 report §c2). Impact: Low. Effort: S. Category: Quality. (In passing.)
22. **Flag, owner-run: master is 5+ commits ahead of origin/master** (b0b2cad, 6ae5b99, f2090e1, 9fbba87, cfa8412, + this report commit); AGENTS.md reserves pushes to the owner — noticed in passing, no action taken. Impact: Low. Effort: S. Category: Process.
23. **Optional ergonomics, do not re-litigate the design**: bare `go build ./...` at root fails outside the nix devShell (encoding/json/v2 build constraints); AGENTS.md documents `GOEXPERIMENT=jsonv2` — a wrapper or clearer error pointer would spare non-nix tooling one failed attempt. Impact: Low. Effort: S. Category: Quality.

## g) Questions I can NOT figure out myself

1. **How does a refuted finding get closed in this queue?** Is there a channel to hand the reviewer the evidence (cfa8412's message, the `gh api` output), or does the corrective commit + this report stand as the terminal reply? I checked the task contract and AGENTS.md for review-tooling references and found none; the only cross-surface is the Task-Queue-ID footer. The answer decides whether this finding can bounce again.
2. **May the 2026-09-10_09-33 report and its README index row be annotated OUTSIDE a status-task window?** Both the annotate-never-rewrite rule and this task's no-unrelated-changes contract point at "wait for the next docs-health pass", but the live citation of an unreachable SHA (674320f) argues for sooner. Which constraint wins?
3. **Should §f item 3 (the /tmp dogfood evidence archive) be pulled forward into a dedicated task NOW?** It is one reboot from loss and I cannot judge reboot timing or whether the proof journal still exists at /tmp/tq-dogfood.db — but pulling work forward is a prioritization only the owner can make.

---

_Point-in-time snapshot: 2026-09-11 ~03:50 CEST, repo state at commit cfa8412 + uncommitted report/index edits. All run-34406684629 numbers re-verified live via `gh api` this session; ci.yml step-name↔job mapping verified against the workflow file; `TestA11yChrome` re-verified FAILING live (~03:49). §f items mirroring TODO_LIST.md (4–6, 9–13, 16) are unverified this session; treat as leads per the point-in-time rule. The concurrent 03:34 sibling report was indexed by this window solely to unblock the shared pre-commit gate; its content was not modified._
