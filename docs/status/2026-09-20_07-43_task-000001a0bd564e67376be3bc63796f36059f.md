# Task 000001a0bd56 — verify-window minimum battery: Conventions promotion — close-out

Window: 2026-09-20 07:38–07:43 UTC. Verify-only re-dispatch window; second
confirming window on this work item (prior: docs/status/archived/2026-09-20_06-36_task-000001a0bd11a4456dc57816672bdd26322d.md
was the first live application and its §f3 said "promote it from row to
Conventions clause once a second window confirms it").

## a) FULLY DONE

1. **Clause promoted** — the verify-window minimum battery is now an
   AGENTS.md Conventions bullet (next to "Claims carry citations"),
   verbatim content of the TODO row with its evidence lineage (03-27 §e2
   set the bar; 03-39 regression; promotion 2026-09-20). This is the fresh
   delta of THIS window: the codified rule now outlives the TODO row.
2. **Fresh cheap gates at HEAD, exit codes captured directly** (per the
   PIPESTATUS hazard clause — redirected, `$?` taken, not piped):
   `go build ./...` rc=0, `go vet ./...` rc=0 (both under
   GOEXPERIMENT=jsonv2), `scripts/check-doc-refs.sh` rc=0,
   `scripts/check-todo-list.sh` rc=0.
3. **TODO row closed** — ticked `[x]` in TODO_LIST.md:150, TODO gate green
   after the tick.
4. **Work commit** — ea79d92 with the Task-Queue-ID footer, exactly one
   footer, commit-msg hook passed. Two files: AGENTS.md + TODO_LIST.md.
5. **Turn-1 ritual honored**: session-start.sh run, whole-repo log + status
   - stash checked (tree clean at start), no prior reports on this task ID
     (confirmed via rg over docs/status/), CONTRIBUTING.md read at turn 1.

## b) PARTIALLY DONE

Nothing. The item was a one-sentence codification plus gates; both halves
shipped in full.

## c) NOT STARTED

- A mechanical ENFORCEMENT of the battery is still absent — the clause is
  prose only. Nothing fails today when a verify window skips its cheap
  gates (it relies on the next close-out reviewer noticing, which is
  exactly how the 03-39 regression survived until review). See §f.
- The queue-side re-dispatch suppression (06-36 §f1) remains an idea only;
  this window was itself a re-dispatch of an already-closed item, proving
  the spend still recurs.

## d) TOTALLY FUCKED UP

Nothing. No red gates, no daemon interference, no mid-session concurrent
edits hit, no rework needed. Honest near-miss: my first TODO edit was
rejected (file not yet read); recovered with one View + one edit — no
damage, but the read-before-edit discipline should not need the tool's
error to remind me.

## e) WHAT WE SHOULD IMPROVE

1. Verify-only windows on ALREADY-PROMOTED rows are pure waste: this ID
   closed at 06-30 with a DONE commit, and this window re-paid for a row
   whose remaining work was mechanical promotion. Suppression (§f1) is
   worth more than any further convention prose.
2. The promotion criterion ("once a second window confirms it") had to be
   recovered from the prior report's §f — the TODO row itself didn't carry
   the promotion intent. Rows that encode a promotion path should say so.
3. The battery clause could name its OWN evidence shape (which gate lines
   a stop verdict must quote); currently it names the gates but not the
   citation format, leaving windows to improvise.

## f) Next tasks (honest, from THIS window's observations — no padding)

1. Queue-side re-dispatch suppression: when a task ID already carries a
   footer-bearing DONE commit + `[x]` row, skip or cheap-verify-only the
   re-dispatch (carried from 06-36 §f1; this window is its second proof).
2. Mechanical battery pin: extend the verify-window battery to an
   enforceable check (e.g. a close-out linter that fails a report whose
   stop verdict lacks fresh gate rc lines at HEAD sha) — prose-only
   enforcement is review-luck.
3. Session-forensics hook: `session-start.sh` could grep for re-dispatch
   evidence (prior DONE commit for the task id) and print a "VERIFY ONLY —
   likely re-dispatch, check git log --grep first" banner mechanically.
4. TODO_LIST hygiene: audit remaining rows that reference a
   "promotion/confirmation path" (like this row did) and make the intent
   explicit in-row so recovery doesn't depend on reading the prior report.
5. That is the full list. The underlying work item shipped one clause;
   inventing 45 more from a docs-only window would be fiction, and every
   prior report on sibling verify windows has ruled the same way (06-36 §f5).

## g) Questions I cannot figure out myself

1. Is re-dispatch of footer-closed IDs a known/journal-visible class (is
   there a fact or counter distinguishing "re-fired original id" from
   "dedup-gap minted duplicate"), and is suppression owner-approved as the
   fix either way?
2. Should the battery enforcement (§f2) live as a script gate in ci-local
   (cheap, but only sees committed reports) or as a closeout-prompt
   requirement (sees the verdict before it ships, but prompt-enforced)?
3. None further — no open design surface remains on the shipped item.

## Evidence

- Clause: AGENTS.md Conventions bullet "Verify-window minimum battery"
  (work commit ea79d92, footer-bearing).
- TODO row ticked: TODO_LIST.md:150, check-todo-list.sh rc=0.
- Gates at HEAD (pre-commit tree): build rc=0, vet rc=0
  (GOEXPERIMENT=jsonv2), check-doc-refs.sh rc=0 ("doc refs ok").
- Prior context: docs/status/archived/2026-09-20_06-36_task-000001a0bd11a4456dc57816672bdd26322d.md
  (§f3 promotion criterion, §f1 suppression).
- Reports on this task ID before this one: none (rg confirmed at turn 1).
