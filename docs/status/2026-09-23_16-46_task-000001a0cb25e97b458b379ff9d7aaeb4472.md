# Status Report — Task 000001a0cb25e97b458b379ff9d7aaeb4472 (ADR-0019 S1 sqlite spike): Repeat-Dispatch Verification Window

Date: 2026-09-23 16:46 CEST · Work item: TODO_LIST row 31 (section
"go-cqrs-lite platform adoption") · Contract: docs/adr/
0019-go-cqrs-lite-platform-adoption.md §S1.

**Window type: repeat dispatch on a DONE row.** The task ID was enqueued
00:04, worked end-to-end by the 16-28 window (divergence report, commit
3f2c1cf8) and closed-out at 16-36 (commit 06213e75); this dispatch
arrived ~16:41. Per the AGENTS.md repeat-dispatch rule the correct
response is verify → cite → stop; this report is that verification plus
the consolidated a–g ledger for the whole task (the two prior same-task
reports own the primary narrative: docs/status/
2026-09-23_16-28_adr0019-s1-sqlitev4-conformance-divergence-report.md
and docs/status/
2026-09-23_16-36_task-000001a0cb25e97b458b379ff9d7aaeb4472.md).

## a) FULLY DONE (each with its evidence)

1. **The work item itself — row 31, end-to-end, by the prior windows.**
   `internal/queue/sqlitev4` implements tq's `queue.Store` over
   `queue/sqlite/v4.0.0` with the tq extras as companion tables; tq's
   3,441-line sqlite conformance suite runs green against it; the
   D1–D6 divergence report exists and is indexed; TODO row 31 is `[x]`;
   footer commits 3f2c1cf8 + 06213e75 carry the task ID. Evidence:
   the two reports above; not re-derived here.
2. **Turn-1 ritual executed (first window in this task's lineage to do
   it).** `scripts/session-start.sh 000001a0cb25…` run on arrival;
   `rg -l 000001a0cb25 docs/status/` surfaced both prior reports BEFORE
   any action was taken — the repeat dispatch was identified as such at
   minute one, not after re-doing work.
3. **Fresh at-HEAD verification battery with captured counts** (HEAD
   06213e75 — the final work commit; log paths in the battery section):
   - sqlitev4 suite, non-race, `-v`: rc=0, **72 pass (incl. subtests) /
     3 skip** — independently reproduces the 16-28 report's headline
     counts at final HEAD.
   - sqlitev4 suite, `-race`: rc=0, **0 failures** (66 top-level pass /
     4 skip; `TestLoadSnapshotScaleAt100k` additionally skips under
     -race — scale-gated, not a divergence). **This closes close-out
     §b2**: the race gate has now run at final HEAD, not just at
     pre-comment code state.
   - **cqrsqlite suite: first cited run in repo history — rc=0 (3.76s).**
     Closes 16-36 §c3/§f7 ("no window has ever recorded a green for
     it"). Both spike suites are now cited green, so the surviving-module
     ruling (g1) is purely semantic, not risk-weighted by "untested
     module".
   - `check-doc-refs.sh` rc=0; root `go build ./...` + `go vet ./...`
     under GOEXPERIMENT=jsonv2/GOTOOLCHAIN=auto rc=0.
   - `.golangci-baseline.txt` grep: **0 rows** for sqlitev4/cqrsqlite —
     confirms close-out §c2's structural-red prediction at the grep
     level (the gate run itself remains undone, see b5).
4. **Count reconciliation across reports.** The 16-28 report says
   "72 pass / 3 skip"; my first (race) pass counted 66/4 top-level.
   Resolved by counting both runs the same way: 72 includes subtests
   (non-race); under -race the scale test additionally skips. No
   substantive discrepancy — but it took a deliberate second counting
   pass to know that (see d2).
5. **No code changed by this window** — zero-delta by design; the
   value delivered is fresh at-HEAD evidence (item a3), not edits.

## b) PARTIALLY DONE

1. **`last_error`-on-Complete parity (D1 residual)** — documented in
   the 16-28 report, fix scoped as TODO row 32; unfixed. Adapter-side
   post-clear means a second UPDATE outside the engine tx, or it is an
   upstream fix; that choice belongs to the decision memo.
2. **The cqrsqlite/sqlitev4 split brain** — unresolved and owner-gated
   (15-59 §g1). This window's cqrsqlite green changes the decision's
   shape (both candidates are now suite-proven; the ruling is about
   semantics and the extras surface), but nothing was deleted or
   merged.
3. **Parity is suite-pinned, not exhaustive** (carried verbatim from
   16-36 §b1): corners the suite does not reach (cross-process
   finalize, completed-fact detail byte-shapes) rest on code reading,
   labeled as such in the divergence report.
4. **The 16-28 control experiment was inherited, not re-run**: the
   divergence-skip control (same tests pass on tq's own backend) was
   executed by the 16-28 window, not this one. Sound inheritance —
   HEAD moved since only by docs-only commits (06213e75) — but stated
   here so the provenance is explicit.
5. **Lint-baseline structural red is grep-confirmed, not gate-run**:
   0 baseline rows for both spike modules (a3); the actual
   `scripts/lint-baseline.sh --check` clean-cache run is deliberately
   left to a policy-owned regen window (AGENTS.md: regen is
   deliberate-only).
6. **TODO_LIST loop**: nothing to close — row 31 was already `[x]`, and
   no genuinely new work item surfaced (everything this window found is
   either row 32-40, already in 16-36 §f, or process improvement in §e
   below). Zero backlog edits made.

## c) NOT STARTED (planned, untouched — with why)

1. **TODO rows 32–40, the serialized S1→S4 plan**: last_error fix (32),
   postgres spike (33), decision memo (34), replay tool (35), S1 flip
   (36), S2 journal unification (37), S3 metaengine read side (38),
   S4 engine deletion (39), dogfood cutover (40 — owner-run, BLOCKED
   by design). Why: ADR-0019 mandates serialize S1→S4, and S1's
   decision inputs (both spikes + memo) are incomplete.
2. **The 16-36 close-out's 30 §f items** — deliberately report-only
   (only the last_error row was promoted to TODO_LIST); they are
   HARVEST fuel, restated in §f here.
3. **Two upstream-issue candidates, unfiled** (enqueued-fact detail
   thinness; nil-payload NOT NULL behavior) — both require the
   verify-before-filing gate first; neither has it.
4. **Carried repo Reds observed in passing**: fuzz workflow red since
   2026-09-22 03:32; `ci-local.sh` never run end-to-end green;
   vendorHash fast gate + `nix build` over the two spike-module FOD
   inputs pending; art-dupl accept-list gate missing while a THIRD
   copy of the conformance suite now exists.
5. **Annotation of 16-36 §b2** (race-predates-HEAD caveat): now factually
   closed by this window's a3 run, but the close-out document itself is
   not annotated — that is docs-health ANNOTATE work, not rewrites.

## d) TOTALLY FUCKED UP

**This window's own:**

1. **CONTRIBUTING.md was not read at turn 1 — the FOURTH consecutive
   documented recurrence** (02-35 §f34, 15-59 §d2, 16-36 §d3, now
   this). The worst part: my turn-1 grep surfaced the 16-36 close-out
   CONFESSION of this exact miss, and I still didn't open CONTRIBUTING.md
   until 16:45 during report prep. Root cause: I treated
   `scripts/session-start.sh` as the complete ritual; it does not
   surface the CONTRIBUTING/stash steps. No damage this time (no
   unknown gate was violated) — but that is luck substituting for
   process, twice over.
2. **First verification pass captured zero counts.** I ran the sqlitev4
   suite, saw `ok … 6.450s`, and recorded "rc=0" — done. Only while
   drafting this report did I count properly (`-v` + `grep -c` over a
   captured file), which surfaced the 66/4-vs-72/3 wrinkle. It resolved
   benignly (subtests + race-gated scale skip), but I did not KNOW it
   was benign until the second pass — I had verified the claim's
   existence, not its content. Same disease both prior windows
   confessed (16-28 §d1, 16-36 §e1), at smaller scale, by an agent who
   had just read both confessions.
3. **No todo list for a ~10-step dispatch.** Sequencing lived in-head;
   it worked, and nothing was dropped — but "it worked" is the
   pre-condition of every future drop, not evidence of safety.

**Prior windows', carried as this task's consolidated ledger** (fully
confessed in 16-36 §d; one line each, not re-litigated): diagnosed a
42-failure suite run from `tail -5`; wrote rc claims into the divergence
report before running the cited gates (lucky-then-true); turn-1 ritual
skipped (that streak's first three datapoints); lost the daemon-fold
race twice so the queue's footer attribution holds only an 8-line
residue; two wasted edit-tool calls on stale reads.

**Structural, nobody's fault, worth naming:** a `[x]`-row task with an
indexed close-out minted a full verification window ~40 minutes after
that close-out. AGENTS.md calls repeat dispatches routine, and this one
did produce fresh value (race-at-HEAD, cqrsqlite green) — but only
because the battery was improvised. The 02-06/02-09 repeats on row 92
produced far less. Nothing guarantees the next repeat dispatch is
useful (see f36).

## e) WHAT WE SHOULD IMPROVE

1. **Ritual → mechanism.** Fold the CONTRIBUTING.md check and
   `git stash list` into `scripts/session-start.sh`'s printed output.
   A four-window streak proves AGENTS.md prose will never win under
   task pressure; the script that already runs at turn 1 is the only
   reliable carrier.
2. **Count-first with teeth.** Every suite/gate claim cites a
   `grep -c` number from a captured file, before any prose mentions it.
   This report's battery section was written only from grepped files —
   that is the bar, and it cost one extra command per gate.
3. **Codify the repeat-dispatch verify battery.** For a DONE-row
   re-dispatch the window's job is: fresh at-HEAD cheap gates + counts,
   explicitly closing any "evidence predates HEAD" caveats in the
   close-out, then stop. Two lines of convention; would have made the
   row-92 repeats (02-06/02-09) as useful as this window.
4. **Suite-copy expiry BEFORE the postgres spike.** Three copies of the
   3.4k-line conformance suite exist (sqlite, cqrsqlite, sqlitev4), all
   now green. Row 33 as-scoped adds a fourth. Land the art-dupl
   accept-list gate (15-59 §f4) or pull the shared conformance harness
   package forward from S4 — otherwise every fix to tq's suite must be
   hand-propagated N ways until S4.
5. **DIVERGENCE-skip ledger check.** A ~20-line script (or test) that
   extracts `t.Skip("DIVERGENCE (S1 spike): …")` markers from both
   spike suites and diffs them against the 16-28 report's skip table —
   mechanical report-vs-code drift detection.
6. **Frame g1 with the new fact.** Both suites green means the
   surviving-module ruling is purely about semantics (cross-process
   finalize behavior) + the extras surface + upstream alignment — the
   decision memo should say so explicitly instead of letting "untested
   module" risk silently weight the choice.

## f) Up to 50 things we should get done next (impact-ordered)

Source tags: `[row N]` = already backlogged in TODO_LIST (HARVEST must
not duplicate); `[co-fN]` = 16-36 close-out §f item N (report-only);
`[new]` = this window. Impact / Effort per the status-report guide.

1. Owner ruling g1: which spike survives + deletion rights for the
   loser (module + its 3.4k-line suite copy). **Critical / S** (ruling
   itself) — [co-f1]; blocks 2, 3, 6.
2. ADR-0019 S1 postgres spike over `queue/postgres/v4.0.0`, judged by
   the TQ_TEST_POSTGRES suite, D1–D6 matrix reused. **High / M** —
   [row 33]; inherit the codec nil-guard + CAST learnings [co-f30].
3. S1 decision memo: per tq-extra surface, upstream-grown vs companion
   table; fact-append path; replay sketch. **High / M** — [row 34].
4. Fix `last_error` on Complete + add the missing pin test to BOTH tq's
   and sqlitev4's suites (its absence is why D1's residual survived).
   **High / M** — [row 32][co-f4].
5. S1 replay tool: journal → fresh engine store, projection equality
   (decoded payloads — D5's TEXT→BLOB class). **High / M** — [row 35].
6. S1 flip: queue/v4-backed store becomes default; worker finalizes →
   claim tokens; facades/vendorHash follow. **High / L** — [row 36];
   blocked on 1–3.
7. Pin test or owner ruling for cross-process same-owner finalize (the
   one residual where the two spikes disagree). **High / S** — [co-f9].
8. Heartbeat ghost-read decision: worker writes `task.heartbeat` facts,
   or probes move to a live signal and the constant's consumers die.
   **Medium / S** — [co-f16, g3].
9. Verify-and-file upstream issue: `task.enqueued` detail carries
   `{project,type}` only (donor-parity regression; breaks journal-drift
   audit needs). **Medium / M** — [co-f5]; verify-before-filing gate
   first.
10. Verify upstream nil-payload behavior (can the default codec ever
    bind NULL?); document verdict in the adapter comment; decide
    codec-seam upstreamization. **Medium / S** — [co-f6][co-f22].
11. Land the suite-copy expiry: art-dupl accept-list gate (seeded with
    all three copies) OR shared conformance harness package — BEFORE
    row 33 starts. **High / M** — [co-f4→§e4][15-59 §f4].
12. `scripts/lint-baseline.sh --check` clean-cache run; deliberate
    regen absorbing both spike modules' rows in one policy-owned
    change. **Medium / M** — [co-f8]; prediction grep-confirmed §a3.
13. ci-local full end-to-end green run. **High / L** — [co-f12];
    carried since 00-21 §b1.
14. vendorHash fast gate + `nix build` over the two spike-module FOD
    inputs. **High / S** — [co-f13].
15. Repair the fuzz workflow (red since 2026-09-22 03:32; nightly
    silently dead). **Medium / M** — [co-f14].
16. Fold CONTRIBUTING.md + stash-list into `scripts/session-start.sh`
    output. **Medium / S** — [new, §e1].
17. Codify the repeat-dispatch verify battery (two-line convention in
    AGENTS.md session-start section). **Low / S** — [new, §e3].
18. DIVERGENCE-skip ledger check script. **Low / S** — [co-f18, §e5].
19. ADR-0019 S2: journal onto upstream `facts.Fact`. **Medium / L** —
    [row 37].
20. ADR-0019 S3: read side onto metaengine + Watcher/ServeSSE.
    **Medium / L** — [row 38].
21. ADR-0019 S4: `system/` composition; DELETE hand-rolled engines +
    mirrored suites (12 art-dupl groups die). **High / L** — [row 39].
22. Dogfood cutover (replay production journal, swap TQ_DB) — owner-run
    only. **Critical / L** — [row 40], BLOCKED by design.
23. Run `TestEnqueueClaimBaseline10k` once against sqlitev4
    (`TQ_BASELINE=1`) — the S1-flip performance question, unmeasured.
    **Medium / S** — [co-f29].
24. Annotate 16-36 §b2 (race-predates-HEAD caveat) as closed by this
    window's at-HEAD race run — docs-health ANNOTATE, inline appendix.
    **Low / S** — [new, c5].
25. Systematic diff of UPDATE-statement shapes: upstream engine vs tq
    sqlite store, to bound the unknowns beyond D1's last_error find.
    **Medium / M** — [co-f26].
26. Package-comment pins: `Requeue` deliberately not engine's
    (resume_closeout evidence) + ClaimDue exclusivity/aging predicate
    names its tq constants — S4-deletion insurance. **Low / S** —
    [co-f24][co-f25].
27. Add a queue-family canary to check-gosec's module enumeration
    (a renamed queue module would currently drop out silently).
    **Low / S** — [co-f27].
28. Assets archive for the suite-triage logs (42→2→0 arc) under
    docs/status/assets/ + SHA256SUMS ritual — /tmp evidence dies with
    sessions. **Low / S** — [co-f20].
29. Decide `facts_archive`/`journal_meta` fate (companion vs
    upstream-grown vs drop ADR-0006 prototype) — memo input; current
    sqlitev4 answer is an erroring stub. **Medium / S** — [co-f19].
30. At S1 flip: check every `tq show`/webui surface rendering
    `last_error` for the stale-error-on-completed case. **Medium / S**
    — [co-f21].
31. Standing rule honored: `check-go-mods.sh` after any go.mod touch in
    spike modules. **Low / S** — [co-f28].
32. Harvest-side DONE-skip: when a TODO row is `[x]` with an indexed
    close-out, the harvester/pool should skip minting a re-dispatch (or
    mint it with a "verify-only" band) — the row-92 triple and this
    window are four datapoints. **Medium / M** — [new, §d-structural;
    cf. 02-06 §e].
33. docs-health HARVEST over this §f list into TODO_LIST/ROADMAP (the
    standing closer — items die in timestamped files otherwise).
    **Low / S** — [new].
34. Frame g1's decision memo input with the both-suites-green fact so
    the ruling is explicitly semantic. **Low / S** — [new, §e6].

(34 items — every one specific and sourced; the count is honest, not
padded to 50. Items 1–6, 19–22 are the critical path.)

## g) Questions I can NOT figure out myself

All three are carried from the 16-36 close-out §g — still open, still
blocking, and still owner-only. What I tried: code reading on both
sides of each question, both suites now green (this window), divergences
catalogued D1–D6 — the facts are exhausted; the remaining input is a
ruling.

1. **Which S1 spike survives — cqrsqlite or sqlitev4 — and may the
   loser be deleted?** The migration plan names cqrsqlite (C03); the
   live work, TODO rows, suite green, and the two extras-only surfaces
   (project exclusivity, resume-closeout requeue) sit on sqlitev4.
   This decides whether rows 33–36 build on sqlitev4 or port the parity
   work across.
2. **Cross-process same-owner finalization: tq parity (sqlitev4
   allows — DB row is the gate), strictness (cqrsqlite/upstream forbid
   — token never leaves the claim), or full token handoff?** Decides
   whether `tokenFor` is the final shape or scaffolding, and is the
   one residual where the two spikes actively disagree.
3. **The heartbeat ghost read: should the worker actually write
   `task.heartbeat` facts (journal growth per tick), or should `tq
   doctor`/`/health` move to a live signal (lease expiry / heartbeat
   row) and the constant's consumers be deleted?** Observability-vs-
   journal-size tradeoff; a permanently-zero oracle serves neither.

## Verification battery (this window; all counts grepped from captured files)

- `date` → 2026-09-23 16:46 CEST; session-start.sh with the task ID;
  `rg -l 000001a0cb25 docs/status/` → both prior reports (turn 1).
- sqlitev4 @ 06213e75: `GOWORK=off go test ./... -count=1 -v` → rc=0,
  72 pass incl. subtests / 3 skip (/tmp/sqlitev4-head-norace.log);
  `-race` → rc=0, 66 top-level pass / 4 skip / 0 fail
  (/tmp/sqlitev4-head-race.log; extra skip = TestLoadSnapshotScaleAt100k,
  scale-gated).
- cqrsqlite: `GOWORK=off go test ./... -count=1` → rc=0, 3.76s — first
  cited green in repo history (/tmp/cqrsqlite-first-cited-run.log).
- `check-doc-refs.sh` rc=0; root `go build ./... && go vet ./...`
  (GOEXPERIMENT=jsonv2, GOTOOLCHAIN=auto) rc=0.
- `.golangci-baseline.txt`: `grep -c 'sqlitev4\|cqrsqlite'` → 0.
- DIVERGENCE markers in sqlitev4 tests: 2 (matches the 16-28 skip
  ledger; the other two skips are env/scale-gated, not divergences).
- Caveat: all log paths are /tmp and die with the session; the counts
  and rc values above are the durable record (co-f20 tracks the
  archive ritual).
- No code changed; TODO_LIST untouched (row 31 already `[x]`; no new
  genuinely-new work item — §f is HARVEST fuel, not backlog edits).
