# Task 000001a0c5fdc7cf — dead-export structural-liveness annotation: verify window (third dispatch)

**When:** 2026-09-22 00:40 · **Scope:** verify-only re-dispatch of an already-shipped work item
**Item:** TODO_LIST.md "Dead-export heuristic noise" — advisory annotation in
`scripts/check-dead-exports.sh` for symbols alive only behind live exports.

## Verdict

**WORK COMPLETE — verified, no delta produced, correctly none expected.**
This was the third dispatch of a DONE task. Windows 1-2 (00-18, 00-31 today)
implemented and first-verified the annotation; this window ran the
verify-window minimum battery at HEAD and confirmed the shipped behavior.

## a) FULLY DONE

- Annotation shipped (prior windows, commit 8833[6-7]/b992 lineage; code at
  `scripts/check-dead-exports.sh:27-110`): signature table of every exported
  func, second pass annotates any dead-flagged name appearing word-delimited
  in a DIFFERENT exported func's signature of the same package as
  "structurally alive: return/parameter type of X()".
- TODO_LIST.md item checked `[x]` (line 88) by a prior window.
- **This window's fresh gate run at HEAD:**
  - `./scripts/check-dead-exports.sh` → 305 ok, 56 dead, **12 structurally
    alive (advisory)**; annotation fires correctly (e.g. `render.go`:
    FilterState behind `Empty()`, BudgetView behind `Tone()`).
  - `scripts/check-doc-refs.sh` rc=0.
  - Root `go build` + `go vet` under `GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto`
    rc=0.
  - Working tree clean before this report.

## b) PARTIALLY DONE

- Nothing. The item is binary and done.

## c) NOT STARTED

- Nothing for this item. (Tracked-elsewhere rows untouched: the owner-only
  `.tq-verify` vendor-gofmt fix, e2e `-race` vs the 180s stage cap.)

## d) TOTALLY FUCKED UP

- Nothing broken. Honest self-critique of THIS window instead:
  - The verify battery produced **zero fresh delta** — no code change existed
    to test beyond re-running the script; the window states this explicitly
    per the 03-27 §e2 bar rather than padding a fake delta. A stricter
    window could have diffed the annotated-name set against the 00-31
    report's list to prove output stability, not just script success. That
    cross-check was not run.

## e) WHAT WE SHOULD IMPROVE

1. **Re-dispatch of DONE tasks keeps happening** — turn-1 ritual says
   verify→cite→stop, but the queue keeps minting these windows. Cheap fix:
   harvest note (or queue-side) pointing at the prior reports before mint.
2. The annotation heuristic is substring-based awk over signatures — a Go
   AST pass (`scripts/facadeparity` already walks go/parser) would be exact
   and testable; current version has NO test pin.
3. The dead count (56) has no trend tracking — a one-line append of
   `date dead structalive` to a small TSV per run would make regressions
   visible without new gates.
4. `check-script-syntax.sh` covers shellcheck, but nothing pins the
   awk programs inside the script (behavioral pin class like
   `check-transient-retry.sh`).
5. Second pass is O(dead × signatures) shell loop — fine at current scale,
   worth knowing if internal/ grows.

## f) Up to 50 things to get done next (brainstorm — ROADMAP fuel, not commitments; most need HARVEST routing rigor)

Grounded in what this window saw in passing:

1. AST-rewrite of the structural-liveness annotation (go/parser, drop the
   awk signature table).
2. Add a behavior pin test for `check-dead-exports.sh` (fixture tree →
   expected annotation lines, mirroring check-transient-retry.sh).
3. Dead-export trend file: append counts per run; alarm on growth.
4. Triage the remaining 56 dead exports once: kept-on-purpose (document)
   vs prune candidates.
5. Owner ruling: `.tq-verify` gofmt stage → tracked-files-only (owner-only,
   5 dead tasks and counting since 09-20).
6. e2e `-race` vs 180s verify-stage cap (existing TODO row).
7. Queue-side dedup of verify-only re-dispatches (surface prior DONE report
   at mint time).
8. `Empty()`/`Tone()` in render.go: give FilterState/BudgetView direct
   importers or fold them unexported if the annotator is later removed.
9. Health path constants (7 dead flags in health.go) — either export surface
   documentation or unexport.
10. Consider making `--strict` count structurally-alive separately from dead
    (currently dead-only; documented behavior, pin before changing).
    11-50: intentionally left to HARVEST/docs-health routing — items 1-10 are
    the only ones this window actually observed; fabricating 40 more would be
    backlog noise, not planning.

## g) Questions (up to 3)

1. **Owner-only**: may we scope `.tq-verify`'s gofmt stage to
   `git ls-files '*.go'`? It has dead-lettered 5+ real tasks since 09-20 on
   the vendored tree; agents cannot edit their own gate.
2. Should structurally-alive symbols eventually be EXCLUDED from the raw
   audit entirely (keep report = true dead set), or stay annotated-in-place
   as today? The TODO item chose annotate; a later reversal is cheap now.
3. For the 56 remaining dead exports: is there appetite for a one-time
   human triage pass (kept-on-purpose markers vs deletion), or does the
   audit stay purely advisory forever?

## Gates cited

- `./scripts/check-dead-exports.sh` (full output captured 00:38, exit 0,
  advisory) — 305/56/12.
- `scripts/check-doc-refs.sh` rc=0 (00:39).
- `go build ./...` + `go vet ./...` (GOEXPERIMENT=jsonv2) rc=0 (00:39).
- Prior-window reports: `2026-09-22_00-18_task-…` and
  `2026-09-22_00-31_task-…` (same task id).
