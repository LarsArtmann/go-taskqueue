# Session-Start Ritual Script — task 000001a0bd4892c79b5bb9e41de586e9b805

**Window:** 2026-09-20 07:2x–07:3x CEST · **Verdict:** DONE (shipped + verified)
**Work commit:** 1d4e1a1 (footer as last trailer line, verified via `%B`) · **Tree:** clean, nothing pushed

## a) Fully done

1. `scripts/session-start.sh` shipped (48 lines, +x, read-only): whole-repo `git log --oneline -5` (repo-root anchored via `git rev-parse --show-toplevel`), `git status --short`, `git stash list`, per-task-ID `rg -l <id> docs/status/` (IDs as argv, hint when absent), CONTRIBUTING.md head (40 lines) + CLAUDE.md head when present.
2. AGENTS.md ritual bullet (line 889) now leads with `scripts/session-start.sh [<task-id>…]`, preserving the prose steps beneath it.
3. TODO_LIST.md row 149 `[x]` with completion notes per file convention.
4. Gates, all fresh at HEAD: `check-script-syntax.sh` 54 checks / 0 findings; smoke-run of the script with this task's ID rc=0; `check-todo-list.sh`, `check-doc-refs.sh`, `check-guard-wiring.sh` rc=0; root `go build` + `go vet` under `GOEXPERIMENT=jsonv2` rc=0. Race suite not run — zero Go source changed (shell + two markdown files only); the C-gates that could be affected are build/vet/syntax, all green.

## b) Partially done

1. **AGENTS.md integration is pointer-only.** The ritual bullet references the script but the prose steps remain the source of truth; if the script and prose drift, nothing fails. A parity pin (script emits every documented step) was considered and NOT built — left to §f.
2. **Script is not wired into any gate.** It's an agent-facing convenience, not a CI step; `check-guard-wiring.sh` passes only because the filename is neither `check-*` nor `smoke/*`. Whether it should appear in the flake apps is a §g question, not done.

## c) Not started

1. Nothing from the task contract. The "Check for and read CLAUDE.md if present" turn-1 addition is covered by the script; no other sub-items existed.

## d) Totally fucked up (honest list)

1. **Invisible-footer trap, self-inflicted on the first amend (N+1 on a codified lesson).** AGENTS.md documents that a `Task-Queue-ID:` footer must be the LAST line — the trailer parser reads only the final paragraph — yet my first amend placed it above `Assisted-by:` + trailing blank lines. Caught it myself by re-reading `%B`, re-amended with the footer last (1d4e1a1). This was exactly the failure mode the AGENTS.md passage describes (the 2026-09-19 0-target heal). Should have been impossible after reading the doc I helped codify.
2. **Daemon race hit without pre-check.** I ran `git add` + commit while the daemon had already swept the three files into 4544fda ("nothing to commit"). The AGENTS.md rule — re-check `git status --short` immediately before committing — was applied late, costing a failed commit and forcing the amend path at all. Amending an unpushed daemon commit is sanctioned, but never needing the amend is better.
3. **Two tool-syntax round-trips wasted.** (a) heredoc-in-command-substitution rejected by the mvdan shell (`$()` with `<<EOF`); (b) `%(trailers)` in `--format` rejected the same way. The fix (write message to a file, `git commit -F`, `git log --format=%B | grep`) is precisely the scripted-message-file playbook the repo already documents for daemon-safe history edits — I reached for it one attempt late each time.
4. **The report-then-wait pattern.** This report is being written at the explicit close-out ask; per the citation rules the gate runs in §a were already in hand, so no claim here is uncited — but the index row below must ride this commit or the pre-commit hook pattern applies.

## e) What we should improve

1. Footer discipline: any commit carrying the queue ID should be composed through a file + a `tail -1` assertion, not a heredoc — the trap is now personal (§d1).
2. Pre-commit daemon check: `git status --short` immediately before ANY `git commit` in this repo (§d2) — this window is a fresh data point for making it a script (`scripts/commit-with-footer.sh` candidate, §f).
3. The script could print a short "known concurrent-agent hotspots" reminder (AGENTS.md warning block digest) — cheap reinforcement, not done.
4. Script exit semantics: it always exits 0; a `--strict` mode that fails when the task ID grep finds a DONE row could turn verify→cite→stop into a mechanical gate (needs owner ruling on what "DONE found" should do, §g).

## f) Up to 50 next things (honest, mostly small; not padded)

1. Footer-carrying commit helper: `scripts/commit-with-footer.sh <id> <file-with-msg>` — enforces footer-last, pre-checks `git status --short` for daemon races, asserts single well-formed footer (mirrors the commit-msg hook server-side of the daemon bypass).
2. Parity pin: a cheap test/audit asserting every step named in the AGENTS.md ritual bullet appears in `scripts/session-start.sh` output (script/prose drift guard).
3. `session-start.sh --strict <id>`: non-zero exit when the task-ID grep hits a report whose index row marks the work DONE (verify→cite→stop made mechanical; needs §g1 ruling).
4. Add `session-start.sh` to flake `apps` so it's discoverable via `nix run .#session-start`.
5. Re-dispatch suppression at the queue source (every 2026-09-20 report §d leads with this; today's row-149 ID was itself a re-mint of a 2026-09-19 ask).
6. git gc / refs/original drop gating for the dead-SHA baseline (carried across ≥5 reports; blocking #6 here keeps it visible).
7. `check-dead-sha-refs.sh --self-test` (carried).
8. ci.yml parity for the dead-SHA gate (carried, wired in ci-local only).
9. Cite-site growth warning in `check-dead-sha-refs.sh` (baseline is SHA-level; site counts drift benignly every report — a WARN band would stop each report re-explaining it).
10. for-each-module guarded enumeration in lint-baseline.sh / lint-annotations.sh / check-go-mods.sh (TODO row 151, unchanged).
11. Regression pin for the row-133 capture shape (TODO row 152, unchanged).
12. Owner ruling on GOEXPERIMENT env for the pool unit (`Environment=GOEXPERIMENT=jsonv2` on the NixOS module — documented owner fix, still open as far as this window saw).
13. `tq ask` prompt-policy ruling (§g of the 21-04 report; still deliberately-not-done).
14. Client/server mode per-repo experiment (AGENTS.md blocker, unverified since 2026-09-14).
15. Upstream issue: crush `--reasoning-effort` mistyped-level error message (verified misleading 2026-09-14; candidate sits in AGENTS.md, never filed).
16. docs/status/README.md index growth: 481+ lines, one row per report, no archive compaction policy for the index itself.

Deliberately stopped at 16: the remaining backlog rows (TODO_LIST 148/150–152, dead-SHA residue mapping) are already minted and owned; duplicating them here as filler would be padding. Items 1–4 are this task's genuine follow-ons; 5–16 are what I noticed in passing.

## g) Questions I cannot figure out myself

1. **Should the ritual script gate (non-zero exit) when the task-ID grep finds an already-DONE dispatch?** Verify→cite→stop is the documented right answer, but making the script fail could break legitimate re-verify flows. Owner call: enforce, warn, or stay informational?
2. **Should `scripts/session-start.sh` be wired into a gate or the flake?** It's agent-facing (no CI value as a check), but discoverability argues for `nix run .#session-start`. Guard-wiring policy says unwired check-scripts get deleted — does that policy's spirit apply here, or is an agent-convenience script exempt?
3. **Footer-carrier policy for daemon-raced commits:** is amend-the-unpushed-daemon-commit the approved standing procedure (as I did), or should I leave the daemon commit and add a footer-carrying note commit? The first is cleaner history but touches daemon-authored commits; the second litters. A one-line ruling would standardize every future window.
