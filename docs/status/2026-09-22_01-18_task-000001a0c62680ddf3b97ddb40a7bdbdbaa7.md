# Task 000001a0c62680ddf3b97ddb40a7bdbdbaa7 — git-identity sanitizer ci.yml parity (third verify window)

2026-09-22 01:18. Verify→cite→stop window on TODO_LIST row 90
("[x] Mirror the git-identity sanitizer into ci.yml's release-gates step … or document the
ci-local-only asymmetry as deliberate"). Primary work window: 00-48 (env-mirror landed in
commit 19ffcd4); second verify window: 00-55. This window found ZERO source delta by design —
the fastest correct response to a checked row with two prior reports is verify → cite → stop.

## a) FULLY DONE

- **Turn-1 state check**: `git log --oneline -5` + `git status --short` over the whole repo
  before anything — saw 19ffcd4 (the mirror) and the two prior same-task reports; clean tree.
- **First-hand mirror confirmation at HEAD**: ci.yml release-gates step carries the step-level
  sanitizer env (`GIT_CONFIG_GLOBAL: /dev/null` + `GIT_CONFIG_COUNT=1 … user.useConfigOnly=true`)
  with a comment naming the parity intent; scripts/ci-local.sh:261-268 carries the identical
  env for the local run. Asymmetry was closed by MIRRORING, not by a deliberate-divergence note.
- **Minimum verify battery re-run at HEAD** (fresh, not inherited):
  - `./scripts/check-doc-refs.sh` → rc=0
  - root `go build ./... && go vet ./...` under `GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto` → rc=0
  - ci.yml YAML parse → ok
  - `./scripts/smoke/release-gates.sh` under the EXACT sanitizer env (+ scratch `TQ_DB`,
    cleaned up) → rc=0, "all cases green" including the dropped `-c user.*` fixtures.
- **TODO_LIST row 90 already `[x]` with DONE note** — consistent with code; no edit needed.

## b) PARTIALLY DONE

Nothing. The row is binary (mirror OR document deliberate) and the mirror ships.

## c) NOT STARTED

- Runner-side confirmation that the ci.yml step is green on a real push (only inference +
  YAML parse locally — see §g).
- The §e proposals below (single-ownership refactor of the sanitizer env, a parity gate).

## d) TOTALLY FUCKED UP (near-misses)

- **First edit attempt this window would have been a no-op redundant change** — I initially
  started inspecting as if the row were open; the first `grep` of TODO_LIST revealed row 90
  already `[x]`. Caught before any file write. The turn-1 lesson that saved this window:
  grep prior reports/TODO state for the task BEFORE planning edits.
- **Long-running battery command** (build+vet) hit the foreground timeout and moved to
  background — handled correctly, but the battery should have been split up front.
- Nothing shipped wrong: zero source delta, zero broken state, working tree clean.

## e) WHAT WE SHOULD IMPROVE

1. **Sanitizer single-ownership**: the env block is duplicated verbatim in ci-local.sh and
   ci.yml. Folding it into `scripts/smoke/release-gates.sh` itself (self-sanitize unless an
   opt-out var is set) would leave ONE copy; ci-local/ci.yml shrink to a bare invocation.
   Needs an owner ruling because it changes what the smoke does when run by hand.
2. **A generic ci-local↔ci.yml parity gate** would have caught this class (and the fullcore
   smoke gap that needed the 2026-09-15 batch) mechanically instead of by TODO row.
3. **Verify-window ritual cost**: the "grep prior reports for the task ID" step (folded into
   `scripts/session-start.sh` per the 00-55 report) was the decisive step here; datapoint N+1
   for making it unconditional in every window, not just work windows.
4. **Scratch-DB hygiene worked**: exported `TQ_DB=/tmp/…` + cleanup for the smoke — the
   AGENTS.md hazard (bare `tq` touches production) did not bite; keep the pattern.

## f) NEXT (scoped, agent-executable)

1. Fold the git-identity sanitizer env into `release-gates.sh` behind an opt-out var; drop the
   duplicated env blocks from ci-local.sh + ci.yml (owner ruling first, §g1).
2. Prototype `check-parity.sh`: a table of (gate, ci-local invocation, ci.yml invocation)
   diffed mechanically — start with the smokes listed in both files.
3. Add a TODO_LIST row + report for a runner-confirmation citation once ci.yml runs the
   release-gates step on the next push (link the run id).
4. Extend `scripts/check-guard-wiring.sh`'s self-test appetite: assert every smoke referenced
   in ci.yml is also referenced in ci-local.sh (and vice versa) — a cheap 80% parity gate.
5. Document in AGENTS.md the one-line fact that release-gates smoke is identity-strict
   everywhere (helps the next debugging session avoid "why does my local run fail here").

## g) QUESTIONS FOR THE OWNER

1. Single-ownership for the sanitizer: OK to fold the env into `release-gates.sh` itself
   (self-sanitizing, `TQ_KEEP_GIT_IDENTITY=1` opt-out), deleting the two duplicated blocks?
2. Do you want a mechanical ci-local↔ci.yml parity gate (§f2/§f4), or is TODO-row-driven
   mirroring acceptable given how rarely the two drift?
3. Citation bar for runner confirmation: is a linked green CI run id in the row/report
   required before the mirror is considered fully proven, or is local smoke + YAML parse
   sufficient?
