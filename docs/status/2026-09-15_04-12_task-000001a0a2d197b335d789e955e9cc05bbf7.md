# Task 000001a0a2d197b335d789e955e9cc05bbf7 — enqueue `$TQ_DB` guardrail — third-dispatch verification lap

- **Date:** 2026-09-15 04:12
- **Item:** "`tq enqueue` guardrail: warn when `$TQ_DB` points somewhere other than `./tasks.db` (the production-DB trap cost a live enqueue 2026-09-10) (05-29 report c4/d2/e2)"
- **Dispatch:** This was a RE-dispatch. The work was already fully shipped by a prior dispatch of this exact task id — commits `9139917` (feature) and `e0f3853` (close-out report).

## a) Fully done

- **Guardrail implementation** — `cmd/tq/main.go:307-308`: `tq enqueue` prints a stderr warning whenever `$TQ_DB` is set, naming the target DB and the `--db` escape hatch. Commit `9139917`.
- **TODO_LIST.md closed** — line 83 marked `[x]` by the prior dispatch.
- **Close-out report committed** — `e0f3853`, with the correct `Task-Queue-ID: 000001a0a2d197b335d789e955e9cc05bbf7` footer (verified this session via `git log -1 --format=%B`).
- **Independent verification (this lap):**
  - Guardrail code confirmed present and wired into the enqueue path (grep, not memory).
  - `./scripts/test-cmd-tq.sh` gate: PASS (`ok github.com/larsartmann/go-taskqueue/cmd/tq 7.806s`).
  - Root `go build ./...` with `GOEXPERIMENT=jsonv2`: PASS.
  - Working tree clean before and after; no new changes needed.
- **Session-start ritual** — ran `git log --oneline -5` + `git status` first, which is exactly what revealed the duplicate dispatch before any wasted work.

## b) Partially done

- Nothing. The item is binary and it is done.

## c) Not started

- Nothing within this work item's scope.

## d) Totally fucked up

- **First verify attempt used the wrong gate.** I ran `cd cmd/tq && go test -run 'TestEnqueue' .` directly, which fails with `ambiguous import` (cmd/tq is a replace-free module per ADR-0017; in-repo builds must go through the devmod shim). AGENTS.md documents this exact failure mode. Cost: one wasted round trip. The fix (`./scripts/test-cmd-tq.sh`) was already documented — I should have read my own context before running the gate.
- **Initial test run filtered to `TestEnqueue` then, when it failed, I dropped the filter entirely** rather than reading the error. The error was setup-level (ambiguous import), not test-level — but the reflex "test failed → widen scope" instead of "test failed → read the error" is sloppy. The full-suite shim run passes in ~8s, so running it directly would have been both correct and no slower.
- **Report-only dispatch almost shipped without a commit.** My work-lap output ended with the TQ_RESULT line but no new commit was made in that lap (correctly — nothing changed), yet the task contract expects the loop closed. The close-out here is the report you are reading. Not a defect in the repo, but the contract's "commit with footer" step has no clean no-op story when the work was already committed by a twin dispatch; worth a queue-side convention.

## e) What we should improve

1. **Duplicate-dispatch guard.** This task was dispatched at least three times (implementation lap, close-out lap, this verification lap). The queue has no notion of "this item's task id already has commits on master." A cheap `git log --grep=Task-Queue-ID: <id>` pre-flight at dispatch time would have made laps 2 and 3 near-no-ops by design instead of by discovery.
2. **Verify-gate muscle memory.** The correct cmd/tq gate is one line in AGENTS.md and I still reached for the raw `go test`. Claims-citations rule cuts both ways: I should cite the gate command I am about to run, which forces reading it first.
3. **TQ_RESULT for no-op laps.** My work lap emitted a TQ_RESULT with `"files_changed": []` and a note; the mechanical gate tolerates optional fields, but a conventional `"noop": true` field (or a dedicated verdict) would make re-dispatch detection trivially machine-readable.

## f) Next up to ~50 (scoped to what I noticed in passing — no unrelated research)

1. Backfill a dispatch pre-flight: check `git log --grep` for the task id before minting agent tasks (queue-side, owner call).
2. Add a `noop`/`already-done` field convention to the TQ_RESULT gate documentation.
3. The `tq enqueue` warning fires on EVERY enqueue while `$TQ_DB` is set, even when the operator intends the env DB — consider an opt-out (`--db` already silences it implicitly; verify + document).
4. Add a smoke/test asserting the warning text (grep found no dedicated pin in cmd/tq tests — the shim suite passed, but I did not locate a `TestEnqueueWarnsTQDB`-style pin; if absent, add one so rename sweeps cannot deaden the message, the class that bit `q.Get("query")` and `--max-attempts` help).
5. The warning message hardcodes `./tasks.db` as the implied target; the actual default also honors a `--db` flag and config precedence — one line of doc alignment in the help text (`main.go:88`) if not already exact.
6. The env-var guard exists only for enqueue (bootstrap/tests explicitly avoid `$TQ_DB`); consider whether `tq worker`/`tq harvest` deserve the same guard or an explicit "operating on $TQ_DB" log line at startup — the 2026-09-10 incident was an enqueue, but any bare `tq …` touches production.
7. Mirror the guard's reasoning into SECURITY.md or README's operational-warnings section if not already there.
8. cmd/tq "ambiguous import" trap: consider a tiny `go.mod` comment or a `// ADR-0017:` pointer at the top of cmd/tq files so fresh sessions hit the reminder before the failure, not after.
9. ADR-0017's "until the next root tag containing the split lands on the proxy" clause — v0.3.0 is tagged; re-verify whether the plain-`go build` trap inside cmd/tq is now resolved or still expected (my lap suggests still expected).
10. Consider extending `scripts/test-cmd-tq.sh` to accept a `-run` passthrough so targeted re-runs don't tempt raw `go test`.

(That is the honest top-10 from this lap. Padding to an arbitrary 50 from a no-op verification dispatch would fabricate scope; the standing harvest sections in TODO_LIST.md remain the authoritative backlog.)

## g) Questions I cannot figure out myself

1. **Dispatch policy:** is triple-dispatch of one task id expected dogfood behavior (re-verify laps), or a mint-side bug I should treat as a defect in the harvest/sweeper dedup?
2. **No-op lap contract:** should a verification-only lap emit TQ_RESULT with an empty `files_changed` + the original commit sha (what I did), or should the queue prefer a distinct "already done" verdict?
3. **Warning ergonomics:** do you want the `$TQ_DB` enqueue warning to stay unconditional (loud on every enqueue), become once-per-process, or gain a `TQ_DB_WARN=off` opt-out? The trap argues loud; batch workflows may argue noise.
