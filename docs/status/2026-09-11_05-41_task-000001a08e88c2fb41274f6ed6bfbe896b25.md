# Status Report — task 000001a08e88c2fb41274f6ed6bfbe896b25

**Date:** 2026-09-11 05:41 CEST
**Work item:** `tq doctor`: warn when crush/git/go are missing from PATH — pool-context visibility of the exact failure class that killed the deployed pool for 20h (02:00 f18)
**Scope:** this task's work only, plus what was noticed in passing. No unrelated research.

## Summary

This is the **second dispatch** of this task id (re-fire). The work was shipped by the first window in commit `3e05dfa` and closed out by that window's report (`2026-09-11_05-38`, same task id — note: that report landed unindexed; indexed as a drive-by fix in this commit, per the 2026-09-06 two-line-fix-on-sight owner permission). This window is therefore a **verification-only close-out**: session-start ritual, independent re-verification of the shipped implementation, and this report.

## a) FULLY DONE

- **Independent re-verification (this window):**
  - Implementation confirmed at `doctorEnvironment` (cmd/tq/doctor.go:344–370): `agent-binary` check (crush, `TQ_AGENT_BIN` override honored) plus `tool:git` / `tool:go` `checkWarn`s via `exec.LookPath`, each naming its consequence and pointing at the service PATH (`agentPath`) — the f18 failure class is visible from the pool context.
  - Verification gate re-run green: `GOEXPERIMENT=jsonv2 go build ./... && go vet ./...` clean; `go test ./cmd/tq/ -run Doctor -count=1` → `ok`.
  - TODO_LIST.md:81 already `[x]`; working tree clean before this report.
- **Drive-by fix-on-sight:** the first window's 05-38 close-out report was committed (`f732f5f`) but never indexed in `docs/status/README.md` — `check-status-index.sh` currently reports it UNINDEXED. Indexed in this commit together with this report's row (two-line class, owner permission 2026-09-06).
- Re-fire pattern data point: this is now the **5th stale re-dispatch** recorded across recent windows (skip-log task ×4, this task ×2). The dispatcher keeps re-firing completed items.

## b) PARTIALLY DONE

- Nothing from this task. The item as written is complete and verified twice by independent windows.

## c) NOT STARTED (noticed in passing, out of scope here)

- Doctor's PATH checks diagnose the *caller's* PATH, not the systemd unit's — a laptop run still says "ok" while the deployed pool is broken. Requires a service-context inspection mode (see f-list item 1).
- All 90 unchecked TODO_LIST items remain; none touched.

## d) TOTALLY FUCKED UP

- Nothing. No edits to product code, no broken state, no reverts, `.crushrc`/`crush.json`/`.tq-verify` untouched. The only working-tree change is this report + the missing index row.
- Process flaw in the *first* window (worth recording): it committed its close-out report but skipped the index row, leaving the status-index gate red at HEAD until now. The close-out contract should include "run check-status-index.sh before declaring done."

## e) WHAT WE SHOULD IMPROVE

1. The status-index gate is only enforced in ci-local.sh — a post-commit local check (or the pre-commit hook) would have caught the 05-38 omission immediately.
2. Re-fire root cause (5 data points now): the dispatcher does not observe `[x]` transitions or TQ_RESULT records reliably; every re-fire burns a full verification window (~agent cost, zero delta).
3. Verification-only windows could be made cheaper: a `--verify-only` work-item flavor, or the queue marking items re-fired N times with a "verify-only" instruction embedded in the item text.
4. Doctor PATH checks should gain a service-context mode (read pool.conf / systemd unit agentPath) — the highest-value follow-up to this task's original intent.
5. The 05-38 report's §e improvements (service-PATH diagnosis, severity-aware worst computation, full ci-local gate) remain valid and are carried into the f-list below.

## f) Up to 50 things we should get done next

Pulled from TODO_LIST.md unchecked items + this window's observations, highest impact first:

1. Doctor service-context PATH check: inspect the deployed unit's agentPath/pool.conf, not just the caller's PATH (05-38 §e1, this §c)
2. Dead-pool detection: PapDashboard alert when scan-failed skips cover every repo for N ticks (TODO:82)
3. Re-fire root cause: why the dispatcher re-enqueues `[x]`-closed items — 5th data point; fix the loop at the source (this §e2)
4. Restore the nix CI gate (runner-only FOD hash mismatch; differential-dump the runner's module fetch first) (TODO:125)
5. `tq pool-health`: per-repo skip streaks + last harvest activity (TODO:85)
6. Master-CI state gate (`scripts/check-ci.sh` wired into ci-local.sh) — DONE verdicts keep landing on red master (TODO:105)
7. Hermetic fullcore smoke `scripts/smoke/fullcore.sh` (TODO:128)
8. Dogfood smoke `scripts/smoke/dogfood-once.sh` (TQ_DOGFOOD=1-gated + stub variant) (TODO:83)
9. Archive dogfood evidence durably before /tmp reboots (TODO:84)
10. Origin reconciliation: local master vs remote `41b817b` bad-footer tip (TODO:160)
11. Tag-lineage fork acceptance / hardening decision (TODO:161)
12. Canonical-ID resolution for the f26 three-ID cluster (TODO:162)
13. Closeout-report placement: root floods ~10 reports/day → subdir or archive cadence (TODO:131)
14. Status/closeout TODO-append caps: backlog outgrows the ~60/day drain (TODO:132)
15. Ticket `…872a00bb18a4f9e326` phantom-footer close-out (TODO:133)
16. Commit-msg hook: require exactly one `Task-Queue-ID` trailer of the right shape (TODO:148)
17. `tq facts`/`tq show`: commits-per-Task-Queue-ID view to surface wrong/duplicate footers (TODO:149)
18. check-status-index.sh: cross-check report filename ID vs commit trailers (TODO:150)
19. Extend check-status-index.sh: filename-date vs report Written-line drift (TODO:151)
20. gosec FP-triage exclude-rule config so the advisory job goes green (TODO:102)
21. Flip govulncheck to a hard gate once green on the runner (TODO:103)
22. ci-local.sh: release-gates smoke under `GIT_CONFIG_GLOBAL=/dev/null` (TODO:104)
23. Changed-lines line-length gate (lll/golines, 120 cols) (TODO:106)
24. `task.AllStatuses` export + retire webui/httpapi twin status lists (TODO:108)
25. gosec/govulncheck findings as CI job-summary artifacts (TODO:109)
26. Required-checks proposal into docs/planning/ (TODO:110)
27. Triage webui zero-importer exports (`BudgetView`, `BoardColumn`, `DashboardData`) (TODO:111)
28. Fix the err113 finding at cmd/tq/doctor.go:472 as a static sentinel (TODO:116)
29. SECURITY.md: add the two advisory scan jobs to the defense-layers matrix (TODO:117)
30. `internal/consumer`: wire into `tq serve` journal tailing or delete the ghost package (TODO:50)
31. Decide the three near-identical FactSource-style interfaces (TODO:51)
32. Postgres CLI store wiring (`--store postgres://…`) (TODO:52)
33. SystemNix host-side deploy + round-9 cutover (owner-run, sudo-gated) (TODO:45)
34. Verify the CQA bridge against a live CQA instance (BLOCKED: needs owner URL+token) (TODO:95)
35. Release-tooling audit for tag-ancestry assumptions (release.sh + release-gates) (TODO:144)
36. RELEASE.md tag-ancestry/rewrite section + `--push` failure docs (TODO:145)
37. Smoke sub-tag cutting end-to-end on a fixture (TODO:146)
38. Automate the "confirm CI green on tag ref" step of `release.sh --push` (TODO:147)
39. CHANGELOG audit: no pre-reword-lineage SHAs cited in v0.2.0 section (TODO:152)
40. CI check: no workflow assumes fast-forward from origin/master while divergence stands (TODO:154)
41. Lint-baseline slice-triage round 2: goconst/mnd/paralleltest/testpackage (TODO:141)
42. Per-linter findings baseline file/CI summary so shrink claims are measurable (TODO:142)
43. Execute scripts/release.sh against a throwaway fixture repo incl. sub-tags (TODO:143)
44. Extract `scripts/for-each-module.sh`; consume from ci.yml (4+ copies) + ci-local.sh (TODO:178)
45. `actionlint` in devShell + ci-local.sh (TODO:179)
46. Unify the golangci-lint version pin (ci.yml vs ci-local.sh drift risk) (TODO:181)
47. Docs-health continuation: annotate + archive the six 2026-09-07 reports (TODO:130)
48. Session-close bridge design + prototype (crush #3146 SessionEnd hook) (TODO:87)
49. Worktree-per-agent design doc (TODO:86)
50. Status-task window payload: include the window's closeout report paths (TODO:129)

## g) Questions I can NOT figure out myself

1. **Re-fire trigger:** the dispatcher re-fired this task ~3 minutes after its first close-out landed (05:38 report → 05:41 dispatch). Is the queue polling a stale TODO_LIST snapshot, or is the `[x]` close-out write racing the harvester? This is the one thing standing between us and ~5 wasted windows.
2. **Master push authorization (carried, TODO:134):** master moved remotely while AGENTS.md says agents never push — were those owner-run, and is the push policy for agents changing? Affects how the origin-reconciliation item (f10) must be executed.
3. **Scan-fail cadence verdict (carried):** after the skip-log change detection shipped (783f4bf), has the production pool's per-tick skip flood actually stopped, or should dead-pool detection (f2) be designed against a different signal?
