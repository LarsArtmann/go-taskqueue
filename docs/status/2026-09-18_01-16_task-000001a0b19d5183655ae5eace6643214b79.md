# Task 000001a0b19d5183655ae5eace6643214b79 — Verify window on the session-volume row

**Date:** 2026-09-18 01:16 CEST
**Outcome:** DONE-on-arrival — verification-only window, zero code changes.
**Authority:** the implementation close-out is
`docs/status/2026-09-18_01-12_task-000001a0b19d5183655ae5eace6643214b79.md`
(filed 4 minutes before this window by the shipping session); its §a–g are
authoritative and are NOT restated here. This report records only what THIS
window did and noticed.

## a) Fully done (this window)

- Turn-1 ritual grep found the row already `[x]` (TODO_LIST:113) with the
  footer commits on master: `1da3457` (implementation) + `6880073` (the 01-12
  report itself). Response per protocol: verify → cite → stop.
- Fresh gate re-run green at HEAD:
  `go build ./...` (GOEXPERIMENT=jsonv2) ok;
  `TestSessionSegmentsRenderFromSnapshot` (internal/webui) ok;
  `TestStatsSessionVolume` via `scripts/test-cmd-tq.sh -run` ok (8.2s).
- No commit was needed for the verification itself; this report + index row
  is the only artifact.

## b) Partially done

- Nothing — the window had no partial deliverables. Open gaps from the 01-12
  §b stand (lamp CSS, all-time volume, unrun webui smoke, CHANGELOG silence).

## c) Not started

- All of the 01-12 §f list (50 items, top-3 starred there) remains the
  authoritative backlog source; nothing new discovered this window.

## d) What went wrong (this window)

- **Wrong verify gate, once**: first ran root-level `go test ./cmd/tq` —
  fails by design (cmd/tq is its own replace-free module, ADR-0017);
  self-corrected to `scripts/test-cmd-tq.sh`. This is now the FOURTH
  occurrence of the same-ID/window class hitting this exact miss (00-57,
  00-14 report it too) — a session-ritual script (01-12 §f item 12's
  neighbor) should carry the correct gate command per surface.
- No other defects: no edits, no reverts, no daemon surprises.

## e) What to improve

- Verify-only windows need exactly three artifacts (gate output, index row,
  report) and nothing else — this window complied; the cost is the queue
  still paying a full window per re-dispatch (mint-side done-check, carried).

## f) Next

- Carry the 01-12 §f list forward unchanged; no new items. Its item 12
  (done-row dedup guard) is now proven with FOUR burned windows on this
  task-class across 00-57/01-01/00-14/here — the highest-leverage next
  queue-infrastructure task.

## g) Questions (carried, unchanged)

1. Lamp color semantics (alarm vs neutral-attention) — 01-12 §g1.
2. Windowed vs all-time volume default — 01-12 §g2.
3. httpapi stats parity for the sessions keys — 01-12 §g3.
