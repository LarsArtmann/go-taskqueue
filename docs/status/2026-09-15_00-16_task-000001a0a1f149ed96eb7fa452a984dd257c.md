# Journal-drift audit — dispatch verification (second pass)

- **Date**: 2026-09-15 00:16
- **Task**: 000001a0a1f149ed96eb7fa452a984dd257c
- **Verdict**: work item was ALREADY FULLY DONE by a prior session; this dispatch verified and recorded only.

## a) FULLY DONE

- `tq audit --journal` feature complete: `cmd/tq/journalaudit.go` (fact replay into a full task projection — status, attempts, priority, dedup key) + `--journal` flag in `cmd/tq/audit.go:21`, advisory per the 2026-09-14 ruling (operator command, never a hard gate on task state).
- Hermetic fixture tests: drift seeded by corrupting a scratch DB's projection; production journal never touched (pinned in feat commit 6b692f2's message).
- TODO_LIST.md row checked `[x]` (line 19), citation to source report intact.
- Commits already on master: feat 6b692f2 (carries the correct Task-Queue-ID footer) + close-out report a0be7be. No new commit was needed for the work itself.

## b) PARTIALLY DONE

- Nothing for this task. The ci-local advisory smoke over a seeded fixture named in the TODO row ("advisory when it lands") did NOT land with the feature — see §e.

## c) NOT STARTED

- The advisory ci-local smoke (`tq audit --journal` over a seeded fixture DB) — explicitly optional, never a gate.

## d) TOTALLY FUCKED UP

- Nothing destructive. Honest misses on this dispatch:
  1. **Turn-1 verification gap**: I confirmed the feat commit and TODO checkbox but did NOT run the journalaudit fixture tests explicitly by name on the first gate run — they ran inside the suites, but a targeted `-run Audit` pass would have pinned the drift detector end-to-end rather than trusting suite aggregation.
  2. **Close-out session ritual**: AGENTS.md mandates `git stash list` and a whole-repo `git log` at session start — I ran `git log --oneline -5` + `git status` but skipped the stash list (recurring miss across sessions per prior reports).
  3. The first `scripts/test-cmd-tq.sh` invocation used a wrong relative path and burned a round trip (self-inflicted, caught immediately).

## e) WHAT WE SHOULD IMPROVE

- Advisory smokes named in TODO rows should land WITH the feature or be split into their own row at check-off time — "advisory when it lands" rows otherwise rot invisibly.
- Always run a targeted test-name filter for the feature's own tests, not just the suite tail.
- Session-start ritual (stash list included) is a checklist item, not a suggestion — apply it before the first tool call.

## f) NEXT (delta items; canonical backlog lives in the repo's TODO_LIST.md)

1. Land the advisory `tq audit --journal` seeded-fixture smoke in ci-local (advisory, never a gate — O5 precedent).
2. `tq audit --journal` coverage for `task.requeued` / dead-letter attempt derivation edge cases vs legacy thin facts (the code notes legacy thin facts are not diffed — document or close that gap).
3. Repair-mode design decision (owner ruling needed): the TODO row says "no repair mode yet" — mint an explicit decision row or a design doc.
4. Postgres journal drift-audit parity (`AppendFact` gap already tracked in TODO row 110).
5. Consider `tq audit --journal --json` for machine consumption by future tooling.
6. Fold this report into the docs-health pass (annotate + archive when fully executed).

## g) QUESTIONS (cannot self-answer)

1. Is the missing advisory ci-local smoke acceptable as-shipped, or should it block considering the row done?
2. Should `tq audit --journal` ever gain a repair mode, and if so under what owner gate (it contradicts the advisory-only ruling as written)?
3. For double-dispatch tasks like this: is a no-change verification dispatch an acceptable spend, or should the queue suppress re-minting rows already checked `[x]` with a landing commit?
