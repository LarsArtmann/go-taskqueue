# Rate-limit retro task (000001a0a271) — 2nd-lap verification close-out

**Task:** 000001a0a27176572e0bcf9c305950d445a5 — "Post-deploy retro: one week
after the input flip, count 429 requeues vs pre-deploy dead-letters and record
the number in a status report (13:29 report f50)"
**Verdict: DONE-on-arrival, verification-only, zero deliverable changes.**
This is the **2nd paid lap** on this task ID (first: 02-28 close-out, indexed).

## What I found

The work item was already fully closed by a concurrent session before my
dispatch:

- **Report exists and carries the numbers**: `2026-09-15_02-30_post-deploy-rate-limit-retro.md` —
  journal-derived counts over the production tq.db (2026-09-10 → 09-15):
  pre-flip **28 429-class failed attempts / 8 429-class dead-letters**
  (24 attempts burned); post-flip **0 / 0 / 0**. Zero 429 requeues because
  zero 429s occurred post-deploy — the requeue mechanism is armed but
  production-unproven until the first real 429 window. 7-day re-check
  scheduled 2026-09-18.
- **TODO_LIST.md:73 checked `[x]`** with a DONE citation naming the report
  and the 09-18 re-check.
- **Commits**: work `a8a84e0` (counts + indexing), close-out `dbe152c`
  (carries the exact Task-Queue-ID footer for this task).
- **Indexed**: row present in `docs/status/README.md` (line 26).

## My session, honestly

1. Session-start ritual: `git log --oneline -5`, `git status` (clean),
   `git stash list` (empty). Found dbe152c/a8a84e0 at HEAD.
2. Verified report file exists, is indexed in README.md, and TODO row is
   checked — all fresh this turn, not from memory.
3. Verified the close-out commit carries the exact Task-Queue-ID footer.
4. Correctly declined to create an empty commit ("no empty commits" rule);
   the prior close-out commit IS the queue cross-reference.

## a) FULLY DONE

- The work item itself (retro report with counted numbers, TODO check-off,
  indexed, footer-bearing close-out commit) — all landed by the prior
  session; re-verified fresh this turn.
- My lap added one thing: this honest 2nd-lap record + its index row.

## b) PARTIALLY DONE

- The retro itself is INTERIM: day +4 of 7, not the full week the item
  asked for. The 2026-09-18 re-check is scheduled but NOT yet run — the
  "one week" number is still pending.
- The mechanism's production proof: zero 429s post-flip means the
  requeue-without-attempt-burn path has never fired for real. The claim
  "429 requeues = 0" is true but vacuous until a window occurs.

## c) NOT STARTED

- The 2026-09-18 7-day re-check (someone must re-run the count and
  finalize the retro).
- Owner DLQ triage of the 8 pre-flip 429-dead tasks (rescue vs dismiss) —
  human surface, noted as open in both prior reports.

## d) TOTALLY FUCKED UP (my laps this session)

1. **No mint-time done-check**: I should have grepped the TODO row + index
   BEFORE accepting the dispatch. This is the 2nd paid lap on this exact
   task ID — the same re-dispatch loop pattern at least five other task IDs
   paid for today (02-01/02-03 CI topology ×5, 01-44/46/49 lint pin ×3,
   00-33/00-30 errname ×2, etc.). The systemic fix is a mint-time `[x]`
   check in the harvester/sweeper, not agent diligence.
2. **Filename clock drift again**: `date` reports 02-30 while the prior
   session's real commit times are 02:27/02:28 — my report minute collides
   with the OTHER 02-30 report on disk (different suffix, so no actual
   collision, but the drift repeats the §d #1 finding of the 02-28 close-out:
   the session clock and git clock disagree by minutes).
3. **First turn answered before checking for prior same-ID reports**: I
   verified the work item state but did not grep the index for
   `task-000001a0a271` until writing THIS report — had I done that first,
   I'd have known instantly this was lap 2 (the lint-pin session's own
   §d near-miss, repeated).

## e) WHAT WE SHOULD IMPROVE

- **Mint-time DONE check** (recurring theme, ~6 task IDs today): the queue
  should refuse to dispatch an item whose TODO row is already `[x]` — this
  single gate would have saved every paid re-lap today.
- **Session clock vs git clock**: reports keyed by `date` drift against
  commit timestamps; a `TZ`/NTP check at session start, or deriving the
  filename from `git log -1 --format=%cI`, would kill the drift class.
- **Close-out commit as the verdict channel**: when a task is DONE-on-arrival
  with zero changes, the prior footer-bearing close-out commit is the
  cross-reference; an explicit "verify-only lap" TQ_RESULT convention
  (files_changed empty, commit_sha = prior close-out) is already emerging —
  pin it instead of rediscovering it per session.
- **Interim vs final reports**: a day+4 interim retro and a 7-day final
  retro are different artifacts; the final one should supersede (annotate
  - archive) the interim, per the annotate-never-rewrite convention.

## f) Up to 50 things to get done next

Direct tail from this task (highest leverage first):

1. Run the 2026-09-18 7-day re-check and finalize the retro (supersede the
   interim report, annotate + archive per convention).
2. Mint-time DONE check in harvest/sweeper: refuse dispatch when the TODO
   row is already `[x]` (kills the re-dispatch loop — six task IDs today).
3. Owner DLQ triage of the 8 pre-flip 429-dead tasks (`tq dlq --dismiss` or
   rescue).
4. Wait for the FIRST real post-flip 429 window and verify
   DetectRateLimit + the no-attempt-burn requeue actually fire in
   production (the mechanism's proof).
5. Journal blind spot: `rateLimitUntil` fast-refuse runs never reach the
   journal (in-process gate) — consider a fact or metric so refusals are
   auditable.
6. Fix the session-clock drift class (derive report filenames from git
   commit time or TZ-check at session start).
7. Pin the verify-only-lap TQ_RESULT convention (empty files_changed,
   prior close-out sha) so DONE-on-arrival laps stop being reinvented.

Adjacent backlog noticed in passing (from the index I read at write time):

8. Help-text smoke open §b: --self-test fixture, machine-derived subcommand
   inventory, ci.yml mirror, CHANGELOG row (01-08 report).
9. errname: unresolved mnd/paralleltest policy-disable scoping under
   GOWORK=off (00-33 report).
10. Rename-diff literal-leak sweep: check-literal-leaks.sh gate proposed,
    not landed (00-53 report).
11. Journal-drift audit: Go-seeded fixture vs python3 seeding, attempts==failures
    invariant pin, postgres conformance pass, repair-mode design (00-12 report).
12. lint-annotations: --self-test fixture + jq guard + CI runtime budget
    (01-21 report).
13. CI topology one-pager: doc drift-gate smoke + 2 hedged claims to
    re-derive (02-03 report).
14. golangci-lint pin: fail-loud derivation (silent-zero grep risk) +
    pin-mechanism policy ruling (01-49 report).
15. Session-snapshot ghost: watch charmbracelet/crush#3842 upstream;
    dangling-commit evidence policy (02-18 report).
16. for-each-module.sh: consolidation of the 3+ loop copies already noted;
    AGENTS.md snippet drift fix (01-33 report).
17. Batched-harvest + prioritize + budget interplay docs: none read this
    session — not verified, listed only as a pointer from AGENTS.md.

(Items 8-17 are pointers, not re-verified this session; the direct tail is
1-7. Refusing filler beyond that — the standing backlogs live in
TODO_LIST.md and the prior same-ID reports.)

## g) Questions I can NOT figure out myself

1. **Who owns the mint-time DONE check** — is it a harvester change (skip
   `[x]` rows at scan time), a sweeper change, or a queue-side dispatch
   gate? Without an owner ruling the next lap is another paid no-op.
2. **Should the 09-18 re-check produce a NEW final report that supersedes
   the interim (annotate + archive), or update the interim in place?** The
   annotate-never-rewrite convention suggests supersede, but the interim is
   the indexed artifact the TODO row cites.
3. **Is a DONE-on-arrival lap allowed to commit ANYTHING (this report), or
   should verify-only laps emit a verdict without a report file?** Six
   laps today each wrote a report; if the queue wants the spend to stop,
   the report-must-commit rule for no-op laps is the lever.

---

Verification evidence (fresh this turn): `git log --oneline -5` HEAD =
dbe152c; `git status --short` clean; `git stash list` empty; report file
present; index row README.md:26; TODO_LIST.md:73 `[x]`; footer verified via
`git log -1 --format=%B dbe152c`.
