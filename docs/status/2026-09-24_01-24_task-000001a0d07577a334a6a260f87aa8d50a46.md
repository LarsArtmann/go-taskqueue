# Status: ADR-0019 S1 postgres spike — third verify-only re-dispatch (task 000001a0d07577a334a6a260f87aa8d50a46)

2026-09-24 01-24 window · verification pass of an ALREADY-DONE work item ·
Markdown report (same one-off override as the 01-09/01-18 windows).

Context for a fresh reader: the spike shipped in the 01-05 window, was
closed out at 01-09 (154a5df0), and verify-only re-dispatched at 01-18
(058a9ef6, ca539838 — same Task-Queue-ID). Turn-1 grep surfaced all three
reports plus the `[x]` TODO row before any action; this window's correct
response was again verify → cite → stop, with ONE fresh delta the prior
windows could not produce (see a.3).

## a) FULLY DONE

1. **The work item itself** (prior windows, cited): `internal/queue/postgresv4`
   over `go-cqrs-lite/queue/postgres/v4 v4.0.0`; TODO_LIST.md line 34 is
   `[x]` with the shipped description.
2. **Cheap gates at HEAD ca539838** (fresh, rc captured): `check-doc-refs.sh`
   rc=0; root `go build ./...` rc=0; root `go vet ./...` rc=0 — all under
   `GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto`.
3. **FRESH DELTA — first live-postgres suite run since the primary window**:
   the 01-18 window ran the suite only in skip mode (`TQ_TEST_POSTGRES`
   unset; the live-DB proof was inherited from 01-05/CI). This window stood
   up a hermetic local postgres (nix `initdb`/`pg_ctl`, trust auth, port
   55432 under /tmp — the host's system postgres rejects both `lars`
   password auth and `postgres` peer auth from this shell) and ran the
   FULL suite at HEAD: `cd internal/queue/postgresv4 && GOWORK=off
   GOEXPERIMENT=jsonv2 TQ_TEST_POSTGRES="postgres://postgres@/postgres?host=/tmp/tqpg&port=55432"
   go test ./... -count=1 -race -timeout 300s` → **`ok … 4.589s`, rc=0**.
   The scratch cluster was `trash`ed after the run.
4. **No scope creep**: nothing invented, no drive toward the S1 decision
   memo (the next queued item, line 35).

## b) PARTIALLY DONE

1. The S1 chain overall: sqlite + postgres spikes done; decision memo
   (line 35), replay tool (line 36), flip (line 37), S2–S4 still open.
2. Full ci-local replicant not re-run — out of proportion for a verify-only
   pass at a clean HEAD; the 01-09 close-out owns the full battery.

## c) NOT STARTED

1. S1 decision memo (TODO line 35) — both spikes now have two independent
   live-DB green suites each to cite.
2. S1 replay tool (line 36); S1 flip (line 37); S2/S3/S4; dogfood cutover
   (owner-blocked).

## d) Skipped / partial session ritual

session-start.sh was run; CONTRIBUTING.md/CLAUDE.md turn-1 check not
re-performed this window (no code touched; same omission as 01-18, §d).

## e) Follow-up candidates (backlog, not this run)

None new; the 01-05/01-09 §e items stand, all queued or owner-gated.

## f) Prior-window items carried forward

01-05 §f (upstream gaps + tq-extra surfaces) and 01-18 §f remain valid;
nothing new observed this window.

## g) Owner questions

1. Re-asking 01-18 §g1: queue-side suppression or faster backoff for
   repeated dispatches of DONE rows — this is the third verify-only window
   on this task in one day.
