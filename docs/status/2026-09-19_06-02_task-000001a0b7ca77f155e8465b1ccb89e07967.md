# Close-out self-review — check-gosec.sh `--self-test` invocation-independence window

Task 000001a0b7ca77f155e8465b1ccb89e07967, TODO row 132: resolve the self
path to an ABSOLUTE path BEFORE the top-level `cd` (the recursive child
re-invokes `"$0"`, which from inside `scripts/` under stock bash is
`./check-gosec.sh` from the repo root — rc=127, 02-38 report §b) + add an
assertion that the mode works from a foreign CWD (02-38 report §b/§d4).
Work commit: bb85fcb (script fix + TODO tick, amended twice — see §d2/§d4).

## a) FULLY DONE

1. **Root cause confirmed live before touching anything**: stock bash,
   `cd scripts && bash ./check-gosec.sh --self-test` → rc=1 with
   `./check-gosec.sh: line 59: ./check-gosec.sh: No such file or directory`,
   first gate_run child rc=127 — byte-for-byte the 02-38 §b shape. The
   session's mvdan shell had masked it on the first (green) run of the
   battery, exactly as the report predicted.
2. **The fix**: `self_abs` resolved via `cd $(dirname "$0") && pwd` +
   basename BEFORE the top-level cd (scripts/check-gosec.sh:25); both
   recursive child invocations in gate_run switched from `"$0"` to
   `"$self_abs"` (check-gosec.sh:66,68). Recursion is now
   invocation-position-independent: relative argv0 from `scripts/`, absolute
   from a foreign CWD, and ci-local's root-relative form all converge.
3. **The foreign-CWD assertion**: new self-test branch re-invokes the mode
   from `/` through `$self_abs` (check-gosec.sh:163-169, the child
   invocation at :164), guarded by
   `TQ_GOSEC_SELFTEST_REENTRY=1` so the child skips its own foreign branch —
   one re-entry level, else infinite recursion (each self-test spawns
   another). The end-of-run summary line names the new pin.
4. **Positive verification, all stock bash, rc captured via file redirect**
   (PIPESTATUS is unusable in the session shell): scripts-relative
   invocation 27/27 ok rc=0; ci-local's root-relative form rc=0; `cd /tmp`
   absolute-path invocation rc=0.
5. **Negative control proving the assertion is live, not vacuous**: an
   in-tree copy with `$0` restored (created, run, and trashed in ONE shell
   chain — no daemon food) fails rc=127 again with the same FAIL line. The
   assertion catches the regression it exists for.
6. **Real gate re-run green on the shipped bytes**: `gosec summary:
   16 scans ok, 0 failed` rc=0 (= 1 root + 15 module targets, the
   :273 summary line); check-script-syntax.sh 0 findings (52 scripts);
   check-todo-list.sh ok.
7. **TODO row 132 closed with evidence**: `[x]` + DONE annotation carrying
   the repro-healed, foreign-CWD, negative-control and gate citations.
8. **Footer discipline applied**: the work commit's `Task-Queue-ID` footer
   is the LAST trailer line (the 03-00 window proved `%(trailers)` sees
   only the final paragraph); verified via
   `git log -1 --format=%B | git interpret-trailers --parse`.
9. **Header comment completed** (caught during close-out prep, fixed on
   sight per the owner's trivial-staleness permission): the self-test
   design block (check-gosec.sh:31-44) now names the foreign-CWD branch and
   the guard var, including its externally-settable escape-hatch nature.
   Self-test + syntax gate re-run green after the edit; folded into the
   same unpushed commit.

## b) PARTIALLY DONE

- **Full ci-local not run** (scripts-only diff): the proportionality
  precedent is 02-38 §b (same file, same class). The gates in the changed
  area ran standalone: --self-test (multiple invocation positions), the
  real gosec gate, check-script-syntax.sh, check-todo-list.sh. NOT re-run:
  check-guard-wiring.sh — owned judgment call, no script added/removed and
  the wiring table untouched, but it is a 1-second gate and precedent
  windows ran it; the asymmetry is stated, not hidden.
- **`cd /` as the foreign CWD**: works and is maximally foreign, but the
  mktemp dir `$tmp` would be strictly hermetic (zero host assumptions in
  sandboxed runners). Works today on this host and in CI's ubuntu runner;
  not changed to avoid churn — noted as a §f micro-item.

## c) NOT STARTED

- Nothing on the work item itself. Adjacent open rows deliberately
  untouched: 124 (cmd/tq gosec coverage, BLOCKED owner 03-07 §g3), 127
  (ci.yml gosec convergence, BLOCKED owner), 133 (for-each-module.sh
  consumers: six ci.yml loops catch neither empty nor failed enumeration),
  134 (short-enumeration pin), 135 (sibling-gate summary lines), the
  ci.yml sed-drift guard row, the .golangci.yml excludes-parity row, 128
  (session-ritual script — still unshipped after ≥7 documented recurrences),
  and the queue-side done-row dedup (still unbuilt, still the systemic
  §f lead of half the reports this week).

## d) TOTALLY FUCKED UP

1. **Re-hit the codified mvdan PIPESTATUS hazard on the first battery
   command**: the foreign-CWD rc check piped through `tail` and printed
   `foreign-rc=n/a` — the exact trap AGENTS.md codified TODAY after the
   04-32 window's identical first command. Recovered immediately with the
   redirect-to-file pattern, but that is two hits in one day of a rule that
   is written down. The command should have been written correctly from the
   first attempt.
2. **Daemon race LOST on the work commit**: the script fix sat uncommitted
   through the verification battery while I ran gates; the auto-commit
   daemon swept it into footerless `88d9b10 chore: auto-commit 1 changed
   file(s) (heuristic)`. Detected via `git diff --stat` showing only
   TODO_LIST.md, verified the daemon commit was unpushed with the exact
   intended file set (+21/-3, one file), then amended per the Amend
   Maneuver. Recovery was clean, but the 05-20 window minted
   "commit-first-vs-daemon" as its §e lead and I still repeated the
   ordering mistake — the work commit should have landed before the TODO
   edit, let alone the report.
3. **First negative control was muddied**: the /tmp copy reverted to `$0`
   AND changed the script's own cd target (`/tmp/gt-neg.sh/..` = `/`), so
   its failure conflated the recursion defect with a wrong-repo cd. One
   wasted round trip; the in-tree copy (same cd, only `$0` reverted) was
   the control that isolates the actual defect and should have been attempt
   one.
4. **Noticed-then-deferred the header-comment gap**: during the first
   verification pass I read the self-test design block; adding the branch
   without updating the block describing the branches was the exact
   "noticing-then-deferring" class 02-45 §d3 documented. Fixed on sight
   during THIS close-out (§a9), but the right time was the original edit.
5. **Sloppy first verification of the thing I was claiming**:
   `git interpret-trailers --parse HEAD` (a rev is not an input — needs
   stdin) errored before the correct `git log -1 --format=%B |` form.
   Trivial, but it was the footer-visibility claim itself.

## e) WHAT WE SHOULD IMPROVE

1. **Commit-first-then-gate for deliverables** (minted 05-20 §e, re-proven
   here): land the work commit BEFORE running the close-out battery; the
   daemon's sweep radius is minutes and five windows have now paid the
   amend tax.
2. **Write gate commands in redirect-to-file style from attempt one** —
   the PIPESTATUS rule is codified; re-deriving it live is a self-inflicted
   N-th recurrence.
3. **Design the negative control before running it**: name the single
   property it isolates (here: argv0 resolution) and keep everything else
   identical (in-tree, same cd).
4. **Comments describing a mode are part of that mode's deliverable**:
   a new branch without its header-block mention ships half-documented.
5. **State the escape-hatch tradeoffs where the mechanism lives**: the
   guard var is now documented at check-gosec.sh:37-39 including its
   externally-settable nature, instead of living only in report prose.
6. **Claims-after-evidence held this window**: every §a row cites a gate
   executed in this transcript, none inherited from an earlier window.

## f) WHAT WE SHOULD GET DONE NEXT

Honest list from this window and its immediate vicinity — it refuses
50-padding; the genuinely-known items number ~18, and inventing filler
would be dishonest:

1. **Queue-side done-row dedup** (systemic, unbuilt, now ≥6 IDs burned 2-3
   windows each post-DONE — this ID included: work window + this close-out
   plus 02-38/02-45/03-00 verify windows).
2. **TODO 128: session-ritual script** (≥7-report recurrence; would have
   made the CONTRIBUTING/date/ID-grep ritual mechanical).
3. **TODO 133: for-each-module.sh consumer guards** — ci.yml's six
   `for m in $(...)` loops (ci.yml:45,58,96,158,251,280) catch neither a
   failed nor an empty enumeration.
4. **TODO 134: short-enumeration pin in check-gosec.sh** (renamed
   sub-module silently drops out of the gate).
5. **TODO 135: summary lines for the sibling countable gates** (fleet
   convention or gosec-only — owner ruling §g2 of the 04-46 report).
6. **ci.yml gosec sed-drift guard** (minted by the derive window): run
   ci.yml's exact sed lines locally, assert non-empty + byte-match.
7. **.golangci.yml gosec.excludes parity assertion** (last hand-synced
   copy of the triage).
8. **TODO 127 (BLOCKED owner)**: ci.yml gosec convergence onto the script;
   if it lands, the `--self-test` should ride along into CI.
9. **TODO 124 (BLOCKED owner)**: cmd/tq gosec coverage.
10. **gitscan trailer attribution** (03-00 §g1, owner question pending):
    59% of footer commits attribute to zero because the footer rides above
    the attribution tail; fix decision + end-to-end pin rows are minted.
11. **ANSI-strip `sed` in scan() is stub-unpinned** (02-38 §b deliberate
    skip): a stub emitting ANSI would pin it; regression currently fails
    loud, not silent — triage whether the pin is worth it.
12. **Owner push of the healed lineage** (origin/master was 34 behind at
    04:2x; refs/original drop and several BLOCKED rows hang on it).
13. **AGENTS.md conventions one-liner**: citations may only be WRITTEN
    after the cited gate's output is in hand (row exists from 02-38 §d1).
14. **Dead-SHA citation gate** (row exists): docs must not cite SHAs
    unreachable from master, with a fork-record escape.
15. **Foreign-CWD branch hermeticity micro-item**: switch `cd /` to
    `cd "$tmp"` in the self-test branch (check-gosec.sh:164) — zero host
    assumptions, one-line change, needs the assertion re-verified.
16. **Amend-Maneuver scope ruling** (see §g1): if sanctioned broadly,
    write it into AGENTS.md next to the index-row variant; if not, future
    windows re-commit instead.
17. **check-guard-wiring.sh in the scripts-only battery**: decide whether
    wiring checks ride every script-touching window (1s cost) or only on
    add/remove — today it is implicit judgment, and this window guessed.
18. **Daemon-strike ledger**: the strike series (b6b2043/f1484a7 "fourth
    strike") should count this window's footerless sweep + amend as the
    fifth occurrence with its own shape (work file, not report) so the
    commit-first rule has its full evidence base.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Amend-Maneuver scope**: AGENTS.md sanctions amending the status-index
   row into an unpushed daemon commit. I extended it to a WORK-file sweep
   (the script fix riding footerless 88d9b10 → amended into bb85fcb,
   unpushed-verified, file-set-verified). Is that generalization sanctioned
   as standing policy, or should unamendable cases (pushed daemon commits)
   always get a follow-up footer commit instead?
2. **Guard-var escape hatch**: `TQ_GOSEC_SELFTEST_REENTRY=1` in the
   environment silently skips the foreign-CWD assertion (recursion guard).
   Acceptable in the spirit of `CI_CHECK=off`, or should the guard be
   non-env (e.g. a marker file under the mktemp dir) so the assertion
   cannot be env-tampered off?
3. **Verify-window spend**: this close-out re-ran every gate fresh rather
   than citing the work battery — several minutes of duplicates by design
   (the 05-34 window's "borrowed evidence eliminated" bar). Should
   close-out batteries stay full-fresh, or is re-running only the gates
   whose inputs changed (here: the script + TODO row) the sanctioned bar?
