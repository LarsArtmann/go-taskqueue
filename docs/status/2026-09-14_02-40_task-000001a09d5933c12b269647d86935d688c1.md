# Status Report — History-rewrite policy task 000001a09d5933c12b269647d86935d688c1, re-dispatch self-review close-out

**Written:** 2026-09-14 02:40 · **Work commit:** c9d36ed (pre-existing; this report adds no deliverable changes) · **Scope:** docs only

## Summary

Re-dispatch of the DONE history-rewrite-policy task (TODO row 168): the
prompt asks again for the a)–g) self-review. This is a no-op verification
close-out — the deliverable (AGENTS.md "History-rewrite policy" bullet,
commit c9d36ed, TODO row `[x]`, prior 02-38 close-out report + index row)
was re-verified fresh this turn, and the gate was re-run FRESH (see
Verification). One genuinely new data point: the stale "still descend"
clause flagged as §d2/§e2 in the 02-38 report is ALREADY FIXED — AGENTS.md
now reads "that specific fork was later healed by merge-append, but only
because someone noticed — treat every fork as permanent until proven
otherwise" (AGENTS.md:602). Prior close-out: `2026-09-14_02-38_task-000001a09d5933c12b269647d86935d688c1.md`
(canonical a)–g) and §f; this report is a delta, not a clone).

## a) FULLY DONE

1. Policy bullet in AGENTS.md (commit c9d36ed), re-verified at HEAD.
2. TODO_LIST row 168 `[x]` with DONE citation; `check-todo-list.sh` green.
3. 02-38 close-out report written AND indexed (row present in README).
4. Fork-healing refresh (02-38's own follow-up item 2) already shipped —
   no dangling "still descend" claim remains.

## b) PARTIALLY DONE

1. **Mechanical enforcement of the policy still does not exist** (02-38
   §b1/§e1): the rule is prose; a pre-rebase guard refusing rewrites of
   origin/master-reachable commits remains unimplemented.
2. **Index bloat worsens monotonically**: now 149 live rows vs 147 at the
   02-38 report, threshold 100 — the archive sweep the warning demands is
   still undone and this report adds a row (the per-report cost problem
   02-26/02-28/02-29 documented for the sibling cluster applies here too).

## c) NOT STARTED

1. Nothing in the item itself. Adjacent untouched-by-design rows: row 171
   (canonical-ID ruling, BLOCKED owner-call), archive sweep (own row).

## d) TOTALLY FUCKED UP

1. **Committed the pipeline-masking sin again, minutes into the session.**
   The first gate run was `go test ./... | tail -3`; it printed a bare
   `FAIL` with the failing package truncated out of view and no exit code
   captured. The documented class (AGENTS.md "Independently verify tool
   output"; multiple 2026-09-13 reports §d) — and I still did it first.
   Recovery was correct (pipefail + full log + re-run), and the re-run is
   green (exit=0, all packages ok), but the failing package from run 1 is
   UNIDENTIFIED — a transient/concurrent-agent flake reported tail-only
   instead of captured. Exact repeat of the 00-30 report's §d2 sin.
2. **Same-ID re-dispatch not detected before doing work**: I ran git log
   and TODO grep first (good), but did not grep docs/status for the task
   ID before planning the session — the 02-38 report and its index row
   were found only when I checked index conventions for THIS report. A
   session-start ritual grep of `docs/status | grep <task-id>` would have
   short-circuited to a pure no-op immediately.
3. CONTRIBUTING.md read late (Nth recurrence of the window's turn-1 miss;
   finally read this session — fork/PR flow, ci-local as the pre-push
   gate; nothing contradicts the work).

## e) WHAT WE SHOULD IMPROVE

1. Mechanical pre-rewrite guard (carried, 2nd occurrence): hook refusing
   rebase/amend over origin/master-reachable commits — only protects
   explicit sessions since the daemon bypasses hooks, but the daemon is
   exactly the 2026-09-10 incident's actor, so a post-hoc detector in
   ci-local (`git tag --no-merged master` + `origin/master..` ancestry
   fork check) may be the better seam.
2. Add "grep docs/status for the task ID" to the session-start ritual in
   AGENTS.md — the single cheapest fix for the re-dispatch-waste cluster
   that has burned 10+ reports this window.
3. Index bloat: the sweep is now 49 rows past threshold and every report
   (including no-ops) still mints a live row.
4. Gate hygiene: gate commands should be copy-exact from AGENTS.md
   (export GOEXPERIMENT=jsonv2 && go build && go vet && go test -race
   -count=1) — first run dropped -race and piped through tail.
5. Consolidation candidate (carried): the policy bullet + mechanics bullet
   still share the immutable-tags/fork-persist emphasis.

## f) What to get done next (delta only)

Canonical 50-item lists: 02-38 §f (this task) and 02-24 §f (window).
Not duplicated; delta items 1–5 follow from §b/§d/§e above, 6–10 from
this turn's observations:

1. Pre-rewrite guard OR post-hoc fork detector in ci-local (§e1) — owner
   seam ruling needed (hook vs gate).
2. AGENTS.md session-start ritual += `ls docs/status | grep <task-id>`
   dedup step (§e2; would have killed this dispatch's planning cost).
3. Archive sweep past the 100-row threshold (149 live, 3rd consecutive
   report flagging it).
4. No-op TQ_RESULT semantics ruling (commit_sha = original work commit
   vs no-op report commit) — 5th occurrence.
5. Same-ID re-dispatch suppression at enqueue time (queue-side) — the
   cluster (01-51/01-54/02-02/02-20/02-26/02-28/02-29 + this one) is now
   its own TODO row's worth of evidence.
6. Transient-test triage note: first-run FAIL with no captured package
   name is unrecoverable evidence loss — consider `gotestsum`-style
   failure capture or at least `tee` in documented gate invocations.
7. check-status-index warning text already carries the action (verified
   this turn: "run an archive sweep … or add a monthly digest row") —
   02-38 §f8 is DONE; strike it from the canonical list at next harvest.
8. Row 170's RESOLVED note + the AGENTS.md healing refresh close 02-38
   §f2 — also strike at harvest.
9. CONTRIBUTING.md: consider a pointer to AGENTS.md's history-rewrite
   policy (carried question g3, still unanswered).
10. When the archive sweep runs, route 02-38 §f1–§f5 into TODO_LIST rows
    (docs-health HARVEST) so they survive their reports' archiving.

## g) Questions I cannot figure out myself

1. **No-op TQ_RESULT commit_sha semantics** (5th ask): for a DONE-on-arrival
   re-dispatch, should the verdict cite the ORIGINAL work commit (c9d36ed)
   or the no-op report commit? Both conventions now exist in the index.
2. **Guard seam**: pre-rebase hook (explicit sessions only) vs ci-local
   post-hoc fork detector (covers the daemon, which bypasses hooks) —
   which does the owner want for enforcing "never rewrite pushed commits"?
3. **Does CONTRIBUTING.md get a history-rewrite pointer** for external
   contributors, or is AGENTS.md-only acceptable because the repo is
   effectively agent-only?

## Verification

- Fresh gate at HEAD (02:40): `GOEXPERIMENT=jsonv2` go build ✅, go vet ✅,
  go test -count=1 exit=0 all-ok (second run; first run had one
  uncaptured FAIL — see §d1, re-run green twice).
- `check-todo-list.sh` ✅; `check-status-index.sh` ✅ (bloat WARNING on
  149 live rows — expected, tracked as §b2).
- Work commit c9d36ed re-confirmed on master; tree clean before this
  report; not pushed (contract).
