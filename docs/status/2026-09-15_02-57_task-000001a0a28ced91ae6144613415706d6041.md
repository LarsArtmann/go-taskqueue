# Task 000001a0a28ced91ae6144613415706d6041 — webui-css CI wiring close-out (verification-only no-op)

- **Date:** 2026-09-15 02:57 CEST
- **Work item:** "Wire `check-webui-css.sh` into ci.yml (byte-canonical pin, not just the a11y minify-property test) (15-39 report b1/f6, e4)" (TODO_LIST row 78)
- **Verdict:** DONE-ON-ARRIVAL — the deliverable already shipped as commit **ece58e2** ("ci: wire check-webui-css.sh into the nix job as a byte-canonical app.css pin") plus its close-out commit 54b9ce9. This session re-verified and wrote zero code changes.

## a) FULLY DONE

- **ci.yml wiring verified at HEAD:** `.github/workflows/ci.yml:196` runs `./scripts/check-webui-css.sh` inside the `nix` job (the only job with nix available, post flake-check) — the byte-canonical app.css pin, exactly what the work item asked for.
- **ci-local parity verified:** `scripts/ci-local.sh:150` runs the same gate, and the guard-wiring meta-check (`scripts/check-guard-wiring.sh`, referenced at ci-local.sh:211) pins that the script can never go orphaned again.
- **TODO loop closed:** TODO_LIST.md:78 is `[x]` with an honest DONE note including the drift fix it caught (committed app.css was unminified, regenerated via `nix run .#webui-css`).
- **Tree state:** `git status` clean; nothing broken left behind.

## b) PARTIALLY DONE

- Nothing in this item. The verification itself was complete but shallow (see §d): I confirmed the wiring lines exist and the tree is clean, but did not re-run the actual gate (`./scripts/check-webui-css.sh` needs nix and takes a rebuild) nor trace which ci.yml step anchors line 196 into. The prior session's DONE note claims the gate was verified green twice — that claim is cited in the note but I did not independently re-execute it.

## c) NOT STARTED

- Nothing that belongs to this work item. The row is checked; there is no residue.

## d) TOTALLY FUCKED UP!

- **The re-dispatch loop continues (6th+ paid lap in this family).** This task was minted for work that was already finished (ece58e2, committed before this session). Same class as the 5th-lap topology close-out and the triple-lap lint-pin task: the queue minted a DONE item. Every one of these laps costs real money for a grep. The systemic fix is unchanged and still unbuilt: a mint-time DONE check (harvester skips items already `[x]`).
- **My own near-miss:** my first action was grepping ci.yml — correct — but I reported "no work remains" in one line without immediately checking TODO_LIST; the row could have been unchecked with the wiring live, which is exactly the half-state this loop produces. Order of checks (TODO state FIRST, deliverable second) would have compressed the session to two commands.

## e) WHAT WE SHOULD IMPROVE!

1. **Mint-time DONE check** — harvester refuses to enqueue an item whose checkbox is already `[x]`. This is the single highest-leverage fix in the whole re-dispatch family; it has been proposed in at least four prior same-class reports and still does not exist.
2. **Session-start ritual ordering** — for queue-dispatched sessions, check the target TODO row's state before verifying anything else; DONE-on-arrival detection should be the first tool call, not the third.
3. **Gate re-execution vs citation** — a verification-only session should either re-run the cited gate or explicitly downgrade its verdict to "wiring confirmed by inspection, gate execution unverified." I did the latter implicitly; make it explicit in the DONE note convention.

## f) Up to 50 things we should get done next

Honest backlog, no padding — the top 3 are the re-dispatch family's own fix; the rest are real adjacent items noticed in passing or carried from the TODO section this row came from:

1. **Implement the mint-time `[x]` check in the harvester** (skip enqueue of checked items) — kills this entire report class.
2. **Add a smoke that proves the mint-time check** (seed journal + checked item, assert no enqueue).
3. **Mine the 2026-09-15 re-dispatch reports (rows 63/64 family) for their §f backlogs and consolidate the still-open items into TODO_LIST** — at least three prior reports carried items (pin-derivation hardening, doc drift-gate smoke, jq guard, self-test, CI runtime budget) whose checkbox status is unknown.
4. Confirm `check-webui-css.sh` runtime cost in CI and whether the nix job should cache the css rebuild between runs.
5. Audit whether any OTHER check-*/smoke script runs only in ci-local and not ci.yml (the guard-wiring check requires ONE of the three surfaces — a script wired only into flake.nix but never CI could still be unenforced on master).
6. Sweep TODO_LIST rows 79+ (post-row-78) for further DONE-on-arrival residue before the next dispatch round.
7. Verify the e4/b1 source claims from the 15-39 report are fully consumed (both the ci.yml pin AND any sibling sub-items of f6).
8. Consider annotating TODO_LIST.md row 78 with the commit SHA (ece58e2) for citation parity with newer DONE notes.

(8 items is the honest list; the instruction allows up to 50, but rows 9-50 would be fabricated filler — refused, consistent with prior same-class reports.)

## g) Questions I can NOT figure out myself

1. **Should the queue auto-cancel tasks whose TODO row is already `[x]` at claim time** (worker-side refusal, refunding the mint), or is the fix strictly at mint time in the harvester? I can build either; the owner ruling decides which is sanctioned.
2. **Do the dispatch laps in this family share one root cause** (the 2026-09-11-evening harvest section was minted while a concurrent session was simultaneously working it), or are mint and worker both re-enqueueing? The journal would answer this, but I do not know whether post-mortem-ing the mint facts of rows 63/64/78 is worth a paid task — owner call.
3. **Is "verification-only no-op" an acceptable terminal verdict for a paid task**, or should such tasks be marked with a dedicated status (e.g. `wontfix`/`done-elsewhere`) so `tq stats` can surface the re-dispatch rate instead of counting them as normal completions?

---

_Point-in-time snapshot; sources: git log at HEAD (54b9ce9/ece58e2), ci.yml:196, ci-local.sh:150/211, TODO_LIST.md:78, docs/status/README.md index._
