# Status report — task `000001a0dbccd9be35b494256bf3f8ffa87d`

**Window:** 2026-09-26, ~06:05–06:25 CEST · **Tree at close:** master `99776b72`
(origin/master verified ancestor; nothing pushed) · **Scope:** one LOW reviewer
finding against the row-109 verify commit `018a6e32` — the 05-02 report battery
claimed the dead-SHA gate green via `check-doc-refs.sh`, a script that never
scans SHAs. Format: Markdown by explicit user request (status-report skill
default is styled HTML; the path/format instruction wins).

## a) FULLY DONE

1. **The finding fixed, exactly and only.** The 05-02 battery item now names
   `check-doc-refs.sh` for what it is (the doc-PATH reference gate; it scans no
   commit SHAs), records the misattribution and its commit-message twin, and
   cites the real gate explicitly with output in hand: `check-dead-sha-refs.sh`
   rc=0, "dead-sha refs ok (133 baselined, 0 new)" — re-run at HEAD AFTER the
   edit, per the claims-carry-citations rule. Structure mirrors the 04-43
   model the reviewer named: each gate listed separately with its own rc.
   Single file changed, +8/−2, zero code touched.
2. **Both gate halves verified, not assumed.** Before editing I read
   `scripts/ci-local.sh` (the two gates sit as separate steps ~10 lines apart —
   the exact 10 lines the original window never opened) and confirmed
   `check-dead-sha-refs.sh` exists and is the wired HARD gate. After editing I
   re-ran all four doc gates plus the root battery:
   `check-doc-refs` rc=0 · `check-dead-sha-refs` rc=0 · `check-todo-list`
   rc=0 · `check-status-index` rc=0 · root `go build` + `go vet` +
   `go test ./... -race` rc=0 (GOEXPERIMENT=jsonv2, GOTOOLCHAIN=auto).
3. **Commit-message site dispositioned honestly.** `018a6e32` carries the same
   misattribution ("check-doc-refs dead-SHA gate") in its message. Verified
   UNPUSHED (`git merge-base --is-ancestor` against origin/master fails;
   123 commits ahead), weighed the reword cascade (~120 descendant SHAs would
   rewrite; report cites and dead-sha-baseline rows pointing at descendants
   would fork en masse), and chose forward-correction: the corrected battery
   item records the disposition in prose. Escalated as question §g1.
4. **Daemon fold repaired per the AMEND MANEUVER, 7th instance.** The
   multi-minute `-race` battery gave the auto-commit daemon its opening: the
   edit rode into footerless `chore:` commit 16fe5199→99776b72 and my first `git
   commit` no-op'd on a clean tree. Repaired by the book: fold content
   verified (exactly my one file), origin-ancestor check, amend onto the
   unpushed HEAD → `99776b72` with exactly ONE footer
   (`000001a0dbccd9be35b494256bf3f8ffa87d`). Dead side uncited anywhere → no
   baseline row needed (06-05 §e6 design); this report cites the pair in
   arrow form `16fe5199→99776b72` so the dead-SHA gate stays green.
5. **Session hygiene held.** No push, no `.crushrc`/`crush.json`/
   `.tq-verify` edits, original task row 109 untouched, scope creep zero.

## b) PARTIALLY DONE

1. **The finding has two sites; one is closed by prose only.** The report
   battery item is genuinely fixed. The commit message is NOT repaired — it
   cannot be without the §g1 history rewrite — so the record's truth lives in
   the correcting report, one hop from the wrong text. Defensible, not ideal.
2. **The misattribution CLASS is fixed at one cite site, not at the root.**
   Nothing mechanical prevents the next battery from asserting gate identity
   by nickname. New TODO row filed (see §c1): an attribution guard or a
   convention pin. Until then the protection is "the next reviewer reads
   ci-local.sh", which is exactly what failed last time.
3. **Close-out battery on the post-report tree** (06-05 §e1 sequence): the
   first run FAILED rc=1 twice — on my own draft (§d4) — and the final state
   is green: status-index, doc-refs, dead-sha-refs, todo-list all rc=0 re-run
   on the post-report tree before the closing commit.

## c) NOT STARTED (noticed in passing; none of it this window's work)

1. **Gate-attribution guard** (NEW, filed): fail any report battery that
   names `check-doc-refs.sh` as/next to a "dead-SHA gate" claim — or pin the
   convention (battery items cite gates by wired identity: script path +
   ci-local step name) in AGENTS.md. Advisory tier.
2. **edit→commit→battery ordering** (NEW, filed): commit doc edits
   immediately; long batteries run after the commit. Codify next to the
   verify-window minimum-battery block. Directly owns the §a4 fold.
3. **The 06-05 window's six dead-SHA-gate follow-ups are UNHARVESTED** —
   verified absent from TODO_LIST by grep this window: close-out-battery
   codify, amend-appends codify, `--baseline-add` mode, baseline-file header
   sentence (exact full-line match semantics), `TQ_DEAD_SHA_VERBOSE=1` INFO
   line in ci-local, reviewer-finding output template. They live only in that
   report's §f.
4. **Master CI is still RED at `e5c386e1`** — re-checked this window
   (check-ci rc=1, "failure", predates tree). It was already red when the
   05-02 window noted it; at least three windows today have reported
   per-gate green around it. Untriaged beyond the row-54 policy ask.
5. **Owner-blocked verify-gate rows** (135 structural gofmt red, 136
   dead-letter forensic sweep, 237 checklist codify, 293 re-dispatch
   protocol, 228 review session-usage detail): all still open, untouched.
6. **ADR-0019 S1+** (companion extraction per
   `docs/planning/2026-09-26_companion-extraction-design.md`, then the
   mirror-clone gate's `MIRROR_CLONES_STRICT=1` flip): untouched.
7. **Row 229** (lint regen must refuse a typecheck-poisoned module — the
   class that poisoned the 01:41 baseline): still open.

## d) TOTALLY FUCKED UP

1. **The original sin (prior window, same task lineage): a verify window
   whose entire purpose was claim accuracy shipped an uncited-in-spirit
   claim.** The battery said "the dead-SHA gate … is still green" and cited
   rc=0 from a script that measures doc PATHS. The citation convention was
   obeyed to the letter — the rc was real — and violated in spirit: the rc
   was evidence for a different claim than the sentence made. I pattern-
   matched the script name instead of opening ci-local.sh. A green that
   protects nothing is worse than no green; it is precisely how the later
   13-DEAD-SHA red stayed invisible until an external reviewer ran the real
   gate.
2. **This window re-learned the write→commit→continue lesson at instance 7.**
   I edited the report, then ran a minutes-long `-race` battery before
   committing — the daemon folded the edit footerless in that gap and my
   commit no-op'd. The miss was predicted by the 05-07 §d list and cost an
   amend cycle. The repair was clean; the miss was avoidable by ordering
   alone.
3. **Row-number citations rot.** I verified my §f references against
   TODO_LIST before writing and found the 05-02 report's "row 286" now
   points at a blank line — line numbers drift every time rows are inserted.
   I nearly repeated the pattern in this report; everything below cites
   content + current line, and bare numbers are treated as hints, not
   anchors. (Not fixing the 05-02 report's stale numbers here — out of
   scope.)
4. **This window shipped its own battery lie for one commit.** The first
   close-out commit claimed "all rc=0 before this commit" while the battery
   was rc=1 TWICE on my own draft: dead-sha flagged my bare dead-side cite
   (the fork-record arrow must live on the SAME line as the dead SHA — mine
   was two lines away; §a4 carries the arrowed form) and todo-list flagged
   my new row's "owner rules
   advisory vs hard" phrasing as UNBLOCKED-OWNER-GATED. Worse: my chain
   guard checked that the four gate log files EXISTED, not their exit codes
   — the documented mvdan-shell pipeline-status hazard, re-hit in a new
   costume, in a commit whose whole message was a battery claim. Both
   defects were mine, both caught by reading gate output, both fixed, and
   the red pair of commits was discarded via soft reset before the final
   green one.

## e) WHAT WE SHOULD IMPROVE

1. **A gate's rc proves only that gate's claim.** Battery sentences that
   assert a gate CLASS ("the dead-SHA gate") must name the wired identity —
   script path + ci-local step — and nothing else counts as its evidence.
2. **Open the wiring before writing about it.** Ten lines of ci-local.sh
   separated the wrong gate from the right one. Any claim about what a gate
   measures gets verified against the wiring, not the name.
3. **Commit doc edits immediately; batteries run after the commit** (or in a
   background shell) — the daemon owns any uncommitted worktree for as long
   as a slow gate runs.
4. **Verify-window verdicts need a semantics pass**: for each battery item —
   does the cited gate actually measure what the sentence claims? The rc
   check alone green-lights the wrong sentence.
5. **Unpushed misattributed commit messages are a standing hazard**: the
   queue cross-references footers (safe here), but prose in messages is
   citation surface too. Forward-correction works only while the correcting
   report is findable; §g1 asks for the standing rule.

## f) NEXT (up to 50, from this window's surface only; queue gates decide when)

Legend: [NEW] filed this window · [06-05 §f.k] unharvested · [row N] =
TODO_LIST line N · [AGENTS] known issue already documented.

1. [NEW, row filed] Gate-attribution guard for docs/status batteries
   (advisory ci-local step) or the wired-identity convention pin in AGENTS.md.
2. [NEW, row filed] Codify edit→commit→battery ordering in AGENTS.md next to
   the verify-window minimum-battery block (owns the 7th AMEND instance).
3. [06-05 §f.1] Codify "close-out battery runs on the POST-REPORT tree" in
   AGENTS.md.
4. [06-05 §f.2] Codify "AMEND MANEUVER appends the dead-side SHA to
   scripts/dead-sha-baseline.txt (or arrow-forms its cites) in the same
   amended commit".
5. [06-05 §f.3] Baseline-file header sentence: exact full-line match, no
   inline comments, exact token length.
6. [06-05 §f.5] Surface `TQ_DEAD_SHA_VERBOSE=1` counts as a non-gating INFO
   line in ci-local (baseline-growth trajectory visible, never red).
7. [06-05 §f.6] Reviewer-finding template: include the failing gate's rc +
   output paste.
8. [06-05 §f.4, owner] Dead-side citation policy ruling: cite only reachable
   twins + prose vs dead-hex forensics + baseline rows.
9. Master CI red at `e5c386e1`: triage it (per-gate green has been reported
   around it by at least three windows today) — see §g3.
10. [row 135, owner] Scope the verify gofmt stage to tracked files in BOTH
    `.tq-verify` and the mint template (the structural vendor/ red).
11. [row 136] Forensic sweep of the gofmt-vendor dead-letter class.
12. [row 137] Retry-ladder signature suppression for proven-environmental
    verify deaths (stop burning attempts on the row-135 shape).
13. [row 229] lint-baseline regen must refuse a typecheck-poisoned module.
14. [row 237] Codify the verify-only re-dispatch checklist in AGENTS.md.
15. [row 293] Pin the re-dispatch work-turn protocol next to the
    session-start ritual.
16. [row 228] `tq show` review-task detail: session tokens/cost for review
    turns.
17. [row 236] History-rewrite heals re-run `check-dead-sha-refs.sh` in their
    own battery.
18. [row 252] ci.yml parity sweep for the ci-local-only gates.
19. [row 54, owner] Pool policy when master CI is red (refuse DONE vs
    disclose).
20. [row 24, owner] `internal/consumer`: wire into `tq serve` or delete the
    ghost package.
21. [row 25, owner] Decide the three near-identical fact-source interfaces.
22. ADR-0019 S1: land the companion extraction
    (docs/planning/2026-09-26_companion-extraction-design.md), then flip
    `scripts/check-mirror-clones.sh` to `MIRROR_CLONES_STRICT=1`.
23. Legacy dead-sha baseline shrink pass 2: drop LOST/RESOLVED rows once
    refs/original drops (per the baseline header's own note).
24. [AGENTS] Toolchain-policy ruling: drop the GOEXPERIMENT=jsonv2
    env story once a ruling lands (every surface carries it today).
25. [AGENTS] tq ask policy ruling: no prompt teaches `tq ask`; agents cannot
    discover it until the owner rules on ask-policy (21-04 §g).
26. [AGENTS, owner] Budget cap semantics: token-vs-count ruling pending;
    UsageToday already sums derived session tokens.
27. [AGENTS] Delete the legacy stdout `TQ_RESULT:` fallback once the live
    pool shows derived outcomes everywhere.
28. [AGENTS] Upstream crush issue: `--reasoning-effort` mistype fails with a
    misleading "does not support reasoning effort" (verified 2026-09-14).
29. [AGENTS] Crush client/server per-repo experiment before any fleet
    adoption (research doc exists; owner-gated).
30. [AGENTS] e2e under `-race` is load-marginal vs the 180s stage cap —
    own the tuning row before the next flaky kill.
31. Docs-health HARVEST of the 06-05 §f items (3–8 above) plus this report's
    §f, deduped — the standing loop this window did NOT run (scope).
32. Index bloat: 227+ live rows vs the 100-row threshold; the archive sweep
    keeps being deferred while the warning fires on every gate run.

(32 items — all real, none padded. The remaining headroom to 50 is
deliberately unspent: everything else I noticed in passing is already one of
the 32.)

## g) QUESTIONS (up to 3; cannot resolve myself)

1. **History-repair policy for factually wrong statements in UNPUSHED commit
   messages.** I forward-corrected (report prose) and did not reword
   `018a6e32`: a reword rewrites ~120 descendant SHAs, forking every report
   cite and baseline row that points at a descendant. Is forward-correction-
   only the standing rule for unpushed message defects, or is there a
   threshold (finding severity, descendant count, cite exposure) at which you
   want the scripted reword playbook run instead?
2. **Kill the confusion class by rename or by guard?** `check-doc-refs.sh`
   (paths) vs `check-dead-sha-refs.sh` (SHAs) differ by one word and the
   shorter name reads like the general "doc gate". A rename
   (`check-doc-paths.sh`) fixes the root cause but churns CONTRIBUTING,
   ci-local, and years of report prose; a guard/convention (§f1) leaves the
   names. Which do you want?
3. **Master CI red at `e5c386e1` has now survived at least three windows'
   "per-gate green" reports today.** Is that red already triaged/owned
   somewhere I did not look (I deliberately did not research it — out of
   scope), or should the next window's first task be the triage before
   anything else ships on top of it?
