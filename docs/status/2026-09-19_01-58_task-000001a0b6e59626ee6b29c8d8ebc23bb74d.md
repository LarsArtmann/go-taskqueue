# Close-out self-review — check-gosec.sh pre-existing-binary pin verification

**Task:** 000001a0b6e59626ee6b29c8d8ebc23bb74d (TODO_LIST row: "check-gosec.sh: verify a
pre-existing GOSEC_BIN is actually v2.29.0 (today the pin only governs the install path;
stale binary silently changes what green means) (02-52 gosec-gate report §b)")
**Window:** 2026-09-19, ~01:41–01:58 CEST
**Work commit:** `e913d7b` (single commit on `38a1a8a`, footer verified once, tree
`0601b97` byte-identical through the daemon-race repair)

---

## a) FULLY DONE

- **Version verification shipped** (`scripts/check-gosec.sh:21-63`): every DISCOVERED
  binary (`$GOSEC` override AND the PATH/GOPATH auto-discovery loop) now reports its
  version via `gosec -version` before any scan:
  - stamped `v2.29.0` → `ok:` line, gate proceeds;
  - stamped ≠ v2.29.0 → hard FAIL with the detected version + the exact
    `go install github.com/securego/gosec/v2/cmd/gosec@v2.29.0` fix line, exit 1,
    BEFORE any scan (fail-fast, nothing half-scanned);
  - UNSTAMPED (`Version: dev` / `Version: (devel)`) → loud WARN, gate continues.
    Rationale baked into the script comment: `go install` is sandbox-blocked for agent
    sessions on this host (verified this window: "command is not allowed for security
    reasons"), and binary provenance is the still-open 02-52 §g q2 owner ruling — a
    hard fail would have bricked ci-local's gosec step on this host with no in-session
    remedy. Only the script's OWN install path is exempt (v2.29.0 by construction).
- **All three branches behaviorally verified** (stubs under /tmp per the
  fixtures-outside-gated-trees rule):
  - FAIL: `GOSEC=/tmp/gosec-stale-stub` reporting `Version: v2.28.1` → exact FAIL +
    fix lines, exit 1, zero scans.
  - OK: stub reporting `v2.29.0` with canned `Files : 5 / Issues : 0` summaries →
    `ok: … is v2.29.0` + full pass, exit 0.
  - WARN + end-to-end: real host binary (dev-stamped, discovered via GOPATH) → WARN
    line, 16/16 scans `ok: Files=… Issues=0`, exit 0 (root scan Files=41).
- **Gates on the final content**: `check-script-syntax.sh` (bash -n 52 scripts +
  shellcheck) 0 findings; root `GOEXPERIMENT=jsonv2 go build ./... && go vet ./...`
  green; `check-todo-list.sh` green; `internal/harvest` `TestRepoTodoListParses` green.
- **TODO_LIST row closed** with a DONE note carrying the same evidence + the operational
  warning that every ci-local run carries the WARN line until the owner installs a
  stamped binary.
- **Turn-1 ritual clean**: whole-repo `git log`/`git status`/`git stash list`;
  CONTRIBUTING.md read BEFORE work (the Nth-consecutive-miss class — clean this time);
  CLAUDE.md absent; task-ID grep over docs/status/ found no prior dispatch (n=1).

## b) PARTIALLY DONE

- **On THIS host, the row's literal goal is not yet achievable**: the only gosec binary
  is the 02-52 scratch-module build reporting `Version: dev`. The CHECK now exists and
  is enforced, but the binary still cannot PROVE it is v2.29.0 — gate green remains
  "unproven provenance" (the WARN says exactly that). Clearing it is a one-line owner
  action (`go install …@v2.29.0`), agent-blocked by the sandbox.
- **Backlog appends were skipped during the work turn** (contract point 5 allowed them):
  the three pre-existing 02-52 §f3/§f4/§f5 follow-ups (self-test mode, ci.yml loop
  convergence, AGENTS.md pointer) had NO TODO_LIST rows. Appended only now, in this
  close-out, after the reflection prompt forced the re-read. Nothing lost, but the
  append belonged in the work turn.
- **Full ci-local not run** (deliberate, stated here so it is not a silent skip): this
  is a scripts-only change; the gosec step itself was run end-to-end standalone (the
  step ci-local executes), plus the syntax/todo/build/vet gates. The ~10-min lint loop,
  smokes, and nix legs were skipped as proportionality. Post-rebase, no gate was
  re-run — justified by the tree hash being byte-identical (`0601b97` before and after),
  which makes a re-run a no-op by construction.

## c) NOT STARTED

- Nothing on the work item itself; smallest-correct-change held (no `--self-test` mode,
  no ci.yml edits, no baseline files touched — same discipline as the 02-52 window).
- Noticed in passing, deliberately NOT done here: for-each-module.sh's scan-target set
  is 16 today vs the "17/17" the 02-52 report cites (module set drifted since); the
  stale citation is docs-health material, not this task.

## d) TOTALLY FUCKED UP

1. **My first multiedit shipped a semantically-broken command substitution**: I
   hand-typed `gosec_version="("$GOSEC_BIN" -version …)"` — the `$(` was lost, so the
   variable captured the LITERAL command string. `bash -n` passed AND shellcheck passed
   (it is valid shell: string concatenation), so both syntax gates are structurally
   blind to this class. Had it shipped, EVERY ci-local run would have gone red with a
   nonsense FAIL ("reports '(/home/lars/go/bin/gosec -version 2>&1 || true)'"). I caught
   it only because I re-read the block immediately after the edit ("I need to verify one
   line I typed"), within the same minute. Root cause: a ~45-line `new_string` typed in
   one batch — the exact multiedit failure family documented in the source report I
   read THIS session (02-52 §d3), recurring with a different mechanism (bad new_string
   instead of stale old_string).
2. **I gave the auto-commit daemon two windows and it used both.** I checked
   `git status` before staging (caught fold #1: the script landed in footer-less
   `e742fb4`), then issued two read-only commands (`git branch -r --contains`, `git log
   0c1477e`) BEFORE the amend — the daemon folded my TODO_LIST edit into ANOTHER
   footer-less commit (`e913d7b`) inside that window, so my amend landed on the wrong
   HEAD and the script change stayed stranded below the footer commit. AGENTS.md says
   re-check immediately before the commit; "immediately" means the SAME command, not
   two tool-calls earlier.
3. **I misread the post-amend state and nearly self-inflicted panic surgery**: seeing
   "1 file changed" in the amend stat I suspected the amend had EATEN the script change
   (it cannot — amend preserves the tree); reconstructing via reflog showed the real
   story (fold #2). Cost: one wasted inspection round. The stat confusion was mine; the
   stranded-script situation was real but the first hypothesis was wrong.
4. **The repair needed a scripted interactive rebase, not the documented amend
   maneuver** — the amend playbook only covers "daemon commit is HEAD". Squashing a
   two-commit strand required the scripted-history playbook (editor script files,
   replay-count check `Rebasing (2/2)`, tree-hash equality). It worked and verified
   clean (one footer commit `e913d7b`, both files in its stat, pickaxe attributes
   `gosec_preexisting` to the footer commit), but it is riskier machinery than the
   window discipline that would have avoided it entirely.
5. **Em dashes in source code on first write** — the global rule bans them; I wrote
   three into my added comment/echo lines (the existing script's house style primed me)
   and needed a second multiedit to strip them. Standing rule outranks local style for
   new text.
6. Minor: miscounted scan targets twice (16 vs the stub run I eyeballed as 17; the
   02-52 report's "17/17" no longer matches today's 16-target module set). Sloppy
   arithmetic, no consequence, but numbers cited from memory instead of counted output
   are exactly the uncited-claim smell.

## e) WHAT WE SHOULD IMPROVE

- **Atomic commit commands**: `git status --short && git add … && git commit …` in ONE
  shell invocation, every time. The daemon window is seconds wide and I handed it two.
  This window cost a rebase; the rule now costs zero.
- **Behavioral test of the changed code path FIRST, syntax gates second**: `bash -n` +
  shellcheck provably cannot catch valid-but-wrong shell (the forgot-`$(` class). A
  5-second stub run of the happy path beats both gates for this failure family — and
  would have caught defect d1 even without the re-read.
- **Smaller new_strings**: batch edits should anchor to the smallest block that carries
  the change; the 45-line block is where the typo hid. The one-line fix edit after it
  was trivially correct.
- **The stub-first pattern deserves promotion into the script** as `--self-test`
  (02-52 §f3, still open): my ad-hoc /tmp stubs are the design, deleted after use, so
  the proof lives only in this report — the exact "proof in a terminal scrollback"
  weakness 02-52 already flagged.
- **Version-stamp provenance is the residual risk surface**: until the owner installs a
  stamped v2.29.0, the gate's WARN is the honest ceiling. The WARN wording deliberately
  names the ruling and the consequence ("green stays unproven") so the drift is visible
  in every ci-local run instead of silently accepted.
- Cite scan-target counts as "N targets via for-each-module.sh at commit X", not bare
  numbers — the set drifts (16 vs 17 across five days).

## f) NEXT (this arc + noticed in passing; honest stop at 8)

1. OWNER: install a stamped gosec v2.29.0 into GOPATH/bin
   (`go install github.com/securego/gosec/v2/cmd/gosec@v2.29.0`) — clears the WARN,
   makes green provable; agent shells are sandbox-blocked, owner-run only.
2. OWNER RULING (carried, 02-52 §g q2): is an UNSTAMPED binary acceptable for a
   security gate? Current encoded default: WARN and continue. A fail ruling is a
   3-line edit + DONE-note update.
3. OWNER RULING (carried, 02-52 §g q1): on a stamped MISMATCH, fail-with-instructions
   (current) or force-reinstall over the user's GOPATH? The FAIL branch bakes in the
   first; reversing it is localized.
4. TODO row appended: `check-gosec.sh --self-test` pinning the three version branches +
   the Files:0 parse with canned stubs (02-52 §f3).
5. TODO row appended: converge ci.yml's hand-rolled per-module gosec loop onto
   scripts/check-gosec.sh (02-52 §f4/§g q3 — BLOCKED on the §g q3 ruling).
6. TODO row appended: point AGENTS.md's gosec baseline note at scripts/check-gosec.sh
   as the canonical one-command entry (02-52 §f5; the note predates the script).
7. cmd/tq gosec coverage stays BLOCKED (existing row, owner ruling 03-07 §g3) — restated
   only so the arc's open-flanks list is complete.
8. Docs-health candidate (not a row): 02-52's "17/17 scans" citation is stale at 16
   targets; annotate per the archiving rules if the sweep touches that report anyway.
Items 9-50 deliberately not fabricated — the honest backlog this window generated is
items 1-8 (three of them appended to TODO_LIST this window).

## g) QUESTIONS (cannot self-answer)

1. **Unstamped provenance (02-52 §g q2, now load-bearing)**: my change encodes
   WARN-and-continue for `Version: dev` binaries. Is that the ruling, or must the gate
   fail (accepting that ci-local stays red on this host until you run the one-line
   install, since agent shells cannot)?
2. **Stamped-mismatch disposition (02-52 §g q1)**: fail-with-instructions (current) or
   have the gate force-reinstall the pin? Force-reinstall mutates the user's GOPATH
   from a gate script; fail is honest but every agent hitting it is blocked on you.
3. **Repair playbook codification**: when the daemon folds work into a footer-less
   commit that is NOT HEAD (this window's two-commit strand), the amend maneuver does
   not apply — I used a scripted squash rebase over unpushed commits with tree-hash
   verification. Codify that as the documented strand-repair, or do you prefer a
   different standard (e.g. always commit immediately after every edit so the daemon
   never has a window)?
