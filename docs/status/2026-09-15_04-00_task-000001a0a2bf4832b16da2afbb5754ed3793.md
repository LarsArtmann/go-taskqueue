# Status: Review-loop e2e smoke — third dispatch, no-op re-verification

**Task:** 000001a0a2bf4832b16da2afbb5754ed3793
**Date:** 2026-09-15 04:00
**Work item:** "Review-loop e2e: `scripts/smoke/reviews.sh` (stub reviewer; review approve/request_changes paths through the real binary) (07-48 report f1/f2)"

## Summary

Third dispatch of an already-shipped work item. The deliverable landed in
commit 4d240ff (test(smoke): add review-loop e2e smoke) with ci-local wiring
(ADR-0009 review-sweeper consumer) and was closed out in e23637a, then
re-verified once more in eb15b67. This session ran the smoke fresh and
confirmed it still passes; nothing was changed.

## Verification (this dispatch)

`TQ_DB=/tmp/reviews-verify.db ./scripts/smoke/reviews.sh` — PASS:

- Build of tq via devmod shim OK (scratch DB exported, not the production journal).
- Seeded repo + stub reviewer; pool run 1 completes 2 work tasks, sweeper mints reviews.
- Pool runs 2–3: approve path completes; request_changes path mints exactly one
  autofix fix task; the fix's own review approves; loop terminates (queue drains).
- Journal assertions: 2 approvals + 1 request_changes; exactly one review per
  reviewed task (dedup); 3 agent + 3 review tasks, all completed.
- `tq doctor`: review-sweeper cursor at head (#18), queue drained, no DLQ, no parked.

## a) Fully done

- [x] `scripts/smoke/reviews.sh` — full review loop e2e through the real binary:
      approve, request_changes, `--review-autofix` fan-out, review dedup, sweep
      termination. 4d240ff.
- [x] ci-local wiring of the smoke (close-out e23637a).
- [x] TODO_LIST.md item checked (line 82).
- [x] This dispatch: fresh green run, no-op confirmed.

## b) Partially done

- None for this item. (The smoke covers the review loop; the close-out-turn
  429-resume path (`closeoutPending`) is exercised elsewhere, not here — by scope,
  not omission.)

## c) Not started

- Nothing owed by this item.

## d) Totally fucked up

- Nothing this session. One process note: the first two dispatches (e23637a,
  eb15b67) were followed by THIS third re-dispatch of the same item — either the
  queue re-minted it despite the checked box, or harvest raced the checkoff.
  Worth one glance at the harvest/dedup timing, not a defect in the deliverable.

## e) What we should improve

1. The smoke asserts journal verdict counts textually; a seeded-fixture variant
   (like release-gates.sh) would make future regressions diffable.
2. reviews.sh rebuilds tq every run (~seconds); a TQ_BIN fast path exists but
   ci-local doesn't use it — minor gate-latency win if wired.
3. Re-dispatch dedup: if a checked TODO item still re-mints, harvest may be
   racing the daemon's checkoff commit — an audit-mode check (`tq audit`) over
   this task id would confirm.

## f) Next tasks (backlog seeds — most impactful first)

1. Audit why task 000001a0a2bf… was dispatched a third time after `[x]` (harvest race vs. manual re-enqueue).
2. Fixture-seed reviews.sh output for diffable regressions.
3. Wire TQ_BIN caching into ci-local's smoke stage to cut gate latency.
4. Extend reviews.sh with a dead-reviewer path (review task itself fails → no infinite re-mint).
5. Add a postgres-backend variant of the review-sweeper cursor assertions.
6. Sweep: confirm every smoke in scripts/smoke/ is guard-wired (check-guard-wiring.sh) — re-run after the last wave of additions.
7. Document the review loop's full state machine (work → review → fix → re-review) in docs/DOMAIN_LANGUAGE.md if not already there.
8. Consider a smoke for `tq verdict` file-outranks-stdout precedence (currently pinned by unit test only).
9. Batch-harvest interaction: add a review-loop smoke variant over a batched task (one finding → one fix covering the batch).
10. Rate-limit during review turn: assert the review is requeued without burning an attempt (mirrors ratelimit-e2e but for reviews).
11. Prune-stale vs. pending review tasks: confirm a `[x]`'d item does NOT cancel its in-flight review (or pin that it does, deliberately).
12. `tq doctor` review-sweeper liveness: add a negative case (cursor behind head with pending reviewable completions → warn).
13. Postgres parity gate for `review:` dedup keys under concurrent minting (two pools, one DB).
14. Windows CI coverage for reviews.sh logic (the script is POSIX; extract assertions into a Go test that runs everywhere).
15. Cap review-mint fan-out: smoke a task completing with 50+ findings under `--review-autofix` (dedup key hash-collision corner).
16. Anchor rejection path: smoke a reviewer filing bare-position anchors → attempt fails → re-file with quoted anchor succeeds (2026-09-15 hardening).
17. Dangling-commit disposition smoke: finding whose sha is gone → re-anchor path.
18. Session-close bridge review dedup (`review:session:<id>`) — smoke the direct-enqueue side.
19. Status sweeper + review sweeper interplay smoke (both mint from the same completion watermark).
20. Evidence-archive gate: reviews.sh writes scratch under /tmp — confirm nothing leaks into docs/status/assets (ghost-archive gate would catch it; assert in ci-local).
21. Measure smoke-suite wall time end-to-end in ci-local and report drift.
22. `tq audit --journal` drift smoke over the reviews.sh journal shape.
23. Add SHA256SUMS-style integrity to smoke fixtures if any fixture grows beyond /tmp scratch.
24. Review prompt's `{{TASK_ID}}` substitution: unit-test exists; add an e2e assertion that the REVIEW run's own footer uses the review's id, not the work task's.
25. Budget gate vs. review tasks: confirm review/fix mints respect the daily budget (mintPass) in a smoke.
26. DLQ rescue of a dead review task → re-mint behavior pin (dedup is forever for dlqfix; is it for reviewfix?).
27. Prioritize sweeper `todo:`-key invisibility to batch keys — document with a test comment cross-reference.
28. Watermark rewind drill: rewind review-sweeper cursor, confirm idempotent replay (re-verify, could fold into reviews.sh).
29. CI: run reviews.sh under `-race`-built tq binary occasionally.
30. Stale-anchor lesson retro: ensure fix-prompt anchor text is quoted VERBATIM from the finding — add a parse-time test for anchor whitespace drift.
31. Refactor shared smoke helpers (seed repo, stub binary) into scripts/smoke/lib.sh to kill duplication across the 12 smokes.
32. `--review-autofix` off: request_changes verdict must NOT mint a fix — negative assertion in reviews.sh.
33. Multiple request_changes findings in one review → exactly N fix tasks (currently one-anchored path only).
34. Reviewer returns unknown verdict string → attempt fails cleanly (mechanical gate) — pin it.
35. tq show priority-provenance on a review task (should it inherit band?) — decide + pin.
36. Cross-repo review: work task in repo A reviewed by pool scanning repo B — confirm payload repo pins the reviewer.
37. Smoke the `closeoutPending` in-process resume after a simulated closeout 429.
38. Assert reviews.sh leaves zero processes behind (pool timeout wrapper audit across all smokes).
39. Docs: FEATURES.md row for reviews.sh — confirm marked DONE.
40. CHANGELOG: confirm 4d240ff's entry exists (append-only rule).
41. Facade parity run after any executor export changes — routine gate, cheap to re-run.
42. `check-dead-exports.sh` advisory re-run over executor module post review-loop work.
43. Lint baseline: regen check that the smoke additions added no new (module, linter) rows.
44. FuzzParseRepo: no interaction expected — skip, but note the audit trail.
45. Telemetry-free verification: confirm `option metrics false` is honored in review stub runs (no PostHog flush stalls in smoke logs).
46. Root `tasks.db` retired journal: periodic reminder it stays untouched by smokes (TQ_DB export discipline) — could add an assertion.
47. Improve AGENTS.md payload-contract section with a one-line pointer to reviews.sh as the executable spec of the review loop.
48. Consider archiving older 2026-09-11 status reports this item cited (07-48) per docs-health annotate mode — repoint citations first.
49. Nightly: reviews.sh + dogfood-once.sh combined soak (stub agents) to catch drift between sweepers.
50. Owner question: should review tasks carry a marker-band priority floor so fixes don't age behind backlog (AgingDaysPerPoint interaction)?

## g) Questions I cannot answer myself

1. Was the third dispatch of this task a deliberate re-run (e.g., seed-corpus-style periodic verification) or an unintended re-mint of a checked item? If the latter, should harvest treat `[x]`-with-recent-closeout as terminal for N hours?
2. Should fix tasks minted from `--review-autofix` inherit a protected priority band (marker-like) so they never age behind new backlog, or is plain priority correct?
3. For the negative cases in f) 32–34, do you want them in reviews.sh itself (longer gate) or a dedicated `reviews-negative.sh` that only ci-local runs?

## Evidence

- Fresh run this session: `TQ_DB=/tmp/reviews-verify.db ./scripts/smoke/reviews.sh` → "review-loop smoke passed"; `tq doctor` ok (review-sweeper at head #18, 6 tasks completed).
- Deliverable commits: 4d240ff (smoke), e23637a (close-out + wiring), eb15b67 (2nd verification).
