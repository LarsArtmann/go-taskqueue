# Board band grouping — close-out self-review (task 000001a0bdb1dbe8597db478315a95c6969c)

Window: 2026-09-20 09:0x–09:30. Work item: TODO_LIST row 158, "Webui board:
band-grouped columns … or band as a secondary grouping" (16-28 report f10;
plan item 44's tail). One dispatch, completed in the first window.

## a) FULLY DONE

1. **Band as secondary grouping on the board** (the lighter of the row's two
   options, chosen deliberately — status columns are pinned by
   `TestBoardViewRendersColumns`'s `data-status` assertions and ADR-0003's
   read-only board design; band-lanes-in-status-lanes keeps both invariants):
   - `internal/webui/render.go` — `BoardBandGroup` type + `bandGroups()`
     partitioning a column's cards into hot → machine → backlog in claim
     order, preserving each band's store-ordered (newest-first) sequence;
     empty bands omitted.
   - `internal/webui/fragments.templ` — `boardColumn` delegates the lane
     body to new `boardColumnGroups`: single-band lanes render the exact
     previous unlabeled list (zero visual noise where the band adds
     nothing), multi-band lanes get one `tq-label` band heading per group.
   - Pinned BOTH directions by `TestBoardBandGrouping` (partition order +
     single-band collapse, pure function) and
     `TestBoardRendersBandLabelsWhenMixed` (rendered labels appear only
     when two bands share a lane).
2. **Gates, all fresh at HEAD, rc=0 each** (citations per the claims-carry-
   citations rule): `templ generate` + `templ fmt`; root build + vet under
   `GOEXPERIMENT=jsonv2`; `gofmt -l` clean; webui suite `-count=1` ok
   10.297s and `-race` ok 12.791s; `check-todo-list.sh`, `check-doc-refs.sh`;
   `scripts/smoke/webui.sh` PASS (live HTTP/SSE + write-lockout assertions).
3. **Loop closed**: TODO row 158 ticked `[x]`; CHANGELOG `## [Unreleased]`
   → Added entry; both docs committed with the footer
   (7335041). Feature commit f8f84a9 carries the footer over ALL four
   feature files (render.go, fragments.templ, fragments_templ.go,
   board_test.go). Working tree clean at 7335041.

## b) PARTIALLY DONE

- **Lint-baseline check not run.** golangci-lint is advisory with a
  `--check` growth gate in ci-local; my diff adds one small function and
  one templ component — `boardGroups` has a nested loop that
  `nestif`/`cyclop` could notice (cyclop cap is 12, function is trivial,
  so risk is near zero), but I did not run
  `scripts/lint-baseline.sh --check` (clean-cache) to prove it. The
  advisory LSP output over the touched files showed no NEW findings class.
- **`nix build` not run.** All changed files are git-tracked before the
  build (templ outputs committed), so the flakes-see-tracked-files trap
  doesn't apply; root build/vet/tests passed. Full ci-local was not
  executed (expensive; cheap battery ran instead per the verify-window
  minimum — and this was a feature window, not a verify-only one, so the
  bar is the standard gate, which arguably includes nix at push time).

## c) NOT STARTED

- **Band-grouped columns as the primary grouping** (the row's "instead of
  status columns" option) — deliberately NOT done (see §a1); re-litigate
  only with an owner ruling that status lanes should go.
- **Status-report HTML format** — the status-report skill names styled
  HTML as canonical; the dispatch explicitly pinned the
  `.md` path, so this file is Markdown per the user override (flagged, as
  the skill requires).
- Nothing else in the row's scope remains.

## d) TOTALLY FUCKED UP (honest ledger)

1. **Two daemon races in one window, and I created the first one.** My
   lazy `sed` on the TODO row (checkpointing the edit mid-flight) left a
   transient BROKEN state (`FIXME's tail …` — the row's head eaten). The
   auto-commit daemon swept that broken line up into abb2de2 before my
   fix landed; the docs commit 7335041 then healed it. Net HEAD is
   correct, but master's history contains a commit holding a corrupted
   TODO row for ~minutes. The sed was a violation of the spirit of
   "never edit files with shell string surgery" (the ban is written for
   Go source; TODO_LIST is the same lesson). Edit tool from the start —
   or sed with a full-line pattern — costs nothing.
2. **Footer attribution race, healed by amend.** The daemon committed the
   feature files (2e9f04c) mid-window WITHOUT the footer, which would
   have orphaned render.go/fragments from the queue↔git cross-reference.
   I amended the UNPUSHED daemon commit into f8f84a9 with the footer.
   This is the right heal for unpushed commits but it is the second
   window this week doing daemon-race surgery — each heal is improvised
   judgment (amend vs follow-up) with no codified rule. See §f1.
3. **Templ LSP phantoms trusted one round too long.** After the first
   edit the LSP reported `undefined: bandGroups` — the KNOWN
   stale-cache phantom class. I correctly regenerated and built before
   believing it, but I still read three rounds of phantom diagnostics
   before annotating them; the ritual "trust the CLI, not the LSP, for
   templ" should short-circuit immediately.
4. **Turn-1 ritual executed but narrowly.** `scripts/session-start.sh`
   ran and `CONTRIBUTING.md`/prior-report grep happened; what I did NOT
   do at turn 1 was read the two sibling reports already on this board
   arc (08-43 shipped `tq tasks --band`, 09-06 shipped the provenance
   section) before designing — reading them first would have surfaced
   the f10-adjacent decisions (band label rendering conventions —
   `bandBadgeType` exists in components.go and I used `tq-label` text
   instead; defensible, but the choice deserved a look at the sibling
   row's styling first).

## e) WHAT WE SHOULD IMPROVE

- **Band label styling decision is unaudited against siblings**: the
  table rows use `display.Badge` with `bandBadgeType()` for non-backlog
  bands; my lane headings use plain `tq-label` text for ALL bands. Two
  band renderings now coexist in one view. Either is fine alone; the
  pair should be a conscious design call, not an accident of two
  windows.
- **No snapshot/SSE regression coverage**: `TestStreamSnapshotHonorsView`
  pins that the board view swaps over SSE, but nothing pins that the
  grouped rendering survives a stream re-render (it does — same
  component — but the pin is implicit).
- **`bandGroups` allocation pattern**: builds three group structs even
  for the overwhelmingly common single-band case, then the template
  calls it per render burst. Negligible at boardColumnLimit scale;
  noted so nobody "optimizes" it without cause.
- **The daemon-race playbook** keeps being re-derived under pressure
  (§d2). The amend-unpushed-daemon-commit maneuver and the "footer must
  land on the feature commit" requirement deserve a script or a written
  decision rule, not tribal memory.
- **My sed-on-TODO_LIST slip** (§d1): the "no shell string surgery" ban
  is written for Go source only. It generalizes to ALL tracked files.

## f) Up to 50 things to get done next (brainstorm — most are ROADMAP fuel, not commitments)

Board/webui arc (adjacent to this task):

1. Codify the daemon-race footer heal (amend-unpushed vs follow-up) in
   AGENTS.md — one decision rule, end the per-window improvisation.
2. Unify band rendering: pick `Badge`-with-`bandBadgeType` vs `tq-label`
   text for band identity across table rows + board lanes.
3. Extend `TestStreamSnapshotHonorsView` (or add one) to assert grouped
   board rendering over the SSE re-render path.
4. Board: show per-band counts in lane headings (true counts are only
   per-status today; a band's `+N older` escape hatch doesn't exist).
5. Board: `+N older` link currently drops the band context — consider
   `?view=board&band=` deep-links per band group.
6. Sort board cards by priority within band instead of newest-first?
   (owner ruling needed — two defensible orders).
7. Detail-page provenance section could link hot/machine tasks back to
   the board band view (cross-navigation).
8. `tq tasks --band` (08-43) vs the webui band filter: shared
   allowlist/validation lives twice (cmd/tq + handlers.go) — consider
   one `queue.ParseBand` helper.
9. Board truncation limit (`boardColumnLimit`) vs band grouping: a band
   group can look empty-ish at the truncation boundary — document or
   count honestly per band.
10. Accessibility pass: band headings as `role="heading"`/aria-label on
    group containers.

Queue/priority system tail (from the same 16-28 sweep, still open):

11. Executor usage parsing: populate `tokens` on PrioritizeResult/agent
    results (TODO row 159, f12) — one-batch scorer cost measurability.
12. Score-cache TTL/eviction pass keyed on item-text change or age
    (TODO row 160, f8).
13. `tq ask` prompt policy: no prompt teaches agents to ask — owner
    ruling pending (§g of the 21-04 report).
14. Band-parity scope ruling: should hot/machine banding apply to
    review/status/dlqfix minted tasks or agent tasks only?

Process/infra debt (noticed in passing):

15. Script the amend-unpushed-daemon-commit maneuver (footer attach +
    verification) so it's one command.
16. Generalize the no-shell-string-surgery ban in AGENTS.md from Go
    source to ALL tracked files (this window's §d1).
17. check-todo-list.sh could reject mid-sentence corrupted rows (the
    FIXME state passed the gate — it's still a syntactically valid
    checkbox line? No: the checkbox was eaten, yet the gate passed
    because the line no longer LOOKS like an item at all; a "dangling
    continuation line" lint would have caught abb2de2's broken state).
18. Templ LSP phantom suppression: a crushrc-level diagnostic filter or
    documented per-project note so agents stop re-verifying the known
    class each window.
19. `chore: auto-commit` heuristic messages carry zero attribution —
    consider a daemon-side footer convention discussion (owner-level).
20. Smoke the board band grouping under the live webui smoke (stub
    tasks at mixed priorities) so the render path is exercised in CI.

(20 honest items; padding refused — rows 21-50 would be invention.)

## g) Questions I can NOT figure out myself

1. **Band label styling ruling**: should band identity on the board
   lanes adopt the table's `Badge`+`bandBadgeType` treatment (colored,
   non-backlog only) or stay uniform `tq-label` text as shipped?
2. **Card order within a band**: newest-first (as shipped, matches
   kanban convention) or priority-desc (matches claim order)? Owner
   call — both are defensible and the difference is visible.
3. **Amend policy for unpushed footer-less daemon commits**: ratify the
   amend maneuver used here (f8f84a9), or mandate follow-up commits so
   history never forks even locally?
