# Attempt 3 full close-out — 000001a0927c6b76816c188ac334cb868ddf (SHA256SUMS manifest, dogfood-proof archive)

*2026-09-12 00:35 CEST · work commit ca627b3 · verification-only attempt: zero work-turn file changes ·
format note: explicit `.md` instruction overrides the status-report/brutal-self-review skills' HTML default, and repo close-out precedent (00-10, 00-19) is markdown.*

---

## a) FULLY DONE

1. **Point-in-time re-verification of every attempt-1 deliverable claim** (per the AGENTS.md rule that status claims are not current truth until re-run): `SHA256SUMS` is git-tracked; `sha256sum -c` reports 17/17 OK; an independent regeneration inside the archive diffs EMPTY against the committed manifest (completeness proven, not just hash validity); `scripts/check-ghost-archives.sh` green ("asset archives ok").
2. **Root verify gate green WITH the manual `GOEXPERIMENT=jsonv2` export**: `go build` + `go vet` + `go test ./... -race` all pass. This is the first attempt in this task's lineage to clear its gate — attempts 1 and 2 both died or predicted death on the pool-env json/v2 lie; attempt 3 dodged it only because the export was applied by hand.
3. **Close-the-loop contract honored without redundant edits**: `TODO_LIST.md:86` was already `[x]` with the DONE note from attempt 1; left untouched. The item is genuinely closed, not re-closed.
4. **Attempt-3 append landed in the right places**: `## Attempt 3` section appended to the 00-19 report plus its index-row update in `docs/status/README.md`, committed as ca627b3 with exactly one well-formed `Task-Queue-ID` footer (commit-msg hook passed), tree clean immediately after, no daemon race this time.
5. **Docs gates green before committing**: `check-status-index.sh`, `check-doc-refs.sh`, `check-todo-list.sh` (the one TRAILER WARNING is pre-existing advisory, different task ID).

## b) PARTIALLY DONE

1. **Verification depth was root-module + docs gates only.** The deliverable is docs-only so this is defensible, but the repo's full pre-push replicant (`ci-local.sh`: per-module loop, smokes, nix artifact check, master-CI state) never ran. "Repo not broken" was proven for the root module and doc machinery — not for the other nine modules.
2. **TQ_RESULT attribution remains improvised.** I emitted `"verified_existing": true` — a field I invented, not queue schema. Attempt 2 flagged exactly this debt (§g.2) and I extended the improvisation instead of resolving it. The queue may ignore it, mis-parse it, or attribute ca627b3 as if it were work.
3. **Report placement is a third unruled variant.** 00-19 §g.3 sketched "append `## Attempt N` to the attempt-1 report". I appended to the attempt-2 (latest) report instead, and this very report makes index row #4 for one task ID. Each attempt has now placed its report differently; the convention question grows more urgent with every requeue.
4. **Gate scope was implicit, not declared.** My final message said "root verify gate green" but never said what was NOT run (ci-local, multi-module loop, gofmt sweep). A DONE verdict that doesn't declare its own blast radius invites the over-reading this repo has scars from (five DONE verdicts shipped on a 3h-red master).

## c) NOT STARTED

1. **The owner env fix** (NixOS `Environment=GOEXPERIMENT=jsonv2` on the tq-agent-pool unit) — still THE structural blocker; not mine to do (owner-only access to SystemNix). Every future root-module pool gate stays a coin flip until it lands.
2. **CHANGELOG entry for ghost-gate manifest enforcement** — blocked on the 00-10 owner question about evidence-task CHANGELOG policy.
3. **All 41 §f items from the 00-19 report** — deliberately untouched; the contract said "do exactly this work item", and the item was already done. Scope discipline held.
4. **Queue-side retry mechanics** (gate-skip on empty-diff retries, attempt-lineage surfacing) — not fixable from inside a prompt; owner/queue-code territory.

## d) TOTALLY FUCKED UP

1. **My manifest-verification command was self-contradictory noise.** First invocation: `ls | grep -v SHA256SUMS | sort | xargs sha256sum -c SHA256SUMS` — piping the file list into xargs AND passing `-c SHA256SUMS` to the same sha256sum, which produced 17 lines of "no properly formatted checksum lines found" noise BESIDE the real 17/17 OK output. The check worked and I followed with a clean regeneration + diff, but: if any single hash had failed, that noise could have invited a skim-read "looks fine". On the exact task whose entire purpose is verification rigor, my first verification command was the pipeline-hygiene anti-pattern this repo has now burned on three separate times (global AGENTS.md cross-cutting entry; 00-19 §d twice). Sloppy. The correct form is two commands: `sha256sum -c SHA256SUMS`, then regenerate and diff.
2. **I verified the manifest but not the prose about the manifest.** Attempt 1 claimed the archive README documents the `sha256sum -c` verify command; I never re-read that README to confirm. An unverified carried claim inside a verification-only report is the precise failure mode this report format exists to prevent — prose has no gate re-running it.
3. Nothing else. No work-turn damage, no fixture leakage, no daemon races, no gate deaths. After attempt 2's report predicted "attempt 2 burns post-close-out", the honest score is: attempt 3 did not burn, and the prediction's mechanism (env lie) was confirmed as dodged-not-fixed.

## e) WHAT WE SHOULD IMPROVE

1. **Manifest verification is two commands, never one.** Check (`sha256sum -c`), then independently regenerate and diff. Candidate concrete form for the global AGENTS.md pipeline-masking entry, which currently lacks the `-c` + xargs variant.
2. **Declare gate scope in every close-out** ("root module + docs gates; ci-local NOT run") so DONE verdicts cannot be over-read as full-CI green.
3. **Verification-only attempts need a claim checklist.** Before appending, enumerate the prior report's factual claims and mark each: re-verified / unverified. My README-prose miss (#d2) was a checklist absence, not a judgment error.
4. **Stop inventing TQ_RESULT fields.** Either retry semantics get documented, or improvisations get collected somewhere an owner can triage them — silent schema drift helps nobody.
5. **Docs-only attempts deserve a written lean gate set** (status-index + doc-refs + ghost + todo-list). I ran exactly the right gates; that knowledge is currently tribal.
6. **The archive README should carry the canonical two-command verify snippet** next to its integrity note — fixes the unverified-prose gap AND hands every future verifier the correct command.

## f) Things to get done next (grounded; NEW = this session, CARRIED = still open from 00-19/00-10)

**Env & queue mechanics — the blockers:**
1. CARRIED, URGENT: owner env fix, `Environment=GOEXPERIMENT=jsonv2` on the tq-agent-pool unit (00-19 §g.1). Attempt 3 cleared its gate only via manual export.
2. CARRIED: self-contained minted verify commands (embed the export into gate commands so no shell env is trusted).
3. NEW: formalize `verified_existing` (or equivalent) in the queue's TQ_RESULT contract — my attempt-3 improvisation needs a ruling or a retraction.
4. CARRIED: verify-gate result cache — skip the gate when HEAD equals the commit a prior attempt of the same task ID already verified.
5. CARRIED: `tq retry --dry-run` (attempts left, gate command, last failure class before requeue).
6. CARRIED: per-signature gate fast-refuse for env-suspect failures (the `rateLimitGates` analogy from 00-19 §f.30).
7. CARRIED: `TQ_VERIFY_ENV` passthrough concept (pool config → gate env) as the generic fix behind #1/#2.
8. NEW: `tq doctor` check for pool runtime env (GOEXPERIMENT/toolchain presence) so env-lie gates self-diagnose instead of burning paid attempts.
9. CARRIED: dead-pool alert class for "all attempts burning on env-suspect gates" (distinct from classic dead pool).

**Report & index mechanics:**
10. CARRIED: per-attempt report placement ruling (00-19 §g.3). Attempt 3 adds a third data point: append-to-LATEST. Three attempts, three placements, zero rulings.
11. CARRIED: `check-status-index.sh` warning when N rows share one task ID without attempt markers — this task now has 3 rows + 1 append; the gate stayed silent.
12. NEW: index-row convention: record the dispatch/attempt number in every `_task-<id>` row text ("Attempt-2 …", "attempt-3 append …" pattern exists; nothing enforces it).
13. CARRIED: extend the T11 commit-msg cross-check to `_task-<id>.md` filenames vs footer IDs.
14. CARRIED: DOMAIN_LANGUAGE.md entries for "attempt", "env-suspect requeue", "verification-only close-out".
15. CARRIED: CHANGELOG entry for ghost-gate manifest enforcement (blocked on owner evidence-task policy question).

**Gate & verification hygiene (from this attempt's §d/§e):**
16. NEW: extend the global AGENTS.md pipeline-masking lesson with the `sha256sum -c` + xargs concrete form; state the two-command manifest-verify canon.
17. NEW: document the lean docs-only gate set (status-index + doc-refs + ghost + todo-list) in AGENTS.md conventions so docs-only attempts have a named gate.
18. NEW: close-out template line declaring gate scope (what ran, what did not) — makes §b.4 structural instead of memo-dependent.
19. NEW: verification-report checklist: prior report's factual claims get re-verified or explicitly marked unverified before append (my #d2 fix).
20. NEW: add the canonical two-command verify snippet to the dogfood-proof archive README's integrity note.

**Repo hygiene noticed in passing:**
21. CARRIED: TRAILER WARNING for task 000001a089c3919c… (f26 cluster class; owner ruling pending) — surfaced again during this attempt's gate run, still unowned.
22. CARRIED: TODO #135/#136 pair (closeout-report placement; TODO-append caps) — this report feeds both once more.
23. NEW: check whether `tq show` surfaces attempt lineage (attempts used / maxAttempts); if absent, a 5-minute CLI addition beats every agent re-deriving attempt counts from report prose. Unverified whether it already exists — check before filing.
24. CARRIED: evidence-task push/durability cadence (00-10 owner question, still open).

*(Stopped at 24. The 00-19 report already carries 41 grounded items; duplicating them here would be the padding lie that its own §f footnote warns about. This list adds the 8 NEW items and re-ranks the carried blockers.)*

## g) Questions I can NOT figure out myself

1. **Env fix — which lever, and when?** NixOS module `Environment=GOEXPERIMENT=jsonv2` (structural), exports baked into minted verify commands (agent-implementable), or both as defense in depth? Every root-module pool task keeps gambling paid attempts until this lands; attempt 3 cleared it by luck of a manual export, not by fix.
2. **TQ_RESULT retry semantics:** is `"verified_existing": true` (which I emitted this attempt) recognized queue schema, ignored noise, or worse — misparsed attribution? And should empty-diff retries skip the paid gate re-run entirely? This is queue-schema territory that no repo-internal doc defines and I cannot fix from inside a prompt.
3. **Per-attempt report placement — ruling, please:** append to attempt-1's report (00-19 §g.3's sketch), append to the LATEST report (what attempt 3 did), or one file per attempt (what the index actually shows)? Three attempts have now each chosen differently; `check-status-index.sh` cannot warn about unmarked multi-attempt IDs until the convention exists.

---

*Attempt 3 of 000001a0927c6b76816c188ac334cb868ddf · 2026-09-12 00:35 CEST · full close-out · work-turn diff: report append + index row (ca627b3) · this report + its index row are this file's entire footprint.*
