# Dead-export audit re-run — internal/session (task 000001a0c3658a96)

Closes TODO row 179 (03-28 §f31). Advisory audit re-run at HEAD 9aa7d91:

```
./scripts/check-dead-exports.sh
# dead-exports summary: 305 symbols ok, 68 dead
```

internal/session flagged 7 zero-importer symbols, all verified FALSE
positives of the zero-importer heuristic (structurally alive via
intra-package use behind `Sweep`/`Close`, both called from cmd/tq):

| Flagged          | Verdict | Evidence |
| ---------------- | ------- | -------- |
| RegistryEntry    | alive   | used by Sweep's LoadRegistry/RewriteRegistry fold; Sweep called at cmd/tq/session.go:114 |
| LoadRegistry     | alive   | called by Sweep (internal/session/registry.go:235) |
| RewriteRegistry  | alive   | called by Sweep (internal/session/registry.go:298) |
| ProcessOwner     | alive   | SweepInput.Owner field type (registry.go:207) |
| PgrepOwner       | alive   | Sweep's default Owner (registry.go:227) |
| SweepOutcome     | alive   | Sweep's return type |
| CloseResult      | alive   | Close's return type; Close called at cmd/tq/session.go:308 |

The row's named symbols confirmed live by substring match outside the
package: `Trailer` (cmd/tq/session.go:220,278,284,324),
`SyntheticTaskID` (cmd/tq/crush_test.go:206, cmd/tq/session_test.go:80),
`CloseInput` (cmd/tq/session.go:308), `GitScanner`/`GitLogScanner`
(cmd/tq/session.go:114,308).

**Verdict: nothing to prune in internal/session. No source delta.**

## Gates (run at HEAD 9aa7d91)

- `./scripts/check-dead-exports.sh` rc=0 (advisory)
- root build+vet with GOEXPERIMENT=jsonv2: ok
- internal/session module tests (GOWORK=off, -count=1): ok
- scripts/check-doc-refs.sh: ok
