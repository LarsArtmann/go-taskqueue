# Status report: ADR-0019 S1 replay tool — re-dispatch verification window (task 000001a0d0ac660ee058fdb4ef543a6a7ae6)

Date: 2026-09-24 02:26
Scope: THIS window only — a re-dispatch of the S1 replay-tool work item
after a concurrent window already shipped it. VERIFY → CITE → STOP: zero
source edits beyond removing one stray untracked build artifact.

## a) DONE-on-arrival, verified with citations

1. **Row already closed**: TODO_LIST.md:36 carries the item `[x]` with the
   built-verdict inline (`internal/queue/sqlitev4/replay` — verbatim
   projection copy + projection-equality gate through the engine store's
   read paths; C12 routed to the follow-up snapshot row at TODO_LIST.md:37).
2. **Package exists and gates green at HEAD e304e103**: `cd
   internal/queue/sqlitev4/replay && GOWORK=off go build ./... &&
   go vet ./... && go test ./... -count=1 -race` → `ok … 1.462s`, rc=0
   (fresh run this window).

## b) Fresh battery at HEAD (rc captured directly, no pipelines)

- root `go build ./... && go vet ./...` (GOEXPERIMENT=jsonv2
  GOTOOLCHAIN=auto): rc=0
- `scripts/check-doc-refs.sh`: rc=0
- gofmt: all tracked Go clean; the only `gofmt -l .` hits are the 44
  third-party files under gitignored `vendor/` — the documented false
  alarm, not a delta

## c) One cleanup

- Untracked stray binary `internal/queue/sqlitev4/replay/replay` (left by
  the shipping window's `go build` of the main package) — trashed, not
  committed. Tree clean at commit time.

## d) Honest misses

- Session-start.sh was run this window; CONTRIBUTING.md was read (turn-1
  ritual complete — a first for this day's window series).
- No close-out self-review turn: this is a short-form verify-only report
  per the 01-18/01-25 §e guidance.

## e) Follow-ups

None new — TODO_LIST.md:37 (enqueued-fact snapshot) and :42 (owner-run
cutover) already cover the discovered forward path.

## f) Owner questions

None added; the pending g-items from the 01-33/01-40 reports (footer
reword approval, re-dispatch suppression) stand.
