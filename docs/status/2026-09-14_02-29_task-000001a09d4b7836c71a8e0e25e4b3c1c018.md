# Status: third-dispatch close-out for row 167 (index-bloat cadence) — full report

> **DUPLICATE — ANNOTATED 2026-09-14 (docs-health)** — re-dispatch of task 000001a09d4b7836c71a8e0e25e4b3c1c018; the canonical 02-24 report carries the per-item inline markers. Every item here resolves via that canonical (its open items remain open there).

Task 000001a09d4b7836c71a8e0e25e4b3c1c018, 2026-09-14 02-28/02-29 window.

## a) FULLY DONE

1. Session-start ritual: `git log --oneline -5` + `git status --short` —
   found the task already shipped (974481a) and two prior same-ID reports
   (02-24 canonical, 02-26 delta).
2. Fresh verification, not trust: `scripts/check-status-index.sh` exit 0
   with the INDEX BLOAT WARNING firing on live data (146 rows at check
   time); work commit 974481a1f9ce41… confirmed carrying the trailer.
3. `scripts/check-todo-list.sh` exit 0; TODO row 167 confirmed `[x]`.
4. Gate implementation read and confirmed in place:
   `scripts/check-status-index.sh:61-73` (live-row count minus archived,
   threshold 100, warning text cites 08-25 f20).
5. Close-out report written at
   `docs/status/2026-09-14_02-28_task-…018.md` (a-g structure).
6. Index row added to `docs/status/README.md` (newest-first, table
   convention matched by View-then-edit).
7. Gates re-run post-edit: index gate exit 0, todo gate exit 0.
8. Committed 5bbd82d with the exact Task-Queue-ID footer; not pushed.
9. Filename timestamp corrected: initial draft used a guessed `02-4x`
   slug, replaced with the real `date` output (02-28) BEFORE commit.

## b) PARTIALLY DONE

1. The item's larger goal — "so the index stays scannable" — is only
   half-met: the check WARNS but nothing consumes the warning. 147 live
   rows and growing ~10/day; the archive sweep the warning demands has
   not been run by anyone.
2. No-op dispatch handling: I followed precedent (full report + index
   row) but flagged the cost; the actual dedup/no-op-fast-path remains
   an open owner question (3rd ask).

## c) NOT STARTED

1. The threshold archive sweep itself (out of scope for this item, but
   it is the warning's explicit next action).
2. Monthly digest row format (the alternative path in the item text)
   — no prior art exists yet; warning names both options.
3. Any upstream/queue-side fix for stale re-dispatch of `[x]` rows
   (this is the 3rd dispatch of one row; sibling rows 149/150 show the
   same pattern).

## d) TOTALLY FUCKED UP

1. Report filename written with a fabricated `02-4x` timestamp slug
   first — the convention is `<real HH-MM>`; caught before commit, but
   the write-then-rename round trip was avoidable by running `date`
   first. Systematic fix: `date` before ANY report file creation.
2. The no-op itself burns a full window + report + index row for a task
   already double-verified 4 minutes earlier (02-24/02-26). The smallest
   correct change may have been a bare no-op TQ_RESULT with no new
   files — but precedent and the index gate push toward a report, and
   I followed precedent without an explicit decision note. Three
   reports now exist for one cadence check.
3. My close-out report's §d self-criticism was thin (2 items) vs this
   report's fuller accounting — the first report under-charged itself.

## e) WHAT WE SHOULD IMPROVE

1. `date`-first ritual for report filenames (d1's systematic fix).
2. A dedup marker: when `git log` shows the task's trailer already
   closed out with a report, emit TQ_RESULT no-op WITHOUT a new file
   (needs owner ruling, g1).
3. The index-bloat warning should point at a concrete runnable sweep
   command, not a mode name, to lower the activation cost.
4. Threshold 100 was underived (02-24 §d admitted it) — could be
   measured from scroll/scannability instead of picked.
5. Contract step 5 says "mark done or remove" — for DONE-on-arrival
   tasks, the flip is already done; the contract could distinguish
   "verify-only" dispatches to skip report ceremony.
6. This window again proved the no-op-report-cost paradox: the row
   complains about index bloat; its own re-dispatches add rows.

## f) NEXT (prioritized)

1. Run the first archive sweep past threshold 100 (the warning's own
   demand; 147 live rows now) — docs-health ANNOTATE pass over the
   oldest fully-executed 2026-09-09/10 batches.
2. Owner ruling: bare-TQ_RESULT no-op path for verified-done
   re-dispatches (carried ×3 now).
3. Define the monthly digest row format (the item's option B) or
   formally rule it out in favor of sweep-only.
4. Make the INDEX BLOAT warning actionable: emit the exact sweep
   command + oldest-N candidates.
5. Add a gate self-test fixture for check-status-index.sh's cadence
   branch (positive: >threshold; negative: <threshold) — currently
   only hand-verified.
6. Consider threshold derivation: rows/day measured from git history
   (02-24 claimed ~10/day; not verified by me).
7. Sweep-rule precision: "fully-executed" vs "age > N days" as the
   archive-eligibility criterion (02-24 g3, unresolved).
8. Check whether row 166 (annotation cadence) and this row should
   share one sweep trigger instead of two cadences.
9. Capture a TODO_LIST convention note: re-dispatched DONE rows need
   a `— DONE <sha>` annotation so the session-start ritual can
   short-circuit before any file work.
10. Audit how many open rows are actually re-dispatch leftovers
    (149/150/167 pattern) — a queue-hygiene task.

## g) QUESTIONS (cannot resolve myself)

1. Should a verified no-op re-dispatch emit TQ_RESULT with NO new
   report/index row (bare no-op), or is the report mandatory for the
   close-out trail? Third ask; three data points now say the cost
   exceeds the value.
2. Warning vs fail: should INDEX BLOAT ever hard-fail (blocking
   master) at a second, higher threshold (e.g. 200), or stay
   advisory forever?
3. For the archive sweep, is eligibility "every forward-looking item
   resolved" (README's current rule) or "age > N days regardless of
   open items" (which would let the 2026-09-09/10 batches go now)?
