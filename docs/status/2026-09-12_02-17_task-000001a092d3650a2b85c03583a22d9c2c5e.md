# Task 000001a092d3650a2b85c03583a22d9c2c5e — close-out (brutal self-review; attempt 3 of 3)

**Date:** 2026-09-12 02:17 CEST. **Lineage:** work 09f6dd5 (attempt 1) →
close-outs 505f856 (01-43), 7f71737 (01-50), 8174670 (attempt-3 work
window, 02-06 report) → this report closes the lineage. **This is the
final attempt; the post-close-out gate is still predicted to dead-letter
the task on the pool-env GOEXPERIMENT lie** (attempts 2/3 burned on
verbatim `encoding/json/v2: build constraints exclude all Go files`
lastErrors — re-read untruncated this turn) — owner path: the
pre-decided 01-50 §f22 rescue-to-done with the env-lie annotation.

**TL;DR:** The deliverable is complete and now triple-verified, with
this turn adding LIVE re-checks the prior windows skipped: all three
load-bearing code anchors of the design doc intact (symbols exact, bare
line numbers already drifted ±1 within ~3h — anchor-rot confirmed and
minted), prior-art PoC scripts present, CHANGELOG/ROADMAP entries
accurate. The work window's honest failures: its own close-out report
shipped a FALSE tq-show-first claim and contradicted itself one section
later (corrected in place this turn), the daemon commit race was lost
for the second time in this lineage, and the work turn's tq-show read
was head-truncated. Full minted verify green (4/4 legs with the
export); sub-module loop not run — declared, standing gap.

## a) FULLY DONE

1. **Deliverable triple-verified across the lineage; this turn's fresh
   layer:** design doc re-read in full in the work window (211 lines;
   pipeline + five-row tradeoffs + nine open questions with ⭐ gating
   Q1); this turn re-verified the doc's load-bearing anchors against
   live code — `WithProjectExclusivity` at cmd/tq/main.go:312 and :663
   (doc says 311/662, drifted +1 each), `assertCleanTree` at
   internal/executor/agent.go:351 (exact), `preflightMaxBackoff` at
   internal/worker/worker.go:115-116 (doc's :115 now hits the comment
   line); prior-art scripts exist (`scripts/poc/worktree-isolation.sh`,
   `pr-mode.sh`); CHANGELOG entry accurate and honestly hedged
   ("Design only — nothing built"); ROADMAP pointer accurate.
2. **TODO loop closed and re-verified:** TODO_LIST.md:90 `[x]` with the
   DONE verdict naming the doc and the three-layer serial lock.
3. **Full untruncated task fact trail read this turn** (field-extract
   grep, no head): enqueued → claimed → failed attempt 1 → claimed →
   failed attempt 2 → claimed (this window). Both prior failures are
   the env lie verbatim; no work-quality failure anywhere in the
   lineage.
4. **Work window's gate:** FULL minted verify, all four legs, run with
   `export GOEXPERIMENT=jsonv2` — build, vet, `-count=1 -race -timeout
   180s` (all root packages ok; e2e 16.0s, webui 10.8s, cmd/tq 8.4s),
   gofmt clean. First window of the lineage to run all four legs
   (01-50 §e3 applied). Scope: root module exactly as minted.
5. **Close-out loop:** 02-06 report + index row committed (8174670,
   exactly one well-formed footer, file set stat-verified pure); footer
   forensics clean on 09f6dd5/505f856/7f71737; this report + index row
   - the 02-06 record correction commit close the loop for good.
6. **Contract hygiene:** CONTRIBUTING.md read in full at session start
   (three-window streak broken); AGENTS.md session ritual run both
   turns; concurrent agents respected twice (README.md grant-line fix
   left to daemon 810f1f3; the 02-12 review-stamp session's report+row
   left to daemon 72ebae0 — read, judged as forward progress, built
   around, never reverted).
7. **TQ_RESULT re-emitted verbatim** per this turn's instruction —
   which also settles 01-50 §g2ii in practice: close-out turns re-emit
   the work turn's line (work sha); the residual ruling narrows to the
   WORK turn's own sha convention (§g2).

## b) PARTIALLY DONE

1. **The work window's tq show was a truncated read** (`| head -50`):
   the decision-grade fields (attempts 2/3, verbatim lastError) were
   visible, but the fact trail below the cut (attempt-2's failure and
   the requeue) was trusted from the 01-50 report rather than read.
   No decision was wrong; the evidence read was partial. This turn's
   untruncated re-read is the remedy and the template (§e3).
2. **Sub-module gates not run anywhere in the lineage** — three
   windows, zero Go changes among them, scope declared every time; the
   gap vs "never leave the repo broken" stands until the per-module
   loop (or a promoted scripts/gate-all-modules.sh) becomes the
   default in verification windows.
3. **CONTRIBUTING's full gate list not run** (GOOS=windows build,
   smokes, ci-local): docs-only window, no push in the contract;
   ci-local is the pre-push gate. Minted verify + the two applicable
   doc gates (status-index, doc-refs) were the proportionate set.
4. **The design doc is DESIGN ONLY** — §c1 of both prior close-outs
   stands unchanged: nothing built; every §Pipeline step gated on
   owner Q1.

## c) NOT STARTED

1. **The entire worktree v1 implementation:** `--worktrees` flag,
   executor preflight branch, closeoutPending worktree-path field,
   reaper, `scripts/smoke/worktree.sh`, review payload branch/range,
   `tq show --commits` branch scan — gated on Q1, nothing scaffolded.
2. **Minting the nine open questions as TODO_LIST rows** — blocked on
   the §g3 ruling (98 unchecked rows already).

## d) TOTALLY FUCKED UP

1. **The work window's close-out shipped a false protocol claim and
   contradicted itself one section later.** 02-06 §a1 said
   "tq-show-first executed as step 0 … queue state read before the git
   ritual", while 02-06 §d1 confessed the git ritual ran in the same
   first tool batch — and §d1's own phrasing was ALSO wrong: tq show
   was not in the first batch, it ran second. The truth: git ritual
   batch one, tq show batch two. No decision depended on the order,
   but the index row repeated the false version to every future
   reader — the same protocol-timing mischaracterization class the
   01-50 §d1 ate, committed by the window that cited 01-50 §d1 as its
   lesson. Corrected in place this turn (02-06 §a1, §d1, index row);
   the correction is the exhibit.
2. **Daemon commit race lost for the second time in this lineage**
   (attempt 1 lost it, attempt 3 lost it): I ran the doc gates between
   edit and commit — the daemon's favorite window — and had to recover
   by amending the daemon's heuristic commit (7efd0eb → 8174670). The
   amend was message-only and safe ONLY because I stat-verified the
   tip's file set was purely mine first; one daemon-timing fluke
   earlier (810f1f3, 72ebae0 are single-source folds) and the amend
   would have repackaged a concurrent agent's work under my footer.
   Two failures in one: a predictable race re-run, and a recovery path
   one fluke away from folding foreign work into a footer-bearing
   commit. The cure (§e1/e2): write → commit → gate → fix-forward,
   and never amend without a file-set proof.
3. **Truncated evidence read in the work turn** (§b1): `tq show … |
   head -50` is the pipeline-masking class with the mask off — the
   exit code wasn't hidden, the evidence was. This turn's untruncated
   read supersedes it; the habit should have been step 0 of the work
   turn.

## e) WHAT WE SHOULD IMPROVE

1. **Edit → commit → gate → fix-forward whenever the daemon is live.**
   Gates between edit and commit hand the daemon the race (three
   lineage hits). The pre-commit hook already runs
   check-status-index, so committing first still gates the index;
   check-doc-refs can follow and trigger a fix-up commit if needed.
2. **Never amend a daemon commit without a `--stat` file-set proof of
   "purely mine."** My work-window rule; promote it to the written
   protocol so the next window doesn't learn it by luck.
3. **Evidence reads unfiltered:** field extraction (`tq show <id> |
   grep '"type"'` or future filter flags) over `| head -50` on
   evidence.
4. **Design docs should cite symbols + a verification stamp, not bare
   line numbers** — the worktree doc's three anchors drifted ±1 within
   ~3h in a multi-writer repo; symbol names don't rot the same way.
5. **Verification windows run the per-module loop by default** (or the
   promoted gate-all-modules script) so "repo green" claims stop
   carrying a scope asterisk.
6. **Index rows for re-dispatch lineages carry "(attempt N of M)"** —
   done ad hoc in the 02-06/02-17 rows; formalize (01-50 §e6).
7. **Close todo ledgers in-turn** — the work window ended with its
   last todos still pending at final output; cosmetic, but the ledger
   is part of the verdict.

## f) NEXT (impact-first; "carried" = 01-50 §f unless marked NEW)

1. **Owner env fix** (standing #1): `Environment=GOEXPERIMENT=jsonv2`
   on the tq-agent-pool unit — would have saved all three attempts of
   this lineage.
2. **DLQ rescue for THIS task** (imminent): rescue-to-done with the
   env-lie annotation (01-50 §f22); the post-close-out gate re-dies by
   design; no fourth verification window.
3. Self-minted verifies: harvest prefixes task verifies with the env
   export (01-50 §f2) — kills the entire genre.
4. tq doctor env-lie detector: ambient env vs repo jsonv2 need (§f3).
5. Requeue-reason classification: "env-lie (GOEXPERIMENT), attempt N"
   (§f4/§e4).
6. **NEW:** write→commit→gate→fix-forward ordering convention (§e1)
   into the docs-health pass / AGENTS.md candidate list — owner taste.
7. **NEW:** worktree doc anchor fix — replace bare line numbers with
   symbol names + verification stamp (5-minute docs edit; anchors
   drifted this turn's evidence).
8. Owner ruling batch — one session resolves four: Q1 merge policy
   (GATING), §g2 index-per-task-vs-window, §g2 TQ_RESULT sha
   semantics, §g3 minting scope.
9. Mint the nine design questions into TODO_LIST post-ruling (Q1 as
   `— BLOCKED: owner merge-policy ruling`) (§f5).
10. Post-Q1: mint the doc's rollout steps 1-4 as ordered TODO rows
    (flag → executor preflight branch → smoke → this-repo dogfood)
    (§f6).
11. Pre-Q1: tq doctor hard check for uncommitted rails
    (`.crushrc`/`.tq-verify`) (§f7).
12. Pre-Q1: `tq worktrees reap` prototype — git hygiene over
    `git worktree list --porcelain` + `tq/` prefix (§f8).
13. Pre-Q1: measure worktree-create cost across Q9 layouts (§f9).
14. Pre-Q1: PoC the committed-artifacts claim (scratch worktree of
    this repo, `go build ./...` inside, zero regen) (§f10).
15. Review payload branch/range extension spec (Q3) (§f11).
16. `tq show --commits` branch-scan gap (footer forensics pre-merge)
    (§f12).
17. closeoutPending registry: worktree-path field (§f13).
18. `scripts/smoke/worktree.sh`: scratch repo, two concurrent
    same-repo stub tasks, isolation + merge + no orphans (§f14).
19. `scripts/gate-all-modules.sh`: promote AGENTS.md's inline
    per-module loop into one command (§f19).
20. Gate-scope declaration convention into docs/status/README.md
    (§f24/§e2).
21. Add the gofmt leg + `-count=1` to AGENTS.md's documented standard
    gate (the minted verifies carry both; the docs don't) (§f17/f18).
22. Fold CONTRIBUTING's operative essence into AGENTS.md (§f20/§e5 of
    01-50).
23. Align the brutal-self-review skill default output to the repo
    contract (§f21/§e7 of 01-50).
24. **Push cadence ruling:** unpushed pile ~14 commits and this report
    adds one; every window without a push widens the loss window
    (§f15).
25. TODO_LIST drift sweep / `tq audit` before the next dogfood round —
    98 unchecked rows, several overlapping lineages (§f23).
26. Budget-guard per-repo awareness design note (Q6 coupling: N
    worktrees per repo multiplies spend) (§f26).
27. Q5 default proposal ("no gitignored seeding; fail fast") bundled
    into the Q1 ruling batch (§f27).
28. Q8 confirmation (status/review stay serial on master) into the
    same ruling batch (§f28).
29. Windows stub for worktree paths in the first implementation PR
    (§f29).
30. Daemon interplay note for worktree mode (merged commits are
    daemon-adjacent) (§f30).
31. ASCII/mermaid sequence sketch of claim→create→exec→verify→merge→
    reap in the doc (§f25, optional polish).
32. **NEW (noticed in passing, owned by the 02-12 report):** root fs
    at 99% — SQLITE_FULL dead-task risk; carried here as a pointer
    only because it operationally outranks most of this list; their
    §f owns the fix.
33. **NEW (noticed in passing):** `tq show` field-filter flags
    (`--facts`, `--payload`) so evidence reads stop being
    head-truncated — tiny tooling fix for my §d3 class.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Merge policy (the doc's ⭐ gating Q1, unchanged):** tq
   auto-merge `tq/<task-id>` on review approve (a), push + real PR
   (b), or owner merges branches by hand (c)? Blocks ALL
   implementation.
2. **Re-dispatch ledger conventions (narrowed by this turn):** index
   rows — one per TASK (amended per window) or one per WINDOW
   (current stacking)? And should a WORK turn's TQ_RESULT carry the
   work sha or the window's close-out sha? (This turn's instruction
   settled the CLOSE-OUT side: re-emit the work line verbatim; the
   work-turn convention is still unruled — 00-19/01-06/01-50 carried.)
3. **Minting scope for the nine open questions:** TODO_LIST rows now
   (Q1 BLOCKED-tagged, Q2-Q9 agent-executable) or doc-resident until
   Q1 lands? (01-50 §g3 carried; 98 unchecked rows make both answers
   defensible.)
