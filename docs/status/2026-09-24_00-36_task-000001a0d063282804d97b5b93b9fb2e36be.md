# Status Report — task 000001a0d063282804d97b5b93b9fb2e36be

**Window:** 2026-09-24 00:2x–00:36 (single-task window)
**Task:** TODO_LIST.md §go-cqrs-lite platform adoption row 33 — lint-baseline regen covering the spike modules (`sqlitev4` + `cqrsqlite` had ZERO rows in `.golangci-baseline.txt`, so `scripts/lint-baseline.sh --check` was structurally blind to them; run the gate, then regen with triage notes, clean golangci cache first per the AGENTS.md ruling).
**Work commit:** 029ccabf "Regen lint baseline to cover sqlitev4 + cqrsqlite spike modules" (3 files: `.golangci-baseline.txt`, `AGENTS.md`, `TODO_LIST.md`).

---

## a) FULLY DONE

1. **Gate confirmed the blindness before the regen** (the ordered proof the task demanded): `golangci-lint cache clean` → `./scripts/lint-baseline.sh --check` → rc=1, FAILING on new `(internal/queue/sqlitev4, linter)` and `(internal/queue/cqrsqlite, linter)` classes. Clean-cache was honored per the AGENTS.md ruling (the committed 32-varnamelen lesson: warm-cache rows are lies; CI is always clean-cache).
2. **Deliberate regen** (`scripts/lint-baseline.sh --regen`, rc=0): both spike modules admitted.
   - `internal/queue/sqlitev4`: **152 findings / 17 rows** (dominant: wrapcheck 50, paralleltest 46, varnamelen 19, errcheck 9).
   - `internal/queue/cqrsqlite`: **122 findings / 13 rows** (dominant: paralleltest 43, wrapcheck 32, varnamelen 18, errcheck 6).
   - Whole baseline: **1410 findings / 168 rows** (was 134 rows + the two missing modules).
3. **Post-regen gate green with captured evidence**: `--check` rc=0, "within baseline (1410 findings vs baseline 1410)" — the exact fail-closed-then-pass arc that proves the gate now sees the modules.
4. **Cited counts independently verified** (python tab-split over the committed baseline; matched the triage note byte-for-byte — counts were written only after the source-of-truth recount, per the claims-carry-citations rule).
5. **Triage note recorded in AGENTS.md** baseline history (eighth regen: 2026-09-23 entry — coverage fix, not drift; no new triage policy; the finding mix is the paralleltest/wrapcheck test-noise class consistent with the mirrored sqlite backend).
6. **TODO_LIST row 33 closed** (`[x]`), consistent with the section's conventions.
7. **Committed with the task footer** (commit-msg hook accepted: exactly one well-formed `Task-Queue-ID`), not pushed.

## b) PARTIALLY DONE

- **Nothing in this task's scope is partial.** The one deliberate non-goal: no shrinking or policy triage of the 274 admitted spike findings themselves — the item asked for coverage, not cleanup. The findings are now GATED (growth fails), which is the deliverable.

## c) NOT STARTED

- **Follow-on spike-module lint cleanup** (wrapcheck/paralleltest reductions in sqlitev4/cqrsqlite) — belongs to the ADR-0019 S1–S4 arc, not this item; not appended as new backlog rows because the adoption section already owns the spike lifecycle (adding per-module lint-cleanup rows there would pre-empt the S4 "DELETE the mirrored backends" decision that may obsolete them).

## d) TOTALLY FUCKED UP

- **Nothing.** Honest near-misses for the record:
  1. **First triage-note draft carried WRONG counts** (wrote 148/15 + 121/13 from reading the tail log by eye); caught it before commit by recounting from the committed file and corrected to 152/17 + 122/13. The citation-before-trust rule caught it, but the first draft should never have been written from a log tail.
  2. **Session-start ritual partially skipped**: `scripts/session-start.sh` was not run (git log/status + prior-task grep WERE run; this task ID had no prior reports). CONTRIBUTING.md re-check also not re-run this window. No consequence (no code touched), but the ritual is unconditional by design.

## e) WHAT WE SHOULD IMPROVE

1. **`lint-baseline.sh` could self-report module coverage gaps**: a `--check` mode warning (or hard fail) when a `for-each-module.sh` directory has ZERO rows in the baseline would have caught this class mechanically instead of by close-out archaeology (the 16-36 §c2 confession predicted exactly this). Smallest fix: compare the module list against `cut -f1` of the baseline and fail on a fully-absent module — new modules are then forced through a deliberate regen at birth.
2. **The blindness window is structural**: baseline coverage depends on a regen happening AFTER a new module's first lint; every new module gets a grace period where growth is invisible. The e1 gate closes it permanently.
3. **Counts-by-eye are how wrong numbers get committed** — the corrected flow (recount from the artifact, then write) is already the rule; this window is one more data point for "never transcribe numbers from scrollback".

## f) Up to 50 things we should get done next

*(Task-adjacent brainstorm — routing/impact ranking; most rows below the top handful are ROADMAP fuel, not commitments. Sources marked.)*

1. **[HIGH, e1] Add zero-rows module coverage check to `lint-baseline.sh --check`** (fail on any `for-each-module.sh` dir absent from the baseline; canary-style, mirrors check-gosec.sh's approach).
2. **[HIGH] Wire the same coverage check into ci-local + ci.yml** so the gate can't be bypassed locally.
3. **[MED] Decide the fate of the 50 wrapcheck findings in sqlitev4** (triage → exclude rule like the mirrored sqlite backend's, or rename sites) — blocked by g1.
4. **[MED] Same decision for cqrsqlite's 32 wrapcheck findings** — blocked by g1.
5. **[MED] paralleltest (46+43 findings) in the spike modules: adopt the repo-wide POLICY-DISABLED stance or add t.Parallel()** — the mirrored sqlite backend lives under the same policy; consistency argues one ruling for all three.
6. **[MED] varnamelen 19+18 in the spikes: extend the test-idiom ignores or rename**, per the 2026-09-10 precedent (renames, not suppressions).
7. **[LOW] errcheck 9+6 in the spikes** — likely the same ignored-return patterns the root policy tolerates; confirm class identity, not per-site triage.
8. **[LOW] sqlclosecheck 2+2 in the spikes** — check whether the mirrored sqlite module carries the same rows or the spikes do something new.
9. **[LOW] gci/golines/inamedparam/nestif/nilerr singles in sqlitev4** — cheapest mass-fix candidates; `golangci-lint fmt` + one pass.
10. **[LOW] tagliatelle 4+3 in the spikes** — if the snake_case machine-payload contract applies (path-scoped exclusion like cmd/tq), exclude; if not, fix.
11. **[LOW] exhaustive single in sqlitev4** — one-line enum coverage.
12. **[GATE] After ANY of 3–11: `golangci-lint cache clean` before judging the delta** (standing ruling; repeat because shrink-judging is where warm-cache lies bite).
13. **[PROCESS] Consider regenerating the baseline immediately as part of `scripts/new-module.sh` scaffolding** (module born with a baseline row = the coverage gap never opens) — complements f1 rather than replacing it.
14. **[ADR-0019 S1, in-flight elsewhere] Spike modules stay alive until the S4 mirrored-backend deletion ruling** — every cleanup decision above should be weighed against "dies at S4 anyway" (g1).
15. **[VERIFY] Next verify-window at this HEAD should cite the baseline coverage check explicitly** (grep the two module prefixes in the committed file — the 16-46 prediction is now falsifiable→falsified; record it in the next report).
16. **[DOC] FEATURES.md / CHANGELOG entry for the baseline coverage fix** — the gates changed behavior; CHANGELOG is append-only and nothing was added this window.
17. **[PROCESS] The e3 class generalizes: any disk-derived enumerator + a checked-in snapshot (baseline, gosec excludes, adoption tables) can silently miss new members — audit the other snapshots for the same gap** (gosec canaries already cover their list; the lint baseline was the hole).
18. **[LOW] Root-module `queue/sqlite` facade row shows 2 findings vs the internal module's 159** — facade aliases are gochecknoglobals-excluded by design, but confirm the 2 are understood.
19. **[STANDING] ci-local remains the pre-push gate** — this window's green `--check` is advisory-layer; a full ci-local before the next release run closes the loop (not required for a baseline+docs commit).

*(Items 20–50: deliberately not padded — the remaining candidate items all belong to the ADR-0019 S1–S4 plan and the D1/32 lineage already tracked in TODO_LIST and the 00-12/00-18/00-23 reports for task 000001a0d04c; duplicating them here would fork the backlog. The two live threads to watch: sqlitev4 D1 last_error fix (shipped bc5aece0, three verify windows) and the platform-adoption serialization.)*

## g) Questions I can NOT figure out myself

1. **g1 (blocking f3–f5, re-asked across windows): do the ADR-0019 spike modules (`sqlitev4`, `cqrsqlite`) get their lint findings triaged on their own merits, or are they throwaway spikes whose findings inherit the mirrored sqlite backend's policy wholesale?** S4 plans to delete the mirrored backends, so per-finding cleanup there may be waste — but the spikes are now baseline-GATED, so their growth is no longer free either.
2. **g2: should a NEW module's birth also mint its baseline rows (via new-module.sh, f13), or is the zero-rows gate (f1) enough?** Both prevent blindness; the first adds scaffold complexity, the second leaves a one-window grace period where the module must land with a deliberate regen. Owner taste decides.
3. **g3: policy confirmation — is `--check`-FAIL-on-blindness (f1) wanted at all, or should coverage gaps stay advisory warnings?** A hard gate forces a regen at every new module, which is a deliberate-act tax; the 2026-09-23 eighth regen was only caught because a close-out happened to predict it.

---

*Point-in-time snapshot: HEAD 029ccabf + this report's commit. Gates cited: clean-cache `lint-baseline.sh --check` rc=1 (pre), `--regen` rc=0, `--check` rc=0 "1410 vs 1410" (post), commit-msg footer hook accepted.*
