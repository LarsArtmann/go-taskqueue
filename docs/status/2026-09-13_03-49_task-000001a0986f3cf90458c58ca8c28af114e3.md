# Task 000001a0986f3cf90458c58ca8c28af114e3 — Close-Out (n=3, verification-only re-dispatch)

**Date:** 2026-09-13 03:49
**Item:** "Harden the two example servers beyond `ReadHeaderTimeout` (IdleTimeout/ReadTimeout), keeping the SSE `WriteTimeout=0` exemption documented (examples/api, examples/sse)"

## Summary

Third dispatch of the same work item. The work itself landed in d3317c5
(03:44:36) and was already closed out twice (03-44 report, then the 03-47
re-dispatch verification). This session found the TODO item checked, the
hardening committed, and nothing left to do. No new code changes.

## What this session actually did

1. Read AGENTS.md (contract step 1).
2. Verified the deliverable on disk, not from memory:
   - `examples/api/main.go:127-132`: ReadHeaderTimeout 5s, ReadTimeout 10s,
     IdleTimeout 60s, WriteTimeout=0 with the SSE-streaming exemption
     documented inline (the api server also streams SSE).
   - `examples/sse/main.go:89-94`: same four knobs, exemption comment
     ("SSE streams are long-lived by design").
   - `examples/fullcore/main.go`: only context timeouts — not an HTTP
     server, correctly out of scope.
3. Confirmed TODO_LIST.md:122 is `[x]`.
4. Ran the standard verify gate from outside the flake devShell:
   `GOEXPERIMENT=jsonv2 go build ./... && go vet ./...` — OK.
5. Made no edits, no commit (nothing to commit; footer requirement is
   satisfied by d3317c5 and this report's commit).

## a) FULLY DONE

- The work item itself: ReadTimeout + IdleTimeout on both example servers,
  SSE WriteTimeout=0 exemption documented inline. Three independent
  verifications now exist (03-44, 03-47, this report).

## b) PARTIALLY DONE

- Nothing. The item is binary and complete.

## c) NOT STARTED

- Nothing within this item's scope. No adjacent follow-ups were opened.

## d) TOTALLY FUCKED UP

- The dispatch system, not this session: the item was already done and
  double-closed, yet the queue re-dispatched it a third time. This session
  spent its window re-verifying a settled fact. Signal to the harvester:
  the item should have been removed or the dedup key kept the re-enqueue
  out. Root cause is outside this repo (harvest re-picked a checked item),
  but the effect is three paid close-outs for one 15-line diff.

## e) WHAT WE SHOULD IMPROVE

1. **Harvest vs. checked-item dedup:** TODO_LIST.md checkboxes are the
   human surface, dedup keys the machine surface; a checked-but-not-
   removed item got harvested. `internal/harvest` should skip `[x]` items
   outright (verify it does — if it does, the item survived as unchecked
   briefly between d3317c5 and the harvest, and the race window is the
   finding).
2. **Session-start ritual gap in my own run:** I recognized the item as
   done within two tool calls (good), but the close-out instruction was
   mechanical and did not tell me re-dispatch n already existed. The
   status-report index (`docs/status/README.md`) is the dedup surface —
   checking `grep <task-id> docs/status/README.md` should be part of the
   ritual for task-scoped sessions.
3. **Example-server coverage:** the timeouts are verified by inspection
   only. A tiny `TestServerTimeouts` asserting the three fields per
   example would make regressions mechanical (cheap, honest test of a
   config struct — borderline tautological, flag for the owner).

## f) Up to 50 things to get done next

Scope note: this session was verification-only; the list below is what
was visible in passing, not a researched audit.

1. Determine WHY the queue re-dispatched a checked item three times —
   fix the harvest/dedup race or the harvester's checked-item handling.
2. Add harvest-side guard: never mint a task whose TODO item is `[x]`.
3. Consider `TestServerTimeouts` for examples/api + examples/sse (see e3).
4. Sweep other example/server surfaces for the same timeout trio
   (fullcore if it ever grows an HTTP server; internal/webui is already
   hardened).
5. Verify the `docs/status/README.md` index rows exist for the 03-44 and
   03-47 close-outs (daemon-folded reports are the known hole).
6. Re-run the status-index gate (`check-status-index.sh`) after this
   report's commit as a belt-and-suspenders check.
7. If re-dispatch storms recur, consider a `tq doctor` check: tasks whose
   dedup key hashes an already-checked item.

(Stopped at 7 deliberately: inventing filler items to hit 50 violates the
smallest-correct-change contract and would be noise. The honest list of
this task's follow-ups is short because the task is done.)

## g) Questions I cannot figure out myself

1. Is the triple re-dispatch of this item expected (queue semantics:
   close-out does not retire the task id) or a harvester bug (the checked
   item was re-picked)? I could not determine this from the repo alone.
2. Should verification-only re-dispatches mint a full close-out report,
   or would a comment on the existing 03-44 report suffice? Three
   near-identical reports is drift food.
3. For e3: is a struct-field assertion test wanted for the example
   servers, or is that tautology the owner would reject?

## Verification citations

- Gate: `export GOEXPERIMENT=jsonv2; go build ./... && go vet ./...` — OK
  (this session, 03:49, root module).
- Deliverable: examples/api/main.go:127-132, examples/sse/main.go:89-94
  (read this session).
- Prior art: commit d3317c5, close-outs 03-44 and 03-47.
