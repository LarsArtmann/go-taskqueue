# Task 000001a0c662018ad2108e23674afa27c470 — close-out (2026-09-22 02:06 window)

**Verdict: DONE — verify → cite → stop.** This window was a REPEAT DISPATCH of row 92
(webui dead-export flags). The work was already fully shipped at HEAD by the 02-01
window (commit 03ffe38, close-out report
`2026-09-22_02-01_task-000001a0c662018ad2108e23674afa27c470.md`). Per the session-start
ritual ("the fastest correct response to a DONE row is verify → cite → stop") this
window made zero code changes and consumed one dispatch instead.

## a) FULLY DONE

- All three symbols substring-verified live via IN-PACKAGE call sites (the audit's
  outside-package search is blind to these, hence the recurring flag):
  - `ErrTokenRequiredOnLAN` — returned at `internal/webui/auth.go:39` (`Config.Validate`),
    `errors.Is` asserted in `auth_test.go:41,60`.
  - `HealthDashboardPath` — routed at `internal/webui/webui.go:169`, referenced at
    `internal/webui/health.go:298`, asserted in `health_test.go`.
  - `HealthSSEPath` — routed at `internal/webui/webui.go:170`, referenced at
    `internal/webui/health.go:299`, asserted in `health_test.go:262`.
- Annotated-keep class landed in `scripts/check-dead-exports.sh` (explicit allowlist
  with per-symbol citations, NOT a heuristic).
- TODO_LIST row 92 marked `[x]` with the cited DONE note (same commit 03ffe38).
- This window's fresh verification (evidence in hand, this session):
  - `./scripts/check-dead-exports.sh` re-run → `3 annotated keep` counted, none of the
    three symbols flagged; audit reports 305 ok / 53 dead / 12 structurally alive
    (advisory, unchanged classes).
  - `rg -l <task-id> docs/status/` turn-1 duplicate check → 02-01 report + index row found.

## b) PARTIALLY DONE

Nothing in this task's scope. The 53 remaining "dead" rows in the audit are the
standing advisory baseline — deliberate (internal/ layout is deliberate until the API
stabilizes), owned by the audit itself, not this row.

## c) NOT STARTED

Nothing in this task's scope. All follow-ups discovered by the 02-01 window were
already appended to TODO_LIST.md there (14 scoped items, §f of that report).

## d) TOTALLY FUCKED UP (this window's misses, brutal edition)

1. **The dispatch itself was waste.** Row 92 was closed, cited, and re-dispatched
   anyway. The turn-1 duplicate ritual exists precisely for this and worked — but the
   queue minted the task regardless. The re-dispatch forensics arc (four prior
   windows) already diagnosed this class; it keeps happening. Cost: one full agent
   window for `grep` + `tail`.
2. **Zero fresh delta produced beyond the audit re-run.** The verify-window minimum
   battery asks for cheap gates at HEAD; I ran the dead-export audit (the directly
   relevant gate) but did not re-run the full cheap battery (check-doc-refs,
   root build+vet) — defensible because I made NO changes at all (nothing to verify
   beyond read-only confirmation), but a stricter reading would have run them anyway
   in the same minute. Noted, not repeated blindly next time.

## e) WHAT WE SHOULD IMPROVE

1. **Dedup gate before mint, not at turn 1.** The harvester should skip minting when
   the TODO row is already `[x]` with a DONE note — the check is one line
   (`grep -F` on the row's text vs TODO_LIST.md at harvest time). The 14-01
   dedup-gating work covered status reports; TODO-row re-minting after closeout is
   the same class one level up.
2. **The audit's outside-package blindness is now annotated, but the fix class is
   manual**: each new verified keep is a hand-added allowlist row. Fine at 3 entries;
   revisit the in-package-use proposal (02-01 §e) if the list grows past ~10.
3. Close-out reports for repeat dispatches should stay this short — the report is the
   pointer, the 02-01 report owns the substance. (Self-note kept.)

## f) NEXT (only genuinely new items; 02-01's 14 items already live in TODO_LIST.md)

1. - [ ] Harvester: skip minting a task when its TODO row is already `[x]` with a DONE annotation (compare row text at scan time, log `harvest: skipped reason="already done"`); kills the repeat-dispatch class this window embodied
2. - [ ] Add a `docs/status/README.md` index freshness note to `check-status-index.sh` output: report when the newest report row is older than the newest `docs/status/*.md` file (catches unindexed reports in one glance)
3. - [ ] Dead-export audit: cap the annotated-keep list at 10 entries with a warning line past that, forcing a revisit of the general heuristic (guard against silent allowlist rot)
4. - [ ] Audit script: make the "53 dead" count link-carrying — emit the full dead list to a committed reference file so triage windows stop re-deriving it from memory
5. - [ ] Consider a `tq doctor` check for rows minted from already-done TODO items (queue-side view of improvement #1)
6. - [ ] Session-start ritual: also grep the LAST 3 status reports' verdicts for the task ID, not just `rg -l` — a DONE-row report is a stronger stop signal than a filename match (this window had to open the 02-01 report to learn it was a repeat)
7. - [ ] Track the advisory audit's 53-dead baseline count over time (one line per ci-local run appended to a small log) so genuine prune candidates surface by trend instead of per-window triage
8. - [ ] Re-check in a week whether row-92-style re-dispatches stop after the harvester dedup lands; if not, the mint side (manual re-enqueue?) is the suspect

## g) QUESTIONS (owner-only)

1. Should the harvester's already-done row check treat `— BLOCKED:` the same way (skip minting silently), or keep minting BLOCKED-skip tasks for visibility? Today BLOCKED items are excluded from the pool but the mint-vs-skip distinction at the DONE boundary is my inference from the dedup contract, not a ruling.
2. Is the annotated-keep allowlist in `check-dead-exports.sh` acceptable as the permanent mechanism, or do you want the general in-package-use heuristic (02-01 §e) built once the list grows?
3. For repeat dispatches of DONE rows: do you want the window to spend the verify battery (cheap gates at HEAD) even when it changed nothing, or is read-only confirmation + citation the intended minimal contract?

---
Citations: commit 03ffe38 (annotation + TODO close), 02-01 close-out report, fresh
`check-dead-exports.sh` run this window (3 annotated keep, none flagged), turn-1
`rg -l 000001a0c662018ad2108e23674afa27c470 docs/status/` → 2 hits (this report's
predecessor + index).
