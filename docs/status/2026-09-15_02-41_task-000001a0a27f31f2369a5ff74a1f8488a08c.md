# Orphaned-guard audit — work report (task 000001a0a27f31f2369a5ff74a1f8488a08c)

**Window**: 2026-09-15 ~02:25–02:41 CEST
**Work item**: TODO_LIST row "Orphaned-guard audit: every `scripts/check-*.sh` + smoke must be referenced by ci-local/ci.yml/flake or be deleted — generalize the check-webui-css lesson into a ci-local meta-check (15-39 report c7/f5, e1)"
**Commit**: 28ffe7e (docs, footer-carrier) + 6ac6e57 (daemon commit carrying the three script changes)

## a) FULLY DONE

1. **New gate `scripts/check-guard-wiring.sh`** (chmod +x): substring-matches every
   `scripts/check-*.sh` and `scripts/smoke/*.sh` against ci-local.sh, ci.yml, flake.nix;
   lists each orphan with "wire it or delete it", exit 1 on any; prints
   `guard wiring ok` on clean. Wired into ci-local.sh directly after the
   status-index step (line ~209).
2. **Orphan inventory at HEAD** (the audit itself, run before writing the gate):
   - `scripts/check-lint-baseline.sh` — verified DEAD: zero callers anywhere
     (AGENTS/lint-baseline.sh/ci-local/CI never reference it); its --check logic
     was superseded by `scripts/lint-baseline.sh --check` (round-13 T5, stricter:
     growth-gate + NEW-class detection + per-file attribution). Deleted.
   - `scripts/smoke/multi-repo.sh` (D24) — wired as a gating ci-local step.
   - `scripts/smoke/papdashboard-e2e.sh` — wired as a gating ci-local step.
   - `scripts/smoke/ratelimit-e2e.sh` — wired as a gating ci-local step.
3. **All three smokes EXECUTED before wiring** (a gate that gates a red smoke would
   be worse than an orphan): each passed end-to-end at HEAD
   (multi-repo: 6 enqueued/6 completed/0 dead, per-pool claims 3/3;
   papdashboard: alert raised + resolved; ratelimit: task parked pending,
   attempts 0, retry_in_ms > 0).
4. **Meta-check verified against reality**: before the fixes it reported exactly
   the five orphans above (including itself, pre-wiring); after the fixes it is
   green. First true finding = zero false positives on a 26-script corpus.
5. **Docs updated**: AGENTS.md smoke/gate list (4 new lines); CHANGELOG
   [Unreleased]/Added entry; TODO_LIST row 77 checked [x] with DONE note.
6. **Verification**: root `go build` + `go vet` green; `go test ./...` green;
   check-todo-list / check-guard-wiring / check-doc-refs / check-status-index
   all green at final HEAD; `bash -n` on ci-local.

## b) PARTIALLY DONE

1. **Gate coverage is ci-local-only, not ci.yml.** The item's wording ("referenced
   by ci-local/ci.yml/flake") is satisfied, but the meta-check itself runs only in
   the local pre-push gate. CI has no guard-wiring step — a guard orphaned in a
   branch that skips ci-local is still invisible on runners (same class as the
   sibling TODO row "wire check-webui-css.sh into ci.yml", left untouched by
   contract: no scope creep).
2. **Reference matching is substring-only.** `grep -qF "scripts/foo.sh"` matches a
   mere mention (comment, string literal). Good enough for the current files, but
   a commented-out invocation would count as "wired". Deliberate: simplest rule
   that catches the real failure mode; noted as future hardening.
3. **Orphan analysis depth**: I verified check-lint-baseline.sh had zero callers
   and was logically superseded, but did not diff it against lint-baseline.sh's
   --check behavior line-by-line (e.g. the old allowance-percentage semantics vs
   the new strict-greater gate). Supersession evidence (round-12 T14–T17 status
   report + ci-local calling lint-baseline.sh --check) was judged sufficient.

## c) NOT STARTED

1. Guard-wiring step in ci.yml (CI-side parity — mirrors the open webui-css row).
2. Sub-modules: the gate scopes scripts/check-_.sh + scripts/smoke/_.sh only; it
   does not audit cmd/tq's or internal/* modules' own scripts (none exist today —
   checked in passing — so nothing is currently missed).
3. No flake.nix invocation for the three newly-wired smokes (flake runs go tests,
   never smokes — consistent with every other smoke; wiring them there would
   break hermetic checkPhase assumptions and was not attempted).

## d) TOTALLY FUCKED UP

1. **Nothing functionally broken**, but one honest process miss: my first
   `git add` chain failed (`pathspec 'scripts/check-lint-baseline.sh' did not
   match`) because `trash` had already removed the file and `git rm -q` inside the
   `||` fallback had raced... on inspection the deletion turned out to be already
   staged-and-committed by the AUTO-COMMIT DAEMON (6ac6e57, "2 changed file(s)")
   before my explicit commit. I adapted (AGENTS.md documents this exact daemon
   behavior) and cited the daemon commit in the footer commit's message, but I
   had staged changes sitting in the working tree for minutes while running test
   gates — the known daemon-race exposure. The commit-per-task rule (git log
   cross-reference carrying the footer) is satisfied by 28ffe7e for the docs, but
   the SCRIPT changes themselves carry no footer — the queue↔git cross-reference
   points at the docs commit only. Acceptable (multiple commits per task are the
   norm) but noted honestly.
2. **Near-miss avoided late**: I nearly wrote the meta-check to fail on
   `check-guard-wiring.sh` itself (it is a check-*.sh with no caller until wired).
   That recursion is actually the gate working correctly, but had I made the gate
   self-exempt, an orphaned gate-future-me added later would silently pass its own
   wiring audit. The non-exempt design is correct; flagging it so nobody "fixes"
   it.

## e) WHAT WE SHOULD IMPROVE

1. **Mint-time done-check**: this task was mintable while fully doable — but the
   queue keeps re-minting DONE-on-arrival tasks (see the 01-44/01-46/01-49 trio on
   row 63). A dispatch-time `[x]`-check on the TODO row would have saved two paid
   laps there and would catch the same here if re-dispatched.
2. **The guard-wiring gate should also cover scripts/lint-_.sh, check-_ wrappers
   under subdirs (scripts/fuzz/, scripts/poc/), and scripts/lib/ eventually** —
   today's scope is exactly what the item asked; the broader "every script is
   reachable from a gate" audit is a natural v2.
3. **Substring matching → invocation-aware matching** (grep for the path in a
   line that also looks like an execution: `./scripts/…`, `run:`, `bash`).
4. **The three newly-gated smokes add ~2–4 min to ci-local** (each builds tq
   through build-tq.sh). A shared prebuilt TQ_BIN passed across smokes would cut
   most of that.
5. **Status-index bloat warning fired during verification** (105 live rows,
   threshold 100) — an archive sweep is due; this report adds row 106.

## f) Up to 50 things to get done next (this task's orbit; not researched beyond passing observations)

1. Wire `check-guard-wiring.sh` into ci.yml (parity with ci-local; closes the CI-side hole in §b1).
2. Same for the still-open sibling row: wire `check-webui-css.sh` into ci.yml (byte-canonical pin).
3. Extend guard-wiring coverage to scripts/fuzz/, scripts/poc/, scripts/lib/, lint-*.sh (§e2).
4. Invocation-aware matching instead of bare substring (§e3).
5. Add a `--self-test` mode to check-guard-wiring (fixture tree with a planted orphan asserts exit 1 — currently the gate's failure path is only proven by history, not pinned).
6. Shared tq build across ci-local smokes (TQ_BIN threading; §e4).
7. Archive sweep of docs/status (index at 105/100 rows; §e5).
8. Consider folding the meta-check output into the ci-local summary line format other gates use ("guard wiring ok" already does — keep consistent when extending).
9. Make the three new smokes honor `TQ_BIN` (papdashboard/ratelimit/multi-repo build themselves; webui-style env plumb-through) — pairs with 6.
10. Document the guard-wiring gate in SECURITY.md's gate inventory if one exists (did not check — flagged, not assumed).
11. Re-run the full ci-local gate end-to-end once before next push (I ran the touched gates + build/vet/test, not the entire 15-step gate; the three newly-serial smokes could interact via leftover ports/tmpdirs).
12. Kill-or-wire audit for the scripts/sweeps/fanout-libdive.sh family (outside today's scope by the item's wording; in scope for the v2 gate).
13. Postgres backend: confirm the three new smokes stay sqlite-only (they pass `--db`/TQ_DB scratch paths; a TQ_TEST_POSTGRES matrix run would re-verify).
14. Windows CI: the new smokes are POSIX (mktemp/trap/rm patterns, git usage) — confirm ci.yml's windows job doesn't try to run them, or guard with `//go:build`-equivalent shell gating.
15. Consider a CHANGELOG lint: verify [Unreleased] entries cite their gate/commit (claims-carry-citations rule applied to changelog).
16. Grep the repo for other `check-*`-shaped guards living OUTSIDE scripts/ (e.g. embedded in Makefile-less tooling) — none found in passing, but a one-shot sweep would pin it.
17. Add check-guard-wiring to the pre-commit hook family (install-pre-commit.sh) for instant feedback, keeping ci-local as the catcher.
18. Evaluate whether `check-dead-exports.sh`'s advisory status should upgrade now that a meta-gate exists to host stricter variants.
19. Time-box check: measure ci-local runtime before/after the three added smokes; record in AGENTS.md if the delta is material.
20. Fix the root cause of the daemon sweeping explicit-work commits: stage+commit immediately after edits, before test gates (process note; §d1).
21. `tq doctor`-style report could surface "guards present but not wired" as a check — the CLI already audits; the gate could feed it.
22. Pin the meta-check's reference set (ci-local.sh, ci.yml, flake.nix) in a variable with a comment so adding release.sh as a legitimate wiring surface is one line.
23. Investigate whether `scripts/check-lint-baseline.sh` deletion invalidates any doc citation (check-doc-refs passed, but it may not index scripts paths — verify its path coverage).
24. Add the orphaned-guard audit to the release-gates smoke fixtures (a fixture go.mod tree exercising a planted orphan = negative test at release time).
25. Sweep docs/status for prior reports that described the three smokes as "wired" — if any did, they were lying and worth a strikethrough pass (docs-health ANNOTATE).
26. Consider making journal-drift smoke exit-nonzero under a strict env (O5 advisory ruling stays, but a strict opt-in mode helps pre-release).
27. In passing: `docs/status/2026-09-15_00-53` etc. propose `check-literal-leaks.sh` — the guard-wiring gate now EXISTS to keep it honest once built; the TODO row remains open.
28. Same for the open row `golangci-lint config verify` — unaffected by this work.
29. Ensure the AGENTS.md "Smokes" block ordering matches ci-local execution order (I appended near help-text; execution order is help-text → multi-repo → papdashboard → ratelimit; doc lists them grouped — cosmetic).
30. Add a `git log --grep Task-Queue-ID` cross-check note for future agents: scripts-only changes must ride a footer commit or be cited from one (§d1 process fix).
31. Consider `shellcheck` over scripts/check-guard-wiring.sh and siblings (advisory; bash -n only today).
32. Verify the gate works from a clean clone (references use repo-relative paths; cd "$(dirname $0)/.." makes it robust — a clean-clone smoke would pin it).
33. Guard against symlinked scripts/ (grep -F follows paths, not inodes — fine today; note only).
34. Once row 2 (webui-css into ci.yml) lands, check-guard-wiring automatically gains a second reference surface — no code change needed; just confirm.
35. Record in AGENTS.md Known Issues that the meta-check intentionally does NOT exempt itself (§d2 anti-"fix").
36. Potential speed win: `grep -qF` loop → single grep -f pattern-file pass (26 scripts × 3 files today; micro).
37. Add the three smokes to `nix run .#smokes` if such an app exists (flake only has #test — decide whether smoke apps are wanted; owner call).
38. Consider a `tq audit --guards` subcommand long-term: the queue's own audit command surfaces its own gate wiring (dogfood).
39. Confirm CI's job that runs the a11y/minify css test is not confused by the new ci-local-only steps (no ci.yml changes made — zero risk this window).
40. Sweep for stale docs citing `check-lint-baseline.sh` (I grepped docs/status — one historical report mentions it; historical reports are immutable by convention, verify no LIVING doc cites it: AGENTS cites lint-baseline.sh only — confirmed green).
41. Improve TODO_LIST done-notes convention: my [x] note names files + verdicts; future rows could standardize on gate-command evidence lines.
42. Run `git log --oneline --all -- scripts/check-lint-baseline.sh` once to archive its history pointer for future archaeology (not done this window).
43. If the multi-repo smoke ever flakes on parallel ports (two pools), add port-zero discipline like papdashboard's — noted from its header comment about fixed-port history.
44. The gate's reference list omits `.github/workflows/release.yml`-class files — none exist today (only ci.yml, fuzz.yml); if a release workflow appears, extend the list.
45. fuzz.yml was NOT included in the reference surfaces (no smoke belongs there) — record that decision so nobody adds it "for completeness".
46. Consider upstreaming the orphaned-guard pattern to the SystemNix deployment docs (pool unit runs tq against this repo; same rot class).
47. Add an AGENTS.md line to the Session-start ritual: run check-guard-wiring early (cheap, ~1s) to catch someone else's half-wired script mid-flight.
48. Explore making the gate emit machine-readable output (one JSON line) for future dashboard integration.
49. Propose in TODO_LIST (not done — scope): "guard-wiring v2: every script under scripts/ reachable from a gate or documented as manual-only".
50. Celebrate restraint: five tempting drive-bys (literal-leaks gate, ci.yml webui-css, index archive sweep, fuzz coverage, TQ_BIN threading) were all left as rows — smallest-correct-change held.

## g) Questions I cannot resolve myself

1. **Gate placement policy**: should guard-wiring (and eventually webui-css) run in ci.yml as a hard step, or does the repo's policy consider ci-local the sole meta-gate with CI trusting it indirectly (ci-local's own step list is duplicated in ci.yml today — what is the intended long-term split)?
2. **Deletion authority**: the item said "or be deleted" — I deleted only the provably-dead duplicate (check-lint-baseline.sh). If a future orphan is a _valuable but unwired_ script, is wire-first the standing policy, or does the owner prefer deletion to keep the corpus lean?
3. **ci-local runtime budget**: the three added smokes cost minutes per pre-push run. Is there a runtime ceiling for ci-local where steps should demote to a nightly/`--full` mode, or is pre-push thoroughness an absolute?

---

Verification at report time: `check-guard-wiring` green, `check-todo-list` green, work commit 28ffe7e on master, not pushed.
