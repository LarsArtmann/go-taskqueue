# Session-Snapshot Ghost Task — 3rd-Lap Verification Close-Out

- **Task ID:** 000001a0a263bab2c4d033e10753b02fd101
- **Date:** 2026-09-15 02:18
- **Lap:** 3rd paid dispatch of the same work item (after e6997d0/6c216b6 and the 02-13/02-16 reports)
- **Verdict:** DONE-ON-ARRIVAL — verification-only session, zero changes

## 0. Summary

The work item ("Report the session-snapshot ghost upstream: work-commit hashes
pre-appearing in the session-start git snapshot (2574569 incident, 08:32 report
d2/g2)") was fully completed by earlier dispatches. This lap re-verified every
claim at HEAD and spent zero tokens on new code or docs beyond this report.

## a) FULLY DONE

- **Work item shipped (prior laps):** Root cause forensics + upstream filing.
  The "pre-appearing commit" (2574569, real, authored 08:32:18) had a reachable
  twin 2574569 (09:15:43) that orphaned the original during the 2026-09-10
  reword-incident rebase. Crush re-captures the system-prompt git snapshot on
  every app init INCLUDING session resume, while `coder.md.tpl:378` labels it
  "at conversation start" — a state-capture/labeling artifact, not a
  concurrent-agent duplicate.
- **Upstream issue live and OPEN (verified this lap via `gh`):**
  charmbracelet/crush#3842 — "Git status env snapshot is re-captured on
  session resume but labeled 'at conversation start'".
- **TODO_LIST.md row 65 checked `[x]`** with the full answer (verified this lap).
- **Prior close-outs exist:** `docs/status/2026-09-15_02-13_task-…md` and
  `2026-09-15_02-16_task-…md` (2nd-lap verification-only, commit 852839c).
- **This lap:** fresh verification at HEAD 852839c — `git log`, `git status`
  (clean), TODO row re-read, upstream issue re-checked. No repo state changed.

## b) PARTIALLY DONE

- Nothing from THIS task. The upstream issue is filed but unresolved upstream
  (Crush-side fix pending; out of this repo's hands).

## c) NOT STARTED

- Upstream fix for #3842 (Crush maintainer side).
- A mint-time done-check that would prevent this 3rd paid re-dispatch (the
  systemic fix named in every prior same-ID report; still not implemented).

## d) TOTALLY FUCKED UP

- **The re-dispatch loop itself (6th+ occurrence repo-wide):** a task already
  `[x]` in TODO_LIST.md was minted and dispatched again, paying a full agent
  window to re-read four files. The 02-16 report named mint-time done-check as
  the fix; this lap is evidence it did not land.
- **My own near-miss this lap:** I checked `git log`/TODO first (correct
  ordering, better than the 01-18 lap's sequencing nit) but I initially prepared
  to answer as if it were fresh work; the DONE-on-arrival detection came from
  the TODO grep, not from consulting the existing same-ID reports early. A
  session-start ritual should include grepping `docs/status/README.md` for the
  task ID BEFORE any other check.

## e) WHAT WE SHOULD IMPROVE

1. **Mint-time done-check:** `tq harvest` should refuse to mint/claim an item
   already `[x]` in TODO_LIST.md (snapshot at mint, not dispatch).
2. **Same-ID report lookup:** dispatch context should carry links to prior
   same-ID reports so a lap-3 agent confirms no-op in one step.
3. **Session-start ritual extension (repo-level):** add "grep the status index
   for your task ID" to the AGENTS.md session-start ritual.
4. **Upstream follow-up:** watch #3842; when fixed, the 08:32-report d2/g2
   explanation should be cross-linked as confirmation evidence.

## f) Up to 50 things we should get done next

Honest list — most are harvested/adjacent items seen in passing, not invented
padding. Top of list is impact-ordered; the tail is deliberately short rather
than padded to 50.

1. Implement the mint-time `[x]` done-check in harvest (kills the re-dispatch
   class — this is lap 3 on this ID alone).
2. Enrich dispatch context with prior same-ID report links.
3. Add "check status index for your task ID" to the session-start ritual in
   AGENTS.md.
4. Watch/comment charmbracelet/crush#3842 when upstream moves; link back here.
5. (Adjacent, in passing) The two prior same-ID reports + this one show the
   DONE-on-arrival pattern now spans 4 task IDs — a single sweep to confirm no
   OTHER checked rows have live tasks would close the whole class.

_(No further items: a 50-row list for a verification-only no-op lap would be
fabrication, matching the precedent set by the 02-16 report.)_

## g) Questions I can NOT figure out myself

1. **Re-dispatch sanction:** is the queue operator intentionally re-dispatching
   done items as a verification probe, or is the mint-time `[x]` check simply
   still unbuilt? (Determines whether laps are waste or policy.)
2. **Evidence policy for dangling commits:** should the 2574569/2574569 rebase
   orphan pair get a permanent note in docs (they cite no SHAs per CHANGELOG
   policy) or stay report-only?
3. **Upstream follow-up ownership:** who watches #3842 for upstream movement —
   should a pool task own it, or is it owner-manual?
