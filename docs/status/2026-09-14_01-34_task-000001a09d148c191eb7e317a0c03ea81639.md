# Task 000001a09d148c191eb7e317a0c03ea81639 — Archive-sweep verification re-dispatch close-out

**Date:** 2026-09-14 01:34 CEST
**Work item (TODO row 139):** Docs-health continuation: annotate + archive the six 2026-09-07 reports (18-41, 19-33, 20-32, 21-19, 21-44, 23-08) — every-item-resolved verification per the archive convention before each `git mv` (06:25 report f6)
**Disposition:** DONE-ON-ARRIVAL (n=2 same-ID dispatch). The deliverable shipped in the prior window (footer commit `2562792`); this turn was independent re-verification only. Zero deliverable changes.

## Verification performed this turn (fresh, cited)

- All six reports present at `docs/status/archived/2026-09-07_*.md` (`ls` against the glob).
- Index rows 187-192 in `docs/status/README.md` repointed to `archived/...` paths; archive counter reads 46.
- Inline annotation spot-check (`archived/2026-09-07_18-41`): archive-note header with per-section a)-g) resolved-or-routed verdicts citing commits (`132554e`, `94bff0f`, round-5 plan path) + 16 strikethrough markers in the body. Appendix-only failure mode NOT present.
- TODO row 139 checked with the DONE 2026-09-14 evidence summary (commit `2562792`).
- Root gate re-run this turn: `GOEXPERIMENT=jsonv2; go build ./... && go vet ./...` — green.
- Provenance finding re-confirmed: the six `git mv` + annotation edits landed via daemon commit `b23ff82` ("auto-commit 14 changed file(s) (heuristic)"); the footer commit `2562792` carries ONLY `TODO_LIST.md` (1 file, +1/-1). Same daemon attribution race the 01-30 same-ID report recorded.

## a) FULLY DONE

- The six 2026-09-07 reports annotated inline and archived; citations repointed before the move; index + counter updated.
- Every-item-resolved convention honored (spot-verified against 18-41; sibling files carry the same note shape).
- TODO row 139 closed with evidence.

## b) PARTIALLY DONE

- Nothing in the deliverable. This turn's verification covered 18-41's strikethroughs by direct read; the other five were verified by file presence + note-header presence only, not full per-item reads (judgment: prior window verified them; re-reading all six would be duplicate spend).

## c) NOT STARTED

- No part of the work item.

## d) TOTALLY FUCKED UP (this session's honest defects)

1. **Turn-1 verification was shallow**: my first dispatch closed the task from `ls` + a TODO grep alone, trusting the `[x]` mark — the checks-trusted-not-run pattern this window keeps flagging. The re-dispatch forced the deeper verification above.
2. **CONTRIBUTING.md unread at turn 1** (Nth recurrence across this window's sessions).
3. **Prior-window daemon race left uncited at first**: the footer commit's file set (TODO only) vs its message (six archives) only got reconciled this turn — the amend-vs-cite ruling for daemon-raced commits still doesn't exist as a rule.

## e) WHAT WE SHOULD IMPROVE

- Dispatch dedup: the queue re-sent a task that was already DONE with a closed TODO row — at least the 5th same-ID cluster this window.
- Footer-commit attribution: when the daemon sweeps the real edits, the human-readable commit lies about what it carried; the index-row precedent (b23ff82/019b18f) is now twice, approaching a convention needing ratification.
- Close-out dispatch should carry the prior same-ID report path in the prompt (the 01-30 report existed; nothing told me).

## f) Up to 50 things we should get done next

Priorities 1-8 are this task's own tail; the rest carry the window's standing backlog (canonical lists live in the 00-37 / 00-52 / 00-57 / 01-30 same-window reports — dedupe at harvest).

1. Ratify the daemon-attribution citation convention (index-row precedent b23ff82/019b18f) as a rule in AGENTS.md.
2. Add a dispatch-side dedup guard: skip re-dispatch when the TODO row is already `[x]` with a cited commit.
3. Pin the archive-sweep status report body-date fix (b3f9395 class) with a lint/gate if cheap.
4. Spot-read the remaining five archived reports' strikethroughs once (closure of §b).
5. Codify `check-status-index.sh` as a pre-dispatch check for close-out reports.
6. Document the "DONE-on-arrival" TQ_RESULT contract (sha to cite: deliverable commit vs close-out commit).
7. Sweep this window's ~7 same-ID reports for unharvested f)-list items into TODO_LIST.
8. Retract-or-note permanently-uninstallable v0.3.0 (carried from 00-37 report g).
9. TODO row 149: deadline-path pinning (standing top item from 00-52/00-57 reports).
10. status-loop.sh REPO_ROOT/TQ_BIN env fix (PASSING FINDING from 00-52 report, still open per later index rows).
11. Re-dispatch root cause investigation (recurring g-item, unanswered).
12. queue dispatch dedup via git trailers or sweeper cursor (carried from 01-12/01-17 reports).
13. Closeout TQ_RESULT "report" field contract (carried from 01-12 report).
14. webui links from task detail to its status reports (carried from 01-12 report).
15. Daemon pause mechanism for scripted git-history work (carried).
16. commit_sha semantics for re-dispatch/no-op tasks (recurring g-item, still unanswered).
17. Race-suite scope ruling for shell-only deliverables (carried from 00-57 report).
18. Stray `cmd/tq/tq.exe` removal (carried from 00-37 report f).
19. Clean-room `go install …/cmd/tq@next` smoke as a release-gate candidate (carried).
20. v0.4.0 cut planning once installability restores (carried).
21. Reproduce + fix the runner-only vendorHash drift class (AGENTS known issue, no diagnostic playbook).
22. Ratify or reject the cluster-note archive-annotation shape (the six 09-07 archives use per-section notes; older archives use pure strikethroughs) — one convention.
23. Add the `GOEXPERIMENT=jsonv2` NixOS unit env fix (`Environment=`) to SystemNix (owner-run, still open per AGENTS).
24. Fix `~/.config/go/env` empty-symlink situation at the home-manager level (root cause of the env lie).
25. Audit whether any of this window's daemon `chore:` commits swallowed unindexed reports (check-status-index catcher vs preventer).

(Items 26-50 would be filler from the canonical window backlog — see the 00-37 report's f-list; deduped here rather than restated.)

## g) Questions I cannot figure out myself

1. Should the archive-annotation convention be the per-section cluster note (used for the six 09-07 archives) or pure inline strikethroughs (older archives) — or is per-file judgment the rule?
2. For a DONE-on-arrival re-dispatch, which commit belongs in TQ_RESULT `commit_sha`: the original deliverable commit, the close-out report commit, or both?
3. Do you want the daemon-attribution split (daemon commit carries edits, footer commit carries TODO closure) ratified as documented convention, or fixed at the root (e.g. daemon hook skip during task work)?

---
*Point-in-time snapshot — re-verify before building on any claim.*
