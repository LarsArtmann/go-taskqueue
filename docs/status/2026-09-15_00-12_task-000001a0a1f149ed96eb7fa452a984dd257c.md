# Journal-Drift Audit (`tq audit --journal`) — Work Report + Status

- **Task:** 000001a0a1f149ed96eb7fa452a984dd257c (TODO_LIST "High Impact" row)
- **Session:** 2026-09-15 00:00–00:15 CEST, autonomous pool dispatch
- **Work commit:** 6b692f2 (footer commit; code itself rode daemon commits 0c5c59e + 6660982)
- **Scope:** Journal-drift audit — replay the fact journal into a task
  projection and diff it against the `tasks` table over **status, attempts,
  priority, dedup key**; read-only report, no repair mode; hermetic fixture
  tests only; stays an operator command, advisory (2026-09-14 ruling, O5
  precedent).

## a) FULLY DONE

1. **Projection replay covers all four contract fields**
   (`cmd/tq/journalaudit.go`): `replayProjection` rebuilds per-task state
   from facts alone — status (all transition facts: enqueued/claimed/
   completed/failed/dead-lettered/cancelled/requeued/released), attempts
   (max `Fact.Attempt` over failed/dead-lettered facts, which carry the
   post-increment number; requeue burns nothing), priority
   (`queue.EnqueueDetail.Priority` on `task.enqueued`, superseded by
   `queue.ReprioritizeEvidence.NewPriority` on `task.reprioritized`),
   dedup key (`EnqueueDetail.DedupKey`).
2. **Legacy-fact guards**: RescueDead re-emits `task.enqueued` with only
   `Rescue` set, and pre-detail thin facts carry nothing — both leave the
   projection's `priorityKnown`/`dedupKnown` flags false, so absence of
   evidence is never reported as drift. (The EnqueueDetail doc comment
   literally names priority+dedup as "the inputs of the journal-drift
   audit" — the store side was already prepared.)
3. **Per-field drift report**: `DriftRow` gained a `field` discriminator
   (status | attempts | priority | dedup_key); rows sort by (taskID,
   field); JSON shape extended (`taskId`,`field`,`stored`,`replayed`);
   text output prints one line per diverging field with the advisory
   epilogue (no repair mode, per contract).
4. **Hermetic fixture tests** (`cmd/tq/journalaudit_test.go`): (i)
   full-lifecycle no-drift test over a scratch sqlite store (enqueue ×3,
   complete, dead-letter, cancel — replay must agree exactly); (ii)
   replay-transition unit test incl. attempt max(); (iii) replay
   priority/dedup test incl. reprioritized supersede and the legacy thin
   fact; (iv) `TestJournalDriftSeededDriftAllFields` — seeds drift by
   closing the store, corrupting the `tasks` table with raw
   `database/sql` (modernc driver), reopening, and asserting all four
   fields diverge with the right replayed values. Production journal
   never touched; everything under `t.TempDir()`.
5. **CLI wiring verified end-to-end** on a scratch DB (TQ_DB exported —
   the production-journal landmine in AGENTS.md was respected):
   `tq audit --journal` prints the new header naming all four fields;
   `--json` emits the report object.
6. **TODO_LIST closed out**: the High Impact row marked `[x]`.
7. **Gates**: cmd/tq module gate (`scripts/test-cmd-tq.sh` — full module
   suite, green); root `go build/vet/test` green; gofmt clean.
8. **Committed with the queue footer** (6b692f2). Not pushed.

## b) PARTIALLY DONE

1. **Advisory ci-local smoke over a seeded fixture** — explicitly optional
   per the TODO row ("advisory when it lands"), NOT built. The seeded-drift
   Go test covers the detector; no shell smoke drives the built binary
   against a fixture journal in ci-local.
2. **Attempts semantics for the completed-after-failures path verified
   only by reading, not by test**: the no-drift lifecycle test completes a
   task with zero prior failures; a task that failed once and THEN
   completed end-to-end through the real store (replayed attempts=1 vs
   stored=1) is not exercised — I reasoned it from the SQL (attempts
   increments only on the fail paths) but never pinned it.
3. **Docs**: FEATURES.md has a derived-outcomes/audit family but I did not
   add or check a journal-drift-audit row; CHANGELOG (append-only) got no
   entry for the new flag semantics.
4. **`tq show` / `tq facts` cross-reference polish**: the report tells the
   operator to investigate with `tq show <id>` and `tq facts -detail`, but
   the JSON output is not plumbed into any machine consumer (fine for an
   operator command, still a gap vs. the bridge/sweeper pattern).

## c) NOT STARTED

1. **Repair mode** — explicitly out of scope ("no repair mode yet").
2. **Postgres-backend conformance for the audit** — journalDrift sits on
   `queue.Store`, so postgres should work unchanged, but no test ran it
   against a postgres store.
3. **CHANGELOG entry, FEATURES.md row** (see b3) — not started.
4. **AGENTS.md memory update** — the `tq audit --journal` flag and the
   four-field diff contract are not yet recorded in the repo AGENTS.md
   command/payload sections.
5. **The `internal/status` pre-existing failure** (`TestSweepPinsCloseoutReportPaths`,
   cwd-dependent fixture) — I diagnosed it as unrelated (module I didn't
   touch, present before my change) and left it per "don't fix unrelated
   bugs"; nobody else may have noticed it yet.

## d) TOTALLY FUCKED UP

Honest list — nothing catastrophic, but real defects in how I worked:

1. **Fought the daemon instead of racing it consciously.** The auto-commit
   daemon committed my journalaudit files (0c5c59e, 6660982) and the
   TODO_LIST tick (6b692f2) while I was still working. My explicit commit
   then failed with "nothing to commit", and I resolved it by AMENDING the
   unpushed daemon tip (6b692f2 → 6b692f2) to carry the real message and
   footer. The amend was legal (unpushed, tip, my own content only) and
   follows the repo's history-rewrite policy exception, but it was a
   reactive scramble: I should have anticipated daemon-folded commits and
   either committed immediately after the first edit batch or amended the
   FIRST daemon commit (the one holding journalaudit.go) so the footer
   rides the code commit, not the one-line TODO tick.
2. **Multiedit self-inflicted collisions in the test file.** My 3-edit
   batch consumed its own anchor comment and duplicated a comment line,
   costing two extra read/edit round trips. Sloppy anchor discipline for
   an exact-match editor.
3. **First test run failed on my own wrong assertion** (attempts == 5, I
   asserted `len(steps)-1` = 11 without counting the fixture indices).
   Deterministic fixture, trivially checkable before running.
4. **Report-before-final-verification ordering**: I never re-ran the cmd/tq
   gate AFTER the gofmt pass + amend; the last green gate run predates the
   final file state by one cosmetic-only step (gofmt -w ran before the
   gate, so risk ≈ 0, but the citation discipline this repo demands —
   claims carry citations — wants the gate run on the exact final tree).
5. **edit-tool staleness whiffle**: two edit calls bounced on
   "file modified since read" (the daemon or my own prior edit) and I
   re-derived state from `sed`/`cat -A` instead of just re-Viewing —
   wasted cycles, against the stated workflow.

## e) WHAT WE SHOULD IMPROVE

1. **Commit-at-boundaries discipline**: with the daemon live, make the
   explicit footer commit the FIRST action after the first green gate, not
   the last action of the session. (Recurring theme in prior reports —
   this session repeated it.)
2. **Pin the completed-after-failures attempts path**: one more lifecycle
   fixture (fail → requeue → claim → complete) would close the only
   semantic gap in the replay that rests on code-reading rather than a
   test.
3. **Test helpers for projection corruption** (`seedDrift`) are generic
   enough to live in a shared testutil if other projection-vs-journal
   tests ever appear (cqrs adapter, sweeper watermarks).
4. **JSON field enum stability**: `field` values are free strings; if the
   advisory smoke or any consumer keys off them, pin them in a test
   (cheap one-assert addition to the seeded-drift test — partially there
   via the fields map, but the string literals themselves aren't pinned
   against typos in the producer).
5. **gopls phantom-noise protocol**: the session burned context reading
   known-GOEXPERIMENT phantom diagnostics in tool output; per AGENTS.md
   these are noise — future sessions should skip them without re-litigating.
6. **Pre-existing internal/status failure should be triaged by someone**:
   it masks real regressions in that module's gate and violates the "master
   CI green" comfort this repo runs on locally.

## f) UP TO 50 THINGS TO GET DONE NEXT

Ranked, impact-first; items 1–8 belong in TODO_LIST, the rest are ROADMAP
fuel. (No item here is a commitment — routing per docs-health HARVEST.)

1. **Pin completed-after-failures attempts replay** with a lifecycle
   fixture (fail → complete; replayed attempts must equal stored).
2. **CHANGELOG entry** for `tq audit --journal` (four-field advisory
   drift audit).
3. **FEATURES.md row** for the journal-drift audit under the audit family.
4. **AGENTS.md**: document the flag + four-field contract + legacy-fact
   guards in the audit/payload section.
5. **Advisory ci-local smoke** over a seeded fixture journal (explicitly
   optional; never a hard gate — O5).
6. **Triage/fix `TestSweepPinsCloseoutReportPaths`** (internal/status,
   pre-existing red, cwd-dependent fixture).
7. **postgres conformance pass for journalDrift** (env-gated
   TQ_TEST_POSTGRES test).
8. **Dedup the seeded-drift raw-SQL helper** into a shared testutil if a
   second consumer appears.
9. Repair mode for the drift audit (owner-gated design first: which field
   wins — journal or projection — is a policy question, not a code one).
10. Exit-code posture decision: should `--journal` ever exit non-zero
    (e.g. `--strict` flag) while staying non-gating in ci-local?
11. Feed drift rows into the webui (`tq serve` doctor panel?) instead of
    stdout-only.
12. Replay coverage for `journal.Orphaned` interactions: orphan → reclaim
    chains through ClaimDue produce Claimed-after-Orphaned; assert no
    false drift on a long-running orphaned task fixture.
13. Test the facts-pagination loop boundary (exactly factsPageSize facts;
    off-by-one at the watermark).
14. Consider `journal.CancelRequested` mid-run → `task.cancelled` chain
    fixture (observation fact between state facts).
15. Pin the JSON `field` enum literals in a test (typo guard).
16. RescueDead replay: assert a rescued task's fresh `task.enqueued`
    (Rescue-only) does NOT clobber replayed priority/dedup (guards the
    known-flags logic against future detail changes).
17. Distinguish "task with no facts" from "task whose facts predate the
    journal" in the report wording (legacy journals may legally have thin
    facts).
18. `tq doctor` integration: doctor could run the drift check and surface
    a section (read-only), giving the operator one entry point.
19. Benchmark the replay on a journal at production scale (the dogfood
    journal is months old; O(repos × facts) is fine but prove it).
20. Wire the report's `TasksCompared`/`FactsReplayed` into stats/trend if
    the operator wants drift-rate over time.
21. Unify the two "audit" JSON shapes (harvest DriftResult vs DriftReport)
    or document why they differ.
22. Add `--task-id` filter to the journal audit for targeted checks.
23. Consider replaying `MaxAttempts` (EnqueueDetail doesn't carry it —
    fact-side gap; would need a store-side detail extension, ADR first).
24. Docs: a short `docs/` runbook section "when drift is reported, what
    next" (show/facts/dlq/rescue ladder).
25. Fuzz `replayProjection` (facts are untrusted-ish input; a malformed
    Detail must never panic — currently guarded by err == nil only).
26. Property test: replay(store-driven random op sequence) == store List
    (quickcheck-style, sqlite scratch store).
27. Sub-second re-audit: incremental mode resuming from a watermark
    instead of full drain (only if journals grow big enough to matter).
28. cqrs adapter parity check: `tq facts --cqrs` and the drift replay
    should agree; a cross-check test would pin ADR-0014's read-only story.
29. Report timing/age context per drift row (fact seq/time of last
    transition) so operators can order investigations.
30. Machine-readable severity: status drift vs priority drift are not
    equally alarming; consider per-field severity in JSON.
31. Lint-baseline check for the touched files (advisory gate; confirm no
    new findings were added — I never ran it this session).
32. Race run of the cmd/tq suite (`-race`) — the shim gate ran without it
    this session.
33. CI matrix check: confirm the windows-latest leg is happy with the new
    files (no unix-build tags needed — pure Go — but push proves it).
34. Docs literal gate: ensure no doc/script quotes the OLD one-line drift
    output format (grep for the old phrasing).
35. Sweep for other tests pinning `replayStatuses` (renamed to
    `replayProjection`) — grep the repo for stale references.
36. TODO_LIST hygiene: the row I ticked may deserve a follow-up row for
    the advisory smoke (split done vs optional-future explicitly).
37. Evaluate surfacing drift in `tq top` as a one-line health indicator.
38. Add the four-field header line to any golden/CLI-output test in
    cmd/tq that asserts full audit output (main_test golden coverage is
    for the harvest audit only — verify).
39. Owner ruling record: add a one-line AGENTS.md note that the drift
    audit is advisory-by-ruling (O5) so no future agent wires it into a
    hard gate.
40. Store-side: document in EnqueueDetail that Priority/DedupKey are the
    drift-audit contract (comment exists — verify it mentions all four
    replayed fields or extend).
41. Consider journal-fact for future attempts-schema changes (claim-time
    attempt stamping) — upstream design note, enables exact attempt
    replay including non-failing claims.
42. Tooling: a `make`-free `scripts/smoke/journal-drift.sh` skeleton stub
    (even if advisory-only) so the smoke exists to be wired.
43. Cross-backend determinism test: same fact sequence through sqlite and
    postgres produce identical DriftReport JSON.
44. Escape hatch: `--journal --json` piped to `jq` recipes in docs for
    common operator queries (e.g. only status drift).
45. Investigate whether `List(ctx, queue.Filter{})` ordering is
    deterministic across backends (report stability for diffs).
46. Memory-file update in global AGENTS.md cross-cutting lessons: "amend
    the FIRST daemon commit of your own work" pattern if it proves out.
47. Check `.golangci-baseline.txt` didn't grow from the new files
    (varnamelen/wrapcheck on the new code).
48. Consider a `--since <seq>` flag for incremental audits (pairs with 27).
49. Annotate the seeded-drift test with the "NEVER production journal"
    contract citation so the constraint survives refactorings.
50. Cut scope check: confirm release-gates/allowlist need nothing for a
    cmd/tq-only change (no go.mod version surface moved this session).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Attempts truth on the completed path**: when a task fails once and
   later completes, is `tasks.attempts` GUARANTEED to equal the failed-fact
   count in every backend (incl. future postgres parity and rescue's fresh
   budget)? My replay assumes attempts == max(failed/dead-lettered
   Attempt). If a backend ever stamps attempts at claim time, the drift
   audit starts lying on every healthy task. Ruling wanted: is "attempts =
   failures only" a frozen cross-backend invariant I can pin in a
   conformance test, or should the audit skip the attempts field where
   Claimed facts exist without failure facts?
2. **Pre-existing `internal/status` red**: is
   `TestSweepPinsCloseoutReportPaths` failing for everyone locally (my
   read: cwd-dependent fixture expecting a repo-root-relative
   docs/status file), or is it a leftover uncommitted state from a
   concurrent agent's in-flight work that I should leave strictly alone?
   (Determines whether it's a quick gate repair to mint as a new task or a
   mid-flight hand-off.)
3. **Footer placement policy under the daemon**: the code landed in daemon
   commits (0c5c59e/6660982) WITHOUT the Task-Queue-ID footer, and only my
   amended tip (6b692f2, the TODO tick) carries it. Is the queue's
   git-cross-reference satisfied by footer-on-any-commit-of-the-task, or
   should the convention be "amend the first daemon commit of your work so
   the footer rides the code"? If the latter, it belongs in AGENTS.md as a
   rule; the 12-52 report flagged the same anonymous-daemon-history gap.
