# Task 000001a0ace13249179222ae9ed9deccc8d5 — Session trigger #3 (crush #3146 adoption): verify-and-block window

**Date:** 2026-09-17 03:00–03:05
**Work item:** TODO_LIST row 108 — "Session trigger #3 — adopt crush #3146 SessionEnd when released: point the hook at `tq session close` (enqueue-only fits the ~2s budget) (03-28 §f3)"

## Summary

Verify-then-stop window. The item is gated on an upstream release that has not
happened; the correct outcome was evidence collection + a BLOCKED annotation,
not code. Verified the gate from three independent sources, marked the row
blocked with the evidence, committed (1 file, 1 line changed).

## a) FULLY DONE

1. **Upstream merge-state verification (the core evidence):** `gh api
   repos/charmbracelet/crush/pulls/3146` → `state: open`, `merged: false`,
   `mergeable_state: dirty` (merge conflicts), 11 issue comments + 29 review
   comments, head 1ec98a85. CI green (CLA/CodeQL/build×3) but green CI on an
   unmerged conflicted PR is not a release.
2. **Release-surface verification:** latest crush release is v0.95.0
   (published 2026-09-16); its release body contains no hook/SessionEnd/#3146
   mention. Installed client is v0.94.2 and has no `crush hooks` command at
   all ("Unknown command") — there is no SessionEnd hook surface on this host
   to point at anything.
3. **Source-surface check:** GitHub code search for `SessionEnd` in
   charmbracelet/crush returned no hits (consistent with the PR not being
   merged to main).
4. **TODO_LIST row updated with a one-line BLOCKED note carrying all three
   evidence points** (row 108, left unchecked per contract), committed as the
   window's only commit with the task footer; commit-msg hook accepted the
   footer (single, well-formed).
5. **Session-start ritual honored:** whole-repo `git log --oneline`,
   `git status`, `git stash list`; grep for the task id across docs/status/
   (no prior report for this id — first window on it); CONTRIBUTING.md
   located and read at turn 1.
6. **Context check that the fallbacks make this safe to park:** triggers #1
   (session registry + `tq session sweep`, row 105) and #2 (`tq crush`
   wrapper, row 107) shipped 2026-09-17 — the close-out loop already runs
   without the hook, so the block costs nothing operationally today.

## b) PARTIALLY DONE

1. **Release-body check was weaker than it looks:** I grepped the v0.95.0
   release body for "hook|sessionend|3146" and read "no output" as absence —
   but a release with an EMPTY body produces the same no-output. The stronger
   evidence is the unmerged PR + no `hooks` command in the installed binary,
   so the conclusion stands, but the release-notes leg of the triad is
   genuinely unverified. Cheap follow-up: `gh api .../compare/v0.94.2...v0.95.0
   --jq '.commits[].commit.message'` and grep for the hook PR.
2. **No gate run:** the change is one markdown line, so I skipped build/tests
   and leaned on the commit-msg hook as the only executed check. Defensible
   for a docs-only diff, but the standing rule prefers citing a gate even in
   verify windows (the 02-54 report dinged exactly this class). Nothing here
   could have broken; the citation convention was still not met with a gate
   run.

## c) NOT STARTED

Nothing owed by this item — it is a single-sentence backlog row gated on an
external event. The downstream work it would unlock (hook config pointing at
`tq session close`) is correctly left unwritten; writing it now against a
config format that may still change in review would be speculative.

## d) TOTALLY FUCKED UP

Nothing this window. Two near-misses worth recording:

1. **The initial `edit` failed** ("read the file before editing") because I
   grepped the row instead of Viewing it first — cost one round trip, caught
   by the tool, no damage. The AGENTS.md read-before-edit rule means View,
   not grep.
2. **First `agentic_fetch` of the PR page failed twice** with a tooling error
   before I switched to `gh pr view` / `gh api` — the gh route should have
   been first for a GitHub PR (it is also cheaper and returns structured
   state).

## e) WHAT WE SHOULD IMPROVE

1. **Verify-cite-stop should still touch a gate:** even a 5-second
   `go build ./...` on a docs-only window satisfies the claims-carry-citations
   convention with an executed check. Add it to the verify-window checklist.
2. **"No output" is not negative evidence:** when grepping release notes or
   bodies, distinguish empty body from absent keyword (print the body length
   or the body itself). This window's release-notes leg has that exact hole.
3. **PR-state checks: `gh api` first, page-fetch second.** The structured API
   call answered in one shot what two agentic_fetch failures could not.
4. **BLOCKED rows should name the re-check command:** the note now carries the
   evidence but not how to re-verify (a `gh api .../pulls/3146 --jq .merged`
   one-liner). Small edit, saves the next window the whole triage.

## f) WHAT WE SHOULD GET DONE NEXT (up to 50)

Ranked; 1–3 are this task's direct tail, the rest are session-adjacent
observations from rows 105–108 and this window's reading. Only 1 is NEW
(the rest live in TODO_LIST already — do not duplicate).

1. _(new, row not yet appended)_ Add the re-check one-liner to row 108's
   BLOCKED note: `gh api repos/charmbracelet/crush/pulls/3146 --jq .merged`
   — flip to do-able the moment it prints `true` and v0.96+ ships the hook.
2. (row 108, when unblocked) Write the SessionEnd hook config pointing at
   `tq session close --db <journal>` enqueue-only; verify the ~2s budget
   holds on a real close.
3. (row 108, when unblocked) Decide begin-side automation (hook on
   SessionStart minting `session.opened`?) — the 02-54 report's open §g
   question; the wrapper's no-id fallback already handles sessions that
   started before any hook existed.
4. (row 109) Postgres session bridge: AppendFact on
   internal/queue/postgres + conformance, or fail fast in `tq session` with
   the clear sqlite-only error.
5. (row 110) `tq session list` + `tq session close --dry-run`.
6. (row 111) Stale-open session visibility in doctor/stats (never
   auto-close without a ruling).
7. (row 112) Session forensics: CloseDetail records AllowDirty/RequireClean +
   review range; `tq facts --type session.opened,closed`; `tq show session:<id>`
   graceful degradation.
8. (row 113) Session volume in dashboard: session-fact counts + open-sessions
   lamp in stats/webui.
9. (row 114) Session-bridge safety pins: prune-stale never cancels
   session-minted tasks; sweeper-namespace cross-dedup; trailer-parser edge
   tests.
10. (row 115) Multi-repo session semantics: document one-close-per-(session,
    repo) or design multi-repo close.
11. Verify the stronger v0.94.2...v0.95.0 commit-range check (§b1) so the
    BLOCKED note's release leg is airtight.
12. Watch #3146 review comments (29) — the config FORMAT may change in
    review; when it merges, re-read the final docs before writing the hook.
13. When the hook lands, decide whether the registry+sweeper (#1) and the
    wrapper (#2) become fallback-only (kept) or get de-emphasized in docs.
14. Consider a periodic (weekly?) upstream-state check item instead of
    waiting for a re-dispatch to rediscover the block each time.

(14 items — the rest of the 50 would be padding; the session-bridge rows
above are the real queue.)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Who re-checks #3146, and on what cadence?** The row now sits blocked
   with evidence, but nothing schedules the next look — is a re-dispatch of
   this task id the intended mechanism, or should a weekly upstream-check
   item go into the backlog?
2. **When the hook ships, does `tq session close` fit the hook's timeout
   contract as-is, or does the enqueue path need a `--hook-mode` (e.g.
   stricter output silence, exit-code mapping) the PR's spec will define?**
   I cannot know until the final PR text is merged.
3. **Should trigger #1's registry sweeper stay armed after #3 lands?** With a
   real SessionEnd hook, the pgrep-ownership sweep is redundant on hook-
   covered sessions but still covers pre-hook sessions and crashes — keep
   both, or does the owner want the sweeper demoted to opt-in once the hook
   is proven live?
