# Close-out — dead-export audit re-run over internal/session (task 000001a0c3658a961763c57a159acbb423b1, second dispatch)

**Date:** 2026-09-21 12:05 (THIRD window on this task ID)
**Window type:** duplicate close-out. The work item was DONE by the first
dispatch window (11-58, docs commit 13db57f), and a full a-g close-out
ALREADY EXISTS (12-01 report, same ID) — written before this one was
requested. Per the repeat-dispatch rule (verify → cite → stop) this window
did NOT rewrite that verdict; it independently re-verified and records only
its own delta. Read the 12-01 report for the prior close-out's §d (it
confesses the gates-cited-before-run sequence that this window's first
response initially repeated and then self-corrected before stopping).

## a) FULLY DONE

- **TODO row state confirmed:** `TODO_LIST.md:179` carries the item `[x]` with
  the DONE citation pointing at the 11-58 report. Nothing to change.
- **Report exists and is indexed:** the 11-58 report file is present; index row
  exists in `docs/status/README.md` (2 references); `check-status-index.sh` rc=0.
- **Audit re-run at HEAD (fresh delta, this window):**
  `bash scripts/check-dead-exports.sh` → rc=0, 68 zero-importer flags (advisory;
  summary "305 symbols ok, 68 dead").
- **internal/session flags independently re-derived (fresh delta):** 7 flags in
  the module — `RegistryEntry`, `LoadRegistry`, `RewriteRegistry`,
  `ProcessOwner`, `PgrepOwner`, `SweepOutcome` (registry.go) and `CloseResult`
  (session.go). Substring grep confirms every one is structurally alive behind
  the package's imported entry points: registry plumbing is called by
  `session.Sweep` (internal/session/registry.go:225), and Sweep/Close are
  consumed at cmd/tq/session.go:114 / cmd/tq/session.go:308; `CloseResult` is
  `Close`'s return type (internal/session/session.go). The zero-importer
  detector only counts cross-package imports, so intra-package usage behind an
  imported wrapper reads as "dead" — exactly the false-positive class the
  11-58 verdict recorded. **Verdict reproduced: nothing to prune.**
- **Verify-window minimum battery at HEAD (this window's own runs, not
  inherited):** `scripts/check-doc-refs.sh` rc=0; root `go build ./...` rc=0;
  root `go vet ./...` rc=0 — all under `GOEXPERIMENT=jsonv2`.

## b) PARTIALLY DONE

Nothing. The item is binary (audit re-run → verdict) and the verdict is confirmed.

## c) NOT STARTED

- No follow-up work was minted. The audit script stays advisory by design
  (AGENTS.md; O5-adjacent ruling class) — see (f) for the improvement candidate.

## d) TOTALLY FUCKED UP

- **Third window on one task ID** (structural, not just this window): this is
  the THIRD report written against 000001a0c3658a961763c57a159acbb423b1
  (11-58 work, 12-01 close-out, 12-05 this) — the same re-dispatch
  multiplication pattern the row-164/173 cluster already flagged. The fastest
  correct response to a DONE row was verify → cite → stop; a second full
  a-g close-out adds index bloat, not information.
- **First response skipped the cheap battery** (this window, self-caught during
  close-out): the initial verify pass confirmed the report + script run but did
  not run the verify-window minimum battery (check-doc-refs + build+vet) before
  stopping — the exact recurring miss codified after 03-27 §e2 / 03-39 and
  promoted to a TODO rule 2026-09-20. The battery ran before this report
  instead of before the first stop. Cheap gates are seconds; there is no excuse
  for ordering them after the stop decision.

## e) WHAT WE SHOULD IMPROVE

1. **Make the battery literal, not remembered:** the minimum battery (doc-refs
   + build + vet) should be a one-liner script (or part of session-start.sh)
   so a verify-only window cannot "stop" without a recorded rc triple.
2. **Dead-export detector should understand intra-package liveness:** a symbol
   referenced anywhere inside its own package (especially as the return type or
   dependency of an imported entry point) is not dead. A cheap fix: treat
   "referenced within the defining package AND the package is imported" as
   alive, or at minimum tag such flags `indirect` so audits stop re-proving the
   same false-positive class (this is the second full manual re-derivation of
   internal/session).
3. **Verdict caching for repeat audits:** the 11-58 report + this window
   re-derived the same 7 flags independently. If the script emitted a
   machine-readable verdict file (per-symbol alive/indirect/dead with the
   citation), a re-run window would diff instead of re-derive.

## f) Next (backlog candidates — up to 50; the genuinely useful ones)

Scoped to what this window noticed in passing; not minted as items (the ones
worth queueing are listed so the owner/pool can admit them):

1. Improve `scripts/check-dead-exports.sh` to mark package-internally-referenced
   symbols as `indirect` instead of `dead` (kills the recurring false-positive
   re-derivation class; internal/session has now burned two audit windows).
2. Add the verify-window minimum battery as a runnable helper (e.g.
   `scripts/verify-battery.sh`: doc-refs + build + vet, prints rc triple) so
   verify-only dispatches run it by reflex.
3. The dead-export audit flags 68 symbols repo-wide; the prior audits
   spot-checked internal/session only — a one-time triage of the remaining ~61
   flags (substring matching, per the AGENTS.md rule) would either find real
   prunes or let the advisory baseline be recorded once.
4. ~~CloseResult API question~~ — withdrawn during close-out: the initial
   hedged claim ("cmd/tq may discard CloseResult") was wrong;
   `runSessionClose` consumes `res.Commits` at cmd/tq/session.go:325 to
   branch the zero-commit path. Recorded here so the wrong hedge is not
   re-discovered by the next audit window.

(Items beyond 4 would be research into unrelated areas — out of scope for this
window per the close-out contract.)

## g) Questions (owner-only rulings)

1. Should the dead-export audit gain the `indirect` classification (improvement
   2 above), or is the advisory manual-verdict workflow the accepted long-term
   shape (each repeat audit is expected to re-prove liveness)?
2. Is a one-time full triage of the remaining ~61 zero-importer flags repo-wide
   worth a window, or is the audit only ever re-run per-accusation?

## Gates cited (all run this window at HEAD)

- `bash scripts/check-dead-exports.sh` → rc=0 (68 flags, advisory)
- `bash scripts/check-doc-refs.sh` → rc=0
- `GOEXPERIMENT=jsonv2 go build ./...` → rc=0
- `GOEXPERIMENT=jsonv2 go vet ./...` → rc=0
- `bash scripts/check-status-index.sh` → rc=0
- Liveness evidence: internal/session/registry.go:225 (`Sweep` uses
  LoadRegistry/RewriteRegistry/RegistryEntry/SweepOutcome/PgrepOwner/
  ProcessOwner), internal/session/session.go (`Close` → `CloseResult`),
  consumers at cmd/tq/session.go:114 and cmd/tq/session.go:308.
