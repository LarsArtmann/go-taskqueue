# Task 000001a0c292f82096b0e01739e8bb162da1 — THIRD window (verify→cite→stop on the DONE SessionMessageCount row) + mandated a)-g) report

Written 2026-09-21 08:35 CEST. Window lineage for this ID:

1. **08-12 WORK window** — decision + wiring, source commit `021f8a4`, TODO row 170 closed.
2. **08-23 SECOND window** — owner-requested brutal a-g report (`0c4a3b1`) + fold-account correction (`138d0fc`).
3. **THIS window** — verify→cite→stop (`ffb9ebc`), zero source delta, fresh battery, row 170 re-verify annotation, this report.

Work item (TODO row 170, "Priority-system follow-ups"): decide whether message-count-only runs render a usage line; wire the suffix if yes; pin either way (04-07 report f2/f3; internal/webui components.go).

## a) FULLY DONE

1. **The row's work itself (prior window, re-verified first-hand this window).** DECISION: message-count-only runs render — a real session whose token/cost extraction was unavailable must not look like a stub. WIRING: `sessionUsageEmpty` now takes the message count and only goes quiet on all-zero; `sessionUsageText` renders " · N messages" as a suffix on a full line, and "N messages" alone when tokens+cost are zero. Both render sites (statusReportCard, prioritizeResultCard) pass `res.SessionMessageCount`. Evidence at HEAD: internal/webui/components.go:102-104 (empty gate), :110-122 (text), fragments.templ:706-707 and :722-723 (+ regenerated fragments_templ.go), source commit `021f8a4`.
2. **Pin.** `TestResultUsageRendersOnDetailPage` (webui_test.go:631) pins all four behaviors: full line with suffix ("… · 7 messages", :666), partial usage with suffix and zero-completion tokens ("10 prompt + 0 completion tokens · $0.0100 … · 3 messages", :698), message-count-only renders "5 messages" (:724), all-zero stays quiet (:728+). Targeted run PASS this window.
3. **This window's fresh battery (at HEAD).** Root build+vet rc=0 under GOEXPERIMENT=jsonv2; check-doc-refs rc=0; internal/webui in-module suite (GOWORK=off, -count=1) ok 10.093s; targeted pin test PASS; check-todo-list rc=0 after the row annotation. Zero source delta since `021f8a4`: `git diff --stat 021f8a4..HEAD` is docs-only (TODO row + two reports + index), consistent with the 08-23 report as corrected by `138d0fc`.
4. **Row + artifacts.** Row 170 re-verify annotation committed (`ffb9ebc`) carrying exactly one `Task-Queue-ID:` footer (verified via `%(trailers)`); this report + index row ride the report commit.

## b) PARTIALLY DONE

1. **Derived-usage surface coverage.** Works: budget token projection (all four result types), `tq show` typed decode + JSON (all types), webui detail usage line for the status + prioritize cards. Missing: the agent + review completion cards render NO usage line (row 172) — the dominant spend class is invisible on the primary detail UI even though its Session* fields are recorded and projected. By the ghost-system definition: data exists, primary UI doesn't show it. Effort M (fragments.templ + pin extension in the same change).
2. **Usage-key exact-pin parity.** The exact-SET marshal pin exists only for AgentResult + PrioritizeResult (TestPrioritizeResultUsageKeysMatchAgentResult); ReviewResult + StatusResult rely solely on budget_test's key-based parse-sum — a key RENAME on those two types would not fail an exact pin. Row 165, BLOCKED on the 04-40 §g1 owner granularity ruling (close-vs-keep). Mechanical, S, once unblocked.
3. **Docs loop for the landed work.** Row 171 (CHANGELOG/FEATURES entries for the usage-rendering work) is still unchecked after three windows on this ID — docs-only, unblocked. Correctly out of my contract (exactly-one-item), but it is the open loop this task's cluster leaves behind.

## c) NOT STARTED

- Row 171 (CHANGELOG/FEATURES for the usage work) — not started; still wanted; S.
- Row 172 (agent/review card usage lines) — not started; still wanted; M.
- Row 165's mechanical half (exact-pin extension) — blocked on the owner ruling; S once unblocked.
- Show-JSON `session_message_count` assertion — NEW observation this window (§f item 6), not started; S.

(c) is small because the row itself was small and its cluster is already inventoried — not because nothing remains.

## d) TOTALLY FUCKED UP

Nothing in THIS window's own execution broke — every gate green, footer verified, zero source delta. The fucked-up items are the systemic ones this window operated inside and cannot fix from an agent seat:

1. **The minted verify gate deterministically dead-letters tasks on dev hosts.** The bootstrap-minted gofmt stage (`test -z "$(gofmt -l .)"`) walks the GITIGNORED `vendor/` tree: every stage before it green, gate rc=1 at the last stage, attempts burn, task dies. 5+ go-taskqueue tasks dead on it since 09-20 (AGENTS.md 2026-09-21 §f4; row 169's window cites the same shape). Severity: daily good-work dead-lettering + queue-budget burn. Fix is one line (`gofmt -l $(git ls-files '*.go')` in BOTH `.tq-verify` and the mint template) and OWNER-ONLY — agents are barred from editing their own gate. This is why this window ran the cheap battery + per-module gates instead of the verbatim gate.
2. **Daemon-fold attribution gap, now at its EIGHTH occurrence.** The 08-12 work window's SOURCE files (components.go, fragments.templ, fragments_templ.go, webui_test.go) were swept into unfootered daemon fold `d556a3f`; the footer commit `021f8a4` carries ONLY TODO_LIST.md. The queue's git cross-reference (commits via footer → changed files via that commit's diff) therefore systematically under-attributes real work. The 04-07 §e1 remedy (commit source with the footer FIRST, docs after) has been known for two days and is enforced by nothing.
3. **Verify-only re-dispatch loop churns paid windows on DONE rows.** This ID has consumed 4 windows (1 work + 3 verify/report). Sibling rows 161-164 show the same 3-4x churn each; row 161 names the cause: harvest mints while the check-off commit is in flight. Each recurrence is a full agent window spent re-citing unchanged code. No queue-side guard exists.

## e) WHAT WE SHOULD IMPROVE

1. **My own window:** I ran the internal/webui suite without `-race`. The cheap-battery rule permits it for a docs-only delta and the 08-12 window's suite stood at the same tree — but `-race` on the targeted pin costs seconds. A verify window that touches test-bearing code should include it; next time, when in doubt, race it.
2. **Verify windows could shrink the backlog instead of only re-citing.** The exactly-one-item contract is correct for the agent, but the MINTING side could attach a sibling docs-only row (e.g. 171) to a re-dispatch of the same cluster — the churn window already pays for the full context read.
3. **Harvest anti-race:** re-read the item's checkbox at claim/dispatch time and skip freshly-checked rows. Turns the (d3) churn into 1 work + 0 verify. Concrete, M, internal/harvest.
4. **Report economy for repeat windows:** the index is at 490 rows and every verify-only window adds a full report + index row. A row-annotation-only convention for 2nd+ verify windows (full report only on state change) keeps the paper trail and cuts the growth. Owner call — a related ruling row already sits in Process follow-ups (verify-only stop-artifact default).
5. **factTone's fallback does quiet double duty:** the switch (components.go:139) has no cases for the 6 newer fact types (Orphaned, Reprioritized, SessionOpened, SessionClosed, QuestionAsked, QuestionAnswered) — they all render ScrollbackToneNeutral, the same tone as heartbeats. Deliberate default, advisory-baseline-covered, but a conscious pass (assign tones or pin the fallback) would end the drift-by-default. Low.
6. **Dead parameter:** `detailItems`' `now` is unused (components.go:188, unparam). Trivial cleanup on next touch of the file.

## f) Things to get done next (ranked; no padding — 20 real items)

Already-filed items carry their TODO row (harvest dedupes against TODO_LIST); NEW items were minted as rows this window (see the two appended sections).

| # | Task | Impact | Effort | Category | Status |
|---|------|--------|--------|----------|--------|
| 1 | Wire derived session usage onto the agent + review completion cards; extend TestResultUsageRendersOnDetailPage to both new cards in the same change | High | M | Feature | row 172 |
| 2 | OWNER: scope the minted gofmt gate to tracked files in BOTH `.tq-verify` and the mint template | Critical | S | Bug | §d1, owner-only |
| 3 | CHANGELOG/FEATURES entries for the usage-rendering work | High | S | Documentation | row 171 |
| 4 | Harvest anti-race: checkbox re-read at claim/dispatch time, skip freshly-checked rows | High | M | Bug/process | NEW → Process follow-ups |
| 5 | Daemon-fold footer-first rule as a check script: flag docs-only footer commits from windows that touched source | High | M | Quality | NEW → Process follow-ups |
| 6 | Extend TestShowJSONCarriesDerivedSessionUsage to assert `session_message_count` (the show e2e pins cost+tokens only; the key's only other cmd/tq occurrence is stats_test.go:144) | Medium | S | Quality | NEW → Priority-system follow-ups |
| 7 | Usage-key exact-pin extension to ReviewResult + StatusResult | Medium | S | Quality | row 165, execute on ruling |
| 8 | OWNER ruling: verify-only repeat windows — row-annotation-only vs full report each time (this window: index at 490 rows) | Medium | S | Process | refines the Process ruling row |
| 9 | Consciously decide factTone cases for the 6 newer fact types (assign or pin the neutral fallback) | Low | S | Quality | §e5 |
| 10 | Drop detailItems' unused `now` param | Low | S | Cleanup | §e6 |
| 11 | v0.3.0 release: pkg.go.dev render check (persistent 404 = investigate) | Medium | S | Release | row 173 |
| 12 | Dead-export audit re-run over internal/session's exports | Low | S | Cleanup | row 178 |
| 13 | `tq show --commits`: surface footer-less daemon commits adjacent to a footer-bearing work commit as "folded here" | Medium | M | Feature | row 183; mitigates §d2's visibility half |
| 14 | Verify-failure logs: capture the failing stage's stdout (gofmt-stage deaths are inferable only from the all-ok shape today) | Medium | S | Quality | Process follow-ups |
| 15 | session-start.sh: print the docs/status README index tail after the task-ID grep | Low | S | Tooling | Process follow-ups |
| 16 | session-start.sh: print `tq show <task-id>` for passed IDs | Low | S | Tooling | Process follow-ups |
| 17 | AGENTS.md: codify the verify-only re-dispatch checklist next to the minimum-battery clause | Medium | S | Documentation | Process follow-ups |
| 18 | check-todo-list.sh: flag checkbox lines whose prefix is not exactly `- [ ]` (the mangled-insertion class) | Medium | S | Quality | Process follow-ups |
| 19 | captureStdout global-swap hazard: enforce/document the sequential convention | Low | S | Quality | Process follow-ups |
| 20 | internal/e2e -race runtime headroom (load-marginal vs the 180s stage cap; 240s kill observed) | Medium | M | Quality | filed per row 162's note |

The remaining open TODO rows (owner-blocked spend/pilot rulings, cqrs facade scope, lint-baseline banking, release-gates asymmetries, …) are real but outside this task's cluster; they are already inventoried and I refuse to pad this list by re-listing them.

## g) Questions I cannot figure out myself

1. **Will you apply the one-line gofmt fix to `.tq-verify` + the mint template now?** 5+ tasks have deterministic dead-lettered on the vendor-walk since 09-20; agents are barred from editing their own gate, so only you can stop the bleed. If the answer is "done", every dev-host verify window gets cheap again.
2. **Row 165: close or keep?** budget_test already marshals all four result types through the shared key-based parse; the exact-SET pin would add rename-drift safety for Review/Status. Your 04-40 §g1 granularity ruling has been pending two windows and the mechanical half is S — close it as redundant, or unblock it and a successor window lands it in minutes.
3. **Do you want the harvest anti-race guard, or is verify-only churn accepted as cheap verification?** The data: this ID burned 3 verify windows on 1 work window; rows 161-164 show the same ratio. If the churn is accepted pricing, I'll close the new Process row; if not, it is an M fix in internal/harvest.

---

Scope note: this report is based ONLY on this task's work and what I noticed in passing (components.go lint diagnostics, TODO rows 161-185, the Process follow-ups section, the `d556a3f`/`021f8a4` fold forensics from the prior windows' reports). No unrelated research. Format override: user mandated Markdown at docs/status/ per this repo's report convention, over the status-report skill's HTML default.
