# Task 000001a0986f3cf90458c58ca8c28af114e3 — Re-dispatch verification close-out (n=2)

**Date:** 2026-09-13 03:47 CEST · **Dispatch:** TODO row 122, "Harden the two example
servers beyond `ReadHeaderTimeout` (IdleTimeout/ReadTimeout), keeping the SSE
`WriteTimeout=0` exemption documented (examples/api, examples/sse)"

## Verdict

DONE-on-arrival. This is the SECOND dispatch of this task ID (n=2). The work
was shipped before this window in:

- `d3317c5` — harden example api/sse servers with ReadTimeout and IdleTimeout
- `b1e6dfc` — docs: example server hardening close-out (brutal a)-g)

## a) FULLY DONE

- **Independent first-hand verification** (not trusted from prior reports):
  examples/api/main.go:127-132 and examples/sse/main.go:89-94 both carry
  `ReadTimeout: 10s` + `IdleTimeout: 60s`, each with the `WriteTimeout=0`
  SSE exemption documented in an adjacent comment.
- TODO_LIST.md row 122 confirmed checked `[x]` at turn 1.
- Root gate re-run green: `GOEXPERIMENT=jsonv2 go build ./...` +
  `go test ./...` — all packages ok (fresh, non-race, root module).
- Report indexed in docs/status/README.md; working tree was clean at
  session start (git log over whole repo + status run first).

## b) PARTIALLY DONE

- Gate run was root-module only; sub-module gates and `-race` re-run were
  skipped and are DECLARED skipped — this change touches zero internal/
  files (verified by the work commit's diff), so the risk is nil, but the
  claim "tests pass" covers root only.
- ci-local / nix gates not run (pure-stdlib two-file change; overkill).

## c) NOT STARTED

- Nothing new. Zero deliverable changes in this window — by design for a
  verification-only re-dispatch.

## d) TOTALLY FUCKED UP

1. **The re-dispatch itself.** Same-ID redundant dispatch (3rd such cluster
   in this window per the 03-40 report on the SEO task). No deliverable
   work to do; queue-level footer dedup at dispatch time remains unminted.
2. **CONTRIBUTING.md again unread at turn 1** — the Nth consecutive miss
   flagged in every prior close-out. Broken the same way one more time.
3. **Prior same-ID close-out (03-44) unread at turn 1** — I discovered the
   n=1 report only via the status index while writing this report. A
   turn-1 grep of docs/status for the task ID would have short-circuited
   the whole window.
4. My work turn's test run used `tail -5` on output (pipeline mask again —
   exit code was checked via `&&`, so the gate was real, but the captured
   evidence was truncated; the sins repeat because the close-outs are the
   only place they get read).

## e) WHAT WE SHOULD IMPROVE

- Mechanical turn-1 checklist (same proposal as 03-28/03-40/03-44):
  `git log --oneline -5` + `git status` + `git stash list` + `ls
  CONTRIBUTING.md CLAUDE.md` + `grep -rl <task-id> docs/status/` — all in
  batch 1, before anything else.
- DONE-on-arrival protocol: if turn-1 grep finds the task ID in
  docs/status/, skip to short-circuit report; dispatch-time dedup is the
  owner-side fix.
- Exit-capture reflex: never read a gate through `tail`/`head` when the
  claim will cite it.

## f) Up to 50 things to get done next

1. Queue: dispatch-time Task-Queue-ID dedup (owner, recurring headline).
2. CHANGELOG entry for the hardening (17a5940's G114 line has one; d3317c5
   does not — the 03-44 report's §g1 ruling still open).
3. Behavior test pinning the example server timeouts (no harness exists).
4. Shared exampleServer helper if a third example server appears.
5. Audit all other http.Server instantiations (webui) for the same trio.
6. Push the master commit stack (14-15 unpushed, owner).
7. Push internal/journal/cqrs/v0.2.0 tag (CI-blocking, carried from 09-12).
8. Codify the turn-1 checklist (skill or hook, not prose).
9. TQ_RESULT sha semantics ruling for n≥2 same-ID dispatches (carried ×4).
10. Same-ID re-dispatch intent question (carried ×3).
11. Decide whether close-outs for DONE-on-arrival dispatches should be
    skipped entirely (report-volume tension, 03-03 §e).
12. CI green verification of the full unpushed stack on the runner.
13. lint-baseline --check stays green after any future lint edits.
14. Add `-race` to the verification-only dispatch gate template.
15. Consider a per-task `verify-only` contract so re-dispatches skip the
    full gate (cost).
16. docs/status README row formatting: the giant Scope column keeps
    reflowing; consider capped-length rows.
17. Grep docs/status for task ID as a hook-injected context block.
18. Empty CONTRIBUTING.md or delete it (the unread-file split-brain).
19. Session-start ritual as a single scripts/session-start.sh.
20. Carry: NoIndex pin test (webui, from SEO window).
21. Carry: smoke robots.txt/noindex assertion.
22. Carry: SECURITY.md auth matrix row for `?token=`.
23. Carry: FEATURES row for hardening examples (if warranted).
24. Mandate `git diff --stat` re-read before every commit (footer count,
    file set) — mechanical, cheap.
25. Commit-msg hook already enforces exactly-one footer; verify it fires
    for agent commits from this client (not just host git).
26. Add timeout trio assertion to webui smoke (serve uses its own Server).
27. Consider `BaseContext` on example servers for shutdown hygiene.
28. Consider `MaxHeaderBytes` hardening while in the area (tiny).
29. Record the n=2 verdict lineage in TODO row 122's note? (row is closed;
    probably no — note here instead).
30. Sweep TODO_LIST for other items already DONE-on-arrival (row 121 was
    re-dispatched 3×; audit the whole "f20–f24 follow-ups" section).
31. Make close-out reports cite the exact verified line numbers (done here,
    make it a rule).
32. Root-module gate script with clean exit capture (gate.sh proposal,
    carried ×3).
33. Dogfood: this task ID should get one close-out, not two — report-cap.
34. Agent pool: consider claiming `verify:*` task type separately.
35. Carry: daemon-commit attribution gap (session-close bridge open item).
36. Carry: budget bypass ruling (session-close bridge open item).
37. Carry: postgres parity for session-close direct enqueues.
38. Consider adding examples/ to a lightweight CI job (build-only) so
    example rot is caught.
39. Verify examples still compile under GOEXPERIMENT unset (they are
    stdlib-only — quick check would settle it).
40. Add examples/api + examples/sse to check-dead-exports scope? (probably
    no — note only.)
41. Re-check row 122's sibling rows in the same section for staleness.
42. doc: add timeout guidance to SECURITY.md server-hardening section.
43. Consider gosec G114 exclusion note update (both examples now fixed —
    baseline comment may be stale; verify before editing).
44. Close-out report for n=2 could have been 3 lines — this report's
    length is a owner-instruction consequence; propose a SHORT form.
45. Add `date` call before report writing to ritual (was done here, cheap).
46. Index-row check BEFORE drafting the report, not after (03-40's fix —
    apply prospectively).
47. Keep a local scratch note of carried items to dedupe across windows.
48. Vet the "Nth consecutive CONTRIBUTING miss" — actually read it once
    and close the loop for real.
49. Owner question queue hygiene: several §g questions from prior reports
    remain unanswered; consolidate into one TODO row.
50. Consider suppressing further re-dispatches of this ID at the queue.

## g) Questions I cannot figure out myself

1. **CHANGELOG scope:** should example-server hardening get a CHANGELOG
   entry (examples/ are not part of the library surface)? The 03-44 §g1
   ruling request stands; I carried it rather than guessing.
2. **Same-ID dispatches:** when the queue re-dispatches a DONE task ID
   (n≥2), is a fresh close-out report wanted at all, or should the agent
   emit TQ_RESULT with the PRIOR commit sha and write nothing?
3. **CONTRIBUTING.md:** it has been unread at turn 1 for N consecutive
   dispatches. Should it be folded into AGENTS.md, or should the
   dispatch prompt link it explicitly so the miss becomes impossible?
