# Status Report — task 000001a08e6424643bb1a854bcb689f5f4ce

**When:** 2026-09-11 05:02
**Work item:** "module-eval hardening: extend the flake module-eval check to assert the tq-agent-pool unit's Environment carries a non-empty PATH (02:00 f11; flake.nix, deploy/nixos/tq-agent-pool.nix)"
**Outcome:** ALREADY DONE — fix pre-existed, re-verified green, no new changes needed.

## The one-paragraph story

Session-start ritual (git log) immediately showed the work had already shipped:
76ecf9a "fix(nix): module-eval asserts pool unit Environment carries non-empty
PATH" plus its close-out report a5a77c3. This is the third stale re-dispatch of
this shape in two days (cf. 2026-09-10_06-25 and 7d23730 for 000001a08e51). I
re-verified rather than trusted: `nix build .#checks.x86_64-linux.module-eval`
passes, the `poolHasPath` helper is present at flake.nix:226-232 (asserts some
Environment entry matches `PATH=.+` on the deployed-shape pool unit), and
TODO_LIST.md:78 was already `[x]` with a done-annotation. No diff to make, no
work commit needed; this report is the only artifact.

## a) FULLY DONE

- **Work item itself (by a prior session, commit 76ecf9a):** flake.nix
  `checks.module-eval` now evaluates the deployed-shape tq-agent-pool NixOS
  module and asserts via `poolHasPath` that the unit's `Environment` carries a
  non-empty `PATH=...` entry. Guards against a regression to the 2026-09-10
  pool-deploy failure mode (systemd default PATH had no git/go/crush → every
  harvest scan failed).
- **TODO_LIST loop closure (prior session):** item at TODO_LIST.md:78 marked
  `[x]` with a dated done-annotation naming the helper and the check.
- **This session's verification:** `nix build
  .#checks.x86_64-linux.module-eval` green on current master; assertion
  confirmed present and correct-shaped by direct read of flake.nix.
- **Close-out report (this file)** written and committed with the task footer.

## b) PARTIALLY DONE

Nothing partial on this item. The only residue: no NEW commit carries this
task's Task-Queue-ID footer (the fix landed under the prior session's commits).
The report commit below closes that loop.

## c) NOT STARTED

Nothing on this item. (The dispatch was stale; the queue should have closed it
at 76ecf9a's close-out.)

## d) TOTALLY FUCKED UP

Nothing in this task. But the pattern behind it deserves the harsh label:
**the queue keeps re-dispatching already-completed items** — this is the third
stale re-dispatch in ~24h (000001a08e51 twice, now this). Each wastes a full
agent boot (AGENTS.md load, session ritual, verification run). The mechanical
gate can't be the only dedup; something upstream isn't consuming the
`TQ_RESULT: {"status": "already_done"}` answers.

## e) WHAT WE SHOULD IMPROVE

1. **Dedup at dispatch time:** before re-enqueueing, grep TODO_LIST.md and the
   last N close-out reports / `git log --grep` for the item's fingerprint.
2. **Respect the already-done signal:** `TQ_RESULT` bodies like
   `"status": "already_done"` should close queue items automatically instead
   of re-arming them.
3. **Session-ritual first-check:** I ran git log immediately and this paid off
   in one tool call — this should be codified as step zero for every dispatch
   (it effectively already is in AGENTS.md; the queue layer should mirror it).
4. **Close-out report indexing worked** (check-status-index.sh + pre-commit) —
   keep relying on it; it's how stale re-dispatches get detected by grep.

## f) Up to 50 things we should get done next

*Brainstorm, not commitments — most belong in ROADMAP/HARVEST triage, sorted
roughly by impact:*

**Queue/process hygiene (directly implied by this task):**
1. Add a dispatch-time dedup gate: fingerprint each TODO item (repo + text
   hash) against recent commits and `docs/status/*` before enqueueing.
2. Auto-close queue items whose agent report says `already_done`.
3. Add `Task-Queue-ID` cross-ref check: warn when an item is dispatched but a
   close-out report for that ID already exists.
4. Backfill: audit open queue items against TODO_LIST.md `[x]` entries from
   the last 48h; sweep the stale ones.
5. Add a `tq audit` extension that flags dispatched tasks with no journal fact
   after N hours (detect zombie dispatches).

**Pool/ops (noticed in passing, from AGENTS.md known issues):**
6. Runner-only vendorHash drift (2026-09-10 report d1): differential-dump the
   runner's module fetch to diagnose the FOD mismatch class.
7. Add a CI check that fails on any `tq` invocation without explicit
   `TQ_DB` in scratch scripts (production-DB footgun).
8. Document the `agentPath` NixOS module option in the flake's module docs.
9. Smoke-test the pool unit's actual systemd PATH, not just the module eval
   (eval-vs-runtime gap).
10. Consider asserting `Environment` non-empty for `tq-serve` too, not just
    the pool unit.

**Code health (carried from prior close-outs, still open):**
11. Continue shrinking the ~400-finding golangci-lint advisory baseline
    function-by-function on touched files only.
12. Re-triage gosec baseline after the next dependency bump (v2.29.0 pinned).
13. Nightly fuzz campaign: confirm seed corpus growth is still committing.
14. Re-run dead-export audit with corrected substring matching after the next
    API-stabilization push.
15. Sweep TODO_LIST.md items older than one week (AGENTS.md mandate).

**Docs:**
16. Annotate the two 000001a08e51 close-out reports with a pointer to this
    one (they document the same stale-dispatch pattern).
17. Add the "already_done" outcome to the release/VERSION-SURFACES decision
    table for when fixes pre-exist their dispatch.
18. FEATURES.md: confirm the module-eval PATH assertion is inventoried.

**Nix/build:**
19. Consider a flake check that runs `scripts/check-go-mods.sh` so drift
    fails at eval time too.
20. Audit whether any new check in flake.nix needs GOEXPERIMENT=jsonv2
    parity with the devShell.
21. Confirm `nix flake check` runtime stays acceptable as checks grow.

**Testing:**
22. Add a unit test for `poolHasPath`'s negative case (PATH entry removed →
    eval must fail) if not already covered.
23. Property-test: PATH assertion survives `poolSettings` overrides.

**Roadmap fuel (bigger ideas, unrefined):**
24-50. Carried wholesale from the 50-item harvest lists in
   docs/status/2026-09-10_06-25 and a5a77c3 — those lists are fresher and
   were harvested with more context than this single-task session can
   justify re-deriving. HARVEST should route from those, not from here.

## g) Questions I can NOT figure out myself

1. **Who re-enqueues closed items?** The queue keeps re-dispatching
   already-completed work (three instances in 24h). Is there a human/daemon
   step that re-arms items after close-out, and can it read
   `TQ_RESULT.status == "already_done"` to stop? I can't see the enqueueing
   side from inside this repo.
2. **Should this task get a fresh work commit at all?** I treated the prior
   session's 76ecf9a as the canonical work commit and only committed this
   report under the footer. If the queue's cross-referencing requires one
   commit per dispatch carrying the footer, tell me and I'll amend the
   convention going forward (tags/history immutability noted).
3. **Is the runner-only vendorHash FOD mismatch (d1) still open?** It
   predates this session, is environment-specific (CI runner vs local), and
   I deliberately didn't research it per the no-unrelated-research rule —
   but if it's still biting, it outranks everything in section (f).
