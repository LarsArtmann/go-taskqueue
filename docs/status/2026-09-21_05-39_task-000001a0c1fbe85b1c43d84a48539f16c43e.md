# Task 000001a0c1fbe85b1c43d84a48539f16c43e — Score-cache TTL/eviction pass (work window)

Full a)-g) close-out, 2026-09-21 05:39 CEST. FIRST window on this ID: a work
window, not a re-dispatch — the TODO row (166 at dispatch HEAD 2db42cc,
section "Priority-system follow-ups") was unchecked, and the feature shipped
this window. Dispatch tree clean; turn-1 ritual complete (session-start.sh
first action, git log/status/stash, `rg -l <task-id> docs/status/` empty —
fresh ID; CONTRIBUTING.md read in full).

## a) FULLY DONE

- **Aging (the TTL half).** Cached verdicts now go stale:
  `DefaultScoreTTL` = 30d (internal/prioritize/sweep.go), knob
  `SweeperConfig.ScoreTTL` (zero selects the default, negative disables
  aging). A stale item re-joins the next mint EVEN when an old batch
  covered it (`verdictStale` checked before the covered/cached gates in
  `mintRepo` — the refresh is the point); the re-score's upsert overwrites
  in place, so provenance keeps serving the stale verdict until the fresh
  one lands. Aging rides the existing triggers (fresh enqueue, batch
  completion follow-up mint): one refresh batch per TTL window per repo,
  bounded.
- **Orphan eviction (the prune half).** `pruneCache` runs at every scorer
  completion (`onCompleted`) — the cache-growth moment per the work item —
  listing the full cache (`PriorityScores`, new store method) against the
  live working set (PENDING harvested backlog tasks across all projects,
  `liveItemKeys`) and deleting orphaned keys (`DeletePriorityScores`, new
  store method). Orphans are exactly the work item's "stale item texts":
  reworded (re-keyed), checked-off, or terminal-task items whose verdicts
  would otherwise rot forever. Read failures log + skip (evicted on the
  next completion); `SweepStats.CachePruned` counts the evictions.
- **Store surface (contract + both backends, mirrored).** `queue.Store`
  gained `PriorityScores` (full list, item-key order — the cache is small
  by construction, unbounded read is deliberate) and `DeletePriorityScores`
  (IN-list delete, empty list is a no-op, unknown keys not an error,
  returns rows affected); sqlite (sqlite.go, `?` placeholders) and postgres
  (postgres.go, `$N` placeholders, error-wrapped per that file's sibling
  style) implemented; both files' compile-time `var _ queue.Store`
  assertions force the parity. No new package-level exported symbols in
  any facaded package (methods only) — facade parity gate green without
  facade edits.
- **Pins (new tests).** internal/prioritize/sweep_test.go:
  `TestStaleVerdictsRejoinTheBatch` (stale item pulled into the fresh
  batch alongside a new enqueue despite prior batch coverage; fresh
  sibling stays out), `TestAgingDisabledKeepsCoveredItemsOut` (ScoreTTL
  < 0: ancient verdict + coverage keeps the item out),
  `TestPruneEvictsOrphanedCacheEntries` (orphan evicted ONLY at the
  scorer completion, live verdict kept, `CachePruned` = 1; also pins that
  a mint-only sweep does NOT prune). internal/queue/sqlite/store_test.go:
  `TestPriorityScoresListAndDelete` (order, empty-list no-op, unknown-key
  tolerance, exact deletion). postgres conformance_test.go: "priority
  score cache list and prune" subtest mirroring the sqlite canonical.
- **Docs + backlog loop.** AGENTS.md prioritize bullet extended with the
  cache-maintenance paragraph (aging knob, eviction trigger, store
  surface); TODO row checked `[x]` with the gate-citation block;
  test/TODO parse guards green.
- **Battery, rc=0 each:** queue + queue/sqlite + queue/postgres module
  builds and full suites (GOWORK=off, -count=1); prioritize suite
  -count=1 and -race; root build+vet under GOEXPERIMENT=jsonv2 AFTER
  `go mod vendor` (root packages resolve internal/queue from vendor/ —
  the documented trap, hit and healed in this window, see §d3);
  gofmt clean on internal/prioritize + internal/queue;
  check-doc-refs.sh ("doc refs ok"); check-todo-list.sh ok;
  check-facade-parity.sh ("7 facades mirror"); golangci-lint
  `--new-from-rev HEAD` zero on all four touched modules
  (prioritize root package, queue contract, sqlite, postgres — run
  in-module per the no-nested-config rule).

## b) PARTIALLY DONE

- **Postgres conformance not executed locally:** the suite is
  TQ_TEST_POSTGRES-gated (CI's env-gated postgres job runs it). The new
  subtest compiles and is textually parallel to the sqlite canonical
  (the two backends' side-by-side diff IS the contract, ADR-0007); the
  postgres module's build + vet + non-gated tests pass. First live CI
  postgres run is the remaining proof.

## c) NOT DONE / BLOCKED

- Nothing. The work item is fully shipped.

## d) WHAT WENT WRONG (confessions)

1. **Daemon fold ×3 at sub-minute latency.** All three source commits
   (c9d108b: contract + both stores; df87063: sweeper + all test files;
   458df68: AGENTS.md + TODO tick + test fix) are FOOTERLESS
   auto-commit daemon commits — the daemon swept each batch between
   edit and my commit chain despite commit-immediately sequencing.
   Resolution per the 09-52/01-11/04-55 precedent: attribution resolves
   via this footer commit (report + index row); the daemon commits carry
   ONLY this task's files (verified via `git show --stat` on all three
   before relying on them). The single-invocation write+commit chain
   (01-11, re-endorsed 04-24 §e) remains necessary but not sufficient at
   this daemon latency.
2. **First-commit attempt lost the race entirely:** `git commit -am`
   returned "nothing to commit, working tree clean" — 458df68 landed in
   the seconds between my `git status` and the commit invocation. The
   footer commit therefore needed fresh content, which reshaped the
   close-out into this report (the originally planned message-only
   commit over the remaining docs was impossible).
3. **The vendor/ trap, hit as documented:** first prioritize build failed
   with "queue.Store has no field PriorityScores" (root module resolves
   internal/queue from vendor/) — `go mod vendor` healed it, rc=0 after.
   Known-issue bullet AGENTS.md, now personally verified.
4. **Test-design race caught before running:** the first draft of the
   refresh test backdated the cache row BEFORE the sweep that consumes
   the completion fact — `onCompleted`'s `SavePriorityScore` would have
   clobbered the backdate (fact time is fresh), making the test
   green-for-the-wrong-reason or red. Restructured: apply sweep first,
   THEN backdate, then trigger the refresh via a fresh enqueue (a real
   aging pathway — idle sweeps never mint, so a bare third sweep proved
   nothing).
5. **gopls phantoms (known class, ignored):** root-view gopls reports
   ~38 undefined-symbol errors in cmd/tq/main.go (own module, ADR-0017)
   and briefly stale MissingFieldOrMethod on prioritize while its index
   lagged the queue edit; per-module CLI builds are the truth, and all
   four were green.

## e) WHAT COULD BE BETTER

- The fold-proof shape is PROCEDURE, not doctrine: assemble the stop
  artifact (report written, index row staged mentally) BEFORE the first
  commit attempt, then one bash invocation (append index + gates + add +
  commit). Adopted here; the daemon can still win the report file (in
  which case the footer commit carries the index row — accepted by the
  04-17/04-24 reports).
- `mintRepo`'s stale-vs-covered reorder is subtle (stale items bypass
  coverage deliberately); the inline comment + `TestStaleVerdictsRejoinTheBatch`
  - `TestAgingDisabledKeepsCoveredItemsOut` pin both directions — future
    readers should start from the tests, not the gate expression.
- `SweepStats` grew a field (`CachePruned`); cmd/tq's prioritize sweep
  log line does not print it yet (log-only delta, not worth its own
  window — noted §f1 instead).

## f) FOLLOW-UPS FILED / CARRIED

1. FILED as a TODO row this window: CHANGELOG/FEATURES check for the
   score-cache TTL/eviction pass (docs-only, same pattern as the 04-07
   f4 row).
2. Carried, not filed: surfacing `CachePruned` in the cmd/tq prioritize
   sweep log line ("pruned" key, mirroring "verdicts"/"reprioritized")
   — three-line change, rides the next window touching that block.
3. Carried, §g2 material only: harvest's enqueue-time priority seeding
   (internal/harvest/harvest.go:495/:885) and the reprioritize sweep
   read cached verdicts WITHOUT the TTL check — a task enqueued while
   its verdict is stale seeds with the old priority, then self-heals
   within one batch cycle (the enqueue itself triggers the mint that
   re-scores it; `applyVerdicts` corrects the PENDING task at batch
   completion). Works as designed for now; unifying on a shared
   stale-check helper only pays if a second aging consumer appears.

## g) QUESTIONS FOR THE OWNER

1. Is 30d the right `DefaultScoreTTL`? It re-scores each live backlog
   item roughly monthly (AI spend, one batch per TTL window per repo).
   A shorter TTL buys freshness; `ScoreTTL < 0` fully disables aging if
   the owner prefers eviction-only hygiene.
2. Should the postgres conformance battery for this subtest be forced in
   the next local TQ_TEST_POSTGRES window (owner-run) before the next
   release, or is CI's env-gated run sufficient? (This window treats CI
   as sufficient: mirrored code, module gates green.)
