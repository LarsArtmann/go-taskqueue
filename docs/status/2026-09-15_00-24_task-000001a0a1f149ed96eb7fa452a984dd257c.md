# Journal-drift audit smoke — dispatch verification + advisory smoke landing (third pass)

- **Date**: 2026-09-15 00:24
- **Task**: 000001a0a1f149ed96eb7fa452a984dd257c
- **Verdict**: work item was already fully done (feat 6b692f2, first close-out,
  second-pass verification 00-16). This dispatch closed the one
  agent-executable residue the second pass named: the ADVISORY ci-local smoke
  over a seeded fixture (O5 ruling honored — warning, never a gate).

## a) FULLY DONE

- **Advisory smoke landed**: `scripts/smoke/journal-drift.sh` — hermetic
  scratch DB, three phases: (1) full lifecycle (2× enqueue + `worker --once`)
  must report "no drift" from `tq audit --journal`; (2) python3 seeds drift by
  corrupting the tasks table directly (status/attempts/priority/dedup_key,
  per-row keys to respect the dedup UNIQUE constraint) and all four diffed
  fields must surface in the report; (3) `--json` output must carry the drift
  field. Production journals are never touched (scratch `--db` throughout).
- **ci-local wiring**: `scripts/ci-local.sh:163` runs it as an explicitly
  ADVISORY step — a failure prints a WARNING, never fails the gate (O5).
- **Docs loop closed**: CHANGELOG Added entry for `tq audit --journal` +
  smoke; AGENTS.md smoke list row added; the done `[x]` TODO row DELETED per
  the file's own DONE-ITEMS-ARE-DELETED convention (the second pass had left
  it checked, contradicting the file header's owner ruling).
- **Verification**: `./scripts/test-cmd-tq.sh` green (all cmd/tq tests incl.
  the journalaudit fixtures); root `go build ./...` + `go vet ./...` green;
  `check-todo-list.sh` green; `bash -n` on both scripts green; smoke PASS
  end-to-end.
- **Commits**: smoke script + ci-local step via daemon commit 03f0140 (daemon
  won the known race); docs via explicit commit 31b42e5 carrying the
  Task-Queue-ID footer. Never pushed.

## b) PARTIALLY DONE

- Nothing for this task. The feature, tests, ruling, and now the advisory
  smoke are all landed. Remaining delta items from the 00-16 report (requeued/
  dead-letter replay edge cases, repair-mode ruling, postgres parity,
  `--json` golden depth) are separate rows, untouched by design (smallest
  correct change).

## c) NOT STARTED

- The 00-16 report's delta items 2-6 (see §b) — all already tracked or
  owner-gated elsewhere; this dispatch deliberately did not grow scope.

## d) TOTALLY FUCKED UP

1. **Daemon lost the footer race** (AGAIN, the known shape): the daemon
   committed `scripts/smoke/journal-drift.sh` + `scripts/ci-local.sh` as
   03f0140 "chore: auto-commit 2 changed file(s) (heuristic)" minutes before
   my explicit commit, so the queue↔git cross-reference footer (31b42e5)
   does NOT sit on the commit carrying the actual feature code. The
   AGENTS.md lesson "re-check `git status --short` immediately before
   `git add`" was followed and STILL lost — the daemon commits DURING the
   staging window. Mitigation would have been: stage+commit within one shell
   chain, or amend the footer onto the daemon commit before it ships
   (already pushed? no — never pushed, but tags-are-immutable rules still
   apply to amends of daemon commits on master lineage; accepted as-is and
   recorded here instead).
2. **Two smoke-iteration burns** seeding drift: first UPDATE hit the
   dedup_key UNIQUE constraint (one literal key for all rows), second used a
   non-existent `seq` column. A quick `pragma table_info` peek before writing
   the python would have made it one shot.
3. **Turn-1 stash list miss AGAIN** (third consecutive report recording
   it): ran `git log` + `git status`, skipped `git stash list`. This is a
   checklist item, not a suggestion — the recurring miss is now documented
   three times by the same task ID.

## e) WHAT WE SHOULD IMPROVE

- "Advisory when it lands" smoke items must land WITH the feature commit —
  three dispatches were needed to close a one-script gap.
- Smoke scripts that touch the DB schema directly should grep the schema
  first (or reuse the Go test's seedDrift helper via a tiny `go run` shim)
  instead of guessing columns/constraints.
- DONE-on-arrival re-dispatch (this is pass #3 on a finished item): the
  queue-side done-guard row already exists (TODO "DONE-on-arrival dispatch
  dedup") — it keeps paying for itself every time it does NOT exist yet.
- When the daemon wins the race, the follow-up docs commit should say so
  explicitly in its body (this one does) so the cross-reference stays
  auditable from git log alone.

## f) NEXT (delta items; canonical backlog lives in the repo's TODO_LIST.md)

1. Queue-side DONE-on-arrival dispatch dedup (the 5th+ same-ID burn — this
   row is already in TODO_LIST; prioritize it).
2. `tq audit --journal`: document or close the legacy thin-facts replay gap
   (requeued/dead-letter attempt derivation edge cases).
3. Repair-mode ruling: owner call — the TODO row says "no repair mode yet";
   mint a design doc or an explicit never-decision.
4. Postgres journal-drift parity (`AppendFact` gap already tracked).
5. `tq audit --journal --json` golden contract test (the smoke only greps
   for a `"drift"` field).
6. Daemon-race mitigation: a commit-msg hook or ci-local check that a
   `Task-Queue-ID` footer exists SOMEWHERE within the daemon commit +
   explicit-commit pair touching the same files (currently unenforced).
7. Smoke fixture seeding via the Go helper (`go run` shim over seedDrift)
   instead of hand-rolled python schema surgery.
8. Fold this report into the docs-health pass (annotate the two prior
   same-ID reports when archiving).

## g) QUESTIONS (cannot self-answer)

1. Daemon vs explicit commit race: is amending the footer onto a daemon
   commit that is already on local master (unpushed) sanctioned, or is the
   footer-on-follow-up-commit convention the ratified shape?
2. Should the journal-drift smoke's drift phase ever be upgraded from
   python3 schema surgery to a Go-seeded fixture for hermeticity, or is the
   runner-available python3 dependency acceptable long-term?
3. For triple-dispatched tasks: does a no-op pass #3 still count as queue
   progress, or should the dispatcher suppress same-ID mints once a `[x]`
   row + landing commit both exist?

Task-Queue-ID: 000001a0a1f149ed96eb7fa452a984dd257c
