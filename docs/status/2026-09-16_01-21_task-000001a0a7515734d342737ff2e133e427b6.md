# Close-out self-review: httpapi parity hardening (task 000001a0a751)

**Date**: 2026-09-16 01:21 CEST · **Task**: TODO_LIST row "httpapi parity
hardening: `X-Content-Type-Options: nosniff` + failed-bearer-auth lockout
decision on `tq api` (03-05 report f2/f3/g2)" · **Work commit**: `ac9da37`
(exactly one well-formed `Task-Queue-ID` footer, commit-msg hook passed)
· **Report format**: Markdown per explicit task instruction (status-report /
brutal-self-review skills default to styled HTML; user path wins, override
flagged here per skill contract).

Scope discipline: this report covers THIS task's work and what I noticed in
passing. No unrelated research was done; every §f item is grounded in
something this session actually observed.

---

## a) FULLY DONE

1. **nosniff on every `tq api` response** (`internal/httpapi/httpapi.go`,
   `guard`): `X-Content-Type-Options: nosniff` set before the auth check, so
   401s, 429s, validation 400s, and unknown routes all carry it;
   `Cache-Control: no-store` kept on authenticated responses. Pinned by
   `TestNosniffOnEveryResponse` (5 cases: auth failure / healthz / stats /
   validation error / unknown route) — green under `-race`.
2. **Lockout DECIDED + IMPLEMENTED (f3/g2)**: the API is the one surface
   designed for non-loopback exposure and it writes, so exemption was
   rejected. `authRateLimiter` (internal/httpapi/httpapi.go): 3 failed
   bearer auths per client IP → 60 s 429 + `Retry-After` on ALL routes,
   checked before the token compare; any successful auth resets strikes;
   bounded map mirrors webui's `writeRateLimiter` (idle prune on contact +
   global idle sweep + LRU eviction at maxKeys; a live lockout survives both).
   Pinned by `TestAuthLockout` (incl. valid-token-during-lockout → 429 and
   expiry recovery), `TestAuthLockoutIsPerClient`, `TestAuthStrikesResetOnSuccess`.
3. **Gates, cited**: root-module `go build` + `go vet` + full `-race` suite
   green (all packages incl. `internal/httpapi` 1.9 s, `internal/webui`,
   `internal/e2e`); `GOOS=windows go build` green (run at close-out);
   `examples/api` module builds green (confirming `New()` remains the only
   constructor path); gofmt clean; `check-todo-list.sh` green.
4. **Docs wired**: SECURITY.md gained the `tq api` section (token matrix,
   lockout semantics, nosniff, honest residual risks: cleartext token on
   plain HTTP, NAT-neighbor lockouts); FEATURES.md api row updated;
   CHANGELOG `[Unreleased]/Added` entry; TODO_LIST row checked off with
   evidence; AGENTS.md security bullet records the decision as CLOSED
   (do-not-re-open) so a future session cannot re-litigate g2.
5. **Lint hygiene on my delta**: new mnd findings avoided via named constants
   (`authMaxHits/authLockout/authIdleKeep/authMaxKeys` — webui's raw literals
   stay in the baseline, untouched); the one wsl_v5 finding my code added
   (`var` cuddle in `boundLocked`) caught by `lint-baseline --check` and
   fixed; scoped re-run proves `internal/httpapi` now carries exactly the 3
   pre-existing mnd findings and 0 wsl_v5.

## b) PARTIALLY DONE (done, but with honest caveats)

1. **Queue↔git cross-reference is weak**: the auto-commit daemon raced me
   three times — the substantive diff (httpapi.go, tests, SECURITY/
   FEATURES/CHANGELOG) sits in footer-less `chore:` commits `dd26e4c` and
   `a09bcf6`; the footer commit `ac9da37` carries only the 7-insertion tail
   (wsl fix, TODO checkoff, AGENTS note). A diff-based cross-reference sees
   a 3-file commit for this task. I considered amending the daemon commits
   (unpushed) and rejected it: scripted history edits under the live daemon
   are a documented hazard, and the footer-on-tip convention closes the
   loop textually. Residual risk accepted, not eliminated.
2. **Lockout tests sleep instead of using the seam I built**: I added
   `nowFunc` to the limiter (copied from webui) and then mirrored webui's
   `lockout = 40ms` + real `time.Sleep` pattern anyway — ~100 ms of real
   sleeping per test run and a theoretical flake on a loaded runner.
   Injecting a fake clock would make the tests instant and hermetic.
3. **Error-contract inconsistency left visible**: the API's handlers answer
   with `{error, fix}` JSON (`writeError`), but auth failures (401) and the
   new 429 use plain-text `http.Error`. Pre-existing shape, but the
   hardening made the inconsistency more prominent on one surface.
4. **Standing instruction from the source report missed in-session**: row 18
   of the 03-05 report says "triage the gosec advisory findings in
   httpapi.go (G118, contextcheck) next time that file is touched" — I
   touched the file and did not triage until this close-out. Triage by
   citation: the flagged line is the canonical fresh-context graceful-
   shutdown pattern that gosec G118 is already exclude-listed for in
   ci.yml/`.golangci.yml` (AGENTS.md gosec triage); contextcheck flags the
   identical shape. No code change owed. Still — I read that report's rows
   2/3 and skipped 10/18 without even flagging them.
5. **Dead `ptr` helper** (03-05 row 10, gopls pings it on every edit of
   `httpapi_test.go`): still there. Separate row, correctly out of scope,
   but I was in the file and left a known papercut untouched.

## c) NOT STARTED (observed, deliberately out of scope)

1. **Live-socket smoke for `tq api`** — no `scripts/smoke/api.sh` exists;
   the feature has only ever run under `httptest`. Nothing has exercised
   401→401→401→429→Retry-After→expiry over a real TCP socket.
2. **`internal/httpauth` extraction** (03-05 row 7): bearer-token extraction
   still exists twice (webui `presentedToken` vs httpapi `bearerToken`) and
   this task made it worse in kind — there are now TWO structurally
   identical rate limiters whose eviction/prune logic is copy-paste. A bug
   fix to one (e.g. the LRU eviction) can silently miss the other.
3. **TODO rows 88-90** (ci retry-on-transient wrapper; the small rate-limit
   batch incl. the HTTP-date Retry-After fixture that touches my Retry-After
   code path; ci.yml concurrency group) — read in passing, untouched.
4. **Lint-baseline regeneration with triage notes** — the gate is red from
   concurrent sessions' growth (see §d); regenerating without owning the
   triage would have blessed unreviewed findings, so I left it.
5. **README dead citations** — `check-doc-refs.sh` exits 1: README cites
   `docs/operations/task-dispatch-contract.md` and `scripts/tq-claim.sh`,
   neither exists (verified present in HEAD's README, absent from HEAD's
   tree — pre-dates this session).

## d) TOTALLY FUCKED UP

Nothing I shipped. Specifically: no test regressions, no new baseline
findings (proven by scoped runs), no broken docs, no scope creep into other
rows. The nearest thing to "fucked" is the **state I found and left**: two
gates are red on the tree any next pusher will hit —

- `scripts/lint-baseline.sh --check` fails on growth rows from CONCURRENT
  sessions' work landing after the 2026-09-15 regen (`cmd/tq` tagliatelle/
  varnamelen, `internal/executor` mnd/gochecknoglobals/varnamelen,
  `root/mnd` — files: journalaudit.go, redact.go, sidecar.go, agent.go,
  main.go, …; none touched by me),
- `check-doc-refs.sh` fails on the two dead README citations.

Both pre-date this session (verified against HEAD before my changes). But
"pre-existing" only means I didn't cause it — ci-local is the pre-push gate,
and the next person to run it will eat both failures.

## e) WHAT WE SHOULD IMPROVE (my own process)

1. **Commit per step, not per task**: the global rule "commit per task when
   explicit commits are authorized; re-check `git status --short` immediately
   before `git add` because the daemon races explicit commits" exists
   precisely for what happened. I batched ALL edits (code → tests → 4 docs)
   and committed once at the end — the daemon swept three footer-less
   commits out from under me. Root cause: I optimized for one clean commit
   over daemon-race hygiene. The first edit should have been committed
   before the first doc was touched.
2. **Run the scoped linter on new code before the gate dance**: I checked
   mnd early (constants) but had never heard of wsl_v5 — it's enabled in
   `.golangci.yml` and my copy-paste-from-webui pattern (`var` cuddle) is
   exactly its shape. One `golangci-lint run --enable-only mnd,wsl_v5` right
   after writing the file would have saved a fix round.
3. **Use the seams you build**: added `nowFunc`, didn't use it in tests (§b2).
4. **Verify platform/PoC blast radius early**: windows cross-compile and
   `examples/api` build ran only at close-out (both green, zero rework —
   luck plus a small surface, not method).
5. **Scan the whole source report when it feeds a work item**: rows 10/18
   were standing instructions anchored to the same file; flagging them in
   the work commit (even as "not done because") would have made the
   close-out honest by default instead of by recall.

## f) NEXT (brainstorm — up to 50; sorted by impact; ROADMAP/HARVEST fuel, not commitments)

**P0 — gates and red state**
1. Regenerate `.golangci-baseline.txt` deliberately WITH triage notes for
   the concurrent drift rows (cmd/tq +2 rows, internal/executor +4 rows,
   root/mnd +1) — until then ci-local fails at `lint-baseline --check`.
2. Fix or consciously remove the two dead README citations
   (`docs/operations/task-dispatch-contract.md`, `scripts/tq-claim.sh`) —
   `check-doc-refs.sh` exits 1.
3. Land TODO row 88: ci-local retry-on-transient-foreign-break wrapper.

**P1 — this task's direct follow-ups**
4. `scripts/smoke/api.sh`: live-socket lockout smoke (spawn `tq api` on a
   scratch DB — remember `TQ_DB` env trap — 3×401 → 429 + Retry-After →
   expiry → 200); wire into ci-local smokes.
5. Extract `internal/httpauth`: bearer extraction ×2 AND the two twin
   limiters (writeRateLimiter/authRateLimiter) behind one tested
   implementation with per-surface policy knobs (strike trigger, guarded
   scope).
6. De-sleep the lockout tests via `nowFunc` injection (httpapi; webui's
   TestWriteRateLimit* has the same pattern).
7. Make 401/429 error bodies match the surface's `{error, fix}` JSON
   contract (or document why plain text is right for auth planes).
8. Pin `?token=`-query auth failure also strikes (one table case — the
   bearerToken fallback path is currently only pinned on success).
9. Pin "lockout covers POST /api/v1/tasks" (I only tested GET healthz; the
   wrap is route-agnostic but ALL-routes is the contract).
10. Port webui's bounded-map tests (maxKeys eviction, lockout-survives-
    eviction, idle sweep) or let item 5 retire the duplication — the
    eviction logic currently has tests only on the webui copy.
11. Triage row 10's dead helpers (`ptr` in httpapi_test) — delete or inline.
12. Row 18 follow-through: the contextcheck/G118-by-shape triage from §b4
    should be recorded where row 18 lives, not only in this report.
13. `tq api` help text: mention the lockout (429/Retry-After) so operators
    whose producer misconfigures a token get a hint from `--help`.
14. `tq doctor` (or `serve` startup log): surface recent auth-lockout WARNs
    so a misconfigured producer is discoverable without journal digging.

**P2 — noticed in passing, real value**
15. Land TODO row 89's small rate-limit batch (it contains the HTTP-date
    Retry-After fixture that touches this task's header code path).
16. Land TODO row 90 (ci.yml concurrency group — the daemon pushes in
    bursts; I watched three daemon commits land inside four minutes).
17. Teach the queue↔git story about daemon-folded commits: either the
    cross-reference follows `chore:` neighbors, or sessions must commit
    per-edit (item 18).
18. Make per-edit committing the default for explicitly-authorized queue
    tasks (this session's §e1 lesson, codified before it recurs).
19. Name the `1<<20` body cap and the two `5 * time.Second` timeouts in
    httpapi (shaves 3 baseline mnd rows to zero in a file that gets
    touched often).
20. webui/auth.go `var oldest *writeStrikes` cuddle — same wsl_v5 shape,
    currently the only root/wsl_v5 baseline row; fixing it zeroes the row.
21. Pass `s.log` into the limiter instead of package-level `slog.Warn`
    (webui does the same; minor testability/logging-consistency win).
22. Consider `Retry-After` on ALL 429s (webui + api) from one helper —
    currently computed as `int(retry/time.Second)+1` in two places.
23. `examples/api` PoC: exercise the lockout there too, or note in its
    README that the PoC predates the hardening.
24. Split-brain watch: `webui` 403-CSRF-strikes vs httpapi 401-auth-strikes
    share semantics but not vocabulary — one DOMAIN_LANGUAGE.md paragraph
    ("strike/lockout") would stop the next implementation from inventing a
    third word.
25. SECURITY.md per-surface response-header matrix (03-05 row 4, Medium —
    still open; my §a4 added the api section but not the matrix).
26. TODO row 89's "stats parked JSON contract test" — adjacent to the api
    stats endpoint this task touched.
27. `TestAuthLockoutIsPerClient` could also assert the other client's
    strike count stays zero (isolation vs merely success).
28. Add a CHANGELOG note style rule: behavior changes for external API
    consumers (like new 429s) get an explicit "behavior change" flag — this
    one is filed under Added.
29. When the next httpapi change lands: re-run `check-facade-parity.sh`
    reflexively (cheap; no facade exists for httpapi today, and if one
    ever appears the lockout must ride it).
30. Consider a tiny integration test that boots `httpapi.Server` via
    `httptest.NewServer` (real socket, still in-process) — closes most of
    item 4's gap without a shell smoke.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Does any live producer actually call `tq api` today** (SystemNix
   fleet, scripts, cron)? A producer with a stale token now gets 60 s
   blackouts after 3 attempts — invisible from this repo, and I found no
   consumer inventory anywhere in docs/.
2. **Baseline regen ownership policy**: when concurrent feature sessions
   grow the lint baseline (as happened before my session), is the intended
   ritual (a) each feature session regenerates with triage notes, or (b) a
   dedicated periodic sweep? I chose not to regenerate unowned growth;
   if (b) is the rule, a standing TODO row should say so.
3. **Daemon-fold vs the queue cross-reference**: is footer-on-tip
   (substantive diff in footer-less `chore:` commits) acceptable queue
   bookkeeping, or should unpushed daemon commits carrying a task's work be
   reworded to add the footer (a history-policy call I currently answer
   "no" out of caution)?

---

**Self-review scorecard (skill questions)**: forgot → row 18/10 standing
instructions, per-step committing; stupid → batched edits under a live
daemon; lied → no — every gate claim above cites a run from this session;
ghost systems → none created (all new code is wired, tested, and
documented); split brains → deepened one known one (twin limiters, §c2),
converged none; scope creep → held (rows 10/18/88-90 left alone and
listed); removed something useful → no.
