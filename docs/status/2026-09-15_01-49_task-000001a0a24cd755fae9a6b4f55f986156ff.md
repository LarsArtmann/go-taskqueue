# Status Report — golangci-lint pin unification (task 000001a0a24cd755fae9a6b4f55f986156ff), THIRD dispatch

**Date:** 2026-09-15 01:49 · **Session type:** verification-only no-op (DONE-on-arrival, re-confirmed twice before me)
**Work item:** TODO_LIST row 63 — "Unify the golangci-lint version pin (duplicated in ci.yml and ci-local.sh) into one source so the two can never drift (08:47 report f22/f23)"

## Executive summary

The task was already shipped in 550fec1 (2026-09-11) and had already been
re-verified by TWO other concurrent sessions today (01:44 and 01:46 reports,
both filed against this exact task ID). This session independently re-confirmed
the same facts: the single pin literal lives at `.github/workflows/ci.yml:82`
(`@v2.13.2`), `scripts/ci-local.sh:96-98` derives it at runtime by grepping
ci.yml, a repo-wide sweep finds zero other pin literals, the working tree is
clean, and TODO row 63 is `[x]` with a citation-bearing done note. No code
changed; no new work commit exists. This is now the THIRD paid dispatch of an
item that was done before any of today's three sessions started.

## a) FULLY DONE

- **The original work item** (shipped 550fec1, 2026-09-11): one source of truth
  for the golangci-lint version. ci.yml:82 owns the `@v2.13.2` literal;
  ci-local.sh grep-derives the pin when `golangci-lint` is not on PATH
  (ci-local.sh:96) — the two can no longer drift by construction.
- **Re-verification (this session):**
  - Read the derivation live: `grep -oE 'golangci-lint/v2/cmd/golangci-lint@v[0-9.]+' .github/workflows/ci.yml | head -1` → `v2.13.2`.
  - Repo-wide sweep: zero other pin literals for golangci-lint.
  - `git status` clean at 61423e4; no uncommitted parallel work in flight at session start.
  - TODO_LIST.md:63 checked `[x]` with today's re-verification note (620023a).
- **TODO loop closure:** nothing to close — already closed correctly with
  citations (commit + grep evidence), per the repo's claims-carry-citations rule.

## b) PARTIALLY DONE

- **Fallback-path coverage:** ci-local.sh's install-from-ci.yml path (lines
  96-99) is exercised only when `golangci-lint` is absent from PATH. None of
  the three sessions (including mine) ran that path end-to-end; it remains
  verified by reading, not by execution. Low risk (simple grep+install), but
  it is the one untested branch of the unification.

## c) NOT STARTED

- **Hardening the derivation mechanism itself** (proposed in both prior
  reports, still open): the grep silently yields empty on a future ci.yml
  reword, degrading to `@unknown` in an error message rather than failing
  loud. The `v2.13.2` literal is single-sourced but the _extraction_ is
  unguarded. Not started; correctly belongs as a small TODO item, not drive-by
  work in a no-op session.
- **Mint-time done-check:** the queue minted this task at least three times
  after its TODO row was checked. No gate exists at mint time (or dispatch
  time) that reaps tasks whose row is already `[x]`.

## d) TOTALLY FUCKED UP

Nothing in the _code_ is fucked up — the deliverable is correct and verified
three times today. What is fucked up is the **dispatch loop**, and my session
adds a data point rather than a fix:

1. **Third paid dispatch of a done item.** The queue minted
   000001a0a24cd755fae9a6b4f55f986156ff at least three times (01:44, 01:46,
   01:49 reports) for work completed 2026-09-11. Each session spent a full
   agent run re-verifying the same four facts. The 01:46 report already named
   this "the re-dispatch loop, 3rd occurrence" — it was describing the second
   occurrence at the time; I am the third.
2. **My own near-misses, honestly:**
   - I did not check for prior reports against this task ID _before_ starting;
     the 01:44/01:46 reports were sitting in docs/status/ and would have told
     me this was the third lap in the first minute. I found them only while
     indexing this report. A `ls docs/status | grep <task-id>` should be step
     zero of every queue-dispatched session.
   - Silent-zero risk in my own verification: my repo-wide pin sweep would
     have "passed" vacuously if the grep pattern were wrong (zero matches
     looks identical to "unified"). I mitigated by positively confirming the
     ci-local.sh derivation line prints v2.13.2, but the check is not
     fail-loud by construction.
3. **Index duplication smell (noticed in passing):** docs/status/README.md now
   carries three index rows for the same task ID. Nothing gates that; the
   index is growing same-task shims. Cosmetic, but it erodes the index's
   value as a dedup surface.

## e) WHAT WE SHOULD IMPROVE

1. **Fail-loud pin derivation:** in ci-local.sh, treat an empty grep result as
   a hard error ("cannot derive golangci-lint pin from ci.yml — ci.yml:82
   reworded?"), never `@unknown` best-effort.
2. **Session-start ritual extension (queue tasks):** before any work, list
   existing reports for the task ID and the TODO row state; abort with a
   no-op report if both say done. Cheaper than a paid re-verification lap.
3. **Mint-time or worker-time DONE check:** the harvester/sweeper could skip
   minting (or the worker fast-refuse) when the item's checkbox is `[x]` —
   prune-stale already cancels PENDING tasks whose item got ticked; the gap
   is the mint→dispatch window.
4. **Done-on-arrival report template:** all three reports today are ~80%
   identical. A one-paragraph "confirmed done, evidence: X" template would
   cut the cost of the unavoidable laps.
5. **Index dedup:** check-status-index could WARN on a second index row for
   the same task ID (not block — history is legitimate — just visible).
6. **Positive-form verification habit:** for "there is exactly one N" claims,
   assert count == 1, not "sweep found nothing else". Applies to pin checks,
   footer checks, and facade-parity reasoning alike.

## f) Up to 50 things we should get done next

Sorted roughly by impact; the top items are actionable TODO_LIST material,
the tail is ROADMAP/brainstorm fuel (docs-health HARVEST should apply
extra routing rigor below ~#15).

1. **Fail-loud pin derivation in ci-local.sh** (empty grep = hard error) — §e1.
2. **Prune-stale / sweeper gap:** cancel or fast-refuse PENDING tasks whose TODO item is `[x]` at claim time (done-on-arrival killer).
3. **Session-start ritual:** grep docs/status for the task ID before starting queue work (add to AGENTS.md session-start bullet).
4. Exercise the ci-local.sh fallback path once in a smoke (unset PATH pin, confirm install-from-ci.yml works).
5. check-status-index WARN on duplicate task-ID index rows.
6. Done-on-arrival report template in AGENTS.md (one-paragraph no-op format).
7. Consider a `tq audit` check: tasks dispatched N times with zero deliverable commits across all attempts (re-dispatch loop detector).
8. Extract the ci.yml golangci-lint pin into an env var / action input consumed by both grep sites (alternative to §e1 if grep keeps drifting-adjacent risk).
9. Add a repo-wide gate test for "exactly one golangci-lint pin literal" (count == 1, fail-loud) — pins the unification itself.
10. Sweep AGENTS.md for stale snippets referenced by earlier reports (01:31 report flagged snippet drift) — one audit pass.
11. Verify the `lint-baseline.sh --check` gate still passes at current HEAD after the pin work (regen policy: only with an owning change).
12. Track the 429-runbook / waves-serialization follow-ups from the templ-components consumer sweep rails (AGENTS.md, ADR-0018) — check TODO_LIST coverage.
13. Review whether `head -1` in the pin derivation should be `head -n1`-robust against multi-line ci.yml edits (cosmetic robustness).
14. Document the re-dispatch policy ruling: after N no-op dispatches, who cancels the task? (needs owner input, see §g1.)
15. Consider a `tq doctor` check for tasks whose dedup key hashes text of now-`[x]` items (stale-suppression visibility).
16. Backup branch hygiene: confirm no `git tag --no-merged master` forks exist before the next release (AGENTS.md history-rewrite policy checklist item d).
17. Postgres backend: TQ_TEST_POSTGRES parity run on CI after recent store changes (env-gated job) — confirm still green this week.
18. Facade parity gate: run `scripts/check-facade-parity.sh` standalone to confirm zero findings at HEAD.
19. Regenerate evidence-archive SHA256SUMS discipline reminder into CONTRIBUTING-adjacent docs if any archive changed recently.
20. Vet the `TQ_RESULT` file-channel adoption status: is the legacy stdout fallback ready to delete yet (AGENTS.md says "only after the live pool shows derived outcomes")? Check the live pool.
21. `crush run --reasoning-effort` misleading-error upstream issue (AGENTS.md flags it as an upstream-issue candidate) — file it or drop it.
22. Re-assess `request_timeout` knob need: zero reasoning-stall DLQ entries since 2026-09-14 — confirm the knob stays documented-but-unused.
23. go-cqrs-lite `claiming/` module: re-check upstream progress toward queue-module parity; re-open ADR only on conformance parity (AGENTS.md bar).
24. examples/embed rot guard: confirm ci-local still builds it after the latest facade bumps.
25. `check-dead-exports.sh` advisory audit freshness (substring-matching rule honored).
26. Nix runner-only vendorHash class (2026-09-10): differential-dump tooling idea — still unfixed class; document or script it.
27. SystemNix `Environment=GOEXPERIMENT=jsonv2` on tq-agent-pool (owner fix flagged 2026-09-11) — verify whether it landed; if yes, retire the AGENTS.md "until then" caveat.
28. Same for repo `.tq-verify` prelude-free status after that unit-env fix (AGENTS.md notes it is blocked on the fix).
29. Crush client/server mode per-repo experiment (2026-09-14 blocker list) — run or formally park it.
30. Session-close bridge open items: trigger automation (crush #3146), daemon-commit attribution gap, budget bypass, postgres parity (design doc's open list).
31. Prioritize sweeper: confirm no stuck `prioritize:` dedup keys after recent dead batches (dead batch recovers only on key-set change — human surface).
32. dlqfix: confirm no second-death tasks waiting silently (dedup-is-forever rule) — periodic `tq dlq` review.
33. Web UI: re-run `check-webui-css.sh` gate freshness after any templ-components bump this week.
34. Nightly fuzz campaign: confirm last nightly committed seeds or clean (fuzz.yml CAMPAIGN SETUP FAILED class).
35. Baseline regen hygiene: 500-finding baseline is fresh (2026-09-14) — nothing due; do not regenerate without an owning policy change.
36. Docs: CHANGELOG entry coverage for the pin-unification commit (550fec1 message bundled many things — verify CHANGELOG mentions the unification).
37. FEATURES.md: confirm the lint-infrastructure features reflect the single-pin source.
38. Consider `justfile` deprecation audit completion (AGENTS.md says migrate to flake.nix where they still exist).
39. ROADMAP harvest: route §f items 13+ that are ideas-not-tasks into ROADMAP.md.
40. TODO_LIST HARVEST: pull items 1-5 of this report into TODO_LIST.md as bounded tasks (docs-health HARVEST mode).
41. Audit whether `grep | head -1 | cut -d@ -f2` derivation is covered by any shellcheck/lint gate in ci-local (scripts are Go-gated, shell is not).
42. Consider pinning golangci-lint via go.mod tool directive (Go 1.26 tooling) as the cleaner single source — design note before touching anything.
43. Verify the commit-msg hook (exactly one Task-Queue-ID footer) still fires on this session's commits.
44. Confirm the daemon did not fold any of today's three reports unindexed (the known hole; check `git log --stat` for chore commits carrying docs/status files).
45. Sweeper cursors health check: `tq watermarks show` — review, reviewfix, status, dlqfix-sweeper, prioritize-sweeper lags.
46. Budget projection sanity after three wasted dispatches (each spent agent tokens): confirm today's spend stayed under the daily cap and note amortization.
47. Delete-or-archive stale scratch fixtures if any daemon-committed negatives slipped in this week (AGENTS.md warning class).
48. `tq top` / live pool: quick glance that the production pool is healthy after today's sessions (no dead-pool ticks).
49. ADR-0018 sweep cohort TSV freshness check (fan-out script `--self-test`).
50. Document "exactly-one" verification convention (count assertions over absence sweeps) in the conventions section of AGENTS.md.

## g) Questions I can NOT figure out myself

1. **Re-dispatch sanction:** this task was minted and paid for three times
   after completion. Should the queue get a mint-time `[x]`-check (item 2
   above), a worker-time fast-refuse, or is re-verification-by-dispatch an
   accepted cost you want kept for paranoia value? I cannot judge the
   intended paranoia budget.
2. **Pin-mechanism policy:** is grep-derivation in ci-local.sh the _final_
   mechanism (and should only be hardened fail-loud), or would you prefer the
   pin move to a go.mod `tool` directive / env var owned once? The second is
   a bigger change than this task warrants, so I did not start it.
3. **Advisory fail-loudness scope:** should the empty-grep hard-error (§e1)
   apply only to the golangci-lint pin, or do other `grep ... | head -1`
   derivations in scripts/ deserve the same treatment (a small sweep would
   find them)? Scope ruling needed before I touch shared scripts.

## Verification evidence (this session)

- `git log --oneline -8` → HEAD 61423e4, clean tree.
- Derivation check: ci-local.sh:96-98 greps ci.yml for the pin; live grep → `v2.13.2`.
- Repo-wide pin sweep → zero other literals; exactly one source (ci.yml:82).
- TODO_LIST.md:63 → `[x]` with citation (620023a).
- No code changed by this session; no work commit (nothing to commit — the
  deliverable predates the session by four days).
