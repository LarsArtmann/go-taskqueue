# Status Report — Verify-Only Repeat Dispatch: Lint-Baseline Spike Coverage (task 000001a0d063)

- **Window**: 2026-09-24 00-46 (verify-only re-dispatch; third window on this task ID)
- **HEAD at verify**: 00734f91
- **Scope**: repeat dispatch of the ADR-0019 lint-baseline coverage task. Work was already DONE and committed by the 00-36 (regen) and 00-41 (verify) windows. Per the AGENTS.md repeat-dispatch ruling, this window ran verify → cite → stop.
- **Fresh delta this window**: clean-cache `./scripts/lint-baseline.sh --check` at HEAD 00734f91 → **rc=0, "1410 findings vs baseline 1410"** (`golangci-lint cache clean` first, per the AGENTS.md clean-cache ruling). Evidence: /tmp/lb-check-verify.log (session-local).
- **Coverage proof (inherited, re-checked)**: `rg "sqlitev4|cqrsqlite" .golangci-baseline.txt` → rows present from line 57 (cqrsqlite: cyclop, err113, errcheck, gci, gocognit, …). The gate is NOT blind to the spike modules.

## a) FULLY DONE

1. **The work item itself** (by prior windows, verified this window): 2026-09-23 eighth baseline regen admitting `internal/queue/sqlitev4` (152 findings / 17 rows) and `internal/queue/cqrsqlite` (122 findings / 13 rows); gate green clean-cache; TODO_LIST row 33 marked `[x]`; status reports 00-36 + 00-41 filed and indexed.
2. **This window's verification**: fresh clean-cache `--check` green at current HEAD (1410 vs 1410, rc=0); coverage grep re-run; working tree confirmed clean before stop.

## b) PARTIALLY DONE

Nothing under this task ID. (The underlying S1→S4 platform adoption is its own tracked work; out of scope here.)

## c) NOT STARTED

Follow-ups already backlogged by the 00-41 report (§f there) — this window re-confirms them as not started; no new ones discovered (verify-only window, zero code delta):

1. CHANGELOG entry for the baseline coverage fix (gates changed behavior; append-only).
2. Mint-baseline-rows-at-module-birth consideration for `scripts/new-module.sh`.
3. Generalize the disk-derived-enumerator blindness class to other checked-in snapshots (gosec canaries already cover theirs; the lint baseline was the hole).

## d) TOTALLY FUCKED UP

1. **The third dispatch itself is process waste, not damage.** The queue re-dispatched a task whose TODO row was already `[x]` with two closed reports. The AGENTS.md turn-1 ritual (grep `docs/status/` for the task ID first) worked exactly as designed — this window cost one clean-cache gate run (minutes) and stopped. No state was harmed. If anything is "fucked", it is the re-dispatch trigger upstream of the queue, and it is self-healing under the verify-then-stop contract.

## e) WHAT WE SHOULD IMPROVE

1. **Verify-window minimum battery worked; keep citing it.** This window ran the cheap gate itself at HEAD and produced a fresh delta (the clean-cache rc=0 at a new HEAD) rather than only inheriting the 00-41 gate — the 03-39-class regression (inherited-gate citation) was avoided.
2. **Report-file format divergence is worth one sentence of policy.** The status-report skill's canonical format is HTML; the queue contract (and both prior windows on this task) name `.md`. User-instruction-wins is the rule applied here; nothing changed.
3. **The `/tmp` evidence trail is session-local.** The gate log lives at /tmp per convention; the report cites it but the artifact dies with the session. Acceptable for a green advisory gate; would matter more for a red gate (that class already archives under docs/status/assets/ with SHA256SUMS).

## f) Up to 50 things we should get done next

Scoped to what THIS window saw (verify-only dispatch — most items are inherited from the prior reports' §f, re-confirmed still open; the queue's pacing/budget gates decide when they run):

1. Mint baseline rows at module birth (`scripts/new-module.sh` extension) — complements the f1 zero-rows coverage class.
2. CHANGELOG entry for the 2026-09-23 baseline coverage fix.
3. Audit other checked-in snapshots (gosec excludes, adoption tables, SECURITY.md matrix) for the same disk-derived-enumerator blindness class.
4. Decide per-finding lint triage policy for the spike modules vs wholesale inheritance from the mirrored sqlite backend (g1 of 00-41).
5. Decide whether `--check`-FAIL-on-blindness should be a hard gate or stay advisory (g3 of 00-41).
6. Full `ci-local.sh` before the next release run (this window's `--check` is the advisory layer only).
7. Owner-only fix for the structurally-red minted verify gate: scope gofmt stage to tracked files (`gofmt -l $(git ls-files '*.go')`) in `.tq-verify` + the mint template (5 dead tasks since 09-20 on vendor/ false flags).
8. internal/e2e race-suite load-marginality vs the 180s stage cap (tracked TODO row; re-confirm before next CI reshuffle).
9. Continue ADR-0019 S1 (spike modules live until the S4 mirrored-backend deletion ruling — weigh every spike cleanup against "dies at S4 anyway").
10. Archive the 00-36 + 00-41 reports per docs-health ANNOTATE/ARCHIVE conventions when their citations are next repointed (this report supersedes them for verify evidence).

(10 items — this was a verify-only window with zero code delta; inventing 40 more would be ROADMAP brainstorm detached from observation. Items 1–6 are the prior report's backlog verbatim because none started.)

## g) Questions I can NOT figure out myself

(Same three as the 00-41 report — owner-gated, unchanged by this window.)

1. **g1**: do the spike modules (`sqlitev4`, `cqrsqlite`) get per-finding lint triage, or do they inherit the mirrored sqlite backend's policy wholesale until S4 deletes them?
2. **g2**: should `new-module.sh` also mint baseline rows at birth, or is the zero-rows coverage gate (f1) enough?
3. **g3**: is `--check`-FAIL-on-blindness wanted as a hard gate, or should coverage gaps stay advisory?

---

*Point-in-time snapshot: HEAD 00734f91. Gates cited: clean-cache `./scripts/lint-baseline.sh --check` rc=0 "1410 vs 1410" (this window, /tmp/lb-check-verify.log), coverage grep over `.golangci-baseline.txt` (rows from line 57), TODO row 33 re-read `[x]`. No work commit this window — verify-only repeat dispatch on a clean tree; this report is the only new artifact.*
