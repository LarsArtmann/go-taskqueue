# ADR-0019 S2 re-dispatch window (12-42) — unblocked-gate repairs

Task 000001a0d802. Third window on this ID (after 12-11 and 12-16). The S1
flip (TODO row 38) is still unchecked, so S2 proper (consumer re-pointing +
engine-default facts-in-same-tx) remains BLOCKED — that part is unchanged
and the item stays unchecked.

What this window found and did instead: the 12-16 report's "sqlitev4 red at
HEAD" finding was confirmed and REPAIRED, because the S2 spike module gate
cannot run while it stands.

## Delta (fresh, this window)

- sqlitev4 gate was red for two reasons:
  1. `go.mod` had no relative `replace` for `internal/queue/sqlite`, so the
     module resolved the STALE v0.3.0 proxy facade whose `CancelOwned`
     still takes an owner string (pre-claim-token). Added
     `replace .../internal/queue/sqlite => ../sqlite`.
  2. `store_test.go` carried claim-token-sweep fallout: unused claim
     variables, `:=` vs `=` after the signature change, and — the real
     semantic bug — expected-failing `ClaimDue` calls (ErrNoTaskDue probes)
     REASSIGNING the held claim variable to its zero value, so later
     `Complete` calls failed "lease not held" (TestDepsBlockUntilCompleted,
     TestProjectExclusivitySerializesPerProject). Expected-fail probes now
     discard the claim. TestProjectExclusivity also discarded the runner's
     claim entirely and completed with an unrelated claim — the loop now
     captures the runner's claim.
  3. `replay_test.go` passed the owner STRING to `Fail` where the adapter
     now takes the claim token.
- cqrsqlite had the same claim-var fallout class (incl. helper
  `parkOnQuestion` and the q-2 re-arm test losing their claims); fixed the
  same way. Gate green.
- worker (and any GOWORK=off consumer of the sqlite facade) failed module
  setup because the facade now imports the UNTAGGED sqlitev4 module and
  worker had no require+replace for it: added
  `require internal/queue/sqlitev4 v0.3.0` + relative replace (mirrors the
  facade's own go.mod). Worker gate green.
- gofmt applied over both spike modules (adapter.go had a pre-existing
  unformatted block).

## Gates at HEAD (rc captured)

- root `go build ./... && go vet ./...` rc=0
- sqlitev4 module gate (build/vet/test, GOWORK=off) rc=0
- cqrsqlite module gate rc=0
- worker module gate rc=0
- all-module loop: every module green (executor/e2e failures investigated,
  both environmental — see below)
- root `-race` suite: one e2e failure (TestAgentPoolStartupPruneSweep,
  "task.enqueued = 1, want 2") that passes in isolation — the documented
  load-marginal e2e-under-race class
- executor `TestQuestionChannelScopePinsSecondOpinions` failed only under
  THIS session shell: the shell leaks `TQ_QUESTION_FILE` into the test
  process env (verified via a probe test), which the stub agents inherit
  through `os.Environ()`. With the var unset the full executor suite is
  green. The code is correct; the environment lies (same class as the
  GOEXPERIMENT trap).

## Still blocked

S2 remainder stays serialized behind row 38 (S1 flip). The BLOCKED note on
row 39 now records this window's gate repairs.

## Notes for the owner

- This window's code changes were again daemon-absorbed footerless (the
  known hole); this commit is the footer-bearing attribution commit.
- sqlitev4/cqrsqlite remain UNTAGGED modules that facade consumers must
  require+replace by hand until the flip cuts their first tags.
