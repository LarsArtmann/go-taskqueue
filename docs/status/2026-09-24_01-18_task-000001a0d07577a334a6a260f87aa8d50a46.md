# Status: ADR-0019 S1 postgres spike — verify-only re-dispatch (task 000001a0d07577a334a6a260f87aa8d50a46)

2026-09-24 01-18 window · verification pass of an ALREADY-DONE work item ·
report format: Markdown (user explicitly requested `.md`; the status-report
skill's HTML default is overridden this once — one-off, not propagated).

Context for a fresh reader: the queue re-dispatched the S1 postgres spike
task, but the work shipped in the 01-05 window and was closed out in the
01-09 window (commits 154a5df0, 058a9ef6 — same Task-Queue-ID). This
window's correct response was verify → cite → stop (AGENTS.md
session-start ritual, turn-1 addition #1). Everything below is grounded in
THIS window's runs; prior-window claims are cited, not re-verified.

---

## a) FULLY DONE

1. **The work item itself** (prior windows, cited): `internal/queue/postgresv4`
   implements tq's `queue.Store` over `go-cqrs-lite/queue/postgres/v4 v4.0.0`
   with the tq extras as same-DB companion surfaces; TODO_LIST.md line 34
   carries the full shipped description and the `[x]`.
2. **TODO close-out**: the row is checked with an evidence-dense completion
   note (66 pass/4 skip + `-race` green via `TQ_TEST_POSTGRES`, CI wiring,
   `pgq` `?`→`$n` seam, per-test schema isolation, LEAST/LIMIT dialect
   fixes, baseline regen 17 rows, report pointer).
3. **This window's verification at HEAD** (fresh, cited):
   - `cd internal/queue/postgresv4 && GOWORK=off go build ./...` → clean.
   - `GOWORK=off go test ./... -count=1` → `ok … 0.003s` (postgres variants
     skip locally without `TQ_TEST_POSTGRES`, by design; the live-DB proof
     is the 01-05/01-09 windows' 66/4 + `-race` runs and CI's
     test-postgres job).
4. **Repo state verified clean**: `git status` empty, stash list empty,
   HEAD = 058a9ef6 (the task's close-out commit). Nothing dangling, nothing
   to amend, no drift between TODO_LIST and reality.
5. **Duplicate-dispatch discipline held**: no new work was invented, no
   scope creep into the S1 decision memo (the NEXT queued item, line 35),
   no second commit of already-committed work.

## b) PARTIALLY DONE

1. **The S1 chain overall** (task family this item belongs to): sqlite spike
   done, postgres spike done, but S1's definition of done also wants the
   decision memo (TODO line 35), the replay tool (line 36), and the flip
   (line 37) — all still open. Both spikes feeding "one decision memo" is
   the unblocked next step.
2. **This window's gate coverage**: I ran the cheap module-level gates
   (build + tests) but not the full ci-local replicant — correct for a
   verify-only pass at a clean HEAD where the 01-09 close-out already
   captured the full battery rc-by-rc, but it means this window adds no
   NEW delta beyond "still green after the close-out commits".

## c) NOT STARTED

1. **S1 decision memo** (TODO line 35) — per tq-extra surface
   upstream-grown vs companion-table, tq-fact append path, replay-tool
   sketch; verdict into docs/adr/0019 or docs/planning/.
2. **S1 replay tool** (TODO line 36) — fact-journal → fresh engine store
   replay, projection-equality verified.
3. **S1 flip** (TODO line 37), **S2/S3/S4** (lines 38-40), **dogfood
   cutover** (line 41, owner-blocked) — the rest of the serialized chain.
4. Nothing in THIS window's scope was left unstarted — the scope was empty.

## d) TOTALLY FUCKED UP

Nothing. This window had one hazard and avoided it: the tempting failure
mode for a re-dispatched DONE task is redoing or extending the work
(duplicate commits fork the dedup story and waste budget). The fastest
correct response — verify, cite, stop — was taken.

Passing observations, not fucked-up in THIS window's scope:
- The queue re-dispatched a task whose row was checked AND close-out-reported within the same hour. If re-dispatch is driven by attempt accounting rather than row state, the harvest/admission side may want a "already has a close-out report → verify-only dispatch" hint (backlog-worthy, see §f).
- **Master CI is RED at HEAD** (fresh check this window: run 35914282327, 2026-09-23 20:11 UTC, `failure` — the second consecutive CI failure; the 01-09 close-out already diagnosed the surfaces: internal/executor windows-GOOS break + `TestQuestionChannelScopePinsSecondOpinions` from 6c4252a9's helper collapse, plus committed app.css drift). My module-scoped gate and root `go build ./...` (rc=0) stay green — the redness is outside this task's touched paths and outside this task's contract; it is owned by the 01-09 report's §f top items.

## e) WHAT WE SHOULD IMPROVE

1. **Verify-window battery for DONE-row re-dispatches**: define the MINIMAL
   battery (row check → prior-report grep → module build+test at HEAD →
   state citation) so verify-only windows are uniform and cheap. This
   window improvised it; it should be codified (AGENTS.md turn-1 ritual
   covers discovery, not the verify-cite-stop response shape).
2. **Duplicate-dispatch economics**: the queue spent a window re-running a
   DONE item. Root-side dedup of dispatches against checked rows (or an
   explicit verify-only task type) would save a window per recurrence.
3. **Postgres suite friction**: the local proof requires a throwaway
   postgres (initdb by hand per the 01-05 window). A `nix run
   .#test-postgres` style app that provisions the scratch instance would
   make the TQ_TEST_POSTGRES suite one-command runnable for any future
   window.
4. **Skip accounting**: 4 of 70 tests skip without the live DB (2 sqlite
   migration stubs, perf baseline, archive stub). Fine, but the skip
   reasons should name their replacement coverage (the replay tool row)
   so a future reader does not re-litigate them.

## f) Up to 50 things we should get done next

Sorted by impact; §f items 1-10 are the load-bearing path (ADR-0019 S1
finish line), the rest are backlog brainstorm noticed in passing.

1. **S1 decision memo** (TODO line 35): one surface at a time —
   questions/RecordAnswer, PriorityScores, CountFacts/FactsSince/
   LastFacts, ProjectCounts, tq-fact append path, finalize fencing
   (owner-string vs token) — upstream-grown vs companion-table each, with
   the ratification-memo pipeline as the upstream channel.
2. **S1 replay tool** (line 36): journal → fresh engine store, verify
   StatusCounts / per-task fact tails / DLQ contents / watermarks.
3. **S1 flip** (line 37): default stores become the v4-backed adapters,
   claim tokens replace owner-string finalizes in worker, facades +
   vendorHash follow ADR-0016 rules.
4. **Upstream ratification memos** for the tq-extra surfaces the memo
   prefers upstream-grown — file via the upstream pipeline, don't block
   the flip on them.
5. **Postgres scratch-instance automation**: flake app provisioning a
   throwaway postgres for `TQ_TEST_POSTGRES` runs (one command, hermetic).
6. **Re-dispatch dedup**: harvest/admission skips or downgrades items whose
   row is checked with a close-out report; or a verify-only dispatch class.
7. **Codify the verify-cite-stop response** for DONE-row re-dispatches in
   AGENTS.md turn-1 ritual (extends the existing grep-prior-reports rule).
8. **ci-local postgres leg**: TQ_TEST_POSTGRES into ci-local's smokes (CI
   already has the job) so local green == CI green for the spike suites.
9. **Divergence ledger consolidation**: D1-D6 (sqlitev4) + postgres deltas
   live across three reports — fold into one living doc the decision memo
   can cite (docs/planning/ candidate).
10. **Decision-memo skeleton**: pre-draft the memo's table (surface ×
    option × cost × upstream appetite) so the next window fills cells
    instead of structuring.
11. Replay tool: idempotent re-runs (replay onto an already-replayed store
    converges, mirroring queue dedup semantics).
12. Replay tool: speed target — the dogfood journal size should replay in
    minutes, not hours; page the Facts reads like the watermark sweepers.
13. Flip: worker finalize-site inventory (every Complete/Fail/Cancel call
    site) as a checklist doc before touching them.
14. Flip: conformance-suite wiring — make the v4-backed adapters run the
    SAME conformance suites the mirrored backends run, so flip confidence
    is mechanical.
15. Flip: performance sanity — engine-store claim latency vs the
    hand-rolled sqlite store on the dogfood working set (TQ_BASELINE-style
    test exists; run it and record numbers).
16. Facades: decide whether postgresv4/sqlitev4 get facade modules now or
    only after S4 deletes the mirrored engines (ADR-0016 parity gate
    applies either way).
17. Vendor/vendorHash dry-run for the flip: compute what root go.mod
    requires when the adapters become default, before the flip lands.
18. `tq doctor`: teach it about the engine-backed store (its probes hit
    StatusCounts/CountFacts — verify both adapters answer identically;
    likely already true, pin it).
19. httpapi/webui: exercise both adapters behind the real surfaces
    (smoke-level, pre-flip) rather than store-level only.
20. Budget projection: confirm `UsageToday` derives identically off
    engine-store facts (shared parse drift-pin exists; run it against both
    spikes).
21. Session-close bridge: `AppendFact` sanctioning — the bridge writes
    non-task facts via `(*sqlite.Store).AppendFact`; decide the
    engine-store equivalent path now (it is a decision-memo input).
22. Watermark cursors: verify all four sweepers' cursor semantics on the
    engine store (checkpoint-after-page) via a focused test per adapter.
23. `tq audit --journal` and drift audit against engine-store facts —
    vocabulary differences (fact types) are a known divergence; enumerate
    them for S2.
24. S2 prep: inventory every tq FactType constant and its upstream
    `facts.Fact` mapping candidate.
25. S3 prep: list the read models (queue projection, stats, budget) and
    their metaengine collection candidates.
26. S4 prep: re-run art-dupl on the mirror clone groups to get the current
    count (12 groups as of 2026-09-23) — the S4 deletion dividend metric.
27. Dogfood cutover runbook: owner-run, but the AGENT-side runbook (steps,
    rollback, verification) can be pre-written.
28. Replay verification: add per-projection equality assertions to the
    replay tool's output so the cutover gate is a report, not a vibe.
29. Postgres adapter: `OpenWithPool` caller-owned-pool semantics — does the
    engine store need the same ownership model? Decide and pin.
30. CI: confirm the test-postgres job pins the same postgres major the
    local scratch used (17) — version drift between local proof and CI
    proof is a silent divergence source.
31. Lint baseline: watch postgresv4/sqlitev4 rows for drift between regens
    (ninth regen just admitted them; next --check failure should triage,
    not regen blindly).
32. AGENTS.md: the multi-module loop (`for m in $(find internal …)`) now
    includes three spike modules — verify the loop picks them up and the
    GOWORK=off gate is honest for each.
33. `check-dead-exports.sh`: run it scoped to the spike modules to catch
    adapter surfaces nothing imports yet (expected findings pre-flip —
    that is fine, but they should be listed).
34. Facadeparity: confirm the spike modules are correctly OUT of facade
    scope (they are internal drivers, not facades) — document the ruling
    so the gate never grows a false expectation.
35. Doc cross-links: docs/adr/0019 §S1 should link all three postgres-spike
    reports (01-05, 01-09, 01-18) — currently only the earlier ones exist
    as links in TODO prose.
36. Archive eligibility: the 01-05 report's forward-looking items — triage
    which are now routed to TODO rows so it can move toward archived/.
37. Decision-memo channel: read the upstream ratification-memo pipeline
    docs before drafting, so the memo is filed in the expected format the
    first time.
38. Spike-module go directive: both spikes aligned to 1.27.1 (16-28 fix) —
    add a check-go-mods assertion pattern note if not already covered.
39. Race-stub files (`race_off.go`/`race_on.go`): verify the build-tag
    split is actually exercised in the -race CI leg (a tag typo would
    silently drop coverage).
40. `pgq` seam: consider upstreaming or extracting it if the decision memo
    keeps sqlite-style SQL — it is generic enough to be a library seam.
41. Test naming: `tqtest_<hex>` schemas leak on crash (no cleanup) — add a
    startup sweep dropping stale `tqtest_%` schemas in the test helpers.
42. Store_test.go size (3.4k lines): candidate for splitting if the
    conformance-suite extraction (14) happens anyway.
43. CI test-postgres job: verify it runs the spike suite AND the legacy
    postgres suite, or decide the spike suite replaces it post-flip.
44. Docs: VERSION-SURFACES.md will need a row for the spike modules if
    they ever get tags — note it now, decide at release time.
45. CHANGELOG: the postgres spike (01-05 window) — confirm it has a row;
    append-only file, fix forward if missing.
46. Roadmap: fold the post-S4 vision (engine-only store, upstream-tracked
    divergences shrunk to zero) into ROADMAP.md so the chain has an end
    state on paper.
47. Verify TQ_TEST_POSTGRES skip reporting is loud in CI (a misconfigured
    secret turning the job into all-skip-pass should FAIL, not pass).
48. Replay tool smoke: a tiny seeded fixture journal in-repo so the tool
    has a hermetic integration test (not just dogfood-journal runs).
49. Decision memo: include a "reject and stay hand-rolled" option honestly
    — the memo is a decision, not a rubber stamp.
50. Session ritual: grep `docs/status/` for the task id BEFORE the first
    tool call (this window did it second) — shave one round-trip off
    duplicate-dispatch discovery.

## g) Questions I can NOT figure out myself

1. **Re-dispatch source**: was this window's dispatch queue-driven
   (attempt accounting / retry policy) or human-triggered? If the queue
   re-dispatches DONE rows by policy, §f-6/7 are gate bugs worth fixing at
   the root; if manual, they are lower priority.
2. **Decision-memo appetite**: for the tq-fact append path, is the owner
   willing to accept an upstream escape hatch (engine-exposed fact write)
   if it exists, or is companion-journal the only acceptable S2-compatible
   shape? The memo's recommendation flips on this.
3. **Postgres in the flip**: the dogfood production journal is sqlite-only.
   Should the S1 flip treat the postgres adapter as first-class default
   (CI-proven only) or as experimental until a postgres dogfood exists?
   This scopes how much flip-gate weight the postgres suite carries.

---

*Verify-only window: no code changed. All fresh gate runs cited in §a-3;
prior-window evidence cited from docs/status/2026-09-24_01-05 and _01-09
reports. Nothing pushed.*
