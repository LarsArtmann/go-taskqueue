# Status Report — task 000001a0db237aa8 (folded-here TODO row, verify window)

**When:** 2026-09-26 04:14 CEST · **Window type:** verify → cite → close (DONE-on-arrival) · **Code changes by this window:** zero (one TODO_LIST row edit + this report)

**Task contract:** TODO_LIST row 108 — "`tq show --commits`: surface footer-less daemon commits ADJACENT to a footer-bearing work commit as "folded here" instead of silent (the 147bd17 shape) (03-46 §f9)".

**Headline:** the feature was ALREADY SHIPPED by a concurrent window (implementation rode daemon commit cedcface, 02:41, with 173 lines of pinning tests) but its TODO row was never closed and no report claims the work. This window verified the shipped feature end-to-end (unit pins + live render on real history), closed row 108 with citations, and committed c355ded2 with the task footer. The fastest correct response to a DONE row is verify → cite → stop; this window is that, plus the honest ledger below.

---

## a) FULLY DONE

1. **Folded-here feature verified shipped, not re-implemented.** `foldedDaemonCommits` (cmd/tq/main.go:2271) claims only exact `chore: auto-commit N changed file(s) (heuristic)` subjects (guard regex `daemonCommitSubject`, main.go:2218), walks parent (`gitCommitMeta(sha^)`) and child (`gitChildCommit` via rev-list) adjacency per footer commit, sha-dedups between multiple footer commits, and emits the `folded_here` view section with a `relation` field; human-adjacent commits stay silent (commitsForTask wires it at main.go:2354). Evidence: read at HEAD c355ded2; landed in daemon commit cedcface (main.go +99, show_commits_test.go +173).
2. **All three pinning tests green** via the canonical cmd/tq devmod shim: `./scripts/test-cmd-tq.sh -run 'TestCommitsForTask|TestDaemonCommitSubject'` → rc=0, 3/3 PASS (`TestCommitsForTaskFoldsAdjacentDaemonCommits`, `TestCommitsForTaskNoFoldForHumanNeighbors`, `TestDaemonCommitSubjectShape`). File: cmd/tq/show_commits_test.go.
3. **Live render on real production history.** Built `/tmp/tq-bin` via `scripts/build-tq.sh` (rc=0) and ran `tq show --commits 000001a0daf11f45d2f680ad1f7587db31f5` — `folded_here` claims 4 daemon commits with correct parent/child relations around that task's 5 footer commits (the exact 147bd17 shape the row asked to surface). Two CLI learnings en route: the flag must precede the positional (`tq show --commits ID`, Go flag package stops at the first positional) and the S4 task's live case renders verdict `AMBIGUOUS` for the NORMAL multi-commit case (see §g1).
4. **TODO row 108 closed with citations** (TODO_LIST.md, section "Priority-system follow-ups"): [x] + implementation provenance (cedcface) + file:line + pinning test names + live-render command + battery line. Commit c355ded2, footer `Task-Queue-ID: 000001a0db237aa84c17f3dfe4488ded8ab8` as the LAST trailer line (trailer-safe position, unlike the f5f5455a/c561ae25 defect class). Not pushed, per contract.
5. **Verify-window minimum battery at HEAD, rc-captured:** root build rc=0, vet rc=0 (GOEXPERIMENT=jsonv2, GOTOOLCHAIN=auto), check-doc-refs.sh rc=0, check-todo-list.sh rc=0. Fresh delta produced: the live-render proof + the 3/3 targeted-shim run (not inherited from any prior report — none existed for this feature).
6. **Master-CI state recorded alongside the DONE verdict** (03-46 §f7 habit): `scripts/check-ci.sh` → FAIL, latest master run e5c386e10 is red and predates this tree (see §d4). Checkpoint commit c355ded2 verified in `git log`.

## b) PARTIALLY DONE

1. **The folded-here feature is code-complete but UNDOCUMENTED.** Works now: the show-view section + pins (§a1-a3). Missing: no AGENTS.md operational-contract note (the payload-contracts section documents every other show/fact surface; `rg -n "fold" AGENTS.md` has zero hits on this feature), no CHANGELOG entry, and no entry in the `tq show` help text about what `folded_here` means. Effort to finish: S (one AGENTS.md paragraph + one CHANGELOG line + optionally a help-text sentence). Not done here because the contract scoped this window to the row's work item, which the row's text covers as shipped-and-verifiable.
2. **Attribution for the fold implementation is unresolved.** cedcface carries the fold code BUT its stat is 2 code files + an UNRELATED report (02-37 artdupl-dedup-window.md, which does not mention the feature anywhere — `rg -i "fold|147bd17"` over it: zero hits on the feature). The queue's derived outcomes therefore attribute the fold code to no task. What exists: this window's row closure + report now document provenance. What remains: a footer-bearing marker commit or queue-side note for the fold work itself (the fold-marker.sh maneuver, TODO row 279 — not started).
3. **This window's own gate scope is partial by design:** the cmd/tq FULL suite was run once (rc=1 — two pre-existing failures, §d1) and the targeted shim run green; ROOT tests (`go test ./...`) were NOT run — the change set here is one markdown row, and the battery rule's cheap gates cover it, but the omission is stated rather than hidden.

## c) NOT STARTED

All items below are correctly OUT of this window's scope (contract: "no scope creep"); they are listed so the queue can decide.

1. **AGENTS.md Known-Issues note for cmd/tq gopls false positives.** gopls reports 41 `UndeclaredName` errors on cmd/tq/main.go (devmod layout: `cmdBootstrap`, `cmdAsk`, … undefined) — the known class AGENTS.md documents for templ LSP ("trust the CLI, not the LSP") but NOT for cmd/tq. Every future cmd/tq window re-derives this. Effort S.
2. **`fold-marker.sh`** (TODO row 279, carried): footer-bearing empty marker commits for daemon-folded work — would have healed §b2 mechanically.
3. **Daemon-fold footer-first check script** (TODO row 238, carried, eighth occurrence noted there): cedcface is one more data point — source rode a footerless daemon commit.
4. **Daemon-fold attribution owner ruling** (TODO row 118, carried, fourth+ consecutive ask): still blocked on the owner; this window's §b2 is another instance of the same hole.
5. **`tq tasks` / webui parity for folded_here:** only the `tq show --commits` surface renders folds; the task table and detail page stay silent. Not requested by the row; possible follow-up.
6. **Master-CI red diagnosis** (e5c386e10): predates this tree; most probable owner is the in-flight S1 claim-token migration (§d1's blast radius), but per contract I did not research it.

## d) TOTALLY FUCKED UP

1. **cmd/tq module suite is RED at HEAD right now** — `TestJournalDriftNoDriftAfterRescue` + `TestJournalDriftSeededDriftAllFields` fail: journal replay coverage is `{Status:1 Attempts:1 Priority:0 DedupKey:0}` (priority/dedup_key never covered by the replay). Root cause (traced, not fixed): the concurrent ADR-0019 S1 window's `ClaimDue`/`Fail` claim-token signature migration (d2dd3cef 03:30 + 9644b749 03:32 migrated the test call sites; the replay/projection side hasn't caught up). Proof it is NOT this window's and NOT the fold feature's: the same two tests pass at afbd48c4 (03:29, pre-migration) via the shim in a scratch worktree. Severity: blocks the cmd/tq gate (full `test-cmd-tq.sh` rc=1) and plausibly explains master-CI red e5c386e10. Mitigation: the S1 window is ACTIVELY mid-flight in exactly this area (their edits landed minutes before mine); touching their half-migrated code would be sabotage, so this window ran the targeted shim scope and left the repair to its owner.
2. **Master CI is red** (run e5c386e10, predates this tree) — per AGENTS.md a local green is worthless while master is red. This window made it no worse (docs-only commit) and records the state per 03-46 §f7. Not diagnosed (out of scope; §c6).
3. **The fold implementation shipped with zero attribution and zero docs** — unreported, mixed into a daemon commit beside an unrelated report file. This is the documented daemon-fold under-attribution class (row 238) happening AGAIN, and it cost the queue a re-dispatch: this verify window exists only because the shipping window never closed its row (1.5h gap between cedcface 02:41 and this window 04:0x).
4. **The `AMBIGUOUS` verdict fires on the NORMAL case.** `commitsForTask` sets verdict "AMBIGUOUS: multiple commits reference this task ID" for count>1 (main.go:2349), but AGENTS.md's commit-msg-hook policy says "multiple COMMITS per task remain the norm (work + close-out)". The live S4 task renders AMBIGUOUS for its ordinary 5-commit chain — the verdict cry-wolf problem: when every multi-commit task is "AMBIGUOUS", the f26-class signal it was built for drowns. See §g1.
5. **This window's own process misses (radical honesty):**
   - **CONTRIBUTING.md turn-1 check skipped AGAIN** (the documented recurring miss: 00-52 d4, 02-17 d3 — now +1). session-start.sh ran, but the CONTRIBUTING/CLAUDE.md step of the ritual was not executed.
   - **Full-suite-first ordering wasted a cycle:** I ran the whole cmd/tq suite (hit §d1's unrelated red, spent a diagnosis roundtrip) before the targeted run. Targeted-first, broad-second is the correct order for a scoped verify window.
   - **One worktree roundtrip lost:** tried `go test -modfile=dev.mod` directly in the scratch worktree and hit "open dev.mod: no such file" (the shim GENERATES dev.mod); reran via the shim. Known-mechanism, should have been first guess.
   - **Master-CI check deferred to report time** instead of turn 1 (03-46 §f7 wants it alongside the DONE verdict; I ran it at 04:12, not 04:0x).
   - **Root `go test ./...` not run** (§b3) — defensible for a docs-only delta, but it is a scope declaration, not a virtue.

## e) WHAT WE SHOULD IMPROVE

1. **Shipping windows must close their own TODO row in the shipping commit** (or the queue needs a "row shipped-but-open" marker). This window is the third-plus instance of the pattern: feature lands, row stays open, a verify window burns a dispatch to close it. Concrete: add a line to the task contract/template — "if your work completes a TODO row, tick it in the same commit".
2. **Pin the `daemonCommitSubject` ↔ daemon coupling.** The fold view's entire world is one regex mirroring the daemon's subject template (`chore: auto-commit \d+ changed file(s) \(heuristic\)`). If the daemon's format string ever changes, the fold view goes SILENTLY blind (no gate connects the two; the regex's test pins only the current string). Concrete fix: derive the pattern from a shared constant the daemon build also imports, or add a check-script that greps the daemon's actual recent subjects against the regex.
3. **Verdict taxonomy for multi-commit tasks** (§d4): distinguish "N commits (normal: work + close-out turns)" from genuine cross-reference ambiguity. The f26 class is about FOREIGN ids, not multiple commits per id.
4. **Document the cmd/tq gopls false-positive class** next to the existing templ LSP note in AGENTS.md — one sentence kills a recurring diagnosis tax.
5. **Flag-before-positional UX:** `tq show ID --commits` fails with a bare usage line. The usage string could show the working order (`tq show [--commits] TASK_ID`) or the flag package could be replaced with a two-pass parse for `show`. I hit this firsthand; every new user will.
6. **Report-format divergence logging:** the status-report skill's canonical format is HTML; every recent report (including this one) is Markdown by explicit user request. The override is now the de-facto norm — either flip the skill default to md or stop re-justifying it per report. (Flagged per the skill's own rule.)
7. **Verify-window battery script** (echoed from 01-25 §e, still not built): a `scripts/verify-battery.sh` that prints rc-captured build/vet/doc-refs/todo evidence in one call would have turned this window's §a5 into a single command and prevents the PIPESTATUS class of silent-green claims.

## f) NEXT (ranked by impact; scoped to this task's orbit + noticed-in-passing — deliberately NOT padded to 50, most of the remaining backlog is already queued in TODO_LIST rows carried above)

| # | Task | Impact | Effort | Category |
|---|------|--------|--------|----------|
| 1 | Land the S1 window's in-flight claim-token migration to green: replay coverage for priority/dedup_key so `TestJournalDriftNoDriftAfterRescue` + `TestJournalDriftSeededDriftAllFields` pass (owner: S1 window; §d1) | Critical | M | Bug |
| 2 | Re-green master CI (e5c386e10 red, predates this tree; likely same S1 fallout) and re-run check-ci before the next push | Critical | M | Bug |
| 3 | Document folded-here: AGENTS.md operational-contract paragraph + CHANGELOG line + `tq show` help-text clause (§b1) | High | S | Documentation |
| 4 | Soften/split the `AMBIGUOUS` verdict for the normal multi-commit case; reserve it for genuine cross-reference conflicts (§d4, §g1) | High | S | Bug |
| 5 | Pin `daemonCommitSubject` coupling to the daemon's real subject template (shared constant or check-script) before a daemon reword blinds the fold view (§e2) | High | S | Quality |
| 6 | Write the AGENTS.md Known-Issues note: cmd/tq gopls devmod false positives — trust the shim gate, not the LSP (§c1) | Medium | S | Documentation |
| 7 | Attribution for cedcface's fold code: footer-bearing marker commit or queue-side note once the fold-marker ruling lands (§b2, rows 118/238/279) | Medium | S | Cleanup |
| 8 | `tq show` usage string/parse: make `tq show ID --commits` work or print the working flag order (§e5) | Medium | S | Feature |
| 9 | Build `scripts/verify-battery.sh` (rc-captured cheap-gate one-shot) and reference it from the verify-window convention (§e7) | Medium | M | Quality |
| 10 | Smoke for the fold view: seeded scratch repo + fixture task asserting `folded_here` appears end-to-end through the built binary (currently only unit pins exist; the live render this window ran is unrepeatable evidence) | Medium | M | Feature |
| 11 | Fold-view limitation note: `gitChildCommit` only walks the path to HEAD — folds on side branches/merge forks are invisible; document or extend (noticed while reading gitChildCommit, main.go:2250) | Low | S | Documentation |
| 12 | `tq tasks`/webui surfaced folds (§c5) — parity follow-up only if the owner wants it | Low | M | Feature |
| 13 | Add "close your row in the shipping commit" to the task-contract boilerplate (§e1) | Medium | S | Process |
| 14 | Report-format default ruling: flip the status-report skill to Markdown if md-by-request is now the norm (§e6) | Low | S | Process |
| 15 | carried: rows 118 (daemon attribution ruling), 238 (footer-first check script), 279 (fold-marker.sh), 135 (vendor gate), 108-successors in the Priority-system section — unchanged, owned elsewhere | Medium | — | — |

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **`AMBIGUOUS` vs the multi-commit norm:** `commitsForTask` calls count>1 "AMBIGUOUS: multiple commits reference this task ID" (main.go:2349), but the commit-msg-hook policy in AGENTS.md says multiple commits per task are the NORM (work + close-out). The live S4 task 000001a0daf11 renders AMBIGUOUS on its ordinary 5-commit chain. Which is intended: soften the verdict for same-ID multi-commit chains (and reserve AMBIGUOUS for genuinely conflicting cross-references), or is count>1 genuinely suspect and the hook policy the outlier? I checked the git history of the verdict line (f26 cluster motivated it) but cannot derive the intended semantics from code alone.
2. **Docs ownership for unreported shipped features:** when a verify window proves a shipped-but-undocumented feature (this window: folded-here lacks AGENTS.md/CHANGELOG), should the verifier write the docs (scope creep vs. closing the loop) or leave them to a docs task? The contract said "smallest correct change wins", so I left them — but that leaves the feature invisible to the next agent reading AGENTS.md.
3. **The red cmd/tq gate ownership handoff:** the S1 window's migration left the suite red minutes before this window started, and master CI is red on a run that predates my tree. Should verify windows in this state (a) proceed with targeted-scope gates + report the red (what I did), (b) hold the DONE verdict until the owning window greens the suite, or (c) attempt the repair themselves when the root cause is legible? (c) risks colliding with the active owner; (a) ships verdicts on a red tree; (b) blocks closures on unrelated windows.

---

*Battery this window: test-cmd-tq.sh targeted 3/3 PASS rc=0 · root build/vet rc=0 · check-doc-refs rc=0 · check-todo-list rc=0 · check-ci rc=1 (master red e5c386e10, predates tree) · full cmd/tq suite rc=1 (§d1, pre-existing, S1-owned).*
