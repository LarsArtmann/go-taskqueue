# Status report — task 000001a0ac6105f14830baf90506ef633b32

**When:** 2026-09-17 00:43
**Work item:** TODO_LIST.md row 93 — "Delete dead `factLines` (`internal/webui/components.go:133`, gopls unusedfunc) + add `retry trail`/`payload section`/`work item`/`verify gate` terms to docs/DOMAIN_LANGUAGE.md"
**Format note:** user explicitly requested Markdown at this path; the status-report skill's HTML default is overridden for this one report (per-skill instruction: flag the override, don't propagate it).

## a) FULLY DONE

1. **Task verified and completed (commit e2b4167, footer byte-verified: exactly 1 well-formed `Task-Queue-ID` footer).** Two files changed: docs/DOMAIN_LANGUAGE.md + TODO_LIST.md.
2. **`factLines` half: verified already dead-and-buried, correctly no-op'd.** `rg -n "factLines"` over the tree shows zero hits in any Go file — it was deleted in the 2026-09-14 UI overhaul (I1 hygiene workstream, 17-35 report §I1). Deleting it again was not possible; verifying instead of blindly "fixing" was the correct response to a stale work item.
3. **`Verify gate` term: verified already present** (DOMAIN_LANGUAGE.md, Harvest loop table) — no-op, not a duplicate row.
4. **Three genuinely missing terms added**, each grounded in code/AGENTS.md, placed in the right section:
   - `Work item` → Harvest loop table (harvested item an agent task is minted for, `item` + dedup-key in the payload).
   - `Retry trail` → Observation table (requeued + dead-lettered fact strip; dead-letter reasons COUNT in — matches `retryTrail` at internal/webui/payload.go:304 and the AGENTS.md ruling).
   - `Payload section` → Observation table (detail-page rendering rules: item-lead + collapsed prompt vs prompt-lead; owner-approved 2026-09-11).
5. **Gates run and green:** root build + `go vet ./internal/webui/` + `go test ./internal/webui/ -count=1` (10.1s ok), `check-todo-list.sh`, `check-doc-refs.sh`.
6. **TODO_LIST row 93 ticked `[x]`** per the file's conventions.
7. **Session-start ritual executed in full this time:** `git log` over the whole repo, `git status`, `git stash list`, prior-report grep for the task ID (no DONE row existed), CONTRIBUTING.md read at turn 1. No recurring-miss repeats.

## b) PARTIALLY DONE

1. **The work item as literally written is 2/4 terms + 1/1 deletions = done, but the item's citation was stale** — `components.go:133` pointed at `factTone`'s closing brace, not `factLines`, and had pointed nowhere since 2026-09-14. The item sat unchecked for 3 days while six other windows referenced it as "row 92/93, untouched" without anyone doing the 10-second `rg` that would have closed it. The task is done; the *item-hygiene* around it was partial until this run.
2. **Verification depth:** build/vet/test over `internal/webui/` (the only Go module the task touches) + two doc gates — but **not** the full ci-local.sh replicant. For a 4-line docs-only diff this is proportionate, yet the repo's own contract says the pre-push gate is ci-local; the commit rides on the narrower gate. Acceptable for a docs commit, but it is the honest boundary of what was proven.

## c) NOT STARTED

1. **No CHANGELOG entry** for the glossary expansion. Defensible (docs-only, CHANGELOG is user-facing shipped change), but the 2026-09-17 00-25 report fixed a "docs shipped without CHANGELOG row" gap in-pass — same class, not applied here. Judgment call, not an oversight I can fully defend.
2. **Glossary drift guard:** nothing (script, test, or gate) checks that DOMAIN_LANGUAGE terms stay in sync with code symbols or that referenced symbols still exist. `factLines` proves the failure mode: the item outlived its referent by 3 days.
3. **The other docs-health harvest rows** visible in the 00-25 report's own §a (row 195 redaction e2e smoke, row 201 secretPatterns pin, row 206 wrapper polish, httpapi live-socket smoke, archive/ANNOTATE sweep, deploy-readiness bundle) — untouched, as they should be: out of scope for this run. Listed here so the next dispatcher doesn't rediscover them.

## d) TOTALLY FUCKED UP

Nothing broken shipped this run. Honest near-misses and process faults:

1. **First `grep` for the four terms used lowercase patterns only** (`retry trail\|payload section\|work item\|verify gate`) and returned "no output" — which briefly suggested ALL FOUR terms were missing. Only reading the file caught that `Verify gate` existed with sentence capitalization. A case-insensitive grep (`grep -i`) is the correct tool for glossary-term existence checks; the first result was a false negative I nearly encoded.
2. **The stale-citation pattern recurred at MY layer too:** I trusted the item's `:133` line number just long enough to view the wrong function (`factTimestamp` now lives there). Symbol-based work items, symbol-based verification — line numbers in a fast-moving multi-agent repo are decoration.
3. **Unparam finding observed in passing and left alone** (correctly — scope discipline): `detailItems`' `now` param is unused (components.go:163), gopls-flagged, adjacent to the exact file this task cited. Fixing it would have been scope creep; recording it in §f is the right channel.
4. **No `git status` re-check immediately before `git add`** (the daemon race). It happened to be clean this time — the commit contains exactly the two intended files — but the ritual skip is a real exposure, ~4th documented instance of this class across recent reports.

## e) WHAT WE SHOULD IMPROVE

1. **Work items should cite SYMBOLS, not line numbers.** `components.go:133` was wrong within days; `factLines` (symbol) would have been greppable-and-correct or honestly gone. This is a harvest-prompt/TODO-writing convention change, zero code.
2. **Done-item verification should be a 10-second reflex for every agent that touches a row in passing.** Six windows read "row 92/93 untouched" and moved on; the fix was `rg factLines` + `grep -i 'verify gate'`. A verify-only dispatch is cheaper than 3 days of a dead row.
3. **Glossary existence checks must be case-insensitive** (or the glossary pinned by a test that greps canonical lowercase keys). Otherwise term-presence checks lie.
4. **Docs-only commits need a defined minimal gate** (build + affected-module tests + doc-ref/todo gates) so runs stop re-litigating whether ci-local is required for a 4-line markdown diff. Today each run guesses; the 00-13 report did the same dance.
5. **CHANGELOG policy for doc/glossary changes should be written down** (in or out) — the repo has now shipped both behaviors within 24h.

## f) NEXT (things noticed in passing; up to 50, honestly weighted — the first ~10 are real work, the rest are cheaper follow-ups/roadmap fuel; several already live in TODO_LIST and are listed for the harvester's dedup, not duplicated)

1. Queue-side dedup-key → COMPLETED short-circuit so re-fired done items self-verify instead of re-dispatching (carried from 00-22 report §f1 — it is why this task's item survived 3 days).
2. Fix `detailItems` unused `now` param (unparam, internal/webui/components.go:163) — trim signature or use it.
3. Add the four missing `journal.FactType` cases to `factTone` (components.go:114 — Orphaned/Reprioritized/SessionOpened/SessionClosed silently fall to neutral; SessionOpened/Closed at minimum deserve a tone on the fact feed).
4. Fix `embedlit` finding at components.go:195 (embedded field type removable from struct literal).
5. Fix `makezero` at components.go:437 (`buckets` needs non-zero initial length or a comment proving intent).
6. Extract the magic numbers 5/15/60 in the humanize switch (components.go:443-447) into named constants — mnd keeps flagging them.
7. Reconsider `sortableColumns`/`statusColorTable` globals (gochecknoglobals, components.go:348/36) — table vars could be package-private consts or moved into their only callers.
8. Add a symbol-liveness lint for TODO_LIST items: a ci-local check that greps each row's backticked symbols and flags rows whose referents no longer exist (would have caught row 93 on 09-14).
9. Extend `check-doc-refs.sh` (or a sibling) to case-insensitively verify DOMAIN_LANGUAGE terms referenced by TODO items/reports exist in the glossary.
10. Decide + document the CHANGELOG policy for docs/glossary-only changes (in or out), then apply retroactively to e2b4167's class.
11. Run full `./scripts/ci-local.sh` before the next push that carries this commit (docs-only, but the contract is the contract).
12. Sweep old reports/plans that still cite `factLines` as LIVE ("gopls flags it unused RIGHT NOW") — annotate as resolved via docs-health ANNOTATE, not rewrite (candidates: 2026-09-12_01-47, archived round-13 T23 row).
13. Glossary completeness sweep: enumerate the AGENTS.md payload-contract vocabulary (Heartbeat, Dead-letter, Refusal, Eviction/Reclaim, Close-out turn, Batch member) against DOMAIN_LANGUAGE and mint the missing rows in one pass.
14. Pin the three new glossary rows the way the repo pins other contracts: a small test asserting DOMAIN_LANGUAGE contains canonical terms (cheap drift guard until item 9 lands properly).
15. Verify `retryTrail`'s doc comment (payload.go:299 "refusals and failures") matches the glossary row's "requeued + dead-lettered" wording — one of them is imprecise; align both (likely extend the code comment).
16. Harvest-check whether the 00-25 report's open §a rows (195/201/206, httpapi smoke, archive sweep, deploy-readiness) are all actually still in TODO_LIST — index-vs-queue drift was the 2026-09-09 silent-loss class.
17. Confirm the `factLines` deletion didn't orphan its test coverage claims in `docs/status/2026-09-14_18-14` §I1 (cited "factLines gone" — cite the commit that removed it).
18. Roadmap fuel: a `tq doctor --glossary` / docs-health mode that diffs AGENTS.md vocabulary against DOMAIN_LANGUAGE.md automatically (replaces item 13's manual pass forever).

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Work-item authoring convention:** should TODO_LIST harvest items be required to cite symbols (grep-able) instead of `file:line`? This is a prompt-template/harvest convention change only you can rule on — but it would have closed row 93 three days earlier.
2. **CHANGELOG scope:** do glossary/docs-only changes get a CHANGELOG row? Both behaviors shipped this week; a one-line ruling ends the guessing for every future docs window.
3. **Is a term-presence pin wanted at all** (test or ci-local check asserting DOMAIN_LANGUAGE's canonical terms exist), or is the glossary deliberately manual-by-decision like docs formatting (dprint ruling) and should stay gate-free?
