# Task 000001a0a2d197b335d789e955e9cc05bbf7 — `tq enqueue` $TQ_DB guardrail close-out

**When:** 2026-09-15 04:09 · **Work commit:** 9139917 · **Verify gate:** `go build ./...` + `./scripts/test-cmd-tq.sh` (ok, 7.598s) + live enqueue repro

Scope: TODO_LIST row 83 ("`tq enqueue` guardrail: warn when `$TQ_DB` points somewhere other than `./tasks.db` (the production-DB trap cost a live enqueue 2026-09-10) (05-29 report c4/d2/e2)"). The smallest correct change: one stderr warning in `cmdEnqueue` (cmd/tq/main.go:304-311), fired only when `--db` is absent AND `$TQ_DB` is set; stdout (task ID) untouched so scripts/smokes keep working.

> Format note: the status-report skill defaults to HTML; the dispatching prompt explicitly requested `.md` at this path — user instruction wins, flagged here per skill rules.

## a) FULLY DONE

1. Warning implemented in `cmdEnqueue`: `tq enqueue: warning: $TQ_DB is set (<path>) — this enqueue targets that database, not ./tasks.db (pass --db to override)` — what/why/fix in one line, the AGENTS.md error-handling bar (what, fix, escape hatch).
2. Stdout contract preserved: task ID still the only stdout line, so `smokes`, scripts, and the queue's `TQ_RESULT` machinery are unaffected (stderr-only warning).
3. Live verification with a scratch DB: `TQ_DB=/tmp/guard.db tq enqueue --type sh ...` printed the ID on stdout and the warning on stderr; scratch artifacts trashed in the same shell chain (no daemon-food fixtures).
4. Verify gates run: root `go build ./...` green; `./scripts/test-cmd-tq.sh` full cmd/tq module suite green (`ok ... 7.598s`).
5. TODO_LIST row 83 checked off (`[x]`), file conventions respected.
6. Committed with the exact `Task-Queue-ID: 000001a0a2d197b335d789e955e9cc05bbf7` footer; not pushed.
7. No edits to `.crushrc`, `crush.json`, or `.tq-verify`; no scope creep (no drive-by touching the other seven `TQ_DB` call sites).

## b) PARTIALLY DONE

1. **No unit test for the warning.** The behavior is verified manually only. A test exists-shaped opportunity: cmd/tq tests run `cmdEnqueue` directly and could capture stderr with `$TQ_DB` set + an explicit `--db`/scratch cwd. Cost: small; risk of rot: low. Deliberately skipped to keep the dispatch minimal, but this is the honest gap in the change.
2. **Warning only on `enqueue`.** The production-DB trap (`defaultDB()` prefers `$TQ_DB` over `./tasks.db`, AGENTS.md Known Issues) applies to every bare `tq` command — `stats`, `tasks`, `cancel`, `dlq` all hit the same inherited env. The work item scoped the guardrail to enqueue (the 2026-09-10 incident), so this is faithful scoping, not sloth — but the trap surface is wider than the fix.
3. **AGENTS.md not updated.** The Known Issues entry about the TQ_DB trap (2026-09-10) now has a partial mitigation; one sentence ("enqueue warns since 9139917") would keep the memory file honest. Not done this lap.

## c) NOT STARTED

1. `tq doctor` could surface the same condition (`$TQ_DB` set + cwd != DB dir) as a doctor check — natural home for a "you are one shell away from the production journal" advisory. Not started (out of scope).
2. `bootstrap.go:274` has its own `--db` flag with the same default; no warning there. Not started.
3. A `--quiet`/`TQ_QUIET` escape for scripted bulk enqueues that legitimately target `$TQ_DB`. Not started — YAGNI until someone complains.

## d) TOTALLY FUCKED UP

Nothing. No broken build, no reverted work, no self-dealing file touched, no daemon-food fixtures, no stray production enqueues (the scratch run exported an explicit `TQ_DB` and was cleaned in-chain). Two honest self-critiques that stop short of "fucked up": the missing unit test (b1) and gopls phantom diagnostics (json/v2 build constraints, a known false-positive class per AGENTS.md) were trusted as noise without a fresh CLI cross-check — the CLI build I ran immediately after was the verification, so the order was right, but I noted the LSP errors only in passing rather than explicitly dismissing them against the green build.

## e) WHAT WE SHOULD IMPROVE

1. **Mint-time done-check.** This task arrived unchecked but the pattern across 2026-09-15 reports is re-dispatch laps on already-done items. A harvest-side check ("skip items already [x] or whose deliverable exists") would have saved multiple paid laps this week — this row was NOT one of them (real work was needed), but the systemic fix remains unbuilt.
2. **Warning coverage decision.** Either extend the warning to all DB-touching subcommands or write the ruling down that enqueue-only is deliberate. Silence invites a future agent to "helpfully" spread it — which would be the wrong surprise surface for `tq stats` in scripts.
3. **Stderr warnings vs smoke assertions.** `smoke/help-text.sh` guards help text for rename leaks; nothing guards the warning text itself. If a rename sweep ever mangles the warning string (the `q.Get("query")`/`tq dlq --max-attempts` class of literal-diff damage), nothing catches it. A one-line test kills that class (same fix as b1).
4. **The comment cites the incident date, not the report.** cmd/tq/main.go:305 says "(2026-09-10 live enqueue)" — fine, but linking the 05-29 report c4/d2/e2 would help forensics. Cosmetic.

## f) Up to 50 things we should get done next

Honest ranking, impact first. Items 1-8 are queue-relevant follow-ups from THIS change; 9+ are the adjacent backlog noticed in passing (docs-health HARVEST should apply its routing rigor — most of 20+ are ROADMAP fuel, not TODO_LIST food).

**From this change (direct):**
1. Unit test: `$TQ_DB` set + no `--db` → warning on stderr, ID on stdout (kills the string-rot class, closes b1).
2. Ruling (owner): warning scope — enqueue-only deliberate, or extend to all bare `tq` DB-touching commands? Write the verdict in AGENTS.md either way.
3. One-sentence AGENTS.md Known Issues update: the 2026-09-10 TQ_DB trap now has an enqueue-side warning (9139917).
4. `tq doctor` check: `$TQ_DB` set and its target exists while cwd holds a `./tasks.db` → advisory row.
5. Same warning (or none, per ruling) in `bootstrap.go:274`'s `--db` path for parity.
6. Consider `TQ_QUIET=1` escape if scripted bulk enqueues ever find the warning noisy (YAGNI gate: wait for a complaint).
7. CHANGELOG entry for 9139917 under the next unreleased version (append-only file, currently untouched by this lap).
8. Link the 05-29 report section anchors (c4/d2/e2) from the new code comment for forensics.

**Adjacent backlog (noticed in passing, not researched):**
9. The recurring mint-time `[x]`-check for harvested items (the re-dispatch-loop killer, §e1; multiple 09-15 reports beg for it).
10. Mirror the recent smokes (reviews.sh, webui-css, rename scanner) into ci.yml where ci-local-only wiring exists (recurring §f item in prior reports).
11. Help-text guard extension: assert key stderr contract lines (the new warning included) so rename sweeps can't deaden them.
12. `defaultDB()` precedence documentation in `tq help` prologue (currently one line at main.go:88; the trap deserves the caveat "env wins over cwd").
13. Version-surface check: does a stderr-warning count as a behavior change for the next minor bump note? (Trivial, but the release checklist will ask.)
14-50. Deliberately not padded — the honest backlog beyond the above lives in TODO_LIST.md rows 60-90 and the prior 09-15 reports' §f sections (review-loop negative-path smokes, lint-baseline CI mirror, facade parity tooling hardening, postgres conformance env-gate coverage). Refusing 37 filler rows per the docs-health HARVEST anti-patterns.

## g) Questions I can NOT figure out myself

1. **Warning scope ruling:** should the `$TQ_DB` warning extend to every bare `tq` command that opens the DB (stats/tasks/cancel/dlq/worker), or is enqueue-only deliberate because it is the only WRITE surface? This changes scripts' stderr everywhere — owner call.
2. **Suppression mechanism:** if scripts legitimately target `$TQ_DB`, is a `TQ_QUIET` env, a `--quiet` flag, or "warning is acceptable noise" the preferred escape? I will not invent a flag without a ruling.
3. **Test placement:** cmd/tq tests avoid falling back to `$TQ_DB` by convention (session_test.go:22 comment) — a warning test must SET `$TQ_DB` deliberately. Is `t.Setenv("TQ_DB", ...)` inside cmd/tq tests sanctioned, or should the test live behind the same explicit-`--db` discipline with a fake env injector?
