# ADR-0019 S2 window (12-44) — close-out + brutal self-review

Task 000001a0d802640e8d1f9e21765644ebd87c. Third window on this ID
(12-11, 12-16, 12-42 — the 12-42 report documents the work; this is its
close-out and self-review, per the close-out contract).

## Headline verdict

The S2 work item itself is **NOT DONE and stays correctly BLOCKED** behind
row 38 (S1 flip, unchecked, in flight concurrently). What this window
actually shipped is the unblocking repair: the sqlitev4/cqrsqlite/worker
module gates, left red by the concurrent claim-token sweep, are green at
HEAD again, with one REAL semantic bug found and fixed along the way
(expected-failing claim probes clobbering held claim tokens).

## a) FULLY DONE

1. sqlitev4 gate unbroken: missing `internal/queue/sqlite` relative
   replace added to `internal/queue/sqlitev4/go.mod` (the module resolved
   the stale v0.3.0 proxy facade whose `CancelOwned` still took an owner
   string). Cited: `internal/queue/sqlitev4/go.mod` replace block; the
   build error it kills is quoted verbatim in the 12-42 report.
2. Claim-token test fallout fixed in `internal/queue/sqlitev4/
   store_test.go`: unused claim vars blanked, `:=` vs `=` corrected after
   the ClaimDue signature change, and the REAL bug — expected-failing
   ErrNoTaskDue probe claims REASSIGNING the held claim variable to its
   zero value, so later `Complete` calls failed "lease not held"
   (TestDepsBlockUntilCompleted, TestProjectExclusivitySerializesPer-
   Project, TestLeaseExpiry-adjacent probes). Pinned by the module suite:
   sqlitev4 build/vet/test all rc=0.
3. TestProjectExclusivitySerializesPerProject semantic repair: the claim
   loop discarded every claim (`got, _, err`) and the later `Complete`
   used an unrelated claim from a DIFFERENT store handle; the loop now
   captures the runner's claim and completes with it. Pinned by the suite
   going green — before the fix the test could NEVER pass post-sweep.
4. cqrsqlite same fallout class fixed (store_test.go: unused/clobbered
   claims incl. the `parkOnQuestion` helper and the q-2 re-arm test
   losing their claims to `_`). Module gate rc=0.
5. replay_test.go: `Fail` called with the owner STRING "worker-2" where
   the adapter now takes a claim token → passed `claim_worker_2`.
   Package `internal/queue/sqlitev4/replay` gate rc=0.
6. worker module setup unbroken: the sqlite facade now imports the
   UNTAGGED sqlitev4 module, so every GOWORK=off consumer without a
   require+replace for it failed module setup ("unknown revision
   internal/queue/sqlitev4/v0.3.0"). worker/go.mod gained
   `require internal/queue/sqlitev4 v0.3.0` + relative replace, mirroring
   the facade's own go.mod. Worker gate rc=0.
7. gofmt applied over both spike modules (sqlitev4/adapter.go carried a
   PRE-EXISTING unformatted var block — fixed on sight).
8. Gates run and rc-captured at HEAD: root build+vet 0; sqlitev4,
   cqrsqlite, worker module gates 0; full all-module loop green; root
   -race suite run (one e2e flake, see d/e); doc gates (check-doc-refs,
   check-todo-list, check-status-index) 0.
9. TODO_LIST row 39 updated: still unchecked + BLOCKED, note extended
   with this window's repairs.
10. Report 12-42 + index row written and committed with the footer
    (commit e9322ff2).

## b) PARTIALLY DONE

1. The task itself (S2: journal on facts.Fact, consumer re-pointing,
   engine-enforced facts-in-same-tx): untouched, correctly serialized
   behind the S1 flip. The flip-independent slice (AppendFact riding the
   engine FactTx sink) was the 12-11 window's work, not mine.
2. Root -race suite: ran, but one e2e failure
   (TestAgentPoolStartupPruneSweep, "task.enqueued = 1, want 2") — passes
   in isolation, consistent with the documented load-marginal
   e2e-under-race class. I did NOT rerun the full race suite after the
   isolated confirmation (time/budget; the module loop is green and the
   failure is environmental).
3. lint-baseline: did NOT run `lint-baseline.sh --check` over the touched
   modules. My diff is test-only plus go.mod lines, and the test-noise
   classes (paralleltest/varnamelen/wrapcheck) are policy-disabled or
   test-ignored, so growth risk is low — but "low" is not "checked".
4. The gofmt fix to sqlitev4/adapter.go rode along although that file was
   not otherwise touched by me — deliberate on-sight fix, but it means my
   diff is not strictly confined to the fallout.

## c) NOT STARTED

1. S2 proper: consumer re-pointing (`internal/journal/cqrs`, webui
   tailer, sweepers, bridges) onto `facts.Fact` — blocked by row 38.
2. Engine-enforced facts-in-same-tx as the DEFAULT store behavior —
   blocked by row 38.
3. First tags for sqlitev4/cqrsqlite (the require-stale-v0.3.0-untagged
   wart every facade consumer carries until the flip cuts them).
4. Owner follow-ups filed by prior windows and still open: footerless
   daemon-code attribution ruling (12-11 §d2); S-stage dispatch
   suppression question (12-16 §g1); sqlitev4 unbreak + facade bumps ride
   the flip (12-16 §e).

## d) TOTALLY FUCKED UP

1. **The TQ_QUESTION_FILE env lie cost a diagnosis detour.** The executor
   suite "failed" — I read the agent.go gating code, suspected the clone
   logic, wrote a scratch debug test — before discovering MY OWN session
   shell leaks `TQ_QUESTION_FILE` into the test process env, which stub
   agents inherit via `os.Environ()`. `unset` → green. Lesson already
   codified for GOEXPERIMENT ("the environment lies"); I re-learned it the
   slow way with a different variable. An `env | grep TQ_` sanity check
   belongs BEFORE code-reading when a test failure smells impossible.
2. **gopls diagnostics were treated as evidence longer than they deserved.**
   Early edits were steered by stale gopls unused-var errors that
   disagreed with real `go vet`; one batch of sed line-edits blanked a
   claim variable that WAS used (undefined at 1766/2433/3338), requiring
   repair. The rule "trust the CLI, not the LSP" (already in AGENTS.md
   for templ) was applied late.
3. **Three messy edit strategies before the right one.** Multiedit
   collisions ("file modified since read"), a python pass with a broken
   regex (`^(\t),` missing the `_`), and per-line seds that initially
   turned `:=` into `=` where the variable was never declared. The final
   loop (vet → parse → targeted sed → repeat) was correct and should have
   been the FIRST move, not the fourth.
4. **The e2e -race failure nearly got mislabeled.** The background race
   job printed the failure and I moved on to module gates; only the
   ritual of re-running before concluding caught that it passes in
   isolation. A wrong "race suite red" claim in the report would have
   sent the next window chasing a ghost.
5. Minor: my first commit attempt staged TODO_LIST.md, but the daemon
   absorbed it first, so the footer commit carried only the report+index.
   Attribution still works (any footer-bearing commit cross-references),
   but the commit message says "unbreak" while the code delta lives in
   footerless daemon commits 82835fd8/f4f9e528/e0586cf3/53784097/
   21d269b1/0c122a5b — the known daemon hole, again.

## e) WHAT WE SHOULD IMPROVE

1. Canonicalize the env-lie check: before diagnosing ANY test failure,
   dump `env | grep -E '^(TQ_|GOEXPERIMENT|GOTOOLCHAIN)'` — the session
   shell leaks pool/agent variables that tests inherit. Candidate for a
   session-start ritual line.
2. Extend "trust the CLI, not the LSP" beyond templ: gopls compiler
   diagnostics in test files went stale mid-edit; the vet-loop is the
   only source of truth.
3. The claim-token sweep that broke three modules was absorbed by the
   daemon without its own gate run — the 12-16 window caught it, I
   repaired it. A per-module gate on ALL go.mod-touching commits (not
   just ci-local pre-push) would catch this at birth.
4. Untagged spike modules required by facades are a structural trap:
   every consumer must hand-carry require+replace. The flip's tag-cutting
   step should also bump the facade consumers in one sweep (already
   tracked as "facade bumps ride the flip").
5. The iterative vet-fix loop should live in `scripts/` as a tiny helper
   (vet → fix unused-var/NoNewVar mechanically → repeat) — three windows
   have now needed it for sweep fallout.

## f) NEXT (up to 50, deduplicated against 12-11 §f 1-25 and 12-16 §f 26-50 which remain open — these are the DELTA items 51+)

1. Land the S1 flip (row 38) — the gate for everything below.
2. Cut first tags for internal/queue/sqlitev4 (+ replay) and
   internal/queue/cqrsqlite so facade consumers stop hand-carrying
   replaces.
3. After 2: sweep the require+replace pairs I hand-added (worker, any
   other facade consumer) onto the real tags.
4. Run `lint-baseline.sh --check` clean-cache over sqlitev4/cqrsqlite to
   confirm this window's test edits grew no baseline rows.
5. Re-run the full root -race suite on a quiet host to confirm the e2e
   flake is load-only.
6. Add the env-hygiene check (e1) to scripts/session-start.sh output.
7. Consider a `scripts/fix-sweep-fallout.sh` helper (e5).
8. Verify no OTHER GOWORK=off consumer of the sqlite facade is missing
   the sqlitev4 require+replace (grep all go.mods) — worker was the one
   the loop caught; a targeted audit is cheap insurance.
9. When row 38 lands: start S2 consumer re-pointing in the serialized
   order the ADR names (journal/cqrs first, then webui tailer, then
   sweepers, then bridges).
10. When S2 lands: verify facts-in-same-tx is engine-enforced by a
    conformance pin (an attempted state-change without its fact must
    fail), not just by construction.
11. File the TQ_QUESTION_FILE env-leak root cause: something in this
    host's agent/session tooling exports it session-wide — find what and
    stop the leak (it poisons any executor test on this host).
12. Archive sweep for docs/status (index bloat warning fired: 214 live
    rows vs 100 threshold — docs-health ANNOTATE run owed).

(12 items — the delta is small by design: this window's job was repair,
not new surface. Items 1-50 from the two prior close-outs remain the real
backlog for S2-S4.)

## g) QUESTIONS I CANNOT ANSWER MYSELF

1. **Footerless daemon code: is the follow-up-footer-commit maneuver now
   the SANCTIONED heal?** Two windows (12-11, this one) have shipped code
   via daemon commits and attributed via a separate footer commit. If a
   third window repeats this, I want a ruling (amend-into-daemon-commit
   vs follow-up commit vs accept-the-hole) so all windows stop guessing.
2. **Should blocked-row re-dispatch be suppressed mechanically?** This is
   the third budget spend on the same ID while row 38 holds the gate.
   The 12-16 §g1 proposal (a `— BLOCKED-ON: <row>` convention the queue
   can parse) is still pending — I re-raise it with three data points.
3. **Is sqlitev4's require of `internal/queue/sqlite v0.3.0` (stale tag,
   papered over by my new replace) acceptable until the flip, or should
   the spike modules drop the old-facade dependency entirely?** The
   replace makes local builds honest, but every fresh `go mod download`
   outside the repo still resolves the stale proxy copy; the replay tool
   genuinely needs the OLD store (to seed the old journal), so the dep
   is real — I chose require+replace. Confirm or overrule.
