# Fullcore postgres run n=3 verification close-out — task 000001a098818cdddd591ba3ff86c538e973

2026-09-13 04:15 CEST · third same-ID dispatch · verification-plus-annotation session

## Context

Dispatch arrived as a TODO_LIST "Fullcore-window follow-ups" item:
"Run examples/fullcore once against a throwaway postgres DB (`--backend postgres`;
the example applies its schema — keep it pointed at a local/test database)
(06:25 report c2)". At turn 1 the item was already `[x]` with a DONE citation:
the n=1 session fixed a REAL nil-store shadowing bug (work commit `3faaf0b`)
and the n=2 session re-verified (`da52369`). Per this repo's
"claims carry citations" rule I did NOT rubber-stamp: I independently
re-ran the claim end-to-end (n=3) on a FRESH throwaway cluster.

## a) FULLY DONE

1. **n=3 independent postgres re-verification** — fresh `initdb` cluster on
   127.0.0.1:55433 (db `taskqueue`, trust auth, unix socket redirected to
   `/tmp` after `/run/postgresql` proved root-owned on this host);
   `GOEXPERIMENT=jsonv2 go build -o /tmp/tq-fullcore ./examples/fullcore` +
   run with `--backend postgres` → **drained 4/4 completed including the
   flaky retry** ("flaky: succeeded on retry", "completed 4"). The 3faaf0b
   nil-store fix held on a clean database with no residual schema.
2. **Cleanup with no residue** — `pg_ctl stop`, all scratch files removed
   (`/tmp/tq-fullcore-pg3*`, `/tmp/tq-fullcore`). Nothing for the daemon to
   sweep, no fixture inside any gated tree.
3. **TODO_LIST row 135 annotation extended** with the n=3 evidence
   (port, /tmp-socket gotcha, 4/4 result, fix-held verdict).
4. **Work commit `68aeeaa`** — TODO_LIST.md only, exactly one well-formed
   Task-Queue-ID footer (commit-msg hook passed).
5. **First-hand DONE-on-arrival detection done right** — turn 1 grep of
   TODO_LIST + `git log --oneline -5` surfaced the prior work and its
   citation; verification was RUN (not assumed) before any close-out claim.

## b) PARTIALLY DONE (skipped-declared)

- **No fresh full test gate** (`go test ./... -race`, sub-module loop,
  ci-local, nix): the only deliverable change is a one-line TODO_LIST
  annotation; the example run itself exercised the root-module build.
  Declared, per the docs-only precedent of the n=2 close-out.
- **No CHANGELOG entry** for this session: it changed zero behavior or
  deliverable code; the bug fix itself already has its close-out trail.
  (Flagging rather than silently omitting — §g2 asks for a ruling.)
- Report is **Markdown**, overriding the status-report skill's HTML default —
  the dispatch explicitly pins `docs/status/<ts>_task-<id>.md` and that is
  this repo's indexed convention.

## c) NOT STARTED

- Nothing within the dispatched scope. Neighboring backlog items
  (hermetic `scripts/smoke/fullcore.sh`, drain-deadline N-times
  determinism, TODO rows 136/148) remain unchecked by design — out of
  scope for this dispatch.

## d) TOTALLY FUCKED UP (honest ledger)

1. **Prior same-ID close-outs not grepped at turn 1** — the 04-07 and 04-09
   reports for THIS task id sat in `docs/status/` and the index, and I only
   discovered them at report time via the README index. The n≥2-cluster
   "unread prior close-out" class recurs here (3rd+ occurrence this
   window). Damage: none (my run was additive verification, no conflicting
   claims), but the turn-1 ritual still isn't mechanical for me.
2. **Two burned `pg_ctl` startup attempts** — first failure: `/run/postgresql`
   lock permission (predictable on NixOS, root-owned socket dir); second:
   the custom `unix_socket_directories` dir didn't exist yet. The cluster
   was up on attempt 3. Cost: ~10 seconds; the final config (socket in
   /tmp, dir pre-created) is now recorded in the TODO annotation so the
   next run skips the dance.
3. **Initial report framing risk** — turn 1 output led with "already done";
   only the immediate in-message re-verify kept that from being an
   uncited rubber-stamp (the exact failure class the 03-58 verification-
   claims report documents). The gap between framing and verification
   should be zero.
4. **`git log --oneline -5` view was truncated to the same cluster** —
   it showed only this task's family of commits; a wider `git log` /
   `git status` sweep (session-start ritual per AGENTS.md) was not run
   before editing TODO_LIST.md. Clean `git status` at commit time
   confirms no concurrent work was clobbered, but by luck, not process.

## e) WHAT WE SHOULD IMPROVE

- **Turn-1 mechanical checklist for re-dispatches**: grep TODO row +
  `git log` + `grep -rl <task-id> docs/status/` in ONE command before any
  verdict; the same-ID close-out cluster is now 5+ sessions wide and every
  report documents missing this.
- **Hermetic fullcore smoke (TODO row 136)** would have turned this entire
  dispatch into `TQ_BIN=... ./scripts/smoke/fullcore.sh --backend postgres`
  — one command, no hand-rolled initdb, no socket gotchas. It remains the
  highest-leverage item in this backlog section.
- **Postgres throwaway-cluster recipe** belongs in the smoke script or a
  doc snippet: initdb `-A trust`, `-c unix_socket_directories=<writable dir>`
  (pre-create it), port >55432, explicit teardown. Today it lives only in
  session memory + a TODO annotation.
- **Verification-mode dispatches should state "verify-only, expected
  DONE-on-arrival"** in the item text — it would let the agent skip
  re-deriving scope and spend the whole turn on the re-run (which is the
  only part with value).

## f) Up to 50 things to get done next (brainstorm — HARVEST with routing rigor; most beyond the top ~10 are ROADMAP fuel)

1. `scripts/smoke/fullcore.sh` — hermetic sqlite-path smoke with explicit
   `TQ_DB=<scratch>` (TODO row 136; top of this section, still open).
2. Extend that smoke with a `--backend postgres` variant using the
   throwaway-cluster recipe above (temp initdb, /tmp socket, teardown).
3. Drain-deadline determinism: N-times exercise of the deadline path
   (TODO row 148, still open).
4. Dispatch-time same-ID dedup guard: harvest should skip items whose
   task id already has a close-out in `docs/status/` (the root surface of
   this 5-report cluster).
5. Turn-1 mechanical check script for verification-only dispatches
   (TODO row + git log + prior close-out grep, one command, cited in the
   DONE note).
6. Document the /run/postgresql root-owned-socket gotcha wherever the
   postgres backend workflow is described (postgres module README/AGENTS).
7. Postgres parity for the session-close bridge (`AppendFact` on postgres)
   — flagged OPEN in the 2026-09-12 session-bridge design doc.
8. Owner rulings still pending from prior same-ID close-outs (daemon
   footer policy, TQ_RESULT commit_sha semantics, examples CHANGELOG
   scope) — consolidate them into one ruling request instead of N.
9. Closeout-report placement decision (TODO row: BLOCKED on owner call).
10. Status/closeout TODO-append caps decision (BLOCKED on owner call).
11. Re-run `tq show --commits` against this task id to confirm both
    commits (68aeeaa work + this close-out) resolve footer=1 — mechanical
    cross-reference proof for the pair.
12. Sweep other `examples/` entries for the run-never-happened class
    (fullcore was never executed until 2026-09-13; are api/sse proven?).
13. Shadow-assignment vet rule (`store, err :=` inside switch cases) as a
    lint/nolint-baseline-safe check for examples/.
14. Consider `errors.Join`/panic guard in fullcore so a nil store fails
    with a named error instead of reaching Enqueue (defense in depth on
    top of 3faaf0b).
15. Record the n=3 run in CHANGELOG only if a ruling says verification
    runs deserve entries (pairs with §g2).
16. Add the postgres throwaway recipe to the release-testing checklist in
    docs/release/RELEASE.md (backend parity gates exist; the recipe
    doesn't).
17. Annotate + archive the six 2026-09-07 reports per the docs-health
    continuation item in the backlog.
18. Verify the commit-msg hook accepted this session's two footers as
    distinct commits with the same ID (ID-reuse is allowed by design —
    confirm the hook log shows no warnings).
19. Nightly fuzz: confirm last night's campaign committed any new seeds
    (in passing — untouched this session).
20. VendorHash check on next go.mod change remains the standing trap —
    no dependency changes this session, nothing to do now.
    21-50. Filler honestly labeled: the remaining backlog in TODO_LIST.md
    (docs-health continuation, BLOCKED owner calls, push-gated items)
    already enumerates the real queue; inventing 30 more rows here would
    be noise, not signal. Route this report through HARVEST and let it
    dedup against rows 135-148.

## g) Questions I can NOT figure out myself

1. **Same-ID dispatch intent**: this task id has now produced three
   close-outs (04-07, 04-09, this) plus a prior session's work commit.
   Is n=3 a deliberate redundancy policy (independent re-verification
   with fresh clusters each time), or a harvester-vs-checked-item dedup
   race? If deliberate, is there a cap where a further dispatch should
   reply DONE-on-arrival without a new cluster run?
2. **TQ_RESULT `commit_sha` semantics for multi-commit tasks**: work
   commit (68aeeaa) vs close-out commit (this report) — which should the
   final line carry? Three prior reports asked; the queue gate reads the
   last line, so an answer would let every future close-out be uniform.
3. **CHANGELOG scope for verification sessions**: does a pure
   re-verification with zero deliverable changes (this session) ever
   warrant a CHANGELOG line, or is the TODO annotation + report the
   complete record?
