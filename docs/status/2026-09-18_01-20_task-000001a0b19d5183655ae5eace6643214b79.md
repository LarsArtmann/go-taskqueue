# Task 000001a0b19d5183655ae5eace6643214b79 — Second verify window on the session-volume row

**Date:** 2026-09-18 01:20 CEST
**Outcome:** DONE-on-arrival — verification-only window, zero code changes.
**Authority:** the implementation close-out is
`docs/status/2026-09-18_01-12_task-000001a0b19d5183655ae5eace6643214b79.md`
(§a–g authoritative, NOT restated); the first verify window is
`docs/status/2026-09-18_01-16_task-000001a0b19d5183655ae5eace6643214b79.md`.
This report records only what THIS window did and noticed. Format note: the
user's dispatch explicitly asked for a Markdown file at the `_task-<id>.md`
path — the status-report skill's HTML default is overridden by that
instruction for this report only.

## a) Fully done (this window)

- Turn-1 ritual grep found the row already `[x]` (TODO_LIST:113) with the
  footer commits on master: `cfcd190` (implementation) + `3f4164b` /
  `85cd97c` (the two prior reports), AND two prior verify reports for this
  exact ID already filed (01-12, 01-16). Response per protocol: verify →
  cite → stop. No code, no TODO edit, no duplicate implementation.
- Fresh gate re-run green at HEAD: `go build ./...` (GOEXPERIMENT=jsonv2)
  ok; `./scripts/test-cmd-tq.sh` full module gate ok (8.9s) — broader than
  the 01-16 window, which ran the two pinning tests via `-run`.
- This report + index row is the only artifact.

## b) Partially done

- Nothing this window — no partial deliverables. Open gaps from the 01-12
  §b stand unchanged (lamp CSS, all-time volume, unrun webui smoke,
  CHANGELOG silence, postgres CountFacts parity note).

## c) Not started

- Everything in the 01-12 §f list (50 items) and the adjacent unchecked
  session rows (114 safety pins, 115 multi-repo semantics, 116 ergonomics,
  117 gosec re-cite, 118–120 `tq crush` docs, plus the blocked row on crush
  #3146). Nothing new discovered this window; no backlog appends.

## d) What went wrong (this window)

- **The dispatch itself was a re-dispatch**: this is the FIFTH window on
  this task ID (01-12 implementation, 01-16 verify, 01-20 verify) after the
  00-57/01-01/00-14 same-class burns the 01-16 report counted. The queue
  minted a window whose correct response was three shell commands and a
  report. Root cause remains the unbuilt done-row dedup guard (01-12 §f12).
- **Wrong verify gate, once, again**: first attempted root-level
  `go test ./cmd/tq/...` — fails by design (cmd/tq is its own replace-free
  module, ADR-0017). FIFTH occurrence of this exact miss class across the
  windows on this ID (00-57, 00-14, 01-16 each recorded it; mine repeated
  it within one turn before self-correcting to `scripts/test-cmd-tq.sh`).
  The session-ritual script candidate from 01-16 §d is now overdue.
- No other defects: no edits, no reverts, no daemon surprises.

## e) What to improve

- Verify-only windows need exactly three artifacts (gate output, index row,
  report) and nothing else — this window complied. But the deeper fix is
  mint-side: a done-check against TODO_LIST `[x]` rows + footer-bearing
  commits before a window is minted would have saved all five windows.
- The repeated wrong-gate miss is now a ritual problem, not a knowledge
  problem: the correct command per surface (`scripts/test-cmd-tq.sh` for
  cmd/tq) should be embedded in whatever session-start checklist runs
  before verification, not re-derived from AGENTS.md each time.

## f) Next (carried; the 01-12 §f list of 50 stays the authoritative backlog source — top items re-ranked with this window's evidence)

1. ★ Done-row dedup guard at mint time (now proven with FIVE burned windows
   on this task-class: 00-57, 01-01, 00-14, 01-16, 01-20) — the highest-
   leverage queue-infrastructure task.
2. ★ Session-ritual verify-gate script: per-surface correct gate commands
   (cmd/tq → `scripts/test-cmd-tq.sh`, never root `go test ./cmd/tq`) —
   FIVE occurrences of the wrong-gate miss across these windows.
3. ★ `card-sessions-lamp` CSS: hot tone when open sessions > 0 (theme.css +
   `nix run .#webui-css`); 01-12 §f1.
4. ★ Windowed session volume (`tq stats --since`); 01-12 §f2.
5. ★ Webui smoke re-cite over a build carrying the sessions segment
   (`scripts/smoke/webui.sh`); 01-12 §f3.
6. CHANGELOG entry for the session observability batch (rows 110/111/113);
   01-12 §f8.
7. FEATURES.md row for the session observability surfaces; 01-12 §f4.
8. Postgres conformance subtest for CountFacts on session fact types;
   01-12 §f5.
9. Session fact types in the `tq serve` `?type=` filter allowlist; 01-12 §f7.
10. httpapi stats parity decision for the sessions keys; 01-12 §f10 (also
    §g3 below).
11. Open-session age in the lamp tooltip; 01-12 §f11.
12. Row 114: session-bridge safety pins (prune-stale external-provenance
    guard, sweeper-namespace cross-dedup, trailer edge tests).
13. Row 115: multi-repo close semantics decision.
14. Row 116: session ergonomics (git ≥ 2.15 guard, zero-commit hint).
15. Row 117: gosec post-config scan re-cite over internal/session +
    cmd/tq session call sites.
16. Row 118: AGENTS.md cmd/tq subcommand list + `tq crush` note.
17. Row 119: re-cite session-close.sh over the runSessionClose extraction
    as used by `tq crush`.
18. Row 120: README + FEATURES docs for `tq crush`.
19. Live-no-id smoke assertion for the wrapper (row 121).
20. Row 135: dead-export audit over internal/session.
21. Row 136: real-agent session E2E dogfood (owner-gated spend).
22. Drill-down link from the lamp once a session-view webui surface exists;
    01-12 §f6.
23. Lamp click-through anchor; 01-12 §f24.
24. `sessionStats` N+1 measurement / batching; 01-12 §f25.
25. `session.List` watermark-cached count if snapshots slow; 01-12 §f37.
26. Per-project session volume scoping decision; 01-12 §f27.
27. Consolidated JSON omitempty contract test (parked/open_sessions/sessions_*),
    01-12 §f28.
28. loadSnapshot error path pin when CountFacts fails; 01-12 §f38.
29. Windows CI TZ check for the fragment test; 01-12 §f29.
30. `session.Stats(ctx, store)` extraction on a third consumer (three-strike);
    01-12 §f31.
31. `tq top` segments parity check; 01-12 §f43.
32. `tq stats` help text mention of the sessions line; 01-12 §f34.
33. `tq facts --type session.opened --json` count fast-path; 01-12 §f44.
34. Session-fact retention policy — owner ruling (journal is append-only);
    01-12 §f36.
35. check-webui-css green citation over the fragments regen; 01-12 §f33.
36. Unchecked-sessions-without-registry-entry lamp severity; 01-12 §f35.
37. SSE swap re-render verification on a live session open; 01-12 §f40.
38. Human-output phrasing pass ("sessions N opened / M closed" vs
    "open sessions N"); 01-12 §f45.
39. stats_test.go captureStdout/JSON helper extraction (4th use); 01-12 §f41.
40. Since-unix-epoch constant making the all-time choice explicit; 01-12 §f47.
41. Grep for downstream consumers of the new stats JSON keys in scripts/;
    01-12 §f48.
42. AGENTS.md session bullet note that stats/webui carry the volume surface
    (memory maintenance); 01-12 §f49.
43. Merge TestStatsOpenSessions' bare-store assertion with the new keys'
    (01-12 §f21).
44. Doctor context line carrying volume; 01-12 §f23.
45. Fact-bucket sparkline session.opened markers; 01-12 §f26.
46. Detail-page header segment consideration; 01-12 §f46.
47. `sessionStats` Server-method vs free-function consistency nit; 01-12 §f30.
48. Adoption-table prose note if a templ-components component ever replaces
    the segment; 01-12 §f32.
49. Centralize nowband marker-class assertions in webui_test; 01-12 §f39.
50. Row 135-neighborhood: confirm no dead exports added by the stats/webui
    session surfaces (cheap sweep riding the next dead-export pass).

## g) Questions I cannot answer myself (carried verbatim from 01-12 §g; unchanged because only the owner can rule)

1. **Lamp styling authority**: alarm red (crash signal) or neutral amber
   (possibly a legitimately long session) for `card-sessions-lamp` when
   open > 0?
2. **Windowed vs all-time volume**: is all-time the right default for
   `tq stats` sessions, or a `--since` window (7d default)?
3. **httpapi parity**: should `GET /api/v1/stats` also carry
   `sessions_opened/closed`, or is session observability dashboard-only?
