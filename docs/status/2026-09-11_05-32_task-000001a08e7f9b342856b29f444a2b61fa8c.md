# Task 000001a08e7f9b342856b29f444a2b61fa8c — fourth-dispatch verification close-out

**Date:** 2026-09-11 05:32
**Work item:** harvest skip-log change detection — log a skip class only when its
example changes (or on first tick) instead of every 5m tick flooding journald
(02:00 f17; cmd/tq/main.go)
**Work commit:** 783f4bf (shipped by the first dispatch; closed out by fb7a6fa,
re-verified by the 05-28 window)
**This window:** verification only — zero code diff. Fourth dispatch of the
same task ID.

## a) FULLY DONE (this window)

- Session-start ritual (`git log --oneline -8`, `git status`, stash list)
  immediately showed the item pre-shipped: 783f4bf implements the change,
  fb7a6fa and 00ba1cb are the prior dispatches' close-outs. Working tree
  clean, no stash.
- Implementation confirmed present: `skipLogExamples map[string]string` gate
  at cmd/tq/main.go:790-844 (log only on first sight or example change;
  per-tick `skipped=N` summary retained at main.go:848), `groupedSkips`
  class derivation at main.go:1063-1081.
- TODO_LIST.md:80 already `[x]` — loop was closed by an earlier window;
  nothing to change.
- Full standard root gate re-run fresh (trust nothing): `GOEXPERIMENT=jsonv2
  go build ./... && go vet ./... && go test ./... -race` — all green.
- Full a)-g) close-out report written and indexed in
  docs/status/README.md (this file), committed with the task footer.

## b) PARTIALLY DONE

- Nothing in scope. The item is a bounded logging change; it shipped whole.

## c) NOT STARTED

- Nothing in scope. Adjacent open threads live in TODO_LIST.md and the
  05-28 report's next-work list: `tq doctor` PATH warning (02:00 f18),
  dead-pool PapDashboard alert (02:00 f6), scan-fail re-warn cadence owner
  decision, and the re-fire root cause (§f1 below).

## d) TOTALLY FUCKED UP

Nothing broke this window. Honest ledger:

1. **I am the FOURTH dispatch of this exact task ID.** The re-fire loop is
   now a pattern (three previous windows said the same thing I am about to
   say). Every stale dispatch burns a full agent window on verification
   theater. The root cause (why a `[x]` item re-enqueues) is named in three
   prior reports and still not diagnosed — this repetition is itself the
   strongest evidence it is a queue/harvest bug, not a fluke.
2. **My first-pass TQ_RESULT (work turn) again closed the loop without
   noticing that a close-out report was contractually owed.** The queue's
   close-out turn had to remind me. Same wobble the 05-28 report logged in
   its §d2 — the close-out reflex should fire on the work turn, not after.
3. **The gate I ran is the root-module gate only.** The per-sub-module loop
   and `ci-local.sh` replicant were not re-run for a zero-diff window
   (master was green at 00ba1cb, and I changed nothing, so the risk is nil —
   but "green" claims here have a history of exactly this qualification gap).

## e) WHAT WE SHOULD IMPROVE

1. **Break the stale-dispatch loop.** Four windows on one already-`[x]` item
   is no longer bad luck; it is a systemic defect (harvest dedup vs
   completion propagation). Fix §f1 or the pool will keep paying for the
   same work forever.
2. **Make no-op windows cheap.** A `verdict: already-done` write-back with
   no report file (05-28 §g3) would have made three of the four dispatches
   one-command close-outs instead of full reports.
3. **Extract the skip-log gate from the runTick closure** into a testable
   helper with a unit test (05-26 e3, still open) — it is the one piece of
   783f4bf no test covers.
4. **Fix the truncation-collision and count-semantics residue** from the
   shipped gate (05-26 e1/e2): suppression keyed on a truncated example can
   hide a genuinely new reason, and `count` is per-tick rather than
   since-last-log.

## f) Up to 50 things we should get done next

Tiered by impact/effort. Items 1-30 carry forward the 05-28 list (all still
open as of this window — the 05-28→05-32 delta shipped none of them);
31-50 are this window's additions.

### Tier 1 — do next (high impact, bounded)

1. Root-cause the task re-fire: why does an item marked `[x]` (and closed
   out three times) re-enqueue? Inspect harvest dedup keys vs completion
   propagation. Kills the whole stale-dispatch class.
2. `tq doctor`: warn when crush/git/go are missing from PATH. (02:00 f18;
   the exact failure class that killed the deployed pool for 20h)
3. Dead-pool detection: PapDashboard alert when scan-failed skips cover
   every repo for N consecutive ticks. (02:00 f6)
4. Owner decision + implementation: scan-failed re-warn cadence (every Nth
   tick vs once-ever). (05-26 §g1 escalation, still unanswered)
5. Extract the skip-log gate from runTick into a testable helper + unit
   test. (05-26 e3)
6. `tq pool-health`: one-shot per-repo skip-streak + last-harvest liveness
   summary. (02:00 f27)
7. Dogfood smoke: scripts/smoke/dogfood-once.sh (env-gated TQ_DOGFOOD=1).
   (02:00 f10)
8. Archive the 2026-09-10 dogfood proof journal from /tmp before reboot.
   (02:00 f9)
9. Fix truncated-example suppression collision: key the gate on the FULL
   reason or document why truncation-keying is correct. (05-26 e1)
10. Fix skip-log `count` semantics: log count-since-last-log, not per-tick
    count. (05-26 e2)

### Tier 2 — quality debt

11. Prove or bound `skipLogExamples` map growth; cap if unproven. (05-26 e4)
12. Run `./scripts/ci-local.sh` once on master post-05-2x/05-3x burst to
    confirm the multi-module gate over the combined concurrent diff.
13. Reconcile agent-pool's inline repo-join with expandRepoSpecs (cwd
    divergence flagged in the 04-43 report; intent unknown).
14. Verify the CQA bridge against a live CQA instance (BLOCKED on owner:
    URL + token).
15. Worktree-per-agent design doc. (02:00 f15)
16. Session-close bridge: prototype the PreToolUse session-registry trigger.
17. Dead-letter retry storm guard: cap re-enqueues per day per dedup key
    even under trivial text changes.
18. `tq tasks --stale`: list queued tasks whose TODO_LIST item is now `[x]`
    (would have caught THIS re-dispatch in seconds).
19. `TQ_RESULT` outcome convention ("already-done" vs "fixed" vs "failed")
    so the queue can statistically separate no-op verifications from work.
20. Status-index: fail CLOSED — reject a NEW unindexed report in the diff.
    (05-17 §e, still open)

### Tier 3 — hardening / hygiene

21. Per-tick harvest summary: per-class counts at debug level.
22. Docs: document once-per-example skip-log semantics in FEATURES.md /
    AGENTS.md agent-pool section.
23. Deploy/systemd README: note the journald flood fix for operators.
24. Two-tick stub-agent smoke counting skip-class log lines (regression net
    for §f5's helper extraction).
25. `tq watermarks show`: human-readable lag durations.
26. Audit the tick loop for OTHER unconditional per-tick slog floods
    (f17 was fixed class-by-class; siblings may exist).
27. FuzzParseRepo nightly: confirm last campaign committed seeds, green.
28. check-dead-exports.sh over this week's diff surface (advisory, cheap).
29. TODO_LIST: annotate the two long design items (session-close,
    worktree-per-agent) with owner/next-action so they stop resurfacing
    without moving.
30. CHANGELOG: confirm 783f4bf is in the unreleased section (append-only,
    one line — unverified this window).

### Tier 4 — this window's additions

31. Close-out-turn detection on the WORK turn: if the work turn's diff is
    empty AND the TODO item is `[x]`, emit the report + TQ_RESULT in the
    same window instead of splitting across turns (the 05-26/05-28/05-32
    pattern all needed the reminder).
32. Consider a per-task dispatch counter surfaced in the prompt ("this is
    dispatch #4") — stale dispatches become self-evident to the agent.
33. Prune docs/status/README.md index rows once reports archive (index is
    growing ~6 rows/day; archived/ files already drop their rows — verify
    that convention is being followed).
34. Report convention: a short "SUPERSEDES" field for verification-only
    reports so readers of README.md can skip straight to the substantive
    close-out (05-26).
35. Add the `-race` qualifier to AGENTS.md's standard gate line only if the
    concurrent-agents warning section already covers why it sometimes
    flakes on this host — check before editing (noticed the tension while
    re-running the gate).
36. Sweep `grep -rn "close-out"` in AGENTS.md contract vs the actual
    close-out turn prompt: the §d2 wobble (work turn missing the close-out
    reflex) suggests the contract text could state WHO triggers the report
    unambiguously.
37. Verify the six reports 00ba1cb indexed actually satisfy
    check-status-index.sh (rerun the hook script directly, don't trust the
    commit's success alone — the daemon commits through the same gate, but
    an empty-diff edge case may bypass it).
38. Sweep the 2026-09-10/11 report pile for owner-gated questions that have
    been asked 2+ times without an answer and consolidate them into ONE
    owner-decision list file (the scan-fail question has now appeared in
    three consecutive reports).
39. `tq stats`: surface dispatch count per task ID (data is already in the
    journal facts; a stale-dispatch dashboard kills this class visually).
40. Check whether harvest still enqueues on `[x]` items race-first: read
    prune-stale ordering (sweep runs before first harvest tick? after?) —
    if prune runs first, the re-fire cannot be a harvest race and must be
    the completion write-back.
41. Add a regression test for the `DedupKey` suppression of cancelled/dead
    tasks vs re-edited text (AGENTS.md documents the contract; find the
    test, add if missing).
42. Audit `groupedSkips` class splitting for reasons containing multiple
    colons (`scan failed: repo X: err`) — class is text-before-first-colon;
    confirm the example truncation keeps enough discriminator.
43. Document in AGENTS.md Known Issues that concurrent same-task dispatches
    ARE happening (4x on fa8c) with the pattern to recognize, so the next
    agent's session-start ritual includes "grep TODO_LIST for the item's
    [x] state FIRST".
44. Consider tombstoning dispatched task IDs in the journal so `tq enqueue`
    can refuse a re-dispatch of an already-completed ID with a warning
    instead of silently queueing it.
45. Review the 05-26 report's §e items 1-4 against the shipped 783f4bf code
    once more before implementing §f9/f10/f11 — the code may have shifted
    in the concurrent windows (verify, don't trust, per repo policy).
46. `tq show <id>`: include the harvest item text + dedup key for harvested
    tasks (would make re-fire forensics a one-command read).
47. Run `scripts/check-todo-list.sh` standalone this window's diff was
    empty, but confirm it would have caught a malformed `[x]` append from
    a concurrent agent.
48. Confirm the auto-commit daemon did not land anything between my work
    turn and this close-out (re-check `git status` — it was clean; a final
    pre-commit re-check is the convention).
49. Evaluate whether docs/status/README.md index rows for verification-only
    reports could carry a `(no-op)` marker so the index stays scannable.
50. After §f1 lands, retro-link all four fa8c reports to the fix commit so
    the causal chain is navigable from any entry point.

## g) Questions I could NOT figure out myself

1. **Why does this task keep re-firing?** (carried from 05-28 §g1, now with
   a fourth data point). The TODO item was `[x]`, the DedupKey contract says
   a completed harvest item's key still suppresses re-enqueue, yet the ID
   came back three times post-close-out. From inside one window I cannot
   see the enqueuer's decision path at the moment of re-fire. Is someone or
   something (manual enqueue? a sweep with a rewound watermark?) bypassing
   dedup?
2. **Scan-fail re-warn cadence** (carried from 05-26 §g1 / 05-28 §g2, owner
   verdict still pending): once-ever logging + `skipped=N` summary for a
   persistently failing repo, or re-warn every Nth tick? This blocks §f4.
3. **Is a full indexed report the right cost for a no-op verification
   window?** (carried from 05-28 §g3, fourth occurrence). A
   `verdict: already-done` write-back with no report file would save three
   of this task's four windows, at the cost of per-dispatch auditability in
   docs/status/. Owner call on the contract trade.
