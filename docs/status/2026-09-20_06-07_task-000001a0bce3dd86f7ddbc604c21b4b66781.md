# Status report — dead-SHA baseline shrink pass, THIRD dispatch (verify-only again)

Window: 2026-09-20 06:05–06:07 CEST. This is the THIRD dispatch of task
000001a0bce3dd86f7ddbc604c21b4b66781. The work was completed by the
05:16-ish window (report:
docs/status/2026-09-20_05-57_task-000001a0bce3dd86f7ddbc604c21b4b66781.md),
re-verified by the second dispatch
(docs/status/2026-09-20_06-04_task-<id>.md), and re-verified again here.
Per the session-start ritual (grep `docs/status/` for the task ID first),
this window identified the DONE state, ran the gates fresh, and stopped —
no code, baseline, or doc content changed; this report is the only commit.

## Fresh verification (this window, 06:06)

- `git log --oneline -5`: HEAD 3b4c388 (verify-only re-dispatch close-out
  from the second dispatch); tree clean; no stash.
- `./scripts/check-dead-sha-refs.sh` → `dead-sha refs ok (104 baselined,
  0 new)`, rc=0.
- `TQ_DEAD_SHA_VERBOSE=1`: all STALE lines are baselined cite-sites; zero
  new dead citations.
- Baseline file: 41 lines, 28 SHAs (22 no-twin intermediates + 4
  wrong-twin folds + dd543ec/e9ef358 sanctioned fork-note pair).
- TODO_LIST row 141 `[x]` with full DONE note; row 143 absorbed + `[x]`.

## a) FULLY DONE (by the first dispatch; re-confirmed 3×)

1. Row 141 itself: 214/242 dead SHAs re-resolved to reachable twins via
   staged signature matching over `git log --all --reflog`; 971
   cite-sites repointed (AGENTS.md + TODO_LIST.md + docs/status/*.md);
   baseline 242→28 SHAs; gate rc=0 after every baseline edit.
2. AGENTS.md:758/793 re-points (row 143), absorbed into the pass.
3. The dead-SHA gate itself (row 140): `scripts/check-dead-sha-refs.sh`,
   HARD ci-local step.

## b) PARTIALLY DONE

1. The 28-SHA residue: disposition documented (rows above), resolution
   pending — backlog row 148 owns positional mapping or the
   citation-loss ruling.

## c) NOT STARTED

1. Row 148: positional mapping of the residue (or drop-on-refs/original
   ruling) — TIME-SENSITIVE while reflog + refs/original still hold the
   old lineage; hard prerequisite for any `git gc`.
2. Row 142: `--self-test` pin for the gate (mirror of check-gosec.sh).

## d) TOTALLY FUCKED UP

Nothing in the work — but the DISPATCH LAYER is broken: the same task ID
has now fired THREE times within ~50 minutes, twice after the row was
closed `[x]` with gate evidence in the DONE note. The turn-1 ritual saved
each re-dispatch from destructive re-work, but the queue kept spending a
window (and a report commit) per duplicate. Root cause is upstream of any
agent: the row's DONE state is visible in TODO_LIST.md, yet the dispatcher
re-enqueued it.

## e) WHAT WE SHOULD IMPROVE

1. **Re-dispatch loop**: a reworded/duplicated TODO row re-fires after
   closure. The 2026-09-17 dedup-gated status prompt addressed report
   minting; the HARVEST side re-dispatching an `[x]`-adjacent row three
   times needs the same treatment (dedup on task-ID history in the
   journal, not just dedup keys).
2. **Verbose STALE count keeps drifting** (82 → 88 → 100 → 104 across
   four readings): every report ABOUT the pass cites the dead SHAs it
   describes, growing the baselined cite-site count. Either exempt
   `docs/status/2026-09-20_0*task-<this-id>*` heal-forensics reports, or
   accept the drift and stop treating the count as a shrink metric.
3. **Row-148 urgency is implicit**: the residue is only resolvable while
   reflog/refs/original survive; nothing in the queue enforces ordering
   against a `git gc`. A `— BLOCKED:`-style marker or explicit ordering
   in TODO_LIST would make the dependency mechanical.

## f) NEXT (queue-owned, one per line in TODO_LIST where missing)

1. Row 148: positional mapping of the 28 residue SHAs before any gc.
2. Row 142: `--self-test` mode for check-dead-sha-refs.sh + ci-local wire.
3. Owner ruling: drop refs/original only after 1 (existing backlog row).
4. Dispatch dedup: harvest/queue-side suppression of re-enqueueing a row
   whose DONE note already cites this exact task ID.
5. Gate metric: decide whether verbose STALE count is a shrink target or
   advisory noise given self-citing heal reports (see §e2).
6. Consider a one-line `tq doctor`-style check that warns when
   `refs/original` or reflog entries covering the 2026-09-10/09-19
   lineages are within gc.prune-expiry grace.

(Items 1–3 already exist as backlog rows; 4–6 are new observations this
window — appended to TODO_LIST if not present.)

## g) QUESTIONS FOR THE OWNER

1. Should the dispatcher treat a task ID already present in a merged
   commit footer + a `[x]` DONE note as a hard re-dispatch block, or is
   triple-dispatch acceptable redundancy you want kept?
2. For the 28 residue SHAs: do you want the row-148 positional-mapping
   attempt, or do you already rule them citation-lost and want the
   baseline rows dropped when refs/original goes?
3. Should heal-forensics reports be exempt from the baselined STALE
   count (path/ID-scoped gate carve-out), or is count drift acceptable?
