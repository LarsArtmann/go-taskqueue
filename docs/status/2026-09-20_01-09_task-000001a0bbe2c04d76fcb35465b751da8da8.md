# Summary-lines window + local fork record — task 000001a0bbe2

2026-09-20 01-09 window. Work item: TODO row 135 (end-of-run "N ok, M failed"
summary lines for the four sibling countable gates, the gosec 81f2db8
mechanism applied fleet-wide).

## What shipped

Parent-shell counters incremented per check via if/else, one summary echo
before exit (the exact mechanism 81f2db8 used for check-gosec.sh):

- `check-script-syntax.sh`: `syntax summary: N checks ok, M failed` —
  52 per-script `bash -n` results + the single shellcheck pass as one check
- `check-go-mods.sh`: `go-mods summary: N checks ok, M failed` — 2 repo-level
  greps + per-module toolchain alignment + per-module `go mod verify`
  (a flake retry counts once per module)
- `check-dead-exports.sh`: `dead-exports summary: N symbols ok, M dead` —
  printed before the strict exit so both paths carry it
- `check-guard-wiring.sh`: `guard-wiring summary: N scripts wired, M orphaned`

Evidence: green runs rc=0 with 53/0, 35/0 (= 2+16+17, matches the 16-module
enumeration), 304/66, 32/0; negative shape proven on guard-wiring via
create+assert+trash temp orphan (rc=1, "32 scripts wired, 1 orphaned",
post-trash rc=0); all four edits shellcheck-clean (check-script-syntax.sh
rc=0 over all 52 tracked scripts); TODO row 135 closed with citations.

## Fork record (history-rewrite policy §a-d)

The daemon raced the explicit commit: the four script edits landed in
unfootered auto-commit b1d0dc5, leaving the task's substance invisible to
the queue's footer cross-reference. Healed in-session per the 0-target
window precedent (d035911/ca514a8 over d035911/ca514a8): scripted
`git filter-branch --msg-filter` (script file per the daemon playbook,
SHA-pinned via `$GIT_COMMIT` — a subject grep would have hit the unrelated
daemon fold c9aa941 in the unpushed range; dry-run also caught an env-prefix
pipeline mistake where `GIT_COMMIT=x git log | filter` never sets the
variable inside the filter stage).

- Old → new: b1d0dc5 → b1d0dc5 (footer appended as last line);
  179dd51 → 179dd51 (message byte-identical)
- Fork point: ccb7eb3. §b: NO release tags and NO remote twins descend from
  the abandoned side — both rewritten commits were local-only (never
  pushed); the abandoned side is reachable only through the backup ref
  `refs/pre-attrib/refs/heads/master` (separate namespace; the pre-existing
  `refs/original` backup ca514a8 from an older healed window is untouched)
- §c: `git tag --no-merged master` = 0 after the rewrite
- Verification: net tree diff backup→master empty (byte-equal),
  author/email/date/subject metadata identical across the range, file stat
  of the work commit unchanged (4 scripts, +37/−2), clean status after
  (no daemon mid-flight commit)
- §d: the dead SHAs b1d0dc5/179dd51 are cited ONLY in this record (the
  sanctioned fork-record exception); TODO_LIST.md and the commit messages
  cite none of them

## Open

- None minted this window: the daemon-race class is already tracked
  (amend-postconditions / footer-salvage rows in the 2026-09-19/20 gosec
  reports); this window is one more data point, not a new shape.
