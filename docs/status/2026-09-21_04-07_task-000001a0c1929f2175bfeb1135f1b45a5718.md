# Task 000001a0c1929f2175bfeb1135f1b45a5718 — Repeat-dispatch verification window (f5-extended usage rendering) — full a)-g) close-out

Window: 2026-09-21 ~03:58–04:07 CEST · HEAD at dispatch AND at every gate run: `4b3082651f2d40276befdd99bfa93edacc5cb402` (03:57:29 +0200) · tree clean at start and before the stop-artifact commit.

**Verdict up front: REPEAT DISPATCH.** The fold DID reach this window (§d7): the daemon swept the row-163 annotation into `1c2f8c5` between the edit and the stop-artifact commit — the 09-52 footer↔content split shape, recorded not rewritten. The item (TODO row 163, Priority-system follow-ups: "`tq show` + webui detail: render derived session tokens/cost for status and prioritize tasks…") was already implemented and ticked before my first tool call: the webui rendering landed in daemon commit `dd920ac` (03:37:32 +0200, components.go / fragments.templ / fragments_templ.go / handlers.go) and the `[x]` tick + done-annotation rode `4b30826` (HEAD at dispatch). This window is therefore a **verify → cite → stop**: zero source edits, fresh gate battery at HEAD, row re-verified annotation, this report. The correct-response precedent is the 02-01/02-10 windows on the sibling §f3 ID; the stop-artifact convention (annotation + footer commit at zero source delta) is applied here — after I initially got it WRONG (§d1).

## a) FULLY DONE

All claims below were **verified first-hand at HEAD 4b30826 by this window**; the implementation itself is the earlier finishing window's work (`dd920ac` + `4b30826`).

1. **Item complete: `tq show` renders derived session usage for status and prioritize tasks.**
   - `resultDetail` decodes the completion fact into the typed result per task type — status case cmd/tq/main.go:2267, prioritize case cmd/tq/main.go:2272 (function at :2250).
   - The show payload marshals `Result any` wholesale (cmd/tq/main.go:1949-1956), so the four `Session*` fields surface through their json tags: `internal/executor/status.go:84-87`, `internal/executor/prioritize.go:93-96` (same keys as `AgentResult` result.go:35-38 and `ReviewResult` review.go:113-116 — the drift pin).
   - The fields are FILLED at run time by `deriveOutcome`: internal/executor/status.go:192-198 and prioritize.go:182-188 (review.go:215, agent.go:308 for the siblings).
2. **Item complete: webui detail renders it.**
   - `DashboardData.Statuses` / `.Prioritizes` feed the detail page (internal/webui/render.go:304-314).
   - `statusReportCard` usage line: internal/webui/fragments.templ:706-708; `prioritizeResultCard` (verdict count + tokens/cost + log path): fragments.templ:718-730, usage at :722-724; both wired on the detail page at fragments.templ:649-654; shared helpers `sessionUsageEmpty`/`sessionUsageText` at components.go:98-113; usage-less results render no line (render-nothing gate).
3. **Both halves are PINNED by tests (read, then executed at HEAD):**
   - `TestResultUsageRendersOnDetailPage` (internal/webui/webui_test.go:624): prioritize card renders "1200 prompt + 340 completion tokens · $0.0042 derived session cost", status card renders its usage line, usage-less status stays quiet.
   - `TestResultDetailDecodesTypedResults` (cmd/tq/main_test.go:352): prioritize decode keeps `SessionPromptTokens: 1200`; status/agent decode; foreign type nil.
4. **This window's fresh battery (all rc=0, all at HEAD 4b30826):**
   - internal/webui `TestResultUsageRendersOnDetailPage` rc=0 (work turn).
   - `scripts/test-cmd-tq.sh` full cmd/tq gate: ok 8.333s (work turn; includes the decode pin above).
   - Root `go build ./...` + `go vet ./...` under `GOEXPERIMENT=jsonv2`: rc=0 / rc=0 (work turn).
   - `./scripts/check-doc-refs.sh`: rc=0 "doc refs ok" (04:07, report window — it was MISSING from the work-turn battery, §d4).
   - internal/webui FULL suite: ok 10.104s (04:07, report window).
5. **TODO row 163 re-verified annotation** appended in place (RE-VERIFIED 2026-09-21 04:07 … report pointer), per the 02-10 stop-artifact pattern.

## b) PARTIALLY DONE

- Nothing of the ITEM is partial. The only partial thing this window owned was its own battery: the verify-window minimum battery convention (AGENTS.md) names check-doc-refs.sh explicitly and I shipped the work turn without it; repaired and cited during this report window (a4). That is a §d4 process miss, not unfinished item work.

## c) NOT STARTED

- Nothing within the item's scope. Adjacent usage-surface work that remains open lives as TRACKED rows and is listed in §f with pointers only (budget token consumption of the non-agent result types, score-cache TTL, etc.) — deliberately not duplicated here.

## d) TOTALLY FUCKED UP

1. **Wrong stop disposition on the first pass — the exact "zero-artifact stop" the sibling windows already codified as a miss.** I ended the work turn with chat-only verification and no repo artifact, reasoning "no changes → nothing to commit (empty commits are forbidden)". Reading docs/status/README.md during report prep showed the house convention: the 02-01 window was dinged as the "FOURTH zero-artifact-stop hit", and 02-10 established the remedy — a re-verified row annotation + footer commit EVEN at zero source delta. I repeated a documented miss class because I never read the sibling close-outs at turn 1 (my prior-report grep used MY task id, which had no history; the CLASS lived under other ids). Corrected in this window: row 163 annotation + this footer commit.
2. **Turn-1 ritual gaps, three of them:** (a) `scripts/session-start.sh` never run (it exists precisely for this); (b) `CONTRIBUTING.md` not read at turn 1 — a codified recurring miss (00-52 d4, 02-17 d3); (c) the queue's own record (`tq show 000001a0c1929f…`) never consulted — the 02-53 §d1 "repo-blind stop" miss verbatim.
3. **Slowest-possible DONE detection.** I grepped docs/status for MY id (correctly: no hits) but never grepped TODO_LIST for the item text. The row was `[x]` at HEAD before my first tool call; `rg "render derived session tokens/cost" TODO_LIST.md` would have surfaced it in one command. Verification was still mandatory (a tick is a claim, not proof — that part of my loop was right), but ~10 minutes of code-reading discovery cost was avoidable.
4. **Verify-window minimum battery incomplete at work-turn time:** check-doc-refs.sh absent (the convention names it); the "produce at least ONE fresh delta" half WAS satisfied (three fresh gate runs). Repaired 04:07 with rc=0.
5. **One wasted invocation on a hand-rolled gate:** `go test -modfile=dev.mod` in cmd/tq fails with "open dev.mod" because dev.mod exists only inside the shim — AGENTS.md names `scripts/test-cmd-tq.sh` as the ONLY entry and I still reconstructed it by hand. rc=1 noise, one burned step.
6. **Minor shell slop:** `tail -3 f1 f2` errored under the mvdan shell ("option used in invalid context") — harmless since both rcs were echoed before the tail, but it is the same session-shell-quirk family AGENTS.md documents; I should have kept the chain trivially simple.
7. **The daemon fold reached the one file I edited:** the row-163 re-verify annotation was swept into footer-less `1c2f8c5 chore: auto-commit 1 changed file(s)` before my add+commit chain ran. Attribution still resolves (the footer commit carrying this report + the status index exists; annotation content is on master via `1c2f8c5`), and no history rewrite was performed under the live daemon (09-52 precedent), but this is the eighth fold hit on 09-21 alone — the class the 01-45/01-49/02-27/02-32 windows keep asking to fix (§f18). The add+commit chain WAS single; the daemon simply runs continuously, and an edit-to-commit gap of minutes is all it needs. Remedy candidate stands: commit immediately after the LAST write, before any gate, not after.

What I did NOT fuck up (worth saying so the record is fair): no wrong code, nothing reverted, and the verification itself was rigorous — every rendering claim was read at file:line AND executed at HEAD before the DONE verdict. (The original draft said "no daemon race" — falsified minutes later by §d7; corrected in the amended report.)

## e) WHAT WE SHOULD IMPROVE

1. **Make the verify-window stop artifact a reflex, not a rediscovery:** row annotation + footer commit at zero delta, ALWAYS (the 02-10 pattern). Concretely: a verify-only window's checklist should end with "annotate row → index report if written → ONE add+commit chain with the footer".
2. **Turn-1 probe order for TODO-row dispatches:** item-text grep in TODO_LIST.md FIRST (cheapest DONE signal), then prior-report grep by id, then session-start.sh + CONTRIBUTING.md, then `tq show <id>`. All four are one-command moves; I did one of four.
3. **Battery as a literal checklist:** build+vet (jsonv2), check-doc-refs, targeted pins — each ticked with rc before a verdict is spoken. "Minimum battery" is written down; treat it as a gate, not a vibe.
4. **Use documented gate entries verbatim** — `scripts/test-cmd-tq.sh`, never a reconstructed `-modfile` invocation; the shim exists because plain module semantics cannot work (ADR-0017).
5. **Queue-side (owner):** a mint-time guard that flags or skips dispatch when the row is already `[x]` at HEAD would have saved this window entirely — see §g1.
6. **Read the sibling close-outs when a dispatch smells like a repeat:** the f5/f3/f4 windows documented the zero-artifact-stop class two hours before I re-committed it; `ls docs/status | tail` at turn 1 is nearly free.

## f) Up to 50 things we should get done next (honest N: 22 — padding refused)

New, discovered in THIS window (agent-executable unless marked POLICY):

1. End-to-end `tq show` wire pin: assert the show JSON of a completed status AND prioritize task carries `session_cost_usd`/`session_prompt_tokens`/`session_completion_tokens` (sqlite test store). `TestResultDetailDecodesTypedResults` pins `resultDetail` only, not cmdShow's actual output — the wholesale-marshal step (cmd/tq/main.go:1949-1956) is unpinned.
2. `sessionUsageEmpty` blind spot (components.go:100): it ignores `SessionMessageCount` — a run with messages but zero tokens AND zero cost renders no usage line. Decide whether message-count-only should render, pin either way.
3. `SessionMessageCount` is recorded on all four result types (result.go:38, review.go:116, status.go:87, prioritize.go:96) but rendered nowhere in webui or `tq show` — surface it (usage line suffix) or write down why it stays budget-internal.
4. CHANGELOG/FEATURES check for the dd920ac rendering work (usage lines on detail cards): I did NOT verify entries exist; append if missing (docs-only).
5. gopls phantom diagnostics in cmd/tq: this session saw ~36 `undefined:` compiler errors in main.go (deadPoolDetector, cmdBootstrap, …) while the devmod gate is green — gopls analyzes the module without the shim and there is no go.work by ADR-0017. Decide the treatment (repo-local LSP disable for cmd/tq, or a trusted-CLI-only AGENTS.md note like the templ-phantom bullet) so no future window "fixes" phantoms. (Owner preference → §g3.)
6. POLICY: queue mint-time guard for already-`[x]` rows (or a `verify-only` task class) — 4+ windows across two ids on 09-21 alone were re-dispatches of finished work (§g1).
7. BRAINSTORM: `tq show` human-render mode (`--render`) — the f5 row text said "cmd/tq show statusPayload section" but show is JSON-only; either the naming drift gets fixed in future rows or a human mode justifies the old wording.
8. BRAINSTORM: prioritize card depth — show top/bottom N verdicts (item key + score), not just the count; the data is already in `PrioritizeResult.Verdicts`.

Carried — already tracked in TODO_LIST.md, pointed at, NOT re-filed:

9. Budget token consumption of ReviewResult/StatusResult/PrioritizeResult usage (the §f1 row from the 09-52 report; usageSince parses by shared key per the 01-45 window).
10. Pinning test for PrioritizeResult-vs-AgentResult usage json keys (row 164) — note: the budget-side drift pin exists (budget_test marshals real executor types per AGENTS.md); row 164 asks for the executor-side pin; reconcile the two so we do not carry a duplicate.
11. Score-cache TTL/eviction (row 165, 16-28 f8).
12. `tq tasks --band` CLI parity (16-28 f9).
13. Priority provenance in the webui detail page (16-28 f11).
14. Webui completion-cost sparkline (09-52 §f6 item) — now that four fact types carry usage.
15. vendor/-scoped gofmt stage in `.tq-verify` + mint template (OWNER-ONLY, never-edit-own-gate; structurally unwinnable on dev hosts per the 03-17 window).
16. internal/e2e runtime vs the 180s gate cap (tracked row; 02-53/03-17 windows).
17. pin-golangci-in-flake (the host-drift class the row 163 annotation itself names).
18. Daemon fold remedy (lockfile / sweep-guard / commit-before-gates) — the most-repeated §g ask across the 01-45/01-49/02-27/02-32 windows; this window again rode clean only because it had no source edits to sweep.
19. `check-status-index.sh` catcher for daemon-folded reports (known hole; this window indexed BEFORE committing so the hole did not apply).
20. SessionMessageCount cap-unit question carried from 01-49 §g3 (overlaps item 3's surfacing decision).
21. BRAINSTORM: `tq show --commits` verdict wording — "MISSING FOOTER" reads as an error for legit no-op re-dispatch ids; a softer "no footer commits (no-op window?)" would stop future windows from minting marker commits to silence it.
22. BRAINSTORM: deriveOutcome result-marshaling repetition — status.go:192-198 and prioritize.go:182-188 (and review.go) repeat the same four assignments + `SetResultDetail` dance; a `applyDerivedOutcome(derived, &result.SessionID-less-fields)` helper would collapse three copies (the 02-27 window's "deriveSessionUsage extraction" ask, still open).

## g) Questions I can NOT figure out myself

1. **Re-dispatch policy:** the queue minted a fresh full attempt on a row that was `[x]` at HEAD (ticked ~20 min before dispatch). Is a mint-time guard (skip/flag when the row is already checked off) wanted, or is periodic re-verification of DONE rows intentional spend? If intentional: what cadence, and should those tasks carry a `verify-only` marker so agents skip straight to verify → annotate → stop?
2. **Footer semantics for zero-delta windows:** I followed the 02-10 stop-artifact pattern (annotation + footer commit carrying docs/TODO only). `tq show --commits <id>` will still report "MISSING FOOTER: no commit references this task ID" for the CODE (correctly — there is none). Is that verdict acceptable noise for re-dispatch ids, or do you want re-dispatches to cite the ORIGINAL implementation id's footer in the annotation so the cross-reference chains to the real work commit (`dd920ac` here)?
3. **gopls cmd/tq phantoms:** every session sees ~36 undefined-symbol compiler diagnostics in cmd/tq/main.go (module needs the devmod shim; no go.work by ADR-0017, by decision). Repo-local `lsp add gopls --disabled true` would also kill real diagnostics for internal/webui etc. if the server is shared — is there a path-scoped mechanism you want, or should I just add the trusted-CLI-only AGENTS.md note and move on?

---

Battery evidence index (all run by this window, outputs captured to /tmp logs): webui targeted test rc=0; `scripts/test-cmd-tq.sh` ok 8.333s; root build rc=0 + vet rc=0 (GOEXPERIMENT=jsonv2); check-doc-refs rc=0 (04:07); internal/webui full suite ok 10.104s (04:07). No ci-local/nix/race/smokes were run: zero source delta, gates inherited from the finishing window's battery where needed (row 163 annotation), per the verify-window convention's inheritance clause.
