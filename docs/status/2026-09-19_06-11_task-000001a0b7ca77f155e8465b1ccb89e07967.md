# Verify-window close-out — re-dispatch of the gosec self-test invocation-independence task

Task 000001a0b7ca77f155e8465b1ccb89e07967 (TODO row 132). **This is the
third window on this ID**: the work shipped at bb85fcb (script fix + TODO
tick + header-comment completion, twice-amended daemon sweep), the work
window's close-out is
`2026-09-19_06-02_task-000001a0b7ca77f155e8465b1ccb89e07967.md`, and THIS
window (dispatched ~06:05) was a **re-dispatch**: the turn-1 ritual grep
found row 132 already `[x]` + the prior close-out, so the codified
response is verify → cite → stop. **Zero file changes by this window**
(tree clean before and after; the only deliverables are this report and
its index row).

## a) FULLY DONE

1. **The work item itself — prior window, re-verified this window, not
   redone**: `self_abs` resolved via `cd $(dirname "$0") && pwd` +
   basename BEFORE the top-level `cd` (scripts/check-gosec.sh:25); both
   recursive `gate_run` children invoke `"$self_abs"` (:66, :68); the
   foreign-CWD re-entry branch re-invokes the mode from `/` guarded by
   `TQ_GOSEC_SELFTEST_REENTRY` (one re-entry level, :163-168). TODO row
   132 is `[x]` with the full DONE-evidence annotation. Work commit
   bb85fcb carries the exact `Task-Queue-ID` footer as the LAST trailer.
2. **THIS window's fresh battery** (all stock bash, rc captured via
   redirect-to-file — zero PIPESTATUS use):
   - `--self-test` from inside `scripts/` (the original rc=127 repro
     position): **rc=0, 27 ok / 0 FAIL**, summary line present.
   - `--self-test` from the repo root: **rc=0, 27 ok**, including
     `ok: --self-test re-runs from a foreign CWD`.
   - **Real gosec gate re-run on current HEAD**: rc=0,
     `gosec summary: 16 scans ok, 0 failed` (1 root + 15 module
     targets). The known UNSTAMPED WARN was present and is by design
     (provenance ruling 02-52 §g q2 still pending — WARN-and-continue
     until the owner installs a stamped v2.29.0 binary).
   - `check-script-syntax.sh`: rc=0, 0 findings.
3. **Immutability proven before trusting fresh runs**: `git log -1 --
   scripts/check-gosec.sh` and `-- TODO_LIST.md` both end at bb85fcb —
   the bytes this window verified are exactly the bytes the work commit
   shipped; no concurrent drift to reconcile between the prior window's
   claims and this window's re-runs.
4. **Turn-1 ritual executed as written**: `git log --oneline` /
   `git status` / `git stash list` + the task-ID grep over `docs/` — the
   grep surfaced the prior DONE row and close-out before any work was
   attempted, and the no-op verify response followed.

## b) PARTIALLY DONE

- **This window's verification is deliberately narrower than the prior
  close-out's battery, and the asymmetry is stated**: (1) the in-tree
  negative control (assertion liveness) was NOT re-run — it requires
  creating a modified copy of a gated script in-tree (daemon-food shape)
  and the prior proof stands on bytes unchanged since bb85fcb (§a3);
  (2) the `/tmp` absolute-path invocation position was not re-run — the
  scripts-relative position (which failed before the fix) plus
  ci-local's root-relative form cover the regression shape; the prior
  window ran all three.
- **The prior close-out's three owner rulings remain unanswered**
  (Amend-Maneuver scope, guard-var escape hatch, battery bar) — the
  decisions they gate are therefore still unmade; carried in §g3.

## c) NOT STARTED

- Nothing on the work item itself — by dispatch design (it was already
  DONE; this window exists because the queue re-fired the row).
- Adjacent open rows untouched, several **verified live this window**:
  124 (cmd/tq gosec, BLOCKED owner), 127 (ci.yml convergence, BLOCKED
  owner), 133 (for-each-module consumer guards), 134 (short-enumeration
  pin), 135 (sibling-gate summary lines), 136 (ci.yml sed-drift guard),
  137 (.golangci.yml excludes parity), 128 (session-ritual script — see
  §d1/§d2: this window is another of its recurrences), 258
  (CONTRIBUTING gate-list sweep — confirmed live: the "individual gates"
  block at CONTRIBUTING.md:29-38 still lacks check-gosec.sh).

## d) TOTALLY FUCKED UP

*(this window's own misses; the shipped work has none to report here)*

1. **Cited the prior close-out without reading it.** The turn-1 grep
   located `2026-09-19_06-02_task-…​.md` and my first response cited its
   existence and the `[x]` row while reading NEITHER's content. Reading
   was the point: its §g holds three open owner rulings and its §f an
   18-item list that this report's §f must build on rather than
   rediscover. It was read only during THIS report's prep.
2. **CONTRIBUTING.md existence-checked, not read** — the exact
   documented recurring miss (04-54 recorded "miss N+12", 05-40 "N+13";
   this window continues the counter). My turn-1 `ls CONTRIBUTING.md`
   confirmed presence and I moved on. Reading it during report prep paid
   immediately: (a) the row-258 gate-list gap is live, and (b) **new
   drift found**: CONTRIBUTING.md:41-42 still claims a "~400-finding"
   lint baseline while the committed truth is the 1113-finding /
   134-row seventh regen (2026-09-18).
3. **Third window burned on one task ID** (work → close-out → this
   verify). Every re-dispatch costs a full battery + a report; the
   queue-side done-row dedup that at least six reports list as their top
   §f item remains unbuilt. This window is exactly the duplicate spend
   it would have prevented.
4. **Nothing fucked up in the verification itself**: rc discipline held
   from attempt one (redirect-to-file; zero PIPESTATUS), zero edits so
   zero edit-before-read violations, no work files existed for the
   daemon to race. Stated plainly so §d cannot be read as padding.
5. **Daemon strike LOST on this report itself — post-text**: §d4's "no
   work files for the daemon to race" was true at write time and false
   by commit time; the daemon swept this report + its index row into
   footerless ce1a940 in the seconds between the pre-commit
   `git status --short` check and the `git commit`. Recovered per the
   Amend Maneuver (unpushed-verified, file-set-verified: exactly 2
   files → this commit). The race window is now proven to be seconds,
   not minutes — the commit-first-vs-daemon lesson applies to REPORTS
   written last, not just work files, and a same-command
   status-check+commit cannot beat it either. Sixth-strike family
   entry (work-file sweep bb85fcb, report sweep fce5454 on this ID
   alone).

## e) WHAT WE SHOULD IMPROVE

1. **Ritual = read, not locate.** The turn-1 grep's purpose is consuming
   the DONE evidence (row text + prior report), not citing its path. The
   session-ritual script (TODO 128) should PRINT the row and the prior
   report's §g/§f so a verify window starts from the prior window's open
   items instead of rediscovering them.
2. **The verify-window battery bar needs a ruling** (06-02 §g3, now
   triple-demonstrated): full-fresh (what this window did — minutes of
   duplicate spend, "borrowed evidence eliminated" bar) vs
   changed-inputs-only. Both are defensible; per-window guessing is not.
3. **The verify-window report format needs a cap** (asked by 05-04 and
   05-40): a zero-edit verify window arguably owes the queue one commit
   (this one) with §a evidence + delta misses — not a full a-g cycle
   that largely restates the prior report. Honesty tax vs spend tax:
   pick deliberately.
4. **CONTRIBUTING drift is structural**: the "~400-finding" number
   rotted through five baseline regens because it is a hand-copied stat.
   Point the doc at `.golangci-baseline.txt` (the committed source of
   truth) instead of restating any count.
5. **Agent-shell deviation consolidation**: the mvdan deviations are
   codified in two places (PIPESTATUS bullet in AGENTS.md Known Issues;
   the CWD-relative `$0` masking in row 132's DONE note + the script's
   header comment). One "agent-session shell hazards" subsection listing
   both stops the next window re-deriving either.
6. **UNSTAMPED-WARN provenance is now a permanent log fixture**: every
   real-gate run this week logged the same WARN (02-52 §g q2 pending).
   Green is formally "unproven provenance" until the owner installs a
   stamped binary — a one-command fix on an unsandboxed host that has
   now been pending across ≥4 windows.

## f) WHAT WE SHOULD GET DONE NEXT

Honest list (22 items; refuses 50-padding). NEW = minted this window;
otherwise carried from the vicinity with this window's status.

1. **Queue-side done-row dedup / re-dispatch policy** — systemic top
   item; this ID = 3 windows, sibling gosec IDs = 3-4 windows each.
2. **TODO 128: session-ritual script** — this window's §d1/§d2 are its
   recurrences; make the ritual print the DONE row + prior report.
3. **TODO 258: CONTRIBUTING gate-list sweep** — verified live this
   window (check-gosec.sh absent from the gates block).
4. **NEW: fix CONTRIBUTING.md:41** — stale "~400-finding" baseline;
   point at `.golangci-baseline.txt` as the source of truth.
5. **TODO 136: ci.yml gosec sed-drift guard** in the self-test or a
   ci-local step.
6. **TODO 137: .golangci.yml gosec.excludes parity assertion** (last
   hand-synced copy of the triage).
7. **TODO 134: short-enumeration pin** in check-gosec.sh (a renamed
   sub-module silently drops out of the gate today).
8. **TODO 133: for-each-module.sh consumer guards** — ci.yml's six
   `for m in $(…)` loops catch neither failed nor empty enumeration.
9. **TODO 135: summary lines for the sibling countable gates** (needs
   the fleet-convention-or-gosec-only ruling, 04-46 §g2).
10. **TODO 127 (BLOCKED owner): ci.yml gosec convergence** onto
    check-gosec.sh; if it lands, `--self-test` should ride into CI.
11. **TODO 124 (BLOCKED owner): cmd/tq gosec coverage** (for-each-module
    omits the CLI module).
12. **Foreign-CWD branch hermeticity**: `cd /` → `cd "$tmp"` in the
    self-test branch (06-02 §f15, unchanged, one line).
13. **gitscan trailer-attribution ruling** (03-00 §g1, owner question
    pending; gates the 59% zero-attribution fix).
14. **Owner push of the healed lineage** (origin/master behind; several
    BLOCKED rows and the refs/original cleanup hang on it).
15. **Amend-Maneuver scope ruling** (06-02 §g1) — see §g3a.
16. **Guard-var escape-hatch ruling** (06-02 §g2) — see §g3b.
17. **AGENTS.md one-liner**: citations may only be WRITTEN after the
    cited gate's output is in hand (02-38 §d1 row).
18. **Dead-SHA citation gate** (docs must not cite SHAs unreachable from
    master, fork-record escape).
19. **Daemon-strike ledger**: record the footerless 88d9b10 sweep
    (amended into bb85fcb) as the fifth strike with its own shape —
    a WORK file, not a report (06-02 §f18).
20. **ANSI-strip `sed` pin triage** in scan() (02-38 §b deliberate skip;
    regression currently fails loud).
21. **NEW: consolidate agent-shell hazards** (PIPESTATUS expands empty +
    CWD-relative `$0` masks rc=127) into one AGENTS.md subsection.
22. **UNSTAMPED gosec provenance**: owner installs a stamped v2.29.0
    binary (02-52 §g q2) so the gate's WARN disappears and green means
    pin-verified.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Re-dispatch policy**: is building the queue-side done-row dedup
   sanctioned, or are verify-windows accepted expected spend? This exact
   question has been asked by ≥5 sibling reports (04-54 §g, 05-00, 05-04,
   05-34, 05-40) and remains unanswered while the IDs keep paying
   3-4 windows each. I tried to answer it from the queue/contract docs —
   dispatch behavior is queue-side and not visible to me.
2. **Verify-window bar + report format**: full-fresh battery + full a-g
   report (what this window did), changed-inputs-only battery + capped
   report, or something between? Every window currently guesses and each
   guess is defensible; a one-line ruling settles ≥4 reports' worth of
   repeated deliberation.
3. **Carried from THIS ID's own close-out (06-02 §g1 + §g2, both still
   open)**: (a) is the Amend-Maneuver sanctioned as standing policy for
   WORK-file daemon sweeps generally (bb85fcb was recovered that way:
   footerless 88d9b10, unpushed-verified, file-set-verified), and (b) is
   `TQ_GOSEC_SELFTEST_REENTRY`'s env-tamperable guard acceptable in the
   `CI_CHECK=off` spirit, or must the assertion be non-env-hardened? I
   re-checked AGENTS.md — neither ruling is written down anywhere.
