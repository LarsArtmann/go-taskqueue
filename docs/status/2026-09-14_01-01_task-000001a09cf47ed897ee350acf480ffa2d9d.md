# Status report — task 000001a09cf47ed897ee350acf480ffa2d9d (hermetic fullcore smoke, 2026-09-14 01:01 window)

**Verdict: DONE-ON-ARRIVAL (n=3 dispatch of this task id).** The deliverable
was already shipped and twice re-verified: `scripts/smoke/fullcore.sh`
(landed via daemon commit `ca8d207`, wired into `scripts/ci-local.sh` +
TODO row 137 closed in `7e9e898`), verified in the 00-52 and 00-57 windows.
This window's contribution is **independent re-verification only — zero
deliverable files changed, no new commit for the deliverable itself.**

## a) FULLY DONE

- **Smoke re-run from scratch**: `./scripts/smoke/fullcore.sh` — PASS
  ("fullcore drained 4/4 on sqlite (scratch TQ_DB honored)"), scratch dir
  `/tmp/tmp.OJN212kcrT/`, drain report asserted (`completed 4` incl. the
  deliberate first-attempt retry).
- **Hostile-env guard proven again**: ran the smoke with an inherited
  `TQ_DB=/mnt/pool/services/tq/tq.db` (the production journal path from
  AGENTS.md's known-issues trap) — PASS with the scratch path in use; the
  script's explicit `export TQ_DB="$TMP/tasks.db"` (fullcore.sh:21) plus
  the example's `--db` flag (fullcore.sh:27) is belt-and-braces and holds.
- **Deliverable provenance confirmed**: `git log -- scripts/smoke/fullcore.sh`
  shows the file landed in daemon commit `ca8d207` (the 00-52 window's
  explicit commit `7e9e898` carried only TODO_LIST.md + ci-local.sh — the
  script itself was swept up by the auto-commit daemon); TODO_LIST.md:137
  is `[x]` with a DONE 2026-09-14 note.
- **Provenance anomaly noted and documented here** (see §d/e): the 7e9e898
  commit message says "Adds scripts/smoke/fullcore.sh" but its `--stat`
  does not contain the file. No action needed — the file IS tracked at
  HEAD — but the commit-message/file-set mismatch is exactly the
  daemon-races-explicit-commits pattern AGENTS.md warns about.
- **Tree state**: clean working tree at HEAD `f9d3409`; no stray files
  created by this window (scratch dirs under /tmp, auto-removed by the
  script's EXIT trap).

## b) PARTIALLY DONE

- **Contract step "run the project's build and tests"**: this window ran
  the smoke itself (which builds `examples/fullcore` end-to-end) but NOT
  the full `go test ./... -race` suite — same narrowing judgment as the
  00-57 window; for a shell-only deliverable with zero Go changes, the
  smoke is the meaningful gate, but the literal contract wording was
  under-run. Awaiting the standing AGENTS.md ruling question (§g2).
- **Contract step 1 (read CONTRIBUTING.md if present)**: AGENTS.md
  followed; CONTRIBUTING.md not opened this window (none found in the
  repo root listing; the ritual cost is one `ls` worth of certainty).

## c) NOT STARTED

- TODO_LIST.md:149 — "pin the fullcore drain-deadline determinism
  mechanically: extend the planned hermetic fullcore smoke to exercise
  the deadline path N times" — the direct successor to this exact task,
  still unchecked.
- Nothing else was in scope for this window.

## d) TOTALLY FUCKED UP

1. **The queue re-dispatched a DONE task for the THIRD time.** 00-52
   shipped it, 00-57 re-verified, 01-01 (this window) re-verified again.
   Three windows of identical verification work on one task id. The
   dispatch-side dedup item has been in every close-out this week; nothing
   has changed. This is the single biggest waste in the lineage.
2. **Commit-message vs file-set mismatch in the shipped work commit**:
   `7e9e898`'s message claims "Adds scripts/smoke/fullcore.sh" but the
   file actually landed in daemon commit `ca8d207` seconds earlier/later
   (the daemon won the race for the script, the explicit commit got the
   wiring + TODO). Harmless at HEAD, but the lineage is confusing to
   archaeology and the window that shipped it did not notice (its own
   00-52 report says the commit "adds" the script). First flagged here.
3. **Nothing else.** This window's own work was clean: verify, confirm
   lineage, report, close. The mistakes above are inherited/carry-over.

## e) WHAT WE SHOULD IMPROVE

- **Queue-side DONE-dedup** (repeat, still unfixed): a task whose
  Task-Queue-ID footer already exists on master should not re-dispatch, or
  the enqueue should carry a "re-verify only" hint so windows skip
  straight to the prior report (they do this manually today — it works,
  but it is tribal knowledge in the reports, not a mechanism).
- **Daemon-race hygiene for explicit commits**: when an explicit commit
  lands while the daemon is live, re-check `git show --stat` immediately
  — `7e9e898` proves the message can claim files the commit does not
  contain. AGENTS.md already documents "re-check `git status --short`
  before `git add`"; extend the ruling to post-commit `--stat` checks.
- **Smoke script self-evidence**: `fullcore.sh` asserts via grep on
  prose output; machine-readable output (`--json`) would let CI steps
  assert without string coupling (carry-over from 00-57).

## f) NEXT (up to 50; grounded in this window + items read in passing; existing TODO rows cited, not re-filed)

1. TODO:149 — extend `fullcore.sh` to exercise the drain-deadline path N
   times (direct follow-up; `16a15d8` was hand-verified only).
2. Queue dispatch-time Task-Queue-ID dedup (three dispatches of this id
   and counting; every close-out this week carries it).
3. Post-commit `--stat` vs message ruling for the daemon race (see §d2).
4. TODO:47 — SystemNix host deploy + round-9 cutover (owner-run).
5. TODO:91 — `tq pool-health` per-repo skip-streak summarizer.
6. TODO:138 — status-task window payload carries closeout/report paths.
7. TODO:52/53 — `internal/consumer` wire-or-delete + FactSource
   consolidation (owner calls).
8. TODO:54 — postgres CLI store wiring (`--store postgres://…`).
9. TODO:140/141 — closeout-report placement + TODO-append caps.
10. TODO:148/175/176 — leftover varnamelen/exitCause rename crumbs.
11. TODO:182/183 — repo-wide rename-literal-leak sweep + help-text smoke.
12. TODO:150 — lint-baseline slice-triage round 2.
13. TODO:185/187 — lint-annotations sub-module + for-each-module script.
14. TODO:167 — status-index maintenance cadence.
15. TODO:168 — history-rewrite policy line for AGENTS.md.
16. TODO:139 — docs-health: annotate/archive the six 2026-09-07 reports.
17. TODO:51 — post-push proxy check + clean-room `go install` for backend
    tags (owner push authorization pending).
18. TODO:101 — CQA bridge verification against a live instance (blocked
    on owner credentials).
19. TODO:128/129/130/142/143/144/171 — the owner-ruling cluster.
20. Status-report index row for this report (added in the same commit).

(No new independent backlog was minted from this window — §d2/§e2 are the
only genuinely new observations, filed as items 3 above.)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Re-dispatch root cause (3rd carry)**: who/what re-enqueues DONE
   tasks — is the n=3 on `000001a09cf4…` the same unknown mechanism the
   2026-09-13 reports chased, and does the owner want dispatch-time dedup
   built queue-side or is re-dispatch tolerated as free re-verification?
2. **Race-suite gate scope for shell-only deliverables**: is
   `go test ./... -race` required when the change is a smoke script +
   TODO checkbox, or is build+vet+the-smoke accepted? One AGENTS.md line
   ends the per-window re-decision.
3. **TQ_RESULT sha semantics for done-on-arrival windows (4th carry)**:
   for a re-dispatch with zero deliverable changes, should `commit_sha`
   be the original work commit (`7e9e898`/`ca8d207`) or the close-out's
   own commit?
