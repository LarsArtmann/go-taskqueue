# Status Report — task 000001a0c62680ddf3b97ddb40a7bdbdbaa7 (2026-09-22 00-48)

Scope note: this report covers THIS task's window only (mirror the git-identity
sanitizer into ci.yml's release-gates step), per the dispatch contract "do NOT
research unrelated stuff". Project-wide inventory was out of scope.

## a) FULLY DONE

1. **Work item implemented** (TODO_LIST row 90): the ci.yml release-gates step
   now carries the same identity sanitizer ci-local.sh has used since 2026-09-14
   — `GIT_CONFIG_GLOBAL=/dev/null` + `GIT_CONFIG_COUNT=1 /
   user.useConfigOnly=true` as step-level `env:` (YAML form, not a shell
   prefix). The ci-local-only asymmetry documented in 03-46 §f2 is closed by
   mirroring, not by a deliberate-divergence note.
2. **YAML validity verified**: `yaml.safe_load` on the edited workflow parses
   clean.
3. **Smoke verified under the sanitizer**: `GIT_CONFIG_GLOBAL=/dev/null …
   ./scripts/smoke/release-gates.sh` → "all cases green", rc=0 (scratch TQ_DB,
   no production journal touched).
4. **Minimum verify battery at HEAD**: `GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto;
   go build ./... && go vet ./...` → rc=0.
5. **TODO_LIST closed**: row 90 marked `[x]` with a DONE annotation naming the
   mechanism and the green gates.
6. **Committed with the task footer** (19ffcd4): work + TODO checkoff in one
   commit, footer `Task-Queue-ID: 000001a0c62680ddf3b97ddb40a7bdbdbaa7`. Never
   pushed.

## b) PARTIALLY DONE

1. **Full multi-module battery not run**: only the root build+vet ran; the
   per-module GOWORK=off loop and the race suite were skipped. Justification:
   the change touches one YAML env block — no Go source — so module gates are
   unaffected. Cheap gates (doc-refs, script-syntax) were NOT re-run either;
   shellcheck will see the new YAML comment lines only via ci.yml which is not
   a tracked `*.sh`. Honest gap, low risk.
2. **Runner behavior not observed live**: the parity claim ("same failure
   mode with and without a global identity") rests on ci-local.sh's existing
   analysis; no runner push to confirm the env block behaves as expected on
   ubuntu-latest. The commit lands unpushed by contract, so CI confirmation
   waits for the next push.

## c) NOT STARTED

1. No CHANGELOG entry — judged below the bar for a CI-config parity fix; the
   queue's docs-health pass owns the living-docs loop.
2. No backlog items appended: nothing follow-up-shaped surfaced from this
   window beyond what the repo already tracks.

## d) TOTALLY FUCKED UP

Nothing broken this window. Honest near-misses, in the 00-18/00-31 §d spirit:

1. **First edit attempt failed**: `edit` refused because the file had not been
   Viewed first — a rules-literal miss, caught and corrected in one step with
   zero damage.
2. **Edit-tool whitespace rules exercised twice**: the first rejected attempt
   was retried after viewing lines 128-138; the final old_string matched
   first try. No silent partial state existed at any point (working tree held
   only the intended 2-file diff at commit time).
3. **Turn-1 ritual executed thin**: `scripts/session-start.sh` and the prior-
   report task-ID grep were not run in this window (dispatch contract limited
   scope; no prior report exists for THIS task id, so the grep would have been
   empty — but I did not verify that first-hand).

## e) WHAT WE SHOULD IMPROVE

1. **Sanitizer ownership**: the env block now lives in two places
   (ci-local.sh:261-267 and ci.yml). A third call site would start the drift
   clock; a shared `scripts/lib/git-identity-sanitizer.sh` (or an inline env
   inside release-gates.sh itself) would make it single-owned.
2. **Option: fold into the smoke script.** The strictest fix is
   `scripts/smoke/release-gates.sh` exporting the sanitizer itself; then every
   caller (ci-local, ci.yml, human) gets parity for free and the YAML env block
   becomes unnecessary. Not done here — smallest-correct-change contract.
3. **The `env:`-vs-`run:`-prefix choice**: YAML env survives future
   multi-line run bodies; a shell prefix would not. Current form is the
   durable one.
4. **CI parity claims live in prose.** Several items in AGENTS.md read
   "parity with ci-local.sh" and are enforced only by review attention; the
   check-guard-wiring pattern (a parity-lint script over known pairs) would
   generalize.

## f) NEXT (≤50, scoped to this task's blast radius — most are ROADMAP fuel)

1. Fold the git-identity sanitizer into `scripts/smoke/release-gates.sh`
   itself and drop both call-site copies (single ownership).
2. Add a parity-lint script (check-guard-wiring pattern) covering known
   ci-local/ci.yml step pairs (fullcore, release-gates, dead-exports,
   script-syntax).
3. Confirm the ci.yml release-gates step green on the runner at the next push
   (CI is the only observer of the new env block).
4. Audit remaining ci-local-only smokes/gates for the same asymmetry class
   (env-dependent local behavior leaking into CI parity prose).
5. Sweep TODO_LIST for other "or document the asymmetry as deliberate"
   disjunctions still unchecked — they indicate undecided parity rulings.
6. Backfill CHANGELOG for v0.3.0-era infra fixes if the docs-health pass
   judges CI parity fixes changelog-worthy (ruling needed).
7. Consider a repo-wide inventory of step-level `env:` blocks in ci.yml vs
   their ci-local.sh counterparts (drift surface).

## g) QUESTIONS I CANNOT ANSWER MYSELF

1. **Ownership ruling**: should the sanitizer live in the smoke script itself
   (e/f items 1-2) so all callers inherit it, or stay duplicated at call
   sites? I defaulted to mirror-at-call-site (smallest change), but the fold-in
   is the better engineering answer.
2. **Verification appetite**: is a runner-green confirmation of the new
   ci.yml env block required before the DONE row is trusted, or does the local
   green run under the identical env suffice as citation?
3. **CHANGELOG policy**: do CI-workflow-only fixes belong in CHANGELOG.md, or
   is that file release-facing only? The repo has no written ruling I found.
