# Status Report — Task 000001a089f15854a1359e7673506ad7415b

**Date:** 2026-09-10 08:42 CEST
**Scope:** Review-finding close-out for the wrapcheck/varnamelen lint-baseline
slice-triage (parent work eaf73a9). One finding, one fix, no scope creep.
**Commits this session:** 9b7c46b (fix), this report commit.

## Summary

A code reviewer rejected the parent lint-triage task with exactly one (low)
finding: the s→store rename regex from the varnamelen sweep had leaked into a
flag help literal — `tq dlq --max-attempts` printed "attempt budget for
rescued task(store)" (cmd/tq/main.go:1391) instead of the original
"rescued task(s)". This session restored the literal, swept the parent diff
for further rename-corrupted literals, verified the repo gates, and committed.

## a) FULLY DONE

1. **Help text restored** — cmd/tq/main.go:1391 back to
   `"attempt budget for rescued task(s)"` (9b7c46b).
2. **Leak-class sweep** — diffed eaf73a9~1..eaf73a9 across cmd/ and internal/
   for other mangled literals containing parenthesized rename victims
   (`(s)`, `(w)`, `(r)`) and for the `(store)` corruption pattern repo-wide:
   **zero additional hits**. The one finding was the only instance of the
   leak class.
3. **Gates green** — `go build ./...`, `go vet ./...`, `gofmt -l cmd/` all
   clean before committing.
4. **Commit contract** — 9b7c46b carries the exact Task-Queue-ID footer;
   clean single-line diff; no unrelated changes.
5. **Self-honesty section (this report)** — see (e): the parent task left
   this bug, and the reviewer caught what my own close-out missed.

## b) PARTIALLY DONE

- Nothing from this finding's scope. The _parent_ task (eaf73a9) is done and
  re-verified, but this finding proves its verification had a blind spot
  (see e1).

## c) NOT STARTED

- Nothing owed by this task. TODO_LIST.md's lint-triage item was already
  closed by the parent; no new item is created for a fixed finding
  (CHANGELOG entry optional — judged below in (f)).

## d) TOTALLY FUCKED UP

- **Nothing this session.** The session did exactly the one finding,
  minimally. The _upstream_ mistake (the regex leak itself) belongs to the
  parent task, already owned in its 07-39 report ("two broken intermediate
  tree states via broad regex renames") — this string was the survivor that
  escaped both that task's build/tests and its self-review, because flag help
  literals are never exercised by tests or the compiler.

## e) WHAT WE SHOULD IMPROVE

1. **Literal-only sweep was missing from the parent close-out.** A
   `git diff` grep for "mangled string literals" (quote-delimited lines with
   multi-char identifiers inside parentheses that used to be one letter)
   would have caught this pre-review. It is cheap; it should be a step in
   every rename-heavy task's checklist.
2. **Rename tooling choice.** The parent used regex renames (per its own
   report) where `lsp_rename` exists precisely to avoid string-literal
   collateral. Use LSP rename first; reserve regex for non-symbol text.
3. **No runtime assertion on help text.** `tq dlq -h` output is completely
   untested; one smoke assertion ("help contains no '(store)'-class
   artifacts" or a golden flag set) would catch the whole class. Low value
   per test, but the class has now bitten once.
4. **Reviewer loop worked — keep it.** One finding, precisely located, with
   the correct generalization ("same leak class as handlers.go") and a sweep
   instruction. That is the right shape for review feedback.
5. **My sweep could have been shown, not just claimed.** The commit message
   says "none found"; embedding the sweep command in the report (done here)
   makes the negative result reproducible instead of trusting the agent.

## f) Up to 50 things we should get next

_Brainstorm, not commitment — most are ROADMAP fuel; docs-health HARVEST
should route rigorously. Ordered roughly by impact._

1. Add the "sweep quote-delimited literals in the diff" step to any
   regex-rename task checklist (AGENTS.md cross-cutting lesson).
2. Add a smoke that runs `tq dlq -h` (and maybe `tq -h` per subcommand) and
   asserts no parenthesized-identifier artifacts in help strings.
3. Consider a tiny test asserting the full `--max-attempts` help string
   verbatim (golden, cheap, stops this regression specifically).
4. Grep the whole eaf73a9 parent-window commits (daemon renames too, per the
   08-25 report's "daemon attribution split" note) for other literal leaks —
   my sweep covered the commit's diff; a repo-wide `git log -S'(store)'`
   style probe is stronger.
5. Verify `internal/queue` handlers.go (the reviewer's other cited leak) is
   actually fixed — I trusted the reviewer's "same leak class" phrasing and
   did not re-open that finding's commit; confirm with a quick read.
6. Update AGENTS.md's lint-triage entry with one line: rename-regex leaked
   into a flag help literal once (f28-class), fixed 9b7c46b — so future
   sessions know the class exists.
7. Decide whether this one-line fix earns a CHANGELOG entry under a
   `Fixed:` heading (probably yes; append-only file, trivial).
8. Optionally annotate the parent report (2026-09-10_07-39) with a
   strikethrough note that its "honest gaps" list gains this finding.
9. Sweep other user-facing strings (error messages, log reasons) touched in
   the parent window for the same corruption class.
10. Add `go vet`-independent "flag help contains identifier" advisory grep
    to scripts/ (advisory only, matching the golangci posture).
11. Evaluate promoting the `check-dead-exports.sh` style substring-matching
    discipline to a general "substring, not word-boundary" lesson in
    AGENTS.md (the reviewer finding was the inverse failure: too-wide regex).
12. Record in AGENTS.md that rename-heavy tasks should run `tq <cmd> -h`
    spot-checks for touched commands as part of verification.
13. Re-run the full multi-module gate after this fix for completeness
    (root gates ran; sub-modules untouched, so skipped — document that
    reasoning in future close-outs explicitly).
14. Consider a `flag` helper in cmd/tq that lints help strings at
    registration time in debug builds (probably overkill — ROADMAP).
15. Fold item 1 above into the "Scripted history edits"/regex cautions
    section rather than a new section (avoid AGENTS.md sprawl).
16. Check whether the daemon auto-commit interleaved any other commits
    between 9b7c46b and this report (session-start ritual on next session).
17. Confirm origin/master has not diverged from local (never pushed; per
    policy no push, but the 07-49 report's fork concern may interact).
18. Sweep docs/ for the literal "task(store)" in case any report quoted the
    mangled help text as truth.
19. Ensure check-status-index.sh sees this report (add README row — done in
    this commit if not, see commit body).
20. Backfill this finding into TODO_LIST.md only if HARVEST promotes items
    5/6 (don't create a one-line TODO for an already-fixed bug).
21. Consider whether golangci's `testable-examples`-style string checkers or
    a custom analyzer could catch "sentence-like strings with parenthesized
    Go identifiers" — linter-building candidate, low priority.
22. Review other flags renamed in the parent commit (`--max-attempts` was
    cited in the recent commit 9b7c46b's predecessor note) for full
    end-to-end behavior, not just help text.
23. Keep the review-response SLA: single finding, single commit, same day —
    replicate this shape for future findings (this went well; preserve it).
24. Periodic: re-run `./scripts/ci-local.sh` before the next push window.
25. ROADMAP: a repo-wide "user-facing string corpus" test (extract all flag
    help + error strings, snapshot-test them) to freeze them against
    mechanical edits.

_(Items 26–50 intentionally not manufactured: the honest list above is the
real backlog; padding to 50 would dilute HARVEST routing. Remaining capacity
goes to the existing ~50-item list from the 07-39/08-25 reports.)_

## g) Questions I can NOT figure out myself

1. **Should one-line review-finding fixes get CHANGELOG entries?** The file
   is append-only and user-facing; a help-text typo fix is borderline. Owner
   preference decides (default: yes, under `Fixed:`).
2. **Was the reviewer's cited handlers.go leak (same class) already fixed in
   the parent window?** I saw no `handlers.go` in the eaf73a9 diff and no
   surviving corruption in my sweep — but I don't know which handlers.go the
   reviewer meant (path/repo), so I couldn't verify their claim directly.
   A pointer would let me close that loop with proof.
3. **Do you want a standing "literal sweep" step added to the AGENTS.md
   session-start/verification rituals, or handled per-task?** Global rule
   vs. checklist-line is a policy call I shouldn't make alone given the
   AGENTS.md sprawl concern.

## Verification performed this session

- `go build ./...` — clean
- `go vet ./...` — clean
- `gofmt -l cmd/` — empty
- Diff sweep: `git diff eaf73a9~1 eaf73a9 -- cmd/ internal/` filtered for
  parenthesized rename victims → zero hits beyond the fixed one
- Repo-wide grep for `(store)` string-literal corruption → only code using
  a `store` variable (legitimate), no string literals
