# Fullcore postgres run — n=2 same-ID dispatch verification close-out

**Task:** 000001a098818cdddd591ba3ff86c538e973 — "Run examples/fullcore once
against a throwaway postgres DB" (06:25 report c2 follow-up)
**Session:** 2026-09-13 ~04:05–04:15 CEST, autonomous, unsupervised
**Verdict:** **DONE-on-arrival, zero deliverable changes.** This is the
**second dispatch (n=2)** of this task ID. The work commit (3faaf0b) and the
n=1 close-out (`2026-09-13_04-07_task-…973.md`, shipped in 58a6299) were both
already on master at turn 1.

## a) FULLY DONE

- **The work item itself** (by the prior session, verified first-hand this
  session, not trusted from the handoff):
  - Work commit **3faaf0b** carries exactly one well-formed
    `Task-Queue-ID: 000001a098818cdddd591ba3ff86c538e973` footer — the
    dispatch ID verbatim. Body documents the REAL bug found: switch-case
    `store, err := ...` shadowed the outer `queue.Store`, so Enqueue hit a
    nil interface on BOTH backends; the example had never actually been run.
  - Throwaway initdb cluster on 127.0.0.1:55432 (db `taskqueue`), example
    drained 4/4 completed including the retry task.
  - TODO_LIST.md row 135 is checked `[x]` with a DONE note citing commit +
    cluster evidence (claims-carry-citations contract satisfied).
- **This session's additive verification:**
  - Root-module gate re-run with **clean file-redirect capture**:
    `GATE_EXIT=0, 15 ok / 0 FAIL, -count=1` (GOEXPERIMENT=jsonv2 exported).
  - `git log --oneline -8` + `git status` + `git stash list` at turn 1
    (session-start ritual): working tree clean, no stash.
  - Commit-footer forensics on 3faaf0b: footer count = 1, hex, matches.

## b) PARTIALLY DONE

- **Sub-module gates skipped-declared:** zero `internal/` files touched by
  this session (docs-only close-out; the deliverable code lives in
  `examples/fullcore` + root go.mod, both root-module).
- **`-race`, ci-local, nix gates skipped-declared:** no code changed by this
  session; the work commit's own session ran build/vet/tests (per its DONE
  note and 04-07 close-out).

## c) NOT STARTED

- Nothing owed by this task. The remaining fullcore follow-ups live in
  TODO_LIST.md rows 136 (hermetic smoke) and 148 (deadline determinism) —
  separate items, correctly left unchecked.

## d) TOTALLY FUCKED UP (my own misses this session)

1. **CONTRIBUTING.md EXISTS and was unread at turn 1.** The N-th consecutive
   miss this window, repeated minutes after multiple prior close-outs
   documented the same failure (04-07, 04-01, 03-55, 03-49…). Read only at
   close-out. No damage — this session made no code changes, so none of its
   gates were violated — but the miss itself is undisciplined.
2. **Pipeline-masked gate capture in my own work turn.** I ran
   `go test ./... -count=1 2>&1 | tail -15` and reported green. `tail`'s exit
   status is what the pipeline returns, and `tail -15` truncated the top of
   the package list — a FAIL in an early package could have been invisible.
   This is the exact masked-capture genus AGENTS.md warns about ("asserted
   before proven"). Repaired at close-out with a clean file-redirect re-run:
   GATE_EXIT=0, 15 ok, 0 FAIL — the claim is now proven, but it was true by
   luck when first asserted.
3. **Prior same-ID close-out not checked at turn 1.** I confirmed the work
   commit and TODO row, but did not grep `docs/status/` for this task ID
   before starting; the 04-07 close-out's existence (and its §f carry-list)
   surfaced only via the index at report time. The n≥2 redundant-dispatch
   cluster therefore continues.
4. **This report itself is report volume on a done-on-arrival ID** — the
   tension every prior n≥2 close-out flagged, unresolved (§g1).

## e) WHAT WE SHOULD IMPROVE

- **Turn-1 checklist must be mechanical, two greps before anything:**
  `git grep -l <task-id> -- docs/status/` (prior close-out?) and
  `ls CONTRIBUTING.md CLAUDE.md`. Both failures this session are of the
  "knew the rule, skipped the step" class; only a script or hook fixes it.
- **Gate captures are file-redirects, never pipe-to-tail.** Run
  `go test ./... > log 2>&1; echo EXIT=$?` then inspect the log. The pipeline
  version has now burned sessions at least six times this window and is
  still the default reflex.

## f) Up to 50 things we should get done next

Brainstorm, not commitment — most are carried from the 04-07 §f (marked C) or
ROADMAP fuel; HARVEST should route with extra rigor. Ordered by impact.

1. **`scripts/smoke/fullcore.sh`** (C, TODO row 136) — hermetic fullcore
   smoke, sqlite path, explicit `TQ_DB=<scratch>` export; the nil-store bug
   survived weeks precisely because the example had no automated run.
2. **Run-never-happened sweep of the other examples** (C) — examples/api and
   examples/sse have presumably also never been executed end-to-end; run
   each once against a scratch setup, or accept the risk in writing.
3. **Shadow-assignment vet for examples/** (C) — the fullcore bug class
   (`x, err :=` shadowing an outer var inside a switch case) is mechanically
   findable; a small vet/analysis pass or a go-critic check scoped to
   examples/ would catch siblings.
4. **Deadline-determinism smoke** (C, TODO row 148) — exercise the fullcore
   drain-deadline path N times mechanically (16a15d8 was hand-verified only).
5. **Dispatch-time same-ID dedup guard** (C, repeated every n≥2 report) —
   harvest should not mint work for already-checked TODO rows with an
   existing indexed close-out; §g1.
6. **Turn-1 ritual enforcer** — tiny hook or script (grep docs/status for
   the dispatch ID + CONTRIBUTING/CLAUDE existence) wired into the close-out
   prompt template or a session hook; ends the recurring §d1/§d3 classes.
7. **Gate-capture convention** — document the file-redirect gate pattern in
   CONTRIBUTING.md (it is currently tribal AGENTS.md knowledge) and/or ship
   `scripts/gate.sh` that runs the standard chain with honest exit capture.
8. **CONTRIBUTING.md visibility fix** (C, carried all window) — either fold
   its content into AGENTS.md's Commands section or make the dispatch
   template link it; N consecutive unread turns means placement, not
   discipline, is the failure mode.
9. **Daemon-commit footer policy ruling** (C, 04-07 §g1) — auto-daemon
   commits (e.g. 6365990, the TODO-row sweep) carry no footer; whether that
   is acceptable for queue cross-referencing needs an owner ruling.
10. **TQ_RESULT `commit_sha` semantics** (C, n≥2 recurring) — for done-on-
    arrival dispatches the reported sha is the WORK commit, for normal tasks
    the session's own commit; the field needs a defined meaning.
11. **n≥2 report-skip policy** (C) — re-dispatch of a done-on-arrival ID
    should re-emit TQ_RESULT without a fourth close-out unless state moved.
12. **Fullcore postgres path in CI-adjacent automation** — the throwaway-
    initdb procedure from the work session is only documented in a close-out;
    promote it into the planned smoke (item 1) as a `--backend postgres`
    variant, skipped when no postgres binary is present.
13. **examples/ CI coverage decision** — either gate examples builds in CI
    (compile-only today) or document that they are best-effort; current
    state is implicit.
14. **Test for the fullcore store-assignment fix** — 3faaf0b has no
    regression test pinning "Enqueue does not nil-panic after Open" for
    either backend; the planned smoke (item 1) subsumes it, but if that
    stalls, a minimal in-package test is cheap.
15. **Carried webui/trail items** (C, from earlier windows, unaffected by
    this session): NoIndex pin test, smoke robots assertion, SECURITY matrix
    row, one-line hardening CHANGELOG policy, FEATURES rejected-eval rows.
16. **Everything else is filler** — honestly labeled; the smallest-correct-
    change contract and prior sessions' filler-to-50 rejections stand.

## g) Questions I can NOT figure out myself

1. **Same-ID dispatch intent:** this task was checked done at 04-07 and
   re-dispatched at ~04:05. Is the queue's harvester intended to re-mint
   checked rows (a dedup race to fix — see item 5), or is redundant dispatch
   accepted and n≥2 sessions should just re-emit TQ_RESULT with no report?
2. **Commit_sha semantics:** when a session verifies done-on-arrival work,
   should TQ_RESULT carry the WORK commit sha (3faaf0b, done here) or the
   session's own close-out commit? The queue's cross-reference presumably
   wants the work sha, but the close-out commit is the one carrying the
   footer this time — both do, so the field is ambiguous.
3. **Daemon footer policy:** the auto-commit daemon's `chore:` commits
   (which swept the TODO-row check into history, 6365990) carry no
   Task-Queue-ID footer. Should the daemon append the session's footer when
   it folds task-attributed changes, or are daemon commits deliberately
   footerless?

## Evidence chain

- Work commit: 3faaf0b (footer = dispatch ID, count=1)
- n=1 close-out: `2026-09-13_04-07_task-000001a098818cdddd591ba3ff86c538e973.md`
  (commit 58a6299), indexed
- This session's gate: root module, GOEXPERIMENT=jsonv2, `-count=1`,
  file-redirect capture → GATE_EXIT=0, 15 ok / 0 FAIL
- TODO_LIST.md:135 `[x]` re-read first-hand
