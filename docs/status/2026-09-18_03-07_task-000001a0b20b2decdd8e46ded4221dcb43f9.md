# Status Report — task 000001a0b20b2decdd8e46ded4221dcb43f9 (AGENTS.md `tq crush` doc gap)

**Date:** 2026-09-18 03:07 CEST
**Scope:** the single TODO_LIST work item "Add `crush` to the AGENTS.md cmd/tq subcommand list + a one-line wrapper note in the session-close bridge bullet". Third window on this task id (02-47 shipped the wrapper, 02-51 close-out starred this doc gap, 02-54 re-dispatch verified it still unfixed; this window fixed it).
**Commits:** work = `d6f5ad0` (docs: add tq crush to AGENTS.md subcommand list + session-close bridge note), footer-verified; report commit follows this file.
**Files changed (window):** `AGENTS.md`, `TODO_LIST.md`, this report.

---

## a) FULLY DONE

1. **`crush` added to the AGENTS.md cmd/tq subcommand list** (AGENTS.md:138) — the subcommand table now ends `… / doctor / crush / version`, matching `cmdCrush`'s registration in cmd/tq/main.go:140.
2. **Wrapper note in the session-close bridge bullet** (AGENTS.md:353-358) — the bullet header now reads "`tq session begin/close` + the `tq crush` wrapper" and carries a 5-line wrapper description: runs a crush session and, on exit (clean or crashed), runs the replay-safe close using the session id from `$CRUSH_SESSION_ID` or the child's output; no id derivable → child's exit code preserved, nothing closed. Text verified against the `tq crush` help block (cmd/tq/main.go:78-83) — no invented semantics.
3. **TODO_LIST row closed** — the work item ticked `[x]` per the file's conventions (row 119).
4. **Commit carries the required footer** — `Task-Queue-ID: 000001a0b20b2decdd8e46ded4221dcb43f9` present in the work commit message (verified post-hoc via `git log -1 --format=%B | grep`); not pushed, per contract.
5. **Daemon-race recovery** — the auto-commit daemon swept the two docs files into a footer-less `chore:` commit (d26252d) mid-window. Per the AGENTS.md amend maneuver the UNPUSHED daemon commit was amended into `d6f5ad0` with the footer — exactly the documented healing path for the daemon bypassing hooks.
6. **Gates run** — `check-todo-list.sh` and `check-status-index.sh` both pass ("status index ok"; the index-bloat WARNING is pre-existing, see e).

## b) PARTIALLY DONE

1. **Full verify gate could not go green end-to-end** — `go build ./... && go vet ./...` pass, but `go test ./... -race` is red in `internal/status`. Investigated far enough to prove it is NOT this window's work (docs-only diff; reproduces identically with the diff stashed) but NOT fixed — it belongs to its own task, not a drive-by fix inside a docs window. Details under d/c.
2. **Section (f) below is grounded in THIS task's visible neighborhood only** — the instruction "do not research unrelated stuff" was honored, so the next-steps list is a harvest of the TODO rows I read in passing plus this window's own findings, not a full-project audit.

## c) NOT STARTED (visible TODO_LIST neighborhood, untouched by contract)

These rows sit adjacent to the closed row and remain open (verified during this window's TODO read, lines 119-125):

1. `- [ ]` Re-cite `scripts/smoke/session-close.sh` green over the `runSessionClose` extraction AS USED BY the `tq crush` wrapper (02-51/02-54 §f) — one smoke run + citation; unit tests cover the wrapper, the smoke hasn't been re-cited since the refactor.
2. `- [ ]` Document `tq crush` in README (short subsection under session triggers) + FEATURES.md row (02-54 §f).
3. `- [ ]` Live-no-id smoke assertion: wrapper exit preserves child code, nothing closed, no DB touched (02-54 §f).
4. `- [ ]` check-gosec.sh: cover cmd/tq (for-each-module.sh omits it) (02-52 gosec-gate report §b) — flagged UNBLOCKED-OWNER-GATED by check-todo-list.sh, see e2.
5. `- [ ]` check-gosec.sh: verify a pre-existing GOSEC_BIN is actually v2.29.0 (02-52 §b).

## d) TOTALLY FUCKED UP

1. **The daemon committed my work footer-less before I could** (d26252d, `chore: auto-commit 2 changed file(s) (heuristic)`). Not lost — amended within the same window — but it is the known hole in action: had the window ended between the daemon commit and my amend, the queue cross-reference would have silently lost the task. The 3-minute race window between "last edit" and "explicit commit" is structurally unguarded.
2. **`internal/status` test suite is flaky on ULID ordering** — `TestSweepPinsCloseoutReportPaths` ("first entry report = '', want docs/status/2026-09-14_10-00_task-000001a0b20cb86b…md") and `TestSweepNthCompletionMintsOneReport` ("window order = […], want oldest first") fail intermittently (-count=3 fails, single -count=1 passes, fails again at -count=5/-count=10). Same signature both: fixtures mint tasks within the same millisecond, so ULID monotonicity within a ms does not guarantee the sort order the tests assert. Pre-existing (reproduces with this window's diff stashed), but it makes the standard verify gate lie in BOTH directions: red on green code (this window) and capable of masking a real ordering regression on another day. Needs a deterministic clock/sequence seam in the test fixtures, not a sleep.
3. **Nothing in this window's own work** — the diff itself is two doc edits + one checkbox; no code, no fixtures, no config touched. The fuckups above are environmental/pre-existing; I claim the daemon race only insofar as I could have committed faster.

## e) WHAT WE SHOULD IMPROVE

1. **Commit the footer FIRST, edits second is impossible — so shrink the daemon race instead.** Practical rule for future windows on footer-bearing tasks: make the edits and the explicit commit in back-to-back tool calls (this window had one intervening verify run; that was the gap the daemon won). A queue-side guard (`tq doctor`-style check that every `chore: auto-commit` on master carrying touched-task files got amended) would catch the missed-amend case automatically rather than relying on agent discipline.
2. **check-todo-list.sh flags a shipped backlog row as UNBLOCKED-OWNER-GATED** ("check-gosec.sh: cover cmd/tq", row ~125) — the gate demands the row be rephrased agent-executable or BLOCKED-annotated. It is a legitimate owner decision (devmod shim vs "CLI gosec stays CI-only"), so the row should carry `— BLOCKED: owner ruling required` NOW instead of failing the gate every window until ruled.
3. **docs/status index bloat** — check-status-index.sh warns 211 live rows (threshold 100) and prescribes the remedy (archive sweep or monthly digest row). Two consecutive windows have now seen the warning; it is advisory, so nothing forces it. One docs-health ANNOTATE pass would clear it.
4. **AGENTS.md subcommand list has rotted once already** — this window's fix was manual. The list is derivable from `cmdCrush`-style registry entries in cmd/tq/main.go:140; a tiny parity check (like check-facade-parity.sh does for facades) could pin help-text commands ↔ AGENTS.md list, preventing the next "shipped subcommand absent from docs" recurrence at near-zero cost.
5. **Status-report format override transparency** — the status-report skill defaults to styled HTML; the task contract explicitly demands `.md` at the `<ts>_task-<id>.md` path. The `.md` instruction wins (explicit user instruction), and this report is markdown accordingly; flagged here so the divergence is visible and NOT propagated into the skill as a new default.

## f) Up to 50 things we should get done next

Grounded in this window's visible neighborhood (adjacent TODO rows + noticed-in-passing); most extras are ROADMAP fuel, not commitments — HARVEST should apply routing rigor. Ordered roughly by impact.

1. Fix the `internal/status` ULID-ordering flake: deterministic clock/seq seam in sweep test fixtures (TestSweepPinsCloseoutReportPaths, TestSweepNthCompletionMintsOneReport); the standard verify gate must stop flip-flopping.
2. Re-cite `scripts/smoke/session-close.sh` green over `runSessionClose` as used by `tq crush` (adjacent TODO row; one run + citation).
3. Document `tq crush` in README (session-triggers subsection) + FEATURES.md row (adjacent TODO row).
4. Live-no-id wrapper smoke assertion: child exit code preserved, nothing closed, no DB touched (adjacent TODO row).
5. Resolve the check-gosec.sh cmd/tq coverage row: annotate `— BLOCKED: owner ruling required` so the TODO gate stops failing on it.
6. check-gosec.sh: assert a pre-existing GOSEC_BIN is v2.29.0 (adjacent TODO row).
7. docs-status archive sweep (docs-health ANNOTATE) to clear the 211-row index bloat warning (third sighting).
8. AGENTS.md subcommand-list parity check: derive from the cmd/tq command registry, gate like check-facade-parity.sh.
9. Daemon-vs-footer race guard: post-commit check that no footer-bearing task's files ride an unamended `chore: auto-commit` (wired into ci-local or tq doctor).
10. Sweep master for OTHER shipped subcommands missing from the AGENTS.md list (ask/facts/tail/watermarks were added over time; audit the full registry once the parity check from 8 exists).

11-50: intentionally not enumerated beyond the evidence — the instruction scoped this window to one item and forbade unrelated research; inventing 40 more rows would be trophy-case padding. Route genuine backlog growth through a docs-health HARVEST pass over the starred items in docs/status/2026-09-17_02-51_* and _02-54_* instead (their §f lists were actioned once; a re-verify against HEAD is cheap).

## g) Questions I can NOT figure out myself

1. **ULID fixture ordering (d2):** is the intended contract "monotonic within a process even across the same millisecond" (in which case the sweep tests are right and the ULID minting is wrong on this host), or "same-ms ULIDs are unordered by design" (in which case the tests need the deterministic seam)? Owner ruling decides fix direction 1 vs the test-side fix in f1.
2. **check-gosec.sh cmd/tq coverage (f5):** devmod-shim gosec run over cmd/tq, or an explicit owner ruling that CLI gosec stays CI-only? The TODO gate will keep flagging the row either way until this is decided.
3. **Daemon race (e1):** acceptable to add a ci-local step that FAILS when a `chore: auto-commit` touches files belonging to an open footer-bearing task (forcing the amend at gate time), or should this stay agent-discipline-only? It trades a rare false positive (legit daemon commits of unrelated files) for catching the silent footer loss.
