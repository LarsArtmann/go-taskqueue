# Status Report — Archive Sweep of the Six 2026-09-07 Reports (docs-health ANNOTATE+ARCHIVE continuation, TODO row 139)

- **Written**: 2026-09-14 01:30 CEST
- **Task**: `000001a09d148c191eb7e317a0c03ea81639` (footer commit `2562792`;
  archive content itself was folded by the auto-commit daemon into
  `b23ff82` + `019b18f` — attribution split, see §d)
- **Scope**: annotate + archive the six 2026-09-07 status reports (18-41,
  19-33, 20-32, 21-19, 21-44, 23-08) under the every-item-resolved archive
  convention, then close TODO_LIST.md:139. Docs-only change; no code touched.
- **Verification state at write time**: `check-status-index.sh` ok (one
  pre-existing trailer warning on an unrelated 2026-09-10 file),
  `check-doc-refs.sh` ok, `check-todo-list.sh` ok, root `go build` + `go vet`
  green, `internal/webui` + `internal/harvest` tests green (`-count=1`).
  Working tree clean at report time.

---

## a) FULLY DONE

1. **Archive bar met for all six reports.** Each gained an inline archive
   note after the title (window-complete + per-item resolved-or-routed
   verdicts), per the cluster-note convention sanctioned by the 2026-09-13
   08-49 pass and followed by the M6 archive batch. Files verified at
   `docs/status/archived/2026-09-07_*` (6 files, zero left at the old paths).
2. **Every-item-resolved verification, not vibes.** Open items were checked
   against HEAD before claiming resolution, with spot-verification greps:
   ci-local shipped; ctx guard fixed by the pool (`aaabe1c`); lint endgame
   decided (option b: `.golangci-baseline.txt`, 111 lines, `--check` gate);
   `tq version` (cmdVersion in cmd/tq/main.go:2267); govulncheck job
   (ci.yml:192); `.github/dependabot.yml` exists; `FuzzExtractResultPayload`
   seeds committed; `tq audit --json` flag exists (audit.go:36); no
   `--open` flag (NOT-DO claim verified); retry strip live in the webui
   detail (fragments.templ:673); DOMAIN_LANGUAGE carries session-bridge
   terms (a too-strong claim was caught and reworded before commit — §e2).
3. **ADR-0004 claim corrected, not echoed.** 19-33 §f31 wanted "ADR-0004:
   lint policy"; the real ADR-0004 is the lifecycle-streaming stance, so the
   note records the lint ruling as living in AGENTS.md instead — verified
   against `docs/adr/` before writing.
4. **Citations repointed BEFORE the moves** (the AGENTS.md archive rule):
   two planning docs (`16-25` round-3 plan → 18-41; `23-51` round-5 plan →
   23-08) and the one in-set reference (18-41 → 19-33). Zero dangling
   references afterwards (doc-refs gate green).
5. **Index + counter maintained.** Six README index rows repointed to
   `archived/<name>`; archive counter 40 → 46.
6. **TODO_LIST.md:139 closed** with a DONE verdict carrying evidence
   (commit `2562792`, trailer parses via `%(trailers)`).
7. **Gates run, raw exit codes.** No pipeline masking this session: every
   gate ran as `cmd && echo` chains, trailer check via `%(trailers)`.

## b) PARTIALLY DONE

1. **Per-item strikethrough rigor.** The six reports keep their pre-existing
   inline strikes (18-41 already had ~14 items struck); the remaining
   resolution evidence lives in the cluster notes, not per-line
   `~~…~~ done at <hash>` markers. Defensible under the sanctioned
   convention (and several reports already carry 2026-09-09 §f annotations),
   but it is the loose reading of the skill's strictest bar — the owner-bar
   question from the 08-49 report (§g1 there) is still formally unanswered.
2. **Coverage of the b/c/e prose sections.** The archive notes resolve
   numbered §f/§g items in detail and summarize b/c/e verdicts, but did not
   strike individual b-table rows. The tables are self-describing
   (works/remains pairs); judged below the "so what?" bar for striking.
3. **Test breadth.** Docs-only change, so the gate run was scoped: root
   build+vet, two targeted packages, three doc gates. Full ci-local was not
   run (no code touched; the archive note convention does not demand it) —
   honest scope, declared rather than hidden.

## c) NOT STARTED

All deliberate, nothing dropped from the commanded scope:

- The remaining docs-health cadence batches (2026-09-06 batch fully done
  earlier; older 09-08/09-09 reports re-check on the next cadence pass —
  TODO row 166 tracks this).
- Status-index digest/cadence item (TODO row 167) — untouched, out of scope.
- Per-item strike re-annotation of the six files (b1) — only on an owner
  bar call.
- HARVEST of this report's §f into TODO_LIST — §f below is ranked view only;
  the genuinely new items are marked and should route on the next
  docs-health pass.

## d) TOTALLY FUCKED UP

1. **Lost the attribution race to the auto-commit daemon — again, despite
   the AGENTS.md rule that names this exact race.** My first commit attempt
   died on a tool-level heredoc parse error (`git commit -m "$(cat <<EOF)"`
   rejected by the shell interpreter); by the time I re-ran with a message
   file, the daemon had already swept the archive content into `b23ff82`
   (8 files: notes + renames + planning-doc repoints) and `019b18f`
   (README index + counter). My authored footer commit `2562792` carries
   ONLY the TODO_LIST closure. The queue↔git cross-reference works (HEAD
   carries the trailer), but the substantive diff is attributed to
   heuristic daemon blobs. The rule says: re-check `git status --short`
   IMMEDIATELY before `git add` and commit per task — I checked status at
   the start of the commit step, but the daemon moved between the check and
   the add because my first attempt wasted a round trip. Lesson: stage and
   commit in ONE shell chain, and never attempt heredoc commit messages in
   this harness (script-file or `-F` from the start).
2. **Heredoc attempt at all.** AGENTS.md and repo history ban heredoc/
   inline-quoted constructions for git messages through this tool layer
   (documented reword incident). I hit the documented failure mode before
   switching to `-F /tmp/file`. One wasted cycle, and that cycle is what
   opened the daemon window.
3. **Two note claims drafted stronger than the evidence, caught only by my
   own re-verify pass**: (a) "tq show retry-trail visibility" — the retry
   strip is in the webui detail page, not `tq show` output; (b) "DOMAIN_
   LANGUAGE carries the status/report contract terms" — it carries the
   session-bridge terms; a dedicated self-report glossary entry does not
   exist. Both fixed before commit, but they shipped in a state where a
   less careful pass would have archived a small lie permanently.
4. **Cited "44 items" precision I did not have.** The 20-32 note asserts
   the five hearsay-routed items were "re-verified in later windows" —
   true for sidecar retention/audit-json/fuzz/free-port/verbose via the
   2026-09-09 §f annotations, but I leaned on those annotations rather than
   re-deriving each one fresh this session (two of the five I did grep:
   audit-json, fuzz). The archive convention's spirit is fresh evidence;
   part of this note is annotation-of-annotations.

## e) WHAT WE SHOULD IMPROVE

1. **One-shell-chain commits**: `git add … && git commit -F msgfile` as a
   single tool call, message file written first — eliminates the daemon
   race window entirely. Should be the standing reflex for footer commits.
2. **Re-verify every factual claim in an archive note against HEAD the same
   session** — the two over-strong claims (§d3) were caught by exactly that
   pass; make it non-skippable, especially for notes that will never be
   edited again.
3. **The archive-note-vs-per-item-strike bar needs a recorded ruling** (same
   ask as 08-49 §g1): three passes have now used cluster notes; write the
   convention into AGENTS.md so future runs stop re-deciding it.
4. **Annotation-of-annotations should be labeled as such** when an archive
   note's evidence is a prior report's §f annotation rather than a fresh
   grep — a one-word "(per 09-09 annotation)" tag keeps the provenance
   honest.
5. **`docs/status/README.md` index rows are stale-formatted** (wide
   whitespace columns, hand-aligned); the six repointed rows inherited the
   form. Cosmetic; a cadence-pass normalization would help, but docs
   formatting is manual-by-decision — only worth it if the digest row
   (TODO 167) lands.

## f) Up to 25 things we should get done next

Ranked view. Items marked [NEW] surfaced this session; the rest restate
open TODO_LIST rows with fresh evidence. HARVEST note: TODO_LIST is
pool food — route with rigor.

1. [NEW] Record the archive-annotation convention (cluster note vs
   per-item strikes) in AGENTS.md Conventions — closes the 08-49 §g1 loop.
2. [NEW] Verify-and-close sweep: the 2026-09-10 trailer warning
   (`check-status-index.sh` flags 07-49 report naming task 000001a089c3…
   with no footer commit) — owner ruling pending per the warning text.
3. [NEW] Commit-per-task enforcement aid: a tiny `scripts/commit-task.sh
   <task-id>` (message file + single-chain add/commit) to end the daemon
   attribution race this session lost.
4. TODO row 167: status-index digest/cadence (the index grew 7 rows since
   yesterday's batch; the 46-file archive layer helps, the live index
   still floods).
5. TODO row 140/141: closeout-report placement + append caps — both still
   BLOCKED on owner calls; the archive sweep makes placement cheaper now
   (the sub-move mechanics are proven twice over).
6. Remaining docs-health cadence batches (TODO 166): re-check the
   2026-09-08/09 reports for archive eligibility on the next pass — this
   session's six were the last 09-07 stragglers.
7. [NEW] The 21-44 report's §f9 "old-vs-new prompt eval" idea is still
   unrouted anywhere — decide ROUTE-or-drop at next harvest.
8. ROADMAP re-verify: several "raw ideas" the archive notes cite as routed
   (W17 /metrics, W20 Datastar, W22 multi-node, journal compaction, SSE
   replay) deserve a freshness pass — some may have shipped silently in
   v0.3.0 work.
9. [NEW] `.golangci-baseline.txt` regen ownership: the file is at 111 rows
   after three regens; the "policy change owns it" rule has no named owner
   step in the release checklist.
10. [NEW] SECURITY.md + SECURITY posture: the 23-08/21-19 notes cite the
    token-auth matrix as the LAN answer; SECURITY.md has not been
    re-verified against `tq serve` flags since the v0.3.0 window (cheap
    doc check).
11. Facade parity gate: run `check-facade-parity.sh` fresh (not run this
    session; docs-only task, but it is a CI parity gate worth a beat).
12. Nightly fuzz campaign: confirm last night's run committed seeds or
    died quietly (fuzz.yml had the 2026-09-12 setup-failure class).
13. TODO row 149 (deadline-path pinning) — repeatedly cited as lead item by
    recent reports; still open.
14. TODO row 46 residual: v0.3.0 `go install` uninstallability — the
    documented note stands until the next root tag; cut v0.4.0 to restore
    installability (owner-gated push/tag chain).
15. [NEW] Add the six newly archived reports' archive notes to the
    annotate-*.py "cluster note" backlog if the tooling ever gets that
    mode (pairs with item 1).
16. CHANGELOG: the archive sweep is docs-process work — no entry needed by
    convention, but the counter bump (40→46) could ride the next cadence
    pass's CHANGELOG note if one is ever written.
17. [NEW] `scripts/check-status-index.sh` trailer warning should link to
    the ruling that would close it (it names "owner ruling pending" without
    a TODO row — findings without rows get lost).
18. Postgres backend: TQ_TEST_POSTGRES CI job green-ness is cited by recent
    reports from memory; verify the last runner run fresh (AGENTS red-
    master history).
19. [NEW] Index row scopes for the six archived rows still read as live
    reports ("Round-3 live web UI executed and verified") — fine per
    convention (archived/ prefix carries the state), but a one-word
    "(archived)" suffix would make the index self-explaining.
20. Roadmap: lint endgame option (a) "trim + re-gate" remains the
    documented alternative if baseline maintenance ever exceeds triage
    cost — revisit trigger unwritten; write the trigger down.
21. [NEW] The 18-41 §f5 "jsonv2 probe check in flake checks" was marked
    routed but never shipped — either route it to TODO_LIST or mark it
    rejected in ROADMAP (it is currently nowhere).
22. [NEW] Two daemon commits (b23ff82/019b18f) now carry substantive
    archive content; if the daemon-attribution ruling ever lands (row per
    04-45 report §g2), these two are the freshest examples to amend-or-cite.
23. webui a11y tail (23-08 §f24-25): focus-order/ARIA audit still routed,
    never scheduled.
24. Budget value decision (23-08 §f33): documented as resolved-by-managed-
    block for the MODEL; the `--daily-budget` VALUE itself was never
    ratified with data (21-19 g1 measured 2 tasks ≈ 13 min agent time).
25. [NEW] This report's own §f: harvest items 1-3, 17, 21 into
    TODO_LIST on the next docs-health pass (the rest are verify/ROADMAP).

## g) Top 3 questions I cannot figure out myself

1. **Archive annotation bar — ratify the cluster-note convention?** Three
   passes (08-49, M6, this one) have now archived 46+ reports using inline
   cluster notes instead of per-item `~~…~~ done at <hash>` strikes. I
   believe that is the de-facto ruling and want to write it into
   AGENTS.md (§f1) — confirm, or order the strict-strike re-annotation of
   the batch.
2. **Daemon attribution: amend, cite, or rule?** The archive diff lives in
   `b23ff82`/`019b18f` (heuristic daemon blobs); my footer commit carries
   only the TODO closure. The 04-45 precedent says "cited, not amended"
   for daemon-folded repairs — should I add a short attribution note to
   the README archive-counter line citing `2562792` as the task commit, or
   is the split acceptable permanent history (it is unpushed, so an amend
   is still possible)?
3. **Trailer-warning ruling**: `check-status-index.sh` warns that the
   2026-09-10 07-49 report names task `000001a089c3…` whose commits carry
   no `Task-Queue-ID` trailer. The warning says "owner ruling pending" —
   what is the ruling: annotate the report as an accepted gap, or accept
   the warning as permanent noise?

---

_Point-in-time snapshot (2026-09-14, ~00:50–01:30 CEST). Docs-only session;
verify claims against HEAD before building on them — the archive notes this
session wrote are themselves historical documents now._
