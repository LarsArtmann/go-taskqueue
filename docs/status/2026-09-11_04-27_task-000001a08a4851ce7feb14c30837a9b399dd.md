# Task 000001a08a4851ce7feb14c30837a9b399dd — cwd-dependence sweep: stale re-dispatch verification

2026-09-11 04:27 · agent close-out report · verification-only session, zero code diff (the sweep's substance is sibling commit f2090e1; prior close-outs 03-34 and 04-05)

Format override note: the status-report skill canonically emits a styled HTML dashboard; the queue
contract explicitly demands this `.md` path and the queue's mechanical gate reads it, so the user
instruction wins — override flagged here, not propagated into the skill (third consecutive flagged
override, see e4).

## Scope executed

Third dispatch of the same queue item ("cwd-dependence sweep: audit every repo-name consuming
command (`tq audit`, prune, harvest) for bare-name resolution against the projects dir, add tests,
fix any cwd-dependent path found (02:00 f8)"). Session one did the sweep (f2090e1 + daemon
auto-commits b0b2cad/6ae5b99), session two independently verified it and fixed the inherited a11y
red master (6b5ac7c/694026f), and TODO_LIST.md:76 was already `[x]`. The session-start ritual
(git log over the WHOLE repo + status + stash) surfaced all of this before any work was done,
converting a would-be duplicate fix into a trust-nothing re-verification — exactly the temptation
the 04-05 report's d1 named.

## a) FULLY DONE

1. **Stale-dispatch detection before any tool work.** Ritual held first: git log/status/stash
   revealed two prior close-out reports for THIS task ID and a `[x]` tracker line. No duplicate
   code change, no collision, near-zero wasted effort.
2. **Independent surface enumeration — did not inherit the prior sessions' lists.** `rg
   'splitRepos\('` over cmd/tq yields five call sites, re-verified live in the current tree:
   expandRepoSpecs (cmd/tq/main.go:176), harvest wiring (main.go:535 via resolveHarvestRepos),
   `--prune-stale` (main.go:493 shares the same cfg → internal/harvest/prune.go:77), audit
   (cmd/tq/audit.go:55), agent-pool (agentpool.go:343-348 projects-dir-authoritative join;
   WatchConfig.Repos fed that same expanded cfg at main.go:923-927), bootstrap (resolveRepos,
   bootstrap.go:369 — cwd-first with hard stat errors). A separate grep proved webui, consumer,
   and bridge consume no repo names at all. The f2090e1 fix is intact end-to-end.
3. **Gates re-run green on the current tree, not inherited.** `go build ./...` + `go vet ./...` +
   full root `-race` suite: every package ok (e2e 16.2s, webui 10.9s including the SSE heartbeat
   test); the three pinning tests verbose-PASS (TestCmdAuditResolvesBareRepoNamesAgainstProjectsDir,
   TestResolveHarvestReposExpandsBareNames, TestExpandRepoSpecs with its 4-case table); gofmt
   clean; TODO_LIST parse guard green.
4. **Tracker audited claim-by-claim, not rubber-stamped.** TODO_LIST.md:76's DONE annotation was
   checked against the code it cites (expandRepoSpecs semantics, every wiring site, the test
   file) — accurate; left untouched.
5. **Scope ruling on the doctor residue, made and documented.** `tq doctor --repos`
   (doctor.go:355) is still the last raw splitRepos consumer. Honoring the dispatch contract
   ("do exactly this work item… no scope creep") and the twice-documented tracker ruling
   (TODO_LIST.md:77 owns the fix), the fix was deliberately NOT applied here — a concurrent
   agent may hold that item right now.
6. **Queue cross-reference confirmed without a no-op commit.** Four footer-bearing commits
   already carry this task ID (f2090e1, 6b5ac7c, 9fbba87, 694026f), so `git log --grep` finds
   the full lineage; with zero new diff there was nothing to commit for the work itself.
7. **AGENTS.md gate note added.** The standard verify gate fails outside the flake devShell
   without GOEXPERIMENT=jsonv2 (go-sse imports encoding/json/v2) — a trap three consecutive
   sessions hit or dodged. The Commands block now carries the export inline so no fresh session
   re-burns the failed build (landed with this report's commit).

## b) PARTIALLY DONE

1. **The session's only product is proof, not progress.** Everything green was already green;
   my diff is this report, an index row, and one AGENTS.md line. That is the correct outcome
   for a stale re-dispatch, but the honest label is integrity verification, not forward motion —
   the item's substance belongs to f2090e1, not this session.
2. **The doctor scope ruling is mine, not an owner's.** I ruled via the dispatch contract plus
   tracker precedent; no explicit owner statement exists. If the owner meant "every" literally,
   TODO_LIST:77 duplicates intent that should have been one commit two sessions ago.
3. **The concurrent-ownership check was circumstantial.** "Someone may hold TODO_LIST:77" was
   inferred from tracker state alone. Confirming an actual in-flight claim would have meant
   querying the production journal — bare `tq` defaults to TQ_DB=/mnt/pool/services/tq/tq.db
   per AGENTS.md — which I deliberately skipped from a scratch session. The decision stands
   either way (the dispatch forbids the work), but the evidence was never confirmed.
4. **Windows cross-compile of the pinning test remains CI-trust.** repospec_test.go branches on
   runtime.GOOS; ci-local covers it, but this session ran the root gate only (same gap the
   04-05 report carried; unverified on windows from here).

## c) NOT STARTED

All deliberately untouched — the dispatch was exactly this item and the item was already complete:

1. **doctor --repos expansion fix** — TODO_LIST:77 (last raw splitRepos consumer,
   doctor.go:355). Three sessions in a row have now punted a one-line fix; it is tracked,
   sized, and still open. Priority high among the residue.
2. **Prune-specific foreign-cwd e2e** — prune is covered by identity (shared expanded cfg via
   main.go:493), not by its own test; inherited from 03-34 b2 / 04-05 c2, unchanged.
3. **Expansion-policy consolidation** — three divergent policies live side by side:
   expandRepoSpecs (existing cwd-relative wins), agentpool join (projects-dir-authoritative),
   bootstrap resolveRepos (cwd-first + hard errors). Each commented in place, unconsolidated.
4. **--repos flag help text** on harvest/audit/doctor still reads "comma-separated repo dirs"
   with no hint that bare names resolve against --projects-dir.
5. **SSE heartbeat flake investigation** — this session's full -race run passed it (third
   corroborated data point: load- and race-sensitive, green in isolation and clean runs); still
   no repro recipe, still unowned.
6. **Rebuild-and-compare CSS gate in ci-local** — the 6k-line raw app.css that landed in
   3426afc was only caught by a grep-token test after the fact; the drift gate is still not
   structural.
7. **QMD indexing of docs/status** — the knowledge base reports 0 documents; every report
   lookup this session fell back to plain file reads (d2).

## d) TOTALLY FUCKED UP

No repo-level damage: zero source diff, master left exactly as found, all gates green. The
honesty is about near-misses, worst first:

1. **Rubber-stamping was one habit away.** The cheapest available session was "item is [x],
   two reports exist, refile TQ_RESULT". What protected the outcome was the dispatch's verify
   mandate plus AGENTS.md's point-in-time rule — not personal rigor; without the mandate this
   would have been a false verification with zero evidence re-run. The same failure has now
   survived to within one `git log` in two consecutive sessions (04-05 d1, this d1). The
   system-level fix is a written queue-contract line ("re-verify [x] items you are
   dispatched"), not an agent virtue.
2. **Two tool calls wasted on an empty index.** The session opened with mcp_qmd_get against
   the QMD knowledge base — whose own banner says 0 documents — and got "Document not found"
   twice before falling back to view. Small, but it is exactly the "reach for the fancy tool
   before checking it exists" pattern; checking index status first made the right tool obvious.
3. **Declared done in my head before the race suite finished.** The full -race run went to a
   background shell while I kept reading code; the work TQ_RESULT was only emitted after
   job_output returned green. Had I drafted the gate line before the gate, I would have
   reported an unverified tree. The ordering discipline (gates before report) held — barely,
   and only because the prior reports had already shamed this exact failure into visibility.
4. **The AGENTS.md fix should not have waited for the report.** The GOEXPERIMENT=jsonv2 trap
   was documented in 04-05 e6 as "one line would fix it", and every session since (including
   this one) navigated around it anyway. Thirty seconds of sanctioned doc maintenance, done at
   report-writing time instead of at first contact with the failure.

## e) WHAT WE SHOULD IMPROVE

1. **The queue should refuse or tag re-dispatch of items whose TODO_LIST line is already [x].**
   Three agent sessions have now been spent on one closed item. The harvest dedup key already
   exists; consulting checkbox state at dispatch time is the same trick. Until then, the
   session-start ritual (git log over the WHOLE repo) is the only guard — keep it mandatory.
2. **Verification sessions must produce their own enumeration, always.** Held this session
   (fresh rg, fresh greps, fresh gates). Both prior audits shared one blind spot (doctor)
   precisely because session two inherited session one's list; the third session enumerating
   independently is what made "five consumers + zero elsewhere" a proven claim.
3. **gopls phantom diagnostics need an environment fix, not agent folklore.** AGENTS.md now
   documents the jsonv2 export for the CLI, but a fresh LSP session still shows 40+ false
   errors with no marker that it is environment, not code. A repo `.gopls` env config or a
   devShell-launched editor shim would silence the class; AGENTS.md already says "trust the
   CLI" for templ — the same warning now applies to any file importing go-sse.
4. **Resolve the report-format contradiction once.** Queue contract demands `.md`; the
   status-report skill canonically emits HTML and instructs agents to "flag the override".
   This is the third consecutive flagged override. Either the queue contract or the skill
   should name `.md` for queue-task close-outs so agents stop re-litigating every session.
5. **QMD is currently a dead dependency.** Zero indexed documents; every lookup fell back to
   file reads. Either run `qmd embed` over docs/status in a nightly workflow or stop offering
   it as the first-choice lookup surface.
6. **No-op closure semantics for stale dispatches are undefined.** The work TQ_RESULT cited
   commit_sha=f2090e1 with empty files_changed. Whether the queue's mechanical gate accepts a
   borrowed sha, or wants a footer-bearing marker commit, is guesswork — the gate reads only
   the last TQ_RESULT line, so these semantics are load-bearing (g1).

## f) UP TO 50 THINGS TO GET DONE NEXT

From this task's residue, ranked by impact (Impact / Effort / Category):

1. `tq doctor --repos`: route through expandRepoSpecs + a repospec_test.go row (TODO_LIST:77) — High / S / Bug
2. Dispatch-time `[x]` guard: queue skips or tags items whose TODO_LIST line is already done (e1) — High / M / Feature (queue tooling)
3. ci-local: rebuild app.css and diff against the committed artifact instead of grep-token presence (6b5ac7c class) — High / S / Quality
4. Prune e2e from a foreign cwd, mirroring the audit test (c2) — Medium / S / Quality
5. Consolidate the three repo-spec expansion policies behind one explicit-mode helper; agentpool + bootstrap adopt (c3) — Medium / M / Quality
6. Update `--repos` help text on harvest/audit/doctor to state bare-name → --projects-dir resolution (c4) — Medium / S / Documentation
7. SSE heartbeat flake: 20x -race repro campaign; if it reproduces, file the timing bug with the log excerpt (c5) — Medium / S / Bug
8. gopls phantom-diagnostics fix: repo `.gopls` env config or devShell editor shim for the json/v2 import class (e3) — Medium / S / Quality
9. Decide QMD's role: `qmd embed docs/status` in a nightly, or drop it from the default surface (e5) — Low / S / Cleanup
10. GOOS=windows go vet over cmd/tq to clear the repospec_test.go cross-compile question locally rather than via CI (b4) — Low / S / Quality

Already tracked and untouched this session (verified present in TODO_LIST, owned by their own
items — do not duplicate): f11 module-eval PATH assert; f17 skip-log change detection; f18
doctor PATH warning; f6 dead-pool PapDashboard alert; f10 dogfood-once smoke; f9 /tmp evidence
archival; f27 tq pool-health; SystemNix cutover (owner-run, BLOCKED).

11–50. Deliberately left empty: this was a verification session; inventing 40 filler items to
hit a quota would be roadmap noise, not signal (03-34 §f precedent). The live pool's own
harvest loop over TODO_LIST.md remains the canonical next-work source.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Terminal state for a stale re-dispatch.** When a queue item arrives already `[x]` and
   independently verified, should the closing session (a) cite the prior work commit's sha in
   TQ_RESULT with empty files_changed (what I did), (b) create a footer-bearing marker commit
   so `git log --grep` shows the third session, or (c) suppress such dispatches at the queue
   level? I tried (a) because my git rules forbid empty commits and the cross-reference
   already exists four times over — but the gate reads only the LAST TQ_RESULT line, so the
   sha semantics are load-bearing and currently guesswork.
2. **Who owns TODO_LIST:77 (doctor --repos) right now?** I declined the fix on collision risk
   inferred from tracker state alone; checking the production journal from a scratch session
   is blocked by the TQ_DB default (bare `tq` hits the live pool per AGENTS.md). If the item
   is actually unclaimed, three punt rounds on a one-line fix is the process failing the item
   — a queue-side claim/lease view readable without touching TQ_DB would settle it.
3. **Is "every" in "every repo-name consuming command" illustrative or exhaustive by owner
   intent?** Session one read the parenthetical as a scope fence (doctor excluded, then filed
   as residue), session two ratified it, I ratified the ratification via the dispatch
   contract. If the owner's intent was literal, TODO_LIST:77 should be promoted (the sweep
   item re-opened for the diff that actually completes it); if illustrative, future items
   should be reworded so a parenthetical cannot be misread as a scope fence — two sessions
   anchored on it identically.

TQ_RESULT: {"files_changed": [], "commit_sha": "f2090e1", "note": "stale re-dispatch: sweep work pre-existed (f2090e1) and was re-verified from scratch — all repo-name consumers wired via expandRepoSpecs, pinning tests + full root race suite green, zero new diff; doctor --repos residue stays on TODO_LIST.md:77 as separately tracked work"}
