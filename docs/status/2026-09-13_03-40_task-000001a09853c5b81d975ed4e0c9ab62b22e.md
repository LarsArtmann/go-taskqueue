# PageProps.SEO + icons.Render DISPATCH-3 Verification Close-Out (Brutal a)-g)

Task: `000001a09853c5b81d975ed4e0c9ab62b22e` (n=3 dispatch of this ID)
Dispatch: 2026-09-13 ~03:34, close-out 03:40. Session type: verify-only, docs-only.

**One-line verdict:** DONE-on-arrival again. The work item (evaluate templ-components
`PageProps.SEO` + `icons.Render`) was completed at 1dbaaac (code), documented at
6d6ed0d/694aa34 (close-outs), and closed in TODO_LIST.md row 121 `[x]` before this
dispatch. Zero deliverable changes this session. What this session added: an
independent re-verification of both verdicts, the verify gate re-run green, and a
malformed status-index row found and fixed.

## a) FULLY DONE

- **Verdict re-verified first-hand, not trusted from the ledger:**
  - `SEO` adopted for **NoIndex only**: `props.SEO.NoIndex = true` confirmed at
    internal/webui/components.go:301 (`dashboardProps`). Canonical/hreflang/JSONLD
    remain zero, per the documented ruling.
  - `icons.Render` **REJECTED** (consumer-supplied `CustomIcon` path-data extension
    point; the four filter/empty-state icons are built-in typed `icons.Name`
    constants). Verdict prose intact at AGENTS.md:401-409.
  - Library pin still v1.16.0 → the "do not re-litigate without an API change"
    guard holds.
- **TODO row 121**: `[x]` with a cited DONE verdict (TODO_LIST.md:121). Loop closed.
- **Verify gate re-run green**: `GOEXPERIMENT=jsonv2; go build ./... && go vet ./... &&
  go test ./... -race` — exit 0, 0 FAIL (webui 12.6s included).
- **Repo state clean** at turn 1; no empty commit made (contract permits it; nothing
  needed committing for the work itself).
- **Close-out report + index row written and committed** with the task footer
  (this document).

## b) PARTIALLY DONE

- **Verification scope**: root-module gate only. Per-module gates (the disk-derived
  loop) and ci-local were skipped — declared, and proportionate for a docs-only
  session where the deliverable code was authored and gated by earlier sessions.
- **`check-status-index.sh` was run only at close-out**, after the report draft —
  not as a turn-1 ritual. It passed, but see §d.
- **Index forensics partial at turn 1**: I confirmed TODO closure and the two prior
  close-outs before writing anything, but did not read the 03-28 report end-to-end
  before starting; risk of repeating its open questions unrefreshed.

## c) NOT STARTED

- Nothing from the task contract. All seven contract points addressed (AGENTS read,
  exact item, verify, no self-dealing edits, TODO loop closed, footer commit, report).
- The **§f carry-over items** below are the queue's, not this task's.

## d) TOTALLY FUCKED UP

1. **The dispatch itself is the headline sin (process, not mine alone):** this is the
   **third redundant dispatch** of a task ID that was DONE before dispatch #2. The
   window has now shipped n=3 patterns on at least two task IDs (KanbanBoard, this
   one) with no dispatch-time footer dedup in the minter. Each no-op still costs a
   full agent turn, a verify run, and a report.
2. **Same-ID report volume**: this is the third report for one task ID. The
   supersede policy flagged at 03-03 (KanbanBoard) is still unresolved, and I
   contributed a fourth datapoint instead of short-circuiting per that proposal.
3. **Index-table rot found by luck, not ritual**: the 03-28 index row was glued onto
   the gonixhelpers row without a newline (broken markdown table). The checker
   passed anyway — the gate does not parse the table structure, only filenames. I
   found it only because I opened the README to add my row; had the checker been my
   only source, the rot ships. Fixed on sight (this session).
4. **CONTRIBUTING.md unread at turn 1** — the Nth consecutive dispatch with this
   exact miss (documented in at least six prior index rows; its gates — todo parse
   guard, doc-refs, harvest test, windows build — were again not front-loaded).
5. **Minor**: the report filename pattern I used (`_task-<id>`) matches house style,
   but the skill default (HTML dashboard) was overridden by the task instruction —
   honored correctly, but I did not flag it in the work turn's final message (the
   skill requires the override be made visible).

## e) WHAT WE SHOULD IMPROVE

1. **Dispatch-time footer dedup in the minter** (the §f lead item): a task whose
   `Task-Queue-ID` trailer already resolves to a completed commit should not be
   re-enqueued — or should enqueue only a cheap "re-emit TQ_RESULT" stub.
2. **Same-ID supersede policy**: for n≥3, re-emit TQ_RESULT citing the lineage
   (work sha + close-out paths) and skip the report, unless the library version or
   verdict inputs moved.
3. **Make the status-index structural check real**: check-status-index.sh should
   verify each index line is a proper table row (starts with `| 2026-`), catching
   the glued-row genus, not just filename presence.
4. **Turn-1 checklist discipline**: CONTRIBUTING.md + prior same-ID close-outs are
   both documented turn-1 misses across this whole window. A mechanical checklist
   (or a Crush hook) beats a sixth index-row confession.

## f) Up to 50 things to get done next (brainstorm — HARVEST routing applies)

**Owner-gated (top):**
1. Push the ~14-commit unpushed master stack (carried since 00-10; every CI-green
   claim in this window is local-only until this lands).
2. Ruling: queue dispatch-time dedup guard (footer-based) — authorization + reachability.
3. Ruling: same-ID supersede policy for verification dispatches (n≥3 → TQ_RESULT-only).
4. Ruling: rejected-eval record surface — FEATURES.md rows vs AGENTS.md prose (carried since 02-47).
5. CHANGELOG entry for the NoIndex hardening (carried from 03-20 §g).
6. Ruling: `Environment=GOEXPERIMENT=jsonv2` on the NixOS tq-agent-pool unit (AGENTS Known Issue; unblocks the pool's root-module verify gate).
7. TQ_RESULT sha semantics for re-dispatches (carried 4th time: cite lineage vs emit new sha).

**Mechanism / code:**
8. check-status-index.sh structural row check (§e3).
9. Minter dedup guard implementation (§e1), once ruling lands.
10. NoIndex pin test: assert the robots meta in the rendered dashboard HTML (unit-level).
11. Smoke assertion: webui smoke checks `X-Robots-Tag`/meta noindex presence.
12. Sub-module gate runner as a single `scripts/gate-all.sh` (drops the copy-pasted loop from AGENTS.md into an executable).
13. close-out checklist hook (turn-1 items: CONTRIBUTING, same-ID reports, index read).

**Docs:**
14. SECURITY.md: add the noindex defense-in-depth line (one line; fits the existing exposure matrix).
15. Resolve the dprint CONTRIBubting-vs-AGENTS split-brain (carried since 00-19).
16. Document the `--override-input go-nix-helpers <path>` testing recipe (AGENTS says "not yet documented").

**Carried hygiene:**
17. TRAILER WARNING f26-class residue in check-status-index.sh (owner ruling pending, prints every run).
18. Master CI watch: newest ci.yml on origin was still red PRE-fix at last check — re-verify after the push lands.
19. Dependabot actions-bump redness on the same stale base (dies with #1).
20. Retired repo-root `tasks.db` (2026-09-07 dogfood journal): trash candidate, needs owner sign-off.

21-50 (lower-signal, listed for completeness): squash the AGENTS.md templ-components adoption prose into a linked eval-notes doc; collapse the four same-ID close-outs into one lineage note once superseded; add `tq facts --cqrs` smoke to ci-local; document gate.sh/exit-capture pattern as a repo script; windows-latest job for the webui suite parity check; CI_CACHE tuning investigation; fuzz.yml CAMPAIGN SETUP FAILED alerting (wired 2026-09-12 — verify it fired correctly next incident); REVIEW/`review:` dedup namespace doc cross-link; session-close bridge open items (trigger automation crush #3146, daemon attribution gap, postgres parity); postgres backend conformance refresh against latest sqlite suite additions; `tq doctor` coverage for GOEXPERIMENT-env lie detection; lint-baseline next deliberate regen review (850 findings); `--dlq-fix` second-death human surface documentation example; dead-export audit runner integration into ci-local (advisory); httpapi rate-limit docs vs SECURITY matrix parity; priority provenance `tq show` example in docs; DOMAIN_LANGUAGE.md additions for prioritize/session vocabulary; FEATURES.md PARTIALLY-DONE sweep; ROADMAP.md harvest of brainstorm items 21-50; TODO_LIST duplicate-dispatch guard note; archive 2026-09-10 planning docs whose citations were repointed; evidence-archive README spot check (ghost gate has not fired since f9); SHA256SUMS manifest spot re-verify; verify scripts/install-pre-commit.sh still current with the commit-msg hook contract.

## g) Up to 3 questions I can NOT figure out myself

1. **Why does the queue keep re-dispatching DONE-on-arrival task IDs?** Two IDs at
   n=3 in one night. Is the minter losing track of completed footer keys, or are
   these deliberate verification probes? The answer decides whether the dedup
   guard is a bug fix or a policy change.
2. **Same-ID close-out policy:** for n≥3 on a done task, is a report REQUIRED (the
   mechanical gate reads TQ_RESULT, not the report), or may the agent emit only
   TQ_RESULT citing the lineage? I can decide it, but it binds every future
   verification dispatch — owner call.
3. **TQ_RESULT `commit_sha` semantics on a zero-change re-dispatch:** is the
   expected value the original work sha (1dbaaac), the latest lineage sha
   (694aa34), or a fresh sha when the close-out commit is the only new work?
   Carried unanswered four times now.
