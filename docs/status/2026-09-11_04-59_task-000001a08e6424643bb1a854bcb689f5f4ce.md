# Task 000001a08e64 close-out — module-eval PATH assertion (02:00 f11)

**When:** 2026-09-11 04:59 · **Scope:** exactly one TODO_LIST item, smallest correct change
**Result:** DONE — committed 76ecf9a (content) + 76ecf9a-adjacent d8e71c2 (daemon race, see §a)

## a) FULLY DONE

1. **The work item itself**: `checks.module-eval` in flake.nix now asserts the
   tq-agent-pool unit's `Environment` carries a non-empty `PATH` entry. New
   `poolHasPath` helper (`builtins.any (e: builtins.match "PATH=.+" e != null)`
   over `serviceConfig.Environment or []`), wired into `allOk` next to the
   existing `TQ_DB=` elem assertion on the deployed-shape branch. This is the
   direct guard for the 2026-09-10 d1 incident class: systemd's default
   service PATH has no git/go/crush, so a vanished/empty agentPath renders the
   pool un-deployable at deploy time instead of at 3am journald forensics.
2. **Verified green**: `nix build .#checks.x86_64-linux.module-eval` passes
   end-to-end (eval of both module branches + rendered pool.conf drv).
3. **TODO_LIST.md closed the loop**: item marked `[x]` with a DONE note in the
   file's own convention (preceding f8 entries set the pattern).
4. **Committed with the queue footer**: `Task-Queue-ID: 000001a08e64…` on the
   TODO commit; flake.nix content landed via the auto-commit daemon's d8e71c2
   a moment earlier (race, not loss — both commits on master, working tree
   clean, nothing pushed).
5. **Status report indexed**: this report gets a `docs/status/README.md` row
   (check-status-index.sh would block the commit otherwise).

## b) PARTIALLY DONE

1. **Assertion coverage is one branch, not both**: `poolHasPath` runs on
   `deployedPool` (deployment shape) only; the default/StateDirectory branch
   (`defaultPool`) is not asserted. The module option is shared, so a removed
   `PATH=${cfg.agentPath}` line fails the check either way — but a _default_
   regression that only fires on the default branch (currently none exists)
   would slip. Deliberate minimalism, not an oversight; noted for honesty.
2. **No demonstrated negative case**: I reasoned the regex fails on
   `PATH=`/absent PATH but did not eval a red branch to watch the check fail.
   Confidence is high (same `allOk &&` mechanism as the seven existing
   assertions, which demonstrably fail — the 23:48 round-8 history proves the
   mechanism), but "high" is not "watched it go red".

## c) NOT STARTED

- Nothing in scope. The neighboring items visible in the same TODO_LIST
  section (f16 `checkProjectsDir` absolute-repos relaxation, f17 skip-log
  change detection, f18 `tq doctor` PATH warning) were explicitly NOT this
  task and remain untouched — contract forbids scope creep.
- CHANGELOG.md was not updated (flake-check hardening, no user-facing
  behavior change; matching the 04-43 report's precedent of skipping it for
  internal gates — flag if the convention should be "always").

## d) TOTALLY FUCKED UP!

Nothing. Small task, small blast radius. Honest wobbles, none fatal:

1. **Auto-commit daemon race**: my `git add flake.nix TODO_LIST.md` staged
   flake.nix that the daemon had _already_ committed seconds before — the
   result is a split history (d8e71c2 = flake.nix by daemon with its generic
   message, 76ecf9a = TODO_LIST.md + my descriptive message that now describes
   only half its own diff). The `git commit` output "1 file changed" is the
   tell I should have checked _before_ walking away: the queue footer landed
   on the TODO commit, so `git log --grep Task-Queue-ID` finds the work, but
   `git show 76ecf9a` alone shows no flake change. A post-commit
   `git log -1 -- flake.nix` check would have caught it in-flight.
2. **Message/content mismatch by construction**: because of the split,
   76ecf9a's message says "module-eval asserts…" while its diff is a TODO
   checkbox. Cosmetically wrong, forensically findable (d8e71c2 is its
   immediate parent). Not worth a history edit under the scripted-history
   hazard rules (AGENTS.md §scripted history edits).

## e) WHAT WE SHOULD IMPROVE

1. **Commit-and-verify ritual under the daemon**: after any commit, verify
   `git show --stat HEAD` contains the files the message names; if the daemon
   raced, note both SHAs in the close-out (done here, but late).
2. **module-eval could assert _both_ pool branches** for one line each —
   cheap symmetry, closes §b1.
3. **A red-branch demo habit**: when adding an assertion to a gate, eval one
   deliberately-broken variant once (locally, never committed) so the gate is
   proven able to fail. The 23:48 report's lesson ("its regex bug survived
   because module-eval had NEVER passed") is exactly this class.
4. **f11's original text named deploy/nixos/tq-agent-pool.nix as a touch
   point**; the fix needed zero module changes because agentPath already
   worked — worth remembering that harvested items sometimes list candidate
   files, not requirements.

## f) Up to 50 things we should get done next

_Harvest guidance: items 1–8 are the actionable tier; 9–50 are
roadmap-fuel brainstorm and should survive a HARVEST routing pass._

**Actionable (TODO_LIST tier):**

1. Assert `poolHasPath` on `defaultPool` too (one line, §b1).
2. f16: `checkProjectsDir` should not require the projects dir when every
   `--repos` entry is absolute (cmd/tq/agentpool.go + test).
3. f17: harvest skip-log change detection — log a skip class only on example
   change/first tick, not every 5m (journald flood; cmd/tq/main.go).
4. f18: `tq doctor` warns when crush/git/go are missing from PATH.
5. f6: dead-pool detection (PapDashboard alert when pool claims go quiet).
6. f10: dogfood smoke script (end-to-end pool liveness probe).
7. f27: `tq pool-health` command (claim rates, lag, DLQ depth in one view).
8. Negative-case habit: add a "gate must be able to fail" note to the
   module-eval header comment so future assertion authors self-check.

**Roadmap fuel (brainstorm, route with rigor):**
9. Module-eval: assert the serve unit's PATH story explicitly (it currently
has none — is that deliberate? document it in the module header).
10. Module-eval: assert `agentPath` flows through even when the user overrides
it (an override branch eval).
11. vm-tests for the module (E3, twice deferred consciously — module-eval
covers eval, never boot; a minimal boot test would catch unit-level
regressions regexes can't).
12. `tq doctor` could check the _rendered unit's_ PATH (systemctl show -p
Environment) rather than the doctor process's own PATH.
13. Pool unit `ExecStart` first-token assertion exists for the deployed
branch — same for the default branch.
14. watermarks lag alerting (bridge/watermark staleness → PapDashboard).
15. `tq watermarks` JSON output for machine consumption.
16. WebUI: show journal-consumer lag per watermark on the dashboard.
17. DLQ auto-rescue dry-run report (`tq dlq --plan`).
18. Harvest: warn when TODO_LIST.md has zero unchecked items (pool starvation
is a silent state today).
19. Harvest: detect BLOCKED items whose blocker text disappeared (stale
blocks).
20. prune-stale: report rescued/cancelled counts as a journal fact for the
webui nowband.
21. FuzzParseRepo: add corpus entries for repo names with unicode/trailing
whitespace (cheap nightly seeds).
22. smoke: `release-gates.sh` negative case for a sub-tag version-skew.
23. check-go-mods.sh: assert no `replace` points outside the repo (drift
guard for the replace-only decision).
24. CI: cache go module downloads per-module to cut the loop-of-modules gate
time.
25. Docs: SECURITY.md matrix row for the non-loopback + `--allow-writes`
combined case (is it forbidden? say so explicitly).
26. WebUI: cancel confirmation fragment shows the task's last error tail
(context at the point of the destructive action).
27. Executor: `sh` payload JSON-array form (`{"cmd": ["a","b"]}`) with argv
passthrough (no shell interp) — safer default for programmatic enqueues.
28. Agent executor: surface `TQ_RESULT` parse failures distinctly from verify
failures in `task.failed` evidence (`stage` already supports this — pin
with a test).
29. Review sweeper: cap reviews minted per tick (thundering-herd guard after
a bulk drain).
30. Status sweeper: `status:<project>:<trigger-id>` dedup — add a `tq facts
    --kind status` filter for debugging stuck dedups.
31. Budget: `tq stats --budget` projection view (today it's checked, not
visible).
32. Budget: per-project caps, not just global daily.
33. runactor: export a started-actors list for `tq top` ("what is the pool
actually running right now").
34. `tq top`: per-agent current-repo + task-id column.
35. Journal: compaction guidance doc (when does the append-only file become a
problem; a `tq journal size` report).
36. SQLite: `PRAGMA optimize` on graceful shutdown (cheap hygiene).
37. Postgres backend: parity test that conformance suites share cases via a
table-driven contract (verify the mirror claim, don't trust it).
38. Bridge: alert dedup window config (currently implicit in watermark).
39. CLI: `--json` output mode for `tq tasks` (scripting surface).
40. WebUI: filter state survives reload (query param ↔ filter bar sync).
41. Harvest: `--dry-run` flag (what WOULD be enqueued, mutate nothing).
42. Doctor: check `TQ_DB` pointing at the production journal from a scratch
cwd (the 2026-09-10 stray-enqueue trap deserves a loud warning).
43. Docs: ADR for "why agentPath instead of relying on systemd's default
PATH" (the incident is folklore in three reports; one ADR ends it).
44. Release: release-gates fixture for the `internal/*/vX.Y.Z` sub-tag
surface (VERSION-SURFACES says seven surfaces; fixture covers some).
45. Tests: table-driven `poolHasPath`-style assertion registry if
module-eval assertions keep growing (a list of {name, check} instead of
one giant &&).
46. Nix: module-eval eval time — consider splitting token-branch into its own
check so a token regression doesn't re-eval everything (perf, low prio).
47. Packaging: `tq version` could embed the nix drv revision (deploy-pinned
binary provenance).
48. WebUI: dark-mode contrast audit for the status nowband colors.
49. Docs: TODO_LIST.md item-grammar lint (check-todo-list.sh could reject
items without a file pointer — most stale items lack one).
50. Meta: harvest-anti-pattern sweep — re-route items 9–50 above through
docs-health HARVEST before any land in TODO_LIST (they are brainstorm,
not commitments).

## g) Questions I cannot figure out myself

1. **Should CHANGELOG.md capture internal gate hardening** (module-eval
   assertions, check scripts), or is it user/feature-facing only? Two
   consecutive reports skipped it by precedent; if that precedent is wrong,
   ~10 entries are missing.
2. **Is the split-commit history (d8e71c2 daemon + 76ecf9a TODO) acceptable
   as-is**, or do you want the daemon's auto-commit behavior suppressed for
   queue-task commits (e.g. a marker file the daemon honors) so work + footer
   land atomically?
3. **For f18 (`tq doctor` PATH warning): warn on the doctor process's own
   PATH, or query the deployed unit's rendered PATH via systemctl when
   running on the pool host?** The second is the real signal but needs
   systemd access assumptions the CLI may not want.
