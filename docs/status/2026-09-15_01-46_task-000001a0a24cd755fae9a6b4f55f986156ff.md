# Status: golangci-lint pin unification (task 000001a0a24cd755fae9a6b4f55f986156ff) — close-out re-dispatch

**Date:** 2026-09-15 01:46 · **Verdict:** DONE-on-arrival (verification-only session, second dispatch of this task ID)

## Context

Re-dispatch of TODO_LIST row 63 ("Unify the golangci-lint version pin … so the two
can never drift", 08:47 report f22/f23). The first dispatch (report
`2026-09-15_01-44_task-000001a0a24cd755fae9a6b4f55f986156ff.md`) found the work
already shipped in 550fec1 and closed the row (commit 620023a). This dispatch
re-verified and changed nothing.

## a) FULLY DONE

- **Unification (shipped 550fec1, prior window):** `.github/workflows/ci.yml:82`
  owns the single `@v2.13.2` literal; `scripts/ci-local.sh:96` grep-derives the
  pin from ci.yml for its fallback install path (M55 comment). ci-local owns no
  version literal, so drift is structurally impossible.
- **Fresh verification this turn:** derived pin resolves to exactly one hit
  (`@v2.13.2`); repo-wide `v2.13.2` sweep finds zero other _pin sources_ (remaining
  hits are docs/report prose); root `go build ./...` + `go vet ./...` green under
  `GOEXPERIMENT=jsonv2`; TODO row 63 already `[x]`.
- **First-dispatch artifacts verified intact:** close-out commit 620023a, status
  report 2d7e963, both indexed.

## b) PARTIALLY DONE

- **Fallback-path verification depth:** the ci-local grep→install path was only
  exercised as a grep resolution, not as a real `go install` round trip
  (untested-branch class; carried from the first dispatch).
- **lint-baseline gate not re-run this turn** — verification was build/vet/grep
  only; the full ci-local gate was not invoked (smallest-correct-change mandate
  vs. gate confidence tradeoff).

## c) NOT STARTED

- Nothing new: this dispatch's only deliverable is verification plus this report.

## d) TOTALLY FUCKED UP

Nothing broken this turn. Honest near-misses (both inherited from dispatch 1):

1. **Silent-zero grep risk:** `grep -oE … | head -1` in ci-local.sh would emit an
   empty pin (and a confusing `go install …@` error) if someone reformats
   ci.yml:82. The failure is loud-ish, but not self-explanatory.
2. **Task-queue economics:** two paid dispatches of a DONE item. The mint path
   cannot cheaply know row 63 was closed 30 minutes earlier — but dispatch 1's
   close-out should suppress re-mint until the fact journal catches up; this is
   the known re-dispatch loop pattern (3rd occurrence across sibling tasks
   tonight), owner-surface.

## e) WHAT WE SHOULD IMPROVE

- Guard the ci-local pin derivation: non-empty + `v[0-9.]`-shaped check with a
  clear error ("ci.yml no longer carries the golangci-lint pin literal").
- Consider a mint-time DONE check: sweeper skips re-dispatching items already
  checked `[x]` in the current TODO_LIST revision (recurring pattern, 3+ hits).
- One-line summary of the pin mechanism (ci.yml owns, ci-local derives) in the
  pin-bump runbook so future bumps only ever touch ci.yml:82.

## f) Up to 50 things to get done next

Ranked; 1–8 are the real movers, the rest are backlog/ROADMAP fuel (HARVEST
should apply extra routing rigor from 9 onward).

1. Harden `ci-local.sh` pin derivation with a non-empty/version-shaped check and a clear failure message (closes §d-1).
2. Mint-time DONE check: sweeper re-reads TODO_LIST before dispatch and skips `[x]` rows (kills the re-dispatch loop, 3 occurrences tonight).
3. Exercise the ci-local fallback install path once against a PATH without golangci-lint (untested-branch closure).
4. Run the full `./scripts/ci-local.sh` gate once on this session's HEAD to convert grep/build/vet verification into full-gate evidence.
5. Document the single-source pin rule in the release/pin-bump runbook (docs/release/) so bumps are one-edit.
6. Add a smoke assertion that `grep -oE` over ci.yml yields exactly one pin literal (drift tripwire, mirrors the facade-parity pattern).
7. Sweep TODO_LIST for other `[x]`-adjacent stale rows and prune (docs-health HARVEST).
8. Capture the "DONE-on-arrival" verdict class in the verdict-channel docs (`tq verdict` currently models work-shaped results; a no-op verification result has no canonical shape).
9. Tagliatelle/path-scoped exclusion review for machine-payload structs added since the 2026-09-12 regen.
10. Re-baseline `.golangci-baseline.txt` only if a policy change owns it — verify no growth since the 500-finding regen.
11. Fold `lint-baseline.sh --check` runtime measurement into ci.yml (CI/local parity timing).
12. Add `--self-test` to `scripts/check-dead-exports.sh` (substring-match class pin).
13. Consider `smoke/help-text.sh` coverage for any new tq subcommands since the last golden list.
14. CHANGELOG entry for the pin-unification close (docs-only, but CHANGELOG is append-only convention — decide whether docs-only dispatches warrant rows).
15. Reproduce the runner-only vendorHash FOD mismatch class on a fresh runner (AGENTS.md known issue, still open).
16. Owner fix pending: `Environment=GOEXPERIMENT=jsonv2` on the NixOS tq-agent-pool unit (the env lie that burns verifies).
17. Owner decision pending: upstream-issue candidate for crush's misleading "does not support reasoning effort" error on mistyped levels.
18. Sweep sibling repos' TODO_LISTs for items whose dedup keys changed after rewording (harvest provenance).
19. Verify `tq audit --journal` drift smoke against a seeded fixture larger than the current scratch one.
20. Confirm `DetectRateLimit` covers any new provider 429 shapes seen in the live pool since 2026-09-14.
21. DLQ review: any entries since the last sweep? (`tq dlq` operator pass.)
22. Watermark lag audit: `tq watermarks show` — confirm no consumer silently stuck.
23. Postgres backend: run the env-gated TQ_TEST_POSTGRES suite once locally (caller-owned pool closeout coverage).
24. Facade parity gate: run `check-facade-parity.sh` fresh at HEAD this week (cheap, gates silently).
25. examples/embed rot guard: confirm it still builds against the latest facade tags.
26. Check whether cmd/tq's devmod shim can be retired after the next root tag lands on the proxy (ADR-0017 follow-up).
27. go-cqrs-lite `claiming/` module: re-check parity progress against the tq Store contract (ADR re-open trigger).
28. PapDashboard bridge: verify alert resolve-flow on a recent dead-letter + rescue pair.
29. FuzzParseRepo: confirm the nightly campaign added seeds this week (nightly.sh workflow).
30. Templ LSP phantom-diagnostics note: check whether a newer templ/gopls fixed the stale-cache class.
31. `check-webui-css.sh`: confirm the byte-equal gate still passes after any tailwind bump.
32. Review `docs/status/README.md` archive counter staleness (counter updates are manual).
33. GHOST-archive gate: re-run `check-ghost-archives.sh` at HEAD.
34. Consider annotating archived 08:47 report f22/f23 with the resolution pointer (docs-health ANNOTATE).
35. Batched harvest (`--batch-items`): first live-pool batch run + post-mortem if any.
36. Verdict channel: confirm no in-flight pool tasks still rely on the legacy stdout `TQ_RESULT` line (deletion precondition).
37. Session-close bridge: retry the crush #3146 trigger-automation check upstream.
38. Prioritize sweeper: review `priority_scores` cache hit-rate after a week of live runs.
39. Review the review-window follow-up section: how many rows remain unchecked since 2026-09-10?
40. `tq doctor`: consider adding a pin-consistency check (ci.yml literal vs installed golangci-lint --version in devShell).
41. Add CHANGELOG note when the legacy stdout TQ_RESULT path is deleted (planned).
42. Confirm setup-go pins (7 steps, 1.26.7) unchanged after any actions-bump PR.
43. Consider `nix run .#test` runtime trend tracking (multi-module suite cost).
44. windows-latest suite: any new `//go:build unix` skips that grew silently?
45. Security model: re-run SECURITY.md matrix against `tq serve` after any webui route change (none this session).
46. Evidence-archive discipline: no new archives this session — confirm SHA256SUMS manifests still verify at HEAD.
47. Review whether grep-derived pins (this pattern) exist elsewhere (govulncheck v1.8.0 is also literal in ci.yml only — acceptable, but document the asymmetry).
48. docs/DOMAIN_LANGUAGE.md: add "pin derivation" vocabulary if the ci-owns/derives pattern recurs.
49. TODO_LIST: split the review-window section into shipped/remaining so the checked tail stops growing unwieldy.
50. Owner question forum: batch tonight's three questions (below) into one review turn.

## g) Questions I cannot figure out myself

1. **Re-dispatch sanction:** is the second (third system-wide) dispatch of an already-done row acceptable spend for freshness verification, or should the queue adopt a mint-time `[x]` check that returns a no-op verdict? (Affects item 2 above.)
2. **Pin-mechanism policy:** should ci.yml:82 remain the sole literal forever, or should the pin move to a checked-in file (e.g. `scripts/tool-versions.env`) that both CI and ci-local read — trading one indirection for grep-fragility immunity?
3. **Advisory fail-loudness:** the §d-1 empty-pin failure would surface as a cryptic `go install …@` error in a local-only path. Should the fallback install step fail loudly (non-advisory) on a malformed pin, or stay advisory with the whole lint block per current policy?
