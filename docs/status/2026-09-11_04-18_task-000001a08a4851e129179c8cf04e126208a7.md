# Status Report — task 000001a08a4851e129179c8cf04e126208a7 (review round 2)

**Date:** 2026-09-11 04:18 CEST
**Work item:** The SAME review finding ("module-gate timings swapped between jobs", low, filed against 674320f) bounced back a second time, verbatim, after round 1 had already refuted it (cfa8412 + the 03:46 report).
**This window:** 04:00–04:18 CEST, repo state 694026f → 58b7b74. One commit: 58b7b74 (dated addendum on this task's 03:46 report). Nothing pushed.
**Format note:** user instruction demands markdown at a task-keyed path; the status-report skill's HTML default is overridden by explicit user instruction (same divergence as the 03:46 report — do not propagate either way).

---

## a) FULLY DONE

- **The re-filed finding was verified against the primary source BEFORE any edit — again, fresh.** Pulled run 34406684629's job/step data via `gh api` in THIS window (2026-09-11 ~04:02): job `test` (21:23:52→21:28:23 = **271s**) owns step "Module isolation gates (GOWORK=off per sub-module)" (21:26:26→21:26:52 = **26s**, 26/271 ≈ **~10%**); job `test-windows` (21:23:53→21:26:46 = **173s**) owns step "Module isolation gates (per sub-module)" (21:25:53→21:26:32 = **39s**, 39/173 ≈ **~23%**). Corroborated by the workflow file itself: ci.yml:34 (GOWORK=off step, inside the `test` job's GOOS=windows cross-build block) vs ci.yml:110 (plain step, inside `test-windows`). Byte-identical to the round-1 pull.
- **Verdict: the finding is refuted on the same two counts as round 1.** Its step-name↔job pairing is inverted ("per sub-module" is NOT the test job's step; "GOWORK=off per sub-module" is NOT test-windows'), and its "~14% and ~15%" derive from that inverted pair (39/271, 26/173). The record it attacks — TODO_LIST.md:71, commits 674320f/15ff1f9, the cfa8412 pinning — matches the cited run exactly.
- **The swap was refused a second time.** Applying it would have replaced correct data with data its own cited run contradicts (verify-external-claims skill: "feedback is a lead, not a source"; AGENTS.md: "independently verify tool output before mutating anything"). A review finding about numbers is a claim, not a fact — held under re-filing pressure.
- **Minimal correct remedy applied, nothing else touched:** a dated "Addendum — review round 2" section appended to this task's 03:46 report (annotate-never-rewrite respected), recording the fresh evidence and the refusal. TODO_LIST.md needs NO change — line 71 was confirmed unchanged this window and is already `[x]`, already step-name-pinned (the round-1 mitigation), and correct. The queue's contract item 5 (close the loop) therefore remains satisfied as-is.
- **Gates green:** `scripts/check-todo-list.sh` ok, `scripts/check-status-index.sh` ok, root `go build ./...` + `go vet ./...` green (`GOEXPERIMENT=jsonv2` per the AGENTS.md env note). Docs-only change; no Go surface touched, so no module-level test run applies.
- **Loop closed per contract:** committed as 58b7b74 with the exact `Task-Queue-ID:` footer; pre-commit hook (status-index gate) passed; working tree clean after; nothing pushed.
- **One defensible sentence in the finding acknowledged:** "the no-tuning conclusion still holds" — true under either pairing, and it was already the recorded conclusion (neither ~10%/~23% nor the reviewer's ~14%/~15% dominates its job).

## b) PARTIALLY DONE

- **The refutation's reach is still partial — and this window PROVED it is not reaching the reviewer at all.** Round 1's evidence lives in cfa8412's message, TODO_LIST.md:71, and the 03:46 report; the queue re-filed the finding verbatim anyway. The only new artifact this round is the addendum, which lives in the same docs the loop demonstrably does not read. Remaining: owner/queue decision on the reply path (§g Q1 — now urgent, see §d1). Effort: S once the channel is known; blocker: review tooling not visible to this session.
- **Ambiguity remediation still covers one artifact of three.** TODO_LIST.md:71 is pinned; the 2026-09-10_09-33 close-out report and its docs/status/README.md index row still carry the same bare (step-name-less) timing pairs and cite unreachable 674320f. All contain CORRECT pairings — bare is exactly the shape that misled the reviewer. Untouched again, deliberately: point-in-time snapshots, annotate-never-rewrite, and this window's fix-exactly-the-finding contract. Remaining: one ANNOTATE-mode pass (docs-health). Effort: S; no blocker.
- **Round-1 report §f/§g carries forward** — its 23 next-items and 3 owner questions were not harvested into TODO_LIST/ROADMAP (it explicitly deferred that to the next docs-health pass, and this window inherited the same scope discipline). §c below lists what this window verified about their current state.

## c) NOT STARTED

- **Annotate pass over the 09-33 report + README:19 index row** (add step names, repoint orphan 674320f → live 15ff1f9) — NOT started, second window in a row; contract-bound both times. Round-1 §f1.
- **HARVEST of round-1 §f into TODO_LIST/ROADMAP** — NOT started; belongs to the next docs-health pass. Round-1 §f15.
- **Resolved in passing this window (via the status index, not re-verified locally):** the red master this task inherited (`internal/webui TestA11yChrome`) was fixed by the sibling 04:05 window via 6b5ac7c (app.css regenerated); the `tq doctor --repos` residue was filed into TODO_LIST by the same window. Round-1 §c3/§f2 accordingly downgraded to "resolved upstream, unverified here."
- **Everything else inherited from round-1 §f is a lead, not started, unverified this session** — see §f for the carried-forward list with provenance labels.

## d) TOTALLY FUCKED UP!

1. **A second full review cycle was burned on the same refuted finding, and my round-1 output demonstrably could not prevent it — that is the systemic fuckup, and it is still unowned.** Sequence: round 1 refused the swap, pinned step names into TODO_LIST.md:71, committed the evidence with the task's own Task-Queue-ID footer, and answered the queue with `outcome: finding_refuted` in TQ_RESULT. The queue re-filed the finding VERBATIM. So the round-1 mitigation (self-evident TODO line) never got read, and the queue's loop re-files without consuming refutation evidence. Cost so far: two full agent windows on a LOW-severity docs line whose numbers were right from the first commit. If re-filing is mechanical (the finding text is byte-identical), rounds 3, 4, … are foreseeable until someone with queue visibility answers §g Q1. I cannot close this loop from inside the repo — the evidence has nowhere to go that the reviewer reads.
2. **Honest self-critique on this window's own efficiency:** I spent the opening calls re-deriving lineage (`git show` of three commits) that the 03:46 report had already documented, before landing on the two calls that actually decided the outcome (fresh `gh api` pull + line-71 check). The AGENTS.md point-in-time rule does demand re-verification of prior-session claims, so the API pull was mandatory; the lineage re-read was belt-and-braces (~2 wasted round-trips). A tighter window: API pull → line-71 check → addendum → commit. No damage done, but the pattern generalizes: re-verify DECISION-CRITICAL claims, cite the prior report for already-documented context.
3. **No data corruption, no red gates, no production contact** — zero bare `tq` invocations (the `TQ_DB` trap avoided), docs-only diff, tree clean. The near-fuckup class from round 1 (complying unverified) did not recur; the verify-before-encode order held twice now.

Neither of the live items blocks development or users; #1 burns review bandwidth, #2 cost minutes.

## e) WHAT WE SHOULD IMPROVE

- **Findings that dispute a number must carry their raw evidence** (query + output), symmetric to the TQ_RESULT contract for work. This finding asserted run data twice without quoting it; one `gh api` line would have exposed the mispairing to its own author before either filing. Owner decision (§g Q1/Q3). Survived two rounds — escalate it.
- **Decide the refuted-finding reply path NOW.** Round-1 §g Q1 asked politely; this window is the empirical answer that silence means bounce. Whatever the channel is (queue comment field, TQ_RESULT outcome semantics, human triage), it must be able to TERMINATE a refuted finding.
- **Verify disputed numbers BEFORE judging the remedy** — formalize the order. It has now decided the outcome twice in a row (both rounds would have corrupted the record if complied with). Keep it mandatory; it is cheap (one API call) relative to the cost of an encoded falsehood.
- **Re-verification effort should scale with decision criticality, not report age.** §d2: fresh primary-source pull for the number under dispute (mandatory), prior-report citation for unchanged context (sufficient). Writing the "what does this window actually need to re-derive" list before the first tool call would have saved the lineage re-read.
- **Quote the observation unit in every measurement record** (step names, run IDs, paths). TODO_LIST.md:71 does it now; the 09-33 report, its index row, and two commit messages still carry bare pairs — that bareness is the root cause of BOTH filings (round-1 §d1 stands). Fold into the annotate pass.
- **Cite reachable SHAs in permanent docs.** 674320f is an orphan fork twin (pushed twin 15ff1f9 carries the identical correct numbers); citations to it will rot at GC. After any rebase/reword, sweep `rg <old-sha> docs/ TODO_LIST.md` and repoint (fold into the scripted-history playbook, docs/status/2026-09-10_07-49 §e).
- **Self-critique, minor:** my round-2 TQ_RESULT included `outcome: finding_refuted` on a guess that the gate might read it — unverified, and the bounce suggests it does not. Per verify-external-claims, that field was a hedged experiment, not a verified mechanism; §g Q3 asks the owner to settle its semantics rather than letting agents invent them per-window.

## f) Up to 50 things we should get done next

Ranked by impact. Provenance labels: **[W2]** verified/effective THIS window; **[R1]** carried from round-1 §f (unverified this session unless marked); **[idx]** learned from the status index in passing this window (not re-verified locally); **[owner]** owner decision. I stop at 27 honest items rather than pad to 50 — the ceiling is a brainstorm bound, not a quota, and fabricated leads are worse than whitespace (round-1 stopped at 23 for the same reason).

1. **Decide the refuted-finding reply path** — can a finding be CLOSED as refuted, and through which channel? Two windows of evidence say the current loop re-files forever. [W2][R1§f3][owner] Impact: High (stops the bleeding). Effort: S.
2. **Queue convention: numeric findings must quote primary-source evidence** (query + raw output). [W2][R1§f7][owner] Impact: High. Effort: S.
3. **Define TQ_RESULT outcome-field semantics** (does any gate read `outcome`? can it carry finding_refuted/verified?) — or drop the field so agents stop guessing. [W2][owner] Impact: High. Effort: S.
4. **Annotate the 2026-09-10_09-33 report + README:19 index row**: add CI step names to both timing pairs, repoint orphan 674320f → 15ff1f9 (docs-health ANNOTATE). [R1§f1] Impact: High (kills the ambiguity class that caused both filings). Effort: S.
5. **HARVEST round-1 §f + this §f into TODO_LIST/ROADMAP** in the next docs-health pass. [R1§f15] Impact: High. Effort: S.
6. **Archive the /tmp dogfood evidence** (/tmp/tq-dogfood.db + review log) before a reboot destroys it. [R1§f4] Impact: High — one reboot from loss. Effort: S.
7. **`tq doctor`: warn when crush/git/go are missing from PATH** (the class that killed the deployed pool ~20h). [R1§f5] Impact: High. Effort: M.
8. **Dead-pool detection**: PapDashboard alert on an every-repo skip streak. [R1§f6] Impact: High. Effort: M.
9. **Regenerate/verify webui CSS gate**: RESOLVED upstream this window per the index (6b5ac7c) — re-verify locally once, then strike; if the "committed CSS vs templ source" drift class recurs, decide the CSS-regen gate tradeoff explicitly. [idx][R1§f2] Impact: High if it recurs. Effort: S.
10. **SSE heartbeat flake**: now two-agent corroborated per the index (03:34 + 04:05) — treat the next recurrence as a real timing bug in the heartbeat shutdown path, not a flake. [idx][R1§f21] Impact: Medium. Effort: S.
11. **Extend the repo-name fix to `tq doctor --repos`** — residue filed into TODO_LIST by the 04:05 window per the index; verify it is actually there, then schedule. [idx][R1§f8] Impact: Medium. Effort: S.
12. **One AGENTS.md Conventions sentence**: DONE-line measurements name the observed step/artifact/run ID. [R1§f14] Impact: Medium. Effort: S.
13. **Harvest skip-log change detection**: log a skip class only when its example changes, not every 5m tick. [R1§f9] Impact: Medium. Effort: S.
14. **`checkProjectsDir` unnecessary when every `--repos` entry is absolute.** [R1§f10] Impact: Medium. Effort: S.
15. **module-eval hardening**: assert the pool unit's Environment carries non-empty PATH. [R1§f11] Impact: Medium. Effort: S.
16. **`tq pool-health`**: per-repo skip streaks + last harvest activity from the journal. [R1§f12] Impact: Medium. Effort: M.
17. **worktree-per-agent design doc** → docs/planning/ with open questions. [R1§f13] Impact: Medium. Effort: M.
18. **dogfood-once.sh smoke** (env-gated TQ_DOGFOOD=1; stub-agent variant ungated). [R1§f16] Impact: Medium. Effort: M.
19. **Average the module-loop timing over ~5 green runs** to replace the one-run snapshot (becomes trivial if #20 lands). [R1§f17] Impact: Low. Effort: S.
20. **`::notice` step-timing echoes in ci.yml's module loops** so timing questions cost one `gh api` call — would ALSO have made both rounds of this finding self-refuting at filing time. [R1§f18] Impact: Low. Effort: S.
21. **Per-module breakdown of the 39s Windows gate step** — needs #20 first. [R1§f19] Impact: Low. Effort: M.
22. **Fold the orphan-SHA citation sweep into the scripted-history playbook** (docs/status/2026-09-10_07-49 §e). [R1§f20] Impact: Low. Effort: S.
23. **Owner-run: master is further ahead of origin/master than round-1 flagged** (this window's log showed 695… lineage unpushed per AGENTS.md no-push rule; round-1 counted 5+, more have landed) — push and remote-CI reconcile is owner-gated. [R1§f22][owner] Impact: Medium (remote CI red/divergence compounds). Effort: S.
24. **Bare `go build ./...` outside the nix devShell fails on encoding/json/v2** — a wrapper or a clearer error pointer (AGENTS.md documents GOEXPERIMENT=jsonv2; do not re-litigate the design). [R1§f23] Impact: Low. Effort: S.
25. **Check TODO_LIST actually carries the doctor --repos + CSS-gate residue items** the index says were filed (index claims are claims). [W2-adjacent] Impact: Low. Effort: S.
26. **Add a one-line "refuted twice" pointer to the 09-33 annotate pass** (#4) so the third reader of the bare pairs finds the refutation from the artifact itself. [W2] Impact: Low. Effort: S.
27. **Session-start ritual compliance note**: this window checked `git log --oneline -8 + status + stash` before editing per AGENTS.md — keep it; the concurrent-agent assumption held (index shows two sibling windows landing while this task cycled). [W2] Impact: Low (process hygiene). Effort: —.

## g) Questions I can NOT figure out myself

1. **How does a refuted finding get CLOSED in this queue — and why did it bounce a second time?** Round-1 evidence (cfa8412, 03:46 report, `outcome: finding_refuted` in TQ_RESULT) demonstrably did not reach the reviewer. Is there a reply channel, does the loop re-file mechanically until a human intervenes, or is the re-file a bug? This decides whether round 3 is foreseeable and whether I should keep writing addenda at all.
2. **May the 2026-09-10_09-33 report and its README index row be annotated OUTSIDE a docs-health window?** (Round-1 §g Q2, still open.) Annotate-never-rewrite and the no-unrelated-changes contract both said wait; meanwhile the bare pairs caused a SECOND filing and the orphan-SHA citations keep rotting. Which constraint wins?
3. **Do the queue's mechanical gates read any TQ_RESULT field beyond the last line** (`outcome` in particular)? I emitted `outcome: finding_refuted` twice on the assumption it might matter; the bounce suggests it does not. If nothing reads it, agents should stop inventing fields; if something does, its vocabulary should be defined somewhere agents can read.

---

_Point-in-time snapshot: 2026-09-11 ~04:18 CEST, repo state at 58b7b74 (this window's commit), tree clean. Run-34406684629 numbers re-verified live via `gh api` this window; ci.yml mapping re-checked; TODO_LIST.md:71 confirmed unchanged and correct. §f items labeled [R1]/[idx] are inherited leads per the point-in-time rule — re-verify before acting. The 04:05 sibling window's fixes (6b5ac7c app.css, doctor residue filing) are known from the status index and git log, not re-verified locally._
