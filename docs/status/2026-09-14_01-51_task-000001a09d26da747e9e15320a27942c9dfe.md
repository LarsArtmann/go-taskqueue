# Task 000001a09d26da747e9e15320a27942c9dfe — varnamelen raw-site rename RE-DISPATCH (2026-09-14 01:51)

> **ANNOTATED 2026-09-14 (docs-health strikethrough pass)** — every numbered item below is resolved inline (`done`/`duplicate` markers) or left untouched (= still open). Unarchived on purpose: open items remain. Evidence verified against HEAD.

Second dispatch of the same task (first close-out: `2026-09-14_01-46_task-000001a09d26da747e9e15320a27942c9dfe.md`,
work commit `d867c72`). This window verified the task was already DONE-ON-ARRIVAL
and re-ran the verification gates fresh. Zero deliverable changes this window.

## a) FULLY DONE

1. ~~**The work item (prior window, re-verified)**: both varnamelen raw sites~~ duplicate — see 01-46 a1
   renamed — `o`→`opts` (cmd/tq/agentpool.go:334 `harvestConfigFromOptions`),
   `f`→`configFile` (cmd/tq/poolconfig.go:26 `loadPoolConfigFile`). Commit
   `d867c72`, footer `Task-Queue-ID: 000001a09d26da747e9e15320a27942c9dfe`
   present (checked via `git show`).
2. ~~**TODO_LIST.md row 148** already `[x]` with DONE-evidence annotation~~ duplicate — see 01-46 a4
   (closed by the same commit d867c72; report indexed by cda9541).
3. ~~**Fresh verification THIS window (evidence for the DONE verdict)**:~~ duplicate — see 01-46 a3
   - Root gate: `GOEXPERIMENT=jsonv2 && go build ./... && go vet ./...` green;
     `go test ./... -count=1` green (root module only, per the multi-module rule).
   - cmd/tq module gate: `./scripts/test-cmd-tq.sh` green (`ok ... 8.3s`).
     Note: the bare `cd cmd/tq && GOWORK=off go test` path FAILS this session
     with `ambiguous import` (proxy `v0.3.0` cmd/tq dir vs the replace-free
     local module) — the devmod shim is the documented path and works.
   - Working tree clean at start and end; no edits made (none needed).
4. ~~**Honest no-op handling**: instead of re-renaming nothing or inventing~~ done — narrative (point-in-time process note, no artifact owed)
   diffs, this window re-verified the prior commit's claims against HEAD and
   reports the no-op explicitly (same discipline as the 01-34/01-37 re-dispatches).

## b) PARTIALLY DONE

1. ~~**Scoped `--enable-only varnamelen` over cmd/tq**: NOT re-runnable raw this~~ done — narrative (point-in-time process note, no artifact owed)
   window — golangci-lint in cmd/tq dies on the same `ambiguous import` setup
   error (the 01-46 report's scoped-0-issues claim stands on the devmod shim
   path). The discrepancy itself (2 vs 0) is closed by the renames regardless;
   this is a tooling papercut, not an open question about the work.
2. ~~**Environment note filed**: the ambiguous-import failure mode for~~ done — narrative (point-in-time process note, no artifact owed)
   `golangci-lint run` inside cmd/tq is now evidenced in this report but not
   fixed (f-list item 2).

## c) NOT STARTED

Nothing from the work item — every clause (rename-or-extend, scoped/full
agreement, TODO closure) is resolved. Outstanding items below are f-list
follow-ups, not parts of this task.

## d) TOTALLY FUCKED UP

Nothing this window. Misses caught early, before damage:

1. **Wasted gate attempt**: ran `go test ./cmd/tq` from root first (AGENTS.md
   explicitly says that fails by design — turn-1 read miss). Self-corrected to
   the shim within one round trip.
2. **Bare-module test attempt**: `cd cmd/tq && GOWORK=off go test` failed on
   ambiguous import before I reached for the documented
   `scripts/test-cmd-tq.sh` (the fix for exactly this exists and is documented).

## e) WHAT WE SHOULD IMPROVE

1. ~~**Re-dispatch dedup at enqueue time**: this task was re-sent after being~~ duplicate — see 01-46 f14
   DONE-on-arrival and already closed (row 148, two commits, one report). A
   queue-side check (footer ID → existing DONE fact/report) would save a whole
   window. (Repeat ask from the 01-37 report — still open.)
2. ~~**cmd/tq lint/test ergonomics**: every gate inside cmd/tq must go through~~ duplicate — see 01-46 e4/f1
   the devmod shim; raw golangci-lint/test die on the proxy-vs-local ambiguous
   import. A `scripts/lint-cmd-tq.sh` sibling of build/test would close the gap.
3. ~~**No-op dispatch contract**: when a dispatched task is already done, the~~ done — AGENTS.md:193 (verified at HEAD)
   honest output is verify-and-report (this window). Ratify that as the
   expected shape so verify-only windows aren't judged as under-delivery.

## f) Top 50 things we should get done next

(brainstorm, highest impact first; 1–20 are actionable TODO_LIST fuel, 21–50
are ROADMAP-grade)

1. ~~Add `scripts/lint-cmd-tq.sh` (devmod-shim-wrapped golangci-lint for cmd/tq).~~ duplicate — see 01-46 f1
2. Fix golangci-lint's `ambiguous import` inside cmd/tq (exclude the proxy
   copy or give golangci-lint the devmod module context).
3. ~~Queue-side re-dispatch dedup: skip dispatch when a task's footer ID already~~ duplicate — see 01-46 f14
   has a DONE fact + closed TODO row.
4. Wire cmd/tq into ci-local's per-module lint loop (currently internal-only,
   per the 01-46 report's finding).
5. ~~Ratify the verify-only re-dispatch convention in AGENTS.md (one paragraph).~~ done — AGENTS.md:193 (verified at HEAD)
6. ~~Document the ambiguous-import failure mode in AGENTS.md Known Issues.~~ done — AGENTS.md:30-32 (verified at HEAD)
7. Sweep TODO_LIST for other already-done rows missing evidence annotations.
8. ~~Add a CI step that greps dispatched-task IDs against closed rows (cheap dedup).~~ resolved — routed row 157 (verified at HEAD)
9. Extend `check-status-index.sh` to flag duplicate index rows for one task ID.
10. Cache golangci-lint results per module to speed ci-local (lint is the slow gate).
11. ~~Amend the verify-gate doc: name `scripts/test-cmd-tq.sh` as THE cmd/tq test path.~~ done — AGENTS.md:40 (verified at HEAD)
12. ~~Add `--count=1` to the shim's test invocation to defeat test caching in CI-adjacent runs.~~ done — test-cmd-tq.sh:27 -count=1 (verified at HEAD)
13. Add a smoke that runs the scoped `--enable-only varnamelen` over cmd/tq so the
    2-vs-0 class of discrepancy can never silently reopen.
14. Baseline regeneration policy doc: when shrink is a policy change vs drift.
15. ~~Add go.mod-health self-check at the top of every agent verify gate (leaf-floor class).~~ done — AGENTS.md:541 (verified at HEAD)
16. ~~Dogfood task: make the queue itself re-verify DONE rows before closing the loop (claims-carry-citations enforcement).~~ resolved — routed rows 148/157 (verified at HEAD)
17. Add `git show --format=%B -s` footer check to the pre-push gate (footer presence, not just commit-msg hook).
18. nightly.sh: alert when a campaign runs 0s (setup-failed masquerade class).
19. ~~Bootstrap: auto-add `Environment=GOEXPERIMENT=jsonv2` guidance to the NixOS module docs (pool verify env lie).~~ resolved — routed row 98 (verified at HEAD)
20. Port `execWithTransientRetry` to any exec site not yet using it (ETXTBSY class audit).
21. Postgres backend: full conformance parity audit vs sqlite (watermarks, DLQ, repri).
22. ~~Session-close bridge: trigger automation (crush #3146) follow-up.~~ resolved — routed row 109 (verified at HEAD)
23. ~~Session-close bridge: daemon-commit attribution gap.~~ resolved — routed row 147 (verified at HEAD)
24. ~~Session-close bridge: budget bypass for SessionEnd-hook enqueue.~~ resolved — routed row 146 (verified at HEAD)
25. ~~Session-close bridge: postgres parity for direct-enqueue review/status.~~ resolved — routed row 110 (verified at HEAD)
26. ~~Prioritize sweeper: dashboard surface for cached priority verdicts (`tq show` provenance exists; aggregate view doesn't).~~ resolved — routed row 123 (verified at HEAD)
27. DLQ autopsy: operator surface for "rescued-then-died-again" second deaths (currently silent human surface).
28. `tq dlq --dismiss` bulk mode with confirmation.
29. Consumer lag: alert threshold wiring into papdashboard bridge (lag is observable, not alerted).
30. ~~WebUI: task-detail payload section for prioritize batches (verdict rendering).~~ resolved — routed row 123 (verified at HEAD)
31. WebUI: filter persistence across reloads.
32. Examples/embed: add a second example wiring the worker facade end-to-end.
33. ~~Facade parity: scripted check that facade go.mods contain EVERY graph-module replace (the worker-class failure is gate-caught, not prevented).~~ resolved — routed row 156 (verified at HEAD)
34. ~~Release: automate sub-tag pre-cutting before release.sh gates (currently manual, gate-caught).~~ resolved — routed row 156 (verified at HEAD)
35. Release: dry-run mode for release.sh that validates allowlists without tagging.
36. Fuzz: add ParsePrioritizeResult to the nightly campaign corpus rotation.
37. Fuzz: seed corpus coverage report (which grammar branches are unhit).
38. Harvest: prune-stale sweep dry-run flag for operators.
39. ~~Harvest: `catchup:` prefix documentation in DOMAIN_LANGUAGE.md.~~ done — DOMAIN_LANGUAGE.md:21,55 (verified at HEAD)
40. Budget: daily-cap rollover policy decision (hard reset vs grace).
41. Review sweeper: configurable review-band priority (currently fixed).
42. Status sweeper: per-project report-in-flight visibility in `tq tasks`.
43. Doctor: check for the empty `~/.config/go/env` store symlink (the env lie detector).
44. Doctor: detect agentPath-less systemd deployments (pool-deploy failure mode class).
45. ~~Docs: SECURITY.md matrix refresh for the `?token=` URL pattern (post-SEO/noindex change).~~ resolved — routed row 159 (verified at HEAD)
46. ~~Docs: ADR for the verify-only re-dispatch convention (if ratified).~~ done — AGENTS.md:193 (verified at HEAD)
47. ~~Tooling: dependabot/renovate for the internal module graph (external deps only).~~ resolved — routed row 31 (verified at HEAD)
48. Tooling: govulncheck as a scheduled (not ad-hoc) gate.
49. WebUI: keyboard navigation on the board (accessibility pass).
50. ~~Roadmap: re-open the go-cqrs-lite durable-work-queue module comparison when upstream ships (documented trigger exists).~~ done — AGENTS.md:749 upstream trigger (verified at HEAD)

## g) Questions I can NOT figure out myself

1. ~~**cmd/tq lint-loop exclusion intent**: is cmd/tq deliberately excluded from~~ duplicate — see 01-46 g1
   ci-local's per-module golangci-lint loop (shim-only policy), or an oversight
   the ambiguous-import failure has been masking? (Determines whether f1/f2 fix
   the loop or add a parallel path.)
2. ~~**gopls in the devmod module**: is anyone successfully running gopls inside~~ duplicate — see 01-46 g2
   cmd/tq via the devmod shim, or does the ambiguous import break IDE support
   there too? (If broken, f2's fix should target module resolution generally.)
3. ~~**Daemon source-commit gate**: should the auto-commit daemon refuse to~~ duplicate — see 01-46 g3
   commit `.go`/source changes for task IDs that already have a DONE fact
   (f3's enforcement end), or is enqueue-time dedup the only intended layer?
