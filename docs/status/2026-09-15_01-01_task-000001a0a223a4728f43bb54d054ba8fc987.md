# Help-text smoke task close-out — task 000001a0a223a4728f43bb54d054ba8fc987

- **Date**: 2026-09-15 01:01
- **Task**: Help-text smoke: run `tq` subcommand help (minimum `tq dlq -h`) asserting no
  parenthesized-identifier artifacts in flag strings (rename-leak class, 08:42 report e3/f2)
- **Work commit**: 67905f9 (TODO checkoff; smoke + ci-local wiring + AGENTS row rode daemon
  commit eaadb1a — see §d footer race)
- **Files changed**: `scripts/smoke/help-text.sh` (new), `scripts/ci-local.sh`, `AGENTS.md`,
  `TODO_LIST.md`, this report + index row

## TL;DR

The work item shipped complete and verified in both directions. One deliverable gap remains
against this task's own bar (a pinned, self-running negative fixture — my negative test was a
throwaway /tmp fake), plus a hardcoded subcommand list that can silently rot. Everything else
is green: root build/vet/race, cmd/tq shim gate, smoke PASS (23 helps), negative FAIL (seeded
`task(store)`), TODO row `[x]`.

## a) FULLY DONE

1. **`scripts/smoke/help-text.sh` (new, 67 lines)** — runs `tq -h` plus every subcommand's
   help: 18 direct `-h` commands, grouped leaves `watermarks show|set`, `session begin|close`,
   and `tq verdict -h` (which prints usage but exits 1 — handled). Minimum coverage
   `tq dlq -h` is explicit and independent of list parsing.
2. **Artifact detection** — `grep -E '[A-Za-z0-9]\([a-zA-Z][a-zA-Z0-9]+\)'`: identifier
   attached DIRECTLY to a preceding word (`task(store)`). The legit plural `(s)` and the flag
   package's space-separated `(default 3)` are exempt. Verified: clean binary PASSES all 23;
   a /tmp fake binary whose help reads `-dlq-max  attempt budget for rescued task(store)`
   FAILS with the offending line echoed. Exports `TQ_DB` into a scratch file so the binary
   never touches production (the inherited-`TQ_DB` hazard in AGENTS.md).
3. **Wired into `ci-local.sh`** as a gating step after fullcore ("help-text smoke (no
   parenthesized-identifier artifacts in tq help)").
4. **AGENTS.md** smoke list row added (with the 08:42 e3/f2 provenance pointer).
5. **TODO_LIST.md** row checked `[x]` with date + evidence, per file convention.
6. **Verification run this turn**: root `GOEXPERIMENT=jsonv2 go build/vet/test -race` green;
   `./scripts/test-cmd-tq.sh` (cmd/tq module gate) green; `bash -n` on both edited scripts;
   smoke positive + negative as above. Both commits carry/at least one carries the
   `Task-Queue-ID: 000001a0a223…` footer; nothing pushed.

## b) PARTIALLY DONE

1. **Negative coverage is not pinned.** The seeded-`task(store)` FAIL was a throwaway
   /tmp fixture run manually during the session. Unlike `release-gates.sh` (positive +
   negative fixture go.mods) and `journal-drift.sh` (seeded scratch fixture), help-text.sh
   has no `--self-test` mode or committed fixture proving the detector regex still matches
   after any future edit. The gate currently proves "today's help is clean", not "the
   detector can still see the class".
2. **CHANGELOG row not added.** The journal-drift smoke got a CHANGELOG row (its 00-24
   report lists CHANGELOG as a shipped surface). I did not add one for help-text.sh —
   convention for "smoke script only" additions is ambiguous (see §g3).
3. **Subcommand list is hardcoded** (21 entries incl. grouped forms) with a comment asking
   to keep it in sync with `tq -h`. A future subcommand that forgets to update the list
   shrinks coverage SILENTLY — the smoke still passes. First version tried to parse the
   subcommand list from `tq -h` and failed honestly (the usage block is prose, not a
   "Available Commands:" section); the fallback to hardcoding is deliberate but unguarded.

## c) NOT STARTED

1. **Extending the detector beyond `(...)` artifacts** — the same rename-regex class
   produced the NON-parenthesized `?query` literal leak (da8f331: `query.Get("q")` vs the
   emitters' `?q=`). That leak class is pinned by a webui Go test, not by this smoke; a
   generic "rename-diff literal-leak" sweeper remains unstarted (the 08:39 f3 sweep was a
   one-time manual pass).
2. **CI (GitHub Actions) wiring** — the smoke now runs in ci-local only. Per f32 precedent
   (advisory lint loop mirrored into ci.yml), the CI side is unstarted; master CI does not
   yet exercise this gate.
3. **Postgres/backend variant** — N/A here (help text is binary-level), listed only to say
   it was considered and correctly skipped. No scope creep.

## d) TOTALLY FUCKED UP

Nothing is red, but three process fumbles are on the record:

1. **Daemon footer race (recurring, now n=4 this task series).** The smoke script, its
   ci-local wiring, and the AGENTS.md row landed in unfootered daemon commit eaadb1a
   ("chore: auto-commit 3 changed file(s) (heuristic)") minutes before my footered commit
   67905f9. The amend maneuver was rejected this time (eaadb1a had not shipped but the
   window had passed by the time I noticed); queue↔git cross-reference therefore points
   only at the TODO-checkoff commit. Same shape as the 00-24 report's scramble.
2. **First smoke iteration failed on a wrong output assumption** — I guessed `tq -h` had an
   "Available Commands:" block (cobra-style); it is a hand-written usage block, so the awk
   parse found nothing and the smoke died on first run. Caught immediately, but the read-
   the-actual-output-first step should have preceded the write.
3. **Two mechanical slips in the write path** — a `d#` typo injected by the first multiedit
   batch (caught by the very next run) and one `bash -n`-catchable syntax risk taken before
   verification rather than after. Both zero-cost, both luck-adjacent.
4. **Recurring-ritual miss (standing): `git stash list`** was never checked at session
   start despite it being in the AGENTS.md session-start ritual and being flagged as a
   recurring miss in the 00-16 and 00-30 reports. Third strike.

## e) WHAT WE SHOULD IMPROVE

1. **Smoke scripts that gate on pattern-matching need a self-test.** Every regex gate in
   this repo is one careless regex edit away from passing everything (the pipe-masking and
   Files: 0 lessons). release-gates.sh and journal-drift.sh already model the fix; help-text
   should get the same `--self-test` / negative fixture treatment (now §f1).
2. **Machine-readable subcommand inventory.** Three consumers want the same list (help-text
   smoke, docs, release checks). `main.go`'s dispatch is the source of truth; a tiny
   `tq __commands` hidden output or a go/parser extraction in scripts/ would kill the
   hardcoded-list rot class (§f2).
3. **Write the smoke's expectations from OBSERVED output, not remembered frameworks.** The
   first-run failure came from importing a cobra mental model into a hand-rolled CLI.
4. **Commit-per-file-set hygiene under the daemon.** With the daemon live, the only way the
   footer lands on the smoke itself is committing within seconds of creation. Either
   `SMOKE_KEEP`-style staging in /tmp first, or accept the daemon split and note it (done
   here).
5. **`check-status-index.sh` worked as designed** — index row added BEFORE the daemon could
   fold the report in unindexed. The amend-maneuver hole was avoided, not survived. Keep
   doing this proactively, not reactively.

## f) Up to 50 things we should get done next

Ranked by impact for THIS task's blast radius first, then standing backlogs observed in
passing (items 15+ are ROADMAP fuel — route through docs-health HARVEST, don't mass-adopt):

**Direct tail of this task (do soon):**
1. Add a pinned negative fixture / `--self-test` to help-text.sh (seeded fake tq whose help
   carries `task(store)`; smoke must FAIL against it) — closes §b1.
2. Derive the subcommand list machine-readably (hidden `tq __commands` or go/parser over
   main.go) so list drift FAILS instead of silently shrinking coverage — closes §b3.
3. Mirror the help-text smoke into ci.yml (f32 parity precedent) — closes §c2.
4. Decide + record the CHANGELOG convention for smoke/gate-only additions (owner ruling,
   §g3) and backfill the help-text row if the ruling says yes.
5. Generalize the detector to the `?query`-style non-parenthesized literal-leak class
   (space-separated identifier swaps in flag/param strings) — §c1.
6. Session-start ritual compliance: put `git stash list` + full-repo `git log --oneline -5`
   into a checklist the harness enforces (three consecutive reports missed it).
7. Daemon-attribution gap: consider a tiny wrapper that lets agent commits claim the
   Task-Queue-ID before the daemon wins the race (or an owner-sanctioned amend window) —
   standing §g question from the 00-24 report, still open.
8. Add the smoke's "23 helps" count assertion internally (fail if the parsed/known list
   count drops) — cheap rotation tripwire alongside §f2.

**Standing TODO_LIST neighbors observed in passing (verified present this session):**
9. Extend `scripts/lint-annotations.sh` to the sub-module loop or decide root-only
   deliberately (per-module `--new-from-rev` baseline strategy).
10. Extract the disk-derived module loop into `scripts/for-each-module.sh`; consume from
    ci.yml (4+ copies) and ci-local.sh.
11. Regression-test the unit-env fix once `Environment=GOEXPERIMENT=jsonv2` lands on the
    NixOS tq-agent-pool module (owner action; then delete the env-lie caveat).
12. Owner fix + verification for the pool-verify GOEXPERIMENT gap (AGENTS.md says owner-run;
    nothing verifies the fix landed).
13. Write the client/server-mode per-repo experiment from the 2026-09-14 research doc, or
    formally park it.
14. templ-components consumer sweep: wave 2 scheduling against the Z.ai 429 runbook.

**Broader, from AGENTS/TODO knowledge (ROADMAP fuel, unverified this session):**
15. Postgres backend parity pass for any store surfaces added since its last conformance run
    (session-close bridge is enqueue-only today; ADR notes it as open).
16. Pin the canonical golangci-lint binary/version for gates (standing §g1 from 00-27).
17. Re-dispatch spend policy: rule for DONE-on-arrival tasks (three this week cost a full
    turn each for a re-verification).
18. Batch-scorer (`--prioritize`) live-pool trial with budget gates, then a verdict on
    default-ON vs stay-OFF.
19. DLQ autopsy loop: review recent second-death cases for prompt-quality signals.
20. Review-window false-negative audit: how many request_changes verdicts were the
    000001a092aa-class footer-quoting confusion vs real defects.
21. `tq doctor` should surface watermarks lag > N as a warning line (operator ergonomics).
22. Sweep for other hardcoded lists mirroring dispatch tables (flags, subcommands, types)
    that can rot silently like help-text's list.
23. `check-todo-list.sh`: reject future `— done <date>:` suffixes older than N days to keep
    [x] rows from accreting forever (archive convention instead).
24. Consider `set -o pipefail` audit across scripts/*.sh (pipe-masking lesson class).
25. Smoke-orchestration: shared lib for the build-tq/TQ_BIN/scratch-TQ_DB preamble repeated
    in every smoke (5+ copies).
26. Nightly fuzz: confirm the seed corpus commit loop is still landing (unverified since
    2026-09-12 env fix).
27. vendorHash runner-only drift: document the differential-dump playbook as a script, not
    a report paragraph.
28. Add `tq version` build-info parity check between nix build and devmod shim build.
29. `tq top` (untested this session): does it expose help artifacts too? Covered by the
    smoke now — verify once and close the question.
30. Facade parity gate: confirm check-facade-parity covers the cqrs adapter's recent
    additions (ADR-0014 surface is frozen by policy; drift check is advisory).
31. Windows cross-compile gate (`CMD_TQ_OS=windows`) in ci-local — currently manual.
32. Pre-commit commit-msg hook (exactly-one-footer) doesn't bind the daemon — document the
    residual hole next to the index-gate hole in AGENTS.md.
33. Status-report HTML-vs-md divergence: the skill default is HTML, every dispatch asks for
    .md — resolve the spec once (owner ruling) instead of per-report overrides.
34. Consider gating `bash -n` for scripts/*.sh in ci-local (one-line, catches class d3).
35. A tiny `scripts/smoke/self-test-all.sh` that runs every smoke's negative mode where one
    exists (release-gates, journal-drift, help-text-after-§f1).
36. Update FEATURES.md if smoke inventory is listed there (unverified; check).
37. ADR or AGENTS note: "help text is a tested surface" so future CLI rewrites keep the
    smoke in scope.
38. Verify the smoke behaves under `TQ_BIN=result/bin/tq` (nix-built binary path) once.
39. Timeout wrapper for the smoke loop itself (23 process spawns; a hung spawn blocks
    ci-local — currently no `timeout` guard).
40. Locale/encoding: confirm the artifact regex is stable under different grep locales
    (runner parity nit).
41. WebUI: no action (the `?q=` regression test owns that surface) — recorded to prevent
    duplicate sweeps.
42. Close-out contract: the batch of "recurring ritual miss" notes across reports suggests
    a shared harness pre-flight rather than per-agent discipline.
43. Document help-text.sh in README/docs if smoke inventory is user-facing (check).
44. `docs/status/README.md` index: reports now arrive ~3/hour; consider an auto-indexer or
    lower the manual-row ceremony for n=2 re-dispatch pairs.
45. Re-run facade gates after the next tag cut so `go install` consumers see the smoke-era
    tree (release checklist item).
46. Audit remaining `grep -w` usages in scripts/ for the substring-matching class.
47. Confirm `SMOKE_KEEP=1` semantics are uniform across smokes (bootstrap-install has it;
    help-text doesn't need it — fine, but document the divergence).
48. Owner question queue: fold §g items into the next owner sync.
49. Consider extracting the artifact-regex into a shared scripts/lib so §f5's generalization
    has one home.
50. After three consecutive dispatches on this task family, propose retiring the
    "close-out per dispatch" cadence for DONE-on-arrival confirmations (spend policy, ties
    to §f17).

## g) Questions I can NOT figure out myself

1. **Should the smoke self-derive the subcommand inventory from `main.go`'s dispatch table
   (new hidden `tq __commands` / go/parser extraction), or is the hardcoded list + manual
   sync comment the accepted pattern?** Self-derivation adds a surface to cmd/tq (its own
   module, ADR-0017 replace-free constraints apply); hardcoding accepts silent shrink.
   Owner call on the tradeoff.
2. **CHANGELOG convention for smoke/gate-only additions:** journal-drift.sh got a row;
   I did not add one for help-text.sh. Which is the ruling — every new ci-local gate gets a
   CHANGELOG row, or only user/backend-visible changes?
3. **Is there a sanctioned way to get the Task-Queue-ID onto work that the auto-commit
   daemon sweeps first** (a grace window for amends, a daemon footer-injection hook, or an
   accepted convention that only the checkoff commit carries the footer)? Four consecutive
   reports have hit this race; the queue↔git cross-reference systematically under-attributes
   deliverable commits.

---
Verification chain for this report: smoke PASS/FAIL pair run this session (clean build vs
seeded-fake binary); root `go build/vet/test -race` green; `scripts/test-cmd-tq.sh` green;
`bash -n` both scripts; commits 67905f9 (footered) + eaadb1a (daemon) on master, unpushed.
