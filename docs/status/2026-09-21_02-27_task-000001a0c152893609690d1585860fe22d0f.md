# StatusResult session usage — §f4 close-out (task 000001a0c152893609690d1585860fe22d0f)

Window: 2026-09-21 ~02:00–02:30 CEST. Work item: TODO_LIST row "StatusResult
session usage: same usage fields + derivation for status/report runs (09-52
report §f4; internal/executor status)".

## a) FULLY DONE

- **Usage fields on StatusResult** (internal/executor/status.go:78-90): the
  four derived fields (`SessionCostUSD`, `SessionPromptTokens`,
  `SessionCompletionTokens`, `SessionMessageCount`) with the shared json keys
  (`session_cost_usd`, `session_prompt_tokens`, `session_completion_tokens`,
  `session_message_count`, all `omitempty`), placed after `SessionID` exactly
  like ReviewResult.
- **Derivation wired** (internal/executor/status.go:190-197):
  `StatusExecutor.Execute` calls `deriveOutcome(ctx, repoDir,
  result.SessionID, t.ID)` after `ExtractSessionID` and copies the four
  fields, mirroring review.go:215-219. Best-effort semantics inherited: a
  missing session id or unreadable crush data leaves zeros, never fails the
  task.
- **Budget projection joins status runs with zero budget-code change**: the
  key-based parse (`internal/budget/budget.go:126-160`) reads any completed
  fact's detail, so report runs are counted as soon as the keys exist. Only
  the doc comments (budget.go:93-98 SessionUsage, :114-118 sessionUsageDetail)
  and the drift pin changed.
- **Drift pin extended** (internal/budget/budget_test.go:169-175): 
  `TestUsageTodaySumsDerivedSessionUsage` now marshals a REAL StatusResult
  alongside AgentResult/PrioritizeResult/ReviewResult — a json key rename in
  any of the four types fails the projection suite (both directions).
- **Docs/memory updated**: AGENTS.md budget table row now lists StatusResult
  among the key-sharing types; the `status` payload-contract bullet records
  the §f4 derivation and the deliberate choice to surface usage only (the
  reporter commits its report, but StatusResult keeps no commit fields — the
  item asked for usage; the report path is already on the result).
- **TODO_LIST row checked off** with gate citations, per the sibling §f3 row's
  convention.

Deliberately NOT done (scope): `tq show`/webui rendering of status usage —
the 09-52 §f5 rendering row already covers the prioritize sibling and stays
the separate backlog item; StatusResult has no unkeyed-literal construction
anywhere (checked cmd/tq + webui callers), so the additive fields are
invisible to existing consumers.

## b) PARTIALLY DONE

- Nothing in the item's scope.

## c) NOT STARTED

- Nothing in the item's scope.

## d) TOTALLY FUCKED UP

- **Lost the daemon race twice in one window** (the exact 09-52 §d lesson,
  third+fourth hits): the three-file code change landed as unattributed
  daemon commit 5446428 and the AGENTS.md/TODO_LIST docs change as 1402151
  before my footer commit existed. I knew the rule ("commit the code change
  explicitly IMMEDIATELY after the edit") and still ran gates between edit
  and commit because the gates were already mid-flight. Queue↔git
  attribution resolves via THIS report's footer commit; the split is
  recorded here rather than rebased under the live daemon (09-52 precedent).
- **Multiedit cross-file mistake**: the two budget.go doc-comment edits were
  bundled into the budget_test.go multiedit and failed silently-ish (2 of 4
  applied); caught by re-grep two calls later and reapplied to the right
  file. Read the file list before batching.
- **gopls phantom "unknown field" errors persisted the whole window** against
  a real green build (the known stale-LSP class); I re-verified with the CLI
  instead of chasing the LSP — correct call, but the vendor refresh + a
  gopls restart would have cleared it.

## e) WHAT WE SHOULD IMPROVE

- The add+commit-immediately rule needs to be TWO rules: (1) commit the code
  the moment the in-module gate (build+vet, seconds) passes — do not wait for
  the full race suite; (2) docs edits get their own immediate chain. Every
  gap between "last write" and "git commit" is daemon food.
- Consider a `deriveSessionUsage` variant of deriveOutcome for the
  commit-less result types (review, status): deriveOutcome also runs the
  Task-Queue-ID trailer scan whose result these executors drop — a wasted
  `git log` per review/status run. Tiny cost, but it grows with every
  usage-only executor.

## f) Up to 50 things we should get done next

1. Pinning test: PrioritizeResult usage json keys must equal AgentResult's
   (TODO row already open, 09-52 f2 — now four types should join the table).
2. `tq show` detail page: render session tokens/cost for status + prioritize
   tasks the way agent tasks already show theirs (09-52 f5, extended).
3. The deriveSessionUsage extraction from §e2 if the owner wants it.
4. Webui metrics row: completion-cost sparkline now that FOUR fact types
   carry usage (09-52 f6).
5. Post-pilot calibration (16-28 §f6) — unchanged by this window.

## g) Up to 3 QUESTIONS

1. Footer/content split policy: is the follow-up-footer-commit pattern (this
   report) the accepted standing remedy for daemon sweeps, or should windows
   fold the unpushed chore commits into the footer commit while the daemon
   is paused? (09-52 and this window both chose the split.)
2. Should `deriveOutcome` grow a usage-only variant for review/status, or is
   the wasted trailer scan accepted as the price of one shared helper?
3. The status reporter commits its report with (presumably) a
   `Task-Queue-ID` footer — should StatusResult surface those commit SHAs
   like AgentResult does, or is the report path the only attribution the
   queue needs?

## Gates this window (all rc=0, GOEXPERIMENT=jsonv2)

- internal/executor module: `GOWORK=off` build + vet + `go test ./...
  -count=1` (ok 29.5s)
- internal/budget: root `go test ./internal/budget/ -count=1 -race` (ok
  1.0s, after `go mod vendor` — the stale-vendor poison hit as expected)
- Root: build + vet + `go test ./... -count=1 -race -timeout 300s`
- gofmt -l clean on the three touched Go files; facade parity OK (7/7);
  all StatusResult construction sites verified keyed-literal (cmd/tq:349,
  webui_test.go:583)
