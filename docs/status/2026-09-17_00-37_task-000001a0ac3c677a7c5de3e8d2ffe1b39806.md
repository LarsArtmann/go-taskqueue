# Verify-only close-out — six-task CI-resilience window done-prompt (second dispatch)

- **Task ID**: `000001a0ac3c677a7c5de3e8d2ffe1b39806`
- **Date**: 2026-09-17 00:37 CEST
- **Dispatch**: second for this task ID. The full done-prompt was delivered 12 minutes earlier in the same session stream: `docs/status/2026-09-17_00-25_done-prompt-six-task-hardening-window.md` (committed e7c949b, footer carries this exact Task-Queue-ID), with the §f harvest appended to TODO_LIST.md in the same commit and the index row at docs/status/README.md:42.

## What this dispatch did

Verify → cite → stop, per the repo's repeat-dispatch ritual. Zero edits beyond this report and its index row. Spot-checks at HEAD, all green:

1. `with_transient_retry` present in `scripts/ci-local.sh:32` with the ci-local self-test step wired (line 55); `scripts/check-transient-retry.sh` exists.
2. `.github/workflows/ci.yml:13-15` carries the concurrency group with `cancel-in-progress: true`.
3. TODO_LIST.md holds 142 unchecked items including this window's harvest section (28 rows, 2 BLOCKED owner rulings), appended by e7c949b.
4. Index row for the 00-25 report present at `docs/status/README.md:42`.
5. `git status` clean at window start; branch `master` ahead of origin by 9 (delivery not yet pushed — never pushed by this task either).
6. Band drift re-check unnecessary: the 00-25 report scanned the journal for the window's timespan and found zero `task.reprioritized` facts (0 of 3,938); no new window has elapsed.

## Why the re-dispatch is wasted spend

The same loop documented in the 00-25 report §c1/§f1: no mint-time done-check exists, so a task whose ID already has a terminal fact and an indexed close-out is re-minted and paid for a full lap to re-verify a DONE row. This is now observed across at least three distinct task IDs (status-append dedup/cap ×4, this done-prompt ×2). The unshipped half of the fix remains queue-side: refuse or downgrade at enqueue/harvest when the task ID already carries a terminal fact or indexed close-out.

## Nothing broken

No regressions, no gate changes, no code touched by this dispatch. The prior delivery's claims (six footer commits 3e0f941/ac54a38/4ba81d8/025cd6b/2d0997e/d4125d1) were verified in that report and the spot-checks above re-confirm the durable artifacts.
