# Status Report — Review-Fix task-000001a0893f0251882bc5b9b65979337826 (README fullcore mention)

**When:** 2026-09-10 05:58 CEST · **Scope:** this task's work only (review-fix of the
f25 full-core example), plus what was noticed in passing — no unrelated research.
**Verdict in one line:** the reviewer's single low finding is fixed, all gates green,
commit `b31f7fd` carries the queue footer — but the review pipeline itself leaked in
three places (stale line anchors, a dangling review target, and a footer/daemon collision).

**Context:** the f25 TODO item (full-core embed example `examples/fullcore`, sqlite/postgres
backend choice) was built in a prior session; the reviewer rejected that work with exactly
one finding: the README examples paragraph enumerated only `examples/api` and `examples/sse`
under "`examples/` stays experimental by design". This session fixed exactly that finding —
no unrelated changes.

---

## a) FULLY DONE

| Item | Evidence |
| --- | --- |
| Reviewer finding fixed: one-line `examples/fullcore` mention added to the README examples paragraph | `README.md:391-396` — "`examples/fullcore` is the full library embed demo in one file: queue + executors + worker pool, with the sqlite/postgres backend picked at run time." Wording verified against the example's own doc comment (`examples/fullcore/main.go`: `--backend` flag, both stores satisfy `queue.Store`, pool drains, status counts printed) |
| Gates green on the exact committed tree | `./scripts/ci-local.sh`: "ALL CI GATES GREEN — this exact tree is what CI will see. Safe to push." (not pushed, per contract) |
| Commit with the required queue footer, clean tree | `b31f7fd` "docs: mention examples/fullcore in README examples paragraph", `Task-Queue-ID: 000001a0893f0251882bc5b9b65979337826`; `git status` clean; 3 insertions / 1 deletion, README.md only |
| TODO_LIST loop already closed (no change needed) | `TODO_LIST.md:64` — the f25 item is `[x]` with a full DONE note from the original work session; the rejection did not reopen it |
| Review target verified to exist on master | `examples/fullcore/main.go` present; the finding was pure docs drift, not a missing example |

## b) PARTIALLY DONE

- **README fullcore documentation surface.** The *finding* (one-line mention) is fully done.
  Anything beyond it — usage snippet, FEATURES.md cross-reference, a dedicated examples
  section — was out of scope and is deliberately not done ("smallest correct change wins").
  Listed here so the gap is visible, not as unfinished work.
- **Review/queue cross-referencing.** The fix commit carries the fix-ticket footer
  (`…37826`); its relationship to the original work commit's footer (`…170652`,
  commit `2d3340a`) is implicit. Whether the queue links both is a queue-side mechanic
  I could not verify from the repo (question 1 below).
- **Security-caveat scoping in that paragraph.** The "binds loopback only / has no
  authentication" warning sits between the PoC sentence and the new fullcore sentence.
  A fast reader could attach it to fullcore, which is not an HTTP server at all.
  The finding is satisfied; the wording is acceptable but not airtight.

## c) NOT STARTED (observed, out of scope, listed for honesty)

- **Runtime execution of the example in this session.** `go run ./examples/fullcore`
  (sqlite) and the postgres path were never run by *this* session — CI built and tested
  the modules, and the original session's verification stands, but I did not repeat it.
- **CHANGELOG entry for the README fix.** Docs-only, low; CHANGELOG is append-only and I
  did not want to guess whether doc fixes belong there. Not added.
- **Everything else.** Per contract, exactly one finding was in scope; nothing else touched.

## d) TOTALLY FUCKED UP (process, not code — the shipped diff is 3+/1- in README.md)

1. **Review target drift — the rejection cites a commit master does not contain.**
   `c4e8b9f` ("examples: add fullcore embed demo…") is NOT an ancestor of HEAD
   (verified with `git merge-base --is-ancestor`); it is a dangling object. An
   identical-message twin, `2d3340a`, IS on master (it rode the auto-commit flow, e.g.
   `fd3156d` touched `examples/fullcore/`). So the review was filed against an
   unreachable ref while the same work exists on master via different shas. Review ↔
   master auditability is broken; nobody can diff "the reviewed commit" against "what
   shipped" without archaeology.
2. **Stale line anchors.** The finding cites "README.md lines 356-358"; the actual text
   lived at lines 391-395 (the 05:29 README overhaul shifted everything). Cost one
   search round-trip; harmless here, fatal when anchors point at the wrong text.
3. **Footer-vs-daemon collision.** The auto-commit daemon committed my README edit
   within seconds with a heuristic message and NO `Task-Queue-ID` footer — while the
   queue contract demands the footer on the work commit. I had to `--amend` a daemon
   commit. That races the daemon and rewrites a commit hash the daemon already "used";
   it was safe only because the daemon never pushes. The footer requirement and the
   daemon's behavior are structurally incompatible as-is (question 2 below).

No code, build, or test in this task's scope is fucked up: CI fully green, tree clean.

## e) WHAT WE SHOULD IMPROVE

1. **Review findings must anchor to content, not line numbers.** Cite the commit sha
   AND the quoted text; line numbers on this repo rot within hours (concurrent agents).
2. **Resolve the footer/daemon conflict.** Either the daemon learns to append the active
   task's footer (queue-side state), or there is a documented, sanctioned amend window
   for agent commits the daemon swallowed.
3. **Define the lifecycle of rejected commits.** Dangling refs like `c4e8b9f` leave twin
   commits in the object store; require reviewers to re-file against a reachable ref, or
   annotate rejected refs so they stay findable.
4. **Update the README examples paragraph in the SAME commit as the example.** This whole
   fix exists because the example-landing commit missed the docs mention — make it a
   checklist item for example work.
5. **Scope the security caveat explicitly.** Name the two PoC HTTP servers (`api`, `sse`)
   as the loopback/no-auth subjects so non-HTTP examples (fullcore) can't be misread.
6. **Queue-side sanity gate for findings:** reject (or flag) findings whose cited
   line range cannot be re-anchored to the quoted content at filing time.

## f) Up to 50 things we should get done next

> Brainstorm, not commitment: extra items beyond the top ~10 are ROADMAP fuel and need
> `docs-health` HARVEST routing rigor. Grounded in this session's observations only.

**Docs / README (near-term, small):**
1. Reword the examples paragraph so the loopback/no-auth caveat explicitly names
   `examples/api` + `examples/sse` (decouple from fullcore). *(also: e4)*
2. Lift the one-line usage (`go run ./examples/fullcore`, postgres variant) from the
   example's doc comment into the README mention or a tiny examples list.
3. Add `examples/fullcore` to FEATURES.md under DONE (honest inventory parity).
4. Decide CHANGELOG policy for docs-only fixes; if in scope, backfill an entry for
   `b31f7fd` + the example itself.
5. README code fence (~lines 341-347): the `import "…/postgres"` line sits between two
   `store, err :=` constructions; split into two labeled snippets for clarity.
6. Add "README examples paragraph" to the example-landing checklist (docs-health owns
   the checklist; this fix is the cleanup for the miss).
7. Sweep README for other directory enumerations that can drift (`rg 'examples/'`
   and similar lists) and anchor each to content, not position.
8. Verify `check-status-index.sh` runs in ci-local (it runs via pre-commit today);
   if not in the push gate, wire it there.
9. Sanity-check the README "Development" section (line ~369+) against current ci-local
   behavior — the 05:29 overhaul rewrote large parts and I did not read it this session.
10. Protect the fullcore mention from the next README overhaul: add it to whatever
    checklist the 05:29 overhaul report left behind.
11. Consider a one-sentence graduation rule: what moves an example from "experimental
    by design" to a cited embed proof (fullcore is closer to a library contract demo
    than a PoC).
12. Link the fullcore mention from the "internal/ packages… CLI is the public surface"
    paragraph (~line 353) — it is the counter-example proving the embed story.
13. docs/status index hygiene: verify no DATE mismatches after today's insert (checker
    enforces), and consider archiving fully-resolved 2026-09-06 reports per convention.
14. TODO_LIST.md:64 DONE note is one very long line — conventions allow it, but consider
    trimming to the pointer form ("DONE 2026-09-10: examples/fullcore") in the next
    docs-health pass.

**Queue / review process (owner decisions):**
15. Give the auto-commit daemon footer-carrying behavior from queue state, or document
    the sanctioned amend procedure for swallowed commits. *(e2)*
16. Require review findings to carry: reachable commit sha + quoted anchor text +
    post-fix verification instruction. *(e1)*
17. Decide the disposition rule for rejected/dangling commits (`c4e8b9f`). *(e3)*
18. Add a queue-side pre-flight that re-anchors cited line ranges to content before a
    finding is filed. *(e6)*
19. Convention question turned task: allow/dual-footer review-fix commits so both the
    original ticket and the fix ticket are cross-referenced (see question 1).
20. Consider making review rejections visible as facts (e.g., surfaced via `tq facts`)
    so rejection loops are auditable from the journal, ROADMAP fuel.
21. Verify the queue picked up `b31f7fd` as the close of ticket `…37826` (operator
    visibility; footer is the only link).
22. Retire-or-annotate the dangling `c4e8b9f` object explicitly (note or rebase marker)
    so future sessions don't re-derive what this session had to.

**fullcore example follow-ups (grounded in its doc comment / AGENTS.md):**
23. Run `go run ./examples/fullcore` end-to-end once on the sqlite path as a post-merge
    smoke (this session did not execute it).
24. Same once for `--backend postgres` against a throwaway local DB (the example applies
    the schema; the doc comment warns to keep it off production).
25. Consider a hermetic `scripts/smoke/fullcore.sh` (sqlite path, CI-safe per repo
    conventions) — with an explicit `TQ_DB=<scratch>` export per the production-DB trap.
26. Add a production-DSN guard/refusal in the example (doc comment says "keep pointed at
    a local/test database" — a heuristic check would make the warning enforceable).
27. Consider exercising the DLQ once inside the example (one deliberately failing `sh`
    task) so the dead-letter story is proven in the same file — only if it doesn't bloat.
28. Verify the example's pool exit semantics match the worker drain contract
    (`--once` drains-then-exits parity).
29. Document the postgres prerequisite (create DB) next to the DSN flag.
30. Decide whether examples deserve smoke coverage at all (fullcore would be the first
    example-level smoke); if yes, add the pattern to AGENTS.md smokes list.

**Repo hygiene / standing guards noticed in passing:**
31. Annotate the 04:09 report's "master CI red since 01:15" claim — ci-local is fully
    green as of 05:58 today; point-in-time reports must be brought current (docs-health
    ANNOTATE, non-destructive).
32. Verify the last fuzz nightly committed its seeds and no crasher landed (routine;
    AGENTS.md records the campaign, state not checked this session).
33. AGENTS.md "Kernel 7.2.3" note is an unusual version string (mainline is 6.x) —
    verify the source of that version claim; fix the note if it's a typo.
34. Confirm the retired dogfood journal `tasks.db` at repo root stays gitignored and
   trap-free (AGENTS.md records the 2026-09-10 TQ_DB incident; no re-check done here).
35. vendorHash discipline: the original commit updated the flake vendorHash for the
    postgres require; next dependency change must repeat the fakeHash dance (standing).
36. GOEXPERIMENT=jsonv2 guard in flake.nix — standing reminder; untouched this session.
37. templ LSP false-positive guard — standing reminder for any webui work.
38. golangci-lint advisory baseline — README-only change had no lint surface; keep
    baseline discipline for future code touches.
39. Dead-export audits must use substring matching, not `rg -w` — standing lesson,
    keep it in future audits.
40. Session-start ritual (`git log --oneline -5 -- internal`) — followed this session;
    keep it mandatory in every session.

**Meta / loop-closing:**
41. HARVEST this section: route items 1-14 → TODO_LIST/docs, 15-22 → owner queue
    decisions, 23-30 → TODO_LIST (example work), 31-40 → annotate/verify or discard.
42. Mark the README fix's review thread resolved with `b31f7fd` (queue-side action).
43. Confirm the pre-commit hook indexed this report (check-status-index) — done in
    this session; keep the gate.
44. If a next README overhaul lands, re-run the finding's verification: `rg fullcore
    README.md` must still hit the examples paragraph.
45. Re-verify `go vet`/tests claim for the root module's postgres require on the next
    dependency touch (ci-local covered it today).
46. Consider a "review-fix" task template for the reviewer side (sha + anchor text),
    distinct from item 16's queue-side gate.
47. Track whether amend-after-daemon-commit ever collides in practice (log it if the
    daemon re-commits mid-amend); feed the result into item 15.
48. Keep the README edit's manual formatting (80-col wrap, no dprint) per the 2026-09-08
    docs-formatting decision — verified this session.
49. When fullcore graduates from "experimental", move its README sentence out of the
    PoC paragraph into the embed-story section (dependency: item 11).
50. Close the loop: this report is the HARVEST input — run docs-health HARVEST before
    the next fullcore/README work session.

## g) Questions I can NOT figure out myself

1. **Dual-footer convention:** a review-fix commit belongs to both the original task
   (`Task-Queue-ID: …170652`) and the fix ticket (`…37826`). Should such commits carry
   BOTH footer lines, or is the fix ticket's footer alone correct? I could not inspect
   the queue's linking mechanics from the repo.
2. **Footer vs auto-commit daemon:** should the daemon learn to append the active task's
   footer (it committed my work without one, forcing an amend of a daemon commit), or is
   amend-after-daemon the sanctioned flow? This is a daemon-capability/owner decision.
3. **Rejected-commit lifecycle:** `c4e8b9f` (the review's cited commit) is unreachable
   from any branch while its identical-message twin `2d3340a` is on master. Should
   reviewers re-file against reachable refs, or should rejected refs be annotated/
   preserved somewhere findable? Without a rule, every review of this repo risks
   auditing the wrong sha.

---

*Point-in-time snapshot — 2026-09-10 05:58 CEST. Written as `.md` with the user-specified
filename (explicit instruction overrides the skill's HTML default for this report).*
