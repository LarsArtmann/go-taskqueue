# Status Report — task 000001a08e51d73180af463e2b7a31e8662a

**2026-09-11 04:43 · `tq doctor --repos`: bare-name cwd-independence (the 02:00 f8 residue)**

Scope discipline: this report covers ONLY this task's window. In-passing
observations are labeled as such; nothing unrelated was researched.

---

## a) FULLY DONE

1. **Work item implemented end-to-end.** `doctorEnvironment` no longer feeds
   raw `splitRepos` output to the autonomy checks: `--repos` entries now run
   through `expandRepoSpecs`, so absolute paths and existing cwd-relative
   paths pass through while bare names join the projects dir — doctor is no
   longer the last raw `splitRepos` consumer (cmd/tq/doctor.go:357).
2. **`--projects-dir` flag added to `tq doctor`** (default `$TQ_PROJECTS_DIR`
   or `~/projects`), mirroring harvest/audit so the resolution root is
   explicit and overridable; `doctorOptions.ProjectsDir` carries it, flag
   help texts updated on both `--repos` and `--projects-dir`.
3. **Contract test added** in cmd/tq/repospec_test.go:
   `TestCmdDoctorResolvesBareRepoNamesAgainstProjectsDir` runs `cmdDoctor`
   from a foreign cwd against a bare name and asserts the healthy repo's
   `TODO_LIST.md present` / `.crushrc present` ok lines (pre-fix this warned
   `no TODO_LIST.md`). Hermetic: agent-binary check points at the test
   binary (nix sandbox has no crush).
4. **`expandRepoSpecs` doc comment updated** in cmd/tq/main.go to list doctor
   as a direct caller and note doctor stats the joined path directly.
5. **Verification green**: gofmt clean, `go build ./...`, `go vet ./cmd/...`,
   targeted tests 6/6 PASS, full root-module `go test ./... -race` PASS,
   plus a built-binary E2E (`ok repo:drifty TODO_LIST.md present` from a
   scratch cwd; `WARN` still fires for nowhere-resolving names — correct).
6. **TODO_LIST.md closed**: item marked `[x]` with a DONE 2026-09-11 close-out
   naming the flag, the routing, and the pinning test.
7. **Committed with the queue footer** (2fa97ea, last line
   `Task-Queue-ID: 000001a08e51d73180af463e2b7a31e8662a`). Not pushed.
8. **Session-start ritual honored**: repo-wide `git log`/`status`/`stash`
   before editing; scratch-DB hygiene (every `tq` invocation in the E2E
   carried an explicit scratch `--db`/`TQ_DB` — production journal untouched).

## b) PARTIALLY DONE

1. **The footer commit's provenance required two amends.** The auto-commit
   daemon captured the three Go files as a generic `chore: auto-commit`
   (227e2d8). I amended per the 04-05 recovery playbook, but the first
   amended message put the Crush attribution AFTER the Task-Queue-ID footer,
   violating the contract's "ending in this exact footer line" — fixed by a
   second amend (2fa97ea). Content was never wrong; the message was.
2. **Verification depth**: the documented standard gate (root module only,
   GOEXPERIMENT=jsonv2) was run, but NOT the heavier gates — no `nix build`,
   no `./scripts/ci-local.sh`. Justified (untouched sub-modules, docs-only
   release surfaces), but it means the nix/vendorHash and multi-module gates
   have not seen this diff.
3. **CHANGELOG.md not updated.** The `--projects-dir` flag is a small
   user-facing surface addition. Precedent check shows the prior sweep's
   footer commit (f2090e1) also didn't touch CHANGELOG per-task, so this
   matches repo practice — flagging it as a conscious call, not an oversight
   silently buried.

## c) NOT STARTED

1. **Nothing from the task itself.** All contract steps (read AGENTS.md →
   implement → test → close TODO loop → commit with footer → report) done.
2. Sister TODO items in the same section left for their own tasks (not this
   window's scope): module-eval PATH hardening (02:00 f11),
   `checkProjectsDir` absolute-`--repos` relaxation (02:00 f16), harvest
   skip-log change detection (02:00 f17), doctor PATH warning (02:00 f18),
   dead-pool PapDashboard alert (02:00 f6).

## d) TOTALLY FUCKED UP

Nothing. No broken master, no reverted work, no damaged concurrent state.
The two self-inflicted wobbles (botched first E2E invocation — forgot
`--projects-dir`, correctly fell back to ~/projects and produced a confusing
"old behavior" output; and the mis-ordered commit footer) were both caught
within the same window and corrected. The worst actual damage was one wasted
`go build` cycle and one redundant amend.

## e) WHAT WE SHOULD IMPROVE

1. **Draft the commit message BEFORE touching git.** Both amends were
   avoidable: had the full message (footer last) been written first, the
   daemon-race amend would have been one clean operation.
2. **Dry-run CLI sanity invocations against the flag list.** The first E2E
   built the right binary but omitted the new flag — a 5-second
   re-read of the invocation would have caught it.
3. **agent-pool's inline expansion diverges from `expandRepoSpecs`**
   (in-passing observation, cmd/tq/agentpool.go:344-348): it unconditionally
   joins every non-abs path to the projects dir, so an existing
   cwd-relative path like `./localrepo` does NOT pass through as it does
   under harvest/audit/doctor. Deliberate per the local comment? Unknown —
   worth a decision, not a drive-by change.
4. **gci flags doctor.go as "not properly formatted"** (advisory,
   pre-existing — imports untouched this window). The advisory baseline rule
   says leave it, but if doctor.go gets touched again the import grouping
   could be fixed in passing.
5. **expandRepoSpecs consumers now span three commands + two hand-rolled
   lookalikes** (bootstrap.resolveRepos, agent-pool inline). The comment
   claims "same policy" for the lookalikes; agent-pool demonstrably differs
   (point 3). One shared helper with one test table would retire the prose
   claim.
6. **Report-file `.md` vs skill's HTML default**: the user's explicit `.md`
   path was honored (this file); the status-report skill prefers styled
   HTML. Flagged per the skill's own override rule — not propagated anywhere.

## f) UP TO 50 THINGS WE SHOULD GET DONE NEXT

A brainstorm, not a commitment list — most items below line 10 are
ROADMAP fuel. Impact-sorted within tiers.

**High impact (this task's direct neighborhood)**
1. Decide + unify agent-pool's repo expansion with `expandRepoSpecs` (e.3);
   if the unconditional join is deliberate, pin it with a test and fix the
   `expandRepoSpecs` comment's "same policy" claim.
2. Same decision for `bootstrap.resolveRepos` (verify its policy really is
   identical; add a repospec_test.go row for it — it currently has none).
3. Add a doctor e2e/smoke asserting the DEFAULT projects-dir path
   ($TQ_PROJECTS_DIR) resolves, not just the explicit flag.
4. Route the report §f harvest through docs-health so items 20-50 land in
   TODO_LIST/ROADMAP with proper routing rigor.
5. Annotate (not rewrite) this report's predecessors if any of their claims
   went stale this window (none found so far — verify doctor.go:355 pointer
   in the 04-05 report now resolves to the FIXED line).

**Repo hygiene / gates (in-passing + adjacent)**
6. Consider a CHANGELOG convention decision: per-task entries for
   user-facing flags vs current release-time summarization.
7. `tq doctor --help` output review: three flags grew parentheticals this
   window; give the whole flag set one consistency pass.
8. Fix gci import-grouping in doctor.go next time it's touched (advisory).
9. Add `--projects-dir` mention to the doctor row in FEATURES.md if the
   flag inventory there lists per-command flags (verify; not checked).
10. Consider teaching `tq doctor` to WARN (not fail) when a `--repos` entry
    resolves nowhere at all, naming the resolution root it tried.

**The queued sibling items (unchanged, restated for one-stop reading)**
11. Module-eval hardening: assert tq-agent-pool Environment carries
    non-empty PATH (02:00 f11).
12. `checkProjectsDir` should not require the projects dir when every
    `--repos` entry is absolute (02:00 f16).
13. Harvest skip-log change detection: log a skip class only when its
    example changes (02:00 f17).
14. `tq doctor`: warn when crush/git/go are missing from PATH (02:00 f18).
15. Dead-pool detection: PapDashboard alert when scan-failed covers every
    repo for N ticks (02:00 f6).

**Deeper quality ideas surfaced while working here**
16. Table-test `doctorRepoAutonomy` itself (it's only covered via
    runDoctor paths today).
17. Doctor JSON output (`--json`): assert the new check lines serialize
    with the resolved path (consumers may string-match names).
18. Document the doctor `--repos`/`--projects-dir` pair in the README's
    doctor section if one exists (verify).
19. Consider `expandRepoSpecs` accepting a resolution-report return
    (which specs got joined vs passed) so doctor could show it in Detail.
20. A conformance note in docs/DOMAIN_LANGUAGE.md for "repo spec" (bare
    name / relative / absolute) now that three commands share the policy.
21. Smoke-test coverage: scripts/smoke/ has no doctor smoke; a
    doctor-against-scratch-db smoke would catch CLI-level regressions the
    unit tests can't (flag wiring).
22. Test the `repo:""` guard in doctorEnvironment (empty entries after
    split) — currently untested dead-looking code.
23. Windows: repospec tests carry the volume-letter dance; confirm the new
    doctor test passes on windows-latest CI (it should — same primitives —
    but it's unverified until the next run).
24. Fuzz-adjacent: `--repos` parsing accepts whitespace-heavy specs;
    confirm splitRepos trimming is covered (it is, main_test.go:72 — leave).
25. VendorHash risk: go.mod untouched this window, so no dance needed —
    record that explicitly to spare the next verifier the check.

**ROADMAP fuel (speculative, low priority)**
26-50. Carried candidates, not independently researched this window: doctor
severity exit-code policy; `tq top`/serve surfacing of doctor results;
per-repo doctor Detail paths; projects-dir autodiscovery warnings; doc
consolidation of the seven splitRepos/expand call-sites into one ADR
addendum; agent-pool `RepoIntervals` key normalization vs bare names;
bootstrap `--repos` + positional args expansion parity; doctor `--json`
schema versioning; status-index staleness lint; report-series naming
convention (task-ID vs topic names); harvest/audit/doctor flag-sharing
via a shared flagset builder; e2e test for prune-stale bare-name path
(carry-over from the 03-34 report); SSE heartbeat flake follow-up
(two-agent corroborated, still open from 04-05); TestA11yChrome CSS-gate
tradeoff decision (open from 04-05); TQ_RESULT outcome-field semantics
(open owner question from 04-18); numeric-findings-must-quote-evidence
convention (open from 04-18); refuted-finding reply path (open from
04-18). Items here are pointers to already-filed work, not new claims.

## g) QUESTIONS I CAN NOT FIGURE OUT MYSELF

1. **agent-pool policy intent**: is `agentpool.go:344-348`'s unconditional
   join of EVERY non-abs `--repos` path to the projects dir (no
   cwd-relative pass-through, unlike expandRepoSpecs) deliberate pool
   hardening (the systemd pool's cwd is untrustworthy) or pre-expandRepoSpecs
   residue that should now share the helper? The inline comment defends
   cwd-independence, not the missing pass-through.
2. **CHANGELOG convention**: should user-facing flag additions get per-task
   CHANGELOG entries, or is release-time summarization the (current,
   apparently practiced) rule? Two consecutive task windows skipped it.
3. **Default-root UX**: for `tq doctor --repos` with a bare name and no
   explicit `--projects-dir`, is falling back to `$TQ_PROJECTS_DIR`/
   `~/projects` silently the desired UX, or should doctor print the
   resolution root it used when a spec had to be joined (visibility vs
   noise)?

---

*Point-in-time snapshot; §f items 26-50 are unresearched pointers, not
findings. Written as Markdown per the dispatching task's explicit `.md`
instruction (status-report skill's HTML default overridden by the user).*
