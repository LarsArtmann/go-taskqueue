# Task 000001a08e71df95f81bb4c091cf4b28073b — close-out self-review (2026-09-11 05:20)

Work item: "checkProjectsDir should not require the projects dir when every
`--repos` entry is absolute (02:00 f16; cmd/tq/agentpool.go + test)".

## What I actually did

- Recognized on session start (git log + TODO_LIST.md:79 already `[x]`) that
  this is a THIRD stale re-dispatch: the work shipped in 9804683
  (`allReposAbsolute` in cmd/tq/seeds.go, guard bypass at
  cmd/tq/agentpool.go:234, table tests in cmd/tq/seeds_test.go).
- Re-verified green from scratch: `go build ./...`, `go vet ./...`, full
  `go test ./cmd/tq/ -count=1`, plus TestCheckProjectsDir/TestAllReposAbsolute
  specifically (all PASS, with GOEXPERIMENT=jsonv2 per AGENTS.md).
- Committed a re-dispatch close-out note
  (docs/status/2026-09-11_stale-redispatch_f16.md) and now this report.
  No code changes: correct — the item was done; inventing changes would be
  scope creep.

## a) FULLY DONE

- Verification that the shipped implementation matches the item's exact
  wording: bypass fires only when `--repos` is non-empty AND fully absolute
  (agentpool.go:234), including mixed-list negative cases in tests.
- Verify gates run fresh today, not trusted from prior reports.
- TODO_LIST.md loop closed (was already `[x]` — left as-is, correctly).
- Commits carry the Task-Queue-ID footer.
- Honest no-op instead of fake work on an already-done item.

## b) PARTIALLY DONE

- Nothing in the item itself. Two adjacent residuals noticed in passing:
  the same guard unconditionally runs in `tq worker` (main.go:463) and
  `tq audit` (audit.go:42) — `allReposAbsolute` bypass was added to the
  agent pool only. Whether worker/audit need it is unverified; not this
  item's scope.

## c) NOT STARTED

- Carry-parity audit of `allReposAbsolute` bypass into worker/audit paths
  (only if the same friction exists there — unchecked assumption, listed
  under f).

## d) TOTALLY FUCKED UP

- Nothing broke. Honest near-misses: I initially had to resist "doing" the
  item; the docs note commit used the heredoc pattern for the message body,
  which AGENTS.md warns can mangle through the shell handoff — it landed
  correctly this time (verified via git log).

## e) WHAT WE SHOULD IMPROVE

- **The re-dispatch loop itself**: this task has now been dispatched three
  times for work that shipped once. The queue's close-out path (this
  TQ_RESULT) does not visibly flip the TODO item back into the pool's
  "done" view fast enough, or harvest re-arms stale items. Each stale
  dispatch burns a full agent session.
- Self-review reports for no-op re-dispatches could be one file, not two
  (I wrote both a redispatch note and this report; they overlap).
- The close-out self-review a)-g) contract for pure-verification tasks
  should permit "no code delta; here is the verification evidence" as the
  primary artifact.

## f) Up to 50 next things (this task's vicinity only — no repo-wide sweep)

1. Decide whether `tq worker --repos` (main.go:463) needs the same
   allReposAbsolute bypass; verify friction exists first.
2. Same for `tq audit` (audit.go:42).
3. Add a test pinning the agent-pool bypass behavior end-to-end (options
   level: absolute repos + `--projects-dir /` must NOT refuse), if not
   already covered at options level — I only verified unit tests today.
4. Investigate why this item re-dispatched twice after close-out: is the
   harvest key colliding with the report filename?
5. Consider a queue-side marker (`— SHIPPED: <sha>`) convention for
   TODO items so prune-stale/harvest never re-arms shipped work.
6. Check that docs/status reports referenced by close-outs are indexed
   (check-status-index.sh) — the 05:20 report is not yet indexed at
   write time.
7. Re-run the full multi-module suite (nix run .#test) once before the
   next release cut; today's green was root-module only (smallest-change
   rule, but pre-release needs the full pass).
8. Sweep cmd/tq for other option guards that could be bypassed by fully
   absolute inputs for consistency (inventory only, no changes).
9. Document the allReposAbsolute bypass in FEATURES.md if not already
   there (I did not check — noticed in passing).
10. Confirm CHANGELOG has an entry for 9804683's behavior change.
11. grep cmd/tq/seeds_test.go beyond line 80 for a full accounting of
    allReposAbsolute cases (I read to line 80; suite passed regardless).
12. Consider teaching `tq doctor` to report when the pool is running with
    a bypassed risky-projects-dir, for observability.
13. In the report-template contract: dedupe "stale re-dispatch close-out"
    reports vs. full self-reviews.
14. Add the item's origin (02:00 f16 report section ref) to the TODO item
    convention so future agents find prior context faster.
15. Verify the sqlite/postgres sub-module conformance suites were not
    affected by anything in the 9804683 lineage (cmd-only change, but a
    one-line confirm closes it).
16. Run `./scripts/ci-local.sh` once this week as the pre-push gate
    rehearsal (I ran targeted gates only).
17. Confirm no other TODO items from the 02:00 f-section are also stale
    re-dispatch duplicates (did not check; out of scope).
18. Tighten the queue's dispatch dedup: if a TQ_RESULT commit sha exists
    for a task ID, skip re-dispatch entirely.
19. Optionally add `tq tasks --filter=footer:<id>` to find prior work by
    Task-Queue-ID (CLI ergonomics idea; YAGNI-check first).
20. Audit whether `checkProjectsDir("/")` message should mention the
    absolute-repos escape hatch (helpfulness polish).
21. Windows: filepath.IsAbs on `C:\repos\foo` — confirm test coverage for
    Windows paths in TestAllReposAbsolute (CI runs windows-latest; suite
    passed there historically, but the absolute-path cases predate?)
22. Record in AGENTS.md the "third dispatch" anecdote if the re-dispatch
    loop proves systemic.
23. Add a smoke that runs agent-pool flag parsing with absolute repos
    against a scratch TQ_DB (AGENTS.md warns: export TQ_DB explicitly).
24. Verify `splitRepos` trimming behavior is documented where
    allReposAbsolute relies on it (`/srv/a, /srv/b ,/srv/c` case).
25. Keep this list bounded — items 25-50 intentionally left open: a
    no-op re-dispatch should not mint 50 speculative tasks; the repo's
    existing 50-item lists from prior close-outs already cover
    repo-wide next work, and minting duplicates would re-create the
    exact staleness problem this task embodies.

## g) Questions I can NOT figure out myself

1. Why did the queue re-dispatch this item after TWO prior close-outs
   (5d926fd, 747cace/082078a) already recorded completion? Is the
   re-arm keyed on something I can influence from the repo side?
2. Should the allReposAbsolute bypass extend to `tq worker` and
   `tq audit`, or is the pool the only path where a default risky
   projects-dir blocks legitimate absolute-repos runs? (Owner intent.)
3. Is the two-report close-out (redispatch note + self-review) wanted,
   or should no-op re-dispatches collapse into one file?
