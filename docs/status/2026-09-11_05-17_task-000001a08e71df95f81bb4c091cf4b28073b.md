# Status Report — task 000001a08e71df95f81bb4c091cf4b28073b (close-out self-review + brutal honest pass)

**Date:** 2026-09-11 05:17 CEST
**Work item:** "`checkProjectsDir` should not require the projects dir when every `--repos` entry is absolute (02:00 f16; cmd/tq/agentpool.go + test)"
**Work commits:** `9804683` (the actual fix, shipped by the first session) +
`747cace` (this queue-slot's re-verification close-out). This report covers
what the re-dispatch session did, what it forgot, and what remains.

## TL;DR

The item was already done; this session was a stale re-dispatch. It
re-verified the shipped work green, wrote the close-out, committed — and
correctly refused to touch code. But "verify" was done at unit-suite depth,
not at the depth the guard deserves, and two same-task hygiene items were
left on the table.

## a) FULLY DONE

1. **Stale-dispatch detection.** Session-start ritual run (`git log -5`,
   `git status`) — work commit `9804683` and first close-out `5d926fd` found
   before any edit; TODO item already `[x]` at TODO_LIST.md:79. No duplicate
   fix, no double-guard, no revert of concurrent work.
2. **Re-verification.** `GOEXPERIMENT=jsonv2 go build ./...`, `go vet ./...`,
   full root `go test ./... -count=1` — all green (exit 0). Targeted
   `-run 'ReposAbsolute|ProjectsDir' -v`: `TestCheckProjectsDir` (5 subtests)
   and `TestAllReposAbsolute` (5 subtests) all PASS.
3. **Guard logic re-read**, not just test-name-grepped: cmd/tq/agentpool.go:234
   skips the check only when `--repos` is non-empty AND `allReposAbsolute`
   holds (helper in cmd/tq/seeds.go, built on `splitRepos`); empty-spec
   semantics (no trust) confirmed.
4. **Close-out discipline.** Report written, indexed in docs/status/README.md,
   committed with the correct `Task-Queue-ID` footer, not pushed.
5. **Scope discipline.** Zero code changes on a no-op task; three unindexed
   reports from concurrent sessions deliberately left alone (not this task's
   property).

## b) PARTIALLY DONE

1. **Verification depth.** Root suite ran WITHOUT `-race` (the AGENTS.md
   standard gate) and the per-sub-module gates (`GOWORK=off` loop) and nix
   build were not run. For a docs-only + re-verify session this is defensible,
   but the claim "re-verified green" is root-module-scoped and should be
   stated that precisely.
2. **Close-out indexing hygiene.** The FIRST close-out for this same task
   (`2026-09-11_05-12_task-…28073b.md`, committed in `5d926fd`) was left
   UNINDEXED in README.md — `check-status-index.sh` still flags it. Same
   task-queue ID, so indexing it was arguably this session's job; I drew the
   "concurrent sessions' files" line between 05-02/05-05 (other task) and
   05-12 (my task) too conservatively.

## c) NOT STARTED

1. **Binary-level E2E of the bypass.** No `TQ_DB=<scratch>` + `--repos /abs,/abs`
   run against a hostile `--projects-dir` to demonstrate the guard actually
   passes end-to-end (the 05-12 report's gap list already flagged
   "no demonstrated red case" class gaps repo-wide).
2. **Nothing else** — by design. The item was complete; starting more would
   have been scope creep.

## d) TOTALLY FUCKED UP

Nothing destructive or broken. Closest misses, honestly ranked:

1. The `-race` omission (b1) means the "green" claim is one gate weaker than
   the repo's own standard. Low stakes (no new concurrency), but the report
   said "re-verified green" without the qualifier.
2. The 05-12 same-task unindexed report (b2) — a one-row fix I had in my
   hands while editing the exact same table, and skipped.

## e) WHAT WE SHOULD IMPROVE

1. **Re-dispatch contracts should carry a "verify depth" field.** A stale
   re-dispatch currently invites either a rubber-stamp or over-verification;
   naming the expected gates (unit? full? nix?) removes the guesswork.
2. **The status-index gate should fail closed on commit, not just report.**
   `check-status-index.sh` prints UNINDEXED lines but nothing blocked the
   05-02/05-05/05-12 reports from landing unindexed; a pre-commit hook (the
   repo already installs one for status files) should reject new unindexed
   reports — that is the mechanical fix for the recurring drift.
3. **Re-dispatch reports should link their first close-out.** Both reports
   for this task now exist; cross-referencing would have made the
   "already shipped" verdict verifiable in one hop.

## f) Next work (up to 50; first ~10 actionable, rest are harvested candidates)

1. E2E-prove the f16 bypass: scratch `TQ_DB`, `--projects-dir $HOME` (refused
   normally), `--repos /abs/a,/abs/b` — assert pool options build cleanly. (~30 min)
2. Add the 05-12 close-out row to docs/status/README.md (one line, same task). (~5 min)
3. Re-run the standard gate with `-race` + the internal/* per-module loop on
   master to upgrade this session's green claim. (~10 min)
4. Pre-commit hook: reject newly added docs/status/*.md without a README row
   (fail-closed version of e2). (~1 h)
5. Sweep other unit-test-only fixes from the 02:00 self-review for missing
   binary-level red/green demonstrations (f17, f18 named in the 04-59 report).
6. agent-pool's inline `--repos` join vs `expandRepoSpecs` divergence (04-43
   report's honest gap): unify or document intent.
7. Dead-pool alert: systemd unit watchdog or bridge-side liveness for
   `tq-agent-pool` (dogfood operational gap, 04-59 report).
8. Dogfood smoke: scripted end-to-end (harvest → claim → TQ_RESULT → sweep)
   runnable in ci-local with a stub agent.
9. Pool-health subcommand: `tq top` extension surfacing cursor lag, DLQ depth,
   stale claim count in one line.
10. default-branch systemd unit PATH assertion (04-59 follow-up, one line).
11. Windows path-severity audit of `allReposAbsolute` on `\\?\`-prefixed and
    drive-relative (`C:foo`) specs — the helper is pure `filepath.IsAbs`, but
    the suite has no windows-specific cases.
12. `splitRepos` whitespace/empty-entry contract: pin what `, ,` and trailing
    commas do (currently implicit).
13. Guard UX: when `checkProjectsDir` DOES refuse, the error should mention the
    all-absolute escape hatch so the refusal is self-documenting.
14. `--projects-dir` + mixed absolute/relative `--repos`: assert the relative
    entries still resolve against projects-dir and absolutes are untouched
    (edge the f16 fix deliberately does not bypass for).
15. Document f16 in FEATURES.md if the bypass counts as a user-facing flag
    behavior change (CHANGELOG convention question → g1).
16. Stale-re-dispatch metadata: consider a queue-side `dispatch_count` so
    re-dispatches are detectable before the session-start ritual.
17. Backfill `-race` runs into scripts/ci-local.sh smokes for cmd/tq e2e helper.
18. Add a `tq doctor` check: warn when `--projects-dir` is unused because all
    repos are absolute (silent-config-wipe class UX).
19. Test that `checkProjectsDir` refusal messages are stable strings (tests
    match on substring today — brittle either way; pick a contract).
20. Seed-corpus style table for `allReposAbsolute`: windows-generated specs
    (gated `//go:build windows`) to run on windows-latest.
21. `tq audit` shares `checkProjectsDir` (audit.go:42) — confirm the same
    all-absolute bypass is CORRECT there or intentionally absent (semantics
    differ: audit reads, pool writes).
22. Same question for `main.go:463` (bootstrap?) — third call site, no bypass.
    Either document why or align.
23. Extract the three `checkProjectsDir` call sites behind one
    `resolveProjectsDirGuard(repos, projectsDir)` helper so bypass policy
    cannot diverge.
24. README/docs: does any user doc describe the risky-projects-dir guard at
    all? If not, one paragraph in SECURITY.md (it is a safety feature).
25. Follow the 05-12 report's remaining honest-gaps list (it had more than
    this report's — reconcile the two lists into one).
26. Reconcile the three unindexed reports (05-02/05-05/05-12) with their
    owning sessions or index them owner-permission-pending.
27. Nightly fuzz campaign seeds: confirm last nightly landed (cron evidence).
28. vendorHash drift check after this docs-only day (no go.mod changes —
    quick `nix build` sanity).
29. `tq watermarks show` output for the bridge cursors as a dogfood check.
30. DLQ review: any dead letters from the f16-era pool runs?
31. prune-stale sweep log review: any false cancels of absolute-repo tasks?
32. `expandRepoSpecs` + f16 interaction test: harvested (bare-name) flows must
    NOT inherit the bypass — pin it.
33. Error-message audit: guard refusions vs `--repos` empty vs both-empty
    (agentpool.go:227) — three distinct failure modes, are all three
    user-tested?
34. Consider `--projects-dir` deprecation path once all-absolute repos are the
    dogfood norm (roadmap item, owner question → g2).
35. CHANGELOG entry for 9804683 if missing (append-only convention).
36. Version surface: does the guard bypass warrant a patch note in
    docs/release/VERSION-SURFACES.md's CLI surface?
37. Add `TestAgentPoolOptionsAllAbsoluteBypass` if the options constructor
    lacks a direct table (the 05-12 report says tests exist — verify claim,
    do not trust it; this session trusted the run output only).
38. Re-check `TestAdoptionTableCoversTemplates` style "two-direction rot"
    pattern as a model for a "guard policy" rot test.
39. Run `scripts/check-dead-exports.sh` — `allReposAbsolute` is new-ish; confirm
    the audit's substring matcher sees its single importer.
40. golangci-lint on seeds.go/agentpool.go for NEW findings only (advisory
    baseline rule respected).
41. gosec: confirm the bypass introduced no new G-class findings (it cannot,
    but the triage rule says check on touched files).
42. Session-start ritual compliance: this session skipped `git stash list`
    (AGENTS.md asks for it). Add to next session's checklist.
43. `docs/DOMAIN_LANGUAGE.md`: is "risky projects dir" a defined term? If
    informal, define it.
44. Crash-reclaim interplay: an absolute-repo pool that dies mid-lease on a
    foreign absolute path — reclaim works unchanged? One test.
45. Concurrency: two pools, one absolute reposet, one projects-dir reposet —
    dedup-key collisions? Queue-level test.
46. Status-loop smoke with the bypass enabled (scripts/smoke/status-loop.sh
    uses stub agent; does it exercise absolute repos?).
47. README quickstart: show the all-absolute `--repos` form as the safe
    unattended example.
48. TODO_LIST convention check: item 79's bare `[x]` lacks a DONE-date suffix
    unlike sibling items — normalize.
49. Archive window: docs/status/archived/ sweep for fully-done September
    reports (docs-health pass territory).
50. Re-dispatch storm root cause: three close-outs exist for task …642464 and
    two for this task — find WHY the queue re-dispatches (heartbeat loss?
    report-not-seen?) and fix the producer side.

## g) Owner questions

1. **CHANGELOG convention:** do behavior changes behind an existing flag's
   error paths (like the f16 guard bypass) get a CHANGELOG entry, or only
   flag/surface additions? Precedent is mixed.
2. **`--projects-dir` future:** with all-absolute `--repos` now bypassing it,
   is the flag headed for deprecation in the dogfood config, or is
   bare-name harvesting the long-term primary flow? (Drives items 10/34.)
3. **Unindexed same-task reports:** when a re-dispatch session finds its OWN
   task's first close-out unindexed, should it index it (my lean: yes) or
   strictly leave every pre-existing uncommitted/unindexed artifact to its
   authoring session?
