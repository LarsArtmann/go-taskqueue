# Task 000001a0c2e55de3cb9b25e94c2d36b5fcd5 — VERIFY window (2nd on this ID): repeat dispatch of a DONE row, verify → cite → stop

Written 2026-09-21 10:00 CEST. SECOND window on this ID: the 09-52 window (same ID) already shipped the work and wrote the mandated a)-g) report (docs/status/2026-09-21_09-52_task-000001a0c2e55de3cb9b25e94c2d36b5fcd5.md). This window was a repeat dispatch of TODO row 172 after its check-off. The session-start ritual (`scripts/session-start.sh 000001a0c2e5…`) surfaced the prior report before any edit, so the window's contract became the ritual's mandated shape: verify the DONE claims first-hand at HEAD, add one fresh gate delta, stop. Disposition: **NO-OP VERIFY — zero source delta, zero commits by this window** (the footer commit 74fb357 from the 09-52 window is the attribution of record; tree clean at HEAD 21bb5b1).

## a) FULLY DONE

1. **Repeat-dispatch detection before any edit.** Ritual ran first (git log/status/stash + ID grep + CONTRIBUTING.md read; CLAUDE.md checked — absent). The ID grep found the 09-52 report under this exact task ID; no work was re-implemented, no files touched before verification.
2. **Every load-bearing claim of the 09-52 report re-verified first-hand at HEAD 21bb5b1** (not trusted from prose):
   - `agentResultCard` exists (internal/webui/fragments.templ:699-711): "agent run" title, footer-commit-count badge, the shared usage line via `sessionUsageText` with all four args including `SessionMessageCount`, and the log-path line; wired in `taskDetailCard` at fragments.templ:646-656 ahead of review/status/prioritize.
   - `reviewFindings` carries the usage line (fragments.templ:689-691), same helpers.
   - Wiring re-read at line level this window (the prior report cited them; now first-hand): `DashboardData.AgentResults` field (internal/webui/render.go:315-319), `agentResultFor` with the all-zero validity gate that keeps no-op/stub runs card-less (render.go:413-424), `handleTaskDetail` populating via `pageResults` over `TaskTypeAgent` (internal/webui/handlers.go:253-258).
   - Generated-artifact parity: `fragments_templ.go` carries 164 long-form `internal/webui/fragments.templ` FileName references — no module-dir templ-generate drift survived at HEAD (the 09-52 §d1 flip was healed there; this window confirmed it stayed healed).
   - Footer commit 74fb357: exactly ONE `Task-Queue-ID: 000001a0c2e5…` footer and it is the LAST line of the message (trailer-parser rule). Its `--stat` is 3 files (AGENTS.md, TODO_LIST.md, render.go); the templ/test deltas rode daemon folds 0f1d5e6/06f2e8c per the commit message's own note — attribution intact across the folds.
   - TODO row 172 `[x]` with the citation-dense closure note; pin `TestResultUsageRendersOnDetailPage` at internal/webui/webui_test.go:624-632 with the agent/review/all-zero-quiet cases in its doc comment.
3. **Fresh battery at HEAD, rc=0 each**: root build+vet under `GOEXPERIMENT=jsonv2`; `./scripts/check-doc-refs.sh`; internal/webui in-module suite `-count=1` ok 10.094s (GOWORK=off); **`./scripts/smoke/webui.sh` passed** — the smoke is this window's FRESH DELTA and the battery item the 09-52 window skipped (its §e1: "next template window: run it before declaring done"). Served-page proof (auth/CSRF/SSE/write-lockout) now stands at the final tree.
4. **Honesty upgrade en route**: the 09-52 report's wiring citations were verified rather than re-trusted, so a future re-dispatch can cite this report's §a2 without a third re-read.

## b) PARTIALLY DONE

1. **-race evidence is inherited, not re-run.** The webui suite ran plain (10.1s) only; the `-race 12.759s` citation remains the 09-52 battery (same webui tree; the two commits since are docs-only). Within the verify-window minimum battery's same-HEAD inheritance allowance for expensive gates — but at 13 seconds wall time it was cheap enough that re-running was the better call (see §e3).
2. **Queue-side verify on this dispatch is EXPECTED to fail** at the structural gofmt-vendor stage (`gofmt -l .` walks gitignored `vendor/`, AGENTS.md 2026-09-21 §f4; rows 204/326/328; owner-only fix). This window's local battery above is the citation of record, same precedent as the 07-12 row's note. No local workaround was attempted — an agent must never edit its own gate.
3. **No new test written.** Zero source delta makes a new pin scope creep; the existing pin (§a2 last bullet) exercises the render path through the server, transitively covering the wiring. Stated so the absence is a decision, not an oversight.

## c) NOT STARTED

All follow-ons below are the 09-52 window's minted rows, untouched here (different rows, not this dispatch's contract):
- Review card log-path line; agent table badge + loadSnapshot parity (§g2-gated); dlqfix Session* budget scope (§g3-gated); `tq api` surface verify; CHANGELOG/FEATURES entries for 74fb357 (rides the row-336 ruling).
- The three §g rulings below remain unanswered; nothing in this window changed their evidence base.

## d) TOTALLY FUCKED UP

Nothing broke, but three execution stumbles, honestly:

1. **Wrong file in the templ-path parity grep on the first try.** Counted `internal/webui/layout.templ` occurrences in `fragments_templ.go` (0 — meaningless; that generated file embeds `fragments.templ`). Corrected to `internal/webui/fragments.templ` (164, canonical). Conclusion reached only on the second try; the check should have asked "which source does THIS generated file embed?" first.
2. **Pipeline-hazard adjacent form in the battery.** The webui suite ran as `go test … | tail -3` — benign here (rc was read off the `ok` line, not `$?` after the pipe), but the codified agent-session form is redirect-to-file + direct `$?` (AGENTS.md PIPESTATUS warning; the smoke invocation in the same window did it correctly).
3. **Inherited the §b1 laziness**: cited 09-52's race run instead of spending the 13 seconds. The inheritance was rule-compliant; rule-compliant is not the same as best.

Systemic and inherited, not this window's to fix: the minted verify gate's vendor-walk gofmt stage keeps converting every re-dispatch of this cluster into one burned verify attempt (§b2; five dead-on-gate tasks visible since 09-20 per AGENTS.md).

## e) WHAT WE SHOULD IMPROVE

1. **The 10-second webui smoke should be STANDARD in any battery whose changed surface is a rendered page** — this window adopted it and closed the 09-52 §e1 caveat; make it default, not a per-window judgment call.
2. **Generated-artifact path parity is a one-grep check** (`grep -c 'internal/webui/<file>.templ' <file>_templ.go`) — cheap enough to add to every templ-adjacent verify battery; it would have caught the 09-52 §d1 FileName flip before review instead of after.
3. **Judge "expensive gate" by wall time, not vibes**: a 13s race run should be re-run; the same-HEAD inheritance rule should be reserved for nix/60s+ suites.
4. **Verify→cite→stop works best compact** (the 06-34 attempt-2 precedent): re-cite file:line, one fresh delta, stop — do not re-derive the cluster's history each time.
5. **The 09-52 report's §f table is the canonical follow-on list for this cluster** — sibling windows should cite it rather than re-inventorying (this report's §f does exactly that).

## f) Things to get done next (ranked; all already-minted rows — no new research, no padding)

| # | Task | Impact | Effort | Category | Status |
|---|------|--------|--------|----------|--------|
| 1 | OWNER: scope the minted gofmt gate to tracked files in BOTH `.tq-verify` and the mint template — ends the deterministic dead-letter burn on every re-dispatch | Critical | S | Bug | row 204 class, owner-only |
| 2 | Review card: render the `full agent output` log-path line like the other three cards | Low | S | Feature | minted (09-52 §f1) |
| 3 | Agent-outcome table badge + loadSnapshot AgentResults parity — after the §g2 ruling | Medium | M | Feature/ruling | minted (09-52 §f2) |
| 4 | DLQ autopsy session usage in the budget token projection — after the §g3 ruling | Medium | M | Budget/ruling | minted (09-52 §f3) |
| 5 | Verify `tq api` mirrors the webui's agent/review usage surface | Low | S | Quality | minted (09-52 §f4) |
| 6 | CHANGELOG/FEATURES entries for the agent-run card + review usage line | Medium | S | Documentation | minted (09-52 §f5; rides row 336) |
| 7 | Show-JSON `session_message_count` assertion in TestShowJSONCarriesDerivedSessionUsage | Medium | S | Quality | row 173 |
| 8 | FEATURES placement ruling for usage-rendering facts (sweeper rows vs first-class webui rows) | Low | S | Documentation | row 336 |
| 9 | Run `./scripts/ci-local.sh` end-to-end before the next push (nix + master-CI + css-gate delta) | High | M | Process | row 190 |
| 10 | Daemon-fold attribution ruling (session footers in daemon commits vs tq-side fallback) | Medium | — | Process | row 194, fourth+ ask |
| 11 | Harvest anti-race: checkbox re-read at claim/dispatch time (kills the verify-churn this ID cluster keeps paying) | High | M | Process | 08-35 §f4 row |
| 12 | Commit-msg hook: require the Task-Queue-ID footer in the FINAL trailer block | Medium | S | Quality | 04-13 §d3 row |
| 13 | Extend TestShowJSONCarriesDerivedSessionUsage-style exact pins to Review/Status usage keys | Medium | S | Quality | row 165, BLOCKED on owner ruling |
| 14 | v0.3.0 release tail: pkg.go.dev render check | Medium | S | Release | row 174 |
| 15 | `tq tasks` payload/verify exposure (stale-verify hygiene without a `tq show` per task) | Medium | M | Tooling | 09-39 §e1 row |
| 16 | `tq doctor --hygiene` stale-verify gate | Medium | M | Quality | 09-39 §e3 row |
| 17 | Consciously decide factTone cases for the 6 newer fact types | Low | S | Quality | 08-35 §e5 row |
| 18 | Drop detailItems' unused `now` param (unparam) | Low | S | Cleanup | 08-35 §e6 row |
| 19 | internal/e2e -race headroom vs the 180s stage cap | Medium | M | Quality | filed per row 162's note |
| 20 | `agentResultEmpty` helper extraction + review-card log-path in one micro pass | Low | S | Cleanup | 09-52 §e4 + §c1 |

## g) Questions I cannot figure out myself

All three are the 09-52 window's open rulings — still unanswered, still blocking their rows; this window's verification strengthened (not changed) their evidence base:

1. **Card-density ratification: keep the new agent/review usage rendering on the detail page, or was status/prioritize-only a deliberate restraint?** It now ships pinned (74fb357 + TestResultUsageRendersOnDetailPage); a reversal is one templ edit + pin update, but saying so BEFORE rows 2/6 spend windows avoids wasted effort.
2. **Should completed agent tasks carry a task-table badge (and loadSnapshot wiring), or is the table deliberately badge-minimal (reviews/statuses only)?** Rules row 3.
3. **Are DLQ autopsy runs inside or outside spend accounting?** dlqfix results carry no Session* fields, so autopsy cost is invisible to budget.session_usage and the webui; if in-scope, the wiring is the standard four-line pattern; if deliberately out (forensics ≠ production spend), row 4 closes with that note.

---

Scope note: based ONLY on this window's verification work and what was read in passing (the 09-52 report, TODO rows 160-200, the ritual output). No unrelated research. The only deltas this window produced are this report + its index row; the code is untouched at HEAD 21bb5b1.
