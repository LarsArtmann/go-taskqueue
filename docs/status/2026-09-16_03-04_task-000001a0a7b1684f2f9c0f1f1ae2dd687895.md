# Close-out: durable self-test pin for ci-local's transient-retry helper

- **Task**: 000001a0a7b1684f2f9c0f1f1ae2dd687895 (TODO row 89, line 89; source: 01-46 report f1/f2)
- **Lap**: 2026-09-16 02:50–03:10 CEST, single session
- **Work commits**: script content rode the auto-commit daemon — `f98224e` (`scripts/check-transient-retry.sh` +120, `scripts/ci-local.sh` +7 wiring); docs + footer on `896339b` (TODO row annotation, CHANGELOG entry, AGENTS.md gate one-liner). Daemon-race protocol followed (§e6 of 01-46): `git status --short` re-checked immediately before the explicit commit; the daemon had already taken the scripts, the docs were committed before it could take them.
- **Scope ruling honored**: the row's smoke half is ruling-blocked BY THE ROW TEXT itself ("needs a runtime-budget ruling first", 01-46 §g3) — an unsupervised lap does not make the pre-push gate's worst-case-runtime policy call. The self-test pin half had no such reservation and shipped.

## a) FULLY DONE

1. **The pin ships**: `scripts/check-transient-retry.sh` — sed-extracts the SHIPPED `with_transient_retry` helper (env-default lines through the function's closing `^}$`) from `scripts/ci-local.sh`, so it always exercises the bytes the pre-push gate actually runs; a copy in the pin would drift silently. Five marker assertions guard the extraction: a rotted range (helper renamed/reshaped/moved) fails LOUDLY with the lost marker named instead of testing the wrong text.
2. **Eight behavior assertions, sub-second** (`TRANSIENT_POLL_SECS=0`): shipped defaults still 45s×3 (the 15-39 f9/e2 budget is contract, pinned); plain gate passes first try (rc 0, 0 WARN, 1 run); compound `bash -c '…' _ "$stamp"` gate heals on first retry (rc 0, 1 WARN, 2 runs — the real per-module-loop shape) with the "possible concurrent edit in flight" WARN text present; always-red gate exhausts (rc 1, exactly 3 WARNs, exactly 4 runs) with BOTH context lines present ("still failing after 3 retry polls" + the "let it land (or re-run this gate on a quiet tree)" guidance); `TRANSIENT_MAX_POLLS=0` escape hatch fails immediately (rc 1, 1 run, 0 WARNs — pre-wrapper behavior stays reachable, 01-46 f14).
3. **Negative-tested the pin itself** — the step the 01-46 session's throwaway /tmp test never got: two broken copies of ci-local.sh in /tmp (repo untouched, verified clean after) — (A) renamed function → marker guard fires, exit 1, names the lost marker; (B) poll-counter drift (`TRANSIENT_MAX_POLLS + 1`) → exhaust case fails with exact got/want (warns=4 runs=5 vs want 3/4). A pin that has never been shown to fail is a hypothesis; this one is proven to catch both its regression classes.
4. **Wired BEFORE the Go gates** (`scripts/ci-local.sh:51-56`, right after check-ci): a broken retry helper fails the gate at second one instead of mid-10-minute-run. `check-guard-wiring.sh` green — the new check-* script is wired, no orphan.
5. **TODO row 89 annotated per file conventions**: original text kept, shipped-note appended, ends `— BLOCKED: smoke half awaits the owner runtime-budget ruling (01-46 §g3: whole re-run ×3 vs two-phase tree-quiet probe)` — the BLOCKED suffix keeps the compound row out of the pool while the open half waits.
6. **Docs captured in the same lap** (01-46 §e3's lag lesson applied, not repeated): CHANGELOG `[Unreleased] Added` entry (what the pin pins, what it replaces, the still-open smoke half) and AGENTS.md Commands one-liner next to the other check-* gates.
7. **Verification battery (all cited runs this session)**: pin green (8 ok lines); `check-script-syntax.sh` green (bash -n 47 scripts + shellcheck severity≥warning 0 findings — the new script included); `check-guard-wiring.sh` green; `check-todo-list.sh` green; `check-doc-refs.sh` green; harvest-parse guard `go test ./internal/harvest/ -run TestRepoTodoListParses -count=1` ok; standard verify `GOEXPERIMENT=jsonv2 go build ./... && go vet ./...` green.
8. **Commit hygiene**: exactly one well-formed `Task-Queue-ID` footer (commit-msg hook passed); exec bit verified `100755` post-commit (fresh clones can execute the gate); changed-line lll gate scope confirmed `*.go`-only — the long .md/.sh lines are house convention (AGENTS.md/TODO_LIST rows run far longer), no hole; nothing pushed.

## b) PARTIALLY DONE

1. **The row is compound; the smoke half is NOT done.** Wrapping the smokes in `with_transient_retry` waits on the owner runtime-budget ruling (01-46 §g3): whole-smoke re-run ×3 multiplies the minutes-long smokes (fullcore, multi-repo) worst-case, vs the two-phase design (cheap tree-quiet probe before each smoke, full re-run only when the probe saw a break). Upholding the recorded deferral was correct; the honest cost is that row 89 stays open and the pool must not re-take it (BLOCKED suffix handles that).
2. **The pin pins the helper's semantics, not its integration.** It exercises extracted bytes in isolation; it cannot catch a future ci-local edit that stops CALLING the wrapper (a silently unwrapped gate loses retry coverage with the pin still green). A call-site census assertion ("N wrapped call sites") was considered and rejected this lap as brittleness-for-now — every legitimate wrapper-count change would need a pin edit. Tracked as a follow-up idea (§f6).
3. **Row 206 overlap left deliberately open**: its f14 item (AGENTS.md note for the `TRANSIENT_MAX_POLLS=0` escape-hatch semantics) is half-covered — the pin PINS the behavior and the CHANGELOG mentions it — but the AGENTS.md knob-semantics note itself was not added; row 206 owns it and scope discipline said one row per lap. A one-line fold-in was possible and not taken.
4. **Full ci-local end-to-end not run** (script-only change; 10+ min; stages the tree, nix build — same 01-46 §b6 residue, carried forward). The new step has run standalone, through the syntax gate, and the wiring through bash -n/shellcheck; it has not yet executed inside a real pre-push run.
5. **Standard verify scope**: root build + vet + the targeted harvest-parse test ran; the full `go test ./... -race` suite did not. Defensible for a zero-Go-code diff (no Go file touched), disclosed for the record.

## c) NOT STARTED

1. Smoke wrapper extension (blocked on §g1 ruling — not started by design).
2. Real-world soak of the pin inside a full ci-local run (§b4).
3. Calibration measurement of actual foreign-edit window durations (01-46 f4 — still zero datapoints; 45s×3 remains report-inherited).
4. Call-site census pin (§b2/§f6 — idea only).

## d) TOTALLY FUCKED UP

Honest answer: nothing in this class shipped — the pin is green, negative-proven, wired, and documented. Three disclosures that stop short:

1. **Turn-1 ritual order violation, repeated.** The grep-prior-reports-for-task-ID step ran in my SECOND tool batch, after `git log`/`git status`/`ls` — 01-46 §d1 named this exact miss ("turn-1 means turn 1", sixth recurrence) and I repeated it as the seventh. Outcome benign again (zero prior reports for this ID — fresh dispatch verified), but the ritual exists precisely for the case where a DONE row or prior report DOES exist; a repeat dispatch would have been detected late, again.
2. **Queue↔git cross-reference wart, third instance.** The actual work content (the pin script + wiring) rides footer-less daemon commit `f98224e`; the footer lands on `896339b`, which carries only docs. The documented fallback was followed (message cites the daemon SHA, so forensics work), but the queue's cross-reference still points at a commit that does not contain the pinned script. Same known daemon-fold weakness as 01-46 §d2 (`a1b4588`); policy question still unanswered (01-46 §g1).
3. **The changed-line lll gate was checked late.** I shipped without running `lint-lll-changed.sh`, and only verified its scope (`*.go` only) during THIS close-out. No hole resulted — but the check belonged in the §a7 battery, not in the retrospective.

## e) WHAT WE SHOULD IMPROVE

1. **Turn-1 means tool-call #1.** It keeps recurring because it keeps being "benign". Bake it into the dispatch template as the literal first call, before the session-start git ritual.
2. **A pin is not done until it has been shown to FAIL.** This lap's best move was negative-testing the pin on broken copies before shipping it. Make demonstrated-failure the standard ship step for every new check-* script (and record it in the close-out, as done here).
3. **Split compound TODO rows at harvest when one half is ruling-blocked.** Row 89 bundled a ruling-blocked half with an actionable half, forcing a partial-close: the shipped half can't be marked DONE (deletion-on-closure would delete the blocked half's tracking too) and lives as prose on an open row. Split rows give the actionable half a clean DONE + CHANGELOG trail.
4. **Extraction-based pins should document their fragility at the extraction site** — the pin's header says why the markers exist and what breaks them; the next ci-local editor should not have to rediscover that the sed range is load-bearing.
5. **Run the full gate list you touch — including the advisory ones — before declaring done.** The lll check was late precisely because it is obscure; the §a battery should be copied from ci-local's step list, not from memory.

## f) UP TO 50 THINGS WE SHOULD GET DONE NEXT

Honest count: 15 real items. Refusing to pad (00-11 and 01-46 precedent).

**From this task's orbit:**

1. **THE ruling (§g1, re-asked with a recommendation)**: smoke-retry budget. Recommendation: the two-phase design — cheap tree-quiet probe (e.g. staged-tree `git diff --quiet` + a fast build probe) before each smoke; re-run the whole smoke only when the probe observed a foreign break. Rationale: smoke runtimes span sub-second (help-text) to minutes (fullcore, multi-repo); whole-re-run ×3 worst case multiplies exactly the expensive ones, while the probe costs milliseconds. Alternative if the probe is judged too clever: wrap only smokes under ~30s. Owner call either way.
2. Split row 89 at the next docs-health pass (per §e3): smoke half becomes its own BLOCKED row; the pin half closes as DONE (it is already fully evidenced in CHANGELOG + this report).
3. Row 206 polish bundle (untouched, owns the same helper): "healed on poll N" success log line; AGENTS.md `TRANSIENT_MAX_POLLS=0` knob-semantics note; one-line rationale for the deliberately unwrapped check-ci step.
4. First full ci-local soak with the pin wired; record whether any poll healed and whether the pin step itself behaves in situ (closes 01-46 §b6/§c1 and this §b4).
5. Calibrate 45s×3 from git-history/report evidence of past mid-edit breaks (01-46 f4; still zero datapoints — the wrapper has never healed a real break).
6. Decide the call-site census pin (§b2): a pinned "N wrapped call sites" makes unwrapping a gate a loud event, at the cost of touching the pin on every legitimate wrapper-count change. Small design call, tracked here.
7. If ci-local ever splits into `scripts/lib/ci-local-lib.sh` (01-46 f18), the pin's sed target must move with the helper — add that dependency note to the lib-split row when/if it is minted.
8. Pin-in-CI parity question (§g2): the pin is wired into ci-local only, so CI runners never execute it; check-guard-wiring accepts that (ci-local is a reference), but a runner-side regression class exists. Cheap fix if wanted: one ci.yml step running the pin (it is hermetic and sub-second).

**Adjacent tracked rows read in passing (legitimately next, unchanged):**
9. Row 89-batch rate-limit/observability items (16-00 f29-f38/f43-f48) — untouched this lap.
10. Row 90 (ci.yml concurrency group), row 91 (status-append cap/dedup + re-dispatch root cause), row 92 (dead `factLines` + DOMAIN_LANGUAGE terms) — untouched.
11. Mint-time done-check for repeat dispatches (01-46 f7 — still the loop's top fix; this task was fresh, but the pattern is one flaky grep away).

**Noticed in passing this session:**
12. docs/status/README.md's live index is deep in 2026-09-16 rows; the 100-row bloat warning is advisory but approaching — an archive sweep (docs-health ANNOTATE) is the standing answer.
13. The pin's own line `scripts/check-transient-retry.sh:95` runs 131 chars — legal (.sh is outside the .go-only lll gate) but worth knowing if that gate's scope ever widens to shell.
14. CONTRIBUTING.md's "individual gates" list predates the pin (and several other check-* gates); a one-line sweep would keep the contributor doc honest — small, unowned, filed here rather than edited mid-lap (scope discipline).
15. The daemon folded the scripts within minutes of the write (f98224e at 03:00:48) — the documented protocol absorbed it without loss this time; the §g1-footer-policy question from 01-46 remains the real fix.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Smoke-retry runtime budget** (the row's own reservation, 01-46 §g3, still unanswered — now with a concrete recommendation in §f1): two-phase probe, whole-re-run ×3, or wrap-only-cheap-smokes? This gates the only unfinished half of the task.
2. **Pin-in-CI parity** (§f8): should ci.yml run `check-transient-retry.sh` too (one hermetic sub-second step), or is pre-push-gate-only ownership correct for a pin whose subject lives in ci-local itself? I can argue both; only you can weight runner-parity against ci-local-internal tooling.
3. **Split-at-harvest convention** (§e3): may the docs-health harvest split compound rows (actionable half + ruling-blocked half) into separate rows going forward? It changes row identity/harvest keys, so it is a process ruling, not a per-instance choice.

---

**Gates cited (this session)**: `check-transient-retry.sh` green (8 assertions) + negative-tested exit-1 on both broken /tmp copies (marker rot, poll-counter drift; repo verified clean after); `check-script-syntax.sh` green (bash -n 47 scripts, shellcheck 0 findings ≥ warning); `check-guard-wiring.sh` green; `check-todo-list.sh` green; `check-doc-refs.sh` green; harvest-parse guard ok; `GOEXPERIMENT=jsonv2` build+vet green; exec bit `100755` verified; single well-formed footer verified on `896339b`. Commits: `f98224e` (daemon, script content), `896339b` (docs + footer), this report commit.
