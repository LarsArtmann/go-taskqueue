# Repeat dispatch — PreToolUse session registry, verify-then-stop (task 000001a0aca5aff91fb2eb718d76e5540095)

2026-09-17 02:27 CEST · second dispatch of a DONE row · repo: go-taskqueue · branch master

## What shipped this pass

Nothing new — by design. This dispatch re-ran the queue's work item already shipped in the 02-21 session. Turn-1 ritual: grepped `docs/status/` for the task ID FIRST this time, found the prior report, read it, then verify → cite → stop (the correct response to a DONE row per AGENTS.md).

Verification performed this pass (all cited):

- Root `go build ./...` green (GOEXPERIMENT=jsonv2 exported).
- `internal/session` `-race -count=1` green (1.2s).
- cmd/tq module gate `./scripts/test-cmd-tq.sh` green (8.2s).
- Artifacts present: `cmd/tq/session.go`, `internal/session/registry.go`, `scripts/hook-session-registry.sh`.
- TODO_LIST row already ticked with the full DONE annotation (TODO_LIST.md:106).

## a) Fully done

- The entire work item (prior session, 02-21 report): `tq session ping`, the JSONL registry, `tq session sweep` (quiet + unowned → replay-safe close, never closes blind on a failed ownership check), the best-effort PreToolUse hook script, help text, tests, live smoke.
- This dispatch's contract steps: verified work, confirmed TODO tick, wrote this report, committed with the footer.

## b) Partially done / honest gaps (inherited + observed)

- **Full ci-local still unrun on this tree** (inherited from 02-21; I ran the touched gates only, same as before).
- **Nothing schedules the sweep** — the sweeper exists, the cadence does not (inherited §b item 37 of the prior report).
- **CHANGELOG/AGENTS rows for `tq session ping/sweep` still missing** (inherited).
- **The hook script is still not wired into any smoke or ci-local** — user-facing script nothing exercises.
- **Deployment story owner-run**: crush-config hook wiring + systemd timer are owner surfaces.

## c) Not started (adjacent, untouched)

- Session trigger #2 (`tq crush` wrapper), #3 (#3146 SessionEnd adoption).
- Postgres AppendFact/session-bridge parity; `tq session list` / sweep `--dry-run`.
- All §f backlog items from the 02-21 report (correctly left to the queue).

## d) What went wrong / almost went wrong

- **Turn-1 ritual finally executed correctly** — but only because the AGENTS.md codification is fresh; still worth noting it was the N+3-th miss LAST session. This pass: grep first, report read, CONTRIBUTING.md previously known.
- **qmd `get` by relative path failed** ("Document not found") for the report file even though `rg` had just found it — fell back to View. Cost one round trip; qmd's collection mapping does not cover docs/status here.
- Nothing else. A verify-only dispatch has a short failure surface, and I kept it that way: no edits, no reverts, no scope creep.

## e) What to improve

1. **The repeat-dispatch fast path works** — ritual → prior-report check → gate re-run → stop. Codify the exact sequence (rg status dir → read report → re-run the touched gates) as the canonical DONE-row response.
2. **Stop the bleeding on inherited §b items**: the sweep scheduler and CHANGELOG row have now been flagged twice; the next docs-health pass or a backlog row should close them rather than a third report restating them.
3. **Verify-only dispatches should cite the exact gate commands + durations** (done here) so the next session can diff against environment drift without re-reading code.

## f) Next items (backlog candidates; queue decides; prior report's 50 still stand)

1. Systemd timer or agent-pool actor running `tq session sweep` on a cadence (top inherited gap).
2. Route sweep mints through the budget projection (design OQ2) before any production run.
3. `tq session sweep --dry-run`.
4. `tq session list` with last_seen age.
5. CHANGELOG Added row for `tq session ping/sweep` + registry.
6. AGENTS.md payload-contracts line for the registry + sweep policy pins.
7. Smoke for `scripts/hook-session-registry.sh` (with/without CRUSH_SESSION_ID, exit 0 both ways).
8. Wire-or-delete ruling for the hook script (check-guard-wiring currently exempts it by design).
9. Owner-check hardening: require `crush` in the pgrep cmdline, not just the id substring.
10. Registry state-vs-cache location ruling (cache implies deletable; deleting orphans open sessions).
11. Postgres session-bridge parity (sweep depends on Close → AppendFact).
12. Full ci-local run over the current tree, then owner push.

## g) Questions I cannot answer myself

1. Should repeat dispatches of DONE rows mint anything (e.g. a queue-side "re-verified" note), or is verify-cite-stop + this report the complete expected output?
2. Who owns the sweep cadence decision (systemd timer vs agent-pool actor) and the budget-gating of its mints before the first production sweep?
3. Is the hook script's ci-local exemption final, or should it get a smoke before the next release?
