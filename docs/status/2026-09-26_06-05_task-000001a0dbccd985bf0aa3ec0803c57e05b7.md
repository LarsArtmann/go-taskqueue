# Status report 2026-09-26 06-05 — task 000001a0dbccd985bf0aa3ec0803c57e05b7

REVIEW-FIX window: one HIGH reviewer finding rejected the 05-07 close-out
(task 000001a0db839d41103a706b7164e9197520) for shipping unbaselined
dead-SHA citations that left `./scripts/check-dead-sha-refs.sh` red. The
contract was: fix EXACTLY that finding, keep the repo's gates green, commit
with this task's footer, never push. Verdict: FIX SHIPPED — the gate is
green at work commit f48b4312 (rc=0, "110 baselined, 0 new"), the gate's
--self-test passes, and the doc battery is green on the post-report tree.

## a) FULLY DONE

1. **Finding located via its verbatim anchor** (`maneuver over daemon HEAD
   fd2176c5, origin/master ancestor-verified first);`): the site is
   docs/status/2026-09-26_05-07_task-000001a0db839d41103a706b7164e9197520.md:9.
   The rejected change b1d6a7d9 was first verified to still exist
   (`git cat-file -e` rc=0), and the CURRENT tree was judged, not the stale
   sha — per the disposition rule.
2. **Gate state measured before touching anything**: the gate ran rc=1 with
   13 DEAD SHA lines across THREE tokens, not just the finding's six:
   fd2176c5 (6 cites: the 05-07 report lines 9/62/106/122/136 +
   docs/status/README.md:646 — the finding's fix target), eb48b416 (3
   cites: the 05-19 report lines 3/11 + README.md:647 — flagged by the
   finding itself as required-for-green), and a51585e1 (4 cites: the 05-47
   report lines 3/14/32 + README.md:648 — landed AFTER the review was
   written).
3. **Forensics verified BEFORE baselining** — a baseline row is a claim, so
   each twin was proven, not assumed:
   - eb48b416 → c460e758: exact patch-id twin (32abd7515cbeb081668f639d2535a8aeab083e35
     on both sides, `git show | git patch-id --stable`).
   - a51585e1 → aab7ee1d: exact patch-id twin (99383cc7e26c6ff1918ef82ed0682e634a29edd6
     on both sides).
   - fd2176c5 → 018a6e32: NOT a patch-id twin — a REBUILD. The dead side is
     the footerless daemon fold (AGENTS.md +12); the surviving commit
     carries the same AGENTS.md hunk among its three files, verified by
     diff-stat containment. The baseline comment says "rebuild, not
     patch-id — verified by diff" so a future auditor is not misled.
4. **Fix applied exactly as the finding prescribed** (the baseline-row
   option, not the arrow-rewrite option): one block appended to
   scripts/dead-sha-baseline.txt with an amend-forensics classification
   comment in the established style, rows in the EXACT cited token form
   (8-hex `fd2176c5` / `eb48b416` / `a51585e1`). Exact form matters: the
   gate's `in_baseline` is full-line string equality, so a 7-hex prefix row
   would silently never match its 8-hex cite (the window-own exact-token
   lesson, honored).
5. **Gates green**: `./scripts/check-dead-sha-refs.sh` rc=0 ("dead-sha refs
   ok (110 baselined, 0 new)"); `./scripts/check-dead-sha-refs.sh
   --self-test` ok (clean rc=0 / exact file:line on rc=1 / arrow old→new
   passes / baselined silent unless TQ_DEAD_SHA_VERBOSE=1);
   `./scripts/check-doc-refs.sh` rc=0 (which wraps the same gate).
6. **Daemon race handled by the documented book**: the first
   `git add && git commit` failed with "nothing to commit, working tree
   clean" because the auto-commit daemon had already folded the baseline
   edit into a footerless chore commit (a483fdbe→f48b4312 names the pair).
   Verified NOT-PUSHED (`git merge-base --is-ancestor HEAD origin/master`
   → not an ancestor), then amended per the AMEND MANEUVER: work commit
   f48b4312, tree intact (1 file, 16 insertions), `git interpret-trailers
   --parse` returns the footer (the daemon original parsed to NOTHING).
7. **Footer convention honored**: exactly ONE `Task-Queue-ID` footer on the
   fix commit — the FIX ticket's id, this report's id — placed as the FINAL
   line after the attribution block (the 05-47 window's §d1 defect class,
   avoided here); no second footer for the parent task; nothing pushed.
8. **Minimum battery re-run on the POST-REPORT tree** (the sequence that
   matters — see §e1): check-dead-sha-refs rc=0, check-doc-refs rc=0,
   check-todo-list rc=0, check-status-index rc=0, root `go build ./...` +
   `go vet ./...` rc=0 under GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto. This
   report's own dead-side cites stay green via baselined tokens
   (fd2176c5/eb48b416/a51585e1) and one arrow-formed cite (a483fdbe→f48b4312);
   no NEW baseline rows were needed for this file.

## b) PARTIALLY DONE

1. **The scope judgment on a51585e1 was made, executed, and is owned here**:
   the finding prescribed fd2176c5 and pre-flagged eb48b416; a51585e1
   landed after the review and also blocked rc=0. I baselined it under the
   same amend-forensics class to reach the finding's required end state
   ("re-run ./scripts/check-dead-sha-refs.sh to rc=0"). A fix that leaves
   the gate rc=1 is not a fix — but this IS an extension beyond the
   finding's literal token list, made autonomously. The narrower reading
   (two tokens only, gate stays red, a51585e1 left for its own window) was
   considered and rejected.
2. **The finding's alternative fix was rejected on one line, not analyzed**:
   arrow-form rewriting of the six fd2176c5 citations would have touched
   five report lines + one index line and fought the reports' deliberate
   dead-side forensics prose; the baseline row matches three-plus windows
   of precedent (the 78e2768 / 68ffbe2 / e19259b blocks). The full
   tradeoff is one §g1 question to the owner, not a settled policy.
3. **Master CI state check (gh) skipped** — docs-only .txt change; house
   practice splits (04-43 checked and reported red-predates-tree; 05-19 and
   05-47 skipped and said so). Asked in §g3 rather than silently dropped.
4. **Turn-1 ritual partially honored** — git log over the whole repo, git
   status, git stash list, and the prior-report grep for THIS task id
   (empty — genuine first dispatch) all ran before any edit; but
   session-start.sh and CONTRIBUTING.md did not. See §d3.

## c) NOT STARTED

1. **No TODO_LIST changes**: this fix task's contract carried no
   backlog-append clause, and the parent task's row (109) was already
   closed by prior windows. §f items below live in this report only.
2. **ci-local end-to-end**: not run — a docs-only .txt diff proves nothing
   against the expensive gates, and the cheap-battery bar (03-27 §e2) is
   met with fresh runs. The ~121-commit unpushed lineage still lacks its
   ci-local window (existing row 114 per the 05-47 report).
3. **Baseline shrink**: the fd2176c5 row could arrow-form-shrink away
   (018a6e32 is its reachable rebuild twin), and shrink is advisory-only
   with no owning policy — untouched, consistent with the file's own rule.
4. **Nothing else**: the contract was exactly one finding.

## d) TOTALLY FUCKED UP

1. **The fix window's own maneuver manufactured a NEW dead SHA** — the
   amend that attached the footer turned the daemon fold into an
   unreachable commit (a483fdbe→f48b4312). It is cited nowhere in living
   docs, so the gate stays green WITHOUT a baseline row — but only by luck
   of non-citation, not by design. Had this close-out cited the daemon
   commit bare, the gate would have gone red again minutes after the fix.
   The irony is the point: the residue class regenerates on every sanctioned
   amend, and this window produced one while closing three.
2. **Commit-before-verify ordering lost to the daemon**: I chained
   `git add && git commit` without re-checking `git status` immediately
   before, and the daemon (documented ~4-minute latency) had already swept
   the file. The commit command failed; my work sat FOOTERLESS in a daemon
   commit until I noticed via git log and amended. The queue would have
   derived NOTHING from the footerless fold — the exact
   invisible-to-the-cross-reference defect class this repo keeps
   documenting — had I not checked. Recovery was one command; the miss was
   the ordering, and it was preventable by one status check.
3. **Session-start ritual partially skipped**: session-start.sh was not run
   and CONTRIBUTING.md was not opened this window — another instance of the
   documented 00-52 d4 / 02-17 d3 miss class. Mitigation offered, not
   pleaded: the entire edit surface was one baseline .txt whose conventions
   live in its own comments and in the gate script, both of which I read in
   full before editing, and the same-day windows had the ritual fresh.
   Partial credit is not compliance.
4. **My mental model of the gate's blast radius was wrong until I read the
   script**: I first grepped only docs/status/ and README for citations,
   by habit; the gate actually scans docs/status/*.md + TODO_LIST.md +
   AGENTS.md. No actual miss resulted (the script enumerates its own scope
   and the gate run surfaced all 13 lines), but I was one grep away from
   judging the finding on an incomplete picture. Reading the gate BEFORE
   interpreting its output is what corrected it.
5. **I nearly re-shipped the exact defect class I was fixing, inside the
   fix's own close-out**: the first draft of this report carried two BARE
   cites of the dead daemon commit (§e6 and §f16 as first written) — the
   post-report battery caught them rc=1 before the commit — and the §e7
   bloat metric was misread (572 raw date-rows taken as live; the true
   live count is 227). I had the hazard explicitly in mind (§d1) and
   still shipped two of three mentions unarrowed. The §e1 sequence —
   battery on the post-report tree, BEFORE the commit — is the only
   reason this report did not hand the next gate run its own residue.

## e) WHAT WE SHOULD IMPROVE

1. **The structural hole behind all THREE of today's residue classes: every
   close-out's battery runs BEFORE its own report exists.** The 05-47
   window ran check-doc-refs.sh rc=0 at its HEAD — then its close-out file
   landed citing a51585e1, and the NEXT gate run went red. Same shape for
   05-19 and 05-07. The battery that matters is the POST-REPORT tree. This
   window ran that sequence (§a8) and it is the single convention I would
   codify first: "the close-out battery re-runs after the report file is
   written, before the commit." First outing of the sequence in THIS
   window: the first post-report gate run FAILED rc=1 on my own draft —
   two bare a483fdbe→f48b4312 cites (§e6/§f16 as first written) — plus
   the §e7
   bloat number was misread (572 total vs 227 live). All four fixed
   before the commit. The battery demonstrably works; §d5 owns the
   near-miss.
2. **Amend-time baseline-append should be mechanical, not remembered**: every
   AMEND MANEUVER manufactures a dead side; the 04-43 e6/f6 rule ("each
   rewrite window leaves the gate as green as it found it") should name the
   concrete action — append the dead SHA to
   scripts/dead-sha-baseline.txt in the SAME amended commit, or arrow-form
   every cite of it. Today compliance relied on the next gate run or the
   next reviewer.
3. **The baseline file's format constraints deserve an explicit header
   sentence**: `in_baseline` matches EXACT full-line equality and skips
   only `^#` lines — an inline `sha # comment` would silently never match,
   and a 7-hex prefix of an 8-hex cite silently never matches. One sentence
   in the file header prevents the next hand-edit from breaking the gate
   invisibly.
4. **Reviewer findings should paste the failing gate's output** (rc + the
   DEAD SHA lines, or at least the count): this finding's text was
   excellent — it even pre-flagged eb48b416 — but the full 13-line reality
   including a51585e1 had to be re-derived by running the gate. The paste
   would have made the §b1 scope judgment unnecessary.
5. **The gate's verbose mode could ride ci-local as a non-gating INFO
   line**: TQ_DEAD_SHA_VERBOSE=1 already prints baselined hits; surfacing
   the count in ci-local would make the baseline-growth trajectory visible
   without ever turning the gate red.
6. **Uncited dead SHAs are invisible to the gate by design** (a483fdbe→f48b4312
   today): a SHA nobody cites has no reader to mislead, so the scope is
   right — but it deserves a conscious owner nod (§f7) so nobody later
   "fixes" it by scanning unreachable objects and flooding the baseline.
7. **Index bloat keeps worsening**: 227 live rows against the 100-row
   threshold (the warning fired during this window's runs; 572 is the
   raw date-row count including archived rows — my first draft misread
   the 572 as live). The archive sweep stays unbuilt while the nudge
   fires on every gate run.

## f) NEXT (from this window's surface only; queue gates decide when)

1. Codify "close-out battery runs on the POST-REPORT tree" in AGENTS.md
   (§e1 — the root cause of all three residue classes shipped today).
2. Codify "AMEND MANEUVER appends the dead-side SHA to
   scripts/dead-sha-baseline.txt (or arrow-forms its cites) in the same
   amended commit" in AGENTS.md (§e2).
3. Add the format-constraints sentence (exact full-line match, no inline
   comments, exact token length) to dead-sha-baseline.txt's header (§e3).
4. Owner ruling: dead-side citation policy for future reports — cite only
   reachable twins and describe the dead side in prose (baseline stops
   growing), vs today's dead-hex forensics + baseline rows (§g1).
5. Surface TQ_DEAD_SHA_VERBOSE=1 counts as a non-gating INFO line in
   ci-local so baseline growth is observable (§e5).
6. Reviewer-finding template: include the failing gate's rc + output
   excerpt (§e4).
7. Conscious owner nod that uncited dead SHAs are outside the gate's scope
   by design (§e6).
8. Archive sweep for the 227-live-row status index (existing bloat
   warning; §e7).
9. ci-local end-to-end on the ~121-commit unpushed lineage (existing row
   114 from the 05-47 report).
10. Master-CI-state check convention for docs-only windows (§g3; split
    across 04-43 / 05-19 / 05-47 / this window).
11. Hook enforcement for footer placement (existing row 112 — still open,
    still the top self-inflicted failure class per the 05-47 §d1).
12. A `--baseline-add <sha>` helper mode for check-dead-sha-refs.sh that
    appends a SHA with the correct comment skeleton, shrinking the
    hand-edit mistake surface (adjacent to §e3).
13. Verify once that the dead-SHA gate is actually wired into ci-local via
    check-doc-refs.sh (the finding calls it a "hard ci-local gate"; I ran
    check-doc-refs.sh directly and saw the gate inside it — confirm the
    ci-local call chain so the "hard" claim stays true).
14. Pin the exact-token lesson in the gate's --self-test: a fixture cite at
    8-hex with only a 7-hex baseline row must still FAIL, so the exact-form
    requirement cannot silently loosen.
15. docs-health pass: annotate the 05-07 report's DONE row with the
    rejection + fix cross-reference (f48b4312) so the report → review →
    fix lineage is discoverable from the original.
16. a483fdbe→f48b4312: no action needed (uncited, invisible by design);
    recorded here so the next dead-SHA audit does not rediscover it as a
    mystery.

## g) QUESTIONS (owner-only; I could not settle these myself)

1. **Dead-side citation policy**: should future reports stop citing dead
   hexes entirely — describe the dead side in prose, cite the reachable
   twin (baseline stops growing), or keep today's dead-side forensics
   prose + baseline rows (history reads naturally, baseline grows)? This
   window chose baseline rows to match precedent; the finding itself
   offered both options without ruling the general case.
2. **Convention vs hook for the two codifications** (§f1 + §f2): shall I
   draft both AGENTS.md paragraphs in one follow-up window, or do you want
   them hook/check-enforced from the start (e.g. a check that fails when a
   commit's parentage implies an amend with no baseline delta)?
3. **Master-CI-state check for docs-only windows**: 04-43 checked gh before
   stopping; 05-19, 05-47 and this window skipped it. Is the check owed on
   docs-only fix/close-out windows, or only on code windows?
