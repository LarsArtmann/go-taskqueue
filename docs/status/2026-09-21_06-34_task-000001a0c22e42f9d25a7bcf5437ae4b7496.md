# Task 000001a0c22e42f9d25a7bcf5437ae4b7496 — docs-only score-cache CHANGELOG/FEATURES row (attempt 3, brutal a-g close-out)

Full a)-g) close-out, 2026-09-21 06:34 CEST. THIRD window on this ID, first
full report for attempts 2-3 (attempt 2 left only its TODO-row note + commit
2a9a6e9 — no report file, per the compact repeat-dispatch shape). Verify-only
repeat dispatch: the work item's "verify entries exist, append if missing"
resolved to "exist" — nothing needed appending. Dispatch tree clean at HEAD
2a9a6e9; turn-1 ritual complete: session-start.sh WITH the task ID as the
first action, git log/status/stash clean, prior-report grep found the 06-20
report (attempt 1) → repeat-dispatch status known from turn 1,
CONTRIBUTING.md read IN FULL (heals attempt 1's §b2 head-only miss),
CLAUDE.md verified absent at turn 1 (`ls` rc=2, only CONTRIBUTING.md).
docs-health skill loaded before acting (task matched its trigger). The
"do not research unrelated stuff" constraint was honored: primary sources
were the 06-20 report, the TODO rows 165-170 visible in passing, and the
status-index tail for the commit mechanics.

## a) FULLY DONE

- **Both entries confirmed present at HEAD** — the verify branch of the item,
  discharged first-hand: CHANGELOG `[Unreleased]/Added` score-cache
  aging+eviction block starting CHANGELOG.md:15 (fresh grep hits at
  :15/:16/:17/:25 — aging DefaultScoreTTL 30d, ScoreTTL negative-disables,
  pruneCache orphan eviction, SweepStats.CachePruned, store surface, file
  refs) and the FEATURES AI batch scorer row FEATURES.md:74 with the same
  three facts appended to the ORIGINAL row (extended, not duplicated —
  status column still 🟡 PARTIALLY_FUNCTIONAL, unchanged since 2499a23).
  Zero source delta needed; "append if missing" had nothing to append.
- **Code facts re-cited by grep at HEAD** (cite-by-grep held; every number
  below came from this window's rg output, not memory): DefaultScoreTTL =
  30*24*time.Hour at internal/prioritize/sweep.go:78; ScoreTTL
  zero-selects-default / negative-disables semantics at :102;
  pruneCache invoked at scorer completion :323, defined :559 (comment
  :551); SweepStats.CachePruned :129, incremented :593; store surface
  PriorityScores queue.go:189 and DeletePriorityScores queue.go:195 —
  all consistent with the docs text being verified.
- **Fresh battery at HEAD, rc captured directly** (redirect-to-file + `$?`,
  no PIPESTATUS reliance): check-doc-refs rc=0, check-todo-list rc=0,
  check-features-roadmap rc=0, root `go build ./...` rc=0, `go vet ./...`
  rc=0 — all under GOEXPERIMENT=jsonv2.
- **Tests waiver STATED this time.** Attempt 1's §b1/§d2 defect (silent
  go-test waiver on a docs-only delta) is healed: the waiver is written in
  the stop message, the commit message, and the TODO note ("tests waived,
  docs-only delta"). The delta was one TODO_LIST.md line; zero Go compiled.
- **Fresh delta produced** (the verify-window minimum battery's requirement):
  the row-167 attempt-3 re-verify note with the re-cited file:line set.
- **Footer commit discipline held**: 2774349 carries exactly ONE
  `Task-Queue-ID:` trailer, verified via `git log --format='%(trailers)'`
  BEFORE any race-outcome or attribution prose was written; staged exactly
  TODO_LIST.md; tree clean after; no daemon fold (edit→commit gap was
  seconds, single-chain shape per 06-20 §e3).
- **Attempt 2's shape followed**: TODO-note-only amendment, no duplicate
  report file — consistent with how the 09-35/09-40 family handled
  verify-only re-dispatches before owner-mandated reports.

## b) PARTIALLY DONE

- **Battery parity with attempt 2 was not exact**: attempt 2 ran six gates
  (including tracked-files gofmt); this window ran five — gofmt dropped
  because the delta is markdown-only. Defensible, but it was not a
  CONSCIOUS, stated deviation at stop time: the docs-only battery remains
  uncodified (06-20 §e2 open), so every window improvises its own gate set
  and parity is luck. §f2.
- **`tq show` of the task record: not run** (see §d1 — this belongs to the
  fucked-up section, not here, but the honest framing is: the "verify" was
  complete for the WORK ITEM and incomplete for the DISPATCH context).
- **Expensive tier consciously skipped, disclosed**: no ci-local, no nix
  build, no race suite, no smokes. Correct for a one-line markdown delta;
  stated here so the waiver is procedure, not omission.
- **go test not run** — waived with an explicit clause (see §a); the
  disclosure is complete, the underlying judgment (markdown-only ⇒ no test
  tier) still awaits codification so it stops being per-window judgment.

## c) NOT STARTED

- **Nothing owned by this work item remains.** Row 167 was already `[x]`
  with two prior verification notes; entries existed; gates green.
- Neighboring rows deliberately untouched (contract: exactly this item):
  TODO_LIST.md:165 usage-key pin parity (BLOCKED on owner granularity
  ruling), :168 e2e wire pin for derived usage in `tq show`, :169
  CachePruned log line, :170 SessionMessageCount render/ignore decision.
- Carried owner rulings (TTL value; postgres truth-holder) — not
  executable by a window; carried to §g.

## d) TOTALLY FUCKED UP

1. **The tq-show gap: SIXTH recurrence, and this one is indefensible.** The
   06-20 report I read at turn 1 names this exact miss "the longest-running
   behavioral bug in the repo's agent fleet" at its FIFTH recurrence — and I
   still never ran `tq show 000001a0c22e42f9…`. The queue record (attempt
   count, the verify tail showing attempt 1 died on the row-203 vendor/gofmt
   structural gate, NotBefore state) was never consulted; I reconstructed
   the attempt history from the TODO note and git log instead. Reading the
   lesson did not transfer it into behavior. This is now a tooling demand,
   not a memory demand (§e1): session-start.sh must print the queue record
   for dispatched IDs.
2. **Three consecutive tool-form failures before the first successful
   commit chain**: (i) the TODO-row edit was REFUSED because I had only
   grepped TODO_LIST.md and not Viewed it — read-before-edit is rule #1 and
   I lean on grep for giant single-line rows; (ii) mvdan/sh cannot parse
   heredoc-in-command-substitution (`$(cat <<'EOF' …)`) — the commit
   message form the git-workflow playbook itself suggests fails here;
   recovered via write + `git commit -F`; (iii) unquoted
   `--format=%(trailers)` parsed as shell syntax — must be
   `'%(trailers)'`. Zero damage, three wasted round trips, but (ii)/(iii)
   are fresh concrete instances for the AGENTS.md mvdan quoting lesson,
   which currently documents the GIT_EDITOR mangling class only (§f15).
3. **Attempt-2 artifacts were not read first-hand at turn 1.** I read the
   06-20 report but reconstructed attempt 2 from the TODO note + `git show
   --stat 2a9a6e9` only after the battery. The sibling doctrine (05-51/
   06-02) says read ALL prior reports for the ID at turn 1; attempt 2 has
   no report (its record IS the note + commit), so the miss is minor and
   disclosed rather than material — but the CHECK ("is there an attempt-2
   report?") should have been explicit at turn 1, not inferred later.
4. **No daemon-fold hit, no false race prose** — recorded because both are
   the family's recurring §d leaders: the single-chain commit ran before
   any claim about race outcome, and the trailer check preceded attribution
   prose. Doctrine held this window; it must keep holding on every window.

## e) WHAT WE SHOULD IMPROVE

1. **tq show in session-start.sh** — sixth datapoint; prose has failed six
   times, tooling has never been tried. One script line, permanent fix
   (carried from 06-20 §e1 with escalated evidence).
2. **Codify the docs-only battery** (AGENTS.md clause next to the
   verify-window minimum battery): named gate list (doc-refs, todo-list,
   features/roadmap when those files are in the delta, root build+vet under
   jsonv2) + the written-waiver clause for tests. This window's silent
   gofmt drop shows what improvisation costs (carried from 06-20 §e2).
3. **mvdan/sh quoting lesson needs two fresh entries**: commit messages via
   message FILE (`git commit -F`), never heredoc-in-command-substitution;
   quote `%(trailers)`-style git format strings. One-line AGENTS.md
   amendment beside the scripted-commands paragraph (§f15).
4. **View before edit, even for appends to giant rows** — the tool guard
   caught what discipline missed; grep output is not a read.
5. **Attempt-note accretion on DONE rows**: row 167 now carries three notes
   on one line (~1900 chars). Still parseable (check-todo-list rc=0), but
   each re-dispatch makes it worse. Decide append-vs-replace (§g3); if
   replace, reports + commits carry the history.
6. **Single-invocation write+commit chain** (06-20 §e3 doctrine) held for
   the work commit and is used again for THIS report+index+commit — keep it
   as the default for every footer-carrying commit.

## f) WHAT WE SHOULD GET DONE NEXT

Honest list; padding to 50 refused (house style since 06-20). Carried rows
cite their current TODO_LIST line where visible this window.

1. `tq show <task-id>` wired into session-start.sh (§e1; closes the
   six-recurrence class).
2. Docs-only battery codified in AGENTS.md (§e2).
3. TODO_LIST.md:169 — surface `SweepStats.CachePruned` in the cmd/tq
   prioritize sweep log line (three-line change; 05-39 §f2).
4. Sibling docs-only row from 06-20 §f7 (usage-rendering CHANGELOG/FEATURES
   entries, 04-07 f4) — same shape as this ID's work; line number drifted
   since 06-20, locate by content at execution.
5. TODO_LIST.md:165 — usage-key pin parity, BLOCKED on owner granularity
   ruling (04-40 §g1).
6. TODO_LIST.md:168 — e2e wire pin for derived usage in `tq show` JSON.
7. TODO_LIST.md:170 — SessionMessageCount render/ignore decision
   (messages>0 with zero tokens/cost renders no usage line).
8. TODO_LIST.md:171 — pkg.go.dev render check for v0.3.0 facades
   (persistent 404 = investigate crawl lag vs blocker).
9. TODO_LIST.md:174 — enable `--prioritize` on the dogfood pool, gated on
   the one-batch live cost measurement (owner go/no-go).
10. Row-203 vendor/gofmt structural gate fix (OWNER-ONLY): scope
    `gofmt -l` to tracked files in `.tq-verify` + the mint template — five+
    dead tasks since 09-20, attempt 1 of THIS ID among them (AGENTS.md
    documents the bug; the fix is still pending).
11. OWNER RULING (4th+ ask, now with a docs-freshness stake): is 30d the
    right DefaultScoreTTL? The surfaces this row verified EMBED the number
    (CHANGELOG.md:15ff, FEATURES.md:74) — a ruling change needs a same-day
    docs edit plus a note here.
12. OWNER RULING (carried): postgres conformance truth-holder for the
    "priority score cache list and prune" subtest — force a local
    TQ_TEST_POSTGRES run (06-02 §b documented the concrete blocker: socket
    up, no role, container-only DSN) or accept CI's env-gated run.
13. ADR-0015 amendment documenting aging + eviction (the score cache now
    has TTL semantics the ADR predates; carried from 05-43 §f).
14. Rulings ledger (one row per open ruling, ask-count, stake) to stop the
    ask inflation visible in §g1/§g2 (carried from 06-20 §e4).
15. AGENTS.md mvdan/sh note: add this window's two fresh parse failures —
    heredoc commit messages → `git commit -F`; quote `%(trailers)`
    (§e3).
16. Row-167 note-accretion convention (§e5, §g3): append vs replace for
    future re-verify notes on DONE rows.
17. Daemon-fold class remains open repo-wide (sub-minute latency losses in
    sibling windows; zero hits this window) — carried by pointer, no new
    evidence beyond "the single-chain shape held".

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **(4th+ ask)** Is 30d the right `DefaultScoreTTL`? Stake sharpened: the
   docs THIS row verified embed the number; a ruling change is a same-day
   docs edit on CHANGELOG.md:15ff + FEATURES.md:74 and a fifth note on row
   167.
2. **(carried)** For the score-cache prune subtest ("priority score cache
   list and prune"): is CI's env-gated postgres run the truth-holder, or
   must a local TQ_TEST_POSTGRES run happen before the next release (06-02
   §b's concrete blocker stands: socket up, no role, container-only DSN)?
3. **(new)** For verify-only repeat dispatches on DONE rows: should the
   attempt note REPLACE the previous note on the TODO row (one note per
   row; history lives in reports and commits) instead of appending? Row 167
   is three notes deep; check-todo-list still passes, but the trend is
   unreadable-line-ward and only the owner owns the file's convention.
