# tq enqueue --wait — THIRD dispatch, verification-only lap (no-op)

- **Recorded:** 2026-09-15 (the task itself and its prior laps below date to 2026-09-11…14)
- **Task:** `000001a0a2df535461f8abc12a16b7320289` (TODO_LIST row 84, "Docs-health harvest (2026-09-11 evening…)" section)
- **Dispatch:** third; work was already SHIPPED on the first dispatch (commit 5c2b4ec, daemon-split twin dec6a7f) and closed out twice (c2ea4a4, b28a266/a2ff38a)
- **Date:** 2026-09-15 04:40 UTC
- **Verdict:** NO-OP VERIFICATION LAP — deliverable re-verified green at HEAD `a2ff38a`; nothing to change, no code commit made.

## 0. What this lap did (and only did)

1. Session-start ritual: `git log --oneline -3`, `git status --short` (clean), located row 84 in TODO_LIST.md — already `[x]`.
2. Confirmed the deliverable exists at HEAD: `--timeout` flag (cmd/tq/main.go:225), `--wait` dispatch path (main.go:325 → `waitForTask`, main.go:331-362).
3. Root gate: `GOEXPERIMENT=jsonv2 go build ./... && go vet ./... && go test ./... -count=1` — ALL GREEN.
4. cmd/tq module gate: `./scripts/test-cmd-tq.sh` — ok, 10.118s. (First attempt at `go test ./cmd/tq` from root failed with the EXPECTED ADR-0017 "main module does not contain package" error — that is by design for a replace-free sub-module, not a failure.)
5. Wrote this report + index row; committed with the task footer.

No working-tree changes to source, docs, or config were needed. The prior laps' evidence stands.

## a) FULLY DONE

- `tq enqueue --wait [--timeout]`: blocking run streaming the task's facts to the terminal (`waitForTask`: 250 ms poll over `FactsForTask` seq-dedup, terminal via `task.Terminal`; exit 0 only on `completed`, non-zero on dead/cancelled/timeout). Commit 5c2b4ec (+ dec6a7f daemon split).
- Tests: `TestCmdEnqueueWaitStreamsFacts` (completed / dead / timeout subtests) — passing in this lap's 10.118s gate run.
- TODO_LIST row 84 `[x]`, status-report index rows current through the 04-35 close-out.
- This lap's verification: root build/vet/test + cmd/tq shim gate, both green at HEAD.

## b) PARTIALLY DONE

- Nothing attributable to this lap. Carried (documented in the 04-27/04-31/04-35 reports, still open): stdout fact-stream format is not pinned by a test; AGENTS.md / FEATURES.md do not yet mention the `--wait` flag.

## c) NOT STARTED

- Nothing in this item's scope. (Carried backlog below, §f.)

## d) TOTALLY FUCKED UP (this lap)

- This is now the THIRD paid dispatch against a row that was DONE and closed out twice before. The queue (or its operator) keeps re-minting the same verified item; each lap costs a full report + index row + commit. The mint-side gap (enqueue should check the [x] row and its index before paying an agent) remains the single highest-leverage fix — flagged in all three prior reports, still not implemented.
- Minor, this lap only: no wasted round trips, no gate missteps, no daemon race. Clean lap.

## e) WHAT WE SHOULD IMPROVE

1. **Mint-time done-check** — `tq enqueue`/harvest should refuse (or cheaply no-op) when the TODO_LIST row is `[x]` and an indexed close-out report exists. Five+ paid laps on this repo have now been no-ops.
2. **Report-mill damping** — the §f lists from previous laps partially overlap; harvest them into TODO_LIST once instead of re-listing per lap.
3. Verify-gate breadth on no-op laps: this lap ran only the root + cmd/tq gates; acceptable for a no-op, but a "no-op lap" profile (build + targeted test only) could be codified to cut cost further.

## f) Next things to get done (up to 50; leaders first — most carried from prior laps, all still open)

1. Mint-time done-check: enqueue/harvest no-ops an already-checked TODO_LIST row (+ indexed close-out) instead of paying a lap.
2. Pin the `--wait` stdout fact-stream format with a test (golden line format).
3. Document `--wait`/`--timeout` in AGENTS.md command table + FEATURES.md.
4. Harvest the carried §f items from the 04-27 report into TODO_LIST.md (they were listed, never harvested).
5. `TQ_CMD_TQ_TESTFLAGS` pass-through for cmd/tq gate test selection.
6. Windows lap for the cmd/tq gate (`CMD_TQ_OS=windows` cross-compile check).
7. Rule on `--timeout` default (0 = forever vs a sane bounded default).
8. Rule on cancelled-task exit code for `--wait` (distinct code vs generic non-zero).
9. Healing play for daemon-split commit pairs (5c2b4ec/dec6a7f class) — docs + optional tooling.
10. Codify the "no-op verification lap" gate profile (build + targeted tests, skip full suite).
11. Policy note: whether a no-op lap needs a full report + index row or a one-line annotation suffices.
12. Sweep `docs/status/` for fully-done reports and archive per the docs-health ANNOTATE conventions (archive counter + citation repointing).
13. v0.3.0 caveat: re-check whether the ADR-0017 raw-module trap note in AGENTS.md is stale now that a root tag containing the split has landed on the proxy (flagged by the 04-14 lap on the sibling task).
14. Unit test pinning the `$TQ_DB`-guardrail warning text (sibling task 000001a0a2d197b335, carried).
15. Backlog harvest of the sibling task's §f list (also unharvested).

16-50 (ROADMAP-grade, one line each, carried or observational — do NOT treat as commitments):
16. `tq enqueue --wait --json` structured output for cron consumers.
17. `--wait` follow mode (`--follow` past terminal into requeues).
18. Fact-stream filtering flags for `--wait` (status-only, evidence-only).
19. Rate-limit-aware `--wait` (surface `RateLimitError` reset time in the stream).
20. `tq enqueue --wait` integration smoke script (hermetic, `TQ_DB` scratch).
21. Batch `--wait` (wait on N ids, exit on first failure).
22. Document `--wait` in README quick-start cron example (replaces the tail+grep dance the row complained about).
23. `tq wait <task-id>` as a standalone subcommand (wait without enqueue).
24. Consider event-driven `--wait` via the journal consumer instead of 250 ms polling (perf, low priority).
25. Pin poll interval as a flag/test constant.
26. Ensure `waitForTask` respects SIGINT cleanly (context cancel → non-zero, no goroutine leak).
27. Add `--wait` to `tq` shell-completion surfaces if any exist.
28. Cross-check `--wait` exit codes against DLQ-rescue flows in docs.
29. WebUI: deep-link from `--wait` terminal output to the detail page.
30. `tq facts` + `--wait` output-format parity check.
31-50: placeholder capacity per the a-g contract — intentionally unused rather than padded with filler; the ten genuinely-open items above are the real backlog.

## g) Questions I cannot figure out myself

1. **Who/what re-mints this task ID?** Five laps on one verified row suggests a queue-side dedup or operator loop; is the re-dispatch intentional (e.g. verify-lap policy) or a mint bug? Answer changes whether fix #1 or #11 is the right lever.
2. **`--timeout` default ruling (asked twice):** should 0 remain "wait forever", or should cron ergonomics favor a bounded default (e.g. 1h) with explicit opt-out?
3. **No-op-lap reporting policy:** should a verification-only lap write a full report + index row (current practice, now three deep), or should it be a one-line annotation on the existing close-out?

— END OF REPORT —
