# Repeat-dispatch root-cause window: row 95 re-dispatch, attempt 2

- **Task**: 000001a0c698f12f76176d0ecf682956927f
- **Window**: 2026-09-22 ~11:41–11:55 CEST
- **TODO row**: 95 — identity-blind one-liner near the smoke list (03-46 §f5) — `[x]` since 03:05
- **Lineage**: minted 02:51:47 (fact seq 5477); the 03-05 window was attempt 1 (claim
  02:58:20, seq 5482) — it shipped the deliverable, closed out, and then **failed the
  worker verify at 03:10:28 (seq 5483, attempt 1 burned)**; ~35 consecutive
  preflight-dirty requeue cycles followed (seq 5485–5684, 03:12–11:27); claimed
  11:40:55 (seq 5688) = **this window, attempt 2 of 3**. This is the FIRST window for
  this task id whose actual job is the re-dispatch itself — the 03-05 report closed
  the row but the task record never completed.

**Verdict: work DONE (verified again at HEAD); re-dispatch ROOT-CAUSED; task-record
completion blocked by two owner-side gate defects, both documented with primary
evidence below.**

## a) FULLY DONE

1. **Turn-1 repeat-dispatch detection**: grep for the task id hit the 03-05 close-out,
   its index row, and TODO row 95 `[x]` — the verify→cite→stop protocol engaged before
   anything else.
2. **Deliverable verified intact at HEAD**: AGENTS.md:98 carries the identity-blind
   one-liner with all three load-bearing facts (exact env, bare-/dev/null-inert caveat,
   self-test fixture pointer, never-simplify warning); TODO_LIST.md:95 `[x]` with the
   DONE annotation.
3. **Wiring verified first-hand, this window**: the sanitizer env wraps the smoke
   invocation in ci-local.sh:265-267, and release-gates.sh:42-57 self-tests the
   property with the stripped-commit fixture ("identity unknown").
4. **Release-gates smoke re-run at HEAD under the EXACT ci-local sanitizer env**:
   rc=0, 12/12 ok lines — fresh delta (the prior run was 03:05 at 575e0c6; 15+ daemon
   commits have landed since).
5. **Re-dispatch root-caused from primary evidence** (this window's real work):
   - The 03:10:28 `task.failed` (seq 5483) is NOT the gofmt stage and NOT a test
     failure. The full verify-failure log
     (`~/.local/state/tq/logs/000001a0c698f12f76176d0ecf682956927f.verify-failure.log`,
     24 lines) shows `build tq: exit status 1` + `write /home/lars/.cache/go-build/…:
     no space left on device` → `FAIL internal/e2e 21.315s`. **Attempt 1 died on
     transient host disk pressure** (ENOSPC on the go build cache), mid-e2e-build.
   - The failure TAIL (last 8 lines, carried in FailureEvidence) shows only ok lines +
     bare FAIL — the FAIL-e2e and ENOSPC lines sit ABOVE the tail window. The
     AGENTS.md attribution heuristic ("all-ok tail = the vendor-gofmt bug") is a
     FALSE POSITIVE on this log.
   - Disk now: `~/.cache` 84% used, 121G free; `/mnt/buildcache` 16% — the 03:10 full
     condition has passed (transient burst, consistent with the 03-37 §d ENOSPC
     toolchain corruption and the 11-30 §g2 GOCACHE-relocation question).
6. **internal/e2e under -race re-run in isolation at HEAD**: rc=0, 27.1s — proves the
   03:10 e2e FAIL was environmental, not a code regression and not a deterministic
   e2e defect.
7. **Live confirmation that the gofmt stage is STILL structurally red**: `gofmt -l .`
   from the repo root lists 44 files, 100% under `vendor/`, 0 tracked files — row 123's
   defect is live at HEAD, so the verify chain's LAST stage fails deterministically
   once the earlier stages pass.
8. **Battery at HEAD, every rc captured directly** (PIPESTATUS rule): root build rc=0;
   root vet rc=0 (both GOEXPERIMENT=jsonv2 + GOTOOLCHAIN=auto); check-doc-refs.sh
   rc=0; check-todo-list.sh rc=0; check-status-index.sh rc=0.
9. **Loop closed**: this report + index row + three follow-up rows appended to
   TODO_LIST.md; committed with the task footer.

## b) PARTIALLY DONE

- The **work item** is 100% done (prior window) and now re-verified (this window).
  The **task record** cannot complete: the minted verify chain ends in a structurally
  red gofmt stage (row 123, BLOCKED on owner ruling — agents never edit their own
  gate). **Prediction on record**: this attempt's close-out verify will pass
  build/vet/race (all re-proven green at HEAD minutes ago) and die at the gofmt stage
  with an all-ok tail. When that happens: on current evidence that death is the
  row-123 defect, NOT a work defect — but per §d2/e1, attribute it from the FULL log
  grep, not the tail shape. Attempt 3 would die the same way → dead 3/3 → DLQ autopsy
  (the designed human surface; row 124's forensic sweep is the classification pass).

## c) NOT STARTED (deliberate scope-keeping)

1. No code or doc changes beyond this report + TODO rows: the deliverable pre-existed;
   smallest-correct-change wins.
2. Row 123 (gofmt gate fix), row 124 (dead-letter forensic sweep), row 226 (stage
   capture in verify-failure logs): queued, explicitly owner/tooling-side, untouched.
3. The three NEW follow-up rows appended this window (§f3–f5): future windows' work.
4. The 11-30 brutal-self-review's 50-row list and the 03-37 §f list: not re-triaged
   here (no unrelated research).

## d) TOTALLY FUCKED UP

1. **Turn-1 shallowness — the big one.** My first pass this session declared the
   re-dispatch "verified, nothing remains" and stopped WITHOUT ever running `tq show`
   on my own task. I treated the dispatch as routine queue noise while the fact trail
   held a burned attempt, an ENOSPC gate death, and an 8.5-hour starvation loop. The
   DONE-row protocol (verify → cite → stop) is right about the WORK ITEM, but a
   re-dispatch deserves one `tq show` deep: WHY does this dispatch exist at all?
   Only the user's "what did you forget?" prompt forced that depth.
2. **Heuristic before evidence.** On first seeing the verify tail I attributed the
   attempt-1 death to the gofmt stage — the AGENTS.md shape-match — before reading
   the full log. The log then showed an ENOSPC death mid-log. I corrected within one
   tool call, but the reflex was pattern-matching, not evidence, and the repo's own
   attribution rule has now been PROVEN to false-positive (this log is the counterexample).
3. Carried from the prior window's §d (mine to not repeat): sed-read instead of
   View-before-edit; ritual rc swallowed by a tail pipe; multi-file `tail -N` quirk
   in the session shell.

## e) WHAT WE SHOULD IMPROVE

1. **Gate-death attribution must grep the FULL verify-failure log** (FAIL / panic /
   DATA RACE / ENOSPC lines), never the tail shape alone. Fix the AGENTS.md
   known-issue sentence (new row §f3) and land row 226's stage capture — this log is
   the proof both need.
2. **FailureEvidence tail is too small**: 8 lines hid BOTH the `FAIL internal/e2e`
   line AND the ENOSPC cause line. Enlarge the tail or attach the failing stage's
   output (extends row 226; new row §f5).
3. **Preflight-dirty starvation on a hot tree**: ~35 futile claim→preflight-refuse
   cycles over 8.5h (seq 5485–5684) because require_clean refuses while concurrent
   agents + the daemon keep the tree dirty. Needs a starvation counter/escalation or
   quiet-window claim mode (new row §f4).
4. **ENOSPC class is now a repeat gate-killer**: this attempt-1 death plus the 03-37
   toolchain-dir corruption plus the 11-30 misdiagnosis marathon all trace to host
   cache-mount pressure. The GOCACHE relocation ruling (11-30 §g2) would kill the class.
5. **Two-gate stack**: a task can survive the ENOSPC lottery only to die
   deterministically on gofmt-vendor (row 123). Both fixes are one edit each,
   both owner-side; together they unblock every go-taskqueue agent task minted
   against the root verify.

## f) Things we should get done next (prioritized; discoveries first, then carried — not padded)

1. **Owner: land row 123** — scope the gofmt stage to tracked files
   (`test -z "$(gofmt -l $(git ls-files '*.go'))"`) in `.tq-verify` AND the bootstrap
   mint template. One edit; unblocks this task's attempts and the whole dead-letter class.
2. **Owner: make the GOCACHE/disk relocation ruling** (11-30 §g2) — ENOSPC has now
   killed a second verify (this task's attempt 1, 03:10).
3. **NEW row (appended)**: fix the AGENTS.md vendor-gofmt attribution sentence —
   require a full-log grep before attributing any gate death; cite this task's
   attempt-1 ENOSPC counterexample.
4. **NEW row (appended)**: preflight-dirty starvation counter/escalation design for
   the pool (8.5h / ~35 cycles of futile requeues on task 000001a0c698).
5. **NEW row (appended)**: enlarge FailureEvidence tail / capture the failing stage's
   output (extends row 226 — the 8-line tail hid both the FAIL line and the cause).
6. Row 124 forensic sweep of the gofmt/dead-letter class — add this task to its
   input set (its death will be attempt 3 unless row 123 lands first).
7. Row 96: `tq show --commits` renders footer-less daemon commits as "folded here"
   (carried; 4426d7c/575e0c6 still the live case).
8. fold-marker.sh script (carried, TODO row exists).
9. session-start.sh prior-windows summary line (carried, TODO row exists).
10. AGENTS.md `tail -N` session-shell hazard note (carried, TODO row exists).
11. Sanitizer single-ownership fold into release-gates.sh (carried, ruling pending).
12. Known Issues entry for identity-blindness (carried, ruling pending).
13. CHANGELOG policy for docs-only changes (carried, ruling pending).
14. Row 97: bank the gochecknoglobals shrink in the next baseline regen (carried).
15. internal/e2e -race vs the 180s verify cap (carried) — upgraded by this window:
    27.1s solo pass proves the suite is fine in isolation; contention/host pressure
    is the killer, supporting an isolated-stage design.
16. Mint-time dedup for DONE-row re-dispatches (02-47 class) — this task is the
    newest live instance: the row was `[x]` at 03:05 and the task still re-dispatched
    at 11:40 (because the gate, not the harvester, refused to let it finish).

## g) Questions I cannot figure out myself

1. **Row 123 timing**: will you land the gofmt gate fix before this task's attempt 3
   runs? Every stage before it is now proven green at HEAD; the deterministic gofmt
   death is the ONLY thing between this task and completion (or between it and the
   DLQ autopsy cycle).
2. **Disk ruling**: is the GOCACHE/cache-mount relocation ruling (11-30 §g2) ready to
   make? Two gate deaths now trace to transient host disk pressure; the fix is
   environmental, not code.
3. **Starvation policy**: is the preflight-dirty requeue loop acceptable by design on
   a hot multi-agent tree, or do you want the pool to escalate (papdashboard alert
   after N consecutive dirty refusals on one task, or a quiet-window claim mode)
   before agents burn half a day in requeue limbo?

## Verification battery (fresh, this window, at HEAD 2280270)

| Gate                | Command                                                                                                                                       | Result                                                                |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| Build               | `GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto go build ./...`                                                                                         | rc=0                                                                  |
| Vet                 | `GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto go vet ./...`                                                                                           | rc=0                                                                  |
| Doc refs            | `./scripts/check-doc-refs.sh`                                                                                                                 | rc=0                                                                  |
| TODO gate           | `./scripts/check-todo-list.sh`                                                                                                                | rc=0                                                                  |
| Status index        | `./scripts/check-status-index.sh`                                                                                                             | rc=0                                                                  |
| Release-gates smoke | `GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=user.useConfigOnly GIT_CONFIG_VALUE_0=true ./scripts/smoke/release-gates.sh` | rc=0, 12/12 ok                                                        |
| e2e solo            | `GOEXPERIMENT=jsonv2 GOTOOLCHAIN=auto go test ./internal/e2e -count=1 -race -timeout 180s`                                                    | rc=0, 27.1s                                                           |
| gofmt stage probe   | `gofmt -l .` (repo root)                                                                                                                      | 44 files, all `vendor/`, 0 tracked — stage structurally red (row 123) |
| Disk probe          | `df -h ~/.cache /mnt/buildcache`                                                                                                              | 84% (121G free) / 16% — the 03:10 ENOSPC condition has passed         |

Not re-run this window: the FULL root `-race` suite end-to-end (the worker's verify
owns it; e2e — the only package that failed at 03:10 — was run solo green, and the
remaining packages all printed ok in attempt 1's log at 03:10).

Commits: one footer commit this window (this report + README index row + TODO_LIST
follow-up section).
