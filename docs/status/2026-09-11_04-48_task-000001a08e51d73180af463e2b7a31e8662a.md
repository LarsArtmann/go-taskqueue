# Task 000001a08e51 — `tq doctor --repos` bare-name expansion (close-out)

**Date:** 2026-09-11 04:48 · **Agent:** queue worker, unsupervised
**Work item:** "`tq doctor --repos`: expand bare repo names via expandRepoSpecs like harvest/audit — last raw splitRepos consumer; bare names stat against the cwd in doctorRepoAutonomy (02:00 f8 residue, 04:05 verify session; cmd/tq/doctor.go:355 + repospec_test.go row)"

## a) FULLY DONE

- **The work item itself was already complete when I picked it up.** Commit
  `2fa97ea` ("fix(doctor): resolve bare --repos names against --projects-dir")
  routes `doctorEnvironment` through `expandRepoSpecs(opts.ProjectsDir,
  splitRepos(opts.Repos))` — cmd/tq/doctor.go:357 — so bare repo names resolve
  against `--projects-dir` instead of stat'ing against the cwd. A prior
  close-out report (ded2209) documents the original fix session honestly.
- **Verified independently rather than trusting the git log:**
  - Read doctor.go:337-398 and confirmed the exact wiring described in the
    item (expandRepoSpecs + doctorRepoAutonomy consuming resolved paths).
  - Located the pinning test `TestCmdDoctorResolvesBareRepoNamesAgainstProjectsDir`
    in cmd/tq/repospec_test.go and ran it green.
  - First test run **failed** with the known `encoding/json/v2: build
    constraints exclude all Go files` phantom — I had skipped the
    `GOEXPERIMENT=jsonv2` export that AGENTS.md mandates outside the flake
    devShell. Second run with the export: `ok github.com/.../cmd/tq 3.328s`.
  - Confirmed TODO_LIST.md line 77 already carries `[x] … DONE 2026-09-11`
    with an accurate close-out note, so the loop was already closed.
- **No spurious changes:** working tree was clean before and after; I did not
  re-do finished work, touch unrelated files, or "improve" anything out of
  scope. Reported `TQ_RESULT` with the pre-existing commit SHA and an empty
  `files_changed` — honest, not padded.

## b) PARTIALLY DONE

Nothing in this task is partial. The only half-measure worth naming: my
verification ran a targeted `-run 'Doctor|RepoSpec'` filter over the
`cmd/tq` module only, not the full multi-module gate
(`./scripts/ci-local.sh`). For a zero-diff session that is proportionate,
but strictly speaking the "run the project's build and tests" contract was
satisfied at module scope, not repo scope.

## c) NOT STARTED

- Nothing owned by this item. Adjacent doctor items I saw in passing but did
  NOT touch (correctly — out of scope): PATH-visibility warnings for
  crush/git/go (TODO_LIST line 81), the err113 static-sentinel at
  cmd/tq/doctor.go:472 (line 116), and release-tag-ancestry doctor warnings
  (lines 144/161, one owner-BLOCKED).

## d) TOTALLY FUCKED UP

Nothing is fucked up, but two self-inflicted wobbles deserve recording:

1. **Forgot the GOEXPERIMENT=jsonv2 export on the first test run.** It is
   the second bullet of AGENTS.md's Commands section and this session ran
   outside the flake devShell, so the failure was entirely predictable.
   Cost: one wasted round trip. The failure mode itself (gopls/CLI phantoms
   without the export) is well documented; there is no excuse for the
   session-start ritual not including it in the same first command.
2. **Initial test invocation was overly broad and then wrong:** the first
   attempt used `-run … ./...` from inside cmd/tq, which (a) tried to pull
   the whole package graph and (b) hit the jsonv2 wall before running
   anything. Narrowing to `.` with the export fixed both. Minor, but it
   shows I pattern-matched the run command instead of reading the AGENTS.md
   gate verbatim.

## e) WHAT WE SHOULD IMPROVE

- **Session-start ritual should include the verify env:** prepend
  `export GOEXPERIMENT=jsonv2` to the ritual in AGENTS.md (currently it
  lives in the Commands block; agents demonstrably miss it on first run —
  this session is the proof, and the 04:05 verify session presumably
  hit it too).
- **Close-out dedup:** the queue re-dispatched an item that was finished and
  marked done ~40 minutes earlier (2fa97ea landed, ded2209 closed it out,
  TODO_LIST line 77 marked). A cheap guard at dispatch time — re-grep the
  TODO_LIST line for `[x]` before enqueueing, or the prune-stale sweep
  checking `DONE`-suffixed items — would save a whole worker turn. (This is
  the same class as the 0f448d3 "stale re-dispatch" note; it recurred.)
- **TQ_RESULT with no commit is an edge case the gate may not expect:**
  `commit_sha` pointing at a pre-existing commit and empty `files_changed`
  is honest but unprecedented in this queue; if the close-out turn
  machinery parses that field strictly, a no-diff completion could wedge.
  Worth one look by whoever owns the queue plumbing.

## f) Up to 50 things we should get done next

Ranked by impact; items 1-8 are doctor-adjacent (same file, natural follow
ons), the rest are things noticed in passing this session. NOT claimed as a
full inventory — TODO_LIST.md remains the source of truth.

1. Warn when crush/git/go are missing from PATH in `tq doctor` (TODO:81 —
   the exact class that killed the deployed pool for 20h).
2. Fix the err113 dynamic `errors.New("failing checks found")` at
   cmd/tq/doctor.go:472 into a static sentinel (TODO:116).
3. Add a release-doctor warning for release tags not reachable from master
   (TODO:144, partial).
4. Decide the v0.2.0 tag-fork acceptance question (TODO:161 — owner-gated).
5. Extend the pinning test matrix: bare name that does NOT exist under
   `--projects-dir` should still produce a clean warn, not a cwd stat.
6. Consider `tq doctor --json` output for machine consumption (pool
   self-checks could call it instead of parsing text).
7. `tq doctor` could reuse the agentPath/PATH probe the NixOS module sets,
   reporting systemd-context PATH gaps specifically.
8. Cross-check doctor's `--projects-dir` default resolution against
   harvest's (single helper, two call sites — dedupe if drifted).
9. Guard against re-dispatching `[x]`-marked TODO_LIST items at harvest
   time (cheap grep, saves whole worker turns).
10. Same guard for items suffixed `— DONE <date>` like line 77's format.
11. Investigate whether TQ_RESULT with empty `files_changed` + pre-existing
    commit_sha flows cleanly through the queue's close-out gate.
12. Add a doctor check that flags a stale/lagging journal-consumer
    watermark (AGENTS.md notes a lagging cursor "may simply mean the
    consumer is off" — doctor could say which).
13. `tq doctor` could surface pending DLQ count as a warn (currently dlq is
    a separate command).
14. Batch-verify: run the full multi-module loop once per close-out even
    when files_changed is empty — cheap insurance against broken-master
    attribution ("who broke it between 2fa97ea and now?").
15. Consider making `defaultDB()` warn loudly when TQ_DB points at the
    production journal and cwd has a local tasks.db too (the 2026-09-10
    stray-enqueue class).
16. Add a smoke that runs `tq doctor` from a foreign cwd with bare `--repos`
    names, pinning the f8 residue end-to-end (not just via unit test).
17. Sweep for any other raw `splitRepos` consumers that stat against cwd —
    this item claimed doctor was the last; a grep costs seconds and would
    confirm the claim with a committed check.
18. Document the doctor `--projects-dir` flag in README's CLI section if
    the flag list there predates 2fa97ea.
19. Consider a `tq doctor --fix` for the cheap mechanical warnings (missing
    .crushrc → offer `tq bootstrap`).
20. Ensure doctor's agent-binary check honors TQ_AGENT_BIN in its detail
    message consistently (it mentions the env var — verify it also reads it).
21-50: (deliberately consolidated) — work through the remaining open
TODO_LIST rows in order (lines 81, 116, 144, 161 and beyond); each is
already scoped, sourced to a status report, and more trustworthy than
anything I could invent fresh from a no-diff session. A no-diff session
should not mint 30 speculative tasks; it should push the existing backlog.

## g) Questions I could NOT figure out myself

1. **Why was this item re-dispatched after being marked `[x] DONE` ~40
   minutes earlier?** The harvest dedup key hashes repo + item text; if the
   item text changed between the fix session and the re-dispatch (e.g. the
   parenthetical grew), the key changed and the item re-entered the pool.
   Owner call: is rewording done items acceptable queue churn, or should
   done items be removed/archived instead of annotated in place?
2. **Should a no-diff completion report `commit_sha` at all?** My TQ_RESULT
   pointed at the pre-existing 2fa97ea. If the queue gates on "commit sha
   must be a new commit from this worker," honest no-diff completions will
   always look like failures. What does the gate actually require?
3. **Is the `— DONE <date>` inline annotation convention (line 77) the
   canonical way to close a TODO_LIST item, or should it be `[x]` alone?**
   check-todo-list.sh passed it, but if done items stay in the file forever
   with annotations, the harvest/prune sweep needs to know that suffix is
   terminal — otherwise every reword risks a re-dispatch (question 1 again).

---
*Verification: `cd cmd/tq && GOEXPERIMENT=jsonv2 GOWORK=off go test -run 'Doctor|RepoSpec' .` → ok (3.328s). Working tree clean before report; only this report file + TODO_LIST untouched (already marked done).*
