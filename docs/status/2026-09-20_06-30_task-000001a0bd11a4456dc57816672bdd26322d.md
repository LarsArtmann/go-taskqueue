# Status report — task 000001a0bd11a4456dc57816672bdd26322d (2026-09-20 06:30)

**Window:** single work window, 06:2x–06:30 CEST. Work item: TODO_LIST.md row
"AGENTS.md Conventions one-liner: DONE-note citations may only be WRITTEN after
the cited gate's output is in hand" (02-38 report §d1/§e1).

## a) FULLY DONE

1. **AGENTS.md Conventions edit** — the claims-carry-citations bullet now carries
   the ORDER sentence: "The citation may only be WRITTEN after the cited gate's
   output is in hand — never cite a gate that is still running (the 2026-09-12
   02-38 window claimed 'real gate re-run green' for a background gate whose exit
   code had not been captured)." Exactly the one-liner the row asked for; no
   scope creep into the rest of the bullet.
2. **TODO_LIST.md closure** — row marked `[x]` with a DONE-suffix citing the
   edit, per the section's own convention (closed convention rows keep their
   text + verdict; e.g. the errorpage row).
3. **Verification gate** — `GOEXPERIMENT=jsonv2; go build ./... && go vet ./...`
   plus the internal test battery (15 packages ok, 0 FAIL) BEFORE writing any
   DONE-note citation — the very semantics the row pins were honored in-order.
4. **Commit with footer** — 4172f1e carries the task footer. The auto-commit
   daemon raced the edit (78e2768, footer-less heuristic fold of both files);
   healed via the documented amend maneuver: unpushed daemon commit amended in
   place with a proper message + footer (origin was 34+ commits behind, no fork
   possible). Tree clean after.
5. **Turn-1 ritual** — whole-repo `git log`/`git status`/`git stash list` +
   task-ID grep over docs/status/ (zero prior reports on this ID — first window)
   all executed before editing.

## b) PARTIALLY DONE

1. **Test gate depth** — ran the internal battery WITHOUT `-race` and skipped
   the cmd/tq gate (`scripts/test-cmd-tq.sh`), per-module loop, and ci-local.
   For a docs-only change (AGENTS.md + TODO_LIST.md) build+vet+tests is the
   proportionate gate, but the citation below says "tests", not "tests -race" —
   the row I just shipped would call that a hypothesis-shaped citation if it
   were load-bearing.
2. **CONTRIBUTING.md** — `ls` confirmed it exists (turn-1 step) but I did not
   READ it. The ritual requires reading it at turn 1. Fourth-plus recorded hit
   of this miss class (00-52 d4, 02-17 d3, and the 05-33 report's own §d lead).

## c) NOT STARTED

- Nothing from this task. The row was a one-sentence convention edit; no
  follow-ups were discovered that belong in the backlog (append-only grant not
  exercised — no NEW items, per contract nothing about THIS task's work).

## d) TOTALLY FUCKED UP

1. **CONTRIBUTING.md not read** (§b2) — the single real miss. Everything else
   held: no PIPESTATUS reliance (gates chained with `&&`, `$?` captured
   directly), no scratch fixtures in gated trees, no .crushrc/.tq-verify edits,
   footer written only after the gate output was in hand.
2. **Near-miss, caught in-flight:** the first TODO_LIST closure edit tried to
   DELETE the row (contract allows mark-or-remove; the section convention here
   is keep-with-DONE-suffix). The tool refused before damage; re-did it as
   `[x]` + DONE suffix. No wrong state ever landed.

## e) WHAT WE SHOULD IMPROVE

1. Read CONTRIBUTING.md mechanically at turn 1 — this is the Nth recorded
   recurrence; the session-start ritual script (TODO row, 2026-09-19 00-32)
   remains the structural fix.
2. State gate SCOPE in DONE citations ("build+vet+15 pkg tests, no -race")
   when the gate was narrowed — this report is itself the first application.
3. The daemon race → footer amend maneuver worked but is undocumented as a
   standard first choice for work windows (only the index-row variant is
   codified); a one-line AGENTS.md note would save the next window the
   deliberation.

## f) What to get done next (honest list — this task's blast radius is tiny, so items are drawn from what this window observed in passing; padding to 50 would be fiction)

1. Session-start ritual script (`scripts/session-start.sh`) — turns the
   recurring turn-1 misses (CONTRIBUTING.md, report grep) into structure.
   Already a TODO row (00-32); unchanged priority.
2. Codify the "amend the unpushed daemon commit to carry the footer" maneuver
   as the standard heal (one AGENTS.md sentence next to the existing amend
   maneuver).
3. Re-dispatch suppression queue-side — five prior reports on sibling IDs
   (06-04/06-07 etc.) show closed IDs getting re-fired; this window was the
   healthy counterpart (first dispatch, real work), but the guard is still
   missing.
4. Verify-window battery bar (TODO row 150, adjacent in the file) — pairs
   directly with the citation-order rule this task shipped.
5. (No further items from THIS window's observations — the change was one
   sentence; inventing 45 more would violate the no-padding ruling every prior
   report on these IDs has repeated.)

## g) Questions I cannot figure out myself

1. Should DONE-note citations from one-shot windows be REQUIRED to state gate
   scope (which gates, with/without -race), or is "the proportionate gate ran
   green" acceptable for docs-only changes? The new ORDER rule pins WHEN, not
   HOW MUCH.
2. Is the amend-the-daemon-commit heal owner-approved as the default for
   footer attribution, or should windows leave the daemon commit and ride a
   dedicated footer commit instead?
3. None further — the task left no open design surface.

## Evidence

- Edit + closure: AGENTS.md claims-carry-citations bullet; TODO_LIST.md row
  `[x]` (former line 145).
- Gates: `GOEXPERIMENT=jsonv2 go build ./... && go vet ./...` rc=0;
  `go test ./internal/... -count=1` 15 ok / 0 FAIL.
- Commit: footer-bearing work commit at HEAD; tree clean at report time.
