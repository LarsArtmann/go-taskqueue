# Task 000001a0a2df535461f8abc12a16b7320289 — `tq enqueue --wait`, second-dispatch verification lap

**Date:** 2026-09-15 04-31 · **Work commit:** `5c2b4ec` (prior lap; impl rode daemon commit `dec6a7f`) · **Branch:** master (not pushed)
**This lap:** DONE-on-arrival verification-only no-op, sanctioned by the 04-27 close-out (§f50). The deliverable shipped in `5c2b4ec` + `dec6a7f` and the full close-out is `docs/status/2026-09-15_04-27_task-000001a0a2df535461f8abc12a16b7320289.md` — read that for the complete a)–g). This report records the re-dispatch verification only; §f/§g extend the 04-27 lists rather than repeat them.

## a) Fully done (verified fresh this lap, at HEAD `c2ea4a4`)

1. **Implementation exists**: `waitForTask` at `cmd/tq/main.go:331-362`, wired at `main.go:325`; flags `--wait` (`main.go:224`) and `--timeout` (`main.go:225`) exactly where the close-out cited them.
2. **Full cmd/tq ADR-0017 gate green**: `./scripts/test-cmd-tq.sh` → `ok github.com/larsartmann/go-taskqueue/cmd/tq 9.717s` (dev.mod shim, GOEXPERIMENT=jsonv2).
3. **Targeted contract test green (verbose, dev.mod shim)**: `TestCmdEnqueueWaitStreamsFacts` all three subtests PASS — `completed_succeeds` 0.25s, `dead_fails_the_run` 0.25s, `timeout_fails_the_run` 0.15s.
4. **TODO_LIST row 84 is `[x]`**; close-out report indexed (`docs/status/README.md` row for the 04-27 file); tree clean before this lap; forbidden files (.crushrc, crush.json, .tq-verify) untouched; zero code changes this lap.

## b) Partially done

Nothing new — inherited from 04-27 §b and re-confirmed still true this lap: streamed stdout not pinned by the test; FEATURES.md and AGENTS.md still carry no `--wait` line (verified by search); no CHANGELOG entry; no smoke script.

## c) Not started

Unchanged from 04-27 §c–§f: Windows cross-compile lap for the new path, sentinel errors for wait outcomes, `Store.Fail` doc comment, dedup+`--wait` test, timeout-default ruling (§g1).

## d) Totally fucked up

1. **I re-hit the ADR-0017 raw-module trap on my first verification attempt** — bare `go test` inside `cmd/tq` fails at setup ("ambiguous import"), exactly as the 04-27 report's §d2 documented one lap earlier; my second attempt (sourcing the devmod lib standalone) also failed on path derivation. The /tmp mirror of `test-cmd-tq.sh` with a `-run` filter was the third and working approach. Three round trips to produce evidence a sed-on-line-27 produced in one; the trap now has two same-day victims on two task IDs.
2. This is another **paid lap on a `[x]` row with an indexed close-out** — the day's recurring mint-time-done-check gap, now on a fifth task ID. Every lap like this one is pure spend until the claim-time `[x]`/close-out-index check lands.

## e) What we should improve

1. The mint-time done-check (recurring ask, §d2) should check BOTH the TODO row and the close-out index by task ID — the daemon-race split that put this task's impl in `dec6a7f` means a row-only or commit-only grep can each miss.
2. `test-cmd-tq.sh` could accept optional `go test` flag pass-through (`TQ_CMD_TQ_TESTFLAGS`), so verification laps don't hand-roll /tmp script mirrors to get a `-run`/`-v` lap.
3. The ADR-0017 caveat keeps costing round trips inside this repo's own agents; the strongest fix is upstream (root tag landing on the proxy), the cheap fix is a louder comment at the top of `cmd/tq/go.mod`.

## f) What to get done next

Direct tail from this lap (extends 04-27 §f1–50, top items not repeated):

1. Mint-time done-check in the pool claim path — row `[x]` OR close-out-index hit ⇒ refuse/downgrade-to-note (recurring across five task IDs today).
2. Pin the `--wait` stdout in `TestCmdEnqueueWaitStreamsFacts` (04-27 §f1, still the cheapest real gap).
3. FEATURES.md + AGENTS.md one-liners for `--wait` (04-27 §f2).
4. `TQ_CMD_TQ_TESTFLAGS` pass-through in `test-cmd-tq.sh` (this lap's §e2).
5. Run `CMD_TQ_OS=windows ./scripts/test-cmd-tq.sh` once (04-27 §f5, still skipped).

Beyond that: the 04-27 §f list stands as written; this lap found no new deliverable work.

## g) Questions I cannot figure out myself

Unchanged from 04-27 §g (timeout default, commit-split healing policy, cancelled exit code) — plus the standing re-dispatch/no-op-report policy questions already raised by sibling laps (03-43 §g1–2). No new ones this lap.
