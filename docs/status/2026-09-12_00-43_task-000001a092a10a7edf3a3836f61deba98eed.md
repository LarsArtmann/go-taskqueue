# Task 000001a092a10a7edf3a3836f61deba98eed — AGENTS.md evidence-copy gotcha (dogfood §f)

- **When**: 2026-09-12, 00:30–00:47 CEST
- **Branch**: master, unpushed (agents never push)
- **Work commit**: `c5992e0` (footer `Task-Queue-ID: 000001a092a10a7edf3a3836f61deba98eed`)
- **Scope honored**: exactly the TODO item — one AGENTS.md gotcha paragraph
  (check-ignore first, daemon `--stat` diff); no code touched, no drive-by
  refactors.

## a) FULLY DONE

1. **Read the cited source material before writing.** Located and read the
   06-41 report's §d1/§e1-2 (the incident the TODO item cites) first, so the
   gotcha carries the real sequence — 16 `.log` files behind a global
   `*.log` ignore → daemon commit `1d925e5` ("2 changed file(s)") → clean
   `git status` → caught only by reading the daemon commit's `--stat` —
   not a paraphrase of the TODO line.
2. **Gotcha bullet landed in AGENTS.md Conventions** (now ~lines 266–272),
   directly after the Evidence-archives bullet it pairs with:
   `git check-ignore -v <targets>` FIRST (copy second) + diff the
   auto-commit daemon commit's `--stat` against the intended file set;
   names the ignore + daemon + clean-status "silent-loss triangle" and
   cites the f9 near-miss + 06-41 report §d1/§e1-2.
3. **TODO_LIST.md loop closed per the file's own convention**: item `[x]`
   with a `— DONE 2026-09-12:` note, matching the neighbors (the 85–86
   DONE pattern).
4. **Gates green**: `scripts/check-todo-list.sh` (TODO gate),
   `scripts/check-doc-refs.sh` (doc cross-reference gate), and the
   root-module verify gate (`GOEXPERIMENT=jsonv2` export + `go build` +
   `go vet` + `go test ./... -race`, all ok). `check-todo-list.sh` +
   `check-status-index.sh` also run as the pre-commit hook on the close-out
   commit below.
5. **Session-start ritual done**: `git log --oneline -5` over the whole
   repo + `git status` + `git stash list` before editing; tree was clean,
   no concurrent work trampled, nothing reverted.

## b) PARTIALLY DONE

Nothing — the item was one documentation bullet and it shipped complete.
Two scope judgments to declare (not partials):

- **Gate scope**: root-module verify + the two doc gates relevant to the
  change; NOT the full `ci-local.sh` replicant. Proportionate for a
  two-file docs-only change (no Go code touched, agents never push), but
  it IS a conscious deviation from CONTRIBUTING.md's "pre-push gate is one
  command" framing — declaring it here so the next reader doesn't have to
  guess.
- **Close-out timing**: this report is the standard close-out turn, not
  part of the work item.

## c) NOT STARTED (noticed in passing, deliberately not touched)

1. TODO 88: harvest same-session-priority flag for /tmp-referencing items
   (06-41 §e3) — still open, next in this TODO section.
2. TODO 89: `tq pool-health` per-repo skip-streak summarizer — still open.
3. TODO 90: worktree-per-agent design doc — still open.
4. TODO 91: session-close bridge design + prototype — still open (large
   trigger-research lineage attached to the item).
5. Branch is 16+ commits ahead of origin/master. Policy says agents never
   push, so this is expected — but the pile keeps growing (the three
   00-1x sibling reports + this one all land in it) and nothing surfaces
   the count anywhere.
6. The daemon is alive and well: `f292651 chore: auto-commit 1 changed
   file(s) (heuristic)` sits three commits below my work commit — the
   exact actor the new gotcha warns about, committing while I worked.

## d) TOTALLY FUCKED UP

1. **CONTRIBUTING.md not read before acting.** The dispatch contract says
   "Read AGENTS.md (and CONTRIBUTING.md / CLAUDE.md if present) first" —
   I relied on the preloaded AGENTS.md context and never checked whether
   the other two files exist. CLAUDE.md doesn't; CONTRIBUTING.md does
   (74 lines). No damage: its individual gates are either the ones I ran
   (vet/build/race/doc-refs) or consciously out of scope for prose (§b),
   and its dprint note doesn't apply (docs formatting stays manual per
   AGENTS.md). But the contract's read-first step was executed at report
   time, not work time. Cost: zero. Discipline: failed. A one-line
   `ls CONTRIBUTING.md CLAUDE.md` at session start clears it.
2. **Nothing else broke.** No gate red, no daemon incident, no amend, no
   fixture leakage, no rename-into-string-literal risk (prose only). The
   honest failure list is short because the blast radius was two files of
   markdown; manufacturing more self-criticism would be noise.
3. Minor: the new bullet's "silent-loss triangle" phrasing partially
   restates context already in the Evidence-archives bullet directly above
   it (global `*.log` ignore + daemon). Deliberate — the gate bullet and
   the procedure bullet serve different readers, and the paragraph must
   stand alone when quoted into a prompt — but a critic can fairly call
   it duplication.

## e) WHAT WE SHOULD IMPROVE

1. **The gotcha is documentation-only — nothing mechanically enforces
   check-ignore-first.** The ghost-archive gate catches the RESULT (ghost
   files) after the fact; the PROCEDURE (check-ignore before copy, --stat
   diff after the daemon commit) lives only in prose. A tiny
   `scripts/evidence-copy.sh <src…> <archive-dir>` doing check-ignore →
   copy → SHA256SUMS regen → print the daemon-commit --stat comparison
   would make the right order the lazy order.
2. **Discoverability split**: the gotcha lives in Conventions; the ⚠️
   Known Issues section (where "Concurrent agents commit constantly"
   already describes daemon behavior) is where skimmers look first. A
   one-line cross-reference from that Known Issue to the new bullet
   closes the gap.
3. **The evidence-archive topic now spans three adjacent bullets** (ghost
   gate, SHA256SUMS manifest, copy procedure). They read fine today; if a
   fourth appears, promote them into a `####` subsection.
4. **Read-first compliance should be mechanical**: add
   `ls CONTRIBUTING.md CLAUDE.md` (or equivalent) to the session-start
   ritual in AGENTS.md — dispatch contracts phrase the read-first step
   differently each time; a ritual doesn't.
5. **Unpushed-pile visibility** (§c5): either a documented owner cadence
   or a threshold flag in reports (see §g2).

## f) Up to 50 things we should get done next

Not 50 — honest list, no padding. Existing TODO items are referenced, not
re-duplicated as new.

1. (NEW) Evidence-copy helper script (§e1): check-ignore → copy →
   manifest regen → daemon --stat diff in one command.
2. (NEW) Cross-reference the new gotcha from the ⚠️ "Concurrent agents
   commit constantly" Known Issue (one line).
3. (NEW, surfaced in passing from sibling reports) The pool unit's missing
   `GOEXPERIMENT=jsonv2` env fix is still un-TODO-tracked and keeps
   burning attempts (00-10/00-19/00-35 reports all hit the env lie);
   track the owner lever in TODO_LIST until the NixOS module carries it.
4. (existing, TODO 88) Harvest same-session-priority flag.
5. (existing, TODO 89) `tq pool-health` summarizer.
6. (existing, TODO 90) Worktree-per-agent design doc.
7. (existing, TODO 91) Session-close bridge design + prototype.
8. (existing thread, 00-10 report §g) Push/durability cadence ruling for
   the unpushed master pile.
9. (existing thread, 00-35 report §f) One canonical close-out
   report-placement ruling in AGENTS.md to stop per-dispatch relitigation.
10. (conditional, §e3) Consolidate the evidence-archive bullets into a
    subsection when a fourth appears.

## g) Questions I can NOT figure out myself

1. Should check-ignore-first + daemon --stat-diff become MECHANICAL (a
   helper script, or a pre-commit hook over `docs/status/assets/**`), or
   is prose + the ghost-archive gate the agreed stopping point? Prose gets
   skipped by the next hurried agent; a hook has false-positive cost for
   README-only edits — I can't weigh that tradeoff for the owner.
2. Is there an owner cadence for pushing the 16+ unpushed master commits,
   or should every report flag the count once it crosses a threshold
   (N=10?) until a policy exists?
3. Should the gotcha also be mirrored into `docs/status/README.md` (the
   discovery surface an archive author actually has open when creating an
   archive), or is AGENTS.md the single home and the README stays an
   index?
