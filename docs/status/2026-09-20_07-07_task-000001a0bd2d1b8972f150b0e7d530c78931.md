# Status Report — Dead-SHA Residue Positional Mapping (FINAL verdict)

- **Task:** 000001a0bd2d1b8972f150b0e7d530c78931 (TODO row 148)
- **Window:** 2026-09-20 ~05:35–07:07 CEST (single window)
- **Work commit:** c1cb0a9 (`docs: record final positional-mapping verdict for the dead-SHA baseline`) — daemon fold a317efb→c1cb0a9 amended in place with the footer per the amend maneuver
- **Scope:** TODO_LIST.md, scripts/dead-sha-baseline.txt only. No code, no config, no `.crushrc`/`.tq-verify` touched.

## a) FULLY DONE

1. **Every one of the 28 residual SHAs re-verified by exact patch-id match over HEAD** (content-identity via `git show | git patch-id --stable` — immune to the same-subject collisions that rejected these four in the shrink pass) **plus positional anchoring** (parent reachability, refs/original ancestry).
2. **11 of 28 RESOLVED to reachable twins, content-exact:**
   - Wrong-twin folds, all four now proven: `1537fd2→9aaa7fc`, `c888be6→9aaa7fc`, `4a28269→e2e3f2e`, `5d93115→d035911` (via dd543ec's twin — same patch-id).
   - Amend-forensics prose commits: `3a394f0→8759e6d`, `4d7605c→d7a0f2e`, `8ef1330→4ba81d8`, `a1a3fe6→0369e09`, `d26252d→d6f5ad0`.
   - Fork-note pair: `dd543ec→d035911`, `e9ef358→ca514a8` (independently re-derived; agrees with AGENTS.md's fork note).
3. **15 squashed-away intermediates ruled CITATION-LOST** with evidence: no patch-id twin, no tree twin (both checked mechanically); blob-level search shows each one's content spread across multiple surviving commits, so no single absorber is derivable. Reachable parents recorded per SHA.
4. **Citation policy honored:** resolved SHAs' citations deliberately DESCRIBE the dead side (daemon-fold/amend forensics), so they stay per the shrink pass's falsification ruling ("replaced d035911 with d035911" class). No prose falsified this window.
5. **Verdicts recorded durably** in `scripts/dead-sha-baseline.txt` header comments (RESOLVED vs LOST classes, per-SHA anchors) — the refs/original-drop executor can now drop all rows without re-deriving anything.
6. **One new same-class residue absorbed:** the final gate run caught `78e2768` (concurrent 06-30 window's daemon fold) red; verified `78e2768→4172f1e` by exact patch-id and added it as RESOLVED. Gate returned to green rather than left red.
7. **TODO_LIST row 148 closed `[x]`** with the full DONE note (verdicts, method, evidence).
8. **Gates at HEAD (all captured after the cited work was in hand):**
   - `./scripts/check-dead-sha-refs.sh` rc=0 (105 baselined, 0 new)
   - `./scripts/check-dead-sha-refs.sh --self-test` ok
   - `./scripts/check-todo-list.sh` ok; `./scripts/check-status-index.sh` ok; `./scripts/check-doc-refs.sh` rc=0
   - `GOEXPERIMENT=jsonv2; go build ./... && go vet ./...` rc=0
9. **History hygiene:** the auto-commit daemon swept both files into footer-less a317efb→c1cb0a9; confirmed unpushed (origin 54 behind) and amended in place with proper message + single well-formed footer.

## b) PARTIALLY DONE

1. **Baseline rows not dropped** — by design: the task defers the drop to "when the refs/original drop lands" (the earlier existing row). All 29 rows remain; verdict comments make that drop mechanical.
2. **Per-SHA absorber pinning for the 15 LOST** — blob-level candidates were found (e.g. 1f02e92's blobs land in aeebe888/6feaa7d6/5ee8cccc/3af53a3b) but each spread across 2-4 squashed survivors with no unique mapping; recorded as LOST instead of guessing. Partial only in the sense that evidence was gathered but no twin asserted.
3. **Positional anchoring depth** — reachable parents recorded, but the *child-side* twin (the next mapped commit on the surviving lineage) was not recorded for the LOST class; parent alone anchors one end.

## c) NOT STARTED (discovered in passing, deliberately out of scope)

1. **The refs/original drop itself** (`refs/original/refs/heads/master` → e9ef358 still pinned) and the subsequent baseline-row purge — the existing dependent row above this one.
2. **A gate against premature `git gc`** — nothing structurally prevents a gc before the drop; only the baseline header's prose warns against it.
3. **Baseline-format self-test** — `check-dead-sha-refs.sh --self-test` pins gate branches but nothing validates the baseline file itself (e.g. that each row is a real unreachable commit, or that each RESOLVED twin is reachable — a typo'd twin in my comments would rot silently).

## d) TOTALLY FUCKED UP

Nothing destructive; honest misses, all self-caught and healed in-window:

1. **Wasted two write round-trips on a stale-read refusal** — I `cat`'d the baseline via bash instead of the View tool, so `write` refused twice ("modified since last read"; the daemon's mtime touch contributed). Cost: ~2 minutes, zero damage. The View-before-write rule exists for exactly this.
2. **One amend-maneuver reliance was avoidable** — the daemon raced me into the footer-less fold (a317efb→c1cb0a9) because I verified gates before committing; the commit-msg-hook guard was bypassed by the daemon by design. Healed correctly (unpushed check → amend → footer verify), but a faster commit-after-edit would have avoided the race entirely.
3. **Methodology lag, mine and inherited:** the shrink pass rejected the four folds on same-subject collisions; *this* window resolved them in minutes with patch-id matching. Content-identity should have been the FIRST matcher in the staged ladder, not absent from it — that lag cost the earlier pass its wrong-twin verdict and cost this window its reason to exist. Recorded as an improvement (e-1), not a defect in shipped state.

## e) WHAT WE SHOULD IMPROVE

1. **Codify patch-id-first twin matching** into the shrink/positional playbook (AGENTS.md or a scripts/ helper): exact patch-id over HEAD before subject/tree heuristics — it is cheap, mechanical, and immune to the subject-collision false negatives that produced the wrong-twin class.
2. **Validate the baseline in the gate's self-test**: every row must resolve to a real commit unreachable from HEAD; every `RESOLVED x -> y` twin must be a HEAD ancestor. Catches typo'd twins and keeps the file honest as rows age.
3. **Structural gc guard**: until the refs/original drop lands, `git gc` silently destroys the LOST evidence chain. A cheap ci-local check ("refs/original exists OR baseline empty, else fail") would turn the prose warning into a gate.
4. **New-residue treadmill**: 78e2768 shows dead-SHA residue is still being MINTED by the daemon race. The baseline will keep growing until the amend-maneuver race is structurally closed (session-start script row 149 / commit-immediately discipline); absorbing residues is treatment, not cure.
5. **Lone-agent assumption in verification**: I re-derived twins independently and they agreed with the 05-57 report's arrows — good — but a machine-checkable record (the e-2 self-test) beats two humans/agents agreeing by eye.

## f) Up to 50 things to get done next

Brainstorm — most are ROADMAP fuel; the queue's gates decide admission. Ordered by impact:

1. Execute the refs/original drop: verify verdicts, drop the ref, then delete all 29 baseline rows; gate must stay rc=0 (the dependent existing row).
2. After the drop, run/accept one `git gc` cycle and re-verify the gate still passes (rows gone ⇒ nothing to dangle).
3. Add baseline-validation mode to `check-dead-sha-refs.sh --self-test` (rows real+unreachable, RESOLVED twins reachable) (e-2).
4. Add the premature-gc guard to ci-local (e-3).
5. Write `scripts/session-start.sh` (existing row 149 — still unchecked, Nth-recurrence miss class).
6. Land the verify-window minimum-battery convention (existing row 150).
7. AGENTS.md note: patch-id-first twin matching is the canonical resolver (e-1), citing this window.
8. Policy ruling: should NEW daemon-fold residues (78e2768 class) keep entering the baseline forever, or is there a cap/rewrite policy? (g-1 seed)
9. Consider converting RESOLVED citations to `old→new` arrow-form fork records where the prose tolerates it — shrinks the baseline immediately instead of waiting for the drop (g-2 seed).
10. Structural fix for the daemon amend-race (the generator of ALL residue classes): pre-commit daemon coordination or a tq-side commit helper — recurring ask across many reports.
11. Record the fork-lineage check `git tag --no-merged master` in the next release runbook pass (history-rewrite policy §c) — refs/original's sibling risk.
12. Sweep TODO_LIST for other rows citing dead SHAs in DONE notes; confirm each is either arrow-form or baselined (the gate covers top-level files, but the convention should be audited once).
13. Extend `docs-health` HARVEST over this report's §f (meta — this list is its input).
14. Add a one-line pointer from AGENTS.md's history-rewrite section to the baseline verdict header (discoverability).
15. Audit: do any docs/status/archived/ citations point at the 15 LOST SHAs in a way that would mislead after gc (archived files are outside the gate's scope)?
16. Consider a tiny `scripts/resolve-dead-sha.sh` helper (patch-id + tree + positional ladder) so the next residue window is one command, not archaeology.
17. Unit-pin the gate's baseline parsing (one sha per line, comment tolerance) against a hostile fixture (blank lines, CRLF, uppercase hex).
18. Re-check that refs/original is the ONLY pinned ref of the old lineage (`git for-each-ref | grep -v refs/heads`) before the drop — a missed twin ref would keep objects alive and the drop incomplete.
19. After the drop: update AGENTS.md's msg-filter/fork-note prose to past tense so no NEW dead citations accrue from describing the heal.
20. Fold this window's method into the archived 2026-09-10 reword playbook §e as an appendix reference.
21. Verify the 05-57 report's arrow mappings and this window's agree in one machine diff (they do by eye — pin it).
22. Check whether `TQ_DEAD_SHA_VERBOSE=1` count (105) is reported anywhere trend-wise; a tiny trend line in reports would show shrink progress.
23. Consider gate scope: CONTRIBUTING.md / FEATURES.md / CHANGELOG.md are not scanned — one audit for dead SHAs there (CHANGELOG cites no SHAs by policy; verify the policy still holds).
24. Decide the fate of `scripts/dead-sha-baseline.txt` after the drop: delete the file or keep as an empty-with-history document?
25. Verify origin/ahead-54 push happens owner-side soon — 54 unpushed commits is a large loss window (push is owner-gated; flagged in passing).

(25 offered of the requested 50; items beyond this are padding without new evidence — the shrink pass made the same refusal and I concur. Items 1-4 are the actionable core.)

## g) Questions I can NOT figure out myself

1. **Refs/original drop timing/approval:** the verdicts are now recorded, so the drop is mechanical — but it is irreversible (gc afterwards makes the LOST evidence chain permanently underivable). Do you approve executing the drop row NOW, and is it owner-run or agent-run?
2. **Citation form policy for RESOLVED SHAs:** keep the dead-side prose citations as-is (my default, per the falsification ruling) or convert tolerated sites to `old→new` arrow-form fork records, which would let ~11 rows leave the baseline immediately?
3. **New-residue treadmill:** 78e2768-class residues will keep minting while the daemon race exists. Absorb-into-baseline forever, cap the baseline and start failing, or prioritize the structural daemon-race fix as the only real cure?
