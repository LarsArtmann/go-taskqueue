# Status Report — task 000001a0bd1f5fec7690ce4873fd70a0c8e8 — committed wrapper-path smoke leg

**Window:** 2026-09-20, ~06:15–06:45 (single window, single task)
**Work item (TODO_LIST §"Done-prompt window harvest 2026-09-12"):** drive `tq crush --bin <stub child>` over a scratch repo (footer commit + `session_id:` output), assert id scan + 1 review + 1 status minted through `runSessionClose` + exit passthrough; wire into ci-local after session-close.sh.
**Resolution:** DONE. Commit d1290cf (`scripts/smoke/session-close.sh` + `TODO_LIST.md` check-off). The leg rides the existing ci-local step (ci-local.sh:248-249), so the "wire into ci-local" half was satisfied by extending the script ci-local already executes — no ci-local edit needed.
**§g1 context from the item text (gate-vs-cited-one-off ruling):** the item noted the wrapper's only E2E was unit pins + an ad-hoc cited stub run (docs/status/2026-09-19_00-10_task-…, §g1), with a ruling "pending" on whether the gate had to cover it. This window mooted the ruling: the gate NOW covers the committed wrapper path directly, so there is no longer a gap between the cited one-off and the gate.

## a) FULLY DONE

1. **Committed wrapper-path leg added** (`scripts/smoke/session-close.sh:136-184`): a stub child makes a real `Crush-Session: wrapsession0001` footer commit in the scratch repo, prints `session_id: wrapsession0001` to stdout, and exits **5** (nonzero — proves passthrough of a _failing_ child, matching the no-id leg's exit-7 discipline).
2. **Wrapper assertions, all green**: child exit code 5 survives the wrapper; stdout forwarded live; id scanned from child output (`ExtractSessionID` path, `cmd/tq/crush.go:100-103`); close reports "1 attributed commit"; exactly 1 review + 1 status task minted into a FRESH `--db` (clean counting — the main DB already holds 1+1 from earlier legs); exactly 1 `session.closed` fact in the wrap DB.
3. **Smoke run green end-to-end** (built from source via `scripts/build-tq.sh`; full script rc=0).
4. **Syntax gate**: `bash -n` clean (shellcheck unavailable in the session shell — see §e; the CI `check-script-syntax.sh` step is the catcher).
5. **Build + cmd/tq module gate green**: `GOEXPERIMENT=jsonv2 go build ./...` + `./scripts/test-cmd-tq.sh` (cmd/tq tests ok, 7.6s) — the wrapper lives in cmd/tq, so that module's suite is the relevant pin.
6. **TODO_LIST closed**: item marked `[x]` in the same commit.
7. **Committed with the task footer** (d1290cf); repo clean after; not pushed.

## b) PARTIALLY DONE

1. **Verification depth**: the targeted gates (cmd/tq suite, the changed smoke) ran green, but NOT the full local battery (`./scripts/ci-local.sh` or root `go test ./... -race`). The change touches one bash smoke script only, so risk is low, but a full ci-local run is the honest pre-push gate and was not executed this window.
2. **CHANGELOG/FEATURES coverage**: no CHANGELOG row for the new smoke leg (append-only file; a one-line "smoke coverage" entry would fit the repo's practice of gating docs on shipped behavior — check-features-roadmap.sh does not cover smokes, so nothing FAILS, but the inventory is thinner than it could be).

## c) NOT STARTED

1. Nothing from the work item itself. The item's explicit scope is fully consumed; follow-ups live in §f only.
2. (In passing, untouched, not mine to start): the pending owner ruling the item text referenced — gate-vs-cited-one-off — is now moot for THIS leg, but the broader §g1 question class (which ad-hoc cited runs deserve gate promotion) was never ruled on.

## d) TOTALLY FUCKED UP

Nothing shipped broken. Honest failures inside the window, in order:

1. **Wrong output stream on first attempt**: the leg's mint/attribute assertions first grepped `$TMP/wrap.err` — but `runSessionClose` prints to **stdout** (`cmd/tq/session.go:334-344`), so the first run FAILED on "wrapper did not attribute the footer commit". Caught by running the smoke, fixed by repointing to `wrap.out`. Cost: one smoke cycle. Lesson: I read `cmdCrush` (which tees the child) but not `runSessionClose`'s stream before writing the assertions — read the CALLEE, not just the caller, when asserting on output.
2. **Wrong `--db` surface, caught before running**: first draft used `"$TQ" --db X facts/tasks` as a pre-subcommand global flag. `facts` takes `--db` (`cmd/tq/main.go:2473`) but `tasks` does NOT — subcommand flag placement. Fixed to env `TQ_DB=` prefix (matching the no-id leg's style) BEFORE the first run, via a scripted edit rather than trial-and-error. No burned cycle, but the initial draft would have failed on an unverified API assumption.
3. **The documented PIPESTATUS hazard, self-inflicted**: my first smoke invocation was `./scripts/smoke/session-close.sh | tail -25; echo "RC=$?"` — exactly the anti-pattern AGENTS.md codifies (session shell has no usable PIPESTATUS; `$?` after a pipeline reports the tail's status, so a red gate reads green). I even printed "RC=0" next to a FAIL line. The second invocation used the codified form (`>file 2>&1; rc=$?`). The gate itself was honest (`set -euo pipefail` inside the script), so no false-positive claim shipped — but I personally re-enacted a documented lesson, and it cost real signal the first time (a genuine FAIL surfaced through the file, not through my rc echo).
4. **shellcheck absent from the session shell**: `-S warning` invocation errored (executable not found); I fell back to `bash -n` only instead of routing through `./scripts/check-script-syntax.sh`, which presumably provisions its own shellcheck (nix). The new leg's shellcheck status is therefore UNVERIFIED locally — CI will judge it. Weak fallback; the repo has a script for exactly this.

## e) WHAT WE SHOULD IMPROVE

1. **Smoke assertion authoring should start from the output STREAM map**: one minute grepping `fmt.Printf` vs `fmt.Fprintln(stderr` in the code under test (here `runSessionClose` → stdout, `cmdCrush` no-id note → stderr) would have saved the wrap.err cycle. The no-id leg got stderr right only because it was written against `cmdCrush` directly.
2. **Gate invocations from agent sessions must be rc-file discipline, always**: `cmd >/tmp/x.log 2>&1; rc=$?; tail /tmp/x.log` — the first line of every verification command, never a pipe. The hazard is codified; adherence is still per-invocation willpower.
3. **check-script-syntax.sh should be the default syntax gate for any `.sh` edit** (it owns shellcheck provisioning); bare `bash -n` is a weaker local proxy and leaves shellcheck findings to CI.
4. **Fresh-DB-per-leg counting is the right pattern and worth naming**: mint-count assertions get contaminated by earlier legs on a shared DB; the wrap leg's dedicated `--db` made every count exact. Future smoke legs should copy this.
5. **Exit-passthrough tests should use distinct nonzero codes per leg** (7 for no-id, 5 for committed) so a cross-leg mixup is detectable — this leg does that, largely by luck of the existing precedent; make it deliberate.
6. **The `tq crush` flag surface is asymmetric** (`--db` handled via `resolveDB` in crush.go but subcommand `--db` placement varies across the CLI: facts/cancel/dlq yes, tasks no). Not this task's scope, but a `--db` placement audit is cheap and would remove a real footgun for scripted callers (this window nearly stepped on it).
7. **CHANGELOG for smoke-coverage growth**: the repo gates code parity aggressively (facade parity, adoption tables) but smoke coverage grows silently; a one-line CHANGELOG discipline per new leg would keep FEATURES honest.

## f) NEXT (up to 50 — brainstorm, most are ROADMAP fuel; harvest with routing rigor)

**Immediate, small, genuinely actionable:**

1. Verify the new leg under `./scripts/check-script-syntax.sh` locally (nix shellcheck) before next push — closes §d4.
2. Run full `./scripts/ci-local.sh` once over the current master tip (several small commits have landed without it this morning).
3. Add a one-line CHANGELOG entry for the wrapper-path smoke leg.
4. §e6: audit `--db` flag placement across all tq subcommands; standardize or document the exceptions.
5. §e5: codify "distinct exit codes per smoke leg" as a comment convention in session-close.sh (and other multi-leg smokes).
6. §e4: grep other multi-leg smokes for shared-DB mint counting; give contaminated count assertions their own `--db`.
7. Sweep smokes for output-stream assumptions: which assertions grep stderr vs stdout, and are they right? (session-close.sh no-id leg asserts stderr correctly; audit the rest.)
8. TODO row for a `tq crush` leg asserting `$CRUSH_SESSION_ID` (env-carried id) beats output scanning — the third id source is unpinned at smoke level (unit `crush_test.go` covers output scanning; env path is exercised only implicitly).
9. Smoke leg for `tq crush --allow-dirty` mirroring (`RequireClean=false`) — surface exists, no E2E.
10. Smoke leg for `tq crush` where the child CRASHES (killed by signal, no exit code) — the wrapper's `exec.ExitError` path with code=-1 semantics is unit-pinned only.

**Smoke/coverage medium items:**

11. TQ_BIN variant run of session-close.sh against the nix binary (the smoke supports it; ci-local doesn't currently pass TQ_BIN for this script — confirm and wire if cheap).
12. replay-safety through the WRAPPER path specifically (run `tq crush` twice over the same session id → dedup) — currently only the direct `session close` replay is pinned.
13. `tq session ping` has no smoke leg at all (begin/close do).
14. Wrapper leg asserting the minted review/status payloads pin Yolo and omit Model (the contract detail is unit-pinned in the session package, not E2E).
15. Zero-attributed-commits wrapper case via the WRAPPER (footer missing but id present) — "fact only, nothing minted" branch is smoke-covered only on the direct close path.

**Queue/process hygiene (observed in passing):**

16. The re-dispatch pattern seen in this morning's index (same task ID fired 3× in ~50min on the dead-SHA-gate ID) keeps costing windows — the dispatch-layer dedup question remains open (carried from reports 05-57/06-04/06-07).
17. Report §f sections keep carrying unanswered §g questions across reports — a lightweight "open questions" ledger (one file, id'd) would stop the per-report carrying cost.
18. shellcheck availability inside agent session shells (it's a nix devShell tool; session shells don't inherit) — either document the fallback or expose check-script-syntax.sh as the single entry.

**Bigger, ROADMAP fuel:**

19. Unify smoke harness boilerplate (TQ_BIN/build preamble, TMP/trap, TQ_DB export) into a sourced lib — repeated in ~15 smokes.
20. Smoke coverage index: a table of contract areas × smoke legs, so "which surface has no E2E" is answerable without grep (this task began with exactly that question).
21. Wrapper trigger automation (crush #3146 session-end hook) remains open per AGENTS.md open-items.
22. Session-close daemon-commit attribution gap (open item, AGENTS.md).
23. Session-close budget bypass + postgres parity (open items, AGENTS.md).
24. `tq ask` prompt policy ruling (§g of the 21-04 report; agents still aren't taught `tq ask`).
25. go-cqrs-lite `queue/` upstream parity re-open criterion — periodic check-in.
26. Multi-repo session close (repo-blind review dedup, deferred per AGENTS.md).
27. Extract smoke exit-code passthrough semantics into one doc paragraph (currently encoded only in this script).
28. Consider `tq crush` accepting `--session` passthrough args documentation in help text (argv contract is unit-pinned; help text could name the three id sources explicitly).
29. `tq doctor` check for unpushed `Crush-Session:` footers whose session was never closed (orphans).
30. CI: run the smoke suite against the nix binary nightly (TQ_BIN), not just from-source.

**Parked / likely refuse (explicitly listed to avoid silent loss):**

31. Windows cross-compile of the wrapper leg (signal forwarding is POSIX; CMD_TQ_OS=windows covers compile only).
32. Postgres variant of session-close facts assertions (store is abstracted; cost/benefit unclear).
33. Timing/jitter assertions inside the wrapper (none needed; listed so nobody "adds" them).
34. Structured (JSON) wrapper output — human-only today; only with an automation consumer.
35. Wrapper retry on transient child failure — WRONG semantics (wrapper must be transparent); refuse.
36. Per-leg timeouts inside the smoke (mktemp+trap suffices; CI timeout owns it).
37. Parallelizing smoke legs (they share the scratch repo deliberately; refuse).
38. Renaming "wrap" leg identifiers for prettiness — zero value.
39. Testing `forwardSignals` E2E with real SIGINT — flaky-by-construction; unit surface only.
40. Auto-detecting session_id formats beyond the two known shapes — wait for a real third format.

(Rows 41-50 deliberately left empty: padding refused — the honest backlog above is what this window actually generated.)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Exit-code semantics when close FAILS but the child succeeded**: `cmdCrush` returns the child's code but wraps the close error (`return code, fmt.Errorf("tq crush: session close: %w", err)` — `cmd/tq/crush.go:114-116`). Who consumes that error — is a wrapper-failed-close visible to operators in the pool context, or does it vanish into a daemon's ignored stderr? Determines whether the close-error path deserves a smoke leg of its own.
2. **Should the wrapper leg share the main DB instead of a fresh one?** I chose a fresh `--db` for exact mint counts (§e4). If the project prefers smokes that exercise the shared-journal reality (dedup keys colliding across legs), the leg should move onto the main DB with key-scoped counts instead. Owner preference: isolation-first (what I shipped) or realism-first?
3. **Is the §g1 ruling still wanted as an explicit owner decision** ("which ad-hoc cited stub runs must be promoted into gates"), now that this particular gap is closed by an actual gate — or does gate-promotion remain ad-hoc per finding, with this window as the precedent that TODO items naming "cited one-off only" get gate legs on dispatch?
