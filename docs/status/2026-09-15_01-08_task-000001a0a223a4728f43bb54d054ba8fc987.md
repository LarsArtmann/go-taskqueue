# Help-text smoke — third re-dispatch, verification-only (task 000001a0a223)

**Date:** 2026-09-15 01:08 · **Task:** 000001a0a223a4728f43bb54d054ba8fc987 (TODO section "Review-window follow-ups") · **Dispatch:** n=4 overall, 3rd verification-only re-dispatch.

## a) FULLY DONE

- **Work item stays done.** TODO_LIST.md row remains `[x]` with the 2026-09-15 closure note citing `scripts/smoke/help-text.sh`.
- **Smoke re-verified fresh this turn:** `./scripts/smoke/help-text.sh` → `PASS (18 subcommands checked)` (built tq via `scripts/build-tq.sh`; scratch DB exported).
- **Footers verified:** work commit 67905f9 (`test(smoke): add tq help-text smoke against parenthesized-identifier artifacts`) carries exactly one Task-Queue-ID footer; daemon commit eaadb1a preceded it; close-out reports 15fabd8/f0664ef exist.
- **Worktree clean** at start; no deliverable changes needed this dispatch — zero new work commits.

## b) PARTIALLY DONE

- No new artifacts this turn (none needed). The 01-01 §b residual gaps remain open and untouched: (1) no `--self-test` positive+negative fixture pinning the smoke's own grep; (2) subcommand list is hardcoded — a future `tq` subcommand removal/renaming shrinks coverage silently instead of failing; (3) no `tq facts`/`session` leaf coverage beyond the four grouped-leaf probes; (4) smoke not mirrored into ci.yml (ci-local only); (5) no CHANGELOG row for the gate addition.

## c) NOT STARTED

- Everything in §b's residual list; nothing new queued.

## d) TOTALLY FUCKED UP

- **Dispatch n=4 on a DONE task.** The queue minted (or re-delivered) a 4th dispatch for work closed at 67905f9 with two prior reports (01-01, 01-05) and an indexed TODO row — pure re-verification spend. This is now a pattern across sibling tasks (rename-diff sweep hit n=3; this task n=4).
- **No dispatch pre-flight.** 01-05 §d explicitly listed "no index grep pre-flight before re-verifying" as the miss — and this dispatch again went straight to `git log`/TODO grep without first grepping the status index or TODO row for prior dispatches of this exact task ID. Known miss, repeated.
- Minor: first `ls docs/status/ | head -5` + wrong grep pattern burned one round-trip before finding the index in README.md — the index location is documented in AGENTS.md.

## e) WHAT WE SHOULD IMPROVE

- Re-dispatch detection should be a FIRST action for queue-fed tasks: grep `docs/status/README.md` + TODO_LIST for the task ID before any verification work.
- The queue's mechanical gate (TQ_RESULT last-line) offers no DONE-on-arrival fast path — a short "verify-only, already closed" report mode is what every re-dispatch collapses into anyway; make it explicit.
- The smoke's hardcoded subcommand list and un-pinned negative case are cheap to harden (~30 min combined) and are the only real residual risk in the shipped artifact.

## f) WHAT WE SHOULD GET DONE NEXT

Direct tail (this task's artifact):
1. Add `--self-test` to `scripts/smoke/help-text.sh`: seed a fake help text containing `task(store)` and assert the grep catches it (pins the detector, not just the binary).
2. Derive the subcommand list from `tq -h` Usage output (or a golden inventory test) so a removed/renamed subcommand fails the smoke instead of silently shrinking coverage.
3. Mirror the help-text smoke into ci.yml so CI gates it without running full ci-local.
4. CHANGELOG row for the new gating smoke (if the convention wants gate-only additions recorded).
5. Rule on verify-only dispatches: can the worker emit a cheap `{"verdict":"already_done","commit":"67905f9"}` result instead of a full report?

Queue/process hygiene:
6. Investigate why dispatch n=4 was minted for a task with two indexed close-outs (sweeper cursor? re-arm on re-dispatch note?).
7. Add task-ID grep of the status index to the session-start ritual in AGENTS.md (make the 01-05 lesson a written step).
8. Consider a dedup window in the harvester: an item harvested fresh within N hours of its `[x]` note never re-mints.
9. Roll the "re-dispatch fast path" into docs-health HARVEST mode: annotate done-before-dispatch items at claim time.
10. Audit sibling repos for the same re-dispatch pattern (rename-diff sweep hit n=3 the same day).

Broader (noticed in passing):
11. `check-status-index.sh` README location: the index lives in `docs/status/README.md` but AGENTS.md never says where — one sentence would have saved the d) round-trip.
12. Index row count on `docs/status/README.md` is growing fast (6 rows for 2026-09-15 before this one); bloat warning threshold (100) may hit this month — schedule the archive sweep.
13. The 00-33/00-53 unindexed-report incidents (follow-up indexing commits) suggest the daemon folds reports faster than indexing happens — consider having the daemon run `check-status-index.sh` pre-commit.

## g) QUESTIONS

1. Should the smoke's subcommand inventory be self-derived from `tq -h` output (robust to subcommand churn, harder to parse) or pinned by a Go test against a committed golden list (brittle but exact)? I can't decide the maintenance tradeoff for you.
2. Is a CHANGELOG entry expected for gating-only additions (a new smoke wired into ci-local), or is CHANGELOG reserved for user-facing behavior? The file's own history is ambiguous.
3. The 429/daemon footer race bit again this task (n=4 dispatches, one daemon race documented in 01-01 §d): is re-dispatch churn an accepted cost of the harvest loop, or should we spend a task on a DONE-on-arrival verdict channel (`tq verdict {"verdict":"already_done",...}`) so re-dispatches stop minting reports?
