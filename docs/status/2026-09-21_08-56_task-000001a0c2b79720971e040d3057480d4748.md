# Task 000001a0c2b79720971e040d3057480d4748 — CHANGELOG/FEATURES docs window for the usage-rendering work — brutal a)-g) close-out

**Verdict up front: the deliverable is done and green, and the window's defining event is self-inflicted anyway.** I appended the two missing doc entries and ticked the row (work commit content rode daemon fold `0084f37`, attribution on footer commit `528b650`), but I lost the daemon-fold race in the exact losing order whose remedy I had READ earlier the same window (04-07 report §d7). Ninth fold on 09-21 — and a tenth hit mid-close-out (§d7). This close-out also caught two precision defects in my own fresh docs (message-count semantics, `d5`-class citation hygiene) and fixes them in the same commit that carries this report.

## a) FULLY DONE

- **a1 — The task itself.** Both entries verified MISSING at window start (grep: zero hits for `PrioritizeResult`/`prioritizeResultCard`/`statusReportCard` in CHANGELOG.md and FEATURES.md; the lone `usage` hit, CHANGELOG.md:303, is the derived-outcomes entry — different surface), then appended: CHANGELOG `[Unreleased]/Added` entry "Derived session usage on the detail page for status + prioritize outcomes"; FEATURES done-prompt-loop row and AI-batch-scorer row extended with the same rendering facts. TODO row 171 ticked with the house done-annotation.
- **a2 — Code facts verified before writing docs** (all first-hand at HEAD): `tq show` typed decode `cmd/tq/main.go:2273-2278` (`resultDetail` docstring :2246, view wiring :1957; landed `1a07541` 2026-09-21 03:35, pinned by `cmd/tq/main_test.go:377-391` + show e2e `cmd/tq/show_test.go:209`); `statusReportCard` usage line `internal/webui/fragments.templ:706-708`; `prioritizeResultCard` `fragments.templ:718-730` (usage at :722-724); shared helpers `internal/webui/components.go:98-113`; render-nothing guard confirmed in BOTH cards by reading the `if !sessionUsageEmpty(...)` conditions; detail-page wiring `fragments.templ:649-654` (initially inherited — see §d5, re-verified first-hand during this close-out).
- **a3 — Gate battery, rc=0 each at HEAD**: check-doc-refs, check-todo-list, check-features-roadmap, root build+vet (GOEXPERIMENT=jsonv2), root `go test ./... -count=1` (non-race; docs-only delta — race suite waived, but see §d3: the waiver was not STATED in the annotation). PIPESTATUS-hazard-proof invocation (redirect + `$?`, per AGENTS.md).
- **a4 — Turn-1 ritual complete**: `scripts/session-start.sh`, whole-repo `git log`/`git status`/`git stash list`, task-ID grep over docs/status/ (no prior report — genuinely fresh work, not a repeat dispatch), CONTRIBUTING.md read at turn 1.
- **a5 — Commit hygiene**: footer commit `528b650` carries exactly one well-formed `Task-Queue-ID` footer, verified by parsing `%(trailers)` after commit; nothing pushed; the daemon fold was handled with ZERO history rewrite (the empty-marker precedent shape named in standing row 264).
- **a6 — Re-dispatch armor**: the done-annotation carries code citations, so the likely verify-only re-dispatch (rows 167's pattern: work + 2 re-fires) can verify→cite→stop.

## b) PARTIALLY DONE

- **b1 — Attribution is half-done by construction.** The commit-half of the queue's derivation resolves (footer `528b650`), but the file-half derives EMPTY for this ID: the actual content (CHANGELOG/FEATURES/TODO_LIST) rode footer-less `0084f37`. Cross-reference works; work-attribution for this task is broken. This is the footer↔content split shape, ninth occurrence (row 330 counts eight).
- **b2 — FEATURES placement decided by default, not by judgment.** The two cards' rendering facts live only on the sweeper rows (done-prompt loop, AI batch scorer); I never examined whether the webui detail-page section should carry them too. Minted as a TODO row rather than resolved unilaterally — placement is a docs-architecture call.
- **b3 — The `tq show` claim is verified at its seam, not end-to-end.** Decode + type-assertion pin + view-struct wiring verified first-hand; I did not trace the show template's rendering of the decoded result beyond that. The CHANGELOG wording ("shows the structured scorer result") was deliberately kept inside verified bounds.

## c) NOT STARTED

- **c1 — dprint**: never run on the three markdown files. CONTRIBUTING.md says "dprint fmt before you commit docs"; AGENTS.md rules docs formatting MANUAL by decision (on-demand, not gated). I followed NEITHER and recorded no waiver — the contradiction itself is now a TODO row (§d4).
- **c2 — No detector exists for the empty-marker fold variant**: row 330's proposed heuristic ("footer-carrying commit whose diff is docs-only while the same window touched source files") would MISS this window — `528b650` has an EMPTY diff. Noted for the row-330 owner here; not minted as a separate row (would duplicate).
- **c3 — No fresh sweep of the 04-07 report's other open §f items** — out of scope per the dispatch contract; the already-tracked ones are pointer-listed in §f1-5 only.

## d) TOTALLY FUCKED UP

- **d1 — The daemon fold, lost with the remedy in hand.** Sequence: 3 file edits → full battery INCLUDING the ~30s root test suite → clean tree at commit time → the daemon had swept all three files into `0084f37` minutes earlier. The 04-07 report §d7, which I read THIS window, says verbatim: "Remedy candidate stands: commit immediately after the LAST write, before any gate, not after." AGENTS.md carries the same lesson. I ran gates-then-commit anyway. The check-status-index gate I was protecting by gating first runs in the pre-commit HOOK — committing first loses nothing. Cost: this task's git-derived file set is empty; this report is the only content the footer commit will ever carry.
- **d2 — The test suite was the race-losing move, not just unnecessary.** A docs-only delta did not need the 30s+ root suite before the commit; sibling precedent (row 167's annotation, three windows deep) explicitly records "tests waived, docs-only delta". Running it pre-commit widened the fold window for zero evidentiary gain — the suite exercises zero markdown.
- **d3 — My done-annotation is thinner than house style.** Sibling rows cite the fresh battery BY NAME ("fresh battery rc=0 each — check-doc-refs, check-todo-list, …"). Mine lists code citations but no battery line, so the next verify-only window on this ID inherits a weaker evidence floor than row 167's verify windows got.
- **d4 — The dprint contradiction sailed through unexamined.** Two governing docs disagree (CONTRIBUTING: run it; AGENTS: manual by decision, not gated) and I resolved the conflict by silence — no dprint, no waiver note. Minted as a row; a one-line ruling ends this class forever.
- **d5 — Citation hygiene violation: I shipped a borrowed citation.** `fragments.templ:649-654` entered the TODO annotation copied from the 04-07 report §c, unverified at write time — "Claims carry citations" demands first-hand file:line. Re-verified during this close-out (still true at HEAD), but the annotation shipped before that. Inherited ≠ verified.
- **d6 — One CHANGELOG file-ref shipped with unpinned provenance.** `internal/webui/render.go` appeared in the ref list without pinning which commit added its prioritize bits. Resolved during this close-out: cards/helpers = `dd920ac` (components.go, fragments.templ, handlers.go), typed decode + render.go map/lookup = `1a07541`. The entry is correct; the write-time provenance discipline was not.
- **d7 — Precision defect in my own fresh docs, caught by this close-out, not by me at write time.** I wrote "usage-less results render no line" (CHANGELOG) and "rendered only when usage exists" (FEATURES) — subtly stale against row 170's landed semantics (closed BEFORE my window): a message-count-only run now renders "N messages" alone. Fixed forward — and the daemon folded the three fix files into `8b746b7` while this report was being written: the TENTH fold, mid-close-out, in the edit-to-commit gap the report itself was drafting. The correction is on master; its attribution rides this report commit per the split precedent. The brutal-review pass earned its keep; the write-time pass did not.

## e) WHAT WE SHOULD IMPROVE

- **e1 — Make write→commit→gate the reflexive order on every window.** The index gate rides the pre-commit hook, so committing first sacrifices nothing; gates then run on the committed tree with fix-forward for reds. Gate-first is structurally unwinnable for any window whose battery exceeds the daemon's sweep period (~1-2 min).
- **e2 — Docs-only windows: waive the test suite explicitly, cite the waiver in the annotation** — shortens the fold window and matches the row-167 precedent.
- **e3 — Annotations carry the named battery line** (gate list + rc), sibling style — verify-only re-dispatchers cite it verbatim.
- **e4 — Inherited citations get re-verified before they ship in ANY stop artifact**, no exceptions for same-day same-arc reports.
- **e5 — Row 330's detector needs the empty-marker variant**: flag footer-carrying commits whose diff is EMPTY-or-docs-only when the immediately preceding chore commit touched files in the same window's scope — or, structurally, pair a footer commit with the chore commit(s) it follows during queue-side file derivation (standing owner ruling: row 264; this window adds a data point, not a fifth ask).
- **e6 — Keep what worked**: verify-MISSING-then-append with code facts read first is what kept this a small correct change instead of a duplicate-entry bug; the sibling-report-first pattern (reading 04-07's §f list at turn 1) gave me the exact item text and the f2/f3 semantics for free.

## f) WHAT WE SHOULD GET DONE NEXT (bounded to this window's scope — tracked pointers first, then the two new mints; padding refused)

Already tracked, verified open this window (no action minted — do not duplicate):

1. Row 172: agent/review completion cards lack usage lines — the dominant spend surface still renders none.
2. Row 173: `TestShowJSONCarriesDerivedSessionUsage` message-count pin gap.
3. Row 330: daemon-fold footer-first check script — ninth occurrence recorded HERE; absorb the empty-marker variant (§e5).
4. Rows 264/194: standing owner rulings on daemon footer attribution (BLOCKED — not re-asked).
5. Row 322: session-start.sh printing the status-index tail — would have surfaced the sibling battery style this window missed (§d3).

Minted THIS commit (new section at TODO_LIST tail, docs-only, agent-executable):
6. Reconcile the docs-formatting ruling: CONTRIBUTING.md ("dprint fmt before you commit docs") vs AGENTS.md ("docs formatting MANUAL by decision, not gated") — pick one, fix the loser, record it (§d4).
7. FEATURES placement check for the two usage cards: sweeper rows only today; decide whether the webui detail-page section should carry them too, move/extend if yes (§b2).

Explicitly NOT re-listed: budget token consumption of the non-agent result types, the completion-cost sparkline, and the rest of the 04-07/09-52 §f residue — all already tracked; re-minting them here would be duplicate noise.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **FEATURES home for the two usage cards** — sweeper rows only (current), or should the webui detail-page section carry them as first-class surface rows? I minted the check row (§f7), but the placement preference is a docs-architecture ruling I shouldn't guess.
2. **Docs-only battery standard**: is "root build+vet + the three docs gates, tests explicitly waived" the accepted battery for docs-only windows (row 167 precedent, followed here after the fact), or do you want the full root suite every time — accepting the wider daemon-fold window as the price?
3. **Release cadence**: `[Unreleased]` now carries three user-visible entries since v0.3.1 (board band grouping, score-cache aging/eviction, this usage rendering) — cut a v0.3.2 soon, or keep piling? (Owner-gated per the go-release rails; I can prepare the cut either way.)
