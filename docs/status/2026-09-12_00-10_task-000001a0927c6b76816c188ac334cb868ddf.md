# Status Report — 2026-09-12 00:10 — task 000001a0927c6b76816c188ac334cb868ddf

Self-review / close-out for the SHA256SUMS-manifest task (06-41 report §c2/§f2).
Written immediately after the work commit; brutal per the queue's close-out
contract.

## Task recap (what the window actually did)

Work item: "SHA256SUMS manifest for `docs/status/assets/2026-09-10-dogfood-proof/`
— completeness + tamper-evidence in one file; pairs with the anti-ghost-archive
gate". Landed, across three commits:

- `09309e7` (auto-commit daemon, swept my working tree): the SHA256SUMS manifest
  (17 entries: 16 logs + README), the archive README integrity note, and the
  `check-ghost-archives.sh` manifest enforcement (+21 lines: NO MANIFEST /
  UNTRACKED / STALE / INCOMPLETE) — plus, unfortunately, my test fixture (§d).
- `8e73f4d` (daemon): TODO_LIST close-out + deletion of that fixture.
- `37e83d4` (mine, footer carrier): empty marker commit — nothing was left to
  commit because the daemon had folded everything; the queue cross-reference
  points at this contentless commit.

Verification: `check-ghost-archives.sh`, `check-todo-list.sh`,
`check-doc-refs.sh` green; root `go build` + `go vet` + `go test ./... -race`
fully green (zero Go changes in this task; sub-modules untouched, per-module
loop skipped deliberately).

## a) FULLY DONE

1. **Manifest exists and is real**: 17-entry SHA256SUMS over the whole archive,
   generated after the README edit (so the README hash is final), self-verified
   with `sha256sum -c --quiet`.
2. **Gate enforcement** (the report §f2 "+ a line in the gate"): every
   `docs/status/assets/*/` archive must ship a git-tracked SHA256SUMS; the gate
   re-verifies hashes (STALE) and coverage of the full file set (INCOMPLETE).
   This is what upgrades the manifest from decoration to an enforced invariant.
3. **Negative-tested**: STALE and NO MANIFEST asserted through the real gate
   against a scratch fixture; UNTRACKED observed live (first positive run
   failed on the not-yet-committed manifest — correct behavior, caught before
   I could fool myself); the completeness `diff` asserted standalone (§b).
4. **TODO_LIST closed** following file conventions (`[x]` + `— DONE 2026-09-12:`
   with specifics).
5. **README documents the verify command**; provenance narrative untouched.
6. **AGENTS.md drift I created is fixed in this report's commit**: the
   ghost-archive conventions paragraph now names the manifest requirement and
   the four failure modes, the regeneration one-liner, and the no-spaces
   filename assumption; the "concurrent agents" gotcha gained the
   fixtures-inside-gated-trees lesson (§d).

## b) PARTIALLY DONE

1. **INCOMPLETE failure mode never asserted THROUGH the gate.** The fixture's
   manifest was untracked, and the UNTRACKED branch short-circuits the
   if/elif chain — so the INCOMPLETE branch fired only in a standalone
   `diff` of the comparison logic, not in a real gate run. The code path is
   reachable only for tracked+verifying manifests; I reasoned it instead of
   proving it.
2. **Gate loop proven on exactly ONE archive.** The enforcement generalizes to
   every future `docs/status/assets/*/`, but only one archive exists today; a
   second archive (or the next one) is the first real multi-dir exercise.
3. **Work and footer live in different commits.** The queue-facing commit
   (`37e83d4`) carries the Task-Queue-ID but zero changes; the actual diff is
   in daemon commits. `git log --grep Task-Queue-ID` cross-references a
   contentless commit and the reader must hop to 09309e7/8e73f4d. Honest, but
   the cross-reference is weaker than the contract intended.

## c) NOT STARTED

1. Sibling TODO items seen while in the file, untouched per contract:
   AGENTS.md gotcha for evidence-copies-vs-daemon (06-41 §d1/§e1-2 — partially
   covered by today's fixture lesson, but the check-ignore/`--stat` procedure
   is still unwritten), harvest /tmp same-session-priority flag (§e3),
   `tq pool-health` (02:00 f27), worktree-per-agent design doc, session-close
   bridge.
2. **CHANGELOG policy for evidence/docs-only tasks**: I skipped a CHANGELOG
   entry (append-only file; the status report is the record) — no explicit
   policy decision exists; see §g2.
3. **Archive README regeneration instruction**: the README says how to VERIFY
   but not how to regenerate after adding files (the procedure now lives only
   in AGENTS.md; the archive itself should carry it or point at it).
4. **Durability**: the manifest proves integrity only while the commits exist;
   master remains unpushed-by-policy, so disk loss still kills the archive
   (06-41 §c2 durability note — unchanged, owner domain).

## d) TOTALLY FUCKED UP

1. **My negative-test fixture is in master's history.** I created
   `docs/status/assets/zz-gate-fixture/` inside the gated tree, and the
   auto-commit daemon swept it into `09309e7` within minutes — while I was
   still mid-test. I caught the staging, unstaged via `git rm --cached`, but
   the COMMIT had already happened; the cleanup rode `8e73f4d` as 18 staged
   deletions. Permanent garbage in unpushed-but-immutable history, and my
   own task's diff is polluted with fixture noise. Root cause: fixture
   lifetime spanned a daemon commit window because I split
   create/assert/trash across calls and then ran the multi-minute race suite
   before committing. AGENTS.md now carries the lesson; /tmp (or a single
   atomic shell chain) was always the right place.
2. **Silent chain death via `set -e` + gate exit 1.** My first negative-test
   one-liner died with no output ("exit status 1") because the gate's
   nonzero exit propagated through the chain — the exact pipeline-masking /
   lost-exit-code class AGENTS.md documents, and I re-enacted it anyway,
   burning a round trip and muddying the fixture state I then had to
   diagnose step by step.
3. **Gate precedence flaw shipped knowingly.** The if/elif chain reports only
   the FIRST applicable failure: an untracked manifest masks STALE/INCOMPLETE.
   I noticed it mid-test (that's why INCOMPLETE wasn't gate-asserted),
   documented it in AGENTS.md instead of restructuring to report all
   applicable modes. Defensible for 21 lines of shell, but "first-match"
   gates hide compound rot — the very thing this gate exists to catch.
4. **Expectation drift on the first gate run**: I framed the initial positive
   run as "expect ok" and it failed on UNTRACKED — correct, but I hadn't
   thought through the order of my own steps (generate manifest → gate →
   commit); the gate was right and my mental model was behind it.

## e) WHAT WE SHOULD IMPROVE

1. **Test tracked-path negatives with a scratch index** (`GIT_INDEX_FILE`):
   lets the gate see a "tracked" fixture without touching the real index —
   would have asserted INCOMPLETE end-to-end and avoided the history garbage
   entirely.
2. **Atomic negative tests**: create → assert → trash in ONE shell command
   with explicit exit-code capture (`cmd; echo $?`, not `set -e` chains over
   intentionally-failing gates).
3. **Commit docs-only work BEFORE slow verification** when the daemon is
   live: the race suite ran between my staging and my commit — that window
   is where both daemon incidents happened. Gate-then-commit-then-test is
   backwards for changes tests cannot break.
4. **Restructure the gate to report all applicable failures** (separate ifs,
   not elif) so precedence never masks evidence — small change, better gate.
5. **Manifest ergonomics**: document regeneration in the archive README
   itself (or a shared conventions blurb), since STALE failures will hit the
   next person who touches the archive without it.
6. **Close-out ritual addition**: for queue tasks, run `git log -2 --stat`
   immediately before the footer commit; if the daemon folded the work,
   decide consciously between marker commit and revert-and-redo instead of
   discovering it at commit time (I got lucky that reverter would have been
   worse).

## f) Up to 50 things we should get done next

Impact-sorted, scoped to what this window touched or saw in passing; ~18
honest items beat padded 50.

1. **Gate: report all applicable failures** (split the elif chain) + assert
   INCOMPLETE/STALE via a `GIT_INDEX_FILE` scratch-index fixture (closes §b1/§d3
   together).
2. **Archive README: add the regeneration one-liner** next to the verify line.
3. **AGENTS.md gotcha (06-41 §d1/§e1-2)**: evidence copies must `git check-ignore`
   targets first and diff the daemon commit's `--stat` against the intended set
   — today's fixture incident is the second data point; the paragraph is overdue.
4. **Daemon-race protocol gotcha**: one AGENTS.md paragraph — for docs-only
   tasks, commit before slow verify; stage+commit in a single command; check
   `git log -1` before writing (extends today's fixture lesson into procedure).
5. **Second evidence archive** (any future proof run) to exercise the manifest
   gate beyond its single data point.
6. **Harvest /tmp same-session-priority flag** (06-41 §e3; internal/harvest) —
   unchanged, still hot-class.
7. **`tq pool-health`** (02:00 f27) — per-repo skip streaks + last activity.
8. **AGENTS.md evidence gotcha §d1 remainder**: `--stat` diff procedure as a
   tiny script (`scripts/archive-evidence.sh`?) that does copy → check-ignore →
   manifest → stage → print daemon-diff checklist in one pass.
9. **Worktree-per-agent design doc** (02:00 f15; docs/planning/).
10. **Session-close bridge** (02:00; the big one — crush#3146 tracking).
11. **CHANGELOG policy ruling** (§g2): do evidence/docs-only tasks get entries?
12. **Push/durability ruling** (§g1): the archive now has integrity tooling but
    still single-disk existence.
13. **`tq doctor`**: add a ghost-archive preflight? (cheap: run the gate's
    manifest check over assets during doctor) — low, nice symmetry.
14. **Gate: use `git ls-files` for the coverage side** so coverage compares the
    TRACKED set (a worktree-only stray file currently only fails via INCOMPLETE
    diff vs manifest — actually consistent; verify this reasoning when touched).
15. **Filenames-with-spaces in archives**: awk `'{print $2}'` breaks; either
    document the no-spaces constraint (done in AGENTS.md) or switch the gate to
    `sed 's/^[0-9a-f]*  //'`/sha256sum -c parsing (low).
16. **CQA live verification** (BLOCKED owner) — unchanged.
17. **Consider `sha256sum --strict`** in the gate so malformed manifest lines
    (wrong hash length) fail instead of silently passing the -c parse (low,
    one flag).
18. **TODO_LIST hygiene**: the "Dogfood round" section header still says
    "harvested from docs/status/2026-09-10_02-00" while most items now cite
    06-41 — retitle or annotate provenance (cosmetic, noticed in passing).

## g) Questions I can NOT figure out myself

1. **Durability**: master is ~11 commits ahead, unpushed by explicit policy.
   The evidence archive now has tamper-evidence but still dies with this
   disk. Is there a scheduled/owner-gated push cadence I should assume, or
   should durable archives additionally land somewhere off-disk
   (`~/.local/state/tq/`, a mirror remote)?
2. **CHANGELOG policy**: should docs/evidence-only tasks append CHANGELOG
   entries, or is the status-report trail + index the canonical record? (I
   skipped it; no written rule found.)
3. **Auto-commit daemon control**: is there a sanctioned way to pause or
   scope the daemon per-session (env var, pidfile, path filter)? Today it
   committed my test fixture into master's history between two of my tool
   calls; "build fixtures in /tmp" mitigates but can't fix the class — the
   daemon will otherwise auto-commit ANY intermediate state that survives
   one sweep window.

TQ_RESULT footer for this window: work commit `37e83d4` (marker; content in
daemon commits `09309e7` + `8e73f4d`).
