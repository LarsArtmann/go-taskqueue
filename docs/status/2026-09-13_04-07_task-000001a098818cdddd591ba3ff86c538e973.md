# Task 000001a098818cdddd591ba3ff86c538e973 — fullcore postgres run + nil-store panic fix

**Date:** 2026-09-13 04:07 CEST
**Work item:** "Run examples/fullcore once against a throwaway postgres DB (`--backend postgres`; the example applies its schema — keep it pointed at a local/test database) (06:25 report c2)"
**Commits:** `3faaf0b` (code fix, footer commit) + `6365990` (daemon sweep carrying the TODO_LIST close-out)

## a) FULLY DONE

1. **Throwaway postgres cluster** — `initdb` into `/tmp/fullcore-pgdata`, port 55432, trust auth, db `taskqueue` created; kept strictly local/throwaway per the item's constraint. Stopped and cleaned up after.
2. **Postgres run** — `go run ./examples/fullcore --backend postgres --dsn 'postgres://tq@127.0.0.1:55432/taskqueue?sslmode=disable'` → the example applied the store schema, enqueued 4 demo tasks, drained, final counts `completed 4` (sh ×2, greet, flaky-retry), exit 0.
3. **Real bug found and fixed** — first run panicked with nil-pointer at `queue.Enqueue` (main.go:83). Root cause: the backend switch used `store, err := sqlite.Open(...)` / `postgres.Open(...)` inside each case, shadowing the outer `var store queue.Store` — the outer variable stayed nil on BOTH backends. The example had literally never been executed before. Fix: named locals (`sqliteStore`/`postgresStore`) + explicit `store = ...` assignment (examples/fullcore/main.go:53-72). `go vet` did NOT catch this (shadowing is legal; the pre-existing compile on master proves it shipped silently).
4. **SQLite path regression check** — `go run ./examples/fullcore --db /tmp/fullcore.db` → exit 0 post-fix.
5. **Gates** — `go build ./...`, `go vet ./...`, `go test ./internal/... -count=1` (exit 0, re-run to confirm the first run's one FAIL did not reproduce — see e4), `go test ./cmd/tq -count=1` exit 0.
6. **TODO_LIST.md closed out** — item at line 135 marked `[x]` with evidence note.
7. **Commit with footer** — `3faaf0b`, `Task-Queue-ID: 000001a098818cdddd591ba3ff86c538e973`, not pushed.

## b) PARTIALLY DONE

- **Commit attribution split:** the TODO_LIST.md close-out was swept by the auto-commit daemon into `6365990` (message "chore: auto-commit 2 changed file(s)"), which carries NO Task-Queue-ID footer. The cross-reference rule (multiple commits per task are fine, but the footer commit should exist) is satisfied by `3faaf0b`, so I did not amend the daemon commit — the known tradeoff is the TODO close-out is only reachable via `git log -- TODO_LIST.md`, not the queue's footer grep.

## c) NOT STARTED (noticed in passing, adjacent fullcore items still open in TODO_LIST)

- `scripts/smoke/fullcore.sh` hermetic smoke (line 136) — untouched, per the no-scope-creep contract.
- Drain-deadline determinism pinning (line 148) — untouched.

## d) TOTALLY FUCKED UP

Nothing rises to "fucked up." Honest near-misses:

1. **I initially ran the gates with a `| tail -3` filter and saw "FAIL" without context** — a pipeline-masked partial view. I then re-ran the full suite with clean exit codes (`exit=0`) and grep for non-ok lines (empty). Lesson already encoded in AGENTS.md; I briefly repeated the class anyway.
2. **Root `go test .` setup failure** — ran `go test .` which fails with "no Go files" (root module is app-layer only; cmd/tq is the code). Cost one round trip; should have known from AGENTS.md's module layout table.
3. **First `edit` of TODO_LIST.md was rejected** for not having Viewed the file. Trivial, but avoidable.

## e) WHAT WE SHOULD IMPROVE

1. **The example is untested-by-construction.** `go vet` passes on the shadowed version; `examples/` has no build-tag or smoke coverage. The nil-store bug survived a full CI-green window (it was committed 2026-09-10 with a "DONE" claim that cited the build, not a run). Example directories should get a hermetic smoke (`scripts/smoke/fullcore.sh` already planned, line 136) — this task proves the plan is not optional.
2. ** vet/lint gap:** shadowing an outer variable in a switch case that hands the value to the rest of main is exactly what `govet shadow` (off by default) or a strict `predeclared`/`importas`-class linter would flag. The advisory-lint baseline policy means nobody turns it on; a narrow `go vet -vettool` shadow check on `examples/` only would have caught this.
3. **DONE-claims hygiene:** the 2026-09-10 close-out claimed the example worked ("proves the backend-choice import") based on compilation. The repo rule "an uncited claim is a hypothesis" should extend to: a run-claim cites the run, not the build.
4. **Test flake or pipeline artifact:** the first `go test ./internal/... -count=1` run showed a FAIL line in the tail; the immediate re-run (and a third run) were fully green with `exit=0`. I did not identify which package flaked (the tail cut the package name). If it reproduces it needs a owner look; one green re-run is a claim, not a diagnosis — noted here per the citation rule.
5. **Postgres smoke parity:** the postgres backend is now exercised by exactly one manual run. The conformance suites cover it, but the _example's_ postgres wiring (DSN flag, schema application, pool drain) has zero automated coverage.

## f) NEXT (up to 50, ranked; first ~8 are the real Pareto)

1. **Write `scripts/smoke/fullcore.sh`** (TODO line 136): sqlite path, `TQ_DB=<scratch>` export, assert exit 0 + "completed 4" — turns this manual run into a CI gate.
2. **Add a postgres mode to that smoke** behind an env var (`FULLCORE_PG_DSN`) so CI-with-postgres can exercise the backend-choice path end-to-end.
3. **Add `go vet` shadow check scoped to `examples/`** (or enable `govet shadow` there only) to prevent the nil-store class.
4. **Identify the one-shot internal test FAIL** from this session (e4): if anyone saw a flaky package around 2026-09-13 03:5x, compare notes; otherwise ignore a single unreproducible blip.
5. Pin fullcore drain-deadline determinism mechanically (TODO line 148) — depends on 1.
6. Consider `examples/fullcore` gaining a `-race` pass in the smoke (worker + goroutine-heavy demo is a cheap race canary).
7. Sweep the other `examples/` (api, sse) for the same run-never-happened class: `go run` each once against scratch state, then decide if they need smokes.
8. Decide the daemon-commit attribution policy for close-outs (b): either let the daemon message carry the Task-Queue-ID via a hook, or accept footerless chore commits and document it.
   9-50. (remainder are the already-tracked backlog items in TODO_LIST lines 136-148 and the standing window items — not duplicated here per the no-research constraint; the queue's own harvest loop owns them.)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Should the daemon's auto-commits carry `Task-Queue-ID` footers when the swept files are close-outs of a known in-flight task?** If yes, the daemon needs session context it may not have; if no, the TODO-item-to-commit traceability rule has a permanent hole (b above). Owner call.
2. **Is the one-shot `internal` test FAIL worth an investigation ticket,** or is a single unreproduced FAIL in a `-count=1` run acceptable noise under the concurrent-agent load? (I re-ran twice, green both times; I did not burn more cycles.)
3. **Should `examples/` be gated at all?** They live outside every per-module gate and ci-local's loop. If the answer is "yes, smokes," item f1/f2 covers it; if "no, examples are documentation," the nil-store incident argues they should at least be compiled-and-run once per release.
