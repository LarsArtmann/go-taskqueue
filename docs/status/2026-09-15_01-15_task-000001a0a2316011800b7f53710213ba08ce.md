# Task 000001a0a2316011800b7f53710213ba08ce — lint-annotations sub-module extension

**Date:** 2026-09-15 01:15 · **Work commit:** fbec002 · **Script (daemon-folded):** bd9f50c

Extend `scripts/lint-annotations.sh` from root-only annotation emission to every
internal sub-module, or decide root-only deliberately (08:47 report c1/f1/g2).

## a) FULLY DONE

1. **Script extended** (`scripts/lint-annotations.sh`): now runs golangci-lint with
   the shared `--new-from-rev` base over the root module AND every `internal/*`
   sub-module via the same disk-derived `find … -name go.mod` loop as
   `lint-baseline.sh` (16 runs total). Per-module `GOWORK=off`; the root
   `.golangci.yml` policy still applies everywhere because golangci-lint ascends
   from the module cwd.
2. **Path correctness**: sub-module findings get their Filename prefixed with the
   module path (`internal/worker/foo.go`), so GitHub annotations point at real
   repo-relative paths; root findings keep raw filenames.
3. **NDJSON aggregation**: per-module run JSON is reduced with `jq` into one
   issue-per-line file; the existing annotation-emitting jq (10-warning cap,
   log-only overflow) consumes the combined stream unchanged. An execution
   failure (exit ≠ 0/1) in ANY module fails the step with the module named.
4. **Verified**: `bash -n` clean; live run at `LINT_BASE=HEAD~3` over all 16
   module runs → 16× "0 issues", final "0 new findings on lines changed since
   HEAD~3", exit 0. No Go code touched, so no build/test delta — but confirmed
   working tree clean after commits.
5. **TODO_LIST.md closed**: item 61 marked `[x]` with a dated, specific
   completion note (mechanism + verification evidence, house style).
6. **Committed with footer**: `fbec002` carries the
   `Task-Queue-ID: 000001a0a2316011800b7f53710213ba08ce` footer; nothing pushed.

## b) PARTIALLY DONE

1. **ci.yml wiring is inherited, not re-verified end-to-end**: the workflow step
   (`run: ./scripts/lint-annotations.sh`, line 107) needs no change — it invokes
   the script — but I did not execute a runner-shaped CI simulation (runner PATH,
   jq availability, `fetch-depth: 0` is already pinned for `--new-from-rev` per
   ci.yml line 25 comment). Local verification is strong; runner verification
   awaits the next real push.
2. **Runtime cost not measured precisely**: the extended run took ~1–2 min locally
   (16 cold-ish lint passes). CI's advisory step is now proportionally slower;
   acceptable (lint-baseline.sh already pays the same loop in ~3 min), but the
   CI-minutes delta is an estimate, not a measurement.

## c) NOT STARTED

Nothing that belonged to this item. Adjacent items I noticed in passing but did
NOT touch (scope discipline):

- `scripts/for-each-module.sh` extraction (TODO row 62) — the disk-derived loop
  now exists in THREE scripts (lint-baseline.sh, ci-local module gate,
  lint-annotations.sh); this TODO item just got MORE justified.
- golangci-lint version-pin unification (TODO row 63).
- CI topology one-pager (TODO row 64).

## d) TOTALLY FUCKED UP

1. **Daemon race, handled imperfectly**: the auto-commit daemon folded my script
   edit into `bd9f50c` ("chore: auto-commit 1 changed file(s)") BEFORE my explicit
   commit — so the script change itself sits in a footerless chore commit and my
   annotated commit `fbec002` contains only the TODO_LIST.md row. The AGENTS.md
   runbook offers the amend maneuver for daemon-folded content that hasn't
   shipped, and both commits are local/unpushed — I chose NOT to rebase/amend
   because scripted history edits are the highest-risk operation in this repo
   (2026-09-10 reword incident) and the footer requirement was still satisfied
   by the last commit. Consequence: the queue↔git cross-reference points at a
   commit whose diff is only the TODO row. Defensible, but a cleaner agent would
   have either (a) committed the script FIRST, immediately after writing it,
   before the daemon's next sweep, or (b) done the sanctioned scripted fold of
   `bd9f50c` into `fbec002`. The real miss is (a): stage-and-commit hot files
   IMMEDIATELY in this repo; the daemon's cadence is minutes, not hours.
2. **First edit attempt was sloppy**: my initial `edit` call tried to splice the
   module loop inline with a convoluted tmp-file reuse scheme (overwriting the
   file being read, cygpath fallback) — it failed the read-before-edit gate, and
   on reflection the design was worse than a clean rewrite. Recovered by
   rewriting the whole script with `write`. Cost: one wasted round trip. Lesson:
   for structural script changes, go straight to `write` after a full read.

## e) WHAT WE SHOULD IMPROVE

1. **Commit hot files immediately** in this repo — the daemon races every edit;
   "edit everything, commit at the end" hands the diff to a footerless chore
   commit. This cost the clean attribution this task's own contract demands.
2. **The disk-derived module loop has 3+ copies** (lint-baseline.sh,
   lint-annotations.sh now, ci-local module gate, plus ci.yml copies) —
   TODO row 62 should be next; every new consumer multiplies the drift surface.
3. **lint-annotations.sh runtime**: 16 sequential golangci-lint passes could
   reuse the warm cache; consider only running sub-modules whose files changed
   since base (the loop already knows `--new-from-rev`; a `git diff --name-only
   $base -- <module>` pre-filter would skip untouched modules entirely).
4. **jq dependency check**: the script degrades silently-ish without jq (ci-local
   guards jq availability for the annotations step; the new aggregation paths
   inside the script assume jq unconditionally). CI ships jq; a guard at script
   top would make local runs without jq fail loud instead of odd.
5. **Self-test harness**: like `fanout-libdive.sh --self-test`, a tiny fixture
   test (synthetic run.json → assert prefixed paths + cap behavior) would pin
   the aggregation jq without needing lint findings to exist.

## f) NEXT 50 (up to; adjacent-first, then repo backlog noticed in passing)

1. Extract `scripts/for-each-module.sh`, consume from ci.yml + ci-local.sh +
   lint-baseline.sh + lint-annotations.sh (TODO 62).
2. Unify the golangci-lint version pin into one source (TODO 63).
3. CI topology one-pager in docs/planning/ (TODO 64).
4. Observe the first real CI run of the extended annotations step; confirm
   sub-module annotations render with prefixed paths.
5. Add the jq guard at the top of lint-annotations.sh (§e4).
6. Add a `--self-test` fixture mode to lint-annotations.sh (§e5).
7. Consider the changed-modules pre-filter for the annotations loop (§e3).
8. Decide whether lint-baseline.sh should consume the same NDJSON aggregation
   (it parses text today; JSON would make per-module counts exact).
9. Re-run `./scripts/ci-local.sh` fully (this task ran only the script + syntax;
   the pre-push gate was not executed end-to-end).
10. Sweep TODO_LIST.md for other "root-only" tooling assumptions the module
    split created (annotations was one; are there more?).
11. Verify `fetch-depth: 0` on ALL workflows that might later invoke the
    annotations script (only ci.yml's lint job documents it today).
12. Add the annotations script to whatever docs enumerate scripts/ (check
    FEATURES.md / README dev section).
13. Consider annotation cap fairness: the 10-cap is global across modules now;
    per-module round-robin would prevent one noisy module starving the rest.
14. Check whether golangci-lint v2.14+ has gained a github-actions output
    format (the script's raison d'être; revisit if upstream fixed it).
15. …(items 15–50 intentionally left to the repo's own TODO_LIST harvest —
    the contract says "up to"; this task generated 14 concrete follow-ups and
    inventing 36 filler rows would violate the anti-noise rule. The four
    08:47-report rows already in TODO_LIST (61 done, 62–64 open) are the
    live backlog.)

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Attribution preference**: when the daemon folds a work edit into a
   footerless chore commit, do you want agents to perform the scripted
   fold/amend (runbook-sanctioned for unshipped commits), or is the
   footer-on-last-commit convention acceptable? (I chose the latter; the
   2026-09-10 history-rewrite incident makes me cautious by default.)
2. **Cap policy**: with sub-modules now feeding the same 10-annotation cap,
   should the cap rise (GitHub allows 10/step, 50/job — the script could emit
   across multiple steps) or stay strict?
3. **Runtime budget**: is +~1–2 min on CI's advisory annotations step
   acceptable, or should the next task add the changed-modules pre-filter
   before the next push makes this the hot path?

TQ_RESULT: {"files_changed": ["scripts/lint-annotations.sh", "TODO_LIST.md", "docs/status/2026-09-15_01-15_task-000001a0a2316011800b7f53710213ba08ce.md"], "commit_sha": "fbec0021a0fdde78e995d2f88e35077c62e68928"}
