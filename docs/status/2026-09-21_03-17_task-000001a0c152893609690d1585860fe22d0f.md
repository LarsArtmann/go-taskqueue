# §f4 task, attempt 3 of 3 — verify-stop that closed the verbatim-gate gap and found the gate is STRUCTURALLY red (gofmt walks gitignored vendor/), task 000001a0c152893609690d1585860fe22d0f

Window: 2026-09-21 ~03:01–03:20 CEST, single session. Fourth report on the
ID, FIRST on the final attempt. Lineage: attempt 1 (02:16–02:45) shipped
the work (02-27 + 02-32 reports) and burned its verify on the internal/e2e
timeout; attempt 2 (02:44–02:56) verified done-state, root-caused the
re-dispatch, and explicitly skipped running the task's verify command
verbatim (02-53 §b2). This window: queue verdict early, done-state
re-verified, the verbatim gate RUN — and the run rewrote the diagnosis:
attempt 1's e2e timeout is a SECONDARY, load-marginal layer; the PRIMARY
layer is a deterministic gofmt-stage failure that kills every verify on
this host regardless of code. Work item: TODO row 162 (checked, shipped).

## a) FULLY DONE

- **Queue verdict before repo work** (02-53 §e1, half-applied — see §d2):
  `tq show 000001a0c152893609690d1585860fe22d0f` at turn 2: `attempts: 2,
  maxAttempts: 3` → this session is the FINAL attempt; `lastError` =
  attempt 1's verify failure (e2e kill); verify payload captured verbatim
  from the record; lease alive (heartbeat extending; leaseExpires 03:22:15
  at report time). No owner answers to 02-53 §g1–3 landed between 02:56
  and 03:01 (git log clean above ce08c4b).
- **Done-state re-verified, zero source edits** (correct for attempt 3 —
  any code change would be fabrication): the four usage fields sit on
  StatusResult (internal/executor/status.go:84-87) and
  `StatusExecutor.Execute` fills them via `deriveOutcome`
  (status.go:192-196), mirroring review.go; TODO row 162 confirmed `[x]`
  with the attempt-2 annotation.
- **Fresh cheap battery at HEAD ce08c4b**: root `go build ./... &&
  go vet ./...` rc=0 (GOEXPERIMENT=jsonv2) and
  `./scripts/check-doc-refs.sh` rc=0 ("doc refs ok").
- **THE VERBATIM GATE RUN** — the 02-53 §b2 gap closed, one data point as
  §e3 of that report demanded: the task's exact verify chain
  (`GOEXPERIMENT=jsonv2 go build ./... && … go test ./... -count=1 -race
  -timeout 180s && test -z "$(gofmt -l .)"`), rc captured via file
  redirect + direct `$?` (PIPESTATUS hazard respected; log
  /tmp/f4-verify-attempt3.log, total 4m54.476s). Result rc=1 with the
  DECISIVE signature: the go test stage is ALL GREEN — every package ok
  (15 test packages; internal/e2e ok 181.601s, webui ok 12.793s), no FAIL
  line anywhere — and the failure is the final gofmt stage. Isolation:
  `gofmt -l .` → 44 files, ALL under gitignored `vendor/` (andybalholm/
  brotli et al., zero non-vendor hits), while
  `gofmt -l $(git ls-files '*.go')` is EMPTY (tracked tree gofmt-clean).
  The repo is healthy; the GATE cannot pass on this host.
- **Root cause + blast radius**: `vendor/` materialized for root builds in
  the 2026-09-17 vendor arc (AGENTS.md known-issues) and is gitignored
  (.gitignore:63); the minted gate's gofmt stage (`gofmt -l .`) predates
  vendor (`.tq-verify` mtime Sep 15 06:44) and began walking vendored
  third-party code the moment vendor/ appeared — deterministic since
  then, invisible to CI (runners have no vendor/). Corroboration beyond
  this window: `tq tasks --project go-taskqueue --status dead` lists FIVE
  dead agent tasks, all `agent verify failed` on this same gate at 3/3
  attempts — two tails opened and confirmed all-ok (000001a0bd9a…,
  created 2026-09-20 08:57; 000001a0c124…, created 2026-09-21 01:26).
  Five tasks × three attempts of spend burned on a gate that no code
  change could satisfy.
- **The two failure layers untangled**: (i) gofmt-vendor — STRUCTURAL,
  kills every verify on dev hosts with vendor/, regardless of e2e; (ii)
  e2e-vs-180s — load-marginal (240.029s kill at 02:45 under concurrent
  race-suite load from the 02-32 battery; ok 181.601s at ~03:05 on a
  quiet host), still open in its own backlog row. Attempt 1 died on (ii)
  and never reached (i); this window's run passed (ii) and exposed (i).
  The 02-53 framing ("gate-headroom burn risk") was necessary but not
  sufficient — the loop was never about headroom alone.
- **Loop closed per contract**: row 162 annotated in place with the
  attempt-3 re-verification + the structural finding (one count
  corrected during close-out, 100+→44, see §d4); TWO backlog rows filed
  next to the e2e row — the owner-gated gate fix (exact one-line
  replacement: `test -z "$(gofmt -l $(git ls-files '*.go'))"` in BOTH
  `.tq-verify` and the bootstrap mint template; BLOCKED per the
  never-edit-your-own-gate ruling) and an agent-executable forensic
  sweep of the dead-on-gate class; `check-todo-list.sh` rc=0; work
  commit b0f9dfc (single well-formed footer, hooks green, tree clean —
  NO daemon fold this window); AGENTS.md Known Issues bullet added
  (memory mandate: a future session loading AGENTS.md must know its
  verify gate lies); this report + README index row.
- **Post-turn outcome pre-registered** (row + §g2): the queue's verify of
  THIS attempt will fail at the gofmt stage the same deterministic way;
  the fresh attempt-3 verify-failure log
  (~/.local/state/tq/logs/000001a0c152893609690d1585860fe22d0f.verify-
  failure.log) is the durable twin of the ephemeral /tmp log and should
  show the same all-ok signature. Expectation recorded BEFORE the fact
  so outcome-vs-expectation is mechanical for any autopsy.

## b) PARTIALLY DONE

- **The verbatim run was framed, not pure**: wrapped in `time ( … )` —
  the command chain is identical to the payload, but the log holds only
  go test output (build/vet are silent-on-success), so ~70s of the
  4m54s total (package sums ≈ 224s) is unattributed overhead; stage
  timings inside the run are inference, not measurement.
- **Dead-task forensics depth**: tails confirmed for TWO of the five
  dead tasks; the other three share the truncated error head only. The
  sweep row exists precisely to finish this classification.
- **Prior-report coverage**: 02-53 read in full; 02-27 and 02-32 (the
  attempt-1 reports) NOT read this window — attempt 1's battery claims
  are inherited via 02-53's summary plus the docs-only delta argument,
  not first-hand (acceptable for a verify-stop on a final attempt with
  the verbatim gate now run fresh, but a coverage hole nonetheless).
- **e2e mechanics advanced by one hypothesis, not resolved**: e2e "ok" at
  181.601s wall UNDER `-timeout 180s` implies the in-test deadline does
  not cover TestMain's build-tq.sh window; attempt 1's kill message
  ("ran too long (4m0s)" = the go command's SIGQUIT watchdog, not the
  in-binary panic timer) is consistent but unreconciled. Row 197 owns
  it; deliberately not chased.
- **Expensive gates beyond the task's own**: lint-baseline, nix, smokes,
  cmd-tq gate not run — the verify-only minimum battery (cheap gates +
  one fresh delta) is satisfied, and the fresh delta is emphatic (the
  verbatim gate run itself), but the full ci-local replicant was out of
  scope for a stop window.

## c) NOT STARTED

- Nothing within the item's scope — the item shipped in attempt 1 and is
  fully closed (code, budget consumption, drift pin, docs, row). The
  gate fix is owner-only by ruling; the forensic sweep is its own
  dispatch; e2e trim, session-start tq-show integration, and the
  AGENTS.md ritual amendment are filed backlog. Deliberate non-starts,
  not omissions.

## d) TOTALLY FUCKED UP

- **Stage-sliced AFTER the expensive run, not before.** The gate command
  was in context from turn 2; a 2-second `gofmt -l .` check would have
  surfaced the structural red BEFORE burning five minutes of race suite
  — and, more importantly, would have de-confounded the experiment: had
  e2e timed out in MY run (it passed at 181.6s partly by quiet-host
  luck), I would have had a doubly-confounded failure and a much weaker
  finding. Cheap stages first, expensive stage only for the data point —
  isolate before integrate. The end state is right; the path was
  wasteful and lucky.
- **tq show landed in batch 2, not step 1** — the 02-53 §d headline
  ("led with the repo, not the queue") was READ by me at turn 1 and
  still half-repeated: session-start.sh + prior-report grep ran before
  the task record was consulted. Nothing queue-side changed in the five
  minutes of difference this time — luck, not method.
- **Attempt-1 reports unread** (§b3): for a FINAL attempt, reading all
  priors is cheap; 02-32's §g owner questions got only implicit answers.
- **Unverified count shipped to the durable record**: the row annotation
  said "`gofmt -l .` lists 100+ vendor/*.go" — extrapolated from a
  20-line head, never counted; the true number is 44 (counted during
  report prep, row corrected in this commit). Claim-before-evidence,
  exactly the 02-10 §d2 pattern, in a window that had read that pattern.
- **Minor**: the first filing of the sweep row tripped
  `check-todo-list.sh` ("for the owner" phrasing → UNBLOCKED-OWNER-GATED)
  and cost a rephrase loop before the commit; one look at the checker's
  rule would have avoided the delay. /tmp evidence cited in the row
  before designating its durable twin (the post-turn verify-failure
  log) — the citation order should be durable-first.

## e) WHAT WE SHOULD IMPROVE

- **Stage-slice compound gates as a battery rule**: when a gate fails —
  or before proving anything about one — run its cheap stages standalone
  first (gofmt/lint checks are seconds); add "isolate before integrate"
  to the verify-window minimum battery wording (§f17).
- **tq show as literal step 1** when a task id is known — before
  session-start.sh, not after it (second window in a row to half-apply
  02-53 §e1); the mechanical fix remains folding it into
  session-start.sh (02-53 §f11, still open).
- **Pre-register expected queue outcomes on final attempts**: verify-only
  windows on their last attempt should state, in the row or report, what
  the queue's verify will do and why (done here) — outcome comparison
  becomes mechanical and an autopsy can be judged against the recorded
  expectation instead of re-deriving it.
- **Gate stages must never walk untracked trees**: any gofmt/lint/vet
  stage scoped by `git ls-files` (or vendor/-excluding) is CI-parity by
  construction; the general lesson beyond this one gate — AGENTS.md
  Known Issues now says it for gofmt; the mint template is the owner's
  half.
- **check-todo-list.sh phrasing rule in AGENTS.md conventions** (one
  line: owner-recipient phrasing without a BLOCKED suffix trips the
  gate) — saves the rephrase loop every time it fires.
- **Dead-letter storms need one-command triage**: with 5×3 attempts
  burned on a structural gate, the sweep row is the highest-value next
  dispatch; it converts the DLQ into a rescue/dismiss list instead of N
  manual `tq show` + log reads.
- **Durable evidence twin cited at write time** next to any /tmp path.

## f) Up to 50 things we should get done next

Honest list — 17 items, no padding. Items 5-13 carried from 02-53 §f and
the live rows (re-checked open this window); 1-4 are new from this
window's discovery; 14-17 are carried mechanics now sharpened.

1. **OWNER gate fix** (new, blocks every queue-side verify in this repo):
   scope the gofmt stage to tracked files — `test -z "$(gofmt -l $(git
   ls-files '*.go'))"` — in the repo `.tq-verify` AND the bootstrap mint
   template; then re-baseline: no task verify on this host can pass
   until it lands.
2. **Forensic sweep of the gofmt-vendor dead-letter class** (new row):
   classify every dead-on-gate task since 09-17 from its verify-failure
   log (gofmt-stage all-ok vs e2e FAIL), check whether the work shipped
   anyway (footer commits + checked rows), produce the rescue/dismiss
   list as a report.
3. **Disposition of the dead-on-gate five** (owner move after 2):
   RescueDead for shipped-work tasks, dismiss the rest — restores five
   tasks' worth of mis-spent history to honest bookkeeping.
4. **Post-fix queue hygiene**: after the gate fix lands, requeue or
   dismiss THIS task's expected dead-letter per §g2 — and confirm the
   first post-fix verify runs all stages green end-to-end (the gate has
   not passed on this host since 09-17; treat the first green as an
   event worth citing).
5. e2e runtime trim + 4m0s-vs-180s reconcile (row 197; load-marginal
   layer now isolated from the structural layer).
6. `tq show` + webui detail: render derived session tokens/cost for
   status and prioritize tasks like agent tasks (unchecked row).
7. Four-type usage-key equality pinning test (unchecked row; budget_test
   marshal pin covers it partially).
8. `deriveSessionUsage` extraction from deriveOutcome for the
   commit-less executors (drops the wasted trailer/diff-tree scan per
   paid run).
9. Webui completion-cost sparkline now that four fact types carry usage.
10. Daemon-race structural fix (sweep-guard/lockfile in the daemon) —
    zero folds this window, but the class recurs across windows; the
    locus is the daemon.
11. Fold the docs/status/README.md index grep into session-start.sh
    (02-32 §e, still manual).
12. dlqfix autopsy usage-surface ruling (paid closeout-free turns).
13. `tq stats` status-usage smoke assertion once live data exists.
14. Score-cache TTL/eviction pass (row 165, unchecked).
15. session-start.sh prints `tq show` attempts/lastError/NotBefore when
    passed a task id (02-53 §f11).
16. Executor evidence tail for verify failures should PREPEND the go
    test driver's FAIL summary lines (02-53 §f12; would have made
    attempt 1's failing package readable at a glance — and would make
    all-ok gofmt-stage deaths self-identifying if paired with the
    gate's rc per stage).
17. AGENTS.md ritual amendment batch: queue-verdict line + same-command
    gates + stage-slicing + "gates never walk untracked trees" + the
    check-todo-list phrasing note (02-53 §f13 + this window's §e).

## g) Up to 3 QUESTIONS (owner-only calls)

1. **Gate-fix semantics + agency**: is `test -z "$(gofmt -l $(git
   ls-files '*.go'))"` the blessed replacement for the gofmt stage
   (tracked-scope, CI-parity by construction — my recommendation), or do
   you prefer path-excluding vendor (`gofmt -l . | grep -v '^vendor/'`)?
   And once semantics are blessed: may an agent land the MINT-TEMPLATE
   half (bootstrap auto-detect lives in repo code), or is the entire
   gate surface permanently owner-only? Five tasks' worth of spend sits
   behind this one edit.
2. **Dead-on-gate disposition + autopsy pre-emption**: after the sweep,
   mass-rescue the dead tasks whose work shipped (RescueDead, original
   budget) and dismiss the rest? Specifically for THIS task: its
   dead-letter is expected and documented — should the dlqfix autopsy be
   pre-empted (dismiss on arrival; the autopsy would spend an agent run
   re-deriving what this report already concludes), or do you want it to
   fire as a live test of the autopsy path on a known-DONE item?
3. **e2e cap ruling** (carried from 02-53 §g3, sharper now that the
   structural layer is isolated): with gofmt fixed, e2e is the remaining
   marginal stage — 181.6s observed-ok vs the 180s stage cap, 240s kill
   under concurrent load. Raise the `.tq-verify` cap (one owner edit) or
   trim/parallelize e2e in code (repo work, trades some determinism)?
