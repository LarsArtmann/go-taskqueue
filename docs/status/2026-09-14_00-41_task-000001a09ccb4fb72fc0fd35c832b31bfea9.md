# Status Report — task-000001a09ccb4fb72fc0fd35c832b31bfea9

> **ANNOTATED 14 Sep 2026 (docs-health strikethrough pass)** — every numbered item below is resolved inline (`done`/`duplicate` markers) or left untouched (= still open). Unarchived on purpose: open items remain. Evidence verified against HEAD.

**2026-09-14 00-41** · Proxy-installability task, second dispatch (dispatch-honoring
duplicate) · verify-only window, zero code changes

> Format override: the status-report skill defaults to a styled HTML dashboard;
> the dispatch explicitly requested `.md` at this exact path — user instruction
> wins. One-off, not propagated into the skill.
>
> **Sibling report**: this task ID already carries
> `2026-09-14_00-37_task-000001a09ccb4fb72fc0fd35c832b31bfea9.md` (written in
> the same task's report turn, commit 3fb6c63). That report owns the canonical
> evidence trail and the full 50-item next-work list. THIS report is the
> second dispatch's independent window: it re-verified the closed state from a
> cold start, adds gate results, and lists only NEW/CHANGED items — items
> already on the 00-37 list are referenced by number, not restated.

## Work item

TODO_LIST row 46 — "Restore proxy-installability of the tq binary
(`go install …/cmd/tq@vX.Y.Z`)… cmd/tq as its own replace-free module".

## a) FULLY DONE

1. ~~**Task found already complete and properly closed** — TODO_LIST.md:46 is~~ done — row closed, 12-45 continuation (verified at HEAD)
   `[x]` with a full DONE annotation citing this exact task ID, commit
   **113dcb1** (ADR-0017 evidence: zero replaces in cmd/tq/go.mod,
   tagged-only requires, release.sh clean-room gate, RELEASE.md literals).
   The correct action was verification, not re-implementation — and the row's
   evidence held under re-verification.
2. ~~**Independent re-verification (this window, cold start)**:~~ done — replace-free at HEAD (verified at HEAD)
   - `cmd/tq/go.mod` — `grep -c replace` → **0** (replace-free invariant of
     ADR-0017 holds at HEAD 3fb6c63).
   - `cd cmd/tq && GOWORK=off GOEXPERIMENT=jsonv2 go build ./... && go vet ./...`
     → **green** (the sanctioned in-repo path per AGENTS.md).
   - Root module `go build ./...` + `go vet ./...` → **green**.
   - `scripts/build-tq.sh /tmp/tq-check` (the devmod shim, ADR-0017) →
     **green**.
3. ~~Working tree clean at start and end; no daemon races, no concurrent~~ done — narrative (point-in-time process note, no artifact owed)
   drift observed during the window.

## b) PARTIALLY DONE

1. **The literal `go install …/cmd/tq@vX` remains unprovable in-session**
   (00-37 §b1 and §g2 — unchanged; permission layer denies the verb).
2. ~~**Full `ci-local.sh` / `-race` / sub-module loop not run** — verify-only~~ resolved — routed TODO 143 (verified at HEAD)
   window, docs unchanged by this window; declaring the skip here per the
   00-37 §e4 improvement (declare skipped gates, don't bury them).
3. ~~**The 50-item 00-37 f-list is untouched** — none of its items (v0.4.0 cut,~~ done — narrative (point-in-time process note, no artifact owed)
   cleanroom-install smoke, tq.exe removal, …) were actioned; they remain the
   open harvest surface.

## c) NOT STARTED

Nothing in-scope for row 46 (complete). Noticed in passing, untouched:

- The sibling 00-37 f-list items #1–#50 (all open; the high-impact head is:
  v0.4.0 cut, cleanroom-install smoke script, stray `cmd/tq/tq.exe` removal,
  v0.3.0 adopter-facing known-issue notes).
- Open TODO rows 148, 271, 275, 276, 290 (BLOCKED owner) — same state as
  00-37 §c.

## d) TOTALLY FUCKED UP (honest misses, this window)

1. **Ran a gate that the design says fails by design, then had to interpret
   it.** `go test ./cmd/tq/ -count=1` from the ROOT module dies with
   ambiguous-import (the module cache's `go-taskqueue@v0.3.0` zip still
   ships `cmd/tq/*.go` inside the replace-laden root module). AGENTS.md
   already documents the same-by-design class for `go test ./internal/foo`
   from root, and 00-37 §d4/§b2 document the ambiguous-import from the
   in-repo clean-room attempt — I re-derived a known failure mode because I
   reached for a root-relative `./cmd/tq` path out of habit instead of the
   documented per-module gate or `scripts/build-tq.sh` first.
2. **Did not grep for prior same-ID reports at turn 1.** The 00-37 report
   (same task ID) existed and was committed 4 minutes earlier; only at the
   status-report turn did I read it. The 00-37 §e1 turn-1 checklist called
   exactly this and it was not applied — the improvement died in its birth
   window.
3. **Work-turn TQ_RESULT repeated the short-hash ambiguity** (`113dcb1`),
   which 00-37 §d5 already flagged as carried-and-unresolved (04-07 §g2).
4. **No new value beyond verification.** The honest verdict: this window's
   total contribution is a second gate run plus this report. A turn-1 grep
   would have converted it into e.g. knocking out one 00-37 f-item (#3
   tq.exe removal was a 2-minute job) instead of a pure re-read.

## e) WHAT WE SHOULD IMPROVE

1. **Turn-1 same-ID grep is now proven necessary twice in one hour** — the
   session-start ritual should include: "grep docs/status for the task ID
   before acting". 00-37 §e1 proposed it; this window is the evidence it
   must become mechanical, not aspirational.
2. ~~**A duplicate dispatch should downgrade to verify-and-reference, loudly.**~~ resolved — routed dedup rulings (verified at HEAD)
   When the row is `[x]` with a DONE annotation citing the SAME task ID, the
   correct output is a short verification + explicit pointer, not a full
   re-derivation. Codify the "already done with matching ID" branch.
3. ~~**The ambiguous-import failure class needs one canonical AGENTS.md~~ done — AGENTS.md ambiguous-import note (verified at HEAD)
   sentence** covering BOTH triggers (root-relative `./cmd/tq` paths AND
   in-repo clean-room copies) with the two sanctioned workarounds
   (per-module cd / build-tq.sh; /tmp copy). 00-37 f-item #19 asked for the
   clean-room half; the root-relative half belongs in the same note.
4. Skipped-gate declaration in the work turn (adopted this window, §b2) —
   make it the standing rule.

## f) Next-work list

**The canonical 50 items live in the sibling report**,
`2026-09-14_00-37_task-000001a09ccb4fb72fc0fd35c832b31bfea9.md` §f (#1–#50,
all verified still open as of this window). Rather than duplicating them
(harvest would double-count), this window adds only NEW/CHANGED items:

1. **Execute 00-37 f#3 now-cheaply**: remove stray `cmd/tq/tq.exe` — re-
   confirmed still present and still unexplained (2-min job, highest
   effort/reward ratio on the board).
2. ~~**Extend 00-37 f#19's AGENTS.md ambiguous-import note** to cover the~~ done — AGENTS.md:30-32 trigger (verified at HEAD)
   root-relative `./cmd/tq` trigger (this window's §d1 repro).
3. **Turn-1 checklist, concrete form**: a pre-work step that greps
   `docs/status` and `git log --grep` for the dispatch's task ID; on a hit
   with a matching-ID DONE annotation, switch to the verify-and-reference
   branch (this report §e2).
4. **00-37 f#2 cleanroom-install smoke script** — this window re-proved the
   need: every verify window re-derives the GOWORK=off copy-build recipe by
   hand. Codify it once.
5. ~~**docs-health HARVEST note**: when harvesting this task's reports, dedupe~~ done — narrative harvest instruction (verified at HEAD)
   on the shared task ID — harvest f-items from 00-37 ONLY, treat this
   report as evidence context.
6. ~~**00-37 f#27** (post-annotation `-race` re-run) remains open and now~~ done — 12-45 root -race green (verified at HEAD)
   applies to two consecutive docs-only windows; batch it with the next
   code-touching task rather than a dedicated run.

(Items 1–6 + the 00-37 list = the full board for this task lineage; nothing
else was observed outstanding.)

## g) Questions I can NOT figure out myself

(Carry-overs from 00-37 §g — all three remain owner-decisions this window
cannot resolve; no new questions arose from a verify-only pass.)

1. ~~**TQ_RESULT `commit_sha`: full 40-char SHA or short hash?** (00-37 §g1;~~ done — superseded TQ_RESULT retired (verified at HEAD)
   this window repeated the ambiguity in its own TQ_RESULT — a ruling fixes
   it permanently.)
2. ~~**Should `go install <module>@<version>` be allowlisted for agent~~ done — AGENTS.md fallback codified (verified at HEAD)
   sessions, or is the GOWORK=off copy-build the official codified
   fallback?** (00-37 §g2; unchanged.)
3. ~~**v0.3.0: is the CHANGELOG/GitHub-Release known-issue note sufficient, or~~ done — CHANGELOG note landed (verified at HEAD)
   do you want `retract`?** (00-37 §g3; owner call on the public release
   surface.)
