# First window on the live-no-id smoke task — task 000001a0b6ceb2a69a870f8855c3b7f7536b

2026-09-19 01:22. Work item (done-prompt-window harvest section): "Live-no-id
smoke assertion: wrapper exit preserves child code, nothing closed, no DB
touched (no-id path is unit-pinned; live path isn't) (02-54 §f)". First
dispatch of this ID — the turn-1 footer grep over docs/status returned zero
prior reports. Deliverable: the committed live no-id leg in
`scripts/smoke/session-close.sh`. Work commits 0dd02a7 (leg + TODO close) and
2d86bef (stdout-forwarding pin + header currency + citation strengthening);
this report + index row is the third footer commit. No Go code touched.

## a) FULLY DONE

1. **Session-start ritual complete:** whole-repo `git log --oneline -5`,
   `git status` (clean), `git stash list` (empty); footer grep over
   docs/status (no prior windows on this ID); CONTRIBUTING.md read in full;
   CLAUDE.md confirmed absent.
2. **The live no-id leg shipped** (session-close.sh:86-112 at 2d86bef): a
   real `$TQ crush` process wraps a `/bin/sh` stub that prints no session
   marker and exits 7, then asserts — (i) wrapper process exit code == 7
   (passthrough of a NON-ZERO child code, the interesting half; the unit pin
   uses 0), (ii) the child's stdout line lands on the wrapper's stdout (the
   live-visibility property), (iii) stderr carries the honest "no session
   id" note, (iv) the explicit `--db` target file NEVER exists — nothing
   closed, nothing minted, no DB touched.
3. **TODO_LIST.md:123 closed `[x]`** with a DONE note citing the exact
   assertions, the bare-exit-0 smoke run, the negative control, and the
   shellcheck gate; the note was strengthened in 2d86bef when the coverage
   grew (citations current with the tree).
4. **Gates green, exit codes captured BARE** (the pipeline-masking lesson
   applied from the second pass on): smoke exit 0 twice (pre- and
   post-strengthening); `scripts/check-script-syntax.sh` exit 0 (bash -n +
   shellcheck, zero findings); `scripts/check-todo-list.sh` exit 0 twice
   (before and after the row edit).
5. **Negative control proves the harness bites:** a mutated copy of the
   smoke (expects 8) exits 1 with "FAIL: wrapper exit code 7, want the
   child's 7". Scratch copy create+assert+trash in ONE chain inside
   scripts/smoke/ (no daemon-food), `TQ_BIN` used to skip the rebuild. The
   first /tmp attempt failed exit 127 (see §d2) — the script's
   `cd "$(dirname "$0")/../.."` root anchor forbids off-tree copies.
6. **Commit hygiene:** 0dd02a7 and 2d86bef, each carrying exactly one
   well-formed `Task-Queue-ID` footer (commit-msg hook passed both);
   `git status` re-checked immediately before each `git add` (daemon race);
   nothing pushed.
7. **cmd/tq LSP phantoms correctly ignored:** gopls reported
   `runSessionClose`/`resolveDB`/`dbFlag` "undefined" all window — the known
   multi-module lie (AGENTS.md; "No packages found for open file"). Trusted
   the CLI: the smoke builds the CLI through the devmod shim and runs it
   live, twice.

## b) PARTIALLY DONE

1. **Gate discipline arrived mid-window.** The FIRST smoke run and the early
   gate runs were piped through `tail` — exit codes not captured, the exact
   pipeline-masking class the global AGENTS.md warns about (`set -e`
   without `pipefail` + content filters turning a red run green). No claim
   in the committed record rests on a masked run (§a4 cites only bare
   runs), but the chat-visible first "smoke passed" did.
2. **The DONE note took two commits to become fully accurate.** The
   stdout-forwarding pin and the negative control landed in 2d86bef, after
   the first note shipped in 0dd02a7. Honest, but citations should be
   complete at first write.

## c) NOT STARTED (all confirmed open with fresh reads THIS window)

1. **TODO 126 — the positive wrapper leg** (footer commit + `session_id:`
   output → id scan → 1 review + 1 status minted through `runSessionClose`
   - exit passthrough). This window shipped the NEGATIVE half as a
     committed gate; the row's "the wrapper's only E2E today is unit pins +
     the ad-hoc stub run" clause is now half-stale. Untouched by decision —
     separate row, no scope creep.
2. **TODO 124 (BLOCKED: owner ruling on CLI gosec coverage) and 125
   (GOSEC_BIN version verify)** — confirmed open verbatim; adjacent to this
   section but not mine.
3. **TODO 126's wording predates this window:** it says "wire into ci-local
   after session-close.sh", written when a separate script was assumed. The
   no-id leg folded INTO session-close.sh, making ci-local wiring automatic;
   the row should be re-worded when picked up (§e6).
4. **No new backlog items appended:** nothing surfaced that is not already
   a row. Forcing one would have been padding.

## d) TOTALLY FUCKED UP

1. **Read-before-edit violation on the first TODO edit** — the tool
   rejected it ("you must read the file before editing"); I had only
   sed-viewed lines 118-126 through bash, never View. The exact-match
   failure was the SAFE failure mode (zero corruption), but the rule exists
   because sed-context edits corrupt formatting silently when they don't
   fail. One round trip lost.
2. **Negative control's first invocation failed exit 127** — I copied the
   smoke to /tmp without thinking about its relative `cd` root anchor. The
   control eventually bit (§a5), but the failed attempt proves I ran a
   script whose environment assumptions I hadn't read back to myself.
3. Nothing else: no tree damage, no stray untracked files, no history
   edits, no pushes, no reverted foreign work.

## e) WHAT WE SHOULD IMPROVE

1. **Prove every new smoke leg bites BEFORE the work commit.** The negative
   control belonged in 0dd02a7, not the strengthening commit. One mutated
   run per new assertion block is minutes-cheap.
2. **Pipe discipline:** capture gate exit codes bare (`cmd >f; echo $?`),
   shape content AFTER. The masking instinct came back the moment the
   first gate ran.
3. **Header currency in the same commit as the leg:** session-close.sh's
   header described only the begin/close bridge until 2d86bef. A file that
   grows legs should describe them when they land.
4. **Stub-family growth:** the wrapper legs add the second inline heredoc
   stub to this script; when TODO 126 adds a third, factor one `mkstub`
   helper at the top instead of a fourth heredoc.
5. **Index bloat is real:** the digest row says live rows ~143 vs the 100
   threshold — every report (including this one) widens the gap; the next
   ANNOTATE sweep is overdue by the README's own cadence rule.
6. **Re-word TODO 126's wiring clause** when picked up so the next window
   doesn't re-derive the placement question (§c3).

## f) NEXT (honest count: 13 — refusing to pad to 50)

1. **Positive wrapper leg** (TODO 126): fold into session-close.sh, own
   `--db`, scope the review/status count assertions per-DB, then close the
   row with a live citation.
2. **Re-word TODO 126's wiring clause** (§c3/§e6) as part of item 1.
3. **Close-failure exit contract** (see §g1): decide whether a failed
   session-close should mask the child's exit code with 1, then pin it.
4. **Signal-death passthrough** (see §g2): decide live-pin vs unit-only for
   `forwardSignals`.
5. **Extend `TestCrushWrapperNoSessionID`** with the stdout-forwarding
   assertion — the live pin now leads the unit pin by one property.
6. **AGENTS.md session-close smoke bullet** still describes the script as
   begin→close→replay only; add the wrapper legs (one line, next docs
   touch).
7. **Grep for sibling "unit-pinned; live path isn't" asymmetries** in
   TODO_LIST.md — the phrasing pattern suggests more exist; one grep.
8. **After item 1 lands**, re-read the ci-local step names/durations for
   the smoke (one build, both legs — should be a no-op, verify once).
9. **ANNOTATE/archive sweep** for the status index (§e5) — owner-cadenced,
   flagged here because this report adds row ~144.
10. **Shared smoke-stub helper** across status-loop/reviews/questions-e2e/
    session-close — only if a fourth stub copy appears (§e4).
11. **Keep the `smoke:` commit-message prefix** for the follow-up leg
    (matches 0dd02a7/2d86bef and the repo's recent history).
12. **Re-run `./scripts/ci-local.sh` before any push** — the tree is now 12
    commits ahead of origin; push is owner-run, the gate is not optional.
13. **Mention the negative-control trick** (in-tree scratch copy + TQ_BIN)
    in the smoke-writing lore if a docs-health pass collects it — it is the
    cheapest bite-proof available.

## g) QUESTIONS (3, genuinely not self-answerable)

1. **Close-failure exit contract:** if `tq session close` FAILS inside the
   wrapper (unwritable DB, store error), `crushRun` returns the child's
   code PLUS an error, and `cmdCrush` returns the error — the wrapper then
   exits 1, masking the child's code (cmd/tq/crush.go:24-37,
   cmd/tq/crush.go:114-118). My no-id leg pins passthrough only on the
   no-close path. Which is the contract: preserve the child's code and
   print the close error, or is exit 1 correct because the close failure
   is the operator's last concern? Either is implementable in minutes; the
   intent is an owner ruling.
2. **Signal-death passthrough, live-pinned or not:** `forwardSignals`
   (cmd/tq/crush.go:123-133) has no live coverage anywhere — a SIGTERM'd
   child surfacing 143 through the wrapper is unpinned end-to-end. My lean:
   NOT in CI (interrupting real children in smokes is flake-prone), maybe a
   documented manual check. Owner call: accept the gap, pin it manually, or
   eat the flake risk?
3. **TODO 126 placement** (§c3): my lean is fold-in — one script, both
   wrapper legs, ci-local wiring automatic, separate `--db` keeps the count
   assertions clean. Confirm, or is there a reason the row wanted a
   standalone script (runtime budget? failure isolation? report legibility)?
