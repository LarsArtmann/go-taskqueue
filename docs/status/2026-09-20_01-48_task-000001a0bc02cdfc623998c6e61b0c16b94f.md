# Task 000001a0bc02cdfc623998c6e61b0c16b94f — Third Window (verify-only re-dispatch, close-out self-review)

Date: 2026-09-20 01:48 CEST · Window type: verify→cite→stop · Source delta since fix commit 39d340e: ZERO

## Context

This is the THIRD window on the gosec ci.yml-derivation-drift-guard ID
(TODO_LIST row 136; lineage: fix 39d340e → row closure 6fb6256 → prior
close-outs 5cebdd7, b47c741). The dispatch was a re-send of the original
work item; the correct response per the session-start ritual is
verify → cite → stop, which is what this window did.

## a) FULLY DONE

- **The work item itself** (shipped in the first window, re-verified twice since): `scripts/check-gosec.sh --self-test` lifts ci.yml's EXACT sed lines (ci.yml:286-287), evals them against the shipped script, fails closed if either line is missing/empty, and byte-compares both extractions against the pinned `GOSEC_VERSION`/`GOSEC_EXCLUDES` constants. Wired into ci-local (scripts/ci-local.sh:111).
- **This window's fresh verification**: `./scripts/check-gosec.sh --self-test` re-executed at HEAD, rc=0, all 32 ok-lines present including `ok: ci.yml derivation matches the pinned constants byte-for-byte` and the foreign-CWD re-entry branch. Full battery output captured in-session.
- **Session-start ritual executed**: `git log --oneline -8` (whole repo), `git status` (clean) before any judgment.
- TODO_LIST row 136 confirmed `[x]` with the full evidence annotation intact (fix commit, negative control, gate counts).

## b) PARTIALLY DONE

Nothing in scope. The window's own battery is disclosed under (d3): green
path executed fresh; negative control CITED (10-minute-old, zero source
delta) rather than re-executed.

## c) NOT STARTED

Nothing in scope. No follow-up work was discovered that belongs in this
run; no new TODO rows minted (consistent with the prior windows' ruling).

## d) TOTALLY FUCKED UP (this window's honest misses)

1. **Turn-1 prior-report grep skipped** — the ritual requires `rg -l <task-id> docs/status/` BEFORE anything. I found the two prior close-outs via `git log` and the index tail instead — right answer, wrong (unreliable) door. This is a VERBATIM recurrence of the 01-44 window's §d1 miss, one read after its documentation. Third-plus occurrence in this task family.
2. **CONTRIBUTING.md probed late** — existence/read happened only after the follow-up dispatch, not turn 1. The family counter (N+18, N+19 documented in the two prior windows) extends to N+20. The 01-20 window on the sibling ID finally read it in full and found a ~400-finding baseline claim that disagrees with AGENTS.md's 500+ counts — still unreconciled.
3. **Negative control cited, not re-executed** — the 01-44 window set the bar at a fresh /tmp-worktree negative run (mangled pin → rc=1 + exact FAIL line); this window re-ran only the green path and cited the 10-minute-old negative. Defensible (zero source delta, no ci.yml/check-gosec.sh byte changed — `git status` clean at HEAD b47c741), but it is a lower bar than the family's most recent standard, decided implicitly instead of explicitly.

No damage: zero source edits, zero commits, tree untouched.

## e) WHAT WE SHOULD IMPROVE

- **Catch re-dispatches mechanically at turn 1**: the FIRST command of any work-item window should be `rg -l <task-id> docs/status/ && grep -n <task-id> TODO_LIST.md` — not git log, not the file. The task family has now paid for this miss in at least four windows across two IDs.
- **Define the verify-window battery bar once** (green path only vs. green + negative control) instead of each window re-deciding it under time pressure and landing on different answers.
- **Make CONTRIBUTING.md read a scripted turn-1 step** (same fix class as above) — willpower-based rituals keep reccurring; mechanical ones don't.

## f) Things we should get done next (carried + observed; NO new mints — the queue/pacing gates own admission)

Carried unresolved from this task family's prior windows (rows noted where they exist):

1. No-op-commit policy for verify-only windows (window-2 §g1, still unanswered).
2. Stale dispatch-line pins: the queue keeps re-dispatching DONE rows — queue-side filter or dispatch-payload staleness check (window-1 §g1).
3. /tmp-worktree negative-control pattern as the codified standard for gate self-test verification (window-2 §g3; partially ratified as row-138 fix).
4. TODO_LIST rows 124/127 (window-2 declared them carried; unresolved here).
5. CONTRIBUTING.md "~400-finding lint baseline" vs AGENTS.md "500+/1089/1113 counts" — reconcile the stale claim in CONTRIBUTING (observed in passing via the 01-20 window's index row, confirmed by this window's read).
6. Verify-window battery bar codification (this window's e2).
7. Turn-1 grep ritual enforcement (this window's e1) — a hook or check-script candidate.
8. Heuristic-baseline count in CONTRIBUTING is one instance of a class: doc-internal numeric claims have no drift gate (check-features-roadmap.sh covers shipped-vs-planned, not prose numbers).
9. check-gosec.sh canary list and ci.yml derivation both now have self-tests — the remaining unguarded sed-derivation class in ci.yml (if any) has never been enumerated; a one-shot audit would close the family.
10. Index-row summaries contain `\x27` escapes (visible in docs/status/README.md rows 451-452) — cosmetic encoding drift worth a sweep.

Items 1-7 are already represented in TODO_LIST or prior reports; 8-10 are
new observations from passing, offered to the harvest pass rather than
minted unilaterally.

## g) Questions I can NOT figure out myself

1. **No-op-commit policy**: for a zero-delta verify-only window, should the window ship a citation-only commit (row annotation refresh) or stay commit-less like this one? Two windows on this ID chose opposite answers.
2. **Queue-side re-dispatch filtering**: is the repeated dispatch of DONE rows (this ID: 3 windows in ~15 minutes) an accepted cost of the at-least-once queue, or should the queue suppress re-claims of tasks whose TODO row is `[x]` with a recent verification citation?
3. **Verify-window battery bar**: is green-path-only fresh execution sufficient for a verify window when the tree is provably byte-identical to the last fully-batteried window, or must the negative control re-execute every time?
