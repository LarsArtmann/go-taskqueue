# Task 000001a0bdc8bf5b31d5faa75823b594d10a — Verify-only re-dispatch (seventh window)

Time: 2026-09-20 10:01. Scope: "Executor usage parsing: populate the `tokens` field on
PrioritizeResult/agent results" — the usage-derivation item.

## a) FULLY DONE

- **Turn-1 repeat-grep** found five prior windows on this ID before any edit
  (08-43/08-47/08-52/09-05 work windows, 09-52 close-out, 09-57 verify-only close-out).
  This window is the SEVENTH dispatch and the second consecutive correct stop.
- **Work confirmed shipped at HEAD** (commit b6b115d): `SessionPromptTokens` /
  `SessionCompletionTokens` / `SessionCostUSD` / `SessionMessageCount` derived via
  `deriveOutcome` in `internal/executor/prioritize.go:184` and `agent.go:312`,
  JSON keys pinned in `internal/executor/result.go:36-37`. TODO_LIST.md line 159 is `[x]`.
- **Cheap verify battery re-run at HEAD, all green** (the verify-window minimum):
  root build+vet rc=0 (GOEXPERIMENT=jsonv2), executor module tests rc=0
  (10.8s, -count=1, output to /tmp file, rc captured directly — no PIPESTATUS), check-doc-refs rc=0.
- Tree clean before and after; nothing committed for the work itself — there was
  no work to commit (correct: a verify-only window with no delta must not manufacture one).

## b) PARTIALLY DONE

- Nothing within this task's scope. The shipped state fully satisfies the item text;
  the one nuance (the item's "always 0 today" premise is now false — usage IS populated)
  is exactly what the prior windows shipped and what this window re-verified.

## c) NOT STARTED

- Nothing for this item. Backlog rows already exist for the genuine follow-ups:
  budget projections over the derived usage (line 160), the JSON-key pinning test
  (line 161), and the live-scorer cost measurement gate (line 166, owner spend go/no-go).

## d) TOTALLY FUCKED UP

- **Nothing in this window.** The near-miss class: had the turn-1 repeat-grep been
  skipped, this would have been the EIGHTH no-op dispatch and a re-work risk.
  The system-level failure stands: **the queue re-dispatched a DONE task 7+ times
  in one morning** (f1, filed from the 09-52 window). Every agent session is one
  grep away from the truth; the mint pipeline apparently is not.

## e) WHAT WE SHOULD IMPROVE

1. **Queue-side DONE-row guard** (the real fix, f1): harvest/mint must skip or
   demote items whose TODO row is `[x]` — or the dedup key must incorporate row
   status. Seven windows is the measured cost of not having this.
2. Close-out reports for repeat dispatches are cheap and useful; keep the
   verify-only precedent (fresh battery at HEAD, no inherited gate citations).
3. Consider a `tq doctor` / pool-start check: "pending dispatches whose
   TODO_LIST row is checked" as an operator-visible warning.

## f) NEXT (genuine follow-ups, mostly already in the backlog)

1. Budget projections: sum derived session tokens/cost from completion facts into the daily-cap projection (TODO line 160; internal/budget).
2. Pinning test: PrioritizeResult usage JSON keys == AgentResult's (TODO line 161).
3. Fix the DONE-row re-dispatch at the mint layer (09-52 §f1) — highest leverage.
4. Enable `--prioritize` on the dogfood pool after the one-batch live cost measurement (owner spend go/no-go, TODO line 166).
5. Property test: N injected non-overlapping fake tokens → SecretHits == N (line 270, unrelated but queued).
6. Root-cause the morning's repeated dispatches in the journal (`tq audit --journal` over the task's facts) once the mint guard lands, to confirm the fix.

## g) QUESTIONS (owner-only)

1. **Mint policy**: should a re-dispatch of an item whose TODO row is `[x]` be
   (a) silently skipped at harvest, (b) minted at machine-band priority as a
   verify-only task, or (c) left as-is pending a `tq doctor` warning? Each has
   different failure modes for legitimately-reopened rows.
2. Should the seventh-window reports (09-57, this one) be archived as a batch
   once the mint guard ships, to keep docs/status/ from filling with
   verify-only twins of one task?
3. For the live-scorer cost measurement (line 166): what one-batch spend ceiling
   (USD) is authorized for the T28 scratch-journal run?
