# Status Report — task 000001a08e6424643bb1a854bcb689f5f4ce

**When:** 2026-09-11 05:05
**Work item:** "module-eval hardening: extend the flake module-eval check to assert the tq-agent-pool unit's Environment carries a non-empty PATH (02:00 f11; flake.nix, deploy/nixos/tq-agent-pool.nix)"
**Outcome:** ALREADY DONE — stale re-dispatch (third close-out for this task ID, after the 04:59 and 05:02 reports this morning alone). Work pre-existed in 76ecf9a; re-verified green; no new changes.

## The one-paragraph story

Session-start ritual (`git log --oneline -5`) immediately revealed the work
shipped: 76ecf9a "fix(nix): module-eval asserts pool unit Environment carries
non-empty PATH", with close-out chain a5a77c3 → 3a83348. Instead of trusting
history, I re-verified: (1) `poolHasPath` helper exists at flake.nix:230 and is
ANDed into the assertions at flake.nix:262; (2) TODO_LIST.md:78 is `[x]` with a
dated done-annotation; (3) `nix build .#checks.x86_64-linux.module-eval` exit 0
on current master. No diff, no work commit; this report is the only artifact of
this session.

## a) FULLY DONE

- **The work item itself** (prior session, 76ecf9a): checks.module-eval now
  evaluates the deployed-shape tq-agent-pool NixOS module and asserts via
  `poolHasPath` that some `Environment` entry matches `PATH=.+` — a permanent
  regression gate against the 2026-09-10 pool-deploy failure mode (systemd
  default PATH lacked git/go/crush → every harvest scan skipped).
- **TODO_LIST loop closure** (prior session): item at TODO_LIST.md:78 marked
  `[x]` with done-annotation naming the helper.
- **This session's independent re-verification:** module-eval check green on
  current master; assertion location confirmed by grep, not memory.
- **Prior close-outs:** two reports this morning (04:59, 05:02) plus
  3a83348's stale-re-dispatch note already document the same verdict.

## b) PARTIALLY DONE

- Nothing owned by this task. The 02:00 self-review §f harvest list (50 items)
  partially remains: TODO_LIST.md:78 was f11 of that list; sibling items are
  tracked in TODO_LIST.md itself.

## c) NOT STARTED

- Nothing for this work item — there is no remaining sub-step (no code, no
  docs, no TODO edits were required this session).

## d) TOTALLY FUCKED UP

- Nothing broken. The honest wobbles worth naming:
  1. **The dispatch itself is the failure** — this task ID was re-claimed at
     least four times (2 commits + 3 reports today). Each wasted a session
     slot on `git log` + one nix build. Root cause: harvested items are
     re-enqueued without checking the TODO_LIST `[x]` state or the fact
     stream for a prior `task.completed` on the same ID.
  2. **My first turn ended without the commit-footer ceremony** — correct in
     substance (nothing to commit), but a dispatcher pattern-matching on
     "no new commit sha" might have misread it. The 05:02 session had the
     identical shape; this suggests the gate tolerates it, but it is a
     recurring ambiguity.

## e) WHAT WE SHOULD IMPROVE

1. **Stale-dispatch guard at claim time**: before a task's work turn, check
   TODO_LIST checkbox state and the journal for prior completions of the ID;
   short-circuit to a zero-cost close-out.
2. **Harvest dedup**: harvest should skip items already `[x]` in the source
   file — the 02:00 harvest listed f11 after it shipped (76ecf9a predates it).
3. **Close-out tombstones**: when a stale re-dispatch is detected, append a
   one-line marker to the item (`— DONE <sha>`) if not already present, so
   the next claimer sees it without a git log.
4. **Session-cost telemetry**: stale re-dispatches burn a nix build (~1 min)
   each; a "re-verify only" contract variant would make the wasted work
   explicit and cheap.
5. **TQ_RESULT for no-op sessions**: the queue's schema allows optional
   fields — make "already done, sha X" a first-class outcome so dashboards
   stop counting them as full work turns.

## f) Up to 50 things we should get done next

Harvested from what I saw in passing this session (TODO_LIST.md, AGENTS.md
known-issues, and the stale-dispatch meta-problem). Priority-ish order:

**Meta / queue hygiene (highest leverage — this exact failure repeated 4×):**
1. Claim-time stale-dispatch guard (journal + TODO_LIST check before work).
2. Harvest skips already-checked `[x]` items.
3. Tombstone annotation on first stale re-dispatch detection.
4. First-class "already-done" TQ_RESULT outcome.
5. Cap re-dispatches per task ID (n-th dispatch becomes a no-op report).

**From AGENTS.md known-issues (concrete, verified problems):**
6. Runner-only vendorHash FOD mismatch class (2026-09-10 report d1):
   differential-dump the runner's module fetch; no local repro exists yet.
7. Kernel ETXTBSY anomaly is worked around per-exec-site; document the
   `execWithTransientRetry` escape hatch for NEW exec sites in one place.
8. `TQ_DB` env leaks into agent shells (cost a stray production enqueue):
   consider refusing `defaultDB()` env fallback when cwd has no repo markers,
   or log a loud warning on env-backed DB use.
9. golangci advisory baseline ~400 findings: shrink the baseline in
   bounded batches (one package per pass), keeping the "no new findings in
   touched functions" rule.
10. Templ LSP phantom diagnostics: consider a documented gopls cache-reset
    command so agents stop re-learning "trust the CLI".

**Dogfood / operations:**
11. dogfood journal `/mnt/pool/services/tq/tq.db`: verify backup story
    (sqlite WAL checkpoint + copy) exists at all.
12. `tq doctor` could assert the deployed unit PATH (reuse `poolHasPath`
    logic at runtime, not just eval time).
13. Add an SMOKE for `--agentPath` propagation in the NixOS module
    (the 2026-09-10 deploy failure escaped both eval and unit tests).
14. Status-every / review sweeper lag metrics exposed on the webui metrics row.
15. `tq watermarks` show could surface consumer staleness age, not just seq.

**WebUI:**
16. Filter bar: encode filter state in the URL so refresh keeps it (the
    deadened `q.Get("query")` bug class suggests fragility here).
17. Task detail page: show FailureEvidence tail (stage/exit/tail) if not
    already surfaced.
18. SSE reconnect backoff visibility (hub lag indicator exists per ADR-0009 —
    confirm the UI shows it).
19. Mobile view pass on the nowband/board (unverified; likely cramped).
20. `tq top` vs `tq serve` feature parity check for DLQ visibility.

**CLI/UX:**
21. `tq enqueue` error when TQ_DB env is set AND cwd differs from the env
    target — confirm intent loudly.
22. `tq cancel`/`rescue` write routes: add an audit fact if not present.
23. `tq show <id>`: print the Task-Queue-ID footer placeholder resolution
    state (resolved-at-run provenance).
24. `tq facts` filtering by fact type (if missing).
25. Shell-completion generation for cmd/tq.

**Robustness / testing:**
26. Fuzz corpus growth: nightly campaign produces seeds — verify the corpus
    actually grew in the last 7 nightly runs.
27. Conformance-suite parity: confirm postgres backend suite covers every
    sqlite fixture added since ADR-0012.
28. `--once` drain semantics test under a task that requeues itself.
29. Consumer at-least-once redelivery test with a poisoned subscriber.
30. Budget projection: test the day-boundary rollover case.
31. Preflight requeue ladder: property test the backoff monotonicity.
32. Crash-reclaim: inject a lease expiry mid-claim race in the sqlite store
    test (single-writer invariant makes this tractable).
33. `check-dead-exports.sh`: switch internals to the substring matcher
    everywhere (AGENTS.md notes `rg -w` undercounts; script is advisory).

**Docs:**
34. docs/DOMAIN_LANGUAGE.md: add "tombstone", "close-out", "re-dispatch"
    vocabulary if this workflow becomes first-class.
35. RELEASE.md: dry-run the two-phase tag flow against a scratch remote once
    (untested paths rot).
36. VERSION-SURFACES.md: verify the seven surfaces list still matches the
    modules after any recent sub-module addition.
37. SECURITY.md: re-verify the auth matrix matches current middleware chain.
38. FEATURES.md: sweep for post-v0.2.0 features not yet inventoried.

**Nix/packaging:**
39. flake.nix GOEXPERIMENT=jsonv2: add a comment-guard test that fails the
    build if the export disappears (the empty-output swallow made this
    invisible once).
40. `nix run .#webui-css`: add a check that app.css is newer than theme.css
    in CI (staleness class guard).
41. templ-components version bump checklist: scripted (templ generate +
    css + adoption-table tests) instead of prose.

**Harvest/self-review pipeline:**
42. `tq audit` drift report could diff TODO_LIST text against last harvest
    and flag reworded-but-unchecked items proactively.
43. Self-review (close-out) turn: enforce the a)-g) section headers
    mechanically (the gate reads only TQ_RESULT today).
44. `— BLOCKED:` items: periodic sweep to un-block or delete stale ones.
45. Review-autofix fix tasks: track fix-task success rate (fact projection)
    to see if the loop is actually closing.

**Smaller hygiene:**
46. `scripts/check-todo-list.sh`: extend to flag items referencing closed
    status reports (like this dispatch class).
47. `check-status-index.sh`: verify it also indexes archived reports or
    explicitly excludes them.
48. examples/: G114 fixed — sweep examples for other never-tested patterns.
49. CONTRIBUTING.md: absent (not seen in repo root) — worth a stub pointing
    at AGENTS.md + ci-local.
50. Retire the repo-root `tasks.db` file entirely (retired journal still on
    disk; confusing to newcomers).

## g) Questions I cannot answer myself

1. **Who re-enqueues stale harvested items, and can the harvester see the
   journal?** If harvest ran with journal access it could dedup by task ID
   itself — is that a design constraint (harvest is stateless by intent) or
   just unimplemented?
2. **Should a no-op "already done" session emit a distinct TQ_RESULT field**
   (e.g. `"outcome":"already-done"`), and does any downstream consumer break
   on unknown fields in the JSON?
3. **Is there a policy for how many times a task ID may be dispatched?** If
   the queue has no such cap, is that acceptable dogfood noise, or should the
   worker refuse a second claim of an already-completed ID?

---

*Stale re-dispatch close-out — the work was real, shipped in 76ecf9a, and
holds green. The queue fed it back three more times anyway.*
