# Fourth window close-out — re-dispatch of the gosec self-test invocation-independence task

Task 000001a0b7ca77f155e8465b1ccb89e07967 (TODO row 132). **This is the
FOURTH window on this ID**: work shipped at bb85fcb (script fix + TODO
tick), close-out self-review at
`2026-09-19_06-02_task-000001a0b7ca77f155e8465b1ccb89e07967.md`, first
verify window at `2026-09-19_06-11_task-…​.md`, and THIS window
(dispatched ~06:10) — another re-dispatch, caught at turn 1 by the
ritual grep. Response per policy: verify → cite → stop. **Zero source
edits**; deliverables are this report, its index row, and one newly
minted backlog row (§f10).

## a) FULLY DONE

1. **The work item itself — prior window, re-verified this window, not
   redone**: `self_abs` resolved via `cd $(dirname "$0") && pwd` +
   basename BEFORE the top-level `cd` (scripts/check-gosec.sh:25); both
   recursive `gate_run` children invoke `"$self_abs"` (:63, :65); the
   foreign-CWD re-entry branch re-invokes the mode from `/` guarded by
   `TQ_GOSEC_SELFTEST_REENTRY` (:163-169). TODO row 132 is `[x]` with
   the full DONE-evidence annotation. **Immutability re-proven before
   trusting fresh runs**: `git log -1` over scripts/check-gosec.sh AND
   TODO_LIST.md both end at bb85fcb — the bytes verified are the bytes
   the work commit shipped.
2. **THIS window's fresh battery** (all stock bash, rc captured via
   redirect-to-file — zero PIPESTATUS use):
   - `--self-test` from inside `scripts/` (the original rc=127 repro
     position): **rc=0, 27 ok / 0 FAIL**, including
     `ok: --self-test re-runs from a foreign CWD`.
   - `--self-test` from the repo root: **rc=0, 27 ok**.
   - `--self-test` from `/tmp` (foreign CWD, absolute path):
     **rc=0, 27 ok** — **fresh delta vs 06-11**, which had skipped this
     position (its §b2); the gap is now closed in-transcript.
   - Real gosec gate at HEAD: **rc=0, `gosec summary: 16 scans ok,
     0 failed`** (1 root + 15 module targets). The known UNSTAMPED WARN
     is by design (provenance ruling 02-52 §g q2, still pending).
   - **TODO 144's cheap-gate bar**: check-doc-refs.sh **rc=0**
     ("doc refs ok"); root `go build ./...` **rc=0** and `go vet ./...`
     **rc=0** under `GOEXPERIMENT=jsonv2`. Second fresh delta: the
     battery initially skipped these (see §d5) and they were run during
     report prep — green, but out of order (§e2).
3. **Verify → cite → stop honored**: no source file touched; the only
   writes are this report, its index row, and the new daemon-proof
   negative-control backlog row (TODO_LIST, gosec section).
4. **Turn-1 ritual executed as written**: whole-repo `git log` /
   `git status` (clean) / `git stash list` (empty) + the task-ID grep
   over docs/status/ — surfaced row 132 `[x]` and both prior reports
   before any work was attempted.

## b) PARTIALLY DONE

- **The negative control (assertion liveness) was NOT re-run this
  window** — same reasoning as 06-11 §b1 (it requires modifying a gated
  script in-tree = daemon-food shape, and the proof stands on bytes
  unchanged since bb85fcb, §a1). What IS new: the hygiene gap now has a
  minted row (daemon-proof /tmp-extract control, §f10) instead of
  living as report prose.
- **Row 144's battery bar is met in this transcript but was met LATE**
  (cheap gates run during report prep, not at verify time). The bar is
  a minted-but-unratified row, and this window's miss is the order
  half — see §d5/§e2.
- **All three owner rulings carried from 06-02/06-11 remain
  unanswered** (re-dispatch policy; verify-window bar + report cap;
  Amend-Maneuver scope + guard-var hardening) — re-asked in §g.

## c) NOT STARTED

Nothing on the work item itself (dispatch design — it was already
DONE). Carried, untouched, all recently verified live by prior windows:

- TODO 133 (for-each-module consumer guards), 134 (short-enumeration
  pin), 135 (sibling summary lines), 136 (ci.yml sed-drift guard),
  137 (.golangci.yml excludes parity) — the gosec-family block.
- TODO 128 (session-ritual script), 144 (verify-window battery clause).
- TODO 124 + 127 (owner-blocked gosec coverage/convergence), 174
  (duplicate-claim ruling, owner-blocked), 258 (CONTRIBUTING
  gate-list sweep).
- 06-11 §f items without a row: CONTRIBUTING.md:41 baseline drift,
  foreign-CWD hermeticity (`cd /` → scratch dir), daemon-strike ledger,
  ANSI-strip sed pin, agent-shell hazards consolidation.

## d) TOTALLY FUCKED UP

*(this window's own misses; the shipped work has none to report)*

1. **Cited both prior close-outs from their INDEX ROWS without reading
   either** — 06-11 §d1 verbatim, one window later. The "read, not
   locate" lesson survived exactly one window. The cost surfaced only
   during THIS report's prep: 06-11's §f list and §g rulings, and TODO
   rows 143/144 — sitting 11-12 lines from the task row — contain the
   battery bar my initial battery skipped. Locate ≠ read, again.
2. **Reached for `mcp_qmd_get` before `view`** for a report file the
   grep had ALREADY located — and qmd in this session carries only the
   `cv` collection (per its own instructions), so the call was
   guaranteed-dead weight. Same disease as §d1 one layer down: a
   reflex to search again instead of reading what is already in hand.
3. **CONTRIBUTING.md existence-checked, not read** — miss **N+15** on
   the codified counter (06-11 recorded N+14, same ID family). The
   check found the file; the read never happened.
4. **Fourth window burned on one task ID** — work + close-out + two
   verify windows, each verify window costing a full battery + a
   ~200-line report. The queue-side done-row dedup listed as top §f
   item by ≥7 reports remains unbuilt; this window is its fourth data
   point.
5. **Battery bar met out of order**: TODO 144 requires the cheap gates
   (check-doc-refs + root build+vet, seconds each) AT VERIFY TIME; this
   window ran them during report prep after finally reading the row.
   Met late is not met — citation-order family (02-38 §d1: claims may
   only be written after the cited gate's output is in hand; the sibling
   sin is running the gate after forming the stop verdict).
6. **Nothing fucked up in the verification mechanics**: rc discipline
   held from attempt one (redirect-to-file, zero PIPESTATUS — the
   2x-hit hazard of the 06-02 window avoided), zero source edits, zero
   edit-before-read violations. Stated so §d cannot be read as padding.

## e) WHAT WE SHOULD IMPROVE

1. **The ritual must PRINT, not locate — and include the neighborhood.**
   TODO 128's script should dump: the DONE row's full text, the prior
   reports' §g/§f, and the ±10 TODO rows around the task row. This
   window proves neighbors matter: row 144, twelve lines below the task
   row, defined a battery bar the window otherwise cannot know.
2. **Run row 144's cheap gates in the FIRST battery batch,
   mechanically** — they are seconds each and make the bar
   order-correct by construction instead of by memory.
3. **Report economics**: a fourth full a-g report on one ID is
   structural spend; the cap ruling (§g2) would let a verify window
   commit §a evidence + delta misses only. Asked by ≥4 reports.
4. **CONTRIBUTING drift is structural** (row minted 06-11 §f4, still
   open): point the doc at `.golangci-baseline.txt` instead of
   hand-copying any count.
5. **Consolidate agent-shell hazards** (PIPESTATUS expands empty +
   CWD-relative `$0` masks rc=127) into one AGENTS.md subsection
   (carried from 06-11 §e5).
6. **Tool-hygiene one-liner candidate for AGENTS.md**: repo-doc lookups
   use rg/view — qmd in this environment carries only the cv
   collection, so each relapse costs one dead call (§d2).
7. **Negative-control hygiene**: minted as a row this window (§f10) —
   liveness proofs must never need to mutate a gated tree.

## f) WHAT WE SHOULD GET DONE NEXT

Honest list: 24 items, refuses 50-padding. NEW = minted this window;
otherwise carried from 06-11 §f with this window's status.

1. **Queue-side done-row dedup / re-dispatch policy** — systemic top
   item; THIS ID = 4 windows; asked by ≥7 reports.
2. **TODO 128: session-ritual script** — extended per §e1 (print row +
   prior report §g/§f + TODO neighborhood).
3. **TODO 144: verify-window cheap-gate clause** — ratify into
   AGENTS.md and make it the mechanical first battery batch (§e2).
4. **TODO 136: ci.yml gosec sed-drift guard** (self-test or ci-local
   step).
5. **TODO 137: .golangci.yml gosec.excludes parity assertion**.
6. **TODO 134: short-enumeration pin** (renamed sub-module silently
   drops from the gate today).
7. **TODO 133: for-each-module.sh consumer guards** (ci.yml's six loops
   catch neither failed nor empty enumeration).
8. **TODO 135: summary lines for sibling countable gates** (needs the
   fleet-convention ruling, 04-46 §g2).
9. **TODO 258: CONTRIBUTING gate-list sweep** (check-gosec.sh absent
   from the gates block, verified live 06-11).
10. **NEW: daemon-proof negative control** — minted this window
    (TODO_LIST, gosec section): /tmp-extract control instead of the
    in-tree mutated gated script.
11. **CONTRIBUTING.md:41 stale "~400-finding" baseline** — point at
    `.golangci-baseline.txt` (minted 06-11 §f4, still open).
12. **Foreign-CWD branch hermeticity**: `cd /` → scratch dir in the
    self-test branch (one line, 06-02 §f15).
13. **TODO 124 (BLOCKED owner): cmd/tq gosec coverage**.
14. **TODO 127 (BLOCKED owner): ci.yml gosec convergence** onto
    check-gosec.sh (then --self-test rides into CI).
15. **gitscan trailer-attribution ruling** (03-00 §g1, owner; gates the
    zero-attribution fix).
16. **Owner push of the healed lineage** (origin/master behind;
    unblocks refs/original cleanup + BLOCKED rows).
17. **Amend-Maneuver scope ruling** (06-02 §g1) — §g3a.
18. **Guard-var escape-hatch ruling** (06-02 §g2) — §g3b.
19. **AGENTS.md citation-order one-liner** (row minted 02-38 family;
    this window's §d5 is its verify-window half).
20. **Dead-SHA citation gate** (row exists; docs must not cite SHAs
    unreachable from master).
21. **Daemon-strike ledger** — ≥6 footerless sweeps across reports;
    one table in AGENTS.md instead of per-report prose.
22. **ANSI-strip `sed` pin triage** in scan() (02-38 §b deliberate
    skip; regression currently fails loud).
23. **Agent-shell hazards AGENTS.md subsection** (PIPESTATUS + `$0`
    masking; carried 06-11 §f21).
24. **UNSTAMPED gosec provenance** — owner installs a stamped v2.29.0
    binary (02-52 §g q2) so green means pin-verified.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

All three carried — each asked across multiple prior reports, none
answered, none answerable from the repo:

1. **Re-dispatch policy**: is a queue-side done-row dedup sanctioned,
   or are verify windows accepted expected spend? THIS ID has now paid
   4 windows; sibling gosec IDs paid 3-4 each. I cannot see dispatch
   behavior from inside a session.
2. **Verify-window bar + report cap, in one line**: full-fresh battery
   + full a-g report (what 06-11 and this window did), or
   changed-inputs-only battery + capped §a-evidence report (row 144's
   direction)? Every window currently guesses; the guesses disagree.
3. **Carried from 06-02 (§g1 + §g2, both still open)**: (a) is the
   Amend-Maneuver standing policy for work-file daemon sweeps (bb85fcb
   was recovered that way), and (b) is `TQ_GOSEC_SELFTEST_REENTRY`'s
   env-tamperable guard acceptable in the `CI_CHECK=off` spirit, or
   must the foreign-CWD assertion be non-env-hardened?

## Post-text — daemon strike + scripted fold (recorded per the history-rewrite policy)

§d6's "no work files for the daemon to race" was true at write time and
false by commit time: the daemon swept the report + the new TODO row
into footerless **2f91e01** in the seconds before the add (same shape
as 06-11 §d5 — the window is proven to be seconds, not minutes). The
recovery was NOT the plain amend this time, because the index-row
commit **0a21a85** had already landed on top of the daemon's, and the
scripted-rebase route died LOUD: git refused `GIT_SEQUENCE_EDITOR=/tmp/tq-todo.sh`
with "cannot exec: No such file or directory" for a file that exists,
is executable, and execs fine from the session shell — a NEW datapoint
for the mvdan→git handoff class (the codified form is a silent no-op;
this is the loud variant). Final recovery used pure plumbing —
`git commit-tree` + `git update-ref` with the message from a file:
2f91e01 + 0a21a85 folded into **69a1d0e** (tree byte-identical to
0a21a85: exactly 3 files; footer now the LAST trailer — 0a21a85 had
copied the generic template's footer-above-attribution shape, the
exact invisible-footer class that forced today's d035911/ca514a8
msg-filter heal). Fork record: `fce5454..0a21a85` (2f91e01, 0a21a85)
is abandoned; `fce5454..69a1d0e` is the lineage; both dead SHAs are
unpushed (verified: no remote branch contains 2f91e01) and docs-only
(no release tag contains them; `git tag --no-merged master` remains
clean for the next release gate).
