# Status Update — task 000001a0a2961551eaf7a81f44c078ad0262 (3rd lap)

**When:** 2026-09-15 03:15 CEST
**Scope:** Review-pipeline hardening (TODO_LIST row 79) — close-out/self-review requested after a verification-only lap.
**Delivery sha of record:** 97070c1 (code rode daemon commits cc50024/cb80a95); close-outs d3b79da + a4d75c5.

## Context: this is a THIRD paid lap on a DONE row

Two reports already exist for this exact task ID (03-10: the implementation
lap; 03-12: a verification-only 2nd lap). This session was dispatched onto
the same ID again, found everything done, re-verified, wrote a one-line
TQ_RESULT, and is now writing this report. The mint-time `[x]`-check gap
(documented in §d of the 02-30, 02-34, 02-44, 02-46, and 03-12 reports — six
times today across task IDs) remains the standing root cause. Every lap on a
DONE row is real money for zero deliverable delta.

## a) FULLY DONE

- **The work item itself (row 79, checked `[x]`):** `ReviewFinding.anchor`
  (verbatim quote, bare positions rejected at parse time — the re-anchoring
  pre-flight), `commit_sha` backfill from the review payload, fix prompts
  carrying anchor + sha + dangling-commit disposition (`git cat-file -e`
  first), and the single-footer ruling (exactly one `Task-Queue-ID` per
  commit — the fix ticket's own). Commit 97070c1.
- **Verification this lap (fresh, at HEAD):** build + vet + tests green in
  `internal/executor` (2.2s) and `internal/review`; facade parity script
  exit 0 ("7 facades mirror their internal packages"); working tree clean.
- **Test pins spot-checked this lap:** `TestReviewExecutorBackfillsFindingCommitSHA`
  (internal/executor/review_test.go:224) and the fix-prompt anchor/re-anchor
  assertions in internal/review/sweep_test.go:334-351 exist and cover the
  claimed behavior — the prior laps' citation claims hold.
- **TODO_LIST row 79** correctly marked `[x]` with an accurate DONE note.

## b) PARTIALLY DONE

- **Nothing in this task.** Adjacent, noticed in passing: the webui does not
  yet render finding anchors (a reviewer's quoted anchor is only visible in
  raw payload JSON on the detail page) — flagged by the 03-10 report §f and
  still open.
- **This report cycle:** the harness demands a per-lap report; the queue's
  mechanical gate reads only the last TQ_RESULT line — three TQ_RESULTs for
  one task means the cross-reference now records three "outcomes" for one
  deliverable. Functional, but noisy and misleading.

## c) NOT STARTED

- **Mint-time done-check** (harvest/mint refuses to mint an already-`[x]`
  item): the repeatedly-identified fix for the paid-lap disease. Still not
  implemented — it is the single highest-leverage open item this task family
  keeps rediscovering.
- A `reviews.sh` end-to-end smoke exercising the full loop (review with a
  bad bare-position anchor → rejected → re-filed with verbatim anchor → fix
  prompt carries disposition) — the parse-time pre-flight and the fix-prompt
  content are pinned by unit tests separately, but the seam between them has
  no e2e cover.

## d) TOTALLY FUCKED UP

1. **Third paid dispatch on one task ID.** The queue minted (or a human/
   scheduler re-dispatched) onto a row that was DONE and checked before this
   session started. Unacceptable economics; the fix is known and still not
   built.
2. **My own check-ordering near-miss:** I inspected `git log` and the
   TODO_LIST row before running gates (correct), but my first instinct in
   the lap was to "do the work" — had the row not already been `[x]`, I
   might have re-implemented over shipped code. The ritual (git log + status
   - TODO grep FIRST) is what saved the lap, not my planning.
3. **Systemic: reports about no-op laps are themselves work.** Each no-op
   lap costs a verify run + a report + an index row + a commit. Two of
   today's six no-op laps produced report files that exist only to say
   "nothing to do". The report-after-no-op requirement deserves a lighter
   path (see §g q1).
4. **The daemon commit-split problem persists:** the implementation commit
   97070c1 carries only CHANGELOG + TODO_LIST; the actual Go changes rode
   unattributed `chore:` daemon commits (cc50024/cb80a95). The footer
   cross-reference points at a docs-only commit, so `git log --grep
   Task-Queue-ID` under-represents the real code. Known, recurring, unfixed.

## e) WHAT WE SHOULD IMPROVE

- **Mint-time `[x]` gate (again, with feeling):** harvest should skip (or
  fast-close) any item already checked. This one change would have saved
  five of today's paid laps across task IDs.
- **Verify-only verdict class:** a no-op lap should be able to record
  `verdict: already-done` without a full report/index/commit cycle —
  `tq verdict '{"verdict":"already-done","evidence":"<gate + sha>"}'` is
  enough for the audit trail.
- **Footer-on-code problem:** the executor's footer instruction lands in the
  prompt, but daemon commits sweeping the code never carry it. Options were
  discussed in earlier reports (daemon footer passthrough / post-hoc
  attribution note); a decision is still owed.
- **Citation discipline held up** — this lap confirmed the prior laps' gate
  and test-pin claims were accurate. Keep the "claims carry citations"
  convention; it made this verification-only lap cheap and trustworthy.
- **Report scope discipline worked:** the instruction "do not research
  unrelated stuff" was honored; nothing outside row 79 + noticed-in-passing
  items appears here.

## f) Up to 50 things we should get done next

Grounded mostly in this task family + what I noticed in passing; items 1-10
are the direct tail. (Larger N is brainstorm, not commitment — most of 20+
are standing backlogs already tracked in TODO_LIST/ROADMAP.)

**Direct tail (this task/feature):**

1. **Mint-time done-check** in harvest (`[x]` items are never minted) — the six-times-today lesson.
2. **`reviews.sh` e2e smoke**: bad anchor → parse rejection → re-file with verbatim anchor → fix prompt disposition, end to end.
3. **Webui: render finding anchors** on the task detail page (quoted text + sha, not raw JSON only).
4. **Verify-only verdict class** (`already-done`) so no-op laps skip the report/index/commit ceremony.
5. **Decision + fix for footer attribution** when the daemon sweeps code into `chore:` commits (§d4).
6. **Dedup the task ID's TQ_RESULT history** in the queue view: show one deliverable with N laps, not N outcomes (display-side; journal facts stay immutable).
7. Add a negative fixture to `check-todo-list.sh`/harvest tests for re-enqueue of a checked row (pin the mint gate once built).
8. Consider surfacing "laps per task" in `tq top`/stats — a repeat-mint detector visible to the operator.

**Adjacent (noticed in passing, already tracked elsewhere):**
9. Owner unit-env fix: `Environment=GOEXPERIMENT=jsonv2` on the NixOS tq-agent-pool module (the 000001a08ebf class).
10. 2026-09-18 7-day post-deploy rate-limit checkpoint (from M124 retro).
11. Owner DLQ triage of the 8 pre-flip 429 dead letters.
12. go-cqrs-lite `queue/` module parity watch (re-open store adoption via ADR when parity lands).
13. vendoredHash runner-only drift diagnosis tooling (2026-09-10 d1 class).
14. golangci baseline regen only on owned policy change — keep; no action.
15. Session-close bridge open items: trigger automation (crush #3146), postgres parity, budget bypass.
16. Prioritize-scorer: batch keys invisible to AI scorer (documented interaction — decide if acceptable permanently).
17. Crush client/server per-repo experiment (stays OFF until the designed experiment runs).
18. `--reasoning-effort` misleading-error upstream issue to charmbracelet/crush (verified on this host 2026-09-14).
19. CI_CHECK/`ci-local.sh` master-state check: consider caching the gh call to cut lap latency.
20. Evidence-archive SHA256SUMS regen helper script (the one-liner is easy to fat-finger).

**Standing backlogs (pointer only — do not re-derive here):**
21-30. TODO_LIST.md rows beyond 79 (machine-consumed source of truth — harvest owns admission).
31-40. ROADMAP.md raw ideas (docs-health HARVEST routing rigor applies; most are ROADMAP fuel, not tasks).
41-45. docs-health HARVEST of this report's §f into TODO_LIST/ROADMAP (the loop-closing step for this very file).
46-50. Go ecosystem watch: go-cqrs-lite claiming/ extraction consumers, templ-components version bumps (rerun webui-css build script on bumps).

## g) Questions I can NOT figure out myself

1. **Re-dispatch sanction:** when a task ID is re-dispatched onto a DONE
   row, should the queue REJECT the report verdict (force cancel), accept it
   as `already-done`, or keep paying? This is an owner-economics ruling —
   the fix (mint gate) prevents mints, but not manual re-enqueues.
2. **Report-after-no-op policy:** is a full timestamped report + index row
   required for a verification-only lap, or is a `tq verdict` + one-line
   close-out acceptable? Three same-ID reports in one hour suggests the
   ceremony exceeds the value.
3. **Footer attribution ownership:** is fixing the daemon commit-split (code
   in footerless `chore:` commits, footer on the docs twin) a queue-side fix
   (post-hoc attribution), a daemon change, or accepted permanently?

---

_Point-in-time snapshot 2026-09-15 03:15 CEST. Gates cited: build/vet/test
green (internal/executor, internal/review) + facade parity exit 0, this
session, at a4d75c5._
