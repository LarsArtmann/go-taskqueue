# Close-out: Secrets-in-logs pass + `--redact` — re-dispatch lap, verification-only no-op

**Task:** 000001a0a7237f14896a336463023ce6a28c (TODO_LIST row 86, "Docs-health harvest" evening batch — minted from the 09-11 reports)
**Session:** 2026-09-16 00:50–00:55
**Landing commit (work):** c7ca518 (auto-daemon commit, 6 files, NO Task-Queue-ID footer — the documented daemon-commit attribution gap; unpushed like all of the 15 local commits ahead of origin/master)
**Landing commit (this report):** carries the task footer, restoring the queue↔git cross-reference at the report level
**Verdict:** DONE-on-arrival — the item shipped 2026-09-16 ~00:28–00:48 by a concurrent session; this lap verified every claim fresh and added zero code. No-op lap per the turn-1 ritual (repeat dispatch → verify → cite → stop).

The work item (from the 17-21 report #18 / 20-58 f33 thread): agent output tails
could carry provider tokens into evidence facts, worker logs, and both sidecar
writers. The shipped answer: a redaction pass over `tailBytes` (default ON,
`--redact=false` / `$TQ_REDACT=false` per-pool opt-out) plus an audit half
(`tq audit --journal` SECRET EVIDENCE scan) so pre-pass facts are findable
without ever printing the secret.

---

## a) FULLY DONE

All citations re-verified fresh at HEAD a21223c during THIS lap (00:50–00:55).

1. **Redaction core** (`internal/executor/redact.go`): `RedactSecrets` (redact.go:61,
   pure), `RedactMarker = "[REDACTED]"` (redact.go:11), `secretPatterns`
   (redact.go:20 — sk-ant-16+, `(?:AKIA|ASIA)[A-Z0-9]{16}`, plus the OpenAI/AIza/
   xoxb/bearer/auth-header/secret-assignment shapes per AGENTS.md), env-gated
   `redactActive` (redact.go:72, default ON) → `redactOutput`/`redactBytes`.
2. **Choke point**: `tailBytes` redacts BEFORE the tail is cut; wired into
   `internal/executor/command.go`, `agent.go`, and `ratelimit.go` — a new
   output→evidence site needs no extra work (the design property the AGENTS.md
   contract promises).
3. **Sidecars**: both writers redact (`TestWriteOutputSidecarRedacts`,
   `TestWriteVerifyEvidenceRedacts`); agentpool `--log-dir` help text warns
   sidecars stay plaintext for paths/prompt content (agentpool.go:256).
4. **Flags**: `--redact` on `tq worker` (cmd/tq/main.go:480) and `tq agent-pool`
   (cmd/tq/agentpool.go:268), both mirroring into `$TQ_REDACT` — per-POOL escape
   hatch exactly as documented.
5. **Audit half**: `tq audit --journal` prints SECRET EVIDENCE rows
   (cmd/tq/journalaudit.go:377) via `executor.SecretHits`; enqueue payloads are
   never scanned (user-provided) — matches the AGENTS.md contract verbatim.
6. **Test pin** (`internal/executor/redact_test.go`, 8 tests): masks
   provider tokens, keeps benign output, counts matches, tailBytes redacts,
   honors `TQ_REDACT=false`, failure-evidence tail, both sidecar writers. All
   8 PASS this lap (verbose run).
7. **TODO_LIST.md:86** already `[x]` with a full shipped-citation — the close-out
   loop was completed by the shipping session; nothing to update.
8. **Gates this lap**: executor module `go build`/`go vet`/FULL suite
   `-count=1` ok (30.1s); root module build/vet/full tests ok; cmd/tq devmod
   shim gate ok (11.8s, run during this close-out lap); tree clean.

## b) PARTIALLY DONE

1. **Runtime (not just unit) evidence for the audit scan**: `TestSecretHitsCountsMatches`
   pins the scanner; the `tq audit --journal` CLI path over a seeded fixture has
   the advisory `scripts/smoke/journal-drift.sh` smoke, but no smoke asserts a
   redacted tail end-to-end (spawn stub agent whose output contains a fake
   `sk-ant-…` token → claim the task → assert the stored fact tail is
   `[REDACTED]` and SECRET EVIDENCE reports 0 hits post-pass). Not done this lap
   (out of no-op scope) — listed §f1.
2. **Footer attribution on the shipping commit**: the queue↔git cross-reference
   for this task ID resolves only via this report commit; c7ca518 carries no
   footer and 14 commits now sit on top of it (amend = rebase of live local
   history under a running daemon — refused). Healing path is §f2/§g3, not
   history surgery.

## c) NOT STARTED (this lap; by design — no-op discipline)

1. Nothing. The deliverable was complete on arrival; the only defensible work
   was verification + this report. Zero source/doc changes made.

## d) TOTALLY FUCKED UP (disclosures)

1. **Wrong verify gate, first try** (lap turn 3): ran `go test ./internal/executor`
   from the ROOT module — the exact multi-module trap AGENTS.md documents as
   failing BY DESIGN ("`./...` never descends into nested modules"). The error
   message even says it plainly ("main module … does not contain package").
   Cost: one round trip; recovered by cd-ing into the module. Documented trap,
   still caught me — the session-start ritual did not include a per-module gate
   lookup before the first test run.
2. **cmd/tq gate omitted from lap-1 verification**: the shipping commit touched
   `cmd/tq/main.go` + `agentpool.go`, but my first verification pass covered
   only root + internal/executor. cmd/tq is its OWN replace-free module
   (ADR-0017) — the root gate says nothing about it. Caught during report
   writing; shim gate then run green (11.8s). The gap existed for ~10 minutes
   in an unpushed no-op lap, but "verified green" was overclaimed for one
   message window.
3. **Lap cost itself**: this is at least the sixth paid re-dispatch lap on an
   already-`[x]` row in the last two days (04-40, 04-14, 03-57, 03-45, 03-31
   precedents). The queue minted work the TODO file had already closed. My
   lap was as cheap as a no-op can be (verify → cite → stop, no re-implementation),
   but the money is still spent. Root cause is mint-side (§f2), not dispatch-side.

## e) WHAT WE SHOULD IMPROVE

1. **Verification must enumerate the modules the COMMIT touched, not the ones
   the task names**: a diff-file → module mapping (`git show --stat` paths →
   owning go.mod) before declaring gates green would have caught the cmd/tq
   omission mechanically. A five-line `scripts/verify-touched-modules.sh`
   helper closes this class.
2. **Turn-1 ritual works — keep it first, always**: the `rg -l <task-id>` +
   TODO-row grep before ANY gate run is what made this lap cheap. The 04-12
   report's lesson ("TODO grep ran after first verify") was honored here and
   the lap stayed a no-op instead of becoming a re-implementation.
3. **No-op laps should end with the missing runtime evidence named, not
   implied**: §b1's end-to-end redaction smoke is now a concrete §f1 item
   instead of a vague "tests exist" claim — cite what was NOT exercised.
4. **Daemon-commit footer gap keeps compounding** (§b2): every shipped item
   whose code rode a daemon commit leaves the cross-reference resting on the
   report commit alone. A sweep tool (§f2) beats per-lap disclosure.

## f) WHAT WE SHOULD GET DONE NEXT (real items only, no padding)

1. **Redaction e2e smoke** (`scripts/smoke/redaction.sh`): stub agent emitting a
   fake `sk-ant-…`/`AKIA…` token on stdout+stderr; assert the stored
   `task.failed/completed` evidence tail is `[REDACTED]`, sidecars redact, and
   `tq audit --journal` reports the pre-pass fixture + zero post-pass hits.
2. **Daemon-commit attribution sweep**: a `tq doctor`-style audit mapping
   daemon commits (heuristic subjects, no footer) to task IDs via their
   `docs/status/*_task-*.md` report files, reporting unattributed shipping
   commits — heals the §b2 class without history rewrites.
3. **Mint-time done-check (standing §f #1, re-asked)**: before minting a task
   from a TODO row, re-read the row and skip/refund if already `[x]` with an
   indexed close-out — the root cause of §d3.
4. **Verify-touched-modules helper** (§e1): map `git show --stat` paths to
   owning modules, run each module's gate — make "which gates must be green"
   diff-derived, not memory-derived.
5. **Add the audit scan to `journal-drift.sh` expectations**: the drift smoke
   currently asserts drift rows only; a zero-SECRET-EVIDENCE assertion makes
   the redaction-vs-audit pair regression-checked in CI.
6. **`--redact=false` help-text symmetry check**: worker (main.go:480) and
   agent-pool (agentpool.go:268) descriptions are near-identical — assert
   parity in `scripts/smoke/help-text.sh`'s rename-leak pass (cheap).
7. **Secret-pattern corpus growth policy**: document in AGENTS.md HOW a new
   token shape gets added (pattern + test + baseline note), so the next
   provider onboarded to a pool repo doesn't silently bypass the pass (see §g2).
8. **secretPatterns vs `SecretHits` divergence guard**: the audit scanner and
   the redaction pass must share the same pattern table — add a test asserting
   both compile the identical slice (currently one `var`, but pin it before
   someone forks it).
9. **Windows-lap for redaction**: the pass is path-independent, but
   `TestWriteOutputSidecarRedacts` writes files — confirm the suite is in the
   windows-latest CI matrix set (POSIX-only suites carry `//go:build unix`;
   redact_test.go carries no such tag — verify it actually runs on Windows CI).
10. **`TQ_REDACT=false` smoke variant**: one ci-local-cheap run proving the
    escape hatch still produces raw output (currently only unit-pinned at
    `TestTailBytesHonorsTQRedactFalse`).
11. **Evidence-tail redaction for NEW fact types**: `auditSecretScans` covers
    failed/dead-lettered/completed/requeued — if a future fact type carries
    output-derived detail, the audit type list needs updating; add a comment
    cross-link at the tailBytes choke point so the pairing is discoverable.
12. **Frontier: `docs/status/` index bloat**: 14+ live rows added in two days;
    the INDEX BLOAT WARNING threshold is approaching again — schedule the
    archive sweep (docs-health ANNOTATE) before ci-local starts warning.
13. **TODO_LIST.md:157** (security hygiene batch) is now the natural next row:
    SECURITY.md per-surface matrix + `redactedRequestURI` pin test + bearer
    dedup — adjacent surface, same threat model as this item.
14. **Retire the legacy stdout `TQ_RESULT:` fallback when derived outcomes
    saturate**: AGENTS.md sets the condition (live pool shows derived
    outcomes); a weekly `tq audit` query counting verdict-file-vs-stdout
    results would give the removal a citation-ready number.
15. **Report-file footer inheritance**: a queue tool that rewrites `commit_sha`
    fields in TQ_RESULT lines into a `docs/status/index` side-table would let
    §f2's sweep resolve attribution WITHOUT parsing daemon subjects.
16. **Session-close bridge trigger**: this task's repeat dispatch is exactly
    the `session.opened/closed` bridge use case (crush #3146 open item) —
    prototype remains untested against the live pool.

## g) QUESTIONS I CANNOT FIGURE OUT MYSELF

1. **Re-dispatch sanction (standing, now with a 6th data point)**: should the
   minter refuse/halve-cost dispatches on rows already `[x]` with an indexed
   close-out (mint-time done-check), or is the paid re-verification lap
   considered worth its cost as an independent audit? I can build either; I
   cannot decide the policy.
2. **Redaction pattern coverage for the live fleet**: the pool runs GLM-5.3-Flash
   via Z.ai — do Z.ai/OpenRouter-style keys have a distinctive shape worth a
   `secretPatterns` entry, or does the generic `bearer`/auth-header rule cover
   them? I cannot learn the token shape from this repo; only you know what the
   fleet's credentials look like.
3. **Attribution-gap healing policy**: for shipping work that rode unfootered
   daemon commits (c7ca518 here; cc50024/cb80a95 precedent), is the §f2 doctor
   sweep the desired fix, or is footer-on-report-commit the accepted terminal
   state (§f15)? The answer decides whether I build the sweep or drop the item.
