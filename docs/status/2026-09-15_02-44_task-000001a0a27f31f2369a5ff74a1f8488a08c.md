# Task 000001a0a27f31f2369a5ff74a1f8488a08c — orphaned-guard audit (docs-health harvest row)

**Date:** 2026-09-15 02:44
**Verdict:** NO-OP DUPLICATE — the work item was already fully shipped before this session started.
**Commit (this report):** see `git log` with this Task-Queue-ID footer.

## What the task asked

TODO_LIST row: "Orphaned-guard audit: every `scripts/check-*.sh` + smoke must be referenced by
ci-local/ci.yml/flake or be deleted — generalize the check-webui-css lesson into a ci-local
meta-check (15-39 report c7/f5, e1)".

## What was found on arrival

- `scripts/check-guard-wiring.sh` already exists and passes: "guard wiring ok: every check-*/smoke
  script is referenced" (exit 0, verified this session).
- Wired into `scripts/ci-local.sh:214`.
- Dead duplicate `check-lint-baseline.sh` already deleted; orphaned smokes
  (multi-repo, papdashboard-e2e, ratelimit-e2e) already wired into ci-local.
- Shipped by commits 28ffe7e ("docs: record orphaned-guard audit gate shipped in ci-local (f50)")
  and a02f72f (status report f50).
- TODO_LIST.md:77 was already marked `[x] — DONE 2026-09-15` with matching evidence.

## a) FULLY DONE

- Verification that the item was already complete: gate run green, wiring located at
  ci-local.sh:214, TODO row confirmed `[x]`, completion commits identified (a02f72f / 28ffe7e).
- Correct no-op decision: no re-implementation, no drive-by changes, no duplicate commit.
- This close-out report.

## b) PARTIALLY DONE

- Nothing. The item was binary (gate shipped or not) and it is shipped.

## c) NOT STARTED

- Nothing attributable to this task. Note in passing: the gate covers ci-local references, and
  AGENTS.md describes it as covering "ci-local/ci.yml/flake" — I verified the ci-local wiring
  directly but did NOT independently verify ci.yml or flake.nix references this session (the
  gate itself is the authority and it passed; residual risk is a gate bug, not a wiring gap).

## d) TOTALLY FUCKED UP

- The queue itself: this work item was enqueued AFTER its own completion (the f50 window shipped
  it and left the TODO row checked, yet a live queue item for the same work existed). This is a
  mint-side dedup failure, not an agent failure — but it burned one whole session slot on a
  no-op. The harvest/dedup path that produced this item should suppress items whose TODO row is
  already `[x]`.

## e) WHAT WE SHOULD IMPROVE

1. Harvester should skip/flag TODO items already `[x]` (this no-op is exactly that class).
2. Consider `tq audit` catching "task dedup key text matches a DONE row" as a warning class.
3. The gate's own self-test coverage: does check-guard-wiring.sh have a negative fixture
   (an unreferenced script → exit non-zero)? Worth confirming; a gate that cannot fail is a
   false-green risk (same lesson as the gosec Files:0 incident, 2026-09-13).

## f) NEXT (candidate items, small and bounded)

1. Confirm check-guard-wiring.sh has a negative-path test; add one if missing.
2. Wire the gate into ci.yml too if it currently only lives in ci-local (defense in depth).
3. Harvester: skip `[x]` items explicitly with a log line (visibility over silence).
4. `tq audit --journal`: report DONE-row/task-id mismatches found here.
5. No other work surfaced from this session — it was a two-command session by design.

(Deliberately capped at 5: this session generated no new findings beyond the duplicate-enqueue
class; padding to 50 would be fabricated scope.)

## g) QUESTIONS I CANNOT ANSWER MYSELF

1. Was this queue item minted before or after the f50 completion commits? (Determines whether the
   harvester needs a race fix or just a `[x]`-skip fix.)
2. Should no-op duplicate tasks be closed as "done" (this report's choice) or dismissed
   (`wontfix`-style) so queue metrics don't count shipped work twice?
3. Is there a desired owner policy for gates that exist only in ci-local but not CI (item f2)?

## Contract compliance

- Verify gate: `./scripts/check-guard-wiring.sh` → exit 0.
- TODO_LIST: row already `[x]` — left as-is, no edit needed.
- No changes to .crushrc / crush.json / .tq-verify.
- No push.
